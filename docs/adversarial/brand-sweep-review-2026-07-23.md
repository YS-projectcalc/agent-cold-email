# Adversarial review — ColdStart→Coldrig brand sweep (2026-07-23)

**Reviewer:** adversary (fresh context) · **Target:** branch `worktree-brand-sweep-20260723` @ `ec73d82` · worktree `/Users/yaakovscher/dev/coldstart/.claude/worktrees/brand-sweep-20260723` · **Diff base:** merge-base `62e3fc6` (note: `main` has since advanced to `43a311c` — sibling activity; the branch's own delta is unaffected).

## VERDICT: SHIP — 0 blocking findings

The two customer-visible RUNTIME surfaces a paying/free customer actually sees (Stripe checkout product name, free-sandbox demo inbox addresses) are correctly branded Coldrig. All other customer-visible surfaces (marketing site, dashboard SPA, CLI, ops/dunning emails, OpenAPI info block, MCP server metadata, README/llms.txt/agent briefs) were already Coldrig before this branch — this branch is the correct tail cleanup. The dangerous "(test mode)" direction (a live customer seeing a test-mode label) is fully impossible. Battery re-run clean; tripwire proven RED on reintroduction.

## Verification performed (evidence)
- **Full battery re-run in the worktree:** platform `tsc --noEmit -p tsconfig.json` → **exit 0**; platform `vitest run` → **601 passed / 85 files**; targeted brand suites (`brand-copy-guard`, `stripe-client-checkout`, `demo-seed`) → **20 passed**. Matches builder's claim. (dashboard/engine/CLI workspaces untouched by the diff — no re-run needed.)
- **Tripwire RED/GREEN proven** in an isolated sandbox (`/private/tmp/.../scratchpad/red-proof`) with the same `?raw` mechanism + main-repo vitest 4.1.10: clean source GREENs, doctored source containing `ColdStart` **FAILS** `not.toContain` as designed. `?raw` reads current file text (proven because the GREEN worktree run read the live Coldrig text and found no `ColdStart`). Mechanism is not tautological.
- **Independent inventory sweep:** `git grep -i coldstart` = 221 files; bucketed and classified every non-excluded hit (see Attacks-that-failed). Zero customer-visible product-surface `ColdStart` remains.
- **Env-var / caller trace:** `createStripeCheckoutSession` is called only from `apps/platform/src/engine/billing.ts:49-52` passing `ctx.env.STRIPE_SECRET_KEY` (the right var; `STRIPE_WEBHOOK_SECRET` is separate, `env.ts:21`, never passed here).

## Findings (all NON-BLOCKING)

1. **NON-BLOCKING · lens 3 (live-surface) / inverse-class · AEO measurement panel still names "ColdStart" as the brand.** `tools/aeo-panel/README.md:5,21,59`, `tools/aeo-panel/panel.v1.json:14`, `tools/aeo-panel/runs/TREND.md:7`. Going forward this internal share-of-voice instrument measures whether AI engines mention the *retired* term, so future runs mis-measure; `tools/buyer-panel` was already updated to "ColdRig", so the two panels now disagree. Internal tooling, never shipped to customers → not a ship-blocker. Recommend a follow-up docs/tooling pass.

2. **NON-BLOCKING · lens 4 (deploy plumbing) / inverse-class · engine infra strings still say "ColdStart".** `apps/engine/package.json:6` description "ColdStart external email engine", `apps/engine/Dockerfile:1` comment, `apps/engine/README.md` docker image name `coldstart-engine`. Owner-operated off-Worker infra (private workspace, not published, not a customer product surface). Correctly out of the customer-visible DoD.

3. **NON-BLOCKING · lens 8 (identifier surface) · `X-Coldstart-Client` CSRF header carries the old name in the docs.** Documented in `site/openapi.yaml:1132`, sent by `apps/dashboard/src/api/client.ts:22`, **validated server-side** at `apps/platform/src/csrf-guard.ts:5` (`CSRF_HEADER = "X-Coldstart-Client"`). A buyer-agent reading the OpenAPI sees the old-brand token in a required header name. RULED excluded by the brief — **exclusion confirmed sound:** renaming is a breaking protocol change requiring atomic server+client+docs coordination and would 403 in-flight cookie sessions. Defer to a coordinated header-rename cut if brand purity matters.

4. **NON-BLOCKING · lens 3 (tripwire strength) · guard scans only 2 files.** `apps/platform/test/brand-copy-guard.test.ts` enumerates exactly `stripe-client.ts` + `demo-seed.ts`; the marketing site HTML, dashboard SPA, OpenAPI, and CLI are unguarded. Judged **acceptable, not false safety:** those surfaces are already clean and re-introduction of a *retired* name into them is low-probability, while the 2 guarded files are the programmatic string-builders most likely to be regenerated/copied. Optional strengthening: a repo-wide CI grep over the customer-visible surface set (site/*.html, dashboard src, openapi descriptions) excluding the known-functional `X-Coldstart-Client` header + `@coldstart/*` scope.

5. **NON-BLOCKING · lens 2 (edge cases) / within tolerance · restricted-key renders no test-mode label.** `stripe-client.ts:97` `secretKey.startsWith("sk_test_")` → a restricted test key `rk_test_...` yields `false` → "Coldrig Launch" with **no** "(test mode)" suffix during test mode. This is the BENIGN direction (a test-mode checkout merely lacking its test-mode label, seen only by internal QA); the DANGEROUS direction is fully protected — `sk_live_` → no suffix → "Coldrig Launch" (correct). Empty/absent key: caller gates on `STRIPE_SECRET_KEY` set (`billing.ts:38,49`), so never invoked empty on the real path; if it were, `""` → no suffix → live brand (harmless; Stripe 401s anyway). `whsec_` webhook secret is a different env var, never threaded here. Documented activation path uses standard `sk_test_`/`sk_live_` keys.

## Attacks that failed (why the PASS is meaningful)
- **Inventory completeness (the main event):** hunted every customer-visible surface class from the brief. Marketing site = **0** `ColdStart` hits, already fully Coldrig (49 files, `index.html` title/og/JSON-LD all "Coldrig"). Dashboard SPA = 0 brand `ColdStart` (all `coldstart` hits are `@coldstart/shared` imports or the CSRF header; centralized `BRAND_NAME="Coldrig"` at `apps/dashboard/src/lib/brand.ts:3`, rendered in auth/signup/settings/logo). CLI (`packages/cli`, workspace `agent-cold-email`) prints only "coldrig". Ops/dunning email identity already `ops@coldrig.dev` / "coldrig ops" (`ops-mailer.ts:19-20`). MCP metadata (`server.json`, `.mcp.json`, `glama.json`, `.claude-plugin/plugin.json`) = 0 hits. OpenAPI info block = "agent-cold-email API", no ColdStart. README/llms.txt/for-agents/agent-evaluation/docs = 0 hits. → the 2 IN-class items are the correct residual.
- **Sent-email brand injection (highest-value cold-email surface):** the platform hardcodes no `ColdStart` footer/from-name/signature — the only `apps/platform/src` non-excluded hit is the ruled `unsubscribe-token.ts:25` label. The `ColdStart, 123 Main St` in `apps/engine/test/message.test.ts:19` is caller-supplied fixture body, not platform-injected.
- **OUT-of-class spot-check (5+ exclusions):** every root-doc `ColdStart` is an internal engineering/owner doc H1 title (SPEC/ARCHITECTURE/ACTIVATION/FINAL-REPORT/CLAUDE) or a functional identifier (`coldstart-monorepo` pkg name, `coldstart-engine` image, `@coldstart/*` scope, `X-Coldstart-Client`) or the handoff doc describing the sweep — none a customer product surface. (`SPEC.md:60` `npx coldstart` is a stale internal-doc example; the real install docs `llms-install.md`/README are clean — noted, not blocking.)
- **Stale demo domain:** `git grep demo-leads.coldstart` = **0** repo-wide → no screenshot baseline / doc / fixture now mismatches the `demo-leads.coldrig.dev` change; `pw-scripts/inbox-shots.js` updated in-lockstep (in the diff).
- **Signature-string safety (ruled item):** the diff (7 files) modifies **no** HMAC/signature/derivation label; `unsubscribe-token.ts` KEY_DERIVATION_LABEL is not in the diff → already-issued unsubscribe links stay valid. Confirmed no other stateless-signature string was touched.
- **Suffix derivation:** live-key deploy renders exactly "Coldrig Launch" (verified by the new `stripe-client-checkout.test.ts` sk_live_ case + independent trace); reads the correct env var.

## UNVERIFIABLE
- **og-image PNG pixels** (`site/assets/og-image.png`): cannot cheaply OCR whether the retired name is baked into the image raster. Not in this diff (assets untouched) and the asset is Coldrig-era; would require visual inspection to fully clear. Low risk.
- **Live production checkout page** with a real `sk_live_` key: no live Stripe call permitted (hard rule) — validated via the stubbed-fetch unit test + source trace instead of a live render.

## NEW (out-of-scope) observations
- The public GitHub repo's engineering docs (SPEC/ARCHITECTURE/ACTIVATION/FINAL-REPORT/CLAUDE, HANDOFF) still carry "ColdStart" in their H1 titles and `SPEC.md:60` shows a non-working `npx coldstart` example. These are living canonical docs (not the customer-visible product surface this lane scoped) — worth a separate docs-consistency pass, outside this DoD.
