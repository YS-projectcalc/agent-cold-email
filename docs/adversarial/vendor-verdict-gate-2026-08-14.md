# Vendor-verdict fix — fresh-context adversary gate — 2026-08-14

**Verdict: SHIP-WITH-NOTES.** Branch `fix/vendor-verdict-class-2026-08-14`, HEAD `a776b29`, base `d112337`. Fresh-context gate; all execution in throwaway clones (live checkout untouched, verified clean); probe files removed. No BLOCKING finding survived self-refutation. The confirmed live defect (terminal vendor registration → eternal 202 success-pending) is genuinely closed, proven through the REAL `RealInboxKitDomainPort` driving the REAL `runSetupInfrastructure`. Two SHOULD-FIX residuals, both in the SECOND guard (the age bound and its escalation), not the type-level fix.

**Disposition (main loop, same day):** both SHOULD-FIXes + three in-class riders (terminal-token addition from a live observation, the `setDns:nameservers` discarded-response check, comment corrections) folded into this cut on the same branch; delta re-gate by the same gate context before merge/deploy. Finding-2 fix ruled as option 1 (visibility-query disjunct) — NOT option 3 (anchor backfill), which would make an already-stuck-but-recoverable domain (Mordy's exact shape: P0 orphan blocked setup from ever running) instantly trip the bound on its first post-fix retry.

## Independent battery (clean clone)
- `npx vitest run --no-file-parallelism` → Test Files 1 failed | 172 passed (173) · Tests 1 failed | 1651 passed (1652) · exit 1 — the 1 failure (`site-tool-count-claims`) reproduced at base `d112337` (1 failed | 102 passed there), PRE-EXISTING VERIFIED.
- typecheck (all 5 workspaces) → exit 0. `wrangler deploy --dry-run` → exit 0; binding manifest contains NO `PROVISIONING_RECONCILE_ENABLED`.
- Builder's RED claim at `0270f5f` VERIFIED EXACTLY: 33 failed | 4 passed (37).
- MUTATION M1 (terminal arm reverted to the pre-fix collapse) → KILLED: 10 failed | 1642 passed (1652). MUTATION M2 (age bound disabled) → KILLED: 4 failed | 1648 passed (1652). Totals intact both times.

## FINDING 1 · SHOULD-FIX · The 6h bound has no `connected` exemption — a customer-OWNED delegated domain is declared permanently dead at hour 6
The bound (`domain-dns.ts:391-397`) conjuncts on `{notPropagated}` only; the success-pending branch 41 lines below (`:433`) additionally requires `connectionType === "purchased"`, with a retained comment stating connected propagation "is the customer's registrar's job and can genuinely stall." The bound's own docstring justifies 6h from the PURCHASED case (~32s vendor registration, two orders of magnitude headroom) — that argument does not transfer to connected NS delegation (routinely 24–48h). EXECUTED at hour 6+: a connected domain flips from pre-fix retryable-and-eventually-successful to non-retryable "this domain needs to be replaced" with `dns_gave_up_at` durably stamped; every later call short-circuits on the marker; the dark reconcile's new predicate would also permanently exclude it once armed. Fail direction is safe (never bills a mailbox) and a give-up clears if a later poll answers ready — but the agent is told to stop calling, so nothing re-drives it. REACHABILITY: `setDnsWithRetry` rows are `source='provisioned'`; `connected` reaches them via the ADOPT path (`provisioning.ts:299-309`) and the legacy `resolveDomainConnectionType` backfill — the adopt/legacy population, not the mainline. FIX SHAPE (gate-preferred, adopted): add `connectionType === "purchased"` to the bound's conjunct set, matching the neighbouring branch.

## FINDING 2 · SHOULD-FIX · The aging escalation cannot see any domain stalled BEFORE this deploy — the exact population the wave exists for
The query (`ops-summary.ts:356-362`): both disjuncts require a column only the NEW code writes (`dns_gave_up_at` solely by `failTerminal`; `dns_first_checked_at` for provisioned rows solely by `recordDnsObservation` inside `setDnsWithRetry` — pre-wave it was BYO-only, and the provisioning INSERT never sets it). Every pre-deploy provisioned row has BOTH NULL and matches NEITHER disjunct. The `:82-84` comment claims "this fires either way" — right about `dns_gave_up_at`, wrong about the anchor (identical dependency), false for precisely the named hard case ("a tenant whose agent has stopped retrying"). EXECUTED with control: a seeded pre-wave-shaped stalled row (3 days old, both columns NULL) → `agingPendingDomains = []`, zero checks; identical row with anchor 6h+60s old → exactly 1 `domain_dns_aging:` check. MITIGATION keeping it non-blocking: `send_starved` still trips for tenants with queued sends. Gate's fix options: (1) `OR (dns_first_checked_at IS NULL AND purchased_at <= ?)` — clock-skew fails SAFE for an is-it-old test (a future `purchased_at` simply doesn't fire), usable here though rightly rejected as the BOUND's anchor; (2) `OR dns_first_checked_at IS NULL` — same-tick noise; (3) one-shot backfill — REJECTED by main loop (see Disposition). Adopted: option 1 + comment correction.

## NOTES (non-blocking)
1. **`vendorState` reaches a customer surface, contradicting two docstrings.** `failTerminal` passes it via `customerSafeDetail`'s `...extra` → `deliverability_actions.detail_json` → `reporting.ts:152 recentActions` → the `account` MCP tool. Same for `MAILBOX_PURCHASE_TERMINAL`/`MAILBOX_PROVISION_TERMINAL`. Contradicts `domain-dns.ts:233-236` and the `VendorReadiness` docstring ("never a customer surface"). Payload is a normalized lifecycle token ("expired"/"suspended") — no provider/endpoint named; leak guardrail not breached. Disposition: keep the (useful, actionable) emission; correct both docstrings.
2. **Attack-8 inventory: all 16 `client.request` sites swept for `200 {error:true}`.** Checked-and-safe: provision, startWarmup, cancelWarmup, warmupSubscriptionState, mailbox release, listDomainRecords, domain buy, domain release, findExactMailbox (this wave's fix). Fail-closed: getHealth (refutes the burn→replace→buy money hypothesis — the throw fires first), showMailboxCredentials. Unchecked, live, not spend-adjacent: `searchLookalikes` (fails toward NOT buying, visible message); **`setDns:nameservers` (`inboxkit-domain-port.ts:287`) DISCARDS its response** — a `200 {error:true}` means nameservers never set → not-propagated forever → under the new bound, a "needs to be replaced" verdict at 6h instead of endless retry. Disposition: add the body.error check (in-class, in-file, visible-immediately). `oauth-mint.ts:96` fails loud at step 2.
3. **The `REPLACE_DOMAIN_TERMINAL` counting rationale is not reproducible.** `deliverability.ts:177` skips non-active rows and `applyReplaceDomain` flips the burning domain to `'burning'` unconditionally BEFORE the attempt; nothing returns a domain to `'active'` — so the claimed buys-forever loop cannot occur. The accounting itself is harmless and mildly protective; `ga-gates-ng5-2` semantics preserved (isolation test green). Disposition: correct the comment.
4. **Guard C does not give `DOMAIN_DNS_PENDING` "its first reader"** — nothing reads that action row; the escalation is a new query over `domains`. Requirement met; description inaccurate (sweep doc guard C + ops-summary docstring). Also: the D3 fake-typechecks test is a runtime tautology — the real gate for fakes is tsc (clean). Disposition: correct the ops-summary docstring; the frozen sweep doc stays frozen, corrected here.

## Attacks that FAILED (16, all executed)
1. Five terminal tokens × 10 consecutive real-port `runSetupInfrastructure` calls: all 50 outcomes non-retryable errors, never 202; exactly 1 `/domains/register` per token, 0 mailbox calls. 2. Never-listed async-failed registration: 5 in-bound calls → 202 each; anchor aged past 6h → 5 non-retryable; still 1 purchase, 0 mailboxes. 3. Recovery at hour 7 on the DIRECT path: post-give-up, vendor flips active → next call succeeds, `dns_status='ready'`, `dns_gave_up_at` CLEARED, 2 mailboxes, still 1 purchase total. 4. Mailbox money asymmetry: `200 {error:true}` and unknown-token runs → `{"buys":1,"billable":0,"lastRetryable":true,"isError":true}` exact match; only `absent` reaches dispatchBuy, still capped. 5. Nothing not-ready→ready promoted (mailbox mapping strictly stricter: unknown token was pending→present/ADOPTED before, now inconclusive→unconfirmed). 6. `inconclusive` creates no new silent stall (domain: bounded-benign, terminal at 6h with alert; mailbox: unconfirmed → founder alert). 7. Deviation (a): burning/paused_primary/retired still cannot reach success-pending (`bound.status === "active"`, same normalization). 8. Deviation (b): `DomainDnsTerminalError` sole discriminating consumer is in-process `instanceof` (no DO/RPC boundary); sibling-subclass narrowing means terminal structurally cannot produce the 202; `namedErrorClass` returns undefined for "VendorError"; HTTP surface maps to non-retryable `vendor_error` 502. 9. Watchtower driven: young silent / aged fires naming the domain / recovered clears / cross-tenant ownership guard holds / unactivated silent / no permanent stuck alert possible. 10. Dark stays dark (reconcile diff = comment + one conjunct; one prod caller behind the flag; dry-run manifest flag-free). 11. Schema safe (nullable, `addColumnIfMissing`, no enum change, no D1 domains table). 12. Clock coherence: `dns_gave_up_at` IS in `clock-migration.ts`'s shift set (`:248`) beside the anchor (`:242`). 13. No BYO/provisioned anchor crossover (all readers source-scoped both directions). 14. A vendor-API outage cannot age a domain into a false give-up (transient throw sets `notPropagated:false` → neither stamps the anchor nor trips the bound; passthrough classes return before bookkeeping). 15. Test honesty: every changed assertion in all 13 touched test files diffed against base — mechanical migration, assertions ADDED not weakened, no skips. 16. Spec-vs-code: all 15 inventory members traced; all six §5 scope rulings honoured; 5 port implementations under the D3 contract tests; dashboard has no display-side member.

## UNVERIFIABLE
1. **`scheduled_for_deletion` grading.** Executed: grades `inconclusive` (bounded-benign — safe, never ready, never a re-buy, terminal at 6h) but semantically the token means terminal, so that domain fails slow-and-mislabeled. Resolution per `vendor-lifecycle.ts`'s own rule ("a live observation, not a plausible synonym"): a live observation EXISTS — today's cron capture shows `dmhadvisor.com` with `status:"scheduled_for_deletion"` in the real workspace. Disposition: add the token to `TERMINAL_TOKENS`, citing the capture.
2. Whether Mordy's tenant sits in fake-pending, and whether his rows are 'purchased' or 'connected' — needs the founder-held ADMIN_TOKEN. Finding 2 bites ANY already-stalled row regardless of type.
3. No live-surface drive (prohibited by brief) — resolved by the post-deploy smoke.

## Recommendation
Ship; fold both SHOULD-FIXes into this cut (finding 2 means the wave's own motivating customer may still be silent after deploy). Delta re-gate scope agreed: finding 1 (connected exempted, control: purchased still gives up at 6h), finding 2 (NULL-anchor pre-deploy row surfaces, control: no false alert on a healthy young row), regression ring on adjacent behavior, NEW findings reported separately.

*Delta re-gate verdict: appended below when complete.*
