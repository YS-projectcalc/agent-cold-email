# Engine-host arc — Adversarial review (frozen record, 2026-07-14)

Fresh-context adversary, two rounds, against the UNCOMMITTED engine diff (`apps/engine/**` + flag-dark Worker wiring).

## Round 1 — NO-SHIP
**BLOCKING (class: "durable state advanced before the cross-boundary effect is confirmed"):** `engine.poll()` persisted `setHighWater(maxUid)` BEFORE returning events, and the Worker consumer never acked — a lost HTTP response permanently dropped replies/bounces/complaints. A missed REPLY does not self-heal: stop-on-reply never fires and we keep cold-mailing a responder.
**Siblings:** `tick.ts` unguarded `email.send()` on a row already `status='sending'` (real port throws ⇒ row stuck forever, no reclaim); real SMTP/IMAP adapters have zero default-CI coverage (GreenMail e2e is Docker-gated); `wire.ts` drift guard is one-way (added optional input fields drift silently); client accepts a plaintext `http://` ENGINE_BASE_URL (leaks the bearer secret).
**Held:** §0 sandbox safety (could not construct a path where a sandbox tenant reaches the real engine), auth (timingSafeEqual, generic 401), DSN/ARF classification (real MIME sub-part scan, not fixture-narrowed), VendorError grading, no SSRF via mailbox ids.

## Round 2 (re-attack of the class fix) — NO-SHIP (new blocker)
**CONFIRMED FIXED:** consumer-owned cursor — engine is genuinely cursor-stateless (`store.ts` = `{sends, threads}` only); the `poll_cursor` UPDATE is the last statement after a fully synchronous event loop, so it commits atomically with the side effects; a lost response truly redelivers; `recordEventIfNew` (INSERT OR IGNORE) gates ALL side effects incl. `cancelPendingSteps`, so redelivery can't double stop-on-reply or double-count. Class sweep verified ACCURATE across all 4 sites (`provisioning.ts`, `lifecycle.ts`, `threads.ts`, engine `recordSend` — all persist-AFTER-confirm). https guard not bypassable (exact-hostname match, fail-closed). §0 safety re-confirmed after the factory/env/schema edits.

**NEW BLOCKER — DOUBLE-SEND RACE.** `SEND_CLAIM_TTL_MS = 5 min` (`apps/platform/src/engine/tick.ts:43`) is HALF nodemailer's default `SOCKET_TIMEOUT = 10 min`; `apps/engine/src/smtp.ts` sets zero timeout overrides; `email-port.ts` puts no `AbortSignal` on the fetch; engine `send()` (`apps/engine/src/engine.ts:42-57`) is bare check-then-act with no in-flight claim. Scenario: tick A claims the row and calls `send()`; the SMTP socket stalls; at t>5min tick B's reclaim reverts the STILL-IN-FLIGHT row to `'pending'`, re-claims, and sends again with the same key — the engine's cache is still empty → **the lead receives the email twice.** The sweep is what proves this is a single-site invariant break: every other caller is protected by a Worker-side serializer (`withRequestIdempotency`, or the tick's atomic `'pending'→'sending'` claim); the TTL reclaim is the one place that deliberately revokes that serializer while the effect is in flight.
**TO SHIP:** bound the in-flight send well under the TTL (explicit nodemailer connection/greeting/socket timeouts + an `AbortSignal` on the Worker fetch) and/or give the engine send-store a claim-then-execute in-flight row mirroring `withRequestIdempotency`. Ship a test that FAILS on current code (stalled send + reclaim ⇒ two SMTP sends).

**NON-BLOCKING (landmines):** `ACTIVATION.md:42` step 6 instructs `ENGINE_BASE_URL=http://$IP:8080`, which the new https guard rejects as PERMANENT ⇒ every due send is marked terminal `'failed'` on attempt 1, and `'failed'` has NO requeue path (written `tick.ts:241/:289`, read only by ops-summary COUNT) — an operator following the runbook verbatim burns the whole queue. A 422 UnknownMailbox (operator-fixable) is likewise graded lead-permanent → terminal. The TTL reclaim does not bump `attempts`, so an orphan-reclaim loop has no ceiling.
