# integrations/cursor

Cursor rule files for coding agents that read `.cursor/rules/*.mdc`.

- [`coldrig.mdc`](./coldrig.mdc) — copy to `.cursor/rules/coldrig.mdc` (or `~/.cursor/rules/`). Depends on nothing outside this file; see [`../README.md`](../README.md) for the install one-liners.
