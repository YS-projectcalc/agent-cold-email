# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE, FIRST PAYING CUSTOMER Mordy (2026-07-28).** Site https://coldrig.dev · API https://api.coldrig.dev + https://agent-cold-email-api.yaakovscher.workers.dev · repo https://github.com/YS-projectcalc/agent-cold-email (PUBLIC) · Code: `~/dev/coldstart/`. **Standing autonomy (founder, 2026-07-22): merge/push/deploy/arm to customer-ready is authorized; browser-consent clicks excepted. 2026-08-14 additions: Claude holds the prod `ADMIN_TOKEN` (Keychain `coldrig/admin-token`) and OWNS acting on watchtower alerts; 2026-08-16: founder authorized browser-driven visibility work ("do whatever you can").**

> **You are resuming `coldrig` with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (current Worker version, the alert-wave branch state, whether Mordy's agent finally reacted). If they hold and the step is non-destructive, proceed. **STOP and confirm before any destructive/founder-owned step** — EXCEPT where the standing autonomy grant covers it (merge/push/deploy/arm).

## Where we are right now (2026-08-16)
1. **Vendor-verdict class fix SHIPPED + DEPLOYED 2026-08-14** (the founder-ordered post-ship re-attack found a BLOCKING live defect — dead vendor registration read as eternal "provisioning in progress"; closed as a 15-member class through a 3-round adversary gate). Frozen: `docs/adversarial/{c3-postship-reattack,vendor-verdict-class-sweep,vendor-verdict-gate}-2026-08-14.md`. Merged `e5598da`, wave closed `d6cc8ce`.
2. **`ADMIN_TOKEN` rotated into Keychain `coldrig/admin-token` (standing access) and the operator reply SENT to Mordy's agent** (201, `regarding sup_3ca260e4`: retry same key `apd-setup-a-2mbx`, spend-safe). **His agent has been SILENT since (>48h)** — recommend the founder nudges Mordy the human; the watch pages on any reaction.
3. **`GET /admin/ops/checks` SHIPPED + DEPLOYED** (gate SHIP-WITH-NOTES; two-store caveat: `d1`/`cron_sweep` dead-man live in WatchtowerDO — pair with `GET /status`). **Current live Worker version: `ba8d0fec`** (latest deploy; `/status` 200 throughout the session).
4. **Alert-policy wave IN FLIGHT** (founder AskUserQuestion ruling 2026-08-16: "Debounce + daily re-alerts" — see the `[ORDER]` in `ROADMAP.md ## Open` for the exact policy incl. the cron_sweep dead-man EXEMPTION and the ≤15-min real-outage guarantee). Builder mid-work — see In flight.
5. **Discoverability: blind-probe verdict = coldrig appeared in 0/51 agent queries; on-site layer already built; real gap = ~zero backlinks/index authority.** Frozen `docs/research/blind-agent-recommendation-probe-2026-08-16.md` (+ same-day correction). Off-site execution started this session — see the 2026-08-16 `[ORDER]` in `ROADMAP.md ## Open` for done/remaining.

## In flight / next
- **Alert debounce+backoff wave — BUILD COMPLETE, gate PENDING (result-bearing):** branch `feat/alert-debounce-backoff-2026-08-16` HEAD `8742830` (worktree `.claude/worktrees/agent-a81d88ccbef7ec5ea`). Battery green (1687 = baseline 1667 + 20, same lone pre-existing failure), RED revert-proof, dead-man exemption mutation-proven, migration 0018 with deploy-day backfill. Full evidence + the 2 spec-conflict resolutions the gate must verify: `ROADMAP.md ## Open` alert-policy `[ORDER]` entry. **Next: fresh adversary gate on the branch diff → merge `--no-ff` → deploy → live-verify → ledger**, all under the standing grant.
- **MCP-registry re-publish [blocked:founder-click]** and **Gmail MCP authorization [gated:founder]** — exact steps in their `ROADMAP.md ## Open` 2026-08-16 entries.
- Still running: the 2-hourly coldrig watch, cron job `be956d32` (**SESSION-scoped — dies with the session; re-create on resume**, spec below).
- Open decisions (founder-gated, unchanged): C3 (d) arm-gate fix (3 blockers — grep `PROVISIONING_RECONCILE_ENABLED` in ROADMAP; ⛔ never arm before they're fixed) · direction-1 (staged) · mailbox W7 tail (`~/dev/agent-mailbox/HANDOFF.md`).

## Landmines / gotchas
- **The watch cron is session-scoped.** Re-create in a resuming session: 4 legs every 2h at :41, ALL via HTTPS — **never `wrangler d1` reads (its transient 7403 auth races generated false leg-failures — the whole reason leg 1 moved to the API), and always write the legs' temp files to the CURRENT session's scratchpad dir (a stale prior-session path makes `curl -o` exit 56, mimicking network failure)** — (1) `GET /admin/support/digest` (tickets; admin token from Keychain), (2) InboxKit `/domains/list` + `/mailboxes/list` (key at Keychain `coldrig/inboxkit-api-key`, workspace `c5188ced-33db-436f-b970-1860e6c8c66b`), (3) `GET /admin/ops/checks?unhealthy=1` (known baseline = the 2 Mordy `domain_dns_aging` checks; stale shared `updatedAt` >15min = dead-cron tell), (4) `GET /status` (sweepAgeSeconds <900; 503 = SEVERE). State + baselines: `~/.claude/mordy-watch/last-seen.json`. NEW issues are handled under the grant (builder→gate→deploy, never a hot patch); notify founder with what happened; demo-tenant single-sweep DO transients get a recurrence tripwire, not a notification.
- **Mordy's 2 purchased domains are genuinely stalled** (`goauthorpitchdesk.com` ~480h, `theauthorpitchdesk.com` ~100h, 0 mailboxes) — the red `domain_dns_aging` checks are HONEST and stay red until his agent retries (the sent reply makes that spend-safe). His agent not reacting is the current bottleneck; platform-side everything behaves.
- **Founder alert-email volume** is the alert-policy wave's target (~8/day from the 2 stuck domains at the current 6h re-alert cadence + single-sweep transients) — do NOT "fix" it by hot-patching cadence; the approved wave carries the policy.
- **GitHub device-flow Authorize buttons reject synthetic browser clicks** — registry publish needs the founder's real click (steps in the ROADMAP entry). GitHub also demands 2FA on YS-projectcalc **before 2026-08-28** (banner seen 2026-08-16) — founder action, or account actions get restricted.
- **Weekly-review launchd job broken since ~2026-07-20** (rc=143/rc=1; `~/.claude/weekly-review.log`) — flagged repeatedly, still unfixed; rough-edge lines are accumulating unreviewed.
- **`mcp-publisher` buffers its device code without a TTY** — run it under `script -q <outfile> mcp-publisher login github` and read the code from the file.
- Prior HANDOFF snapshot: `archive/2026-08-16-visibility-alerts/prior-HANDOFF.md`. All session commits are pushed through `c5c1436` (verify `git log origin/main..main` empty at resume).

## Key files
- `docs/adversarial/vendor-verdict-gate-2026-08-14.md` — 3-round gate + ship record of the class fix.
- `docs/research/blind-agent-recommendation-probe-2026-08-16.md` — probe verdict + corrected diagnosis + option space.
- `ROADMAP.md ## Open` — the two 2026-08-16 entries (visibility [ORDER], Gmail [ASK]) + alert-policy [ORDER] + arm-gate blockers.
- `server.json` — v0.2.3 registry manifest, committed, awaiting publish.
- `apps/platform/src/admin/watchtower.ts` + `watchtower-do.ts` — the alert-policy wave's target files (its branch has the changes).

## Resume — the next action is the alert wave's completion (KIND A until its gates; founder clicks are KIND B)
1. **Alert wave (build DONE at `8742830` — gate next):** verify the branch still heads at `8742830` (`git -C ~/dev/coldstart log --oneline -1 feat/alert-debounce-backoff-2026-08-16`), then dispatch a fresh adversary gate on `git diff $(git -C ~/dev/coldstart merge-base main feat/alert-debounce-backoff-2026-08-16) feat/alert-debounce-backoff-2026-08-16` scoped to the approved policy AND the builder's 2 spec-conflict resolutions (all in the ROADMAP alert-policy `[ORDER]` entry — the gate re-runs the dead-man + one-shot mutations itself). On SHIP: merge `--no-ff`, `npx wrangler deploy` from `apps/platform`, verify `/status` 200 + new version, push, ledger, reap the worktree (branch preserved).
2. **Founder clicks (KIND B — present, don't do):** (a) MCP-registry Authorize click (exact commands in the ROADMAP visibility [ORDER]); (b) Gmail `/mcp` authorization; (c) GitHub 2FA before 2026-08-28; (d) nudge Mordy the human (his agent >48h silent on a spend-safe retry).
3. **Re-create the watch cron** (spec in Landmines) if not already running.
