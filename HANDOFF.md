# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · npm publish · real vendor spend · external send) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-15 evening — win-the-comparison build wave shipped live)

- **Design + $99 pricing + legal-final + RFC 8058 unsubscribe DEPLOYED LIVE** (Worker `4836515a`→`b21ca47d`, site `--branch main`, founder-authorized). Live-verified: `coldrig.dev/pricing` title "$99 for 5 Mailboxes, Then $10 Each" + JSON-LD `lowPrice":"99"`, `/status` 200, MCP `tools/list`==17 unauthenticated, `/unsubscribe?token=garbage` → 400. Full narrative + adversary record: `ROADMAP.md` 2026-07-15 design-lane entry (`## Now`).
- **Pricing RATIFIED by Yaakov, now live on every surface:** public pitch = **"starts at $99/month for 5 mailboxes, then $10 per additional mailbox"**; **send volume is NOT capped** — capacity is presented as ideal/recommended deliverability guidance, never contractual. Backend billing still legacy tiers (see Open decisions).
- **CLI 0.2.0 = a real installable MCP server** (`324a15c`): new `agent-cold-email mcp` stdio↔hosted bridge. Adversary 2 rounds → SHIP. **Published to npm 0.2.0 same day** — see below.
- **Legal FINALIZED per founder ruling** (`45a8d13`), now live: draft banners removed, governing law New Jersey (law + venue).
- **Watchtower/ops-email/monitoring lane SHIPPED + committed `2795a64`, deployed live in `b21ca47d`**: OpsMailer over the Cloudflare `send_email` binding (`OPS_EMAIL`, confirmed bound in `apps/platform/wrangler.toml`), 5-min cron armed (`*/5 * * * *`, confirmed in config), healthy→unhealthy alert state machine, dunning notices on suspend, inbound `support@` handler, `X-API-Key` auth alternative, repo-root `.mcp.json`. Adversary SHIP frozen (`docs/adversarial/watchtower-ops-email-review-2026-07-15.md`); non-blocking residuals ledgered in `ROADMAP.md ## Open`. All email legs dark/log-only until owner arming (wrangler login email scopes → sending enable → routing → verify address → route support@). Same commit fixed the waitlist CORS custom-domain bug — live-verified: `coldrig.dev` origin echoes ACAO, disallowed origin gets none.
- **`git push` DONE**: `origin/main` now at `c0b9e1f` (confirmed 0 ahead/0 behind local). Repo-root `.mcp.json` is public (cursor.directory unblocked); `X-API-Key` live (Smithery unblocked).
- **Engine droplet PROVISIONED DARK** (founder-authorized spend, doctl-confirmed): `coldstart-engine` (ID `584813711`, IP `142.93.12.85`, nyc1, firewall `coldstart-engine-fw` 22+8080), container Up, `/health` `{"status":"ok"}`, unauth `/v1/send` 401. NOT armed — no mailbox creds, no HTTPS tunnel, no Worker secrets, no `ENGINE_TENANTS`. A duplicate droplet from a doctl footgun was caught + deleted (confirmed: only one `coldstart-engine` droplet exists). Dockerfile npm defect ledgered (`ROADMAP.md ## Open`).
- **Directory/discoverability batch executed** (founder-authorized): llmstxt.site ✓, directory.llmstxt.cloud ✓, mcp.so ✓ (free tier, in review queue), awesome-mcp-servers PR #10106 — Glama badge added + bot answered (maintainer's merge pending), Glama already auto-indexed. cursor.directory + Smithery finish-publish is DONE — see below.
- **Flagship guides SHIPPED LIVE** (`0a87f7c`): 3 operation guides (`guide-cold-email-operation-{claude-code,codex,cursor}`) — adversary SHIP, zero blocking (`docs/adversarial/guide-pages-review-2026-07-15.md`). Live-verified: all three 200 on coldrig.dev, listed in `llms.txt`, wired to sitemap.
- **Five comparison pages SHIPPED LIVE** (`6849c80`+`a074aa7`+`0744e15`): `compare-vs-{agentmail,skyp,foxreach,maildoso,smartlead}` — adversary NO-SHIP→SHIP over 2 rounds (sole blocker: internal buyer-panel paths leaked into public HTML → reworded; frozen record `docs/adversarial/compare-pages-review-2026-07-15.md`). Live-verified: all five 200 on coldrig.dev, confirmed in sitemap.xml and `llms.txt` (7 compare entries total live), zero `tools/buyer-panel`/`briefs/`/`CHOICE-TREND` path leaks (grepped clean across every `site/*.html`). Every competitor number live-verified twice (builder + adversary independent fetches).
- **Buyer-CHOICE panel cycle 1 COMPLETE** (`tools/buyer-panel/`, `CHOICE-TREND.md` 3 rows, `runs/` 3 frozen records — confirmed on disk): canonical NOT SURFACED (winner Salesforge/Forge ~$102-120/mo); starter SURFACED+SHORTLISTED organically via Glama, killed "no real sending/no published pricing" (winner Smartlead ~$62-65/mo); agency SURFACED+SHORTLISTED ("closest match to your entire brief"), same kill (winner Smartlead ~$550-630/mo). Fix orders ledgered in `ROADMAP.md ## Open`: directory-shopfront refresh [ORDER], agency pricing-shape [IDEA], review-site presence [IDEA], reply auto-classification [IDEA], canonical price band [IDEA]. Harness runbook hardened (cwd/identity-leak preconditions).
- **npm + MCP Registry PUBLISHED** (founder hands): `agent-cold-email@0.2.0` live on npm (dist-tag `latest` confirmed via `npm view`) + registry entry v0.2.0 `isLatest:true` with BOTH the hosted remote and the npm package block (confirmed via registry API — v0.1.0 entry still present, `isLatest:false`). CLI-publish founder item CLOSED.
- **Smithery PUBLISHED** (smithery.ai/servers/yaakovscher/agent-cold-email — page live-confirmed 200, quality 70/100 per publish-time scan via the X-API-Key gateway header) + **cursor.directory SUBMITTED** (cursor.directory/plugins/agent-cold-email, pending their security scan — not independently re-verified this pass, page rate-limited on check). Tasks #24/#25 complete.
- **Founder rulings captured on ledger** (`ROADMAP.md ## Open`, already reflected there): send-framing copy (no-send-quota headline, physics number demoted — AMENDED same day after "3,300 doesn't sound as good as 30,000"), annual billing option (~2-months-free, folds into quantity-billing migration), webhooks RULED BUILD (next core lane), warm-lead deep dive (not a Mordy blocker).
- **Win-the-comparison program running** (Yaakov order): competitor research delivered + persisted (`docs/research/competitor-pages-research-2026-07-15.md`); buyer-CHOICE harness committed (`tools/buyer-panel/`, `c0b9e1f`). Engine droplet remains live DARK (`142.93.12.85`, `/health` `{"status":"ok"}` re-verified, unauth `/v1/send` 401; no creds/tunnel/secrets/allowlist).

## In flight / next

- **No agents currently running.** Guide-builder and both comparison-page/buyer-run lanes reported and closed out this wave — see `## Where we are`.
- **Next action, in order:** (1) directory-shopfront refresh build (repo `README.md` + `server.json` description + `site/.well-known/mcp/server-card.json` + `packages/cli` README — ordered by the starter buyer-run kill, unbuilt; `ROADMAP.md ## Open`); (2) webhooks core-platform lane (ruled build, next now that watchtower+CORS+comparison commits have cleared); (3) BYO intake build (§20, queued behind webhooks, unblocks Mordy migration); (4) warm-lead lifecycle deep dive (Yaakov ruling 07-15, NOT a Mordy-start blocker); (5) capacity/send-framing copy pass + annual-billing toggle — rides the pending Stripe quantity-billing migration. See `## Resume`.
- **In progress (not finished):** ROADMAP done-drain — 8 `- [x]` lines are checked but not yet moved to `archive/ROADMAP-done.md` (still 8, unchanged this session; purely mechanical, do at next).
- **Open decisions / blockers (all founder-owned):** the two CLI publish commands are now DONE (npm 0.2.0 + MCP Registry v0.2.0 live, see `## Where we are`) — remaining founder-hands queue: Glama claim click (his GitHub OAuth; proven kill surface per the starter buyer-run, not a nice-to-have), GSC/Bing + CF Web Analytics (his clicks), PulseMCP (bot-walled — his browser or email), **Stripe live keys + quantity-billing backend migration** (dashboard billing UI is built but deliberately inert until per-mailbox quantity billing + spend-ceiling persistence + plan-enum migration + annual-price variants land — the last core-code gap, now also carrying the annual-billing ruling), agency-bundle pricing ruling (buyer-run #3 finding, `ROADMAP.md ## Open`), review-site listings (G2/Trustpilot/Capterra — buyer-run #1 finding), ~$10 test domain (no registrar/card on this machine), Mailforge signup OR a GWS app-password box for Gate-1 smoke, Cline (needs one real Cline-setup test to attest honestly), dogfood 3 calls (competitors? Jack Clark? "roast it" CTA), external uptime prober (UptimeRobot/BetterStack — CF can't watch itself), activation chain (test domain, Mailforge signup, `wrangler login` email scopes, external prober). Mailbox provider is RESOLVED (Mailforge managed-path; Mordy rides BYO on his own Google Workspace boxes) — droplet spend is DONE (provisioned, live dark, see above).

## Landmines / gotchas

- **`site/` deploys whatever is on disk, not what's committed** — `wrangler pages deploy site --branch main` uploads the local directory as-is (confirmed: no git-based Pages integration, no deploy script wraps it). Working tree is clean of any site-page drafts as of this pass (`git status --short` shows only the pre-existing untracked worktrees dir below) — but re-check before any future site redeploy, this bit twice already.
- **Deploy batch + guide/compare pages are now live** (resolved landmine) — `coldrig.dev` serves the new design + $99 pricing + legal-final + 3 flagship guides + 5 comparison pages; live-verified per `## Where we are` (all 200, zero internal-path leaks). Don't re-run the Worker+site deploy sequence expecting a no-op; check `git log`/live state first.
- **Stale leftover worktree** `.claude/worktrees/agent-affd823ae7fd92292` (branch `integrate/design-2026-07-15`) — the design-integrator's; already merged to main, safe to `git worktree remove` when convenient. The designer's own worktree is `~/Documents/Codex/2026-07-14/.../coldrig-design` (branch `coldrig-human-interface`), now unrelated to main's history.
- **support-kb.ts:35-36 still quotes dead $299/$799 tiers** — the AI support agent's KB contradicts the ratified pricing (now live); fix with the billing migration (`ROADMAP.md ## Open`).
- `apps/platform/vitest.config.ts fileParallelism:false` is REQUIRED. Fresh-worktree verification needs `apps/platform/.dev.vars` (from `.dev.vars.example`) + local D1 migrations or the suite shows phantom failures.
- **npm + MCP Registry publish is DONE** (resolved landmine) — `agent-cold-email@0.2.0` live on both, confirmed via `npm view` dist-tag and the registry API. Remaining founder OAuth = Glama claim, PulseMCP, GSC/Bing, CF Web Analytics, Cline setup test.
- **Engine droplet is provisioned but wide open on 8080** (firewall `coldstart-engine-fw` allows 0.0.0.0/0 on 22+8080) with an empty creds map — low risk while dark (unauth `/v1/send` → 401) but put it behind the Cloudflare Tunnel before arming, per the existing ACTIVATION Gate-2 plan; don't skip that step under pilot time pressure.

## Key files

- `SPEC.md` (§0 locks · §12/§12.1 economics · §18 pricing · §19 dashboard · §20 BYO) · `ROADMAP.md` (`## Now`/`## Open` ledger — source of truth for all open items) · `ACTIVATION.md` (owner-hands gates; Gate 2 engine = CLEARED) · `CLAUDE.md` (project law).
- `docs/adversarial/{design-integration,cli-mcp-bridge,b4-optout,engine-host,engine-tenants-allowlist}-review-2026-07-1{4,5}.md` — frozen verdicts.
- `docs/research/{agent-buyer-research-forensics,dogfood-targets}-2026-07-14.md` — the buyer-agent playbook + dogfood list.
- `packages/cli/src/commands/mcp.ts` — the new stdio MCP bridge. `apps/engine/` — dark Node SMTP/IMAP daemon (committed, flag-dark).
- `archive/2026-07-15-day2-design-email/prior-HANDOFF.md` — the resume state this replaced.

## Resume — KIND A: the next step is a build lane, no pending review

No agents are running and no reviewed-but-unshipped work is sitting on disk — the guide-page and comparison-page lanes both landed clean (adversary SHIP, committed, deployed, live-verified). `git -C ~/dev/coldstart status --short` should show only the pre-existing untracked `.claude/worktrees/` dir; if it shows anything else, orient from that first.

**Build lanes, in order** (see `## In flight / next` for the full list + rationale):
1. **Directory-shopfront refresh** — repo `README.md` + `server.json` description + `site/.well-known/mcp/server-card.json` + `packages/cli` README, carrying the $99/5-mbx pricing + "free sandbox live today, no card" framing per the starter buyer-run kill (`ROADMAP.md ## Open` 2026-07-15 [ORDER]). Claim-class rules apply before it ships.
2. **Webhooks** (core-platform, ruled build) — real per-tenant reply/bounce webhook subscriptions, next now that watchtower+CORS+comparison commits have cleared.
3. **BYO intake build** (§20) — queued behind webhooks, unblocks the Mordy migration.
4. **Warm-lead lifecycle deep dive** — scope in `ROADMAP.md ## Open`; not a Mordy-start blocker.
5. **Send-framing copy pass + annual-billing toggle** — rides the pending Stripe quantity-billing migration (founder-gated, see below).

**Then, and only with Yaakov's explicit confirmation** (each is founder-owned, irreversible, or spends money — STOP and confirm before each):
1. His quick clicks: Glama claim (GitHub OAuth), PulseMCP submission, GSC/Bing + CF Web Analytics, the Cline setup test, `wrangler login` (email scopes, needed to arm the watchtower's outbound send).
2. Pilot chain (droplet + deploy batch already DONE): buy ~$10 test domain, Mailforge signup OR a GWS app-password box for Gate-1 smoke → Gate-1 smoke → arm Mordy pilot.
3. Stripe live keys + quantity-billing backend migration ruling (unblocks annual billing + agency-bundle pricing, both ledgered `ROADMAP.md ## Open`).
4. Review-site listings (G2/Trustpilot/Capterra) — founder-hands per the canonical buyer-run finding.

Do NOT deploy, push, publish, or spend without confirming with Yaakov first.
