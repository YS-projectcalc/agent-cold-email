# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE:** site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry 0.2.2 (24 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (below) explicitly covers it.

## Where we are right now (2026-07-24, go-live program — GA wave shipped + armed, quantity-billing design landed)

**Standing authorization (founder, verbatim on the ledger):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere."* Plus arming authorization: arming steps execute autonomously (Keychain keys + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder. Full program definition: `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" entry (2026-07-22) — steps (1)-(4) are now DONE/merged; step (5) (final claim-surface pass + IndexNow) is next after quantity-billing.

- **Signup-auth SHIPPED (earlier 07-23):** Worker `65415e9c` + site deployed, "Free sign up" live. §1.7b empirical outbound gate PASSED (DKIM/SPF/DMARC all pass under `p=REJECT` from login@coldrig.dev — alignment cleared) but placement is Gmail SPAM (new-domain reputation, not config) — email-login advertising stays dark pending "Not spam" click + Postmaster registration + accrued volume/time.
- **GA WAVE MERGED + DEPLOYED (this session):** both GA lanes landed on main — OFAC SDN screening `32d5b6b` (fail-closed w/ self-heal + race guards, adversary SHIP) and provisioning-core GA `8674da7` (factory InboxKit wiring — real provisioning now REACHABLE when armed — spend ledger/ceiling/reaper, slot gate, honest activationState; adversary SHIP 0-blocking, 4 deviations ratified), plus integration glue `1dccd94` and a gitlink-cleanup fix `d89516c`. **The battery arc ran FIVE times**: run #4 FAILED 5 fixture tests — correctly, not a regression — because two pre-existing G3/factory fixtures drove checkout with no SDN list seeded and the new N-OF-1 fail-closed gate correctly landed them `screening_hold`; the fix (`6299788`, benign-list seeding, test-only, assertions unweakened) took run #5 to **PASS 763/763**. Deploy sequence (`wrangler deployments list`-verified): `65415e9c` → InboxKit secret changes → **`a8a0b117`** (current live version, see the OFAC fix below). D1 migrations 0009+0011+0012 all applied remote (confirmed via `wrangler d1 migrations list --remote`: "No migrations to apply!"). Live-verified: `/status` ok, `/checkout/simulate` 404, MCP `tools/list` 24, `serverInfo.version` **0.2.2** (the stale hardcoded "0.1.0" claim is closed, `ca072ac`).
- **OFAC's first live cron run hit HTTP 525 — root-caused and fixed.** The cron fired its fail-loud alert exactly as designed on first activation (prior list retained, retry ~5min — nothing broke silently). Root cause: treasury.gov's public download URL redirects through a front-end that rejects the Workers TLS/fetch handshake. Fix `a7f3b97`: `OFAC_LIST_URL` repointed to the direct `sanctionslistservice.ofac.treas.gov` publication-service URL (verified this session: 302 → signed S3 URL → 200, real 12-column SDN CSV). Redeployed as `a8a0b117`. **Verified at bookkeeping time (2026-07-24 00:11 local, ~6 min post-redeploy): `sdn_list_meta` is still EMPTY on remote D1** — the first successful load has not yet been confirmed (too-soon-to-tell, not necessarily a new failure — see Landmines).
- **Arming state: InboxKit ARMED, engine tunnel not yet.** `INBOXKIT_API_KEY` + `INBOXKIT_WORKSPACE_ID` are set as Worker secrets (confirmed via `wrangler secret list` this session) — workspace uid was discovered live via `GET /workspaces/list`. `GMAIL_OAUTH_GRANTS` is still absent (confirmed) — blocked on a founder-run command (classifier denies it from the main loop; exact command below). Cloudflare Tunnel still needs a founder `cloudflared tunnel login` browser-consent click before `ENGINE_BASE_URL` + a fresh `ENGINE_AUTH_SECRET` can be set both sides. `ENGINE_TENANTS` and the gate-(e) throwaway-mailbox probe are deliberately deferred — the InboxKit workspace "Starter" has ZERO connected domains today (verified live), so gate-(e) has no domain to probe against until Mordy's onboarding connects one.
- **Quantity-billing design LANDED** (`71fb17f`, `docs/research/quantity-billing-design-2026-07-23.md`): licensed 2-item subscription + committed-count meter + durable Stripe Prices + agency-bundle sketch; flags a latent metered-vs-licensed mismatch for the adversary. Adversary review is dispatched next; `MORDYPILOT` must ride whatever mechanic ships.
- **Founder rulings (07-21/22, still governing; all on ledger):** OFAC-stubbed pilot YES · prewarm pilot buy WITHDRAWN (no Instantly deadline; prewarmed lookalikes impossible by construction) · Mordy price 60% off · registrar = Cloudflare default (delegated choice) · review-sites list-now/PH-defer + dogfood scope (delegated) · magic-link login + human signup + one-funnel "Free sign up" (shipped) · warm-lead Q1-Q6 ratified (built + deployed).

## In flight / next

- **Next action:** quantity-billing adversary review (design frozen at `71fb17f`) → build → deploy → site pricing-info pass + IndexNow (program step 5, the last program step).
- **Arming tail, in leverage order:** (1) founder runs the `GMAIL_OAUTH_GRANTS` mint command (see Landmines) — classifier-blocked for the main loop; (2) founder does `cloudflared tunnel login` on the droplet, then main loop sets `ENGINE_BASE_URL` (https tunnel) + fresh `ENGINE_AUTH_SECRET` both sides per `ACTIVATION.md` steps 2/6; (3) `ENGINE_TENANTS` + gate-(e) throwaway-mailbox verify ride Mordy's first domain connect (natural supervised probe, InboxKit workspace has no domains yet).
- **SDN load — verify next session:** re-check `sdn_list_meta` row count on remote D1 before treating OFAC screening as loaded (screening currently fails safely CLOSED to `review` on an empty list — not unsafe, just not yet steady-state).
- **Founder click queue, by leverage:** Glama Dockerfile build ★ (3 ratified values on ledger, unblocks PR #10106) → "Not spam" click on "Your Coldrig sign-in link" in Gmail → `GMAIL_OAUTH_GRANTS` command → `cloudflared tunnel login` → GSC URL-Inspect Request-Indexing on ~10 money pages → Bing Webmaster Tools verification-status check (unknown — IndexNow submission ≠ console verification) → Stripe dashboard support_email + public business name/descriptor "Coldrig".
- **Watchdog:** the 3h session-only cron has not been confirmed re-armed since the 07-22 classifier denials — verify/re-arm from a session where `CronCreate` passes, or get the founder to allow the tool.
- **Open founder items (none block the lanes):** www.coldrig.dev custom-domain click (NXDOMAIN — and the Stripe business profile lists `https://www.coldrig.dev`!) · uptime-prober service name · Cline test [gated:founder-hold] · Instant Start SKU price ratification (gated on credits→USD verify).

## Landmines / gotchas

- **Live money is now real (as of 2026-07-23).** The Worker runs LIVE Stripe keys (flip executed + verified) — NEVER complete a live checkout in testing; a completed session charges a real card. `MORDYPILOT` is live-mode, single-use, and now consumable by anyone with the code — the test-mode twin was already consumed proving the flow. Never print Keychain values; read via `security find-generic-password -a coldrig -s <service> -w`.
- **InboxKit is ARMED and real provisioning is now REACHABLE for paid+screened tenants.** Unlike the earlier state (INBOXKIT_* secrets alone were insufficient), the provisioning-core GA merge (`8674da7`) wired `inboxKitConfig` into the DO factory call — so a tenant that goes paid + active + OFAC-screened-clear will now trigger REAL InboxKit vendor calls, not a sandbox stub. Containment while ENGINE_TENANTS/tunnel stay unarmed: OFAC fail-closed, $150 vendor-spend ceiling, slot cap, G5 domain hard-block, and — as of this bookkeeping pass — zero tenants have actually reached a real-send-activated state. Don't assume "InboxKit secrets are just sitting there inert" anymore when reasoning about spend risk.
- **SDN list may still be empty.** The OFAC_LIST_URL fix (`a7f3b97` → deployed `a8a0b117`) was verified to fetch a real CSV, but `sdn_list_meta` read 0 rows on remote D1 ~6 minutes after redeploy (2026-07-24 00:11 local) — the first successful cron load was not yet confirmed at write time. Re-check the row count before treating OFAC screening as loaded; an empty list still fails CLOSED to `review` (safe), so this is a data-freshness gap, not a security hole.
- **Worktrees reap-ready (merged, safe to prune) — 9 total, all confirmed ancestors of main via `git merge-base --is-ancestor`:** `brand-sweep-20260723` (`60275b3`), `g5-gate-a-20260723` (`91f8227`), `test-env-hermetic-20260723` (`bca6db9`), `agent-a8f87cd1437a20f72` (I3+I4, `5b151e7`), `signup-auth-20260723` (`65415e9c` deploy lineage — no longer active, safe to prune), `provisioning-core-20260723` (`8674da7`), `ofac-20260723` (`32d5b6b`), `fixture-fix-20260723` (`6299788`). The dead LOCKED worktree `agent-a3b6ad8f1d279ebb6` (a prior brand-sweep attempt, abandoned mid-write when the 07-22 session died) is SUPERSEDED and also reap-ready — it holds exactly 3 uncommitted partial files that never landed anywhere.
- **Non-blocking residual carried from I3+I4 (07-23, still open):** a crash between the `released_at` UPDATE and the revoke call can leave a single mailbox's vendor token un-revoked (skipped on retry via the `released_at IS NULL` filter) — same class as the founder-accepted Gate-2 crash residual, narrower than the original never-revoked bug. Optional hardening: revoke before the UPDATE.
- **Simulate endpoint:** `GET /checkout/simulate` 404s whenever Stripe OR engine is armed (`isRealSpendArmed`) — expected, not a bug. Adding any vendor env binding without extending that guard trips `apps/platform/test/spend-armed-env-coverage.test.ts` RED by design.
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20` or POST /v1/promotion_codes rejects `coupon` (see repo `MEMORY.md`).
- **Site deploys whatever is on disk** (`npx wrangler pages deploy site --project-name=agent-cold-email --branch=main`) — check `git status` first. Deploy-ordering law: Worker before site whenever counts/claims change.
- **`site/agent-evaluation.md` still says "Stripe cannot take money yet"** — now FALSE as of the live-key flip; the program's final info pass (step 5) must correct it (claim-surface class — sweep by inventory, not memory).
- **Gov/vendor download URLs: pin the FINAL host, never a redirect front-end.** treasury.gov's redirect front-end rejected the Workers TLS/fetch handshake (HTTP 525) even though a normal client follows the redirect fine — the direct `sanctionslistservice.ofac.treas.gov` host works. Same class risk for any future Workers `fetch` against a gov/vendor download endpoint.
- UNVERIFIED left: SDN list load completion (see above, re-check next session). Live claims otherwise were command/API-verified at write time (`wrangler deployments list` shows `a8a0b117` as current, `wrangler secret list` shows INBOXKIT_* present + GMAIL_OAUTH_GRANTS absent, D1 migrations all applied, MCP `serverInfo.version` 0.2.2 live). Prior history: `archive/2026-07-22-golive-program/prior-HANDOFF.md`.

## Key files

- `ROADMAP.md ## Now / ## Open` — single source of truth for every open item + the program entry. `archive/ROADMAP-done.md` — drained done items.
- `docs/adversarial/*-2026-07-2{1,2}.md` — frozen verdicts (committed as of `c8b9ee3`) · `docs/research/*-2026-07-2{1,2}.md` — frozen designs/research.
- `archive/2026-07-22-golive-program/` — prior-HANDOFF snapshot + promo-checkout E2E evidence (screenshots + browser script).
- `apps/platform/src/engine/activation.ts` (live activation gate) · `apps/platform/src/engine/billing.ts` (`isRealSpendArmed`) · `apps/platform/src/billing/stripe-client.ts` (inline pricing + required coupon constraints comment).

## Resume — KIND A: process the in-flight lane per the authorized program

Verify preconditions: `git -C ~/dev/coldstart status -sb` → main at/past `116a058`, in sync with origin (skill-produced doc commits may sit above it). `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`. Confirm the live-key-flip precondition holds before treating Stripe as live: `cd apps/platform && npx wrangler secret list` should show `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` present (never print values). Then check the in-flight lane: if this is the SAME session, the task list / notifications carry it; if this is a FRESH session (task list gone), check on disk instead — `git -C ~/dev/coldstart/.claude/worktrees/signup-auth-20260723 log --oneline -5` for build progress, or does a `docs/adversarial/signup-auth-build-review-2026-07-2*.md` (or similarly dated) verdict already exist? For the delivered lane, process per `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" step (3): adversary verdict gates the merge; merge → battery → deploy per the deploy-ordering law. §1.7b's empirical outbound gate is a MAIN-LOOP step (droplet-IMAP DKIM/DMARC read), not a subagent step — run it after increment-A deploys. A lane that died at a usage limit: RESUME it via SendMessage to its agent id — never re-dispatch its scope. Continue autonomously under the grant; stop only at browser-consent clicks or genuinely new founder-scope decisions.
