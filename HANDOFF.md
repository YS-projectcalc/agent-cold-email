# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.1.0` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything has CHANGED, surface exactly what and ask before acting. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · real vendor spend · npm publish · external send) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-14 — "activation day, round 2")

- **Engine COMMITTED DARK, `eb8ee42`, after adversary re-attack #3 = SHIP.** The double-send race (`SEND_CLAIM_TTL_MS` half nodemailer's socket timeout, bare check-then-act send) is fixed: in-flight claim-before-await, SMTP inactivity timeouts 20/20/60s < 180s Worker-fetch abort < 300s reclaim TTL, TTL reclaim now bumps `attempts` with a ceiling, 422/409 regraded retryable. Revert-fail proven (defeated claim ⇒ 2 SMTP sends). Suites per the commit + frozen verdict: engine 30 passed / 3 Docker-gated skips, platform 263 passed / 49 files, both typechecks exit 0 (adversary's own re-run, not independently re-executed by this bookkeeping pass). Frozen verdict: `docs/adversarial/engine-host-review-2026-07-14.md` → Re-attack #3, `VERDICT: SHIP`. Two residuals ruled **ledger-able, not blocking**, carried into `ACTIVATION.md` Gate 2 (verified present): (a) crash-after-SMTP-accept-before-record-flush window — MUST resolve or get an explicit founder accept before arming; (b) multi-instance claim coordination — N/A for the current single-daemon shape. `ACTIVATION.md` Gate 2's engine line now reads **CLEARED TO PROVISION** (was "⛔ DO NOT PROVISION YET"). `ARCHITECTURE.md` #6 updated in the same push: Node-over-Go ratified, cites `eb8ee42` (verified in file). Follow-up nit fix, `7616d19` (verified in `git log`): corrected a `store.ts` comment that understated the crash-after-accept-before-record window — it's a tracked Gate-2 residual, not covered by the in-memory claim; code behavior unchanged, comment-only. Local `main` is now 5 commits ahead of `origin/main`; push is still founder-gated.
- **Legal D3 COMMITTED, `5bade3e`** (not deployed) — new `site/dpa.html` (9 `<h2>` sections, verified), `site/terms.html` §8 raised to ROSCA-grade (named statute, disclosure timing, `POST /cancel` mechanism), DPA footer-linked from all 4 legal pages + sitemap, AUP reviewed unchanged. Enforcement map per ROADMAP D3: 7 clauses BUILT w/ file:line, 2 N/A-legal, 2 PARTIAL (follow-up ledger entries). All pages draft-flagged, pre-attorney. **NOT deployed** — blocked on the RFC 8058 overclaim below; deploy is founder-gated regardless.
- **Docs reconciliation COMMITTED, `5a30457`** — `SPEC.md` §12/§18 cost basis corrected to sourced $4.50–5/mbx (Mailforge path) vs ~$6.10/mbx (Inboxkit), new §12.1 pre-warmed-inventory analysis (ruling-pending, not yet a founder decision), `ROADMAP.md` fully reconciled (C4 + seed-indexing + A5 + both engine defect entries closed with evidence), `ARCHITECTURE.md` #6 Node ratification.
- Everything from the prior "activation day" round still holds: npm + MCP Registry live, coldrig.dev host-swap live, waitlist form fix deployed, SPEC §20 (BYO domains) shipped-verdict (build not yet started). See `archive/2026-07-14-activation-day/prior-HANDOFF.md` for that round's full detail if needed.

## In flight / next

- **Nothing is currently running.** Verified `git status --short` at write time: clean except 4 untracked `pw-scripts/verifier-*.js` files (prior-session scratch, now archived — see Landmines). **No `ENGINE_TENANTS` diff or references found anywhere in the tree** (`git status`, `git diff --stat`, and a repo-wide grep for `ENGINE_TENANTS` all came back empty) — if a prior brief told you this lane was in flight, it either hadn't started writing code yet or the session ended before any file touched disk. Don't assume partial work exists; re-verify with the same commands before acting.
- **Next code step: B4 — sequencing/reply-engine opt-out flow.** Build the hosted RFC 8058 one-click POST endpoint (`TODO(B4)` at `tick.ts:46-56`); this is also the fix for the marketing-copy overclaim below, so it clears two ROADMAP items at once. After it lands: fresh-context adversary pass, commit only on clean.
- **Founder-gated queue (Yaakov's hands/word), unchanged from prior round, nothing resolved yet:**
  - PRICING RULING — designer + Yaakov independently converged on $49 + $10/mailbox; economics validated in-session (marginal GM 40–52%); live `pricing.html` also has an internal send-estimate math inconsistency to fix in the same pass. Detail: `ROADMAP.md` `## Open`.
  - PulseMCP submission — prepared, needs Yaakov's direct in-session statement (standing approval alone isn't sufficient for this classifier).
  - Smithery / mcp.so / cursor.directory — all behind GitHub-OAuth; one headed login persists a profile for future automated submissions.
  - llmstxt.site + directory.llmstxt.cloud — scripts ready in `archive/2026-07-14-activation-day/scratch-rescue/`, ~10s each once run.
  - Deploy batch (legal pages + any site fixes) — blocked on the RFC 8058 overclaim resolving (see above).
  - Droplet provisioning — runbook in `ACTIVATION.md` Gate 2, **CLEARED**, not yet run.
  - Mailbox provider ruling (Inboxkit vs Mordy's own Google Workspace) + ~$10 test domain purchase.
  - Dogfood campaign — 3 founder calls needed (include competitors? include Jack Clark/Import AI? "roast it publicly" CTA risk tolerance?). Target list + copy drafted: `docs/research/dogfood-targets-2026-07-14.md` (28 entries, tiered; verified present). Contact-path completion pass for top Tier A names still needed before send.
  - Stripe live activation (Gate 2) · Cloudflare Web Analytics toggle (`ROADMAP.md` `## Now`).
- Full itemized list: `ROADMAP.md` `## Now` + `## Open` (29 open lines, 6 checked-not-yet-drained at write time).

## Landmines / gotchas

- **Engine is committed but DARK — arming ≠ committing.** `apps/engine/**` is on `main` as of `eb8ee42`, flag-dark (`NotActivatedError` until `ENGINE_BASE_URL` + `ENGINE_AUTH_SECRET` are set). Arming carries the two Gate-2 residuals above — do not silently provision past the crash-window one without a founder decision.
- **RFC 8058 marketing overclaim = deploy blocker.** `site/faq.html`, `site/pricing.html`, and at least one guide page reference "RFC 8058 one-click unsubscribe" — most instances already carry an "(on the roadmap)" qualifier, but `site/guide-cold-email-deliverability.html:92` states it flatly without the qualifier. Resolve via B4 shipping the real endpoint (preferred) or softening that line — before/with the next site deploy. Full detail: `ROADMAP.md` `## Open`, 2026-07-14 entry.
- **`ENGINE_TENANTS` per-tenant allowlist is NOT built.** No code, no diff, no references anywhere in the tree as of this write — see "In flight" above. Whatever a prior handoff said about it being in progress, verify fresh before trusting it.
- **Stripe CANNOT take money:** `STRIPE_SECRET_KEY` unset in prod — `/checkout` returns a simulated URL and upgrades a tenant with zero card/dollars (intentional test-mode; anyone with the API can self-upgrade). Webhook fails closed (503, `STRIPE_WEBHOOK_SECRET` unset). ACTIVATION Gate 2.
- **No human can buy or use this today** — no human signup form, no dashboard billing controls. `[ORDER]` on record for the external design lane. Evidence: `pw-shots/human-journey-2026-07-14/` (14 screenshots, both widths, gitignored).
- **Design is EXTERNALLY owned** (`685a202`) — a different LLM builds landing/human pages in `~/Documents/Codex/2026-07-14`. Do NOT design here — integrate + re-verify when handed over.
- **4 prior-session `pw-scripts/verifier-*.js` files archived this pass** (were untracked scratch from the human-journey/dashboard/billing/waitlist verification drives, evidence already captured in `pw-shots/`) — moved to `archive/2026-07-14-activation-day/verifier-scratch/`, pointer in `archive/2026-07-14-activation-day/INDEX-notes.md` and `archive/INDEX.md`. Nothing deleted.
- Deploys: Worker via `npm run deploy` in `apps/platform/` (applies D1 migrations FIRST — never bare `wrangler deploy`). Pages deploys from an agent worktree **must pass `--branch main`** or they land as preview, not production.
- `apps/platform/vitest.config.ts` `fileParallelism: false` is REQUIRED (shared Miniflare) — removing it re-introduces flakes.
- npm + MCP-Registry credentials are Yaakov's identity (2FA / device-flow) — publishes need his hands.
- This repo is a shared/live worktree across concurrent agent sessions — re-`git status`/re-Read before editing tracked docs; don't `git checkout`/`reset`/`clean` any path without confirming nothing else is mid-write there.

## Key files

- `SPEC.md` (§0 locks · §12/§12.1 economics · §19 dashboard · §20 BYO domains/mailboxes) · `ROADMAP.md` (`## Now` / `## Open` ledger) · `ACTIVATION.md` (owner-hands gates; **Gate 2 engine line = CLEARED TO PROVISION**) · `ARCHITECTURE.md` (#6 = Node engine, ratified) · `CLAUDE.md` (project law) · `MEMORY.md` (build lessons) · `AGENTS.md` (the 17-tool surface).
- `docs/adversarial/engine-host-review-2026-07-14.md` — engine Round 1/2/Re-attack #3 verdicts (frozen, `VERDICT: SHIP`) · `docs/adversarial/byo-domain-design-review-2026-07-14.md` — 4-round BYO verdict (frozen).
- `docs/research/dogfood-targets-2026-07-14.md` — 28-target dogfood list + copy drafts (frozen) · `docs/research/warmforge-bundle-verification-2026-07-12.md` + `docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md` — sourcing for the corrected §12/§18 cost basis.
- `apps/engine/` — the Node SMTP/IMAP daemon, **committed** (`eb8ee42`), flag-dark · `site/dpa.html` + `site/terms.html` §8 — new legal, draft-flagged.
- `archive/2026-07-14-activation-day/` — this day's provenance dir (`prior-HANDOFF.md`, `scratch-rescue/` for pending llmstxt/Tally scripts, `verifier-scratch/` for completed verification drive scripts).

## Resume — KIND depends on what you verify, not on what this file assumed earlier

**Verify first:** `git -C ~/dev/coldstart status --short` and `grep -rn "ENGINE_TENANTS" apps/ packages/`. This lane has now been checked twice in one day and found empty both times — first at the initial write of this section, then again after a teammate reported it "resumed" (checked a second time at 21:3x, still zero hits, tree clean besides the already-archived scratch). **Two false starts on the same lane is itself a signal — don't re-dispatch a third time on the same brief without asking why it keeps dying** (usage-limit interruption? builder erroring silently? brief ambiguous?). If you still find it empty on your check:

- **The lane died again — re-dispatch, but flag the pattern.** Dispatch a builder (sonnet/opus, never Fable) for `ENGINE_TENANTS`: a per-tenant allowlist with default-empty (never wildcard), fail-closed on malformed input, plan-check dominant (a demo/free tenant on the allowlist still gets sandbox), `realAdaptersActivated` stays a separate global gate, tenantId from the verified DO identity. Before re-dispatching a third time, check whether the prior two attempts left any trace (agent logs, a stray branch, a `.claude` scratch note) explaining why nothing landed — don't just re-run the same brief blind.
- **If instead you find an `ENGINE_TENANTS` diff present-but-uncommitted:** resume = get a fresh-context adversary pass on it (same guards as above) and commit only on a clean pass. Do not assume the diff is correct just because it exists.
- **Either way, once `ENGINE_TENANTS` is committed (or if it's deliberately deferred), the next code step is B4** — sequencing/reply-engine opt-out flow: hosted RFC 8058 one-click POST endpoint (the `TODO(B4)` at `apps/platform/src/engine/tick.ts:46-56`), campaign scheduling/rotation/stop-on-reply/suppression already contracted in SPEC, subject-honesty + lookalike-brand validators, A/B. This build also resolves the marketing-copy overclaim (softens or backs the existing site copy with a real endpoint) — don't ship the copy fix separately from the endpoint. Then re-attack with a fresh-context adversary and only commit on a clean pass.

**Then, and only with Yaakov's confirmation** (each is founder-owned or spends money):
1. Provision the engine host — verbally approved (~$6/mo droplet, "whatever size we need"); runbook in `ACTIVATION.md` Gate 2, now CLEARED.
2. Build the per-tenant `ENGINE_TENANTS` allowlist (if not already done per the check above).
3. Get his ruling on the mailbox provider (Inboxkit vs Mordy's own Google Workspace) and the ~$10 test domain — both block the Gate-1 real-send smoke, which blocks the Mordy pilot.
4. Pricing ruling, PulseMCP statement, and the other founder-gated queue items above — any of these can be picked up independently, none block each other.

Do NOT deploy, push, or spend without confirming with Yaakov first.
