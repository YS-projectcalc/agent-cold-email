# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` (v0.2.0, hosted remote + npm package) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold before acting.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · npm publish · real vendor spend · external send · DO support ticket) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-16)

**COMMITTED + PUSHED (founder-authorized 07-16):** local `main` = `d0e91ec`, in sync with origin — `f406132` (engine 443 transports) + `54ad72c` (directory readiness) + `6aec7a5` (ledger) + `bf3a927` (server.json 0.2.1, registry duplicate-version fix) + `3d7d27d` (poll bounded-fetch fix — CLOSED, adversary SHIP after 2 addenda) + `d0e91ec` (webhooks lane — CLOSED, adversary SHIP; MCP surface now 19 tools, was 17). **✅ GATE-1 SMOKE PASS 07-16:** first-ever real send proven — Mac-local engine, one self-send over 465, `/v1/send` 200, delivery IMAP-verified, Message-IDs match (see `ROADMAP.md` SMTP-wall entry). That same smoke found the poll defect live, and it was closed the same session. **Founder OAuth mint DONE 07-16** (Gmail refresh token minted, used for the Gate-1 smoke, then securely deleted — not persisted, must be re-minted or founder re-supplied for the droplet deploy). Remaining chain to "provably live in production": droplet deploy (rsync new image + creds) → one droplet-side 443 send → ratified fully-live reframe. The four lanes, as committed:

1. **Gmail-API + MS-Graph HTTPS/443 send transports** (SMTP-wall path c — see `ROADMAP.md ## Open` 2026-07-16 [ORDER] "SMTP wall"). Files: `apps/engine/src/{config,engine,index,smtp}.ts` (modified) + new `api-send.ts`/`gmail.ts`/`graph.ts`/`http.ts`/`message.ts`/`oauth.ts`/`scripts/mint-gmail-token.mjs` + their tests, `apps/engine/README.md`. Adversary SHIP, zero blockers (`docs/adversarial/engine-443-transports-2026-07-16.md`); bookkeeper-verified: engine typecheck exit 0, 62 tests passed / 3 skipped (docker-gated GreenMail).
2. **MCP Connectors-Directory readiness bundle** (see `ROADMAP.md ## Open` 2026-07-16 [ORDER] "Become THE recommended pick"). Files: `apps/platform/src/mcp/{tools,handler}.ts`, `apps/platform/src/engine/{mailbox-state,provisioning,dashboard-views}.ts` + their tests, `packages/cli/{README.md,src/index.ts,src/commands/signup.ts,src/claude-code-hint.ts}` (new), `.claude-plugin/plugin.json` (new). Adversary cycle r1 NO-SHIP → fix rounds → reattack caught 1 new blocking regression → round-3 fix → ADDENDUM SHIP (`docs/adversarial/directory-readiness-2026-07-16.md` + `-reattack-2026-07-16.md`); that doc records platform `npm test` 362/362, typecheck exit 0.
3. **`/v1/poll` unbounded first-fetch fix** (found live by the Gate-1 smoke — see `ROADMAP.md ## Open` 2026-07-16 [ORDER] "poll unbounded first-fetch defect", now CLOSED). Files: `apps/engine/src/engine.ts` + README, `apps/platform/src/engine/provisioning.ts`, `schema.ts`, `tenant-do.ts`, `test/idempotency.test.ts`. Adversary NO-SHIP → round-2 fix → re-attack SHIP → re-grounded SHIP after HEAD advanced mid-session (`docs/adversarial/poll-bounded-fetch-2026-07-16.md`, 2 addenda); engine 68 passed/3 skipped, platform 407 passed/62 files, both typecheck exit 0.
4. **Per-tenant outbound webhooks** (reply/bounce/complaint push — see `ROADMAP.md ## Open` 2026-07-16 "WIN-THE-COMPARISON (d)" / "forensics §5 (c)", both now closed). Files: `apps/platform/src/engine/{webhooks,webhook-delivery,webhook-enqueue,webhook-security}.ts` (new) + `routes/webhook-subscriptions.ts` (new) + `mcp/{tools,schemas}.ts`, `schema.ts`, `tenant-do.ts`, `scheduled.ts`, `index.ts`, `admin/ops-sweep.ts`, `engine/reply-processor.ts`. Adversary SHIP + SSRF re-attack SHIP (`docs/adversarial/webhooks-lane-2026-07-16.md`); platform 420/420, typecheck + dry-run build clean. Closes the buyer-checklist hard-gate on real reply/bounce push; capability-matrix/site copy still needs the follow-up pass (deferred, joins the fully-live reframe copy batch).

All four lanes are fully committed and pushed — the only files still dirty in the working tree are this bookkeeping pass (`ROADMAP.md`, `HANDOFF.md`) plus `.claude/agent-memory/verifier/coldstart-platform-battery.md` and the poll-fix adversary doc's re-ground addendum (docs-only, non-blocking). Real sending is now unblocked **in code** via lane 1 (engine-443 transports) with the poll-defect risk from lane 3 also closed; arming still needs the droplet deploy + one live per-transport smoke (the one thing the adversary review flagged UNVERIFIABLE without real creds — the Gate-1 Mac-local smoke satisfied it for `smtp` only, `gmail_api`/`ms_graph` remain UNVERIFIABLE until the droplet-side send).

Findability work also moved: AI-crawler/Google probes done (`ROADMAP.md` 2026-07-16 [ORDER] "AI-crawler wall" — `site:coldrig.dev` = 0 results, CF bot-management unreadable with current token scopes) and Connectors-Directory research complete (same file, "Become THE recommended pick" entry) — both still gated on founder clicks, see `## Resume`.

## In flight / next

- **This session landed six commits total on `main`** (see `## Where we are right now`), all pushed to origin — do not assume other in-flight lanes are done just because these are.
- **Founder decision queue — updated 07-16, mirrors `ROADMAP.md ## Now` return-queue (six items, in order):** (1) headline ask now **"deploy the engine"** (or approve the rsync prompt) — new engine image + gmail_api creds to the droplet + ONE production 443 self-send, triggering the fully-live reframe; the prior gate — reading the droplet credential file — is DONE and the Mac-local Gate-1 smoke already passed; note the pre-staged image context predates the poll-fix/webhooks commits and needs a rebuild first; (2) name the mcp.so + Cline public-listing targets — the permission classifier blocks agent-initiated public submissions without his naming them; (3) re-run `mcp-publisher publish` (server.json now 0.2.1, duplicate-version fixed); (4) GSC day-2 Request-Indexing batch; (5) ChatGPT platform identity verification + 2-min in-browser read of openai.com/policies/usage-policies; (6) MCPB form check (Team-org-gated or not?). Two items from the prior checkpoint are now RESOLVED and dropped from the queue: the CF Security→Bots toggle (founder confirmed already set to allow AI bots) and the claude.ai org-tier question (founder ruling: SKIP the Team org for now).
- **Headline blocker now:** droplet-side send — DigitalOcean still blocks 465/587 (2525/8025 open, useful for managed-mailbox vendors); the 443 API transports (gmail_api/ms_graph) route around this entirely and are built + committed, just need the droplet deploy (queue item 1) + one live send to prove it in production. Mac-side send is UNBLOCKED and proven (Gate-1 smoke). Full detail `ROADMAP.md ## Open` 2026-07-16 [ORDER] "SMTP wall".
- **Non-destructive build lanes unaffected by the founder queue** (all test-mode, no spend, carried from the prior handoff — status unchanged this pass, verify directly in `ROADMAP.md ## Open` before resuming): BYO intake build (§20), warm-lead lifecycle deep dive, send-framing copy pass + annual-billing toggle. Webhooks core-platform lane SHIPPED this session (`d0e91ec`) — no longer open.
- **Other open decisions (unchanged from prior handoff):** Stripe live keys + quantity-billing migration · agency-bundle pricing ruling · BYO-mailbox pricing · review-site listings · dogfood-calls scope · `mcp-publisher publish` re-publish · Glama listing sync-click.

## Landmines / gotchas

- **⚠️ SMTP egress is blocked on the DO droplet** (465/587; 2525/8025 open) — the Mac's network is UNBLOCKED and Gate-1 smoke-proven (real self-send over 465, 07-16). Do NOT assume the DROPLET can send SMTP until it's deployed + smoked; the 443 API transports (gmail_api/ms_graph) route around the droplet block entirely and are the durable fix — still need one droplet-side live send to prove it in production.
- **`site/` deploys whatever is on disk**, not what's committed — `wrangler pages deploy site --branch main` uploads the local dir as-is (no git-based Pages integration). Tree is clean now, but re-check `git status` before any future site redeploy (bit twice before).
- **support-kb.ts:35-36 still quotes dead $299/$799 tiers** — contradicts the ratified $99 pricing; fix with the billing migration (`ROADMAP.md ## Open`).
- **`SPEC.md:102` header still reads "~8–12 tools"** — stale (actual 17); same staleness class the Glama shopper hit; fix at next SPEC touch (`ROADMAP.md ## Open` shopfront residual i).
- `apps/platform/vitest.config.ts fileParallelism:false` is REQUIRED; fresh-worktree verification needs `apps/platform/.dev.vars` + local D1 migrations or the suite shows phantom failures.
- **Gmail app password for yaakovscher@gmail.com sits on the droplet** `/root/mailboxes.json` (bearer-gated, world-reachable :8080 but can't even send) — rotate anytime and re-paste; nothing depends on it staying.
- Stale worktrees were pruned this handoff (deploy-wt, engine-wt, the merged design integrator). The designer's own worktree `~/Documents/Codex/2026-07-14/.../coldrig-design` is the user's, unrelated to main — leave it.
- **This is a shared/live worktree with multiple parallel-agent lanes** — the 07-16 four-lane batch (engine-443, directory-readiness, poll fix, webhooks) is now fully committed, but treat this as a standing pattern, not a one-off: before any `git add`, run `git status` and check what else is dirty — do not `git add -A` or commit one lane's files blind.

## Key files

- `SPEC.md` (§0 locks · §12/§12.1 economics · §18 pricing · §19 dashboard · §20 BYO) · `ROADMAP.md` (`## Now`/`## Open` ledger — source of truth for all open items) · `ACTIVATION.md` (owner-hands gates; Gate-2 engine host = the SMTP-wall context) · `CLAUDE.md` (project law).
- `apps/engine/` — the Node SMTP/IMAP daemon (committed `eb8ee42`). `apps/engine/dist/` — built; runnable from any SMTP-capable network for a local smoke (SMTP-wall path (b)).
- `tools/buyer-panel/` — the blind-shopper CHOICE harness; `runs/2026-07-15-claude-{canonical,starter,agency}.md` — the frozen evidence that we surface but get killed on "not live".
- `docs/adversarial/*-2026-07-15.md` — frozen SHIP verdicts (shopfront, tool-descriptions, guide-pages, compare-pages, watchtower-ops-email).
- `archive/2026-07-16-handoff-smtp-wall/prior-HANDOFF.md` — the resume state this replaced.

## Resume — KIND B: founder commit-confirm + a decision queue, with non-blocked build lanes available meanwhile

**First verify state still holds:** `git -C ~/dev/coldstart status -sb` (expect HEAD `d0e91ec`, in sync with origin, clean except this handoff's own docs edits) and re-read `ROADMAP.md ## Now`/`## Open` for the current [ORDER]/[gated:founder] entries. If HEAD differs or the working tree looks different, a commit or reset already happened out-of-session — re-derive state from `git log` before proceeding.

**Step 1 — the founder return-queue** (present all six from `ROADMAP.md ## Now`, do NOT act unilaterally; item 1 touches credentials + spend, items 2/4/5/6 are his outward-facing accounts/attestations):
- **(1) Droplet deploy — critical path, everything else below is independent:** say "deploy the engine" (or approve the rsync prompt) — new engine image + gmail_api creds to the droplet + ONE production 443 self-send. Clean image context is pre-staged from `bf3a927` but predates the poll-fix (`3d7d27d`) and webhooks (`d0e91ec`) commits — rebuild before deploying. The OAuth refresh token was minted + used for the Gate-1 smoke then securely deleted (not persisted) — re-mint or founder re-supply needed.
- **(2) Directory naming:** state the mcp.so + Cline public-listing targets directly — the classifier will not relay agent-initiated public submissions without it.
- **(3) Registry re-publish:** re-run `mcp-publisher publish` (server.json now 0.2.1, duplicate-version conflict fixed).
- **(4) Findability clicks:** GSC day-2 Request-Indexing batch (domain property + sitemap already verified/submitted per the AI-crawler-wall ROADMAP entry; the CF Security→Bots toggle question is RESOLVED — already set to allow AI bots).
- **(5) ChatGPT:** platform identity verification + a 2-min in-browser read of openai.com/policies/usage-policies.
- **(6) MCPB form check:** clau.de/desktop-extension-submission — confirm Team-org-gated or not.

**Meanwhile, a resuming chat may START (non-destructive, no founder input, test-mode):** the BYO-intake / warm-lead build lanes (`## In flight / next`) — webhooks already shipped this session, drop it from consideration. Dispatch a builder against the chosen lane; git READ-ONLY for subagents in the live worktree; adversary-gate before any commit; commit + deploy only with Yaakov's confirm.

**Then, only with Yaakov's explicit confirmation** (each founder-owned): the return-queue above · Stripe live keys + quantity-billing migration ruling · agency-bundle + BYO-mailbox pricing rulings · review-site listings · once the droplet-side 443 smoke passes (Mac-local smoke already done) → the early-access→fully-live reframe (`ROADMAP.md ## Open` 2026-07-15 [ASK]).

Do NOT deploy, push, publish, spend, file a support ticket, or send without confirming with Yaakov first.
