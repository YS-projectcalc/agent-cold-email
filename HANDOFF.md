# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE:** site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry 0.2.2 (24 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (below) explicitly covers it.

## Where we are right now (2026-07-24, SDN arming complete + billing arc — one live build lane)

**Standing authorization (founder, verbatim on the ledger):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere."* Plus arming authorization: arming steps execute autonomously (Keychain keys + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder. Full program definition: `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" entry (2026-07-22) — every step is now DONE except step (5), final claim-surface pass + IndexNow, which follows the quantity-billing build.

- **SDN screening is now LIVE against the real Treasury list — the last major "still sandbox/stub" piece is closed.** The relay branch (`966793b` + `7084081` throttle + `1da4307` pre-arm hardening) got adversary FINAL SHIP and merged `3b29fc5`, deployed `be7342c5`. Arming was executed: `SDN_INGEST_TOKEN` is a live Worker secret (confirmed via `wrangler secret list`) and the droplet side is reported armed too — a droplet cron (`17 6 * * *`) pushes the real SDN.CSV to `/admin/sdn/ingest`. The first live push found two real wire-shape bugs same-hour (Treasury answers HEAD 200 but redirects GET — fixed with `curl -L`; the real feed ends with a trailing 0x1A DOS-EOF byte the parser rejected — fixed with a tail-tolerant parser, interior corruption still fatal by design) — both fixes verified present in code, redeployed `71bfa830`. **I independently verified on remote D1 this pass:** `sdn_list_meta.active_version = "sdn-relay-1784888169248"`, `entry_count = 19241`, and `sdn_entries` holds exactly 19241 rows — the real list is loaded, not a stub. Full detail + the one unverifiable sub-claim (the recovery-email delivery itself, no inbox access from here): `ROADMAP.md ## Open` "SDN RELAY SAGA COMPLETE".
- **Quantity-billing: design arc CLOSED, build IN FLIGHT.** Founder delegated the committed-vs-active meter choice → design landed on the ACTIVE/PROVISIONED meter, closed a real double-billing bug found by the second adversary round (burn-replacement was pause-not-release, which double-billed the active meter — fixed via a bill-invariance governing rule), FINAL SHIP `5b1edf7`. Build is running in worktree `quantity-billing-20260724`, 3 of 6 increments landed (durable Stripe Prices, tier collapse to managed + two-item checkout, `syncMailboxQuantity`). This is the one active build lane right now.
- **Repo chores + billing-claims truth sweep both merged and deployed.** Chores (`5aa4154`): openapi pinned 3.0.3 (0 lint errors, live-verified), BYO tool-count row added, aeo-panel now tracks both brand names, dev-doc titles rebranded. Billing-claims sweep (`66928b9`, 11 files incl. machine-facing `llms.txt`/`server-card.json`): every "billing live" claim on the site is now accurate; site redeployed from this commit (`7c6c240d`).
- **Signup-auth SHIPPED (07-23):** Worker + site deployed, "Free sign up" live. §1.7b outbound gate PASSED (DKIM/SPF/DMARC all pass under `p=REJECT`) but placement is Gmail SPAM (new-domain reputation) — email-login advertising stays dark pending the "Not spam" click + accrued volume/time.
- **GA wave (OFAC screening + provisioning-core) merged and deployed earlier 07-23/24** — real InboxKit provisioning is reachable for paid+screened tenants; `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` are armed. Engine leg (tunnel + `GMAIL_OAUTH_GRANTS` + `ENGINE_TENANTS`) is the one arming piece still open — see In-flight.
- **Founder rulings (07-21/22, still governing; all on ledger):** OFAC-stubbed pilot YES (now superseded by the real-list arming above) · prewarm pilot buy WITHDRAWN · Mordy price 60% off · registrar = Cloudflare default · review-sites list-now/PH-defer + dogfood scope (delegated) · magic-link login + human signup + one-funnel "Free sign up" (shipped) · warm-lead Q1-Q6 ratified (built + deployed).

## In flight / next

- **Next action:** the ONLY live build lane is quantity-billing (worktree `quantity-billing-20260724`, 3/6 increments landed) — next increments are `remove_mailboxes` intent + the 5-scenario Stripe test-mode gate, then adversary → merge → battery → deploy → site pricing copy + IndexNow (the LAST program step).
- **Engine-leg arming tail, in leverage order:** (1) founder runs the `GMAIL_OAUTH_GRANTS` mint command (see Landmines) — classifier-blocked for the main loop; (2) founder does `cloudflared tunnel login` on the droplet, then main loop sets `ENGINE_BASE_URL` (https tunnel) + fresh `ENGINE_AUTH_SECRET` both sides per `ACTIVATION.md` steps 2/6; (3) `ENGINE_TENANTS` + gate-(e) throwaway-mailbox verify ride Mordy's first domain connect (InboxKit workspace still has zero domains).
- **Founder click queue, by leverage:** Glama Dockerfile build ★ (unblocks PR #10106) → "Not spam" click on "Your Coldrig sign-in link" → **re-point any saved Gmail filter to the new subject "SDN list load [failing/RECOVERED]"** (old subject "SDN list refresh failed" is stale) → `GMAIL_OAUTH_GRANTS` command → `cloudflared tunnel login` → GSC Request-Indexing on ~10 money pages → Bing Webmaster verification-status check → Stripe dashboard support_email + public business name/descriptor "Coldrig".
- **Non-blocking, carried post-arm:** document a baseline-reset recovery path for the (low-probability) case of a genuine >10% single-day SDN delisting tripping the monotonicity guard's stale-rejection.
- **Watchdog:** the 3h session-only cron has not been confirmed re-armed since the 07-22 classifier denials — verify/re-arm from a session where `CronCreate` passes, or get the founder to allow the tool.
- **Open founder items (none block the lanes):** www.coldrig.dev custom-domain click (NXDOMAIN — and the Stripe business profile lists `https://www.coldrig.dev`!) · uptime-prober service name · Cline test [gated:founder-hold] · Instant Start SKU price ratification (gated on credits→USD verify).

## Landmines / gotchas

- **Live money is now real (as of 2026-07-23).** The Worker runs LIVE Stripe keys (flip executed + verified) — NEVER complete a live checkout in testing; a completed session charges a real card. `MORDYPILOT` is live-mode, single-use, and now consumable by anyone with the code — the test-mode twin was already consumed proving the flow. Never print Keychain values; read via `security find-generic-password -a coldrig -s <service> -w`.
- **InboxKit is ARMED and real provisioning is now REACHABLE for paid+screened tenants.** The provisioning-core GA merge (`8674da7`) wired `inboxKitConfig` into the DO factory call — so a tenant that goes paid + active + OFAC-screened-clear will now trigger REAL InboxKit vendor calls, not a sandbox stub. Containment: $150 vendor-spend ceiling, slot cap, G5 domain hard-block, and — as of this bookkeeping pass — zero tenants have actually reached a real-send-activated state.
- **SDN screening now runs against the REAL Treasury list — never manually swap SDN lists without the monotonicity/baseline context.** `sdn_entries` holds 19,241 real rows (verified this pass) loaded via the droplet-relay ingest (`3b29fc5` → `71bfa830`). The ingest has a content-hash replay guard AND a monotonicity guard that rejects any single-step entry-count drop of >10% as `stale` (by design — this is what makes the system safe against a corrupt/truncated push). If you ever need to hand-load or replace the list (e.g. recovering from a bad state), you MUST go through the same ingest path with the same guards — a raw D1 write that bypasses `swapInSdnList`/the monotonicity check could silently accept a corrupt or truncated list. A genuine large legitimate delisting (>10% drop) would ALSO get rejected by that guard — this is a known, accepted, non-blocking gap (see the SDN-relay Open entry); don't "fix" it ad hoc without reading that context first.
- **Worktrees: only `quantity-billing-20260724` is an active (unmerged) lane.** `sdn-relay-20260724` is fully MERGED (confirmed via `git merge-base --is-ancestor` — its tip `1da4307` is a parent of the `3b29fc5` merge) but its worktree directory is still sitting on disk, unreaped — don't mistake it for an in-flight lane. All other 07-23 worktrees (brand-sweep, g5-gate-a, test-env-hermetic, I3+I4, signup-auth, provisioning-core, ofac, fixture-fix) plus the dead `agent-a3b6ad8f1d279ebb6` are merged/superseded and reap-ready. A `sdn-tail-fix-20260724` branch ref also exists (its commit `7d111cb` is merged via `080b3fd`) with no corresponding worktree directory — already reaped, just a dangling branch name.
- **Non-blocking residual carried from I3+I4 (07-23, still open):** a crash between the `released_at` UPDATE and the revoke call can leave a single mailbox's vendor token un-revoked (skipped on retry via the `released_at IS NULL` filter) — same class as the founder-accepted Gate-2 crash residual, narrower than the original never-revoked bug. Optional hardening: revoke before the UPDATE.
- **Non-blocking, post-arm: baseline-reset recovery doc for SDN delistings is unbuilt** (see above landmine) — the frozen `docs/adversarial/sdn-relay-review-2026-07-24.md` recommends it; low-probability, not urgent.
- **Simulate endpoint:** `GET /checkout/simulate` 404s whenever Stripe OR engine is armed (`isRealSpendArmed`) — expected, not a bug. Adding any vendor env binding without extending that guard trips `apps/platform/test/spend-armed-env-coverage.test.ts` RED by design.
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20` or POST /v1/promotion_codes rejects `coupon` (see repo `MEMORY.md`).
- **Site deploys whatever is on disk** (`npx wrangler pages deploy site --project-name=agent-cold-email --branch=main`) — check `git status` first. Deploy-ordering law: Worker before site whenever counts/claims change.
- **Gov/vendor download URLs: pin the FINAL host, never a redirect front-end** — treasury.gov's redirect front-end previously rejected the Workers TLS/fetch handshake (HTTP 525) even though a normal client follows the redirect fine; the relay's droplet-side `curl` hit a related-but-distinct quirk (HEAD 200 vs GET 302) fixed with `curl -L`. Same class risk for any future fetch against a gov/vendor download endpoint, whether from a Worker or a droplet script.
- UNVERIFIED left: the SDN-relay "recovery email delivered" claim (no inbox access to check it directly — the D1 alert state is fully consistent with it, but I didn't see the email itself) and the droplet-side `/root/sdn-relay.env` file permissions (not re-checked over SSH this pass; the live successful ingest is strong indirect evidence both sides are correctly armed). Everything else this pass was command/API-verified: `wrangler secret list` (SDN_INGEST_TOKEN present, GMAIL_OAUTH_GRANTS still absent), `wrangler deployments list` (`71bfa830` current), remote D1 (`sdn_list_meta`/`sdn_entries`/`sdn_alert_state`), `redocly lint` (openapi clean), `git show --stat`/`git merge-base` (all cited SHAs and merge-ancestry). Prior history: `archive/2026-07-22-golive-program/prior-HANDOFF.md`.

## Key files

- `ROADMAP.md ## Now / ## Open` — single source of truth for every open item + the program entry. `archive/ROADMAP-done.md` — drained done items.
- `docs/adversarial/*-2026-07-2{1,2}.md` — frozen verdicts (committed as of `c8b9ee3`) · `docs/research/*-2026-07-2{1,2}.md` — frozen designs/research.
- `archive/2026-07-22-golive-program/` — prior-HANDOFF snapshot + promo-checkout E2E evidence (screenshots + browser script).
- `apps/platform/src/engine/activation.ts` (live activation gate) · `apps/platform/src/engine/billing.ts` (`isRealSpendArmed`) · `apps/platform/src/billing/stripe-client.ts` (inline pricing + required coupon constraints comment).

## Resume — KIND A: process the one remaining lane per the authorized program

Verify preconditions: `git -C ~/dev/coldstart status -sb` → main at/past `080b3fd`, in sync with origin. `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`. Confirm SDN is really live before assuming the arming above still holds: `cd apps/platform && npx wrangler d1 execute coldstart-platform-db --remote --command "SELECT COUNT(*) FROM sdn_entries;"` should read ~19,241+ (never fewer by more than the monotonicity guard's 10% tolerance — if it reads 0 or wildly different, something regressed, don't assume the last session's state still holds). `npx wrangler secret list` shows STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET/INBOXKIT_API_KEY/INBOXKIT_WORKSPACE_ID/SDN_INGEST_TOKEN present, GMAIL_OAUTH_GRANTS absent (never print values). Then: the ONLY in-flight lane is **quantity-billing** — worktree `.claude/worktrees/quantity-billing-20260724`, check `git log --oneline -10` in that worktree for progress past increment 3/6 (durable Prices → tier collapse → syncMailboxQuantity landed as of this handoff; next are `remove_mailboxes` intent + the 5-scenario Stripe test-mode gate). On all 6 increments landing: one adversary pass over the whole branch → merge → battery → deploy → site pricing-info pass + IndexNow (program step 5, the LAST program step — after this, the go-live program as originally scoped is complete). Separately, the engine-leg arming tail (`GMAIL_OAUTH_GRANTS` command + `cloudflared tunnel login`) is founder-gated and can be executed whenever the founder is present, independent of the billing lane. Founder-blocked items live in `ROADMAP.md ## Now`'s click queue — never work around the classifier on them. A lane that died at a usage limit: RESUME via SendMessage to its agent name — never re-dispatch its scope. Continue autonomously under the grant; stop only at browser-consent clicks or genuinely new founder-scope decisions.
