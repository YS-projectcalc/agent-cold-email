# ColdStart — Handoff

Agent-operated cold-email platform, BUILT + LIVE in test mode. Live: API https://agent-cold-email-api.yaakovscher.workers.dev · site https://agent-cold-email.pages.dev · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming ColdStart with zero prior context. Re-orient from the `## Resume` step below, then VERIFY its preconditions still hold** (the decisions listed as pending may have been made since this was written — check `ACTIVATION.md` Gate 0 first). **If they hold, proceed per Resume — don't ask open-endedly what to work on.** If anything has CHANGED, surface exactly what and ask before acting. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · real vendor spend · npm publish · external send) — this project's SPEC §0 locks NO real vendor spend until the owner works ACTIVATION.md.

## Where we are right now (2026-07-12)

- **AEO content arc: SHIPPED + LIVE + PUSHED.** Commit `3477e7f` on main, pushed to GitHub (`HEAD`==`origin/main`, verified). Site live on Cloudflare Pages — prod https://agent-cold-email.pages.dev, Production deployment `96a55be8` (verified via `wrangler pages deployment list`); all 4 new pages HTTP 200 via extensionless routing; RFC 8058 honesty fix across 12 surfaces confirmed via `git show 3477e7f` diff. Weekly citation-panel measurement ARMED (`coldrig-aeo-citation-panel-weekly`, Mondays 06:00 UTC, first run 2026-07-13, TREND rows land in `tools/aeo-panel/runs/TREND.md`). The engine-correctness increment was deliberately excluded from this commit. Full detail: `ROADMAP.md` 2026-07-12 (ship) session-log entry.
- **Engine round-2 fix lane: IN FLIGHT, uncommitted** in `apps/platform/` + `packages/shared/` (working tree currently has ~27 modified + 5 untracked files, including `migrations/0005_support_dedupe.sql` and new `engine/idempotency.ts`/test files). Gates before it gets its own commit: a clean verification battery + a fresh adversarial re-attack. Do not commit this work until that gate is clean, and do not deploy the worker before `wrangler d1 migrations apply` picks up 0005.
- **Build: COMPLETE (test-mode CORE) since 2026-07-09.** 130/130 tests, 3 adversarial panels remediated, all lanes live in sandbox (full pipe, AI deliverability loop, Stripe test-mode billing, MCP 12 tools + CLI + demo, support/dunning/digest, lifecycle). Worker v `13f8ee36`, D1 migrations through 0004, `STRIPE_*` unset by design (webhook fails closed). Details: `FINAL-REPORT.md` (point-in-time build report).
- **This session (07-12) was the founder-decisions round.** State of the 3 held decisions:
  1. **Name — DECIDED: coldrig.** Adversarial NO-SHIP on pure-keyword "agentcoldemail" as brand (`docs/adversarial/name-review-2026-07-12.md`): keep distinct brand + permanent `agent-cold-email` keyword slug. Brand word = **coldrig**, decided 2026-07-12 (verbal, in-session); founder registered `coldrig.dev` on Cloudflare the same day (`coldrig.com` registered/parked, `.net`/`.tech`/`.io`/`.sh` available at decision time). Attorney trademark clearance is the remaining open owner action — public display-brand rollout held for it, domain + infrastructure wiring may proceed.
  2. **Vendor resale model — DECIDED: Mailforge-first** at activation while pursuing an Inboxkit enterprise/reseller agreement in parallel; **option c APPROVED (07-12) as the ADDITIONAL premium "Dedicated" tier** (customer owns their Inboxkit account, we operate it) — fast-follow, build prereq = per-tenant `VendorPort` credentials. `ACTIVATION.md` Gate 0/1/2 updated; positioning + blast-radius rationale in `ROADMAP.md` final 2026-07-12 entries.
  3. **Pricing — DECIDED 2026-07-12 (verbal, in-session): tiers unchanged + deferred-paywall onboarding model.** Launch $99 / Growth $299 / Scale $799 signed off unchanged, together with a free no-card sandbox setup stage as the marketed onboarding path (payment gate at the go-live `/checkout` moment only) — full decision record: `ACTIVATION.md` Gate 0. ⚠️ Key findings behind the sign-off: Mailforge standalone all-in ≈ **$13.5/mbx/mo** (Warmforge warmup $10/mbx is NOT in the $3 headline); the Salesforge "unlimited Warmforge" bundle was then desk-verified **BUNDLE LIMITED** (99-cap + Whitelabel exclusion — `docs/research/warmforge-bundle-verification-2026-07-12.md`), so the margin basis is the **ramp-only warmup model: all-in ~$4.50–5.00/mbx, tiers clear ~2.6–3.3×**. Inboxkit direct-retail verified: $46/mo @5 mbx, $61 @10, $285–296 @50, API on all tiers. Full receipts: `docs/research/pricing-landscape-2026-07-12.md` + `docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md`.
- **Agent-search panel** (8 probes, 46 verbatim queries — note: the frozen doc's "44" line is an undercount, 46 is correct per `ROADMAP.md` correction pointer): agents search category keywords ("cold email MCP server" 8/8, "Instantly vs Smartlead" 8/8) but cite proper nouns — the empirical basis for the name structure. The derived SEO/AEO backlog WITH its arguments (tool-count-heuristic counter-page, assembly-question guide, maintenance signals, exact-phrasing guides) is frozen at `docs/research/aeo-backlog-2026-07-12.md` — pending owner green-light. Raw queries: `docs/research/agent-search-queries-2026-07-12.md`.
- **A5 engine spike DONE (2026-07-12).** `EmailPort` contract validated against GreenMail 2.1.3 (real SMTP+IMAP in Docker, $0) — all 5 contract behaviors (send/reply-detect/threading/bounce-classify/list-unsubscribe round-trip) satisfiable against a real server; root suite stayed 130/130. Spike code + README: `spikes/a5-engine-imap/`. Surfaced 4 findings that MUST resolve before the `VendorPort` freeze — checklist is in `ROADMAP.md`'s 2026-07-12 session log (missing List-Unsubscribe header on the send port, no hard/soft bounce flag + over-eager permanent suppression, real-IMAP thread reconstruction needs a real Message-ID stored on send, and no inbound-event dedupe key).
- **SEO/AEO backlog build (2026-07-12, uncommitted in `site/`).** Items 1/2/3/4/6 built (comparison pages, tool-coverage matrix, assembly-question guide, educational guides + domain calculator, vendor-blog-style retargeting); item 5 (maintenance signals) deliberately excluded — orchestrator-owned. Currently under fresh-context adversarial review; commit + deploy is founder-gated, not yet done.

## In flight / next

- Next action: check the engine round-2 fix lane's gate outcome → see `## Resume` below (single source for the exact steps).
- AEO content: **SHIPPED, LIVE, PUSHED** (see "Where we are right now" above) — no longer founder-pending. Frozen adversarial record: `docs/adversarial/aeo-content-review-2026-07-12.md` (both rounds, screenshots verified).
- Engine-correctness fix lane IN FLIGHT (uncommitted, `apps/platform/` + `packages/shared/`): A5 spike findings F1/F2/F4 (fixes) + F3 (adapter contract doc) GREEN-LIT by founder 2026-07-12 ("fix whatever needs fixing — do it right"); class-sweeps dispatched per the Bug Response protocol. Blocked on: soft-bounce streak semantics (1 blocking finding) + 6 non-blocking findings; gates = a clean verification battery + a fresh adversarial re-attack, then its own commit.
- Post-ship audit scheduled **2026-07-26** (founder-requested 07-12) — citation-panel trend / crawler analytics / indexation / design-pass QA / coldrig.dev wiring / funnel numbers / engine-guard re-check. Full checklist: `ROADMAP.md` 2026-07-12 (ship) session-log entry.
- Open decisions / blockers: (1) **Pricing — CLOSED, DECIDED 2026-07-12** (verbal, in-session): tiers unchanged (Launch $99/Growth $299/Scale $799) signed off on the ramp-only COGS basis, together with the deferred-paywall onboarding model (free no-card sandbox setup as the marketed onboarding path, payment gate at the go-live `/checkout` moment only) — recorded in `ACTIVATION.md` Gate 0. (2) **Brand word — CLOSED, DECIDED 2026-07-12**: **coldrig**, `coldrig.dev` registered on Cloudflare same day (`ACTIVATION.md` Gate 0, now fully closed). (3) **AEO content commit/deploy — CLOSED, SHIPPED 2026-07-12** (see above). Remaining open items are all owner-hands (domain wiring, CF Web Analytics/GSC/Bing, npm auth, design polish, standing brand-account authorization) — see `ROADMAP.md` 2026-07-12 (ship) entry — plus the engine round-2 gate above.

## Landmines / gotchas

- **SPEC §0 lock: NO real vendor spend / real sending until the owner works `ACTIVATION.md`.** Everything real-adapter throws `NotActivatedError` until wired.
- **Mailforge API is unconfirmed:** their pricing page omits it from included features; their ToS presupposes it exists. Do not build against it before the support confirmation (`ACTIVATION.md` Gate 1).
- **Warmforge/Salesforge "unlimited warmup" bundle: RESOLVED — BUNDLE LIMITED, do not rely on it** (ToS 99-accounts/workspace warmup cap + Whitelabel FAQ excludes Warmforge from the reseller option). The margin model runs on **ramp-only warmup** instead (~$4.50–5.00/mbx all-in — `docs/research/warmforge-bundle-verification-2026-07-12.md`); the drafted support inquiry there is optional upside only.
- The Mailforge 63%-placement claim in the research is **competitor-authored (Inboxkit)** — directional only.
- npm is NOT authed on this machine (npm publish = owner-hands, `ACTIVATION.md` Gate 3).
- Memory split (pre-existing): global memory (`~/.claude-acct2/.../memory/coldstart-platform-build.md`) holds session-recall state; repo `MEMORY.md` holds build lessons. Both are live; don't consolidate mid-handoff.
- Cron watchdog `baf7d82d` from the original build session was session-only w/ 7-day expiry — dead; ignore references to it.
- Engine increment: RESOLVED 2026-07-12 — committed + pushed as `d342cd0` after a CLEAN round-2 re-attack. Worker deploys must go through `npm run deploy` (the script now applies D1 migrations before `wrangler deploy` — 0005 must land before the new support-intake code).

## Key files

- `ACTIVATION.md` — the owner go-live checklist (Gates 0–4), updated 07-12 with decision state + 2 new Gate-1 verification items.
- `FINAL-REPORT.md` — the build report (frozen at 07-09; decision status has moved on — HANDOFF + ACTIVATION are current).
- `docs/research/agent-search-queries-2026-07-12.md` · `pricing-landscape-2026-07-12.md` · `vendor-costs-mailforge-inboxkit-2026-07-12.md` — frozen decision research (the 46-query AEO target list lives in the first).
- `docs/adversarial/name-review-2026-07-12.md` — name verdict (NO-SHIP pure-keyword; hybrid structure).
- `SPEC.md` — canonical design (§18 pricing, §13 vendors, §0 locks) · `ROADMAP.md` — ledger + session log · `ARCHITECTURE.md` · repo `CLAUDE.md` (project law) · `MEMORY.md` (build lessons).
- Deploy (when authorized): `cd apps/platform && wrangler deploy` · migrations `wrangler d1 migrations apply coldstart-platform-db --remote` · site `wrangler pages deploy site --project-name agent-cold-email`.

## Resume — KIND B: the next steps are founder-owned decisions (present, don't execute)

First verify preconditions: read `ACTIVATION.md` Gate 0 — if any of items 1–3 below is already marked decided there, skip it and move to the next. (Item 4 is a deploy approval, not a Gate-0 decision — tracked in this file's "In flight / next" instead.)

Present these to Yaakov, with the pre-computed recommendations:

1. ~~Brand word~~ — **DECIDED 07-12: coldrig** (`coldrig.dev` registered on Cloudflare same day, `coldrig.com` registered/parked, `.net`/`.tech`/`.io`/`.sh` available at decision time — see `ACTIVATION.md` Gate 0). No founder action needed now beyond attorney trademark clearance (open owner action); public display-brand rollout (site header, server-card display name, Stripe entity naming) held for it — domain + infrastructure wiring may proceed.
2. ~~Option-c additional premium tier~~ — **DECIDED 07-12: APPROVED** as the Dedicated tier, fast-follow (see "Where we are" #2.2). No founder action needed now; the per-tenant `VendorPort` credentials build item goes on the build queue when activation work starts.
3. ~~Pricing sign-off~~ — **DECIDED 07-12: tiers unchanged** (Launch $99 / Growth $299 / Scale $799, quotas in `packages/shared/src/pricing.ts`: 2dom/5mbx · 6/20 · 18/60) on the ramp-only COGS basis (~$4.50–5.00/mbx, clears ~2.6–3.3×), signed off together with the deferred-paywall onboarding model (see "Where we are" #3). No founder action needed now; full record in `ACTIVATION.md` Gate 0.
4. ~~AEO content commit/deploy approval~~ — **DONE, SHIPPED 2026-07-12**: commit `3477e7f`, pushed, deployed live to Cloudflare Pages (deployment `96a55be8`). See "Where we are right now" above.

All four Resume items are now closed. **Resume order for a fresh session:**
1. ~~Engine round-2 gate~~ — DONE 2026-07-12: re-attack CLEAN, committed + pushed `d342cd0` (suite 148/148). Nothing in flight in apps/ or packages/.
2. Owner-hands items from the 2026-07-12 ship (`ROADMAP.md` session log): domain wiring (CNAME + Pages custom-domain attach), CF Web Analytics + GSC/Bing verification, npm auth session, standing brand-account authorization decision.
3. Design polish pass (queued next session, per the 2026-07-26 audit item (d)).
4. 2026-07-26 post-ship audit (full checklist in `ROADMAP.md` 2026-07-12 (ship) session-log entry).

Work `ACTIVATION.md` top-to-bottom in parallel where unblocked (first hard gate before any paying customer: the real-world deliverability smoke test, Gate 1).
