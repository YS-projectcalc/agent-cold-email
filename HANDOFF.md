# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE:** site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry 0.2.2 (24 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (below) explicitly covers it.

## Where we are right now (2026-07-24, PROGRAM BUILD PHASE COMPLETE — zero active build lanes)

**Standing authorization (founder, verbatim on the ledger):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere."* Plus arming authorization: arming steps execute autonomously (Keychain keys + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder. Full program definition: `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" entry (2026-07-22) — **every build step is now DONE.** Confirmed this pass: `git worktree list` shows zero active worktrees against `~/dev/coldstart`.

- **Everything buildable is built and LIVE.** Quantity billing: live durable Stripe Prices + a provisioned-count meter (see below, independently re-verified this pass). SDN screening: real 19,241-entry Treasury list, refreshed daily via a droplet relay. InboxKit provisioning: armed behind a spend ceiling, slot cap, OFAC screening, and a domain hard-block. Magic-link recovery: shipped. Claim surfaces: swept for truth across the site and machine-facing docs. What remains is exclusively founder-gated clicks, Mordy-onboarding-gated steps, or ledgered post-GA follow-ons — see In-flight below, none of it is agent-buildable right now.
- **Quantity billing SHIPPED LIVE — this was the last build lane.** Design arc closed (founder delegated the committed-vs-active meter call → landed on ACTIVE/PROVISIONED; a second adversary round caught a genuine double-billing bug in burn-replacement, fixed via a bill-invariance governing rule; FINAL SHIP `5b1edf7`). Build (10 commits) merged `4e53e2d`. **Battery #7 caught a real class**: 817/817 tests green but `tsc` failed — the plan-tier collapse left 2 stale `"launch"` literals in a test file that only typecheck caught, not vitest; fixed `313963e`. **I independently re-ran both this pass:** `npm run typecheck` exits 0 across all 5 workspaces; platform suite is 817/817 (116 files) — exact match. Deployed `7f197f7d` (migration 0015 confirmed applied remote). **Live-verified via a read-only Stripe Price lookup (no checkout session created):** `coldrig_platform_monthly_v1` = $49.00/mo product "Coldrig Platform", `coldrig_mailbox_monthly_v1` = $10.00/mo product "Coldrig Mailbox", both `livemode:true` — a 5-mailbox checkout totals exactly $99.00. Full evidence + the API-breaking-change note: `ROADMAP.md ## Open` "QUANTITY BILLING SHIPPED LIVE".
- **SDN screening runs against the real Treasury list.** Relay branch merged `3b29fc5`, deployed through `71bfa830` after fixing two live wire-shape bugs (Treasury HEAD/GET mismatch; a trailing 0x1A DOS-EOF byte). `sdn_entries` verified holding 19,241 real rows. Full evidence: `ROADMAP.md ## Open` "SDN RELAY SAGA COMPLETE".
- **Repo chores + billing-claims truth sweep both merged and deployed.** Chores (`5aa4154`): openapi pinned 3.0.3 (0 lint errors, live-verified), BYO tool-count row added, aeo-panel tracks both brand names, dev-doc titles rebranded. Billing-claims sweep (`66928b9`, 11 files incl. machine-facing `llms.txt`/`server-card.json`): every "billing live" claim on the site is now accurate; site redeployed (`7c6c240d`).
- **Founder rulings (07-21/22, still governing; all on ledger):** OFAC-stubbed pilot YES (superseded — real screening is now live) · prewarm pilot buy WITHDRAWN · Mordy price 60% off · registrar = Cloudflare default · review-sites list-now/PH-defer + dogfood scope (delegated) · magic-link login + human signup + one-funnel "Free sign up" (shipped) · warm-lead Q1-Q6 ratified (built + deployed).

## In flight / next

- **In flight: NONE.** No agent build lane is currently running — the program's build phase is complete. Do not start new autonomous build work without a fresh founder-scoped ask; the remaining items below are gated on the founder or on Mordy's onboarding, not on further building.
- **Founder click queue, by leverage:** Glama Dockerfile build ★ (unblocks PR #10106) → "Not spam" click on "Your Coldrig sign-in link" → re-point any saved Gmail filter to the new subject "SDN list load [failing/RECOVERED]" (old subject "SDN list refresh failed" is stale) → `GMAIL_OAUTH_GRANTS` mint command (see Landmines; classifier-blocked for the main loop) → `cloudflared tunnel login` on the droplet (unblocks `ENGINE_BASE_URL` + a fresh `ENGINE_AUTH_SECRET` both sides, `ACTIVATION.md` steps 2/6) → GSC Request-Indexing on ~10 money pages → Bing Webmaster verification-status check → Stripe dashboard support_email + public business name/descriptor "Coldrig".
- **Mordy onboarding gates (unblocked by his signup, not by us):** domain connect on `authorpitchdesk.com` is the natural gate-(e) throwaway-mailbox probe (InboxKit workspace still has zero connected domains until then) · `ENGINE_TENANTS` allowlist arms at his signup · `MORDYPILOT` (60% off, single-use) is live and ready.
- **Post-GA follow-ons, ledgered non-blocking:** durable pre-send intent log before real GA sending volume (07-19 ruling, still required, unbuilt) · baseline-reset recovery doc for a >10% legit SDN delisting · event-driven alert class sweep (per-tenant-action storms) · agency-bundle billing design (deferred out of this arc) · buyer-panel cycle 3 (after the founder's Glama re-scan).
- **Watchdog:** the 3h session-only cron has not been confirmed re-armed since the 07-22 classifier denials — verify/re-arm from a session where `CronCreate` passes, or get the founder to allow the tool.
- **Open founder items (none block anything):** www.coldrig.dev custom-domain click (NXDOMAIN — and the Stripe business profile lists `https://www.coldrig.dev`!) · uptime-prober service name · Cline test [gated:founder-hold] · Instant Start SKU price ratification (gated on credits→USD verify).

## Landmines / gotchas

- **Live money is now real (as of 2026-07-23), on TWO durable Prices now.** The Worker runs LIVE Stripe keys — NEVER complete a live checkout in testing; a completed session charges a real card. `MORDYPILOT` is live-mode, single-use, and consumable by anyone with the code. Checkout now creates a subscription against durable live Prices (`coldrig_platform_monthly_v1` $49, `coldrig_mailbox_monthly_v1` $10 × mailbox count) rather than the old inline `price_data` — verified this pass via a read-only Stripe Price lookup (no session created). Never print Keychain values; read via `security find-generic-password -a coldrig -s <service> -w`.
- **⚠️ BREAKING API CHANGE: `POST /checkout` now REQUIRES a `mailboxes` count (5-60) and has no `plan` tier field at all.** The old tiered-plan checkout shape is gone (`packages/shared/src/intents.ts`, `CheckoutInput`). `site/openapi.yaml` and the MCP tool descriptions are confirmed already updated to the new `{ mailboxes, interval }` shape — but any EXTERNAL cached example (a customer's agent that memorized the old request shape, a screenshot, a third-party integration doc) will now 400. If you see a checkout failure that looks like a stale-shape request, check for a `plan` field in the body before assuming a real bug.
- **InboxKit is ARMED and real provisioning is REACHABLE for paid+screened tenants.** The provisioning-core GA merge (`8674da7`) wired `inboxKitConfig` into the DO factory call — a tenant that goes paid + active + OFAC-screened-clear now triggers REAL InboxKit vendor calls, not a sandbox stub. Containment: $150 vendor-spend ceiling, slot cap, G5 domain hard-block, and — as of this bookkeeping pass — zero tenants have actually reached a real-send-activated state.
- **SDN screening runs against the REAL Treasury list — never manually swap SDN lists without the monotonicity/baseline context.** `sdn_entries` holds 19,241 real rows (verified this pass) loaded via the droplet-relay ingest (`3b29fc5` → `71bfa830`). The ingest has a content-hash replay guard AND a monotonicity guard that rejects any single-step entry-count drop of >10% as `stale`. If you ever need to hand-load or replace the list, you MUST go through the same ingest path with the same guards — a raw D1 write that bypasses `swapInSdnList`/the monotonicity check could silently accept a corrupt or truncated list. A genuine large legitimate delisting (>10% drop) would ALSO get rejected by that guard — known, accepted, non-blocking (baseline-reset recovery doc unbuilt, see In-flight); don't "fix" it ad hoc without reading that context first.
- **Worktrees: ZERO active (confirmed via `git worktree list` this pass).** The quantity-billing worktree was reaped after merge. `sdn-relay-20260724` and all other 07-23 worktrees (brand-sweep, g5-gate-a, test-env-hermetic, I3+I4, signup-auth, provisioning-core, ofac, fixture-fix) plus the dead `agent-a3b6ad8f1d279ebb6` are already gone/reaped. If you see any worktree directory under `.claude/worktrees/` in a future session, treat it as new/unexpected, not a residual from this program.
- **Non-blocking residual carried from I3+I4 (07-23, still open):** a crash between the `released_at` UPDATE and the revoke call can leave a single mailbox's vendor token un-revoked (skipped on retry via the `released_at IS NULL` filter) — same class as the founder-accepted Gate-2 crash residual, narrower than the original never-revoked bug. Optional hardening: revoke before the UPDATE.
- **Simulate endpoint:** `GET /checkout/simulate` 404s whenever Stripe OR engine is armed (`isRealSpendArmed`) — expected, not a bug. Adding any vendor env binding without extending that guard trips `apps/platform/test/spend-armed-env-coverage.test.ts` RED by design.
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20` or POST /v1/promotion_codes rejects `coupon` (see repo `MEMORY.md`).
- **Site deploys whatever is on disk** (`npx wrangler pages deploy site --project-name=agent-cold-email --branch=main`) — check `git status` first. Deploy-ordering law: Worker before site whenever counts/claims change.
- **Gov/vendor download URLs: pin the FINAL host, never a redirect front-end** — treasury.gov's redirect front-end previously rejected the Workers TLS/fetch handshake (HTTP 525) even though a normal client follows the redirect fine; the relay's droplet-side `curl` hit a related-but-distinct quirk (HEAD 200 vs GET 302) fixed with `curl -L`; the real feed also has a trailing 0x1A DOS-EOF byte, tail-tolerant-parsed. Same class risk for any future fetch against a gov/vendor download endpoint.
- **Split typecheck/vitest discipline caught a real class this pass:** a plan-tier collapse left 2 stale string literals that vitest's runtime never exercised but `tsc` caught — always run BOTH `npm test` and `npm run typecheck` before calling a battery clean; a green test suite alone is not sufficient evidence.
- UNVERIFIED left: the SDN-relay "recovery email delivered" claim (no inbox access to check it directly — the D1 alert state is fully consistent with it) and the droplet-side `/root/sdn-relay.env` file permissions (not re-checked over SSH; the live successful ingest is strong indirect evidence). Everything else across this program's final two passes was command/API-verified: `wrangler secret list`/`deployments list`, remote D1 (`sdn_list_meta`/`sdn_entries`/`sdn_alert_state`), live Stripe Price lookup by `lookup_key`, a fresh `npm test` (817/817) and `npm run typecheck` (0 errors) run, `redocly lint`, `git show --stat`/`git merge-base`/`git worktree list`. Prior history: `archive/2026-07-22-golive-program/prior-HANDOFF.md`.

## Key files

- `ROADMAP.md ## Now / ## Open` — single source of truth for every open item + the program entry. `archive/ROADMAP-done.md` — drained done items.
- `docs/adversarial/*-2026-07-2{1,2}.md` — frozen verdicts (committed as of `c8b9ee3`) · `docs/research/*-2026-07-2{1,2}.md` — frozen designs/research.
- `archive/2026-07-22-golive-program/` — prior-HANDOFF snapshot + promo-checkout E2E evidence (screenshots + browser script).
- `apps/platform/src/engine/activation.ts` (live activation gate) · `apps/platform/src/engine/billing.ts` (`isRealSpendArmed`) · `apps/platform/src/billing/stripe-client.ts` (durable-Price bootstrap via `ensureStripePrices`/`lookup_key` — NO LONGER inline `price_data`, that shape retired by the quantity-billing build).

## Resume — KIND B: program build-complete, process founder clicks + Mordy onboarding

Verify preconditions: `git -C ~/dev/coldstart status -sb` → main at/past `313963e`, in sync with origin. `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`. `git worktree list` should show ZERO worktrees under `.claude/worktrees/` — if you see one, something new started after this handoff and supersedes the "build-complete" framing below; investigate before assuming it still holds. Confirm SDN is still live: `cd apps/platform && npx wrangler d1 execute coldstart-platform-db --remote --command "SELECT COUNT(*) FROM sdn_entries;"` should read ~19,241+ (never fewer by more than the monotonicity guard's 10% tolerance). `npx wrangler secret list` shows STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET/INBOXKIT_API_KEY/INBOXKIT_WORKSPACE_ID/SDN_INGEST_TOKEN present, GMAIL_OAUTH_GRANTS absent (never print values). If all of that holds: **there is no build lane to resume** — the program's build phase is complete. Process the founder click queue and Mordy-onboarding gates in `ROADMAP.md ## Now` in leverage order; do not start new autonomous build work without a fresh founder-scoped ask (post-GA follow-ons listed in `## Now` are candidates for a NEW dispatch, not automatic continuations). Founder-blocked items live in `ROADMAP.md ## Now`'s click queue — never work around the classifier on them. A lane that died at a usage limit: RESUME via SendMessage to its agent name — never re-dispatch its scope. Continue autonomously under the grant; stop only at browser-consent clicks or genuinely new founder-scope decisions.
