# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE:** site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry 0.2.2 (24 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (below) explicitly covers it.

## Where we are right now (2026-07-23, go-live program — wave shipped live, next wave building)

**Standing authorization (founder, verbatim on the ledger):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere."* Plus arming authorization: arming steps execute autonomously (Keychain keys + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder. Full program definition: `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" entry (2026-07-22) — steps (1) and (2) are now marked DONE.

- **Shipped 2026-07-23:** the wave at `485a4fe` (ledger commits through `116a058`) is LIVE. Three lanes merged + adversary-SHIP'd: I3+I4 credential path (`5b151e7`, r1 SHIP-AFTER-FIXES → fix `6f46b7e` → r2 SHIP), brand sweep (`60275b3`, SHIP), G5 gate(a) (`91f8227`, SHIP — closes domain-registrar arming decoupling), hermetic test-env fix (`bca6db9`, SHIP — closed a real battery-FAIL class: ambient `.dev.vars` STRIPE key was flipping checkout tests to real-Stripe mode under test). Integration `485a4fe` pinned REGISTRAR_* into the hermetic guard + a generic 503 body. wave-verifier battery on the integrated tree: typecheck 5/5 clean, dashboard 99/99, engine 95p/3s, platform **648/648** (93 files), CLI 9/9 — checkout.test.ts GREEN with the real ambient STRIPE key present (the prior FAIL condition). Deploy was blocked once by the auto-mode classifier; founder authorized in-session via AskUserQuestion ("Deploy now — I authorize it") → `npm run deploy` succeeded: Worker version `3f7acce1-bfb6-4c46-8e88-62ec4ced9fce`, live-verified `/status` ok + simulate 404 + tools/list 24.
- **LIVE STRIPE KEY FLIP EXECUTED:** STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET set from Keychain (`coldrig/stripe-live-*`). Live verification: signup test tenant `ten_248885a6-dc4b-45e8-8f3d-1c427f8e1ad7` ("Coldrig Flip Verify (internal)") → POST /checkout → `cs_live_` session, Stripe API read-back: **livemode:true**, amount_total 9900, line-item "Coldrig Launch" (brand fix verified on the live money surface). Session left open — NO charge completed. `MORDYPILOT` (60% single-use) now redeemable.
- **Adversary residuals ledgered (non-blocking, carried forward):** revoke-after-release crash window (Gate-2 class); N-G5-2 tick-sweep replace-domain path lacks RegistrarUnarmedError isolation (dark until `inboxKitConfig` wires into the DO factory call); ga-gates informational — INBOXKIT_* secrets alone do NOT enable real provisioning via the factory (no caller passes `inboxKitConfig` yet) — the arming sequence must account for this.
- **Session context:** this session resumed after the 07-22 session death; all 4 dead lanes were relaunched; the watchdog cron re-arm was DENIED by the classifier (no cross-gap resume net this session); signup-auth + GA-gates designs were both adversary-cleared with fixes applied same day (frozen verdicts in `docs/adversarial/*-2026-07-23.md`).
- **Founder rulings (07-21/22, still governing; all on ledger):** OFAC-stubbed pilot YES · prewarm pilot buy WITHDRAWN (no Instantly deadline; prewarmed lookalikes impossible by construction) · Mordy price 60% off · registrar = Cloudflare default (delegated choice) · review-sites list-now/PH-defer + dogfood scope (delegated) · magic-link login + human signup + one-funnel "Free sign up" (design adversary-cleared) · warm-lead Q1-Q6 ratified (built + deployed).

## In flight / next

- **Next action** (one-line; exact steps in `## Resume`): signup-auth build is the active lane (worktree `signup-auth-20260723`); process per `ROADMAP.md ## Open` "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" step (3).
- **In flight:** signup-auth builder in worktree `signup-auth-20260723` (increments A/B, unblocked at the I3+I4 merge `5b151e7`; design adversary-cleared `docs/adversarial/signup-auth-design-review-2026-07-23.md`). **§1.7b empirical outbound gate is a MAIN-LOOP step, not a builder step** — once increment-A deploys, verify from the main loop: send a real magic-link from login@coldrig.dev to a yaakovscher@gmail.com tenant, then droplet-IMAP read of the delivered message's `Authentication-Results` header — must show `dkim=pass d=coldrig.dev` and `dmarc=pass` (DMARC on coldrig.dev is live `p=reject`, so a misaligned sender hard-fails silently). On FAIL, flip the sender to ops@ and re-gate; the site funnel flips live ONLY after PASS.
- **After signup-auth lands (program order):** GA-gates build (G0→G2→G4, G1a/G1b, G3; design adversary-cleared `docs/adversarial/ga-gates-design-review-2026-07-23.md`; G5 gate(a) micro-lane already merged `91f8227`) → ARMING (tunnel + `ENGINE_BASE_URL` + `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID`/`GMAIL_OAUTH_GRANTS` secrets + gate-(e) throwaway-mailbox verify — ⚠️ INBOXKIT_* secrets alone do NOT enable real provisioning: no caller passes `inboxKitConfig` into the DO factory yet, wire that call before assuming secrets alone are sufficient) → final coordinated claim-surface/info pass everywhere + IndexNow.
- **Watchdog:** the 3h session-only cron was NOT re-armed this session — `CronCreate` was denied twice by the auto-mode classifier. A fresh session has no cross-gap resume net; re-arm from a session where the classifier passes it, or get the founder to allow the tool.
- **Open founder items (none block the lanes):** www.coldrig.dev custom-domain click (NXDOMAIN — and the Stripe business profile lists `https://www.coldrig.dev`!) · uptime-prober service name · GSC sitemap/Request-Indexing + Bing clicks · Cline test [gated:founder-hold] · Instant Start SKU price ratification (gated on credits→USD verify).

## Landmines / gotchas

- **Live money is now real (as of 2026-07-23).** The Worker runs LIVE Stripe keys (flip executed + verified, see above) — NEVER complete a live checkout in testing; a completed session charges a real card. `MORDYPILOT` is live-mode, single-use, and now consumable by anyone with the code — the test-mode twin was already consumed proving the flow. Never print Keychain values; read via `security find-generic-password -a coldrig -s <service> -w`.
- **Worktrees reap-ready (merged, safe to prune):** `brand-sweep-20260723` (merged `60275b3`), `g5-gate-a-20260723` (merged `91f8227`), `test-env-hermetic-20260723` (merged `bca6db9`), `agent-a8f87cd1437a20f72` (I3+I4, merged `5b151e7`). The dead LOCKED worktree `agent-a3b6ad8f1d279ebb6` (a prior brand-sweep attempt, abandoned mid-write when the 07-22 session died) is SUPERSEDED by the merged sweep and is also reap-ready — verified 2026-07-23: it holds exactly 3 uncommitted partial files (`apps/platform/src/billing/stripe-client.ts`, `apps/platform/test/sql-raw.d.ts`, `apps/platform/test/stripe-client-checkout.test.ts`), none of which landed anywhere.
- **Active worktree, do not touch:** `signup-auth-20260723` (in-flight builder).
- **Simulate endpoint:** `GET /checkout/simulate` 404s whenever Stripe OR engine is armed (`isRealSpendArmed`) — expected, not a bug. Adding any vendor env binding without extending that guard trips `apps/platform/test/spend-armed-env-coverage.test.ts` RED by design.
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20` or POST /v1/promotion_codes rejects `coupon` (see repo `MEMORY.md`).
- **Site deploys whatever is on disk** (`npx wrangler pages deploy site --project-name=agent-cold-email --branch=main`) — check `git status` first. Deploy-ordering law: Worker before site whenever counts/claims change.
- **`site/agent-evaluation.md` still says "Stripe cannot take money yet"** — now FALSE as of the live-key flip; the program's final info pass (step 5) must correct it (claim-surface class — sweep by inventory, not memory).
- **INBOXKIT_* secrets alone are insufficient.** No caller passes `inboxKitConfig` into the DO factory yet (ga-gates-adversary, informational) — setting the arming secrets does NOT itself enable real provisioning; the arming sequence must wire that call.
- UNVERIFIED left: none material — live claims above were command/API-verified at write time (Worker version `3f7acce1`, `/status` ok, simulate 404, tools/list 24, Stripe `livemode:true` read-back). Prior history: `archive/2026-07-22-golive-program/prior-HANDOFF.md`.

## Key files

- `ROADMAP.md ## Now / ## Open` — single source of truth for every open item + the program entry. `archive/ROADMAP-done.md` — drained done items.
- `docs/adversarial/*-2026-07-2{1,2}.md` — frozen verdicts (committed as of `c8b9ee3`) · `docs/research/*-2026-07-2{1,2}.md` — frozen designs/research.
- `archive/2026-07-22-golive-program/` — prior-HANDOFF snapshot + promo-checkout E2E evidence (screenshots + browser script).
- `apps/platform/src/engine/activation.ts` (live activation gate) · `apps/platform/src/engine/billing.ts` (`isRealSpendArmed`) · `apps/platform/src/billing/stripe-client.ts` (inline pricing + required coupon constraints comment).

## Resume — KIND A: process the in-flight lane per the authorized program

Verify preconditions: `git -C ~/dev/coldstart status -sb` → main at/past `116a058`, in sync with origin (skill-produced doc commits may sit above it). `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`. Confirm the live-key-flip precondition holds before treating Stripe as live: `cd apps/platform && npx wrangler secret list` should show `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` present (never print values). Then check the in-flight lane: if this is the SAME session, the task list / notifications carry it; if this is a FRESH session (task list gone), check on disk instead — `git -C ~/dev/coldstart/.claude/worktrees/signup-auth-20260723 log --oneline -5` for build progress, or does a `docs/adversarial/signup-auth-build-review-2026-07-2*.md` (or similarly dated) verdict already exist? For the delivered lane, process per `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" step (3): adversary verdict gates the merge; merge → battery → deploy per the deploy-ordering law. §1.7b's empirical outbound gate is a MAIN-LOOP step (droplet-IMAP DKIM/DMARC read), not a subagent step — run it after increment-A deploys. A lane that died at a usage limit: RESUME it via SendMessage to its agent id — never re-dispatch its scope. Continue autonomously under the grant; stop only at browser-consent clicks or genuinely new founder-scope decisions.
