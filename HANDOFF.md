# ColdStart / coldrig — Handoff

Agent-operated cold-email platform (28 MCP tools). **LIVE, FIRST PAYING CUSTOMER Mordy (2026-07-28).** Site https://coldrig.dev · API https://api.coldrig.dev + https://agent-cold-email-api.yaakovscher.workers.dev · repo https://github.com/YS-projectcalc/agent-cold-email (PUBLIC) · Code: `~/dev/coldstart/`. **Standing autonomy (founder, 2026-07-22): merge/push/deploy/arm to customer-ready is authorized; browser-consent clicks excepted. 2026-08-14 additions: Claude holds the prod `ADMIN_TOKEN` (Keychain `coldrig/admin-token`) and OWNS acting on watchtower alerts; 2026-08-16: founder authorized browser-driven visibility work ("do whatever you can").**

> **You are resuming `coldrig` with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (current Worker version, whether Mordy's agent finally reacted). If they hold and the step is non-destructive, proceed. **STOP and confirm before any destructive/founder-owned step** — EXCEPT where the standing autonomy grant covers it (merge/push/deploy/arm).

## Where we are right now (2026-08-18)
-1. **Vendor-truth wave SHIPPED TO PRODUCTION 2026-08-18** (supersedes item 0 below as the current deploy). Merged `a0fd314` (classes A-F: three-valued vendor grading + `operatorActionable` + honest emission, E1 warmup pre-check, release idempotency, E4 per-candidate exclusion, zod seam + `getHealth` truth, `vendor_wallet` + orphan watchtower checks, provisioning-state truthful reads, `ackedAt` rename). Deployed Worker `54b00702` both hosts (confirmed latest via `wrangler deployments list`, created 2026-08-18T20:52:42Z) + site Pages `62115561` (Production, `main`, commit `a0fd314`, confirmed via `wrangler pages deployment list`); `/status` 200 both hosts this pass. Gate doc **RESTORED and verified this pass**: `docs/adversarial/vendor-truth-gate-2026-08-18.md` (re-emitted post-reap; original was untracked in the reaped lane worktree — authentic re-emission by the gate agent from its own context, committed `629a20e`, unpushed local-`main`-only as of this pass) reads SHIP, 0 blocking, 6 non-blocking (NB-1..NB-6, folded into `ROADMAP.md ## Open`), battery `190 files/1831 tests passed/1 skipped, exit 0`. Worktrees `vendortruth`/`vendorwatch` reaped, branches `feat/vendor-truth-2026-08-18`/`feat/vendor-watch-2026-08-18` preserved. **Incident RESOLVED same day, per the gate's own LIVE-STATE CORRECTION (authoritative timeline):** the Mordy InboxKit-wallet-empty incident (tickets `sup_8a486bb0`/`sup_1b6746fc`) went diagnosis → founder wallet top-up + auto-topup → his agent's retry settling `apd-setup-a-2mbx` to `done` at **2026-08-18T18:22:37Z**, UNDER THE OLD CODE, before this deploy (`mordytee12` warmup enrolled 18s later, wallet credits 35→38). At gate-probe time `goauthorpitchdesk.com` still read `dnsStatus:"pending"`/0 mailboxes; a further claimed setup-B (~19:14Z, 2 mailboxes bought there, DNS flipped READY) postdates the gate's probe — orchestrator-reported, not independently re-verified against live admin reads this pass (Keychain-probe classifier-blocked). Warmup-semantics ticket `sup_71e8b4d1` answered; his side is now running **fully unattended agent-to-agent** (ticket `sup_dce385a8` retracted by `sup_9d2c9a3a`, registrar_unarmed wording feedback). **Customer-continuity program:** design done (`docs/research/customer-continuity-design-2026-08-18.md`), founder rulings captured (Q1 one-nudge/1-day/in-product-only, Q2 per-domain distribution BUILD-authorized, Q3 blame-split alert channels), design gate round 1 = SHIP-AFTER-FIXES (14 blocking + 9 non-blocking, `docs/adversarial/customer-continuity-design-gate-2026-08-18.md`), v2 in flight → gate round 2 IN PROGRESS as of this pass.
0. **Product-hardening TRAINS 1+2 SHIPPED TO PRODUCTION 2026-08-18** (supersedes item 6 below on the F1 point). Worker `3c549e5e` live on both hosts (verified `/status` ok, 28 MCP tools, new tool descriptions on the wire); site deployed Pages `1ea77c9e`, live `openapi.yaml` byte-identical to committed; `origin/main` at `2cef30d`, 0 ahead/behind. Wave content: TRAIN 1 (channel-truth — F1 Settled contract: non-terminal outcomes never recorded; capacity_pending discriminator; alert-delivery truth at all 3 sweep-named sites incl. dead-man + d1 decide/send/commit split; recovery-basis typing; operator-notified honesty; retry-dedup downgrade; teardown invalidates recorded responses; shared result vocabulary `packages/shared/src/provenance.ts`) + TRAIN 2 (loop-isolation — `forEachIsolated` at 9 sites + coverage tripwire) + the N1 arc (absolute keyed remove via durable per-key remove-intent `mailbox_release_intents`, `releaseMailboxes {limit}→{ids}`, chunked at the 100-param ceiling). Four-round adversary gate: R1 NO-SHIP (B1 merge-semantic, B2 member-5 2-of-3-sites, B3 partial-release-replays-forever) → R2 NO-SHIP (N1 unbounded over-release, measured 12-destroyed-for-ask-3) → R3 SHIP-AFTER-FIXES (R3-1 100-param ceiling at count≥21) → R4 SHIP (21/60 drives verified at the ceiling). Frozen `docs/adversarial/wave-1-2-integration-gate-2026-08-18.md`. Batteries: 186f/1766p/1skip + main-HEAD re-run exact match, typecheck clean, all real (non-piped) exit codes. Second operator notice sent to Mordy's agent ~08:55Z 2026-08-18 (deploy-complete, retry-same-key-now, spend-safe), 201. Lane worktrees (channelfix/loopfix/msgread) reaped, branches preserved. **The old F1-trap watch line is now OBSOLETE:** the Settled contract means a done row is always a genuinely finished outcome — do not carry item 6's "F1 trap ARMS" framing forward.
1. **Vendor-verdict class fix SHIPPED + DEPLOYED 2026-08-14** (the founder-ordered post-ship re-attack found a BLOCKING live defect — dead vendor registration read as eternal "provisioning in progress"; closed as a 15-member class through a 3-round adversary gate). Frozen: `docs/adversarial/{c3-postship-reattack,vendor-verdict-class-sweep,vendor-verdict-gate}-2026-08-14.md`. Merged `e5598da`, wave closed `d6cc8ce`.
2. **`ADMIN_TOKEN` rotated into Keychain `coldrig/admin-token` (standing access) and the operator reply SENT to Mordy's agent** (201, `regarding sup_3ca260e4`: retry same key `apd-setup-a-2mbx`, spend-safe). **His agent has been SILENT since (>48h)** — recommend the founder nudges Mordy the human; the watch pages on any reaction.
3. **`GET /admin/ops/checks` SHIPPED + DEPLOYED** (gate SHIP-WITH-NOTES; two-store caveat: `d1`/`cron_sweep` dead-man live in WatchtowerDO — pair with `GET /status`). **Current live Worker version: `c6b658c4`** (latest deploy, superseding `a507f12a`; `/status` 200 throughout the session).
4. **Alert-policy wave SHIPPED + DEPLOYED 2026-08-16** (founder AskUserQuestion ruling: "Debounce + daily re-alerts"). Fresh-context adversary gate: SHIP, 0 blocking, 6 non-blocking notes (`docs/adversarial/alert-policy-gate-2026-08-16.md`; both builder spec-conflict resolutions — the one-shot `mailbox_*` exemption and the pre-damped `cron_legs` exemption — UPHELD; mutation battery 7/8 killed, 60k-timeline fuzz zero violations, dead-man exemption mutation-proven). Merged `--no-ff` `a981163`, deployed Worker `a507f12a`, migration 0018 applied to prod D1 (backfill verified live: the 2 stuck Mordy `domain_dns_aging` rows carry `unhealthy_obs=2, alert_count=2`; pre-deploy `last_alert_ts` only 1.05h old so no deploy-day duplicate email, next re-alert ~23h out then daily). Founder alert-email volume drops from ~8/day to ~2 on day one, then 2/day (one per stuck domain, daily ladder) until Mordy's agent retries; cron_sweep dead-man ≤15-min page guarantee untouched.
5. **Discoverability: blind-probe verdict = coldrig appeared in 0/51 agent queries; on-site layer already built; real gap = ~zero backlinks/index authority.** Frozen `docs/research/blind-agent-recommendation-probe-2026-08-16.md` (+ same-day correction). Off-site execution started this session — see the 2026-08-16 `[ORDER]` in `ROADMAP.md ## Open` for done/remaining. MCP-registry publish DONE 2026-08-17T00:12Z (v0.2.3, isLatest verified). Directory layer executed 2026-08-17 (mcp.so #3602 filed, awesome-mcp-servers PR #10106 refreshed to 28-tool Communication entry awaiting merge, PulseMCP auto-pickup pending, glama already live); remaining = outreach + probe re-run. Docker MCP registry PR #4707 open; 4 form-gated directories need a founder-present browser session; outreach target list built (docs/research/backlink-outreach-targets-2026-08-17.md), sends founder-gated per message. best-of-mcp-servers PR #366 open; square logo live at coldrig.dev/assets/logo.png (Cline gate now = real install test only).
6. **Operator-message read endpoints SHIPPED + DEPLOYED 2026-08-17** (gate SHIP-WITH-NOTES, 0 blocking, `docs/adversarial/admin-read-endpoints-gate-2026-08-17.md`). Merged `018dc65`, deployed Worker `c6b658c4`, live-verified. **First use immediately produced ground truth on tenant `ten_91aab24a`:** the 2026-08-14 operator reply has `readAt=NULL` (never read); `requestIdempotency` empty (audit F1 not yet live for him); `goauthorpitchdesk.com` is the LAST ordinal so the F1 trap ARMS once mailbox adoption completes — train 1 is time-critical against his ~2h retry cadence. Corrected operator reply drafted, AWAITING FOUNDER approval.

## In flight / next
- **Next lane: customer-continuity BUILD, gated on the design-gate round-2 verdict.** Design v2 is in flight against the round-1 SHIP-AFTER-FIXES findings (`docs/adversarial/customer-continuity-design-gate-2026-08-18.md`); once round 2 SHIPs, build increments I1-I8 (`docs/research/customer-continuity-design-2026-08-18.md`: one `deriveNextSteps` primitive feeding responses+watchtower+nudges, dry-run-not-sentence anti-drift rule, convergence guard I3) against the founder's Q1-Q3 rulings, each adversary-gated. The vendor-truth gate's non-blocking batch (NB-1/2/3/5 + 2 test-infra notes, see `ROADMAP.md ## Open` 2026-08-18 entry) is folded into this wave, not a separate lane.
- **Trains 3-6 QUEUED behind continuity** (unchanged scope, just resequenced after the continuity wave): TRAIN 3 (claim-surface truth sweep, ~50 sites + dashboard blind spots) · TRAIN 4 (dedup-semantics wave + R4-1/NB-R3-1 hardening) · TRAIN 5 (monitoring denominators + `*_FAILED`-reach-no-watchtower-check) · TRAIN 6 (scale wave, founder-GO'd on S1/S3-S11; S2 deferred). Also queued: 3 promoted admin-read-endpoint bugs + msgchannel Inc4. Full breakdown: `ROADMAP.md ## Open` 2026-08-17 `[ORDER]` entries.
- **Mordy state (2026-08-18, post-incident):** 4 mailboxes across 2 domains (`theauthorpitchdesk.com` + `goauthorpitchdesk.com`), all bought/provisioned as of his agent's unattended setup-A/setup-B retries this evening; ripening ~Sep 14-15 per the warmup ramp. His side is now running fully agent-to-agent with no human relay — response/doc wording carries the whole interaction, which is exactly what the customer-continuity program is hardening. Watch continues on the 2-hourly cron (Landmines).
- **MCP-registry re-publish: DONE** — v0.2.3 published 2026-08-17T00:12Z, `isLatest` verified via registry API (details in `ROADMAP.md ## Open` 2026-08-16 visibility entry). **Gmail MCP authorization [gated:founder]** — exact steps in the `ROADMAP.md ## Open` 2026-08-16 Gmail entry.
- Still running: the 2-hourly coldrig watch, cron job `be956d32` (**SESSION-scoped — dies with the session; re-create on resume**, spec below).
- Open decisions (founder-gated, unchanged): C3 (d) arm-gate fix (3 blockers — grep `PROVISIONING_RECONCILE_ENABLED` in ROADMAP; ⛔ never arm before they're fixed) · direction-1 (staged) · mailbox W7 tail (`~/dev/agent-mailbox/HANDOFF.md`).

## Landmines / gotchas
- **`wrangler d1 migrations apply` can hit a transient 7403 auth error at DEPLOY time too, not just at watch-cron read time** (orchestrator-reported from today's ship; not independently re-derived by this bookkeeping pass). A single retry cleared it this session — `wrangler d1 migrations list coldstart-platform-db --remote` confirms "No migrations to apply!" as of this pass, so the current state is clean. If a deploy's D1 migration step 7403s, retry once before treating it as a real failure.
- **Lane worktrees without `node_modules` resolve `@coldstart/*` UP to the main repo's packages**, not the lane's own edited copies — `tsc`/`vitest` inside the worktree silently run against stale shared code until you either run `npm ci` in the worktree or hand-symlink `node_modules/@coldstart/{shared,platform,engine,dashboard}` to the worktree's own `packages/*`/`apps/*` (see `.claude/agent-memory/hard-builder/worktree-without-node-modules-resolves-to-main.md`). Same family: a fresh worktree's `packages/cli/dist/` is stale/absent until `npm run build -w agent-cold-email` runs there too. Check this before trusting a lane's own green battery.
- **The watch cron is session-scoped.** Re-create in a resuming session: 4 legs every 2h at :41, ALL via HTTPS — **never `wrangler d1` reads (its transient 7403 auth races generated false leg-failures — the whole reason leg 1 moved to the API), and always write the legs' temp files to the CURRENT session's scratchpad dir (a stale prior-session path makes `curl -o` exit 56, mimicking network failure)** — (1) `GET /admin/support/digest` (tickets; admin token from Keychain), (2) InboxKit **`POST /domains/list`** + **`POST /mailboxes/list`** — ⚠️ **CORRECTION 2026-08-18: both legs are POST with a JSON body, never GET** (verified in `apps/platform/src/vendors/real/{inboxkit-domain-port,mailbox-port}.ts` — `/domains/list` sends `{page, limit}`, `/mailboxes/list` sends `{keyword, limit}` or the equivalent paged-walk body depending on call site; a bare GET 404s and mimics a vendor outage rather than surfacing as a request-shape bug) (key at Keychain `coldrig/inboxkit-api-key`, workspace `c5188ced-33db-436f-b970-1860e6c8c66b`), (3) `GET /admin/ops/checks?unhealthy=1` (known baseline = the 2 Mordy `domain_dns_aging` checks; stale shared `updatedAt` >15min = dead-cron tell), (4) `GET /status` (sweepAgeSeconds <900; 503 = SEVERE). State + baselines: `~/.claude/mordy-watch/last-seen.json`. NEW issues are handled under the grant (builder→gate→deploy, never a hot patch); notify founder with what happened; demo-tenant single-sweep DO transients get a recurrence tripwire, not a notification. **Post alert-debounce-wave (2026-08-16):** leg 3 may now see `status: unhealthy` rows with `lastAlertTs: null` — these are debounce-pending flaps, deliberately unemailed (gate Finding 3). Treat a SINGLE-observation new-unhealthy row as pending-confirmation, not an incident; don't notify the founder on it alone.
- **Mordy state, SUPERSEDED same day (kept for the "he wasn't reading messages" lesson, don't act on the numbers):** this line originally read 2 mailboxes active / `goauthorpitchdesk.com` stalled / 2 unread notices — his agent then DID read and act: retry settled `apd-setup-a-2mbx` to `done` at 18:22:37Z (gate-verified), and a further claimed setup-B afterward (~19:14Z, orchestrator-reported, not independently re-verified) would put him at 4 mailboxes across both domains. Re-verify live before trusting either count.
- **Piped test-runner exit codes lie (gate lesson, 2026-08-18):** `npx vitest run 2>&1 | tail -80` (or any `| head`/`| tail`) exits **0** even when vitest itself failed — the pipe discards vitest's real exit code, so a backgrounded run can report "exit code 0" while its own tail shows failures. Verify a battery from the printed summary line (or `set -o pipefail`), never from a piped command's `$?`.
- **Founder alert-email volume is now governed by the shipped debounce policy** (`apps/platform/src/admin/watchtower-policy.ts`): expected ~2/day (one per stuck Mordy domain, daily ladder) until his agent retries. Any cadence change goes through builder→gate→deploy against that policy table — never a hot patch.
- **GitHub device-flow Authorize buttons reject synthetic browser clicks** — any future device-flow auth (e.g. a registry re-publish) needs the founder's real click; mint codes only when they're at the keyboard (~15-min TTL). GitHub also demands 2FA on YS-projectcalc **before 2026-08-28** (banner seen 2026-08-16) — founder action, or account actions get restricted.
- **Weekly-review launchd job broken since ~2026-07-20** (rc=143/rc=1; `~/.claude/weekly-review.log`) — flagged repeatedly, still unfixed; rough-edge lines are accumulating unreviewed.
- **`mcp-publisher` buffers its device code without a TTY** — run it under `script -q <outfile> mcp-publisher login github` and read the code from the file.
- Prior HANDOFF snapshot: `archive/2026-08-18-vendor-truth-ship/prior-HANDOFF.md` (this pass); `archive/2026-08-18-trains-1-2-ship/prior-HANDOFF.md` (earlier same day). **Push is BEHIND as of this pass:** local `main` is 1 commit ahead of `origin/main` — `629a20e` (the re-emitted vendor-truth gate doc) is unpushed; `git log origin/main..main` should read exactly that one commit at resume, push it (fast-forward, no conflicts expected) before assuming `origin/main` reflects the ship.

## Key files
- `docs/adversarial/vendor-verdict-gate-2026-08-14.md` — 3-round gate + ship record of the class fix.
- `docs/research/blind-agent-recommendation-probe-2026-08-16.md` — probe verdict + corrected diagnosis + option space.
- `ROADMAP.md ## Open` — the two 2026-08-16 entries (visibility [ORDER], Gmail [ASK]) + alert-policy [ORDER] + arm-gate blockers.
- `server.json` — v0.2.3 registry manifest — PUBLISHED to the official MCP registry 2026-08-17 (isLatest).
- `apps/platform/src/admin/watchtower-policy.ts` — the shipped alert policy table (debounce, ladders, exemptions); gate record at `docs/adversarial/alert-policy-gate-2026-08-16.md`.

## Resume — no in-flight build; next lane is customer-continuity build, gated on design-gate round 2
1. **Vendor-truth wave + Mordy incident: DONE end-to-end 2026-08-18** (merged `a0fd314` → Worker `54b00702` both hosts → site Pages `62115561` → gate SHIP verified `docs/adversarial/vendor-truth-gate-2026-08-18.md` [RESTORED — re-emitted post-reap; original was untracked in the reaped lane worktree; committed `629a20e`, that commit is UNPUSHED, push it along with the next batch] → incident resolved under the old code at `18:22:37Z` per the gate's own live-state probe). Nothing to resume; branches `feat/vendor-truth-2026-08-18`/`feat/vendor-watch-2026-08-18` preserved, lane worktrees (vendortruth/vendorwatch) reaped.
2. **Next lane: customer-continuity BUILD, gated on design-gate round 2.** Round 1 was SHIP-AFTER-FIXES (14 blocking + 9 non-blocking, `docs/adversarial/customer-continuity-design-gate-2026-08-18.md`); design v2 is in flight against those findings. Once round 2 SHIPs, run build increments I1-I8 (`docs/research/customer-continuity-design-2026-08-18.md`) against the founder's Q1-Q3 rulings, adversary-gated per increment. The vendor-truth gate's non-blocking batch (NB-1/2/3/5 + 2 test-infra notes) folds into this wave.
3. **Trains 3-6 QUEUED behind continuity (KIND A, pick one after):** TRAIN 3 (claim-surface truth sweep) · TRAIN 4 (dedup-semantics + R4-1/NB-R3-1 hardening) · TRAIN 5 (monitoring denominators) · TRAIN 6 (scale wave, founder-GO'd on S1/S3-S11, S2 deferred). Full scope: `ROADMAP.md ## Open` 2026-08-17 `[ORDER]` entries.
4. **Founder clicks (KIND B — present, don't do):** (b) Gmail `/mcp` authorization; (c) GitHub 2FA before 2026-08-28. (The prior "nudge Mordy" ask is stale — his side is now running unattended agent-to-agent.)
5. **Visibility [ORDER] remainder (KIND A):** mcp.so + PulseMCP submissions, guide/listicle outreach (see the ROADMAP entry for done/remaining).
6. **Re-create the watch cron** (spec in Landmines — ⚠️ InboxKit legs are POST, not GET; D1 migration-apply 7403 is a retry-once transient, not a real failure) if not already running.
