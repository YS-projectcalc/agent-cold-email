# ColdStart — Handoff

## 2026-07-14 design-lane addendum — read this before the older handoff below

- **The complete human journey is now built in `coldrig-human-interface`.** Public pages: `/signup`, `/connect`, `/security`, `/status`, `/support`, `/replies`, `/byo-domain`, recipient previews `/unsubscribe` + `/why-email`, and branded `/404`. The homepage now routes humans into sandbox signup/client setup and links trust/support surfaces in the footer.
- **The real Worker dashboard now includes working human sandbox signup and new owner screens.** `/app/signup` calls the existing `POST /signup`, shows the tenant token once, requires a secure-save acknowledgment, establishes the existing HTTP-only dashboard session, then opens `/app/setup`. The shell now has Dashboard, Inbox, Setup, Billing, and Settings on desktop and mobile. `/app/recover` truthfully explains hash-only token storage and the current fresh-sandbox/support path.
- `/app/setup` contains current configurations for Codex, Claude Code, Cursor, and Cline. Codex/Claude instructions were checked against their official current manuals; Cline uses its documented `streamableHttp` + headers shape. Cursor uses personal `~/.cursor/mcp.json` and explicitly tells owners to verify bearer-header behavior on their installed release rather than hardcode a token into a project.
- `/app/billing` uses the new shared `quoteProvisionedMailboxes()` helper for 5–60 mailbox planning, including $99/$149/$249/$649 examples, domains, non-contractual capacity, and a proposed owner ceiling. **All payment/subscription mutations are intentionally disabled** until the legacy backend below is migrated; the UI never simulates a successful billing action.
- New repeatable QA: `pw-scripts/static-site-integrity.js`, `pw-scripts/human-journey-qa.js`, and `pw-scripts/dashboard-human-journey-qa.js`. Final results: 28 HTML pages/link/canonical/sitemap targets valid; public desktop+mobile browser suite green; real local signup→token→setup→billing flow green; dashboard 99/99 tests; platform 243/243; four-workspace typecheck; production dry-run build; dangerous-HTML guard.
- **Activation gates still visible in the UI:** real sending is off; support/security/abuse inbound aliases must be owner-verified; push reply webhooks are absent; BYO-domain intake is designed but not public; original tokens are not recoverable; billing mutations and owner-ceiling persistence do not exist yet. Do not remove these disclosures merely because the layouts are complete.
- Founder ratified **provisional activation pricing**: **$49/month platform + $10/month per provisioned mailbox, five-mailbox/$99 minimum**. Exact examples: 10=$149, 20=$249, 60=$649. This supersedes the older $99/$299/$799 bundles described later in this historical handoff.
- The `coldrig-human-interface` worktree now integrates the formula across `site/pricing.html`, the interactive calculator, FAQ JSON-LD and visible copy, `/for-agents`, `/agent-evaluation.md`, `llms.txt`, the Salesforge comparison, SPEC §18, ROADMAP, and the design decision record.
- Billing meter definition: a mailbox counts while provisioned and retained, including warming, send-ready, or temporarily health-paused. Domains are included. Sends are not separately metered. Calculator capacity assumes 30 campaign sends/mailbox × 22 sending days only for conservative after-warmup planning; it is non-contractual.
- **Core implementation is not migrated.** `packages/shared/src/pricing.ts` now contains the provisional quote helper alongside the legacy table, and the dashboard consumes only that helper for previews. Checkout schemas, Stripe test products, plan enums, quota enforcement, webhooks, and persisted dashboard billing still use legacy `launch`/`growth`/`scale` tiers. The core chat must implement quantity-based mailbox billing, quote-before-provision, owner spend ceilings, downgrade/cancel semantics, migrations, and tests before paid activation. Do not interpret the completed UI as proof checkout supports the new model.
- Current design commits before this addendum: `86de90e` and `edffa9b`. Inspect the current branch tip for the pricing commit before integrating.

Agent-operated cold-email platform, BUILT + LIVE in test mode. Live: API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (dashboard at `/app`) · site https://agent-cold-email.pages.dev · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming ColdStart with zero prior context. Re-orient from the `## Resume` step below, then VERIFY its preconditions still hold.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything has CHANGED, surface exactly what and ask before acting. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · real vendor spend · npm publish · external send) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-13, evening)

- **MCP parity + discoverability wave DONE, uncommitted-to-remote — `main` ahead of `origin/main` by 3 commits (`b8d57e4`, `1e84672`, `fd13aa0`), push pending founder confirm.** MCP tools `list_campaigns`+`activity` added (15→17 tools, SPEC §19.0 parity, byte-identical to the HTTP routes; 6 new tests, platform suite 239/239 green) + `site/openapi.yaml` 13→24 operations. Tool-count staleness swept as a CLASS across `site/llms.txt`, `server-card.json`, `AGENTS.md`, READMEs — zero stale "12/15 tools" survivors confirmed. New discoverability surface: OG/Twitter cards on all 16 site pages + generated `site/assets/og-image.png` + JSON-LD on docs/pricing/compare + GitHub `homepageUrl` set (description field NOT yet updated — still "~12 tools", queued). IndexNow built fire-ready (`fd13aa0`): key file + `tools/indexnow/submit.sh` with a verified pre-deploy abort guard — **inert until the site deploys**. Deep research frozen: `docs/research/traffic-channels-selfserve-2026-07-13.md` (13 ranked zero-founder-touch channels). Fresh-context adversary verdict: **SHIP** on the full diff, with one gate carried forward — **DEPLOY ORDERING**: the Worker must deploy before/with the site, else `server-card.json` advertises tools the live Worker doesn't yet serve; also verify live `tools/list` count == 17 pre-deploy. Full detail + evidence: `ROADMAP.md` `## Open` (2026-07-13 evening checked items).
- **Still TEST-MODE:** no real vendor spend or real sending anywhere — SPEC §0 unchanged.
- **Prior arcs (all closed, all pushed as of `origin/main`'s current tip `71bcd81`):** Dashboard + Unified Inbox SHIPPED/deployed/pushed (SPEC §19, worker `e9734c42`) · AEO content live on Pages (`3477e7f`, deployment `96a55be8`; weekly citation-panel cron armed) · engine round-2 fix lane (`d342cd0`, re-attack CLEAN) · core build COMPLETE since 07-09 (3 adversarial panels) · Gate-0 founder decisions all CLOSED 07-12 (brand **coldrig**, vendor **Mailforge-first**, pricing **$99/$299/$799 + deferred paywall**).
- **Doc-convention update this session:** the global ROADMAP Contract (`~/CLAUDE.md` § ROADMAP Contract) is now live for this repo — ROADMAP.md keeps exactly `## Now` (≤5 items) + `## Open` (everything else, one line each, `[ORDER|ASK|IDEA]` tags); completed items get checked in place and self-drain to `archive/ROADMAP-done.md` at the next handoff. The old narrative "Session log" section has been migrated verbatim to that archive.

## In flight / next

- Next action (exact steps in `## Resume` below): push `main` (ahead 3), deploy the Worker, THEN the site (ordering matters — see the adversary gate above), then run the post-deploy checklist (IndexNow submit, Wayback-snapshot key URLs, curl live `tools/list`==17, update gh repo description 12→17), then seed grep.app/DeepWiki on-demand indexing. Full item list: `ROADMAP.md` `## Open`.
- Still running: **none** delivered work this session is idle. **The hourly wake-up cron armed mid-session to survive a usage-limit reset is SESSION-ONLY — it dies with this session, it is not a durable/standing cron.** Don't assume it'll fire in a future session.
- In progress (not finished): **none** — this wave is fully built + gated SHIP, just not pushed/deployed.
- Open decisions / blockers (all founder-owned): push+deploy confirm (Worker-before-site ordering) · attorney TM clearance for the coldrig display-brand rollout · coldrig.dev DNS wiring + Pages custom-domain attach · CF Web Analytics + GSC/Bing verification · npm auth/publish (`ACTIVATION.md` Gate 3) · standing brand-account authorization · Tier-2 insurance-channel asks (per research doc) · then `ACTIVATION.md` Gates 1–4 toward real sending. Post-ship audit scheduled **2026-07-26** (checklist: `ROADMAP.md` `## Open`).

## Landmines / gotchas

- **SPEC §0 lock: NO real vendor spend / real sending until the owner works `ACTIVATION.md`.** Real adapters throw `NotActivatedError`.
- **Deploys go through `npm run deploy` in `apps/platform/`** (applies remote D1 migrations BEFORE `wrangler deploy`) — never bare `wrangler deploy`; migrations through `0006` are applied.
- **Test flake guard:** `apps/platform/vitest.config.ts` sets `fileParallelism: false` — required (shared per-project Miniflare in `@cloudflare/vitest-pool-workers`); removing it re-introduces intermittent ECONNRESET/timeout flakes (see `MEMORY.md`).
- **New authed routes must register in `AUTHED_PATH_PATTERNS`** (`apps/platform/src/index.ts`) or they 404 (documented in `src/routes/README.md`); `public/index.html` is a deliberate duplicate of `public/app/index.html` synced by the dashboard postbuild (see `apps/platform/public/README.md`).
- **Mailforge API unconfirmed** (pricing page omits it; ToS presupposes it) — do not build against it before support confirms (`ACTIVATION.md` Gate 1). Warmforge "unlimited warmup" bundle is **BUNDLE LIMITED** — margin model runs on ramp-only warmup (~$4.50–5/mbx; `docs/research/warmforge-bundle-verification-2026-07-12.md`).
- npm NOT authed on this machine (publish = owner-hands, Gate 3).
- **IndexNow submit is post-deploy ONLY**: `tools/indexnow/submit.sh` aborts (verified) if the key file isn't already live at the target host — running it before the site deploy just fails closed, but don't bother trying.
- **Deploy ordering requirement**: Worker must deploy before/with the site — `site/.well-known/mcp/server-card.json` now advertises 17 tools; if the site goes live first while the Worker still serves fewer, the server-card overclaims a live surface (adversary-flagged gate, not yet closed).
- **The hourly wake-up cron from this session is SESSION-ONLY** — it does not survive this session ending; do not rely on it in a future session without re-arming.
- Memory split (pre-existing, deliberate): the active account's global memory file `coldstart-platform-build.md` (this session: `~/.claude-acct3/projects/-Users-yaakovscher/memory/`; an older copy may exist under `~/.claude-acct2`) holds session-recall state; repo `MEMORY.md` holds build lessons. Both live; don't consolidate mid-handoff.
- Uncommitted in tree (intentionally): `.claude/agent-memory/**` + `apps/platform/.claude/**` (subagent memory files — not project docs, never staged).

## Key files

- `SPEC.md` — canonical design (§19 dashboard+inbox, §18 pricing, §13 vendors, §0 locks) · `ROADMAP.md` — ledger + session log (B7.1 = the dashboard lane) · `ARCHITECTURE.md` · repo `CLAUDE.md` (project law) · `MEMORY.md` (build lessons) · `ACTIVATION.md` — owner go-live checklist (Gates 0–4).
- `docs/adversarial/dashboard-spec-review-2026-07-12.md` — frozen two-round spec verdict; `apps/dashboard/` — the SPA; `apps/platform/src/mcp/tools.ts` — the 15-tool MCP surface; `pw-scripts/agent-control-e2e.js` — the live agent-control proof harness.
- `FINAL-REPORT.md` — frozen 07-09 build report (status has moved on; HANDOFF + ACTIVATION are current).

## Resume — KIND B: founder-gated (push + deploy confirmation required)

Verify preconditions: `cd ~/dev/coldstart && git status -sb` shows `## main...origin/main [ahead 3]` with no dirty tracked files outside `.claude/agent-memory/**` + `apps/platform/.claude/**`; `git log --oneline -3` shows `fd13aa0`/`1e84672`/`b8d57e4`; `cd apps/platform && npm test` is green (239 tests). If any of this has drifted, surface exactly what changed and ask before acting.

This wave is fully built + gated SHIP. It is NOT push-blocked by any open work — only by founder confirmation, because push+deploy is irreversible/external. Once confirmed:
1. `git push` (pushes the 3 commits to `origin/main`).
2. Deploy the **Worker first**: `npm run deploy` in `apps/platform/` (applies remote D1 migrations before `wrangler deploy` — never bare `wrangler deploy`). Verify live `tools/list` returns 17 tools before proceeding.
3. Deploy the **site second**: `wrangler pages deploy site --project-name agent-cold-email`. This ordering matters — the site's `server-card.json` advertises 17 tools; deploying it before the Worker serves 17 would overclaim a live surface (adversary-flagged gate).
4. Post-deploy checklist (see `ROADMAP.md` `## Open`): run `tools/indexnow/submit.sh`, Wayback-snapshot key URLs, curl live `tools/list`==17 (re-confirm), update the gh repo description 12→17 tools, then seed grep.app/DeepWiki on-demand indexing.
Stop at the first gate: confirm push, confirm each deploy, before proceeding to the next step.
