# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` (v0.2.0, hosted remote + npm package) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold before acting.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · npm publish · real vendor spend · external send · DO support ticket) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-19)

**PRODUCTION 443 SMOKE PASS — the deploy chain's engine leg is DONE.** Engine deployed LIVE on the droplet (142.93.12.85): staged tarball (from HEAD `c30cd68`, image id `c897fa886d4f` — as reported this session, not independently re-verified by bookkeeper — includes gmail_api/ms_graph 443 transports `f406132` + poll fix `3d7d27d`, both bookkeeper-confirmed ancestors of `main` HEAD) scp'd + docker-loaded + container swapped. Bookkeeper-verified live: `GET /health` → `{"status":"ok"}` (fresh uptime, consistent with today's swap), unauth `POST /v1/send` → `401`. Container now runs as `USER node` (uid 1000); `/root/mailboxes.json` is chown 1000:1000 mode 400 (a chmod-600-root attempt broke the container read — EACCES crash-loop — fixed by uid-1000/400, tighter than the prior 644) — as reported, not independently re-verified here (no droplet SSH). Fresh `ENGINE_AUTH_SECRET` generated + set on the new container (rotation; old container's secret retired with it) — Worker-side `wrangler secret put ENGINE_AUTH_SECRET` still PENDING a founder keypress (bookkeeper-verified `wrangler secret list` from `apps/platform` shows only `ADMIN_TOKEN`/`TOKEN_HASH_PEPPER`, no `ENGINE_AUTH_SECRET`/`ENGINE_BASE_URL`); `ENGINE_BASE_URL` still unset (needs the Cloudflare Tunnel step before tenant traffic — unchanged). Founder re-minted the Gmail OAuth token this session (client secret was missing its leading "G" in transit — triplet validated against Google's token endpoint, `gmail.send` scope confirmed, before any droplet write); gmail_api send block merged into `/root/mailboxes.json` (imap block preserved, `.bak-<ts>` left on droplet). **THE SMOKE:** `POST /v1/send` via gmail_api transport from the droplet → `200`, Message-ID `<d7f83093-3541-4379-81b6-a3f961b2b7b5@gmail.com>`, `sentAt 1784460155100` — delivery double-verified (founder real-time inbox confirm + independent IMAP fetch on the droplet, `[Gmail]/All Mail` uid 247017, subject "coldrig production 443 smoke", date `2026-07-19T11:22:34Z`) — as reported, not independently re-verified by bookkeeper (no droplet SSH / mailbox access from here). **THE SMTP WALL IS ROUTED AROUND IN PRODUCTION.** New confirmed bug found by that IMAP verification: Gmail API rewrites the wire Message-ID (delivered as `<CAMc35PQ9axcPb86Sr9hnWHhJDUTEa7CdKiAuqffNeZ06=vc3fw@mail.gmail.com>`) → reply loop broken-in-waiting for gmail_api sends — already ledgered as the 2026-07-19 `[ORDER]` entry at the top of `ROADMAP.md ## Open` (do not duplicate). Fix lane DISPATCHED this session: hard-builder in worktree `~/dev/coldstart-worktrees/msgid-fix` (branch `msgid-wire-fix` off `main@c30cd68` — bookkeeper-verified branch exists at that base, zero commits yet), adversary-gated. Fully-live reframe lane OPENED this session (trigger fired): spec-builder drafting `site/` copy edits in the main working tree (site/ files only), claim-class adversary pass to follow, deploy ordering Worker-BEFORE-site — not yet deployed.

## Where we were (2026-07-17)

**COMMITTED + PUSHED:** local `main` = `c30cd68`, in sync with origin (verified `git diff origin/main main` empty). Two docs commits landed on top of the 07-16 four-lane arc (`f406132`→`d0e91ec`, unchanged this pass): `8cca0c6` (ledger — Gate-1 smoke PASS, poll+webhooks lanes closed, integration green) and `c30cd68` (docs — corrected OAuth-token provenance, rescued submission drafts, pruned a stale worktree), pushed to origin this session on founder confirmation ("orchestrate everything"). No new code commit landed on `main` this session — this pass is docs + a staged-but-unrun deploy + a completed-but-unmerged branch, three lanes:

1. **Deploy staged, not run.** Engine re-verified at HEAD `c30cd68` (typecheck exit 0; 68 passed/3 skipped). `apps/engine/Dockerfile`'s npm defect (`ROADMAP.md` 2026-07-15 `## Open` item) got its REAL fix, uncommitted in the working tree (`npm pkg delete devDependencies` before `npm install --omit=dev`) — reproduced the 404 on the unfixed Dockerfile, applied the fix, clean rebuild, ran it, `/health` ok, confirmed no dev deps leaked into `node_modules`. Deploy artifact staged at `/private/tmp/claude-503/-Users-yaakovscher/2d80a426-039c-47e2-8ff4-c6ebcf240703/scratchpad/deploy-stage/`: `coldstart-engine.tar` (61MB, linux/amd64, `docker load` round-trip proven — loaded into a fresh tag, ran, `/health` ok, unauth `POST /v1/send` 401'd, torn down) + `DEPLOY-RUNBOOK.md` (folded into `ACTIVATION.md` Gate-2 this pass). ⚠️ **SESSION-MORTAL:** the tarball is in scratchpad and disappears if this session dies — durable fallback is a cheap rebuild from HEAD with the fixed Dockerfile (command in `ACTIVATION.md` Gate-2 step 1). Droplet `/health` re-verified this session (~24.7h uptime, still dark/unarmed — no creds, no tunnel, no Worker secrets). **Correction to the staged runbook, verified this pass:** `npx wrangler secret list` (`apps/platform`) shows the Worker has NEVER had `ENGINE_AUTH_SECRET`/`ENGINE_BASE_URL` set (only `ADMIN_TOKEN`/`TOKEN_HASH_PEPPER` exist) — there is no existing Worker-side secret to reuse; `ACTIVATION.md` Gate-2 now says generate a fresh one and set it on the new container and the Worker together at deploy time.
2. **Warm-lead lifecycle deep dive CLOSED** (already ledgered in `ROADMAP.md ## Open`, not duplicated here): adversary SHIP, frozen dive `docs/research/warm-lead-thin-layer-dive-2026-07-16.md`, ratified design `SPEC.md` §22 (4 new tools, `lead_dispositions` + `followups` tables, webhook/poll split, adversary amendments on the send mechanism). Build gated on founder rulings Q1-Q6 (§22).
3. **BYO domain/mailbox intake BUILT COMPLETE, on branch, unmerged.** Branch `byo-intake` (`~/dev/coldstart-worktrees/byo-intake`), 6 commits off `main@c30cd68`: `aeda46f`→`62b692c`→`217ca2b`→`011576e`→`8dc3305`→`f46e935`. Shape-A (managed mailboxes on the customer's own domain) is the primary path; §20.6 connect seam; OAuth consent UI is a documented stub; real DNS/reputation/domain ports are `NotActivatedError` stubs. Adversary SHIP-dark (`docs/adversarial/byo-intake-2026-07-17.md`) found one must-fix-before-arm design hazard — a burned `fresh_standalone` BYO domain would auto-BUY a lookalike of the customer's brand once real adapters activate — fixed same session (`8dc3305` source-keyed `HARD_PAUSE_DOMAIN` routing + `f46e935` ≤1-active-BYO-primary guard) and re-attacked clean (addendum SHIP, all four attack angles held, RED test proven). Battery at `f46e935`: typecheck 0, platform 537/537 (was 420 pre-lane). **The safe default the adversary prescribed is now built: a burned BYO domain hard-pauses and alerts, it is never auto-replaced/bought.** Yaakov may override that product decision later, but the built behavior is the conservative one. Merge to main [gated:founder].

All three lanes' evidence is frozen on disk (`docs/adversarial/byo-intake-2026-07-17.md`, the staged `DEPLOY-RUNBOOK.md`, `SPEC.md` §22) — nothing above is asserted without a pointer.

## In flight / next

- **This session added no new commits to `main`** — the two commits now on `main` (`8cca0c6`, `c30cd68`) were the PRIOR session's handoff docs, pushed to origin this session. This session's own output is an uncommitted working-tree batch + a staged-but-unrun deploy + a completed-but-unmerged branch (all three, `## Where we are right now`).
- **Founder decision queue — regrouped this pass into four items (was six flat items, now grouped by shape):**
  1. **Commit-confirm + merge batch.** Everything sitting in the main working tree — `apps/engine/Dockerfile` (the real npm fix), `SPEC.md` §22 (warm-lead design), `ROADMAP.md`/`ACTIVATION.md`/`HANDOFF.md` (this pass), plus untracked `docs/adversarial/byo-intake-2026-07-17.md`, `docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`, `docs/research/warm-lead-thin-layer-dive-2026-07-16.md` — PLUS a merge of branch `byo-intake` (6 commits) into `main`. All test-mode, no spend; a single founder look unblocks both the commit and the merge.
  2. **OAuth mint + deploy "go".** Chain: founder re-mints the Gmail OAuth refresh token (the 07-16 mint was deleted from scratchpad at handoff, no-persisted-secrets rule) → founder says "go" → scp the staged tarball (or rebuild if stale) + load it → write `/root/mailboxes.json` with a `send.kind:"gmail_api"` block (shape now in `ACTIVATION.md` Gate-2 step 5) → swap the container with a FRESH shared secret set on both the container and the Worker together (see the correction in `## Where we are right now` — do not assume an existing Worker-side secret) → ONE production 443 self-send → triggers the ratified early-access→fully-live reframe.
  3. **Warm-lead Q1-Q6 rulings** (`SPEC.md` §22) — unblocks build increments #1-#4.
  4. **Unchanged older items:** name the mcp.so + Cline public-listing targets · re-run `mcp-publisher publish` (server.json 0.2.1) · GSC day-2 Request-Indexing batch · ChatGPT platform identity verification + in-browser policy read · MCPB form check (Team-org-gated?) · enable Cloudflare Web Analytics.
- **Headline blocker RESOLVED 2026-07-19:** droplet-side send is NO LONGER blocked — DigitalOcean still blocks raw 465/587 (2525/8025 open), but the 443 API transports (gmail_api/ms_graph) route around it and are now proven LIVE in production (see `## Where we are right now` — engine deployed, gmail_api smoke `200`, delivery double-verified). Remaining to full tenant-live: Worker-side `ENGINE_AUTH_SECRET` (pending founder keypress) + Cloudflare Tunnel/`ENGINE_BASE_URL` + `ENGINE_TENANTS` arming + the fully-live reframe deploy (in flight, not yet deployed).
- **No non-destructive build lane is currently open.** BYO intake (this session) and warm-lead (prior session) were the two remaining ones and both are now built/designed — further work on either needs a founder ruling (merge-gate or Q1-Q6), not more building. A resuming session with no founder input available should re-verify the staged deploy artifact still exists and is still built from current HEAD, rather than starting new scope.
- **Other open decisions (unchanged):** Stripe live keys + quantity-billing migration · agency-bundle pricing ruling · BYO-mailbox pricing · review-site listings · dogfood-calls scope · Glama listing sync-click.

## Landmines / gotchas

- **⚠️ SMTP egress is blocked on the DO droplet** (465/587; 2525/8025 open) — the Mac's network is UNBLOCKED and Gate-1 smoke-proven (real self-send over 465, 07-16). Do NOT assume the DROPLET can send SMTP until it's deployed + smoked; the 443 API transports (gmail_api/ms_graph) route around the droplet block entirely and are the durable fix — still need one droplet-side live send to prove it in production.
- **`site/` deploys whatever is on disk**, not what's committed — `wrangler pages deploy site --branch main` uploads the local dir as-is (no git-based Pages integration). Tree is clean now, but re-check `git status` before any future site redeploy (bit twice before).
- **support-kb.ts:35-36 still quotes dead $299/$799 tiers** — contradicts the ratified $99 pricing; fix with the billing migration (`ROADMAP.md ## Open`).
- **`SPEC.md` header may still read "~8–12 tools"** — stale (actual **19** after the webhooks lane added tools 18-19; was 17); same staleness class the Glama shopper hit; verify/fix at next SPEC touch (`ROADMAP.md ## Open` shopfront residual i).
- `apps/platform/vitest.config.ts fileParallelism:false` is REQUIRED; fresh-worktree verification needs `apps/platform/.dev.vars` + local D1 migrations or the suite shows phantom failures.
- **Gmail app password for yaakovscher@gmail.com sits on the droplet** `/root/mailboxes.json` (bearer-gated, world-reachable :8080 but can't even send) — rotate anytime and re-paste; nothing depends on it staying.
- Worktrees at this handoff: `main` (`c30cd68`, working tree dirty — see `## Resume`), the designer's own `~/Documents/Codex/2026-07-14/.../coldrig-design` (branch `coldrig-human-interface`, the user's, unrelated to main — leave it), and **`~/dev/coldstart-worktrees/byo-intake`** (branch `byo-intake` @ `f46e935`, 6 commits, adversary SHIP-dark + re-attack ADDENDUM SHIP, unmerged — ⚠️ do NOT prune, it holds real unmerged work pending the founder merge-gate).
- **OAuth secret hygiene:** the Gmail refresh token minted this session (`gmail-oauth.json`) + the smoke's app-password file were DELETED from scratchpad at handoff (rule g — no secret persists). The droplet still holds its own app-password at `/root/mailboxes.json` (see below). Re-mint the OAuth token before the droplet 443 deploy.
- **This is a shared/live worktree with multiple parallel-agent lanes** — the 07-16 four-lane batch (engine-443, directory-readiness, poll fix, webhooks) is now fully committed, but treat this as a standing pattern, not a one-off: before any `git add`, run `git status` and check what else is dirty — do not `git add -A` or commit one lane's files blind.

## Key files

- `SPEC.md` (§0 locks · §12/§12.1 economics · §18 pricing · §19 dashboard · §20 BYO) · `ROADMAP.md` (`## Now`/`## Open` ledger — source of truth for all open items) · `ACTIVATION.md` (owner-hands gates; Gate-2 engine host = the SMTP-wall context) · `CLAUDE.md` (project law).
- `apps/engine/` — the Node SMTP/IMAP daemon (committed `eb8ee42`). `apps/engine/dist/` — built; runnable from any SMTP-capable network for a local smoke (SMTP-wall path (b)).
- `tools/buyer-panel/` — the blind-shopper CHOICE harness; `runs/2026-07-15-claude-{canonical,starter,agency}.md` — the frozen evidence that we surface but get killed on "not live".
- `docs/adversarial/*-2026-07-15.md` — frozen SHIP verdicts (shopfront, tool-descriptions, guide-pages, compare-pages, watchtower-ops-email).
- `archive/2026-07-16-handoff-smtp-wall/prior-HANDOFF.md` — the resume state this replaced.

## Resume — KIND B: founder commit-confirm + a decision queue, no unblocked build lane left

**First verify state still holds:** `git -C ~/dev/coldstart status -sb` — expect HEAD at `c30cd68`, in sync with origin, working tree dirty with exactly `ROADMAP.md` + `SPEC.md` + `apps/engine/Dockerfile` modified, plus untracked `docs/adversarial/byo-intake-2026-07-17.md`, `docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`, `docs/research/warm-lead-thin-layer-dive-2026-07-16.md`, `ACTIVATION.md`, `HANDOFF.md`, `archive/ROADMAP-done.md`. Also check `git -C ~/dev/coldstart-worktrees/byo-intake log --oneline -1` — expect `f46e935`. If either differs, a reset or another session's write happened — re-derive state from `git log` before proceeding.

**Step 1 — the founder decision queue** (present all four groups from `## In flight / next`, do NOT act unilaterally):
- **(1) Commit-confirm + merge:** get Yaakov's look at the working-tree batch + the `byo-intake` branch, then commit (main working tree) and merge (`byo-intake` → `main`) on his go.
- **(2) Deploy chain — critical path, independent of (1):** re-mint OAuth → "go" → scp/load the staged tarball (or rebuild if stale) → write `mailboxes.json` → swap the container with a FRESH shared secret set on both sides (see the correction in `## Where we are right now` — no existing Worker-side secret to reuse) → one 443 self-send → fully-live reframe.
- **(3) Warm-lead Q1-Q6:** `SPEC.md` §22.
- **(4) The unchanged older items:** directory naming, registry re-publish, findability clicks, ChatGPT verification, MCPB check, CF Web Analytics.

**Meanwhile:** no non-destructive build lane is currently open — both prior candidates (BYO intake, warm-lead) are now built/designed and waiting on a founder ruling, not on more building. A resuming session with no founder input available should re-verify the staged deploy artifact still exists and is still built from current HEAD (`ls` the scratchpad path in `## Where we are right now`; if gone or stale, rebuild per `ACTIVATION.md` Gate-2 step 1 — cheap) rather than starting new scope.

**Then, only with Yaakov's explicit confirmation:** the decision queue above · Stripe live keys + quantity-billing migration ruling · agency-bundle + BYO-mailbox pricing rulings · review-site listings · once the droplet-side 443 smoke passes → the early-access→fully-live reframe (`ROADMAP.md ## Open` 2026-07-15 [ASK]).

Do NOT deploy, push, publish, spend, merge `byo-intake`, commit the working tree, or send without confirming with Yaakov first.
