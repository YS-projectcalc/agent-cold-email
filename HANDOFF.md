# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · npm publish · real vendor spend · external send) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-15 — "day 2 cont'd", deploy batch shipped live)

- **Design + $99 pricing + legal-final + RFC 8058 unsubscribe DEPLOYED LIVE** (Worker `4836515a`→`b21ca47d`, site `--branch main`, founder-authorized). Live-verified: `coldrig.dev/pricing` title "$99 for 5 Mailboxes, Then $10 Each" + JSON-LD `lowPrice":"99"`, `/status` 200, MCP `tools/list`==17 unauthenticated, `/unsubscribe?token=garbage` → 400. Full narrative + adversary record: `ROADMAP.md` 2026-07-15 design-lane entry (`## Now`).
- **Pricing RATIFIED by Yaakov, now live on every surface:** public pitch = **"starts at $99/month for 5 mailboxes, then $10 per additional mailbox"**; **send volume is NOT capped** — capacity is presented as ideal/recommended deliverability guidance, never contractual. Backend billing still legacy tiers (see Open decisions).
- **CLI 0.2.0 = a real installable MCP server** (`324a15c`): new `agent-cold-email mcp` stdio↔hosted bridge. Adversary 2 rounds → SHIP. **NOT published to npm yet** — npm auth was dead, Yaakov re-logging in (owner step below).
- **Legal FINALIZED per founder ruling** (`45a8d13`), now live: draft banners removed, governing law New Jersey (law + venue).
- **Watchtower/ops-email/monitoring lane SHIPPED + committed `2795a64`, deployed live in `b21ca47d`**: OpsMailer over the Cloudflare `send_email` binding (`OPS_EMAIL`, confirmed bound in `apps/platform/wrangler.toml`), 5-min cron armed (`*/5 * * * *`, confirmed in config), healthy→unhealthy alert state machine, dunning notices on suspend, inbound `support@` handler, `X-API-Key` auth alternative, repo-root `.mcp.json`. Adversary SHIP frozen (`docs/adversarial/watchtower-ops-email-review-2026-07-15.md`); non-blocking residuals ledgered in `ROADMAP.md ## Open`. All email legs dark/log-only until owner arming (wrangler login email scopes → sending enable → routing → verify address → route support@). Same commit fixed the waitlist CORS custom-domain bug — live-verified: `coldrig.dev` origin echoes ACAO, disallowed origin gets none.
- **`git push` DONE**: `origin/main` now at `c0b9e1f` (confirmed 0 ahead/0 behind local). Repo-root `.mcp.json` is public (cursor.directory unblocked); `X-API-Key` live (Smithery unblocked).
- **Engine droplet PROVISIONED DARK** (founder-authorized spend, doctl-confirmed): `coldstart-engine` (ID `584813711`, IP `142.93.12.85`, nyc1, firewall `coldstart-engine-fw` 22+8080), container Up, `/health` `{"status":"ok"}`, unauth `/v1/send` 401. NOT armed — no mailbox creds, no HTTPS tunnel, no Worker secrets, no `ENGINE_TENANTS`. A duplicate droplet from a doctl footgun was caught + deleted (confirmed: only one `coldstart-engine` droplet exists). Dockerfile npm defect ledgered (`ROADMAP.md ## Open`).
- **Directory/discoverability batch executed** (founder-authorized): llmstxt.site ✓, directory.llmstxt.cloud ✓, mcp.so ✓ (free tier, in review queue), awesome-mcp-servers PR #10106 — Glama badge added + bot answered (maintainer's merge pending), Glama already auto-indexed. cursor.directory + Smithery now unblocked (see push, above) — rescan/finish-publish still open.
- **Win-the-comparison program running** (Yaakov order): competitor research delivered + persisted (`docs/research/competitor-pages-research-2026-07-15.md`); buyer-CHOICE harness committed (`tools/buyer-panel/`, `c0b9e1f`) — see In flight for run #1 + guide-page state.

## In flight / next

- **Running: `guide-builder`** — writing 3 flagship guides `site/guide-cold-email-operation-{claude-code,codex,cursor}.html` ("run your entire cold email operation with Claude Code/Codex/Cursor + ColdRig"), FoxReach rival-guide intel relayed mid-build. **UNCOMMITTED** (confirmed on disk, untracked) and **404 on the live site** (correctly not yet deployed) — gated behind a fresh adversary claim-classes pass before they ship, same discipline as `docs/adversarial/design-integration-review-2026-07-15.md`.
- **Running: `buyer-run-canonical`** — baseline shopper run #1 (`tools/buyer-panel/briefs/canonical-scale.md`) against the buyer-CHOICE harness. No completed run yet (`tools/buyer-panel/CHOICE-TREND.md` confirmed empty, no baseline row) — first real row lands when this reports.
- **Next action, in order:** (1) guide-builder reports → dispatch a fresh-context adversary on the 3 new guide pages (claim-classes sweep: qualifier-heavy titles, no unsubstantiated "fully autonomous" claims, verify every stat against repo/live reality) → commit on clean pass → redeploy site only (`wrangler pages deploy site --project-name agent-cold-email --branch main` — Worker untouched); (2) comparison pages build (ranked order AgentMail → Skyp → FoxReach → Maildoso per `docs/research/competitor-pages-research-2026-07-15.md`, UNVERIFIED flags in that doc must resolve-or-omit before publish); (3) warm-lead lifecycle deep dive (Yaakov ruling 07-15, NOT a Mordy-start blocker — scope in `ROADMAP.md ## Open`); (4) webhooks lane (ruled build, core-platform, next now that watchtower+CORS commits have cleared); (5) BYO intake build (§20, queued behind webhooks, unblocks Mordy migration). See `## Resume`.
- **In progress (not finished):** ROADMAP done-drain — 8 `- [x]` lines are checked but not yet moved to `archive/ROADMAP-done.md` (still 8, unchanged this session; purely mechanical, do at next).
- **Open decisions / blockers (all founder-owned):** the two CLI publish commands (`npm publish` 2FA + `mcp-publisher publish` — npm auth was dead, Yaakov re-logging in) · **Stripe live keys + quantity-billing backend migration** (dashboard billing UI is built but deliberately inert until per-mailbox quantity billing + spend-ceiling persistence + plan-enum migration land — the last core-code gap) · ~$10 test domain (no registrar/card on this machine) · Mailforge signup OR a GWS app-password box for Gate-1 smoke · Glama claim (his GitHub OAuth click) · PulseMCP (bot-walled — his browser or email) · GSC/Bing + CF Web Analytics (his clicks) · Cline (needs one real Cline-setup test to attest honestly) · dogfood 3 calls (competitors? Jack Clark? "roast it" CTA) · external uptime prober (UptimeRobot/BetterStack — CF can't watch itself). Mailbox provider is RESOLVED (Mailforge managed-path; Mordy rides BYO on his own Google Workspace boxes) — droplet spend is DONE (provisioned, see above).

## Landmines / gotchas

- **`site/` deploys whatever is on disk, not what's committed** — `wrangler pages deploy site --branch main` uploads the local directory as-is (confirmed: no git-based Pages integration, no deploy script wraps it). The 3 uncommitted, un-adversary-passed guide pages (`site/guide-cold-email-operation-*.html`) are sitting in that directory right now — do NOT redeploy site until they either commit clean or are moved/excluded, or they ship unreviewed.
- **Deploy batch IS now live** (biggest landmine from last handoff, resolved) — `coldrig.dev` serves the new design + $99 pricing + legal-final; live-verified per `## Where we are`. Don't re-run the Worker+site deploy sequence expecting a no-op; check `git log`/live state first.
- **Stale leftover worktree** `.claude/worktrees/agent-affd823ae7fd92292` (branch `integrate/design-2026-07-15`) — the design-integrator's; already merged to main, safe to `git worktree remove` when convenient. The designer's own worktree is `~/Documents/Codex/2026-07-14/.../coldrig-design` (branch `coldrig-human-interface`), now unrelated to main's history.
- **support-kb.ts:35-36 still quotes dead $299/$799 tiers** — the AI support agent's KB contradicts the ratified pricing (now live); fix with the billing migration (`ROADMAP.md ## Open`).
- `apps/platform/vitest.config.ts fileParallelism:false` is REQUIRED. Fresh-worktree verification needs `apps/platform/.dev.vars` (from `.dev.vars.example`) + local D1 migrations or the suite shows phantom failures.
- npm + MCP-Registry + all founder OAuth = Yaakov's hands. `mcp-publisher` cached token is EXPIRED; npm auth was also dead — Yaakov re-logging in to both.
- **Engine droplet is provisioned but wide open on 8080** (firewall `coldstart-engine-fw` allows 0.0.0.0/0 on 22+8080) with an empty creds map — low risk while dark (unauth `/v1/send` → 401) but put it behind the Cloudflare Tunnel before arming, per the existing ACTIVATION Gate-2 plan; don't skip that step under pilot time pressure.

## Key files

- `SPEC.md` (§0 locks · §12/§12.1 economics · §18 pricing · §19 dashboard · §20 BYO) · `ROADMAP.md` (`## Now`/`## Open` ledger — source of truth for all open items) · `ACTIVATION.md` (owner-hands gates; Gate 2 engine = CLEARED) · `CLAUDE.md` (project law).
- `docs/adversarial/{design-integration,cli-mcp-bridge,b4-optout,engine-host,engine-tenants-allowlist}-review-2026-07-1{4,5}.md` — frozen verdicts.
- `docs/research/{agent-buyer-research-forensics,dogfood-targets}-2026-07-14.md` — the buyer-agent playbook + dogfood list.
- `packages/cli/src/commands/mcp.ts` — the new stdio MCP bridge. `apps/engine/` — dark Node SMTP/IMAP daemon (committed, flag-dark).
- `archive/2026-07-15-day2-design-email/prior-HANDOFF.md` — the resume state this replaced.

## Resume — KIND B: the next step is a fresh-context review, then founder-gated actions

**First, check the guide-builder + buyer-run lanes:** `git -C ~/dev/coldstart status --short` and read the `guide-builder` / `buyer-run-canonical` task output.
- **If guide-builder's 3 pages are present but uncommitted:** the next code step is **decided and non-destructive** — dispatch a fresh-context `adversary` on the 3 guide pages (claim-classes sweep: qualifier-heavy titles, no unsubstantiated "fully autonomous" claims, verify every stat against repo/live reality — same discipline as `docs/adversarial/design-integration-review-2026-07-15.md`). Commit ONLY on a clean pass, then redeploy site (`wrangler pages deploy site --project-name agent-cold-email --branch main` — see Landmines re: disk-state deploy), then update `ROADMAP.md` + this HANDOFF.
- **If it didn't finish / died:** re-dispatch with the same brief (`ROADMAP.md ## Open` win-the-comparison entry).
- **If `buyer-run-canonical` reports:** fold the run record into `tools/buyer-panel/CHOICE-TREND.md` per its own README, then feed any fix-list items into `ROADMAP.md ## Open`.

**Then, and only with Yaakov's explicit confirmation** (each is founder-owned, irreversible, or spends money — STOP and confirm before each):
1. **Publish the CLI:** `cd packages/cli && npm publish` (2FA; npm auth was dead, confirm he's re-logged in), then from repo root `mcp-publisher login github && mcp-publisher publish` (cached token expired).
2. His quick clicks: `wrangler login` (email scopes, needed to arm the watchtower's outbound send), Glama claim, PulseMCP, GSC/Bing, CF Web Analytics, the Cline setup test.
3. Pilot chain (droplet + deploy already DONE): buy ~$10 test domain, Mailforge signup OR a GWS app-password box for Gate-1 smoke → Gate-1 smoke → arm Mordy pilot. BYO intake build (§20) is the next core-code lane, unblocked now that the watchtower lane shipped.
4. Redeploy the site once the guide-page adversary pass is clean (item 1 under "First, check…" above) — comparison pages (AgentMail/Skyp/FoxReach/Maildoso) queue behind it.

Do NOT deploy, push, publish, or spend without confirming with Yaakov first.
