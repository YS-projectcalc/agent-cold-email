# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE + has its FIRST PAYING CUSTOMER (Mordy, 2026-07-28); registrar ARMED, awaiting his first provisioning.** Site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · branded API https://api.coldrig.dev · npm `agent-cold-email@0.2.1` · MCP Registry 0.2.2 (27 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (armed secret, latest deploy, whether Mordy has run setup yet). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (merge/push/deploy/arm to customer-ready; browser-consent clicks excepted) covers it.

## Where we are right now (2026-08-10)

**🔥🚢 (2026-08-10, later, supersedes the 🚢⭐ Wave-3 paragraph immediately below) P0 purchased-domain DNS-wait deadlock SHIPPED + DEPLOYED + VERIFIED; InboxKit support answered both open questions.** InboxKit support replied (verbatim, both cited in full in `ROADMAP.md ## Open`/`## Now`): (1) *"The domain goauthorpitchdesk.com is now active, and the DNS will be configured during mailbox processing"* — confirming the propagation checker (`dns_propagation_status`/`nameserver_match_status`) belongs to the CONNECT flow only and never runs pre-mailbox on a purchased domain, which is why `purchasedDomainIsReady` requiring both verdicts deadlocked Mordy's provisioning for 6 days; (2) *"we don't support using the generated refresh tokens for programmatic sending through the Gmail API"* — Inc-F programmatic OAuth is DEAD as designed, manual mint stays Mordy's path, founder ASK still open on the customer-#2 alternative (`ROADMAP.md ## Open` 2026-08-10). **Fix shipped same day:** `polledDomainIsReady` (`vendors/real/inboxkit-domain-port.ts:453-455`) now short-circuits `connection_type='purchased'` + `status='active'` past the propagation wait; `connected`/`unknown` unchanged. Adversary gate SHIP 0-blocking (frozen `docs/adversarial/purchased-dns-wait-gate-2026-08-10.md`) — RED-proven (delete the line → 4/24 tests fail; restore → 24/24), battery independently re-run 166 files/1528 tests + typecheck ×5 clean. Merged `main` `bfce3a9` (--no-ff), deployed Worker `e2e9f061-e7e0-4999-bdb3-258d36582002`, live-verified `/status` 200 both hosts, `/account` 401. **No flag, no migration — the fix is armed now.** Mordy's pre-existing row self-heals via the existing connection-type backfill on his first post-deploy retry (gate-traced end to end, see `ROADMAP.md`'s checked-off P0 bullet). **The wire-A `retry_setup` operator message to Mordy is FOUNDER-GATED** — it needs the prod `ADMIN_TOKEN` (owner-held, confirmed no local copy exists), paste-ready curl delivered in-chat 2026-08-10; Mordy's agent self-retries against the fixed gate regardless of whether that message is ever sent. Gate left 3 non-blocking findings + a NEW-section out-of-scope item, all ledgered as their own `ROADMAP.md ## Open` bullets dated 2026-08-10 (a-f: deadlock-class detector, post-buy DNS re-poll, vendor-contract capture watch, module-comment fix, burn/replace-lane test gap, silent-replace idea). Full ledger detail: `ROADMAP.md ## Open`.

**🚢⭐ (2026-08-10, supersedes the 🚢⭐ Wave-2 paragraph immediately below) Wave 3 SHIPPED + DEPLOYED + VERIFIED — five lanes, combined gate SHIP 11/11 attacks failed.** Merged `main` `17e05d4` (--no-ff, 138 files +7254/-678), pushed. Lane branches preserved: `msgchannel-inc23-2026-08-06`, `webhook-inc2-2026-08-06`, `dash-idem-fixes-2026-08-06`, `watchtower-hardening-2026-08-06`, `byo-teardown-2026-08-06`, `integrate/2026-08-09-wave3` (incl. the B-1 fix `414b58e` + the frozen verdict `279462a`). Content: msgchannel Inc2+Inc3 (operator route + `list_messages`/`ack_message` tools — tool count now 27, was 25 — gate F1/F2 fixes `1eef443`), webhook inc2 (decline-code second Stripe read closing the `readDeclineCode`-dead-on-real-payloads residual, `dispute.created` staleness exemption bounded to the dispute's OWN resolution [gate B-1 fix], refused-events `applied=0` flag closing N-2, 413 actual-bytes cap closing audit item 7), dash-idem (buy-intent Idempotency-Key convergence, `/remove-mailboxes` honors Idempotency-Key, token-rotate revokes all sessions, `launch_campaign` double-submit guard), watchtower hardening (D1-outage-safe alert ordering, a new `WatchtowerDO` dead-man + migration v3, wedged-DO visibility, flap windowing), byo-teardown (never vendor-release customer-connected resources). Gates: byo-teardown SHIP 0-blocking (frozen `docs/adversarial/byo-teardown-gate-2026-08-06.md`); msgchannel r1 SHIP-AFTER-FIXES → fixed `1eef443` (frozen `docs/adversarial/msgchannel-inc23-gate-2026-08-06.md`); combined integration gate r1 SHIP-AFTER-FIXES (1 blocking B-1: the unconditional `dispute.created` staleness exemption bricked a tenant on closed(won)-before-created ordering — a real Stripe delivery-order case, not synthetic) → fix `414b58e` (exemption bounded by `isDisputeSettledWithoutFreeze` + `closed` now UPSERTs the disputes row; RED 3→GREEN, both halves proved load-bearing by partial revert) → r2 **SHIP**, 11/11 attacks failed, affirmative clean pass (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`, git-tracked on `main`, verified present + committed this pass). Battery (per the merge commit's own message): typecheck ×5 clean; 227 test files/1810 tests 0 real failures (platform 166f/1522t post-fix); builds clean; md5 tripwire clean. **DEPLOYED + LIVE-VERIFIED THIS PASS:** Worker version `0f197c4d-ffe8-4da6-a1f4-94c3f23063d6` both hosts — `/status` returns 200 `{"status":"ok","sweepAgeSeconds":215}` (workers.dev) / `216` (api.coldrig.dev), independently fetched, confirming the deploy AND surfacing the new sweep-freshness field for the first time; server-card `coldrig.dev/.well-known/mcp/server-card.json` independently fetched: version 0.2.2, 27 tools. Cron `*/5 * * * *` intact; WatchtowerDO migration v3 applied (per orchestrator evidence — the `[[migrations]] tag = "v3"` block is confirmed present in `wrangler.toml:102-104`, but the REMOTE-applied state wasn't re-checked via `wrangler` this pass). Site deployed: Pages deployment `20a4a523` --branch main; IndexNow fired HTTP 200 for 15 changed pages (reported, not independently re-checkable after the fact). Worktrees: ALL lane/audit/integration worktrees removed (branches preserved); the stale `agent-a25bfeea` registrar-lane worktree carried since 2026-08-05 archived to branch `d451d28` and removed. Remaining: main checkout + the external Codex design worktree only (`git worktree list`-confirmed this pass). **NB-2/NB-3/NB-4 of the ORIGINAL `audit-alerting-completeness-2026-08-06.md`** (cron-leg error counters unread, digest thrown away, provisioning-vendor-failure-alerts-nobody) were **NOT** part of this wave and stay genuinely open, un-tracked as their own bullets — distinct from the wave-3 gate's OWN NB-2/NB-3/NB-5 which already have separate `ROADMAP.md ## Open` bullets. Full ledger detail: `ROADMAP.md ## Now` / `## Open`.

**🚢⭐ (2026-08-06, supersedes the 🟡 gate-pending paragraph immediately below) Wave 2 SHIPPED + DEPLOYED + ARMED.** Combined-diff adversary gate: NO-SHIP r1 (2 blocking — the migration would have terminally retired the paying customer's zero-mailbox domain; the global event watermark dropped cross-lane events incl. chargeback freezes) → both fixed (`27ba269`) → SHIP r2 (frozen `docs/adversarial/wave2-integration-gate-2026-08-06.md`, r1+r2). Merged to `main` `f7ee786` (--no-ff; lane branches preserved: `wave2/engine-credstore` `6e6acce`, `wave2/platform-cred` `bcb602e`, `wave2/clock-migration` `6f17afa`, `followups-2026-08-06` `624d349`, `rebuy-guarded-2026-08-06` `bb48a31`, `fix/stripe-webhook-p0` `9b432a8`; integration commits `89aade8`/`ce51294`/`22d792e`/`27ba269`), pushed (`64d5eee..f7ee786`), deployed Worker `8187d36e-056d-4d3b-a52f-3814dd4fcff9` both hosts (bookkeeper-reconfirmed via `wrangler deployments list`: latest version, created 2026-08-06T16:56Z), D1 migration 0016 applied remote (bookkeeper-reconfirmed: `wrangler d1 migrations list --remote` → "No migrations to apply!"). Live checks green: `/status` ok both hosts, `/account` 401, `/app` 200, `tools/list` 25. **ARM VERIFICATION PASSED:** the first post-deploy cron cycle was observed via `wrangler tail` — `runScheduledPoll`→`runScheduledTick` "Ok" fleet-wide, zero errors, zero sends, no alert storm, no migration-failure logs (it fails loud) — AUTO-SEND IS LIVE (armed; kill switch `AUTOSEND_DISABLED` env, unset; bookkeeper-reconfirmed the switch is wired at `env.ts:210` / `admin/ops-sweep.ts:378`). Vendor re-polled after the cycle: `goauthorpitchdesk.com` still active/0-mailboxes — untouched, exactly as the blocker-1 fix guarantees. Battery at ship: platform 152 files/1393 tests, engine 136+4sk, dashboard 29/143, cli 12/12, typecheck ×5, build clean. Worktrees: all 8 lane/integration worktrees REMOVED (branches preserved); remaining = main checkout, 3 stale `.claude/worktrees/agent-*` (08-05), the external Codex design worktree. **Mordy:** go-message SENT (unchanged from below), not yet retried — his workspace still reads 0 mailboxes, DNS still pending, vendor's own NS checker never ran (`last_nameserver_check` still null); a support-ticket draft covering the checker kick + OAuth scope question remains with the founder.

**🟡 (2026-08-06 afternoon, supersedes the go-message status in the 🟢 paragraph below) Wave 2 + the day's full orchestration BUILT + INTEGRATED on branch `integrate/2026-08-06-wave2` `22d792e` (worktree `~/dev/coldstart-wt-integration2`), pending its COMBINED ADVERSARY GATE (running now; verdict lands `docs/adversarial/wave2-integration-gate-2026-08-06.md`, does not exist yet — do NOT merge/deploy before it does).** Content: clock migration (real wall-clock for all non-sandbox tenants, one-shot `transactionSync` per-DO, delta both signs, sandbox mailboxes+domains retired), auto-send drivers (5-min cron leg, DO-side 4-leg gate, shared per-mailbox eligibility predicate, `AUTOSEND_DISABLED` kill switch — confirmed wired in `env.ts` + `admin/ops-sweep.ts`), R5 timeout ladder ENFORCED by assertions (SMTP ~100s < engine 120s [`ENGINE_REQUEST_TIMEOUT_MS`, `vendors/real/email-port.ts:62`, confirmed] < tenant budget 135s [`SEND_PIPELINE_TENANT_BUDGET_MS`, `admin/ops-sweep.ts:281`, confirmed] <= leg deadline 150s [`SEND_PIPELINE_LEG_DEADLINE_MS`, `:290`, confirmed]; 285s < 300s cron), credstore both sides (pushSeq ordering + tombstones), guarded mailbox re-buy (founder-ruled once-with-guards — durable dispatch-claim table `mailbox_buy_dispatches`, 15-min absence age), Stripe webhook P0 fixes (detail below), step-aware retry messages, NEW-4 aggregate hygiene, docs sweep (ARCHITECTURE #4, SPEC §23, ACTIVATION wave-2 arming order). Six lane branches preserved for revertability: `wave2/engine-credstore` `6e6acce`, `wave2/platform-cred` `bcb602e`, `wave2/clock-migration` `6f17afa`, `followups-2026-08-06` `624d349`, `rebuy-guarded-2026-08-06` `bb48a31`, `fix/stripe-webhook-p0` `9b432a8`; integration merges `89aade8` (lane 6/6) + Inc-D `ce51294` + NEW-4/docs `22d792e` — all confirmed on `integrate/2026-08-06-wave2`'s log. Builder battery at Inc-D: platform 152 files/1389 tests ×2 runs, all other workspaces green; independent verifier partial-confirmed (typecheck/engine/dashboard/cli/build exact matches; platform count run still pending).

**Stripe webhook boundary audit — FAIL, 3 blocking, all three FIXED in `fix/stripe-webhook-p0` (in this integration).** Frozen `docs/adversarial/audit-stripe-webhook-2026-08-06.md` (verified present — currently sitting untracked in the `main` worktree, not yet committed/copied into the integration branch). Findings: (1) tenant resolution read only `data.object.metadata.tenantId`, a field real `invoice.payment_failed` and both dispute event types never carry (only checkout + subscription events do) — the dunning and D5 chargeback-freeze lanes were silently inert in prod, returning `200 {"applied":false}` on every real delivery; (2) the checkout path claimed its dedup row BEFORE its effects, so an ordinary Stripe API hiccup on the awaited `getSubscription` call left OFAC `screenTenant` unrun — fail-**open** on the compliance gate, permanently (no later event repairs it); (3) no event-ordering guard — a stale redelivered `invoice.payment_failed` can regress a recovered paying tenant to `past_due`, and the armed 5-min dunning cron then suspends them. **Fix verified in code**: resolution now walks `metadata.tenantId` → `subscription_details.metadata.tenantId` → a new D1 `stripe_customer_index` (migration `0016_stripe_customer_index.sql`, confirmed present); `screenTenant` (`billing.ts:552`) now runs BEFORE the awaited `captureSubscriptionState`/`getSubscription` call (`:557`); an `event.created` watermark (`billing_event_order` table) refuses strictly-older events and advances synchronously before effects (`billing.ts:358-436`). New findings ledgered, NOT yet fixed: `readDeclineCode` was dead on every real payload while finding 1 stood (the permanent-decline fast path never fired pre-fix; needs a second Stripe read to confirm post-fix behavior); audit item 7 (413 content-length-omission bypass) is still open.

**Founder rulings captured 2026-08-06 (full text in `ROADMAP.md`):** mailbox re-buy = YES, once with guards (vendor-confirms-nothing-exists first, max 1, alert both ways) — **now BUILT** in the `rebuy-guarded-2026-08-06` lane above; BYO teardown = NEVER touch a customer-connected domain — buildable now that the cred lane landed, still UNBUILT, ledgered.

**Go-message to Mordy: SENT** (founder confirmed) — supersedes every "awaiting founder send" / "DRAFTED" line elsewhere in this doc. No retry yet as of the last poll: his workspace still reads 0 mailboxes, his domain's DNS is still pending, and the vendor's own NS checker has never run (`last_nameserver_check` still null). A support-ticket draft covering the checker kick + the OAuth scope question was delivered to the founder.

**🟢 PROVISIONING FIX WAVE 1 + DUNNING P0 + MSGCHANNEL INC1 (with gate fixes) are BUILT, MERGED, DEPLOYED, and LIVE-VERIFIED.** The three-lane wave merged to `main` `c90b972` (--no-ff; lane branches preserved for revertability: `fix/dunning-p0` `8fd578e`, `fix/provisioning-wave1` `00a6230` + gate-fix `8e55bfc`, `msgchannel-inc1` `a9f57a4`), pushed to `origin` (`ef1c2db..c90b972`), deployed Worker `bb8dc830-4785-4e58-a99c-74a05841fb2e` (both hosts + cron `*/5 * * * *` intact, confirmed via `wrangler deployments list`). Live-verified: `/status` ok both hosts, `/account` 401, `/checkout/success` 302, `/app` 200, `tools/list` 25, `serverInfo` 0.2.2. Combined-diff adversary gate SHIP (round-1 SHIP-AFTER-FIXES, 1 blocking → DNS-false-ready fix `8e55bfc` → round-2 SHIP; frozen `docs/adversarial/wave-integration-gate-2026-08-05.md`). Battery at ship: platform 140 files/1271 tests, typecheck clean ×5, build clean. **Ready-token stall risk CLOSED by live observation**: the workspace's already-propagated domain reads `dns_propagation_status="propagated"` / `nameserver_match_status="matched"`, both in READY_STATUS_TOKENS — the gate's stall-risk directive is satisfied; `goauthorpitchdesk.com` will read ready once the vendor's own propagation verdict flips the same fields (its internal checker hasn't run yet — `last_nameserver_check` still null; public registry check confirms the NS delegation itself is real: `dig`/`whois` show `goauthorpitchdesk.com` NS = `alexandra`/`phil.ns.cloudflare.com` live at the registry). **Go-message DRAFTED, awaiting founder send** (founder-owned step; retry, same key `apd-setup-a-2mbx`). **Wave 2 design arc is COMPLETE**: design v1 → adversary NO-SHIP (8 blocking, headline: as-designed arming would have drained Mordy's first campaign to sandbox mailboxes) → v2 delta → round-2 SHIP-AFTER-FIXES (0 blocking); frozen `archive/2026-08-05-provisioning-fixwave-plan/wave2-design-2026-08-05.md` + `docs/adversarial/wave2-design-review-2026-08-05.md`. Build briefs must fold in R1-R8 + NEW-4 + U1/U2 probes (detail in `ROADMAP.md`'s Wave 2 entry). **Founder rulings captured in-session 2026-08-05** (all in `ROADMAP.md`): Wave 2's three gates lifted — real wall-clock for ALL non-sandbox tenants (his words: *"Why not get a real clock for any non sandbox"*), auto-send build+arm now, OAuth manual-for-Mordy + verify-programmatic in parallel; msgchannel FULL CHANNEL authorized (Inc1 ratified+shipped, Inc2-4 to follow, each gated). **Live before this deploy**: Mordy's agent fired one diagnostic retry (~02:30Z) — same `vendor_error` as previously diagnosed, domain state safe (1 domain active, 0 mailboxes, no double-charge), his agent independently confirmed the defect class; he was HOLDING until the go-message. InboxKit workspace polled pre-deploy: `goauthorpitchdesk.com` status active / connection_type purchased / DNS pending / 0 assigned; workspace mailboxes 0 — his retry (once sent) will re-poll this live. **This supersedes the "NOT fixed" framing in the 🔴 paragraph immediately below** — the fix is now shipped and deployed; that paragraph's root-cause diagnosis still stands as history.

**🔴 (superseded above — fix now built, not yet deployed) Mordy's mailbox provisioning is FAILING (diagnosed late 08-05); root cause = a wrong vendor operation on PURCHASED domains.** He retried after the hotfix: **domain adoption WORKED in prod** (his $12.50 orphan `goauthorpitchdesk.com` adopted, `/infrastructure-status` domains 1, no double-charge — the hotfix's B2 fix landed). But mailbox provisioning returns `{"code":"vendor_error","retryable":true}` on 3 retries, zero progress. Root cause (frozen `docs/adversarial/sweep-domain-type-2026-08-05.md` + live vendor state: `nameserver_match=pending, actual_ns=[], last_ns_check=None` 24h+): `setDns` runs InboxKit's connect-EXISTING-domain nameserver flow (`POST /domains/nameservers`) on a domain InboxKit itself registered — wrong operation, throws at step 1 before mailboxes are ever attempted; `retryable:true` is a LIE (retrying re-runs the same wrong op). **The incident hotfix's "32s race" premise was a MISDIAGNOSIS — its round-2 gate passed only because it STUBBED setDns to succeed (fixture-unreal for a purchased domain).** A full forward-path audit (`docs/adversarial/forward-path-audit-2026-08-05.md`) then mapped **7 MORE downstream landmines** (L1 DNS-ready lie · L2 async-buy-treated-as-sync → re-buy/wedge · L3 manual-only OAuth mint · L4/L6 no send/inbox cron drivers · L5 frozen VirtualClock → ~25 lifetime sends then send-dead · L7 billing counts unprovisioned rows) — 2 systemic causes: "buy-accepted ≠ usable" (async everywhere) + the frozen paid-tenant clock. Vendor-leak sweep (`docs/adversarial/sweep-vendor-leak-2026-08-05.md`) + founder rule: customer surfaces must NEVER name InboxKit. All consolidated into `ROADMAP.md`'s **PROVISIONING FIX WAVE 1** (code-only, unblocks provisioning) + **WAVE 2** (founder-gated: clock-model / send-arming / OAuth — 3 decisions pending). **Mordy's domain is recoverable IN PLACE** (live-confirmed vendor status=`active` → adopt-before-buy recovers it once Wave 1 ships; NO re-provision, NO re-charge). Founder told him to HOLD; do NOT send another go-message until Wave 1 deploys. **This SUPERSEDES the "Mordy NOT yet retried" line in the incident-hotfix paragraph below.**

**Session-2 arc: full boundary audit round-1 (2 of 7 mapped gaps closed) + msgchannel Increment 1 built (gated, uncommitted).** Audit: credstore PASS (`docs/adversarial/audit-credstore-2026-08-05.md`) + dunning-sweep FAIL (`docs/adversarial/audit-dunning-2026-08-05.md`, 3 blocking, cron LIVE — now the top `ROADMAP.md ## Open` P0). Msgchannel Inc1 (system→agent message channel, wire A `retry_setup` + wire B `credential_ready`): full-suite-green (133 files/1215 tests) + both wires behaviorally tested, adversary gate SHIP-AFTER-FIXES (`docs/adversarial/msgchannel-inc1-gate-2026-08-05.md`, 1 blocking — a phantom-domain customer message on retry). Source is UNCOMMITTED in the working tree — deliberately not merged/deployed yet. Full ledger detail: `ROADMAP.md ## Open`.

**🔥 Incident hotfix SHIPPED + DEPLOYED; Mordy can retry now but hasn't yet.** 08-04: his first `setup_infrastructure` call registered `goauthorpitchdesk.com` at InboxKit ($12.50) then 500'd opaquely — `setDns` raced InboxKit's ~30s async domain registration, and the throw left the domain vendor-registered but never persisted locally (orphan); his retry regenerated a lookalike + re-attempted registration instead of replaying. Founder-ordered full deep-dive + class-sweep (7 read-only lanes) converged into a 14-member hotfix. Adversary provenance: hotfix-gate R1 NO-SHIP (3 blocking: B1 setDns-not-re-driven, B2 adopt-filtered-by-availability, B3 DO-brick via raw CREATE UNIQUE INDEX in constructor) → fix round → **R2 SHIP** (all execution-verified incl. a forced DO reboot via `state.abort()`); frozen `docs/adversarial/incident-hotfix-gate-2026-08-05.md` (R1+R2) + `docs/adversarial/provisioning-pipeline-deep-dive-2026-08-05.md`. Battery: platform 129 files/1189 tests, typecheck clean ×5. **SHIPPED + DEPLOYED 2026-08-05** (bookkeeper-reverified this pass): merged `fad3a3e` (hotfix) + `7ab780f` (openapi/site contract + frozen adversary docs); Worker `451a1876-c49a-4fcd-905a-505489509528` live both hosts (`serverInfo` 0.2.2, `/status` ok, cron intact); site Pages `60ad814c` --branch main off `7ab780f`; `openapi.yaml` setup responses now `[202,400,401,409,502,503]` + `registerDomains`/`registrant` schema, live-confirmed in-repo. **Mordy re-confirmed 2026-08-05: NOT yet retried** — InboxKit workspace `c5188ced…` still 0 mailboxes; `goauthorpitchdesk.com` still owned/0-assigned/DNS pending. His unblock is execution-verified IN-GATE (same key `apd-setup-a-2mbx` → zero new buys, orphan ADOPTED, mailboxes provisioned) but nothing further happens until the founder sends his go-message. ⚠️ 'provisioned' ≠ 'can send': the manual OAuth/credential-push grant (deep-dive F5) is a human prerequisite before his mailboxes can actually send — it can't succeed on first provisioning (the manual minter is keyed by mailbox emails that don't exist pre-provisioning); an H-alert surfaces the moment it's needed.

**🎉 First real customer paid; agent has NOT yet provisioned. Warmup-claims wave shipped + deployed on top.** Mordy Tokayer paid 2026-07-28 17:06Z: tenant "Press Outreach" (`ten_91aab24a-43a8-45c1-bf43-af88ef633221`), Stripe sub `sub_1TyESe…` ACTIVE (platform×1 + mailbox×5, single $39.60 invoice, Stripe-verified), go message sent 2026-07-29. **As of this bookkeeping pass (2026-08-02): still 0 mailboxes / 0 rows in `vendor_spend_entries` — Mordy's agent has not run `setup_infrastructure` yet.** ⚠️ `tenants_index.plan` still reads `demo` — that column is NEVER updated post-signup (by design; `db.ts` only writes `status`); read the tenant DO `/account` or Stripe for real plan/billing state.

**Warmup-claims wave SHIPPED + DEPLOYED 2026-08-02:** commits `e4250e9` (guarded single-send primitive on the reply path + warmup auto-cancel via a new cron sweep + send-governance coverage guard) and `86996ef` (site claims: warmup lifecycle truth, exact ramp schedule 5/15/25/35/40, security-page rewrite, `/AGENTS.md` `_redirects` 301→200, sitemap lastmod 35/35, version sync server 0.2.2/CLI 0.2.1, `api.coldrig.dev` as primary host) — pushed to `origin/main`. Adversary provenance: round-1 NO-SHIP (3 blocking) frozen `docs/adversarial/warmup-claims-fix-review-2026-07-31.md`; round-2 fix wave NO-SHIP (2 blocking: A1 no-production-driver, A2 per-tenant-ceiling overclaim) → fix round → **R2 SHIP**, frozen `docs/adversarial/warmup-wave-final-review-2026-08-02.md`. Battery at ship: platform 125 files/1157 tests, typecheck clean ×5 workspaces. Deployed + live-verified: Worker `dbc4ae45-dc63-4b63-9455-51e8ffafc09f` (both hosts, cron intact; `/account` 401, `/checkout/success` 302, `/app` 200, `tools/list` 25, `serverInfo` 0.2.2); site Pages deployment `09b8deb2` --branch main (security-page/faq copy live, `/AGENTS.md` 301→200, server-card 0.2.2, sitemap 35 lastmod, zero hits of the old "AI-driven layer" fiction); IndexNow fired HTTP 200 post-site-deploy. Founder rulings honored same-deploy: warmup = bundled + auto-cancel after ramp (option b) and reply-guard = "build the guard now" — both BUILT + DEPLOYED. Full ledger detail: `ROADMAP.md ## Now`.

**Shipped LIVE + ARMED across this milestone (all drained to `archive/ROADMAP-done.md` 2026-07-29):** (1) **Token prefix + rotation** — `cr_live_` mint (legacy `cs_test_` grandfathered), `POST /token/rotate` + Settings rotate card; adversary NO-SHIP→fix→SHIP (frozen `docs/adversarial/token-rotation-review-2026-07-28.md`), merged `b843963`. (2) **Registrar arming** — `REGISTRAR_PROVIDER` env leg + per-tenant `registerDomains` opt-in + structured `Registrant` capture (required-when-true; `registrant_json` column; `IncompleteRegistrantError`→400 naming fields, no PII); adversary NO-SHIP on B1 (domain port read one call stale) → fixed (setup re-selects the domain port from THIS call's validated input) → round-2 SHIP with 5 repros (frozen `docs/adversarial/registrar-arming-review-2026-07-28.md`), merged `c1e24a5`. Integration battery GREEN (typecheck clean all 5 workspaces; platform 1122/1122; dashboard 143/143, engine 126+4skip, cli 12/12), pushed `a68361c`. **Deployed + armed by founder 2026-07-29:** Worker `4a7bbf0d-299b-4db4-b949-0bf25e841209`, both hosts live, cron intact; `REGISTRAR_PROVIDER=inboxkit` confirmed present via `wrangler secret list` alongside `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` (the arm is real, not sandbox). Post-arm checks: `/account`→401, `/checkout/success`→302, `/app`→307→200.

**CF-registrar wall — Q1 ROOT CAUSE FOUND 2026-07-29 (support ticket DROPPED).** Deep research (frozen in `docs/research/cf-registrar-domain-connect-2026-07-28.md`): the 400 `"domain is not allowed"` was OUR malformed request — InboxKit's blog guide documents a stale `{zone_id, domain, api_token}` shape; the real OpenAPI schema wants a `domains` **array** (or `connect_all`), no `zone_id`/singular-`domain`. Live-corroborated read-only via `GET /cloudflare-domains/zones` → 200 (docs source is genuine, not a WebFetch hallucination). No vendor support ticket needed. This unblocks a future **BYO-Cloudflare product path** (zone-scoped token, no NS change) — NOT on Mordy's path (he uses lookalikes); ledgered IDEA in `## Open`.

**Mordy site feedback shipped:** subdomain-vs-separate-domain mechanics (the burn/disposal argument) added to the deliverability guide's burn section, deployed + IndexNow-fired, committed `775b529`.

**Standing authorization (founder, verbatim):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done."* Arming executes autonomously (Keychain + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder.

## In flight / next

- **Wave 3 AND the P0 purchased-domain DNS-wait fix are both shipped + deployed + verified 2026-08-10** — Wave 3 combined gate SHIP r2 (11/11 attacks failed), merge `17e05d4`, deploy `0f197c4d`; P0 fix gate SHIP 0-blocking, merge `bfce3a9` on top, deploy `e2e9f061` (see "Where we are right now" above). Nothing left to gate/merge/deploy for either.
- **Next stack, in order:**
  1. **Watch Mordy's provisioning** — the deadlock is fixed and armed; his agent's next retry should clear the DNS gate and progress to mailbox provisioning (his row self-heals its `connection_type` on that same retry, no action needed). Once mailboxes exist, watch for the `cred_push_aging` alert (manual OAuth credential mint, founder-hands).
  2. **Founder click, optional acceleration:** the ADMIN_TOKEN-gated wire-A `retry_setup` operator message (paste-ready curl delivered in-chat 2026-08-10) nudges Mordy's agent sooner — his own retries work without it.
  3. **P0 fix's gate follow-ups (`ROADMAP.md ## Open`, dated 2026-08-10, items a-e):** deadlock-class detector (highest-value — no ops check covers a domain stuck pending) → post-buy DNS re-poll (cheapest close, gated on the vendor-contract capture) → vendor-contract capture on Mordy's first real provision (3-point `/domains/list` snapshot) → module-comment fix (small) → burn/replace-lane test gap (`deliverability-actions.ts:194` inherits the fix untested).
  4. **Canceled-tenant resurrection lane** — a late `charge.dispute.created` on an already-canceled tenant, plus that dispute's win, restores `billing_state='active'` and un-freezes provisioning/launches (wave-3-gate-executed, pre-existing, NOT wave-3's regression — `billing.ts:439-455`'s `isStaleBillingEvent` returns false before any exemption logic runs when there's no watermark row yet). Needs its own small lane (a state guard on the `dispute.created` write). `ROADMAP.md ## Open` 2026-08-09.
  5. **Multi-dispute unfreeze arc** — `dispute.closed(won)` lifts the freeze whenever `billing_state='disputed'` without checking for OTHER still-open disputes; deserves its own careful arc, not a mid-wave fold (`ROADMAP.md ## Open` webhook-inc2-residuals bullet, item a).
  6. **Watchtower NB-2/NB-3** (wave-3 gate's own, `docs/adversarial/wave3-integration-gate-2026-08-09.md`) — `watchtower_state` unbounded growth (needs a retention/pruning design) and wedged-DO visibility covers a THROWING DO but not a HANGING one (needs a per-tenant timeout/race in the sweep legs). Plus NB-5 (test glob narrower than the class it pins) and the CLI pretest gap (`packages/cli` needs a `pretest` build step) — both cheap.
  7. **Msgchannel Inc4** — email mirror (Inc2/Inc3, operator route + list/ack tools, shipped wave-3).
  8. **Inc-F OAuth is DEAD, not "live-verify pending"** — InboxKit's reply ruled out the generated-refresh-token path entirely; do NOT resume the live-verify item. Needs a founder decision between (a) our own Google OAuth consent flow, (b) an SMTP-egress-friendly engine host + InboxKit's plaintext SMTP credential export, (c) stay manual-mint per customer. `ROADMAP.md ## Open` 2026-08-10 ASK.
- **Founder clicks, by leverage:** (1) wire-A `retry_setup` operator message — ADMIN_TOKEN-gated curl delivered in-chat, optional; (2) Inc-F alternative decision (a/b/c above) — needed before customer #2, not urgent for Mordy; (3) Stripe dashboard webhook-delivery spot-check — still MOOT, zero Stripe events of ANY type since the wave-2 deploy (API-verified 2026-08-10); the first real delivery doubles as the readDeclineCode + `applied` live-verify.
- **Re-arm the InboxKit provisioning watch each session** — it dies with the session that started it (workspace `c5188ced-33db-436f-b970-1860e6c8c66b`, currently 0 mailboxes). Dashboard login is now durable — playwright profile `inboxkit-coldrig` (email-code flow; Google SSO dead-ends on TOTP headless) — no need to re-auth each session.
- **CF token revoke still pending (founder click)** — unchanged from prior handoff; see Landmines.
- **Fast-Start prewarmed SKU: still HELD by the founder** — unchanged, do not resume silently.
- **Open decisions / blockers (ledgered `ROADMAP.md ## Open`):** unit-economics + build-our-own-infra study · agency-bundle billing · owner spend-ceiling wiring + billing self-serve actions · BYO-Cloudflare connect path · stray InboxKit CF credential + workspace-isolation question · metric hygiene · warmup-wave fast-follow residuals R1/R3/R4/N-b · N-3 DO-table-reshape shape-version-guard convention (still no convention landed) · stale-checkout subscription-id overwrite (pre-existing, needs its own design) · alerting-completeness audit's OWN NB-2/NB-3/NB-4 (cron-leg error counters unread, digest thrown away, provisioning-vendor-failure alerts nobody — NOT this wave's scope, not yet their own bullets).

## Landmines / gotchas

- **Dunning cron fix is LIVE** (`wrangler.toml:107-108` `crons=["*/5 * * * *"]`) — F1/F2/F3 + siblings `buildOpsDigest`/`reapStaleReservations` shipped `8fd578e`, merged `c90b972`, deployed `bb8dc830` (Wave 1, 2026-08-06 morning), superseded live by wave-2's `8187d36e`. The cron now runs the FIXED code, not the old unconditional-suspend/no-try-catch version. Frozen `docs/adversarial/audit-dunning-2026-08-05.md`.
- **Msgchannel Inc1-3 (wire A `retry_setup`/wire B `credential_ready`, operator route, `list_messages`/`ack_message` tools) are ALL LIVE — but the operator route (`routes/admin-messages.ts`) is ADMIN_TOKEN-authed like every other `/admin/*` route, not usable without the prod owner-held secret.** Inc1 Finding 1+2 fixed `a9f57a4`, merged `c90b972`. Inc2+Inc3 gate F1 (`list_messages` surfaces only unread) + F2 (operator-authored messages bypass the lifecycle freeze gate) fixed `1eef443`, merged `f7ee786`→`17e05d4`, deployed `0f197c4d`. Only **Inc4 (email mirror)** remains unbuilt, founder-authorized, its own gated arc.
- **The dead-man arms only AFTER the first cron tick — not on deploy.** A fresh `WatchtowerDO` has no heartbeat recorded until the first sweep completes; `/status`'s new `sweepAgeSeconds` field is the observable (live-verified 215s/216s this pass) — healthy is `< 900` (`SWEEP_STALE_MS`, `admin/watchtower-grading.ts:62`); a stale/missing value right after a fresh deploy is expected for up to one cron interval, not a defect.
- **⚠️ CLASS (gate B-1): an exemption carved into an ordering/staleness guard for the cross-OBJECT case can silently exempt the SAME-object reverse-delivery-order case too.** The N-1 fix exempted `charge.dispute.created` wholesale from the per-lane staleness watermark (correct for "a different dispute's win shouldn't refuse this dispute's chargeback") — but Stripe orders nothing WITHIN one dispute either, so a `closed(won)` delivered before its own `created` let the late `created` write a terminal `billing_state='disputed'` with no guard, bricking the tenant forever with no alert. Fixed by bounding the exemption to the dispute's OWN recorded resolution (`isDisputeSettledWithoutFreeze`, `billing.ts:407,488`) rather than to its event type. Detail: `.claude/agent-memory/hard-builder/exemption-inherits-none-of-the-guards-reasoning.md`. Watch for this shape any time a guard grows a type-scoped exemption — walk the exempted type's OWN full lifecycle in both delivery orders, not just the case that motivated the exemption.
- **`tick()` (campaign sends) now HAS a production driver — auto-send is ARMED.** The wave-2 cron leg + DO-side 4-leg gate + per-mailbox eligibility predicate drive `runTick` every 5 min; kill switch is `AUTOSEND_DISABLED` (env var, unset = armed — wired at `env.ts:210` / `admin/ops-sweep.ts:378`). Merged `f7ee786`, deployed `8187d36e`, first post-deploy cron cycle observed quiet-correct via `wrangler tail`. Ledgered `ROADMAP.md ## Open` 2026-08-02 (now checked off).
- **The frozen VirtualClock is FIXED — real wall-clock now drives every non-sandbox tenant.** The one-shot `transactionSync` per-DO clock migration (wave-2 clock-migration lane) stamps `clock_mode='real'` and applies the accumulated delta (both signs) on first DO touch; `VirtualClock` stays sandbox/demo-only. The day-29 warmup auto-cancel and `sent_today` reset now see real elapsed time. Merged `f7ee786`, deployed `8187d36e`. Ledgered `ROADMAP.md ## Open` PROVISIONING CLASS WAVE, 2026-08-05 (now checked off).
- **'Provisioned' ≠ 'can send' until the manual OAuth grant-mint — still TRUE, but no longer silent.** `InboxKitOAuthMinter` still has zero production construction sites (`ManualOAuthMinter` hardwired); a newly-provisioned mailbox's credential-push leg still needs a 10-min manual grant. What changed: the `cred_push_aging` watchtower alert (`admin/watchtower.ts:84-95`) now surfaces the moment it's needed instead of relying on hand-tracking, and per-mailbox send eligibility (wave-2 Inc-D picker) means one unminted mailbox no longer blocks its siblings from sending. Production construction of the programmatic minter is Inc-F, still blocked on the founder's InboxKit-ticket scope answer.
- **The sandbox is built to always-succeed — green suites hide live provisioning failures.** Discovered 2026-08-05: 3 adversary rounds run against REAL entry points (not the sandbox) each caught a blocker the sandbox-backed test suite couldn't see (opaque 500s, DO-brick paths, silently-discarded opt-in fields). Gate any future provisioning-path work against real/simulated-real entry points, not sandbox-only tests. **Reconfirmed 2026-08-06:** the Stripe webhook boundary audit found the same pattern a third time (tenant-resolution field that real Stripe events never carry, dedup-before-effect fail-open) — 6 gate/audit rounds now share this exact pattern. Method: fault-inject the real path, assert durable state, never trust a green sandbox.
- **⚠️ N-3 CLASS HAZARD: a Durable Object table's PRIMARY KEY was reshaped mid-wave with no migration path (found 2026-08-06, wave-2 re-gate round 2).** `billing_event_order` changed its PRIMARY KEY shape and `CREATE TABLE IF NOT EXISTS` never alters an existing DO-SQLite table — no migration mechanism exists for a DO-table reshape, and no fresh-DO test can catch a live mismatch (fresh DOs always get the new shape). Not reachable this ship (no DO had the old shape live), but the class is real: never re-shape an existing DO-backed table's schema without a shape-version guard convention landing first. Ledgered `ROADMAP.md ## Open` 2026-08-06.
- **Shared-worktree battery tripwire lesson (2026-08-06):** a full-suite battery run in a repo with concurrent sibling worktrees/sessions can silently pass against a stale snapshot if a sibling edits a shared file mid-run. This wave's ship gates used an md5-tripwire pattern (hash the files the test run touches before and after) to catch that — any future "battery re-run" claim in this repo should carry the same tripwire, not just a raw green count.
- **The warmup ramp cap ladder (5/15/25/35/40/day) is a CEILING, not a guarantee.** The deliverability throttle/pause logic sits on top and can pin actual sends below the cap at any point in the ramp — don't read "day N cap = X" as "day N will send X".
- **Live money is real.** Worker runs LIVE Stripe keys — NEVER complete a live checkout in testing. `MORDYPILOT` redeemed (single-use, inactive). Never print Keychain values (`security find-generic-password -a coldrig -s <service> -w`).
- **`tenants_index.plan` is a signup snapshot, NEVER updated** — payment-detection must read the DO `/account` or Stripe, never that column.
- **`tenants_index` row count ≠ signups** — 63 rows (2026-07-29), 62 internal test/QA, only 1 real (Mordy). Filter known-internal patterns (`yaakovscher+*`, `@example.com`, `Burst*`, `*Test*`, verify/probe/smoke) before any business metric.
- **`registerDomains` + `registrant` must ride EVERY `setup_infrastructure` call that uses the registrar** — omitting either on a later call resets the tenant's opt-in to 0 (safe direction: 503, zero buys — but surprising). Every registrar-using call needs the full registrant object, not just the first.
- **CF-connect real schema = `{auth_type, api_token, domains:[…]}` (array) or `connect_all:true`** — NOT the blog guide's `{zone_id, domain, api_token}` (stale). Two-step dashboard flow = connect(save-credential) → `GET /zones` → connect(domains). Wire this when building the BYO-CF path.
- **Stray InboxKit CF credential + zone** — the founder's zone-scoped CF token is stored server-side at InboxKit (from the 07-28 UI connect); `dmhadvisor.com` zone is `scheduled_for_deletion` but the credential persists. Revoking the CF token invalidates it; clear both from workspace `c5188ced…` before Mordy provisions. Whether all tenants share ONE InboxKit workspace vs one-per-tenant is an open isolation question (`## Open`).
- **Registrar arming is DECOUPLED from mailbox arming by design** (`apps/platform/src/vendors/factory.ts`) — `RegistrarUnarmedDomainPort` throws until BOTH `REGISTRAR_PROVIDER` env AND the tenant `registerDomains` opt-in present. Do not collapse them.
- **`wrangler.toml` `routes` implicitly disables workers.dev** unless `workers_dev=true` is explicit — took prod down ~3 min on 07-28. Both hosts must stay live.
- **Pre-send intent log is `[dark-unarmed]`** — merged `608c80a`, engine-side, NOT armed; don't treat the double-send residual as closed until arming is verified live. `ACTIVATION.md` Gate-2 residual (1) line is STALE ("uncommitted") — fix on next doc-touch.
- **SDN "unchanged" fix** (weekend false-alarm): byte-identical re-push is `ok:true reason:"unchanged"` 200 and advances `fetched_at`; a >10% entry-count drop is still `stale`/422. Never hand-load bypassing `swapInSdnList`.
- **Glama API stale at "~12 tools"** independent of our surfaces (confirmed 25) — DONE this session (founder ran the manual description edit).
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20`.
- **Site deploys whatever is on disk;** Worker before site when counts/claims change; IndexNow fires post-site-deploy.
- **dmhadvisor.com probe residue:** its mail DNS + kwong root CNAME were deleted; pre-probe DNS snapshot for restore at `archive/2026-07-28-mordy-golive-wave/scratch-rescue/dmhadvisor-dns-snapshot-pre-probe.json`.
- **Commit state at THIS handoff (2026-08-10, current):** `main` is at `bfce3a9` (P0 purchased-dns-wait merge, --no-ff, on top of the wave-3 merge `17e05d4`), pushed, deployed `e2e9f061-e7e0-4999-bdb3-258d36582002`, live-verified this pass — supersedes the wave-3-only state (`17e05d4`/`0f197c4d`) recorded earlier in this file. P0 lane branch preserved for revertability: `fix/purchased-dns-wait-2026-08-10` (fix commit `bc1a927`, frozen gate `docs/adversarial/purchased-dns-wait-gate-2026-08-10.md` on-branch via `c3cdfd0`). All wave-3 lane branches also still preserved (`msgchannel-inc23-2026-08-06`, `webhook-inc2-2026-08-06`, `dash-idem-fixes-2026-08-06`, `watchtower-hardening-2026-08-06`, `byo-teardown-2026-08-06`, `integrate/2026-08-09-wave3`). Site deployed: Pages deployment `20a4a523` --branch main (unchanged this pass, no site-copy change in the P0 fix). **The previously-flagged stray `apps/platform/src/.claude/` nested agent-memory location is GONE this pass** (verified: path doesn't exist) — resolved at some point between 2026-08-06 and now, not by this bookkeeping pass; superseding the "still present" note in the prior version of this line. Worktrees: main checkout + the external Codex design worktree only (unchanged, not re-verified via `git worktree list` this pass).

## Key files

- `apps/platform/src/vendors/factory.ts` — adapter arming decisions (sandbox/real/unarmed); registrar-decouple comment block.
- `apps/platform/src/vendors/registrar-arming.ts` — registrar arming + registrant derivation/validation.
- `apps/platform/src/routes/token-rotate.ts` — `POST /token/rotate`.
- `apps/platform/src/engine/warmup.ts` — the ramp engine (Mordy's mailboxes = first live run).
- `apps/platform/src/engine/guarded-send.ts` — NEW 2026-08-02: shared guarded single-send primitive (caps/warmup/deliv-pause/suppression re-check + structured 429/409), wired into the reply path; `schedule_followup` must reuse this, never a second single-send path.
- `apps/platform/src/engine/warmup-cancel.ts` — NEW 2026-08-02: cron-swept auto-cancel of InboxKit warmup add-on at ramp completion (day 29); has known non-blocking residuals R1-R4 (`ROADMAP.md ## Open` fast-follow entry).
- `apps/platform/src/routes/checkout.ts` — checkout + success/cancel return routes.
- `docs/research/cf-registrar-domain-connect-2026-07-28.md` — CF-connect research; Q1 root cause + corrected schema.
- `docs/research/warmup-posture-2026-07-28.md` — ramp-only default + warmup-network honest-add-on verdict; ramp numbers.
- `docs/research/review-site-listing-packs-2026-07-27.md` — paste-ready G2/Capterra/AlternativeTo packs.
- `docs/adversarial/{registrar-arming,token-rotation}-review-2026-07-28.md` — frozen adversary verdicts for this milestone's two lanes.
- `ROADMAP.md ## Now` / `## Open` — live ledger (drained clean 2026-07-29); `archive/ROADMAP-done.md` — completed-item history.

## Resume — the definitive next-session stack, in order: watch Mordy provision (deadlock now fixed) → founder clicks incl. the ADMIN_TOKEN-gated operator message → P0 gate follow-ups a-e → canceled-tenant resurrection lane + multi-dispute unfreeze arc → watchtower NB-2/NB-3 → msgchannel Inc4 email mirror → Inc-F founder decision (NOT a live-verify — the path is dead)

**KIND B this time — watch + next-arc cycle, not a gate/merge/deploy.** Wave 3 AND the P0 purchased-domain DNS-wait fix are both fully SHIPPED + DEPLOYED + VERIFIED (`main` `bfce3a9` on top of `17e05d4`, Worker `e2e9f061-e7e0-4999-bdb3-258d36582002`) — nothing left to gate/merge/deploy. First verify state still holds: `cd ~/dev/coldstart/apps/platform && npx wrangler deployments list` should show `e2e9f061` (or later) as latest; re-check InboxKit workspace `c5188ced-33db-436f-b970-1860e6c8c66b` mailbox/domain counts via a fresh `domains/list`+`mailboxes/list` poll (**this watch monitor dies with the session that starts it — re-arm it every session**); confirm whether Mordy's agent has retried since the fix deployed (2026-08-10T15:38Z) — his next retry should clear the DNS gate and progress to mailbox provisioning without any further support-ticket dependency. **Mordy's go-message is already SENT** (founder confirmed) — do not send it again. **InboxKit dashboard login is now durable** — playwright profile `inboxkit-coldrig` (email-code flow; Google SSO dead-ends on TOTP headless) — no need to re-authenticate to check the dashboard by hand.

1. **Watch Mordy provision** — sequence: adopt (proven live) → DNS-wait (FIXED 2026-08-10, no longer blocks purchased domains) → provision → `cred_push_aging` watchtower alert → manual OAuth credential mint (founder-hands) → first sends under warmup caps. No action needed until his agent retries.
2. **Founder click, optional acceleration** — the ADMIN_TOKEN-gated wire-A `retry_setup` operator message (paste-ready curl delivered in-chat 2026-08-10) can nudge his agent sooner; his own retries work without it.
3. **P0 fix's gate follow-ups** (`ROADMAP.md ## Open`, 2026-08-10, items a-e) — deadlock-class detector (highest-value) → post-buy DNS re-poll (gated on the vendor-contract capture) → vendor-contract capture on Mordy's first real provision → module-comment fix (small) → burn/replace-lane test gap.
4. **Canceled-tenant resurrection lane** — a late `charge.dispute.created` on an already-canceled tenant, plus that dispute's win, restores `billing_state='active'` and un-freezes provisioning/launches; needs its own small state-guard lane (`ROADMAP.md ## Open` 2026-08-09, `billing.ts:439-455`).
5. **Multi-dispute unfreeze arc** — `dispute.closed(won)` lifts the freeze whenever `billing_state='disputed'` without checking for other still-open disputes on the same tenant; deserves its own careful design, not a mid-wave fold (`ROADMAP.md ## Open`, webhook-inc2-residuals item a).
6. **Watchtower NB-2/NB-3** (wave-3 gate's own findings) — `watchtower_state` unbounded growth needs a retention/pruning design; wedged-DO visibility covers a THROWING DO but not a HANGING one, needs a per-tenant timeout/race in the sweep legs. Cheap alongside: NB-5 test-glob widen, CLI `pretest` build step.
7. **Msgchannel Inc4** — email mirror, the last increment of the founder-authorized full channel (Inc1-3 all live this wave).
8. **Inc-F founder decision** — InboxKit's reply killed the programmatic-refresh-token path outright; NOT a live-verify item anymore. Decide between (a) our own Google OAuth consent flow, (b) an SMTP-egress-friendly engine host + InboxKit's plaintext SMTP credential export, (c) stay manual-mint per customer (`ROADMAP.md ## Open` 2026-08-10 ASK).

**Worktree state (2026-08-09, post wave-3 merge):** all lane/audit/integration worktrees REMOVED (branches preserved — see "Where we are right now" above for the full list); the stale `agent-a25bfeea71119a691` registrar-lane worktree carried since 2026-08-05 archived to branch `d451d28` and removed, along with the other 2 stale 2026-08-05 agent worktrees. Remaining: main checkout + the external Codex design worktree (`coldrig-design`, unrelated) only — 2 entries, `git worktree list`-confirmed this pass.

Do NOT push past founder-owned clicks (CF token revoke, G2/Capterra, listings@ rule, PulseMCP, uptime-prober, GSC/Bing recrawl) without the founder doing them. Prior HANDOFF: `archive/2026-08-05-provisioning-fixwave-plan/prior-HANDOFF.md`.
