# ColdStart / coldrig — Handoff

Agent-operated cold-email platform (28 MCP tools). **LIVE, FIRST PAYING CUSTOMER Mordy (2026-07-28).** Site https://coldrig.dev · API https://api.coldrig.dev + https://agent-cold-email-api.yaakovscher.workers.dev · repo https://github.com/YS-projectcalc/agent-cold-email (PUBLIC) · Code: `~/dev/coldstart/`. **Standing autonomy (founder, 2026-07-22): merge/push/deploy/arm to customer-ready is authorized; browser-consent clicks excepted. 2026-08-14 additions: Claude holds the prod `ADMIN_TOKEN` (Keychain `coldrig/admin-token`) and OWNS acting on watchtower alerts; 2026-08-16: founder authorized browser-driven visibility work ("do whatever you can").**

> **You are resuming `coldrig` with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (current Worker version, how far Wave B's two lanes have progressed). If they hold and the step is non-destructive, proceed. **STOP and confirm before any destructive/founder-owned step** — EXCEPT where the standing autonomy grant covers it (merge/push/deploy/arm).

## Where we are right now (2026-08-20)

1. **🚢 WAVE A (TRAINS 3+4) SHIPPED TO PRODUCTION 2026-08-20** (supersedes everything below as the current deploy; prior 2026-08-19 narrative preserved at item 2). Lanes `feat/claim-truth-2026-08-19` (train 3: claim-surface truth sweep, `003ad4d`/`cac362a`/`ea1fdaa`) + `feat/dedup-semantics-2026-08-19` (train 4: dedup-semantics wave, `5d0e720`..`d1dabc8`), folded on `integrate/wave-a-2026-08-20` (`7f1b50c` integration + `8582c3d` fix round + `6e3021c` ordered correction), merged `2c07add`, dashboard bundle rebuild `69ec655` (current `main` HEAD, confirmed = `origin/main`, 0 ahead/behind). **Gate `docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md`:** r1 **FAIL** — 1 BLOCKING (B1: `remove_mailboxes` reported `deduplicated: true` on a call that had just irreversibly released real mailboxes — the wave's own headline field lying on the money path) + 6 non-blocking → fix `8582c3d` → r2 **SHIP**, 0 blocking, 0 new (gate re-ran its own probes byte-identically to confirm). **Battery green at merged HEAD** (independently re-run by `merged-battery`, verified this pass against its own log): typecheck EXIT:0 (5 workspaces); platform 223 files/2156 passed/1 skipped; dashboard 31 files/165 tests; engine 17 files passed|2 skipped (19)/144 passed|4 skipped (148); CLI 12/12. **Deployed 2026-08-20:** Worker `a74ae0ab-14ae-42cb-8bff-31ef247c2831` both hosts (`/status` ok both); site Pages deployment `8a8333f0` (manual, project `agent-cold-email`, branch `main` — not git-connected, same as prior deploys); live `coldrig.dev/openapi.yaml` byte-identical to committed (`/remove-mailboxes` path + `deduplicated` field on the wire); dashboard serving the rebuilt bundle, MessagesPage chunk live (HTTP 200). Gate r2's non-blocking follow-ups routed to fresh 2026-08-20 `ROADMAP.md ## Open` entries (generic `deduplicated` stamp at `engine/idempotency.ts:126-127` for Wave B; IN-7 30-day epoch-reset residual; `syncMailboxQuantity` disposition correction — it does NOT throw on failure, contra the builder's report; two founder-facing ASKs; `/checkout`'s openapi gap; a `contact_operator` 201-vs-200 wire question, UNVERIFIED this pass; the Wave-B deferral set IN-9/10/11/12/17+U-2+NB3-mechanism confirmed as ONE alert-state design increment).
2. **CUSTOMER-CONTINUITY wave SHIPPED TO PRODUCTION 2026-08-19** (fully superseded as the live deploy by item 1 above; content unchanged, kept for the chain of evidence). Design (4 gate passes, 20 blocking closed) → build phase A (I1-I8, hard-builder) + phase B (I9-I15, spec-builder) in worktree `continuity`, branch `feat/customer-continuity-2026-08-18` (7 commits `5165f58..ffccdc7`, preserved) → 5 build-gate rounds: r1 SHIP-AFTER-FIXES (2 blocking) → r2 SHIP-AFTER-FIXES (1 blocking) → r3 NO-SHIP (2 blocking: `ordinal_slot_shortfall` couldn't discriminate a real failure from a deliberate/autonomous/persona-driven live-set change; remedy-coordinate mismatch) → r4 SHIP-AFTER-FIXES (1 blocking, a NEW-1 class: three setup-family bill-raising calls shipped `effect: null`) → r5 SHIP (NEW-1 class closed 3/3, incl. the pre-existing `domain_dns_incomplete` member — full evidence `docs/adversarial/customer-continuity-build-gate{,-r2,-r3,-r4,-r5}-2026-08-19.md`). Merged `--no-ff` `cf9b323`, post-gate ordered cleanups `0cc9feb` (r5 N4 comment correction + N5 unused-export drop). Merged-tree battery GREEN at `0cc9feb` (independently re-run by `merged-battery`): typecheck 0; platform 211 files/2063 passed/1 skipped; dashboard 29/143; engine 17+2 skipped/140+4 skipped; cli 12/12; wrangler dry-run bundle clean. **Deployed 2026-08-19 ~22:00Z:** Worker `319e15c4-1de7-4690-be8b-61a68082a226` live on both hosts (`/status` ok both, cron `*/5` intact); site Pages deployment `40fe9f0c` (project `agent-cold-email`, branch `main`, manual deploy — the Pages project is NOT git-connected, unlike some others); live `coldrig.dev/openapi.yaml` byte-identical to committed, `ordinal_slot_shortfall` enum on the wire. **Live-verified on `ten_91aab24a`:** persona backfill worked in production — both `domainIntents` now carry `personaSlug:"mordytee"`, were NULL pre-deploy (r1 BLOCKING-1's recovery mechanism proven on real data); `inboxesEach` still NULL both intents (the deliberate no-backfill abstention holding — this is the precondition the arm-gate blocker below still depends on); zero unhealthy checks at deploy time. Findings from the design gate and all 5 build-gate rounds are routed to 12 fresh `[IDEA]` entries in `ROADMAP.md ## Open` dated 2026-08-19 (fill-remedy re-buy of released addresses, platform-wide domain-liveness status mismatch, burned-ordinal-covered-by-no-reason, legacy-NULL-`inboxes_each` closure tie to the reconcile arm-gate, N1-N3 pricing/guard notes, a `billClaimSentence` anchor bug found by direct code read this pass, withheld-remedy persona-split, r2-gate leftovers, an emit-coverage guard idea, and 3 post-deploy watch items) — none of them blocking, all non-gating.
3. **⚠️ Still shut: the `PROVISIONING_RECONCILE_ENABLED` arm-gate (gate R6, pre-existing, dark, UNCHANGED by this wave).** The reconciler's skip test is a row COUNT while the provisioner derives per SLOT — an acknowledged `remove_mailboxes` hole is invisible to the count and the re-provision is a REAL PURCHASE; also R5: INSERT-only `inboxes_each` means a narrowed distribution can't update, and the reconciler would re-drive toward the stale wider count. Full history and the other 2 blockers (spec divergence, lifecycle-freeze guard): `ROADMAP.md ## Open` 2026-08-13 `[ASK]` entry. Do not arm.
4. **Vendor-truth wave (`a0fd314`, Worker `54b00702`) and the trains-1+2 wave (`2cef30d`, Worker `3c549e5e`) are both fully superseded deploys** — item 1 above is the current Worker/Pages version. Both waves' content (classes A-F vendor grading, channel-truth, loop-isolation) is live and unchanged by this pass; their non-blocking residuals were folded into the customer-continuity design/build and are closed or re-routed above. Detail on both, if needed: `archive/2026-08-19-continuity-ship/prior-HANDOFF.md`.
5. **Mordy (`ten_91aab24a`, "Press Outreach"): 4 mailboxes across 2 domains** (`theauthorpitchdesk.com` + `goauthorpitchdesk.com`), bought/provisioned via his agent's unattended setup-A/setup-B retries on 2026-08-18; ripening ~Sep 14-15 per the warmup ramp. His side has been running **fully agent-to-agent with no human relay** since 2026-08-18 (the InboxKit-wallet-empty incident was the last time a human touched his loop) — the customer-continuity wave just shipped is exactly the hardening this pattern demanded. No new tickets as of this pass.
6. **Prior wave history (channel-truth/loop-isolation, vendor-verdict class fix, alert-policy tune, discoverability work, operator-message read endpoints)** — all shipped+deployed across 2026-08-14 through 2026-08-18, all superseded as live deploys by item 1, evidence chains intact in `ROADMAP.md` and `docs/adversarial/`. Full prior-session narrative: `archive/2026-08-19-continuity-ship/prior-HANDOFF.md` and earlier archive dirs indexed in `archive/INDEX.md`.

## In flight / next

- **WAVE B in flight**, dispatched 2026-08-20 right after Wave A's deploy — trains 5+6 scale/monitoring lanes split by the collision analysis, plus an alert-state design increment running parallel: **watchtower-seam lane** (worktree `.claude/worktrees/scalemon`, branch `feat/scale-monitoring-2026-08-20`, branched clean off `69ec655` — S1/S4/S5/S6/S10/S11 + the train-5 monitoring-denominator members) · **disjoint-modules lane** (worktree `.claude/worktrees/scalemodules`, branch `feat/scale-modules-2026-08-20`, also clean off `69ec655` — S3/S7/S8 + the 3 promoted admin-read-endpoint bugs/S9 + the spend-ceiling remedy + the generic `deduplicated` stamp at `engine/idempotency.ts:126-127`, wave-A r2 gate ruling) · **alert-state design pass** (per-episode announced-set + materiality keys, folds IN-9/10/11/12/17 + U-2 + NB3-mechanism per the wave-A gate's confirmed deferral set — its BUILD waits for the watchtower lane to land). msgchannel Inc4 (email mirror) queued behind Wave B. **Do NOT touch these three `.claude/worktrees/*`** — live/merged lanes, orchestrator reaps them. Full scope: `ROADMAP.md ## Open` 2026-08-17 `[ORDER]` entries (TRAIN 3/4 now marked SHIPPED there).
- **Post-deploy confirmations still pending** (not code work, just watching): (a) `expires_at` stamping on the stale `retry_setup` rows via the 5-min ops fan-out; (b) a priced-effect spot check — no admin next-steps surface exists yet (404), so this lands passively on the customer's next `infrastructure_status` poll; (c) one real Stripe quantity-change vs the Stripe dashboard. Ledgered as a `ROADMAP.md ## Open` 2026-08-19 `[IDEA]` entry.
- **Open decisions (founder-gated, unchanged):** `PROVISIONING_RECONCILE_ENABLED` arm-gate (3 blockers — see "Where we are" item 3) · direction-1 (staged, after the arm-gate) · mailbox W7 tail (`~/dev/agent-mailbox/HANDOFF.md`).
- **MCP-registry: DONE** — v0.2.3 published 2026-08-17T00:12Z, `isLatest` verified. **Gmail MCP authorization [gated:founder]** and **GitHub 2FA before 2026-08-28 [gated:founder]** — both still open, see Landmines.
- Still running: the 2-hourly coldrig watch, cron job (**SESSION-scoped — dies with the session; re-create on resume**, spec below in Landmines).

## Landmines / gotchas

- **⚠️ NEW (2026-08-19): the `continuity` worktree (`.claude/worktrees/continuity`, branch `feat/customer-continuity-2026-08-18`) was NOT reaped this pass** — its branch is fully merged (`cf9b323`) and the worktree itself is currently clean (`git status` verified this pass, zero untracked files), but per the documented incident where a `git worktree remove --force` destroyed an untracked post-commit gate doc, ANY future reap of this worktree must `git status` it first and rescue anything untracked before removing. Do this before the next reap, not after.
- **`wrangler d1 migrations apply` can hit a transient 7403 auth error at DEPLOY time too, not just at watch-cron read time.** A single retry has cleared it every time it's been seen; if a deploy's D1 migration step 7403s, retry once before treating it as a real failure.
- **Lane worktrees without `node_modules` resolve `@coldstart/*` UP to the main repo's packages**, not the lane's own edited copies — `tsc`/`vitest` inside the worktree silently run against stale shared code until you either run `npm ci` in the worktree or hand-symlink `node_modules/@coldstart/{shared,platform,engine,dashboard}` to the worktree's own `packages/*`/`apps/*` (see `.claude/agent-memory/hard-builder/worktree-without-node-modules-resolves-to-main.md`). Same family: a fresh worktree's `packages/cli/dist/` is stale/absent until `npm run build -w agent-cold-email` runs there too.
- **The watch cron is session-scoped.** Re-create in a resuming session: 4 legs every 2h at :41, ALL via HTTPS — never `wrangler d1` reads (transient 7403 races generate false leg-failures), and always write the legs' temp files to the CURRENT session's scratchpad dir (a stale prior-session path makes `curl -o` exit 56, mimicking network failure) — (1) `GET /admin/support/digest` (tickets; admin token from Keychain), (2) InboxKit `POST /domains/list` + `POST /mailboxes/list` — **both are POST with a JSON body, never GET** (a bare GET 404s and mimics a vendor outage), (3) `GET /admin/ops/checks?unhealthy=1` (known baseline = the 2 Mordy `domain_dns_aging` checks; stale shared `updatedAt` >15min = dead-cron tell), (4) `GET /status` (`sweepAgeSeconds` <900; 503 = SEVERE). State + baselines: `~/.claude/mordy-watch/last-seen.json`. **Keychain retrieval — the correct form (verified working this pass):** `security find-generic-password -s admin-token -a coldrig -w` and `security find-generic-password -s inboxkit-api-key -a coldrig -w` (`-s` = service, `-a` = account, `-w` = print the secret only) — the memory shorthand "coldrig/admin-token" means account=`coldrig`, service=`admin-token`; same pattern for the InboxKit key (workspace `c5188ced-33db-436f-b970-1860e6c8c66b`). NEW issues are handled under the standing grant (builder→gate→deploy, never a hot patch); notify founder with what happened.
- **Piped test-runner exit codes lie.** `npx vitest run 2>&1 | tail -80` (or any `| head`/`| tail`) exits **0** even when vitest itself failed — the pipe discards vitest's real exit code. Verify a battery from the printed summary line (or `set -o pipefail`), never from a piped command's `$?`.
- **Founder alert-email volume is governed by the shipped debounce policy** (`apps/platform/src/admin/watchtower-policy.ts`): expected ~2/day (one per stuck Mordy domain, daily ladder) until his agent retries. Any cadence change goes through builder→gate→deploy against that policy table — never a hot patch.
- **GitHub device-flow Authorize buttons reject synthetic browser clicks** — any future device-flow auth (e.g. a registry re-publish) needs the founder's real click; mint codes only when they're at the keyboard (~15-min TTL). GitHub also demands 2FA on YS-projectcalc **before 2026-08-28** — founder action, or account actions get restricted.
- **Weekly-review launchd job broken since ~2026-07-20** (rc=143/rc=1; `~/.claude/weekly-review.log`) — flagged repeatedly, still unfixed; rough-edge lines are accumulating unreviewed.
- **`mcp-publisher` buffers its device code without a TTY** — run it under `script -q <outfile> mcp-publisher login github` and read the code from the file.
- Prior HANDOFF snapshot: `archive/2026-08-20-wave-a-ship/prior-HANDOFF.md` (this pass, full pre-rewrite narrative — customer-continuity wave and all older waves in full; the 2026-08-19 snapshot at `archive/2026-08-19-continuity-ship/prior-HANDOFF.md` covers the wave before that). **Push state:** `origin/main` confirmed at parity with local `main` (`69ec655`) at the time of this bookkeeping pass (verified via `git rev-parse HEAD`/`origin/main`, exact match) — re-check `git log origin/main..main` at resume rather than trusting this line if time has passed.

## Key files
- `docs/adversarial/customer-continuity-build-gate-r5-2026-08-19.md` — final SHIP verdict + the exact deploy checklist for this wave (site deploy required, full rebuild required, pre-arm re-check).
- `docs/research/customer-continuity-design-2026-08-18.md` — the 1752-line design canon (`deriveNextSteps` primitive, §7 precedence, founder rulings Q1-Q3).
- `docs/adversarial/vendor-verdict-gate-2026-08-14.md` — 3-round gate + ship record of the class fix.
- `docs/research/blind-agent-recommendation-probe-2026-08-16.md` — probe verdict + corrected diagnosis + option space.
- `ROADMAP.md ## Open` — the two 2026-08-16 entries (visibility [ORDER], Gmail [ASK]) + alert-policy [ORDER] + arm-gate blockers + the twelve 2026-08-19 `[IDEA]` entries from this wave's gates.
- `server.json` — v0.2.3 registry manifest — PUBLISHED to the official MCP registry 2026-08-17 (isLatest).
- `apps/platform/src/admin/watchtower-policy.ts` — the shipped alert policy table (debounce, ladders, exemptions); gate record at `docs/adversarial/alert-policy-gate-2026-08-16.md`.

## Resume — Wave B in flight (scale/monitoring lanes + alert-state design)
1. **Wave A (trains 3+4): DONE end-to-end 2026-08-20** (merged `2c07add` → dashboard bundle rebuild `69ec655` → gate SHIP `docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md` → Worker `a74ae0ab-14ae-42cb-8bff-31ef247c2831` both hosts → site Pages `8a8333f0`). Nothing to resume on Wave A itself; the `continuity` worktree from the PRIOR wave still exists too (`.claude/worktrees/continuity`, branch `feat/customer-continuity-2026-08-18`, merged, preserved) — **`git status` it before any reap** (see Landmines).
2. **Wave B is IN FLIGHT (KIND A — already dispatched 2026-08-20, no founder input needed to continue):** watchtower-seam lane (`.claude/worktrees/scalemon`, branch `feat/scale-monitoring-2026-08-20`) + disjoint-modules lane (`.claude/worktrees/scalemodules`, branch `feat/scale-modules-2026-08-20`) + the alert-state design pass (waits on the watchtower lane to land before it builds). Both lanes were branched clean off `69ec655` as of this pass, zero commits yet — check each lane's own `git log`/`git status` (READ-ONLY; don't touch these worktrees directly, the orchestrator reaps them) before assuming a lane needs restarting. Full scope: `ROADMAP.md ## Open` 2026-08-17 `[ORDER]` entries (TRAIN 1-6 bullet, TRAIN 3/4 now marked SHIPPED).
3. **Founder clicks (KIND B — present, don't do):** (a) Gmail `/mcp` authorization; (b) GitHub 2FA before 2026-08-28.
4. **Visibility [ORDER] remainder (KIND A):** mcp.so + PulseMCP submissions, guide/listicle outreach (see the `ROADMAP.md` 2026-08-16 entry for done/remaining).
5. **Re-create the watch cron** (spec in Landmines — InboxKit legs are POST, not GET; D1 migration-apply 7403 is a retry-once transient; use the corrected `security find-generic-password -s <service> -a coldrig -w` form) if not already running.
6. **Pre-arm re-check before ever touching `PROVISIONING_RECONCILE_ENABLED`:** confirm no live tenant has picked up a non-NULL `domain_intents.inboxes_each` for a short ordinal (this is what currently insulates `ten_91aab24a`) — and don't arm at all until the 3 blockers in `ROADMAP.md ## Open` 2026-08-13 close.
