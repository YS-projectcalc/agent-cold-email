# class-sweeper memory index

- [coverage-ledger.md](coverage-ledger.md) — ColdStart surfaces that UNDER-COUNT in a sweep (downstream signal consumers, port error contract, sandbox masking, dropped webhook fields, schema can't-express, cron lanes, stalls-not-just-throws, apps/engine as a second service). Read FIRST.
- [idempotency-at-least-once-surfaces.md](idempotency-at-least-once-surfaces.md) — sweeping non-idempotent at-least-once external inputs: activation-latent inputs (sandbox delivers exactly-once), fake `:${now}` idempotency keys, client-retry vs vendor-redelivery variants.
- [vendor-mutation-saga-surfaces.md](vendor-mutation-saga-surfaces.md) — stranded-vendor-state sweeps: wrappers that delete their own record on throw, `_idempotencyKey` no-op adapters, spend ledger without resource identity, worktree copies inflating greps.
- [idempotency-replay-surfaces.md](idempotency-replay-surfaces.md) — keys that exist but don't replay: `_idempotencyKey` ignored, the claim-kept-effect-lost mirror, clients that send no key, a test encoding the defect, docs asserting absent coverage.
- [error-mapping-surfaces.md](error-mapping-surfaces.md) — opaque-500 sweeps: THREE mapping chains (HTTP onError / MCP handler / engine statusFor), subclass-mapped-but-base-unmapped, openapi as published contract, per-call-site graceful catches.
- [vendor-identity-leak-surfaces.md](vendor-identity-leak-surfaces.md) — vendor-abstraction sweeps: throw→response boundary is guarded; leaks hide in caught-and-stored `err.message` (deliverability_actions + events metadata → activity/account, mailboxHealth.vendorHealthError) and in the CUSTOMER_SAFE_STEPS endpoint-path allowlist itself.
- [monitoring-completeness-surfaces.md](monitoring-completeness-surfaces.md) — narrowed-view-as-truth sweeps: members with NO WHERE/LIMIT (hardcoded 0, skip-dark check, two-store split), count-half blind with list-half, watch-spec vs state-file leg drift, compliant pagination/bounded-sweep templates.
- [claim-surface-drift-surfaces.md](claim-surface-drift-surfaces.md) — claim-drift sweeps: the EIGHT hand-written copies of the tool contract, drift runs both directions, support-kb.ts + server-card.json unguarded, openapi enum as a machine contract, the tool-count guard's two scope leaks.
- [nonterminal-frozen-terminal-surfaces.md](nonterminal-frozen-terminal-surfaces.md) — non-terminal-frozen-as-terminal sweeps: enumerate EVERY return (keyword greps miss quoteOnly/CapacityPending), `DEFAULT '<terminal>'` in schema, discarded trySend booleans, alert-clear-by-absence, tool descriptions as call sequences.
- [dedup-semantics-surfaces.md](dedup-semantics-surfaces.md) — undisclosed-collapse sweeps: the disclosure boundary (decision→RPC→route→shared type→openapi), engine-side drop-for-missing-dedup-key, alert debounce deleting FLAPS not just one-shots, re-stamp breaking the keyset cursor, tests pinning the defect.
- [vendor-inventory-and-operator-read-scope-surfaces.md](vendor-inventory-and-operator-read-scope-surfaces.md) — paid-vendor-resource-without-a-row + operator-read-scope sweeps: MailboxPort has no list method (reconcile unbuildable), the only vendor enumeration lives in an out-of-repo watch cron, mailbox_intents has ZERO readers outside its writer, `count: list.length` after a LIMIT.
- [billed-effect-and-adapter-contract-surfaces.md](billed-effect-and-adapter-contract-surfaces.md) — billed-effect (E) + adapter-vs-LIVE-response drift (F) sweeps: `request<T>`'s `body as T` is F's one root, the live-probe recipe, fixtures whose header lies about provenance, pre-checks made impossible by a `private` method, the 4-axis score for a billed effect.
- [money-out-funding-surfaces.md](money-out-funding-surfaces.md) — vendor-FUNDING sweeps: THREE grading surfaces per vendor (mapper + nine 200-`{error:true}` branches + engine), `use_wallet_balance` as the money-out tripwire, a pre-call dispatch counter burned by zero-cost refusals, ceiling models OUR cost not the wallet, sandbox ports have zero throws.
