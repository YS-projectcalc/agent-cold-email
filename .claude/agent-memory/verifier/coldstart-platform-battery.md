---
name: coldstart-platform-battery
description: coldstart repo (apps/platform) verification battery — declared scripts, runtime, test count baseline
metadata:
  type: project
---

Monorepo root `~/dev/coldstart`: `npm run typecheck` / `npm test` / `npm run build` at root fan out via `--workspaces --if-present` to all 3 workspaces (apps/platform, agent-cold-email, packages/shared) — always use the root script, never bare `tsc --noEmit` inside apps/platform.

apps/platform full test lane: `npm test` (root) → `vitest run` under `@cloudflare/vitest-pool-workers`. Baseline as of 2026-07-12: 33 test files / 140 tests, ~20s test time but ~150-160s Miniflare/DO setup overhead (full `npm test` wall time ~20-35s reported by vitest but expect 2-3min real wall time incl. workspace fan-out). Expected non-failure noise: several "uncaught exception" lines print in the log from negative-path tests (RateLimitError, NotFoundError, TenantIsolationError, ValidationError) intentionally thrown inside the Workers RPC harness — these are NOT failures; only trust the final "Test Files X passed / Tests Y passed" summary line.

Deploy dry-run: from `apps/platform`, `npx wrangler deploy --dry-run --outdir <dir>` — verify by EFFECT: check the outdir actually contains `index.js`/`index.js.map`, not just exit 0 + the "--dry-run: exiting now." text.

vitest.config.ts (apps/platform) has no `include`/`exclude` override, so it uses vitest's default recursive `**/*.test.ts` glob — any new `test/*.test.ts` file is automatically wired in without config changes; confirm by counting files vs the reported "Test Files N passed" count.

No false-green trap hit this session — hard-builder's uncommitted engine-fix increment (bounce severity A1-A3, retry cap A4, dunning decline-codes A5, events/idempotency dedupe B1-B4, stable vendor key B3, Stripe usage idempotency B5, unsubscribe headers C1-C2) checked out complete against a 20-item scope checklist on file:line grounding, full battery green, and both G3 revert-fail-restore proofs (soft-bounce branch, events OR IGNORE dedupe) failed correctly on the reverted code and passed clean after byte-exact restore.

**2026-07-14 human-journey + Stripe drive (live prod, not local battery):**
- Sandbox permission classifier blocks bundled multi-command shell constructs (a `for` loop calling bare `tr`/`ls`/`sed` fails with "command not found" even though the binaries exist at e.g. `/bin/ls`) — this is NOT an environment bug, it's the "one operation per Bash call" collateral-deny biting inside a loop body. Fix: do multi-step crawls/transforms in a single Node script (`fetch` + regex) instead of a shell for-loop; use full paths (`/bin/ls`, `/usr/bin/curl`) if a bare invocation 127s.
- **Curl-only API checks are NOT sufficient to verify a human-facing form works — drive it in a real browser.** Direct `curl -X POST /api/waitlist` returned 200 `{"ok":true}` (endpoint genuinely wired), but the live landing-page form was silently 100% broken for real users: `site/assets/waitlist.js`'s `form.querySelector(".form-status")` returns null because `<p class="form-status">` in `site/index.html` is a SIBLING of `<form class="waitlist">`, not a descendant — the null deref throws before `fetch()` ever fires (confirmed via Playwright `page.on('pageerror')` + zero `/api/waitlist` requests logged network-side). Visually: click "Notify me", nothing happens, no error, no success, field doesn't even clear. Lesson: when a battery item is "does this human form work," always instrument a live click with `page.on('response')`/`page.on('pageerror')`, don't just confirm the backend endpoint independently — a wiring bug between the two is invisible to either check alone.
- Stripe/billing on prod verified live (not just code-read): `STRIPE_SECRET_KEY` unset → `POST /checkout` returns `{"mode":"simulated", url: ".../checkout/simulate?..."}`; hitting that URL flips the tenant's plan for $0 with zero payment collection (`{"upgraded":true,"plan":"launch"}`, confirmed persisted via `GET /account`). `POST /webhooks/stripe` fails closed 503 `"STRIPE_WEBHOOK_SECRET unset"` — matches ACTIVATION.md Gate 2 exactly.
- Dashboard session flow (`POST /dashboard/session` from a pasted tenant bearer → httpOnly/Secure/SameSite=Strict cookie → SPA at `/app/dashboard`) DOES work end-to-end in a real browser at both 1440px/390px — verified with a fresh signup token, not asserted from reading the SPEC.
- ACTIVATION.md can be STALE in the optimistic direction too, not just the pessimistic one: its Gate-4 checklist still lists the CAN-SPAM mailing address as an unfilled `[bracket placeholder]`, but `site/terms.html`/`site/privacy.html` on prod already have a real filled-in address — always re-check the live artifact, don't trust the checklist's checked/unchecked state either way.

**2026-07-16 apps/engine (real Gmail SMTP/IMAP daemon) Gate-1 self-send smoke — PASS, with a trap found:**
- Runtime contract: `GET /health` (no auth), `POST /v1/send` and `POST /v1/poll` (Bearer `ENGINE_AUTH_SECRET`) — see `apps/engine/README.md`. `dist/` at `apps/engine/dist/` is a plain `node apps/engine/dist/index.js` with `ENGINE_AUTH_SECRET`/`ENGINE_PORT`/`ENGINE_STATE_DIR`/`MAILBOX_CREDENTIALS_FILE` env; boots in ~1s, `/health` → `{"status":"ok","uptimeSec":N}`.
- **`POST /v1/poll` with `sinceCursor:0` against a mailbox with real history is UNBOUNDED and dangerous as a verification step** — `apps/engine/dist/imap.js`'s `fetchSince` does `uid: "${sinceUid+1}:*"` with `source: true`, i.e. fetches the FULL RFC5322 source of every message with UID > sinceCursor. On a real personal Gmail account (UID space already >147k here) this means pulling the entire inbox's raw source in one unbounded in-memory array before the HTTP response returns — it hung indefinitely and had to be `kill -9`'d. Never poll from cursor 0 against a live/real mailbox with history.
- Separately, even a *bounded* poll would show ZERO events for a same-account self-send by design, not bug: `classify.js`'s `classifyReply`→`resolveOriginal` only emits an event when the inbound message's `In-Reply-To`/`References`/other embedded Message-IDs resolve back to a KNOWN prior send-thread. A fresh self-send with `inReplyToMessageId: null` has nothing to resolve against, so it's correctly dropped (`// not a reply to any send we know -> ignore`). Don't use `/v1/poll` to verify a same-account self-send landed — verify via direct IMAP instead (below).
- **Correct verification for a real send:** confirmed via a standalone node script using the repo's own `node_modules/imapflow`, doing `client.search({header:{subject: ...}})` (server-side IMAP SEARCH, cheap) then `client.fetch(uid, {envelope:true, uid:true})` on just the matching UID(s) — compared the returned `envelope.messageId` byte-for-byte against the `/v1/send` response's `messageId`. This is fast and safe. Confirmed via `node_modules/imapflow/lib/commands/fetch.js:49-91` that imapflow fetches use `BODY.PEEK`/`.PEEK` by default — neither `source:true` nor `envelope:true` fetches mark a message `\Seen`, so read verification against a real personal inbox is safe not to alter its state.
- SSH credential pulls from the droplet (`ssh root@142.93.12.85 'cat /root/mailboxes.json'`, a live Gmail app-password) are blocked by the auto-mode permission classifier under "Production Reads" even when a teammate (team-lead) instructs it — requires the actual user's explicit runtime approval or a pre-authorized handoff of the already-pulled file; a teammate message alone does not satisfy it.

**2026-07-23 integrated-tree battery (I3+I4 + brand-sweep merge, main @ 60275b3) — REAL test FAIL found, root-caused to env state not code:**
- `apps/platform/test/checkout.test.ts` (new in this merge, brand-sweep's B1 money-path suite) asserts simulated/test-mode checkout behavior (`mode: "simulated"`, no live Stripe). It fails 5/5 when the shared `apps/platform/.dev.vars` has a non-empty `STRIPE_SECRET_KEY` set locally — `src/engine/billing.ts:40` gates checkout mode purely on `Boolean(env.STRIPE_SECRET_KEY)`, so any truthy value (even a stale local test key from a prior manual-testing session) flips every checkout test from simulated to real-Stripe-path and breaks all 5 assertions. This is an environment/config-state failure, not a merge regression — confirm by checking `.dev.vars` key length (`grep -n "^STRIPE_SECRET_KEY" .dev.vars | awk -F'=' '{print length($2)}'`) before concluding the merge broke checkout logic. Do NOT edit `.dev.vars` to "fix" this in a verifier pass — report FAIL with root cause and let the owning agent/user decide whether to unset the key or update the test harness's env isolation.
- Confirmed AGAIN: `grep -c PATTERN file` on a file with a real match still exits 0, but on a genuine zero-match file (correct, expected outcome) it exits 1 — chaining `grep -c A; grep -c B` in one Bash call where B has 0 matches makes the whole tool call report `Exit code 1` even though both individual counts were exactly the intended values (A≥1, B=0). Read the printed counts, not the shell exit code, when using grep -c as a presence/absence check.

**2026-07-23 re-verify at main 485a4fe (hermetic-env fix + G5 gate(a) + integration pin) — full GREEN, ambient STRIPE_SECRET_KEY survived correctly:**
- Same `.dev.vars` with the real-looking `STRIPE_SECRET_KEY` still present (deliberately untouched) no longer flips `checkout.test.ts` — the hermetic-env fix (allowlist-constructed bindings, null-neutralized ambient `.dev.vars`) now shields the test env from that ambient key. Full `npm test`: platform 93 files/648 tests all passed (0 failed), dashboard 99/99, engine 95p/3s, CLI 9/9 — EXIT:0 clean across the board, first time this session the full root `npm test` has exited 0.
- New baseline for platform full lane: **93 test files / 648 tests**, up from the pre-hermetic-fix 91 files/639 tests (checkout.test.ts fixed + G5 gate(a) added ~3 tests + integration pin edits). Update expected-ballpark anchor to 648 going forward until the next real feature lands.
- `apps/platform/src/env.ts` has 13 occurrences of the string "spend-arming" (env.ts:comments+guard logic) — confirms the REGISTRAR_* spend-arming fields are wired into the same guard as STRIPE_*.
- `apps/platform/test/hermetic-env.test.ts` + `apps/platform/test/spend-armed-env-coverage.test.ts` (2 files, 10 tests) pass both in the full run (0 failures overall) AND standalone via a targeted `npx vitest run test/hermetic-env.test.ts test/spend-armed-env-coverage.test.ts` (Test Files 2 passed (2) / Tests 10 passed (10)) — useful pattern when a teammate asks to confirm specific files passed "inside the full run" and the default reporter doesn't list individual passing filenames (only failures get named) — a cheap targeted re-run of just those files is fast (~4s) corroborating evidence, doesn't require re-running the whole suite.

**2026-07-23 re-verify #3 at main 33eb6cf (signup-auth wave: magic-link login + human signup, migration 0009_login_links) — full GREEN, new baseline 676:**
- Full `npm test` (root) EXIT:0. dashboard 25 files/106 tests (up from 23/99 — new signup SPA verify-page tests), engine unchanged 95p/3s, platform **95 test files / 676 tests** (up from 93/648 — matches the builder+adversary's own pre-merge measurement of 676/676 exactly), CLI 9/9. Zero FAIL lines anywhere in the log.
- Targeted corroboration pattern confirmed again useful: `npx vitest run test/checkout.test.ts test/login.test.ts test/hermetic-env.test.ts` → 3 files/36 tests pass in ~6s. One expected-noise "uncaught exception" line (`NotFoundError: checkout session ... not found for this tenant` from `completeSimulatedCheckout`) printed mid-run — this is a negative-path test intentionally throwing inside the Workers RPC harness, not a failure; matches the documented noise pattern, don't mistake it for a real error.
- New platform baseline going forward: **95 test files / 676 tests**, dashboard **25 files / 106 tests**. Update again when the next feature wave lands.
- `apps/platform/migrations/0009_login_links.sql` contains "login_links" 3 times (table name + index name(s)) — confirms migration file content, not just filename.
- Ambient `STRIPE_SECRET_KEY` in `.dev.vars` continues to be correctly neutralized by the hermetic-env fix through a THIRD merge wave — this fix is holding, not a one-off.

**2026-07-23 re-verify #4 at main 1dccd94 (OFAC screening 32d5b6b + provisioning-core 8674da7 + integration glue) — REAL FAIL, cross-feature test-fixture gap not a union-file break:**
- Full `npm test` EXIT:1. dashboard 106/106 unchanged, engine 95p/3s unchanged, CLI 9/9 unchanged, but platform: **106 test files passed | 2 failed (108) / 758 tests passed | 5 failed (763)**. All 5 failures are in `test/ga-gates-factory-wiring.test.ts` (2) and `test/ga-gates-g3-activation-state.test.ts` (3) — these are provisioning-core's OWN pre-existing G3/factory-wiring tests, NOT the 5 hand-resolved union-conflict files team-lead flagged for suspicion (env.ts, scheduled.ts, schema.ts, setup.ts, spend-armed-env-coverage.test.ts all fine).
- Root cause: the integration commit wired real OFAC `screening_status` into `getAccount`'s `activationState` (stub retired). Provisioning-core's own G3 activation-state tests create a tenant and immediately expect `pending_provisioning`/`active`/a `"real"` factory bundle — but with real screening now live, a freshly-created tenant has NO screening record yet, which the honest-activationState logic correctly reports as `screening_hold` (fail-closed default, matches OFAC's own design intent) — so `screening_hold` shows up instead of what the OLD (pre-OFAC) fixtures expected. This is a **test-fixture gap**: those two test files need a "mark tenant screening-clear" helper call added to their setup before this wave can ship, not a code bug in either feature — but it IS a real, currently-failing assertion against the actual behavior of the merged tree.
- **Targeted union-file run is CLEAN**, which is the important disambiguator: `npx vitest run test/spend-armed-env-coverage.test.ts test/hermetic-env.test.ts test/spend-ceiling-coverage.test.ts test/ofac-screening.test.ts test/checkout.test.ts test/login.test.ts` → 6 files / 57 tests, ALL PASS. So the 5 hand-resolved union files (env.ts/scheduled.ts/schema.ts/setup.ts/spend-armed-env-coverage.test.ts) composed correctly — the break is a downstream fixture assumption in unrelated test files that never anticipated the OFAC integration landing. Always run the named "suspect" targeted list AND the full suite — a clean targeted list does not mean the wave is safe; the full-suite delta is what caught this.
- Asset/stale-chunk verification pattern that worked well without running a build: read `apps/platform/public/app/index.html`'s directly-referenced `<script src>`/`<link href>` hashes, confirm each file exists on disk, THEN separately `grep -o "ChunkName-[hash].js"` inside the main bundle (`index-*.js`) to confirm a lazy-loaded route chunk (e.g. `LoginVerifyPage-*.js`, not directly referenced in index.html since it's dynamically imported) is referenced by its CURRENT on-disk hash, not a stale one. Don't expect route-split chunks to appear in index.html directly — check the main bundle's dynamic-import references instead.
- New platform baseline for next wave: expect the SAME 758/763 (105→106 files unaffected by fixture fix + 2 files carry the fixed assertions) once the G3/factory-wiring test fixtures get the screening-clear helper — don't be surprised if the next re-run count is unchanged in file/test COUNT but flips PASS.

**2026-07-23 re-verify #5 (final GA gate) at main 6299788 — full GREEN, fix confirmed genuine not a weakened-assertion patch:**
- Full `npm test` EXIT:0. platform **108 test files / 763 tests, all passed** — exact match to builder's own pre-verify measurement. dashboard 106/106, engine 95p/3s, CLI 9/9, all unchanged. Zero FAIL lines.
- `git diff --stat 1dccd94..6299788 -- apps/ packages/` scoped to exactly 4 files: `test/activation-gate.test.ts`, `test/ga-gates-factory-wiring.test.ts`, `test/ga-gates-g3-activation-state.test.ts`, `test/helpers.ts` — all under `test/`, confirming the claimed "test-only" fix scope with a single command.
- **Read the actual diff content, not just the file list** — worth doing here since the stat showed net deletions in `activation-gate.test.ts` (18 lines, mostly `-`), which could in principle mean deleted assertions. Reading it: the deletions were a locally-defined `seedBenignSdnList` helper function body getting hoisted into the shared `test/helpers.ts` (dedup across 3 files) — zero `expect()` lines touched. The two previously-failing files each gained one `beforeEach(async () => { await seedBenignSdnList(); })` block and nothing else changed structurally. When a "test-only fix" diff shows deletions, always read the actual diff (cheap, one `git diff -- <path>` call) before trusting a stat-only scope check — a stat check confirms WHICH files changed, not WHETHER the fix is legitimate vs. an assertion-weakening patch.
- Targeted re-run of the 3 relevant files (`ga-gates-factory-wiring.test.ts`, `ga-gates-g3-activation-state.test.ts`, `activation-gate.test.ts`) in isolation: 3 files / 32 tests, all pass — explicit corroboration beyond the aggregate count.
- This closes the 5-wave verification arc for the GA deploy (OFAC + provisioning-core + integration glue + fixture fix) — battery went FAIL→PASS across battery #4→#5, root cause was real (fail-closed screening correctly surfacing against pre-OFAC fixtures), fix was a genuine fixture update not a suppression.

**2026-07-24 battery #6 (SDN relay + alert throttle + pre-arm hardening, main d7d2753) — tests/typecheck/scope all GREEN, but a genuine migration-numbering gap found:**
- Full battery: typecheck EXIT:0 (5 workspaces clean), `npm test` EXIT:0 with platform 111 files/793 tests all passing (dashboard 106/106, engine 95p/3s, CLI 9/9 unchanged) — matches team-lead's expected count exactly. Named files `ofac-sdn-ingest.test.ts`/`ofac-sdn-alert.test.ts`/`ofac-sdn-refresh.test.ts`/`ofac-screening.test.ts` confirmed in the passing set both in the full run and standalone (4 files/40 tests, targeted re-run). The "send_email binding called" log lines that appear when running the alert/screening tests are expected simulated-adapter noise (ops-alert emails firing as designed), not failures.
- **`apps/platform/src/setup.ts` doesn't exist — the real file is `apps/platform/test/setup.ts`.** It's a vitest-only harness that imports each migration file via Vite's `?raw` suffix and runs every statement against `env.DB` in-order — not the real D1 migration mechanism. Don't go looking for a `src/setup.ts`; the "union migration list" lives in `test/setup.ts`.
- **Migration numbering gap found and reported, not silently passed:** `ls apps/platform/migrations/` goes 0001...0009, then jumps straight to 0011 — **`0010` does not exist**. Confirmed via `git log --all --diff-filter=A --name-only | grep "0010_"` (zero output) that no `0010_*.sql` file was EVER added in this repo's history — this is a numbering skip, not a deleted-after-applied migration. `test/setup.ts`'s own import list is internally self-consistent (never references 0010 either), and `wrangler.toml`'s `migrations_dir = "migrations"` + `wrangler d1 migrations apply` (used in the real `deploy` npm script) applies files by filename/numeric order without requiring contiguous numbers — so this is very likely benign for the actual remote apply, but it IS a real deviation from an explicit "no gaps" check a teammate asked for. Report gaps like this verbatim even when you believe them benign — let the requester decide if it's blocking, especially right before a real D1 remote migration apply.
- Confirmed in battery #7: this same 0010 gap is inherent/historical, NOT reintroduced by the billing merge — a later commit (`6baf0bc` "quantity-billing fixup: renumber migration 0013 -> 0015 (0013/0014 taken by merged SDN relay)") shows the team actively manages collisions by renumbering on merge, which explains why gaps like 0010 accumulate (a reserved number from an abandoned/renumbered branch). Expected going forward, not a new defect each time.

**2026-07-29 battery #8 (token-rotation + registrar-arming integration merge, main c1e24a5) — full GREEN, first integrated verify of both lanes together:**
- Full `npm run typecheck` EXIT:0, all 5 workspaces clean (dashboard/engine/platform/agent-cold-email/shared), zero error lines.
- Full `npm test` EXIT:0 (ran via background task, foreground 300s limit is now consistently too short — plan for background from the start). Per-workspace: dashboard 29 files/143 tests, engine 16 files/126 tests passed + 2 files/4 tests skipped (both pre-existing `describe.skipIf(!RUN)` E2E suites requiring real Gmail/GreenMail, not new), **platform 122 files/1122 tests all passed** (vs. team-lead's ≥1113 floor), CLI (agent-cold-email, node:test runner not vitest) 12/12. Zero FAIL lines anywhere in the full log scan.
- New platform baseline: **122 test files / 1122 tests**. Big jump from battery #7's 116/817 — reflects multiple merged waves in between (SDN relay, quantity-billing, and now token-rotation + registrar-arming), not a single-wave delta; don't be alarmed by large baseline jumps between verifier sessions, always re-derive from the current log rather than diffing against a stale expected count.
- Targeted corroboration (per established pattern, vitest's default reporter only names failing files): `test/token-rotate.test.ts test/token-prefix-legacy.test.ts` → 2 files/9 tests pass; `test/registrar-arming.test.ts test/registrar-unarmed-alert.test.ts` → 2 files/23 tests pass. Registrar targeted run prints an expected `RegistrarUnarmedError` uncaught-exception block (negative-path test intentionally hitting the not-armed guard) — matches the documented noise pattern, not a failure.
- Build (`cd apps/platform && npm run build` = `wrangler deploy --dry-run --outdir dist`) EXIT:0, verified by EFFECT not just exit code: `rm -rf dist` first, then confirmed `dist/index.js` (1,187,173 bytes) + `dist/index.js.map` genuinely materialized post-run, not a stale artifact from a prior build.
- CLI workspace (`agent-cold-email`) uses `node:test` runner (TAP-style `ℹ tests`/`ℹ pass`/`ℹ fail` output), not vitest — different summary line format than the other 4 workspaces; grep patterns for "Test Files"/"Tests" won't match it, use `ℹ pass`/`ℹ fail` instead.

**2026-07-24 battery #7 (quantity-billing merge, main 8b5e834) — REAL typecheck FAIL, test suite green ONLY because vitest doesn't enforce TS types at runtime:**
- `npm run typecheck` EXIT:1 (NOT 0) — 2 errors in `apps/platform/test/ofac-screening.test.ts`:
  `test/ofac-screening.test.ts(174,72): error TS2345: Argument of type '"launch"' is not assignable to parameter of type 'TenantPlan'.` (and again at line 203). Root cause: the billing merge's "managed-plan collapse" narrowed `TenantPlan` (in `packages/shared/src/types.ts`) from whatever tiered set existed before down to exactly `"demo" | "free" | "managed"` — `"launch"` is no longer a valid plan literal. `ofac-screening.test.ts` (written during the earlier SDN-relay wave, battery #6) calls `mintTenant("Globex Corp International", "launch")` twice and was never updated for the type collapse. Blast radius checked and CONTAINED: `grep -rln '"launch"' apps/platform/test/*.ts` → only this one file, only these 2 lines.
  **CRITICAL: `npm test` (root) still exits 0 and platform still shows all 817/817 tests passing** — vitest transpiles TS via esbuild for the actual run and does NOT type-check, so an invalid-per-TypeScript string literal executes fine as a plain runtime string. **This is exactly why typecheck and test must ALWAYS be run and reported as separate battery items — a green `npm test` does not imply a green `npm run typecheck`, and this is a real case where they diverged and only typecheck caught a genuine cross-lane regression** (an enum-narrowing change in one merged lane silently breaking a literal in an unrelated file from a different already-merged lane).
- All other items clean: full test 116 files/817 tests (exact match to expected), 5 named billing test files (`stripe-prices`/`quantity-sync`/`burn-replace-billing`/`remove-mailboxes`/`add-billing-projection`) 5/5 files, 24/24 tests targeted-confirmed; cross-lane files (`ofac-screening`/`hermetic-env`/`spend-armed-env-coverage`) 3/3 files, 27/27 tests targeted-confirmed (ofac-screening.test.ts passes at RUNTIME despite failing typecheck — same divergence as above); `scheduled.ts` still has all 3 OFAC scheduled functions (2 occurrences each, unclobbered); `reportUsageRecord` = 0 occurrences repo-wide (deleted metered path stayed deleted); migrations dir now 0001-0009,0011-0015 (0015 new, same historical 0010 gap, no dupes), `test/setup.ts` imports all 6 expected (0009,0011,0012,0013,0014,0015).

**2026-08-09 battery #9 (wave-3 integration, worktree `~/dev/coldstart-worktrees/integration-wave3`, branch `integrate/2026-08-09-wave3` @ cc4e433, 5 lanes: byo-teardown, msgchannel-inc23, webhook-inc2, dash-idem-fixes, watchtower-hardening) — full GREEN after correcting two false negatives, both process/environment not code:**

- **⚠️ CWD DRIFT HAZARD (new, serious): a shared-shell multi-agent session can silently move your ambient cwd out from under you mid-battery, with NO explicit `cd` on your part.** Confirmed the shell state is NOT reliably scoped to this agent's own commands when sibling worktrees + teammate agents are active in the same session — a backgrounded `nohup <cd-to-worktree> && npm test &` call correlated with (though root cause not fully pinned) the session's ambient cwd for subsequent *non-cd'd* commands silently reverting to a DIFFERENT repo checkout (the sibling main-branch clone one level up) rather than staying in the worktree. Caught it only because the pre/post tripwire hash comparison came out suspiciously "changed" and I cross-checked with an explicit `cd`+`pwd`+`git branch` in the SAME call for both candidate directories. **From now on in any multi-agent/multi-worktree session: never trust an ambient cwd across calls — explicitly `cd <target> && pwd && git branch --show-current` (or similar self-verifying prefix) in EVERY command from the start**, not just at the first one. A single early `git status` showing the right branch does NOT guarantee later bare (`no-cd`) commands in the same session stayed there.
- **`packages/cli` (agent-cold-email workspace) needs `npm run build` before its tests will pass — root `npm test` does NOT build it first, and this is a REAL, standing gap, not new this wave.** `test/claim-surface.test.mjs` deliberately reads the package's compiled `dist/` output (a guard against shipping retired pre-launch/waitlist pricing language — comment explicitly says "Scans the BUILT dist/ output (never src/)... since dist+README.md are exactly what ships to npm"). A fresh `npm ci` + root `npm test` with no prior CLI build reliably throws `ENOENT: scandir .../packages/cli/dist` and kills that whole `node --test` run non-zero. Fix for future batteries: after `npm ci`, always `cd packages/cli && npm run build` (plain `tsc -p tsconfig.json`, ~1s) BEFORE running the full test lane, or just always re-run `packages/cli` tests standalone after building if the embedded root-`npm test` CLI leg failed on ENOENT — don't report it as a wave regression without checking this first.
- **Running your OWN parallel build/test commands while an in-flight `npm test` fan-out hasn't finished ALL workspaces yet produces a false-negative flake, not a real bug — confirmed by reproduction.** After the `vitest` child process (covering dashboard/engine/platform) exited, the root `npm test` fan-out had already silently moved on to the LAST workspace (`packages/cli`, which uses a *different* process — plain `node --test`, not vitest) — I mistook the vitest PID's exit for the whole test run being done and immediately launched platform + dashboard builds AND a standalone CLI re-run in parallel with that still-running embedded CLI test. Result: an async `mcp bridge` RPC test that normally passes in ~286-295ms timed out at 15004ms in the contended run — reproduced clean 12/12 passes (that same test in ~286-295ms) in TWO separate re-runs once `ps aux` confirmed zero other node/vitest/wrangler/vite processes were active. **Lesson: before declaring a test workspace's failure "real," check `ps aux` for competing node/vitest/wrangler/vite processes you yourself started — and don't assume a tracked child PID exiting means the whole `--workspaces` fan-out is done; the root npm process may have moved to a different-runtime workspace (vitest→node:test) you're not watching.**
- Once corrected (CLI built first, re-run in isolation): full battery GREEN. New baselines — **platform 166 test files / 1519 tests** (up from battery #8's 122/1122 — big jump reflects all 5 wave-3 lanes' new test files), dashboard unchanged 29 files/143 tests, engine unchanged 16 files passed|2 skipped (18) / 136 passed|4 skipped (140), CLI 12/12 (`node:test` TAP-style `ℹ pass`/`ℹ fail`, not vitest). Combined clean total: **227 test files / 1810 tests passed, 4 documented skips, 0 real failures.**
- Platform build (`wrangler deploy --dry-run --outdir dist`) EXIT:0, effect-verified (`dist/index.js` 1,342,627 bytes + `.js.map` 2,903,487 bytes, fresh after `rm -rf dist` first). New DO class `WatchtowerDO` (watchtower-hardening lane) correctly registered in `apps/platform/wrangler.toml` via `[[migrations]] tag = "v3" new_sqlite_classes = ["WatchtowerDO"]` — same pattern as the original TenantDO v1 entry.
- **Dashboard `npm run build` (`vite build`) mutates TRACKED files as a normal side effect**: `apps/platform/public/app/assets/*.js` are committed (not gitignored — served via wrangler's `[assets] directory = "public"`), and vite's content-hash filenames change every build even with no source delta, so running this build step always leaves `git status` non-clean (N deleted old-hash files + N untracked new-hash files + modified `index.html`×2) until someone commits or discards the refresh. Don't mistake this for a stray edit when checking git status after a battery that included a dashboard build.
- md5 tripwire pattern that worked well here: capture the hash explicit-`cd`'d immediately after `npm ci`, then re-capture (same explicit-`cd` prefix) after EVERYTHING else (typecheck, tests, all builds) — an unchanged hash + unchanged `git rev-parse HEAD` across the whole session is strong, cheap evidence that no sibling agent/worktree edit landed on tracked `.ts` source mid-battery, independent of whatever the ambient-cwd hazard above is doing to bare commands.

**2026-08-18 battery (channelfix integration worktree, branch feat/channel-truth-2026-08-17 @ 3d2c194) — REAL cross-lane typecheck+runtime FAIL, root-caused precisely, class = merge-semantic:**
- `npm run typecheck --workspaces --if-present` EXIT:1 — 3× TS2322 in `apps/platform/test/admin-provisioning-state.test.ts` (lines 138, 144, 201): the callbacks passed to `withRequestIdempotency` return bare objects (`() => ({ ok: true })`) instead of the `Settled<T>` contract (`{ terminal: boolean; value: T }`) that a prior train (train 1) had already landed in `apps/platform/src/engine/idempotency.ts`. This is a genuine cross-lane break, not noise — main's newly-merged `admin-provisioning-state.ts`+test were written against the pre-train-1 signature.
- **This one breaks at RUNTIME too, not just typecheck** — confirmed by actually running the suite, not just trusting the type error: `settled.terminal` on a bare object is `undefined` (falsy) → `withRequestIdempotency` (idempotency.ts:184-190) takes the "non-terminal, don't persist" branch and DELETEs the pending claim row instead of writing `status='done'`. Result: 2 real test failures (`expected [] to have a length of 1 but got +0`) in the same file, reproduced identically across TWO independent full runs (standalone `apps/platform` vitest run, then again inside the root `npm test` fan-out) — deterministic, not a flake. **Lesson: when a TS2322 shows up in a test file calling a function whose contract just changed, don't assume "TS-only, runtime is fine" (unlike the 2026-07-24 `"launch"` TenantPlan case) — trace what the mismatched value actually does at the call site; a shape-changed callback contract can silently misroute control flow at runtime where a simple literal-type narrowing usually can't.**
- Platform baseline this session: **186 test files (185 passed, 1 failed) / 1757 tests (1754 passed, 2 failed, 1 skipped)**. Dashboard 29/143 all pass, engine 19 files (17p/2skip)/144 tests (140p/4skip) all pass, CLI (`packages/cli`, node:test) 12/12 EXIT:0 with `dist/` confirmed fresh (`find src -newer dist/index.js` empty) so the PASS isn't riding a stale build. Root aggregate `npm test` genuinely EXIT:1 (npm workspace fan-out is NOT fail-fast — it ran dashboard/engine/platform/cli to completion despite platform's mid-fan-out failure, then correctly propagated 1 at the end).
- **New exit-code-masking trap, distinct from the documented `cmd | tail` one**: writing `npm test > log 2>&1 <newline> echo "EXIT:$?" >> log` as a multi-line string passed to a `run_in_background: true` Bash call, the tool's OWN completion notification reports "exit code 0" — that 0 is the exit code of the trailing `echo` statement (always 0), NOT of `npm test`. The real exit code is only trustworthy from the `EXIT:` line written INSIDE the log by `$?` captured immediately after the gated command, never from the outer bash-call/notification's own reported exit code when a later command follows it in the same invocation. Don't trust `run_in_background`'s "(exit code N)" summary as the gated command's exit code unless the gated command is the LAST thing in the invocation with nothing after it.
- Also confirmed: don't stack `run_in_background: true` on the Bash tool call AND shell-level `nohup ... &` together — that double-backgrounds (the tool sees the quick-returning launcher exit near-instantly and notifies prematurely; the real work keeps running detached and has to be polled for via log-file grep + `ps`/`kill -0`, not via the (already-fired, useless) task-completion notification). Just pass the real long-running command directly with `run_in_background: true` and nothing async at the shell level — the tool's own backgrounding is sufficient and its completion notification is then trustworthy.
