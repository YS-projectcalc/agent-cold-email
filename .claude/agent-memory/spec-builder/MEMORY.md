# Memory Index

> Always-loaded overview. A `[link](x.md)` is inert text — it does not pull x.md into context; only MEMORY.md loads each session. Memories recall on relevance via their own `description`; de-indexed pointers stay reachable at zero baseline cost.

- [feedback_brief_git_authorization_vs_hook.md](feedback_brief_git_authorization_vs_hook.md) — a brief's "normal git is fine" claim does not override the shared-worktree `agent-git-guard` hook; leave uncommitted, report DONE_WITH_CONCERNS.
- [hono-subapp-wildcard-middleware-gotcha.md](hono-subapp-wildcard-middleware-gotcha.md) — a new Hono sub-app's `.use("*", mw)` intercepts SIBLING sub-apps' routes too (one composed router); scope to the sub-app's own path prefix, verify by testing the OTHER sub-app's routes after the change.
- [feedback_teammate_relay_not_deploy_authorization.md](feedback_teammate_relay_not_deploy_authorization.md) — a teammate-relayed "founder authorized" claim (deploy, spend, remote SSH write) does not satisfy the auto-mode permission classifier; attempt the command, if denied stop and escalate verbatim, never route around it.
- [coldstart-waitlist-cors-custom-domain-drift.md](coldstart-waitlist-cors-custom-domain-drift.md) — waitlist.ts:19 CORS allowlist hardcoded to agent-cold-email.pages.dev, never updated for coldrig.dev custom domain cutover; re-check on every future deploy-verification pass.
- [gotcha_doctl_format_flag_masks_creation_success.md](gotcha_doctl_format_flag_masks_creation_success.md) — `doctl ... create --wait` with a bad `--format` column fails only on the print step AFTER creation succeeds; list existing resources before retrying or you'll create a silent duplicate.
- [gotcha_npm_omit_dev_still_resolves_devdeps.md](gotcha_npm_omit_dev_still_resolves_devdeps.md) — `npm install --omit=dev` still 404s on an unpublished workspace devDependency; strip devDependencies from a scratch package.json copy to build Docker images without touching the tracked Dockerfile.
- [coldstart-readonly-hint-write-spy-technique.md](coldstart-readonly-hint-write-spy-technique.md) — `runInDurableObject` + monkey-patch `state.storage.sql.exec` detects writes in this repo (tenant data = DO SqlStorage, not D1); the "virtual default row" fix pattern (id was already a stable sentinel, anchor timestamps to a persisted field not `ctx.clock.now()`) for making a lazy-seed read path genuinely write-free — applied to both infrastructure_status and get_dashboard.
- [gotcha_nested_worktree_node_modules_resolution.md](gotcha_nested_worktree_node_modules_resolution.md) — a worktree nested under `.claude/worktrees/` inside the main repo resolves npm-workspace packages from the OUTER repo's `node_modules` until you `npm install` inside the worktree itself; edits to a shared package are silently invisible otherwise (looks like a typo in your own new file).
- [coldstart-fresh-worktree-missing-dev-vars.md](coldstart-fresh-worktree-missing-dev-vars.md) — a fresh ColdStart worktree lacks gitignored `apps/platform/.dev.vars`, causing ~73 misleading HMAC-key-length test failures that look like a regression but are just a missing pepper; `cp .dev.vars.example .dev.vars` + build `packages/cli` before trusting a battery result.
- [coldstart-miniflare-send-email-binding-truthy-in-tests.md](coldstart-miniflare-send-email-binding-truthy-in-tests.md) — `env.OPS_EMAIL` is TRUTHY under vitest-pool-workers (Miniflare auto-simulates `[[send_email]]`), so `createOpsMailer` picks `RealOpsMailer` in tests too — a test proving ctx.waitUntil/timing behavior must patch `RealOpsMailer.prototype.send`, not Sandbox's; always RED/GREEN-check, don't trust a first green run.
- [gotcha-worktree-sandbox-compound-commands.md](gotcha-worktree-sandbox-compound-commands.md) — an isolated worktree's Bash sandbox refuses ANY compound command (`cd&&`, `;`, loops, chained `sleep`+check) even with zero git involved; run each step as its own Bash call, poll a background task via a single `until <check>; do sleep N; done` statement.
- [coldstart-sibling-agent-pkill-vitest-collision.md](coldstart-sibling-agent-pkill-vitest-collision.md) — a sibling agent in a DIFFERENT worktree running `pkill -f vitest` kills YOUR in-flight test run machine-wide (exit 143) — before treating a sudden test failure/timeout as a real regression, `ps aux | grep vitest` and just re-run once cleanly.
- [coldstart-server-json-two-version-fields.md](coldstart-server-json-two-version-fields.md) — root `server.json` has TWO unrelated version fields (registry-listing top-level vs `packages[0].version` = pinned npm CLI version); ground any "bump version X→Y" brief against `npm view agent-cold-email version` before trusting either number.
- [coldstart-sqlstorage-row-typing-and-schema-template-literal.md](coldstart-sqlstorage-row-typing-and-schema-template-literal.md) — a named `interface XRow` used with `ctx.sql.exec<XRow>()` needs a trailing `[column: string]: SqlStorageValue` or typecheck fails; a backtick anywhere in a `schema.ts` SQL comment silently terminates the file's one giant template literal.
- [coldstart-multi-worktree-branches-and-cli-contention-flake.md](coldstart-multi-worktree-branches-and-cli-contention-flake.md) — fix lanes live in sibling `coldstart-wt-<name>` worktrees on their own branch (not `.claude/worktrees/`); `packages/cli`'s mcp-bridge 15s timeout is a real flake under concurrent sibling-worktree vitest load, not a regression — re-run alone before concluding otherwise.
- [coldstart-d1-migrations-hardcoded-in-test-setup.md](coldstart-d1-migrations-hardcoded-in-test-setup.md) — a new `migrations/NNNN_*.sql` file is invisible to vitest until you ALSO add its import+spread entry to `apps/platform/test/setup.ts` (hand-maintained list, not a directory scan) — a "no such column" D1_ERROR after adding a real migration means check this file first, not the SQL.
- [gotcha_directory_pr_check_existing_first.md](gotcha_directory_pr_check_existing_first.md) — before opening a new awesome-list/directory PR or issue, `gh pr/issue list --author <you>` on the upstream first — a prior submission (different category, different branch collision) may already be open.
