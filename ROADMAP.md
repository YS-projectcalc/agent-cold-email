# ColdStart — ROADMAP

> Build order + status. Canonical; updated every session that touches the project.
> Program started 2026-07-09. Owner interview complete (SPEC §0). Adversarial panel #1 complete → amendments adopted (`docs/adversarial/panel-01/SYNTHESIS.md`). Autonomous to the finish line; open questions HELD for the final report, never asked mid-build.

## Definition of done — "test-mode CORE complete" (rescoped by panel #1)

The core business exists and runs itself in **test mode**: the pipe (provision→warm→send→reply, sandbox adapters) + control plane + Stripe test-mode billing/metering + agent surface (MCP+CLI+demo) + real-but-minimal compliance guardrails + the business-ops lanes (dunning, teardown, abuse-drop, metrics) + legal docs + a publish-now distribution shell that's already indexing — all adversarially hardened on the REAL surfaces — plus **ACTIVATION.md**, the single checklist of owner-hands steps that flips it live. Un-sandbox-testable *tuning* (live deliverability tuning against real Gmail, warmup optimization) is built-to-contract now and tuned at activation.

## Hardening-budget rule (panel #1)
Adversarial panels apply to what is REAL: control plane, auth, tenancy, billing, isolation logic, compliance, legal, distribution. Deliverability/warmup/AI-ops get contract-level review only until activation — don't burn budget polishing synthetic behavior.

## Now
- [x] 2026-08-09 [ORDER] **Wave 3 SHIPPED + DEPLOYED + VERIFIED — five lanes, combined gate SHIP 11/11 attacks failed.** Merged `main` `17e05d4` (--no-ff, 138 files +7254/-678), pushed. Lanes: msgchannel Inc2+Inc3 (operator route + `list_messages`/`ack_message`, 25→27 tools; live-confirmed `apps/platform/src/mcp/tools.ts:347-348`), webhook inc2 (decline-code second Stripe read — `resolveDeclineCode`/`getDeclineCode`, `billing.ts:335,750-751` + `stripe-client.ts:322-353`, code-verified — closes N-1 refinement + the readDeclineCode-dead-on-real-payloads residual; `dispute.created` staleness exemption bounded to `isDisputeSettledWithoutFreeze`, `billing.ts:407,488`, code-verified; refused-events `applied=0` flag, `billing.ts:598,620`, code-verified — closes N-2; 413 cap now enforced on ACTUAL bytes read not declared Content-Length, `validate.ts:37-79`, code-verified — closes audit item 7), dash-idem (buy-intent Idempotency-Key on setup + remove-mailboxes, `infrastructure.ts:11-13`/`checkout.ts:27-30`, code-verified; token-rotate revokes all dashboard sessions, `token-rotate.ts`, code-verified), watchtower hardening (D1-outage-safe alert ordering — `watchtower.ts:82,321` "NEVER THROWS on a D1 outage", code-verified; dead-man via `WatchtowerDO` new Durable Object + migration tag v3, `wrangler.toml:102-104`, code-verified; new `/status` field `sweepAgeSeconds`, `routes/status.ts:31-33`, live-confirmed 215s/216s both hosts, well under the 900s `SWEEP_STALE_MS` threshold), byo-teardown (never vendor-release customer-connected resources). Lane branches preserved: `msgchannel-inc23-2026-08-06`, `webhook-inc2-2026-08-06`, `dash-idem-fixes-2026-08-06`, `watchtower-hardening-2026-08-06`, `byo-teardown-2026-08-06`, `integrate/2026-08-09-wave3`. Gates: byo-teardown SHIP 0-blocking (frozen `docs/adversarial/byo-teardown-gate-2026-08-06.md`); msgchannel r1 SHIP-AFTER-FIXES → F1/F2 fixed `1eef443` (frozen `docs/adversarial/msgchannel-inc23-gate-2026-08-06.md`); combined integration gate r1 SHIP-AFTER-FIXES (1 blocking B-1: unconditional `dispute.created` staleness exemption bricked a tenant on closed(won)-before-created ordering) → fix `414b58e` (exemption bounded by `isDisputeSettledWithoutFreeze` + `closed` UPSERTs the disputes row; RED 3→GREEN, both halves proved load-bearing by partial revert) → r2 **SHIP**, 11/11 attacks failed (frozen, git-tracked `docs/adversarial/wave3-integration-gate-2026-08-09.md`, verified present + committed). Battery (as reported by the wave-3 verifier lane, cited from the merge commit `17e05d4` message): typecheck ×5 clean; 227 test files/1810 tests 0 real failures (platform 166f/1522t post-fix); builds clean; md5 tripwire clean. **DEPLOYED + LIVE-VERIFIED THIS BOOKKEEPING PASS:** Worker version `0f197c4d-ffe8-4da6-a1f4-94c3f23063d6` both hosts — `/status` returns 200 `{"status":"ok","sweepAgeSeconds":215}` (workers.dev) / `216` (api.coldrig.dev), confirming the deploy AND the new sweep-freshness surface are live; live server-card `coldrig.dev/.well-known/mcp/server-card.json` reads version 0.2.2, 27 tools (independently fetched, not just reported). Cron `*/5 * * * *` intact; WatchtowerDO migration v3 applied (per orchestrator's evidence, not independently re-checked via `wrangler` this pass). Site deployed: Pages deployment `20a4a523` --branch main; IndexNow fired HTTP 200 for 15 changed pages (reported, not independently re-checkable after the fact). Worktrees: ALL lane/audit/integration worktrees removed (branches preserved); the stale `agent-a25bfeea` registrar-lane worktree from the 2026-08-05 session archived to branch `d451d28` and removed. Remaining: main checkout + the external Codex design worktree only (`git worktree list`-confirmed this pass). See `## Open` below — B1/B2/B3/NB-1/N-1/N-2/item-7/readDeclineCode entries updated with their own NOW SHIPPED notes; NB-2/NB-3/NB-4 of the ORIGINAL `audit-alerting-completeness-2026-08-06.md` (cron-leg error counters unread, digest thrown away, provisioning-vendor-failure-alerts-nobody) were NOT part of this wave and remain open, untracked as their own bullets — distinct from the wave-3 gate's own NB-2/NB-3/NB-5 (which do have separate `## Open` bullets).
- [ ] 2026-07-28 [ORDER] **2026-07-27/28 hardening + claims wave COMPLETE — five items shipped, zero active build lanes** (`git worktree list`: only this repo + the external `coldrig-design` worktree remain). (1) SDN weekend-false-alarm fix — 2-round adversary arc (NO-SHIP r1 → fix A → SHIP r2), merged `f583fe5`, deployed Worker `b672b403`; LIVE-VERIFIED via a real droplet re-push → HTTP 200 `{"reason":"unchanged"}`, `fetched_at` advanced, `failure_streak` 0 — weekend alerts dead, droplet-down backstop proven by a co-fire test; honesty-statement addendum appended `docs/research/ofac-v1-honesty-statement-2026-07-23.md`. (2) Pre-send intent log (closes the GA double-send / ACTIVATION Gate-2 residual (1)) — design SHIP-after-fixes → build SHIP 0-blocking → NB1 `sentOnWire` guard DELTA-SHIP; merged `608c80a` (`apps/engine/src/send-log.ts` WAL + `apps/engine/src/reconcile.ts` boot reconciliation); **engine-side, `[dark-unarmed]` until arming is verified in prod** — ⚠️ `ACTIVATION.md`'s Gate-2 residual (1) line still reads "uncommitted, pending review + adversary re-gate + merge" as of this pass — now STALE, the chain has since merged; not corrected here (out of this bookkeeping brief's file scope), flagging for the next doc-touch. Frozen verdicts `docs/adversarial/{pre-send-intent-log-design-review,intent-log-build-review}-2026-07-27.md`. (3) CLI stale-claims fix — retired waitlist/early-access line killed, claim-surface guard test added, `0.2.0`→`0.2.1` (verified: `packages/cli/package.json` reads 0.2.1); merged `7dd0182`/`5e09d46`. npm publish of 0.2.1 still PENDING founder OTP (click queue below). (4) Go-live UX — 3-round adversary arc (money-path selector-inert defect caught pre-deploy; billable-vs-count-all display≠charge class closed via a single-sourced `billableMailboxCount` helper; state-derived chips; double-checkout server guard 400-on-active; work-email→Email; stale "not active yet" banner killed; sandbox banner + checklist go-live step); merged `15a2e01`, deployed Worker `3a031af4` + site — live-verified; frozen `docs/adversarial/golive-ux-review-2026-07-27.md` + screenshots `pw-shots/golive-review-2026-07-27/`. (5) Claims-truth pass — price-math rubric ("The all-in math, not just the sticker price." — live heading `site/pricing.html:173`) on pricing/for-agents/llms.txt/README/compare; purchase-path + Stripe promo-code clarity (verified live on `site/for-agents.html`/`site/faq.html`); concierge swept to scoped truth across 40 surfaces incl. `plugin.json`/`llms-install`/`cli-README`; no temporal activation claims; provisioned-count billing line; 177-assertion guard; adversary SHIP-after-fixes applied; merged `ec0f972` (hand-resolved 8 conflicts toward 25-tools/0.2.1/scoped-truth); site deployed `e1bfe52c` (bookkeeper-reconfirmed: Pages deployment off `ec0f972`, `coldrig.dev` → 200) + IndexNow 200; frozen `docs/adversarial/claims-truth-pass-review-2026-07-27.md`. Bookkeeper-reconfirmed this pass: Worker `/status` → `{"status":"ok"}`, engine `/health` → `{"status":"ok"}`; main is 14 commits ahead of `origin/main` / 0 behind (push pending, not a divergence).
- [ ] 2026-07-28 [gated:founder] **Founder click queue, by leverage — DONE this wave:** ✅ Cloudflare Tunnel login (engine armed, see `## Open` 2026-07-14 entry) · ✅ `ENGINE_BASE_URL` set · ✅ droplet firewall 8080 dropped · ✅ Bing Webmaster verify + Request-Indexing clicks · ✅ GSC Request-Indexing clicks · ✅ AlternativeTo account created · ✅ Glama "Build & Release" clicked — but the listing REFRESH FAILED to pick up the new tool count (Glama's own API still serves "~12 tools" post-release, unrelated to the 25-tool server-card/site truth, which is confirmed correct) → now needs a MANUAL description edit in the Glama dashboard (text drafted, delivered in-chat) · ✅ InboxKit UI connect attempt (see `## Open` 2026-07-27 CF-registrar entry, CLOSED) · ✅ npm publish 0.2.1 (published 2026-07-28, confirmed live). **REMAINING 2026-07-29 (✅ Stripe receipts toggle + ✅ Glama manual description edit DONE, founder-confirmed 07-29):** send Mordy his go message (paste-ready, delivered in-chat 07-29 — HIGHEST leverage, unblocks first provisioning) → ~~InboxKit support ticket v2~~ **NOT NEEDED (Q1 root cause found 2026-07-29: stale blog schema — `connect` wants a `domains` ARRAY, not `zone_id`+`domain`; live GET /zones 200 corroborated; see research doc)** → revoke the CF token (Keychain `cf-dns-dmhadvisor`) in the CF dashboard, then Claude clears the Keychain entry → G2/Capterra listing packs → `listings@` routing rule → PulseMCP word → Stripe dashboard support_email → uptime-prober service name.
- [ ] 2026-07-29 [IDEA] **BYO-Cloudflare connect path now UNBLOCKED (Q1 root cause, research doc):** the real `cloudflare-domains/connect` schema is `{auth_type, api_token, domains:[…]}` (array) or `connect_all:true` — the two-step dashboard flow = connect(save-credential) → GET /zones → connect(domains). When we build the BYO-CF product path (not on Mordy's critical path — he uses lookalikes), wire this corrected schema; end-to-end live-connect confirmation on a genuine throwaway CF domain still pending (dmhadvisor is scheduled_for_deletion, unsuitable).
- [ ] 2026-07-29 [ASK] **Stray InboxKit CF credential + workspace-isolation question (exposed by the 07-29 zones probe):** the founder's zone-scoped CF token is stored server-side at InboxKit (from the UI connect); revoking the CF token invalidates it, and the dmhadvisor zone/credential should be cleared from workspace `c5188ced…` before Mordy provisions. Separately confirm whether all Coldrig tenants share ONE InboxKit workspace vs one-per-tenant (tenant-isolation). **Isolation half RULED 2026-07-31 (founder): ONE shared InboxKit account/workspace for ALL tenants — never per-tenant accounts (each new account needs founder input); ladder the plan tier as slots fill (Professional → Agency at ~25 total mbx → Enterprise at ~80; breakevens per docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md, re-verify prices at upgrade time). Blast-radius tradeoff accepted (one vendor account = shared suspension risk). Stray-CF-credential cleanup half still open (gated on founder's CF token revoke).**
- [ ] 2026-07-28 [ORDER] **🎉 MORDY PAID — first real customer (17:06Z).** tenant "Press Outreach" (`ten_91aab24a-43a8-45c1-bf43-af88ef633221`); Stripe sub `sub_1TyESe…` ACTIVE (platform×1 + mailbox×5), MORDYPILOT redeemed, invoice `in_1TyESe…` amount_paid **$39.60** (verified). ⚠️ `tenants_index.plan` still reads `demo` — that column is NEVER updated post-signup (by design); read the DO/Stripe for truth. Post-payment fixes shipped SAME session: Stripe success/cancel redirects were UNROUTED (404 JSON — what Mordy saw) → now 302 → /app/billing (Worker `860542ea`, commit `6c84648`); `api.coldrig.dev` branded API host added (Worker `f94139e1`, commit `fcb233e`; workers.dev kept). **CRITICAL PATH to his first send — (a) NOW CLOSED 2026-07-29, (b)-(d) PENDING his agent:** (a) `REGISTRAR_PROVIDER=inboxkit` + per-tenant `registerDomains` opt-in — SHIPPED LIVE + ARMED (full evidence chain drained to `archive/ROADMAP-done.md` 2026-07-29: merge `c1e24a5`, deploy `4a7bbf0d`, `REGISTRAR_PROVIDER` secret armed, post-arm live checks green); (b) his agent still needs to run `setup_infrastructure` **twice** — `{domains:1, inboxesEach:3}` then `{domains:1, inboxesEach:2}` = 2 domains × (3+2) mailboxes, all 5 paid seats, bill stays $39.60; (c) fleet-mint OAuth verify (his mailboxes are its FIRST live test) or the 10-min manual grant fallback; (d) first-live warmup-ramp watch. Wallet funded $36 (founder). `authorpitchdesk.com` domain-connect still walled (CF-registrar) but MOOT — pilot uses InboxKit-registered lookalikes, not his domain. Founder is next to send Mordy the go message (drafted in-chat) — nothing in (b)-(d) has happened yet as of this bookkeeping pass. **UPDATE 2026-08-06 (wave-2 close):** provisioning fix + auto-send + the real wall-clock migration are ALL LIVE (`main` `f7ee786`, Worker `8187d36e-056d-4d3b-a52f-3814dd4fcff9`, D1 migration 0016 confirmed applied remote — `wrangler d1 migrations list --remote` reads "No migrations to apply!"). His retry (same key `apd-setup-a-2mbx`) now adopts-in-place + provisions once the vendor's DNS/NS-checker flips ready (still pending as of the last poll — workspace 0 mailboxes, `goauthorpitchdesk.com` DNS-pending, vendor's own nameserver checker never run). Once provisioned, sending still needs the manual OAuth credential mint — no longer a silent gap: the `cred_push_aging` watchtower alert (`admin/watchtower.ts`) now surfaces it.
- [ ] 2026-07-28 [ORDER] **Warmup = platform-managed ramp, INCLUDED free (founder 2026-07-28: "platform is officially offering this free... we gotta do it"):** the ramp engine (`apps/platform/src/engine/warmup.ts` — `warmup_started_at` → `warmupDailyCap` by age, `sent_today` metering, deliverability throttle/pause) is BUILT + tested, NEVER live-proven; Mordy's mailboxes are the first real run. DoD: verify caps bite at the engine boundary during his week 1 (ledgered as "first live warmup ramp watch" in `## Open`). InboxKit's $3/mbx warmup-NETWORK add-on stays shelved (gray-zone; research lane running 2026-07-28 to rule on posture). Ramp-only has no friendly-recipient engagement signal — that gap is exactly what the warmup research + prewarm SKU address.
- [ ] 2026-07-28 [ASK] **Unit-economics + self-hosted-infra question (founder 2026-07-28):** at 5 mailboxes we register ~2 domains (~$24/yr) + consume 5 of InboxKit's 10 slots (~$19.50/mo) → ~$22-24/mo COGS vs $99 list (77% margin) / vs Mordy's $39.60 (~break-even, intended). Founder's open concerns to answer with real numbers: (a) slot headroom — 10-slot sub caps us at ~2 concurrent 5-mbx customers before a second $39 sub; model the per-customer slot economics + when to jump tiers; (b) **build-our-own-infra study** — what it takes to self-host mailbox provisioning (Google Workspace reseller/API, domain registrar API, warmup, deliverability) vs InboxKit resale, and the real breakeven customer count. Research-lane candidate; NOT started.
- [ ] 2026-07-24 [ORDER] **Post-GA follow-ons, ledgered non-blocking:** baseline-reset recovery doc for a >10% legit SDN delisting (post-arm, see the SDN-relay entry) · event-driven alert class sweep (per-tenant-action storms, distinct from the cron class) · agency-bundle billing design (flat per-workspace add-on, deferred out of this billing arc) · buyer-panel cycle 3 (after the founder's Glama re-scan).

## Open
- [x] 2026-08-09 [ORDER] **InboxKit support ticket SENT (founder click #1, executed with founder authorization 2026-08-10T01:35Z)** — emailed support@inboxkit.com from yaakovscher@gmail.com via browser session, verified in Gmail Sent (subject "Purchased domain stuck on pending nameserver check (goauthorpitchdesk.com) + OAuth scope question"). Covers the NS-checker kick AND the Inc-F OAuth scope question. New evidence cited in the ticket: the InboxKit dashboard shows NS Status "Matched" for goauthorpitchdesk.com while the API still serves pending/last_nameserver_check=null — the vendor's stored-field checker is stuck, not the delegation. Durable dashboard login saved to playwright profile `inboxkit-coldrig` (email-code flow; Google SSO dead-ends on TOTP headless). Also: Stripe webhook spot-check (founder click #2) is MOOT until a real delivery — zero Stripe events of ANY type since the wave-2 deploy (API-verified 2026-08-10T01:20Z); the first real `invoice.payment_failed` doubles as the readDeclineCode live-verify.
- [ ] 2026-08-09 [ORDER] **Wave-3 gate NB-2 — `watchtower_state` growth still unbounded post-rewrite:** no `DELETE FROM watchtower_state` anywhere in `src/` or `migrations/` (grep-verified); the rewrite widens it with a fifth per-entity prefix `tenant_do_wedged:<tenantId>` alongside `mailbox_provisioning:`/`mailbox_rebuy:`/`cred_push_aging:`/`send_starved:` — every sweep does two full-table reads (`readWatchtowerState`, `readReportedCheckNames`, `watchtower.ts:390,395`) every 5 min, rows never reclaimed. Needs a retention/pruning design. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`, lands on main with the wave-3 merge)
- [ ] 2026-08-09 [ORDER] **Wave-3 gate NB-3 — B3 wedged-DO visibility covers a tenant DO that THROWS, not one that HANGS:** `scanTenants` awaits `opsSummary(sinceMs)` per tenant sequentially with no `AbortSignal`/timeout, so one hung-but-not-throwing DO stalls the watchtower leg and every leg after it in `scheduled.ts`, including the heartbeat leg (deliberately last) — the dead-man then fires, but with the WRONG stated cause ("The 5-minute Cron Trigger appears to have stopped"). Fix = per-tenant timeout/race in the sweep legs. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`)
- [ ] 2026-08-09 [ORDER] **Wave-3 gate NB-5 (minor) — `body-cap-coverage.test.ts` globs narrower than the 413-cap class it pins:** globs `../src/routes/*.ts` + `{mcp,ofac,admin}/*.ts` + `validate.ts` only — misses top-level `src/*.ts`, `src/engine/*.ts`, `src/billing/*.ts`, `src/vendors/**`, and any nested `src/routes/**` subdir (non-recursive glob). Gate grepped all of `src/` for `req.text()`/`req.json()`/`req.arrayBuffer`/`req.formData`/`parseBody`/`blob`/`raw.body` and confirmed zero stragglers TODAY — widen the glob so a future body-reading module can't dodge the guard. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`)
- [ ] 2026-08-09 [ORDER] **Pre-existing (NOT wave-3): late-dispute resurrection of a CANCELED tenant** — a late `charge.dispute.created` on a canceled tenant, plus that dispute's win, restores `billing_state='active'` and un-freezes provisioning/launches (wave-3 gate-executed; frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`). `billing.ts`'s own comment (main `apps/platform/src/engine/billing.ts:539`: "...billing_state='active' and resurrecting a canceled tenant. Superseded...") names this exact outcome. Root cause (verified on main): at the moment the late `created` event lands, the dispute lane has no `billing_event_order` row yet, so `isStaleBillingEvent` (`billing.ts:439-455`) returns false at the `row === undefined` check (`:447`) before any exemption logic runs. Identical on main; belongs to wave-2's per-lane watermark, not wave-3; needs its own small lane (a state guard on the `dispute.created` write is the shape suggested, since both 'canceled' and 'disputed' freeze anyway).
- [ ] 2026-08-09 [ORDER] **CLI workspace test/build ordering gap (pre-existing):** `packages/cli/test/claim-surface.test.mjs` reads the BUILT `dist/` by design (`readAllJs(DIST_DIR)` runs at module load, no fallback) — verified: `readdirSync` throws ENOENT if `dist/` is absent, and root `package.json`'s test script (`npm run test --workspaces --if-present`) never builds the CLI workspace first, so a fresh-clone `npm test` fails ENOENT before any assertion runs. Fix = a `pretest` build step in `packages/cli/package.json` (npm auto-runs `pretest` per workspace). Found by the wave-3 verifier; clean re-run 12/12 PASS.
- [ ] 2026-08-06 [ORDER] **BYO-teardown gate out-of-scope discoveries (gate SHIP 0-blocking; frozen `docs/adversarial/byo-teardown-gate-2026-08-06.md`):** (a) REPLACE_DOMAIN's `inboxesEach` counts byo mailboxes → the burn path buys a PAID replacement for a customer-owned mailbox (`deliverability-actions.ts:162-172`, pre-existing) — fix candidate for the next arc. (b) [ASK] **teardown retains offboarded customers' own credentials indefinitely** — `mailboxes.transport_json` (their SMTP passwords / OAuth refresh tokens) is never cleared at teardown; mirror-image of the never-touch ruling (we must not destroy THEIR resources, but we also shouldn't KEEP their secrets). Recommendation: purge customer-held transport secrets at teardown. Awaiting founder word. Context: INBOXKIT_* armed in prod since 07-24 (mailbox port is REAL, not sandbox), zero BYO tenants today — exposure is forward-looking, not live.
- [ ] 2026-08-06 [ORDER] **Webhook-inc2 lane residuals (builder-flagged, orchestrator-triaged):** (a) **multi-dispute unfreeze** — `dispute.closed(won)` lifts the freeze whenever `billing_state='disputed'` without checking for OTHER still-open disputes (pre-existing on main, adjacent to N-1 but distinct; one-line `NOT EXISTS` guard sketched in the lane report; interacts with wave-2-gate-verified dispute behavior → deserves its own careful arc, NOT folded mid-wave). (b) **post-deploy live-verify**: that `charge.outcome.reason` carries the issuer decline code on API 2024-06-20 is documented-shape only, not live-proven — confirm on the first real `invoice.payment_failed` delivery (60s Stripe-dashboard check); failure direction safe (wrong field → null → transient). (c) test-pattern lesson for future gates: deliver TWO independent instances of the same vendor object type — every dispute fixture repo-wide used one dispute, which is why one-watermark-per-lane looked correct. (d) `signup-abuse.test.ts` wall-clock flake (7-signup burst can cross the minute boundary on slow runs → "expected 6 to be 5"; assert >=5 or pin the clock; pre-existing). **Wave-3 gate status (2026-08-09, NOW CLOSED):** fix lane folded into `integrate/2026-08-09-wave3`; combined gate r1 SHIP-AFTER-FIXES (1 blocking B-1 in the dispute-exemption) → fix `414b58e` → r2 **SHIP**, 11/11 attacks failed, merged `main` `17e05d4`, deployed `0f197c4d`. Live-verified `/status` 200 both hosts this pass.
- [ ] 2026-08-06 [ORDER] **Alerting-completeness audit (boundary gaps 6+7) — FAIL, 3 BLOCKING, one root defect: the watchtower cannot report a failure of anything it stands on; fix lane `watchtower-hardening-2026-08-06` dispatched same session** (frozen `docs/adversarial/audit-alerting-completeness-2026-08-06.md`; 14 fault-injection assertions vs the real Worker+D1+DO harness, asserting actual ops emails, never code-reading). **B1** a SUSTAINED D1 outage is a total silent alerting blackout: `evaluateHealthChecks` hits unguarded D1 reads (`admin/watchtower.ts:184,191`) and `reconcileAlerts` reads `watchtower_state` (`:294`) BEFORE any send — while `RealOpsMailer` needs only the `send_email` binding, so the alert IS deliverable during the outage (ordering defect, executed: 10/10 legs fail, 0 emails; intermittent blips DO alert). **B2** no dead-man anywhere: `last_sweep_ts` is read only by the sweep itself; cron death silences every alert and silence ≡ health; the planned external prober (still-open founder click) wouldn't catch it — `/status` is a bare SELECT 1, green with a dead cron. **B3** a wedged tenant DO (real class — 3 repo precedents cited) silently drops that tenant from every check AND every sweep leg while `do_storage` reports HEALTHY (probe pings a RateLimiterDO canary, wrong DO class). **NB-1 ARMS WITH THE CUSTOMER:** `failure_signals` flaps on intermittent failure (executed: 24 emails/2 simulated hours, alternating UNHEALTHY/RECOVERED; the exact founder-reported 160-email cry-wolf class `sdn-alert.ts` was built to fix) — windowed/streak evaluation required BEFORE real sends arm. NB-2 cron-leg error counters (`errors`/`budgetExpiries`/`skippedForLegDeadline`) are console-logged and never read by anything. NB-3 digest `watchdogAlerts` (incl. `gaveUpWarmupCancels` — "money leaking" — and `pendingCredentialPushes`) computed every 5 min and thrown away; pull-only via /admin/ops/digest. NB-4 plain provisioning vendor-failure on a paying tenant alerts nobody (known-deferred F8; inventory only, self-healing via adopt-on-retry). Gap-6 diff-checks: OFAC lane CLEAN with a CAVEAT — `fad3a3e`'s brand-override on the sanctions screen carries one sentence of coverage inside a provisioning gate; no OFAC-lane review since 07-27 (focused re-review candidate). Outbound-webhook lane CLEAN (`7ce59dc` covered by the warm-lead review, SHIP). Observations (no verdict weight): `cred_push_aging` clear-path can wedge forever on a deleted mailbox row; `watchtower_state` grows unbounded, full-table SELECT every 5 min. **NOW SHIPPED + DEPLOYED 2026-08-09 (verified in code): B1, B2, B3, NB-1 fixed.** B1 — `evaluateHealthChecks`/`reconcileAlerts` now NEVER THROW on a D1 outage (`admin/watchtower.ts:82,321`, code-verified). B2 — dead-man built as a new `WatchtowerDO` Durable Object (self-scheduling alarm, `watchtower-do.ts:42,93,121,128`, code-verified) + migration tag v3 (`wrangler.toml:102-104`, code-verified) + a new `/status` field `sweepAgeSeconds` (`routes/status.ts:31-33`, code-verified; live-confirmed 200 `{"status":"ok","sweepAgeSeconds":215}` both hosts this pass, well under the 900s `SWEEP_STALE_MS` threshold, `admin/watchtower-grading.ts:62`). B3 wedged-DO visibility and NB-1 flap windowing per the merge commit `17e05d4` message (not independently code-read this pass beyond the commit's own description). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). **NB-2 (cron-leg error counters unread), NB-3 (digest thrown away), NB-4 (provisioning vendor-failure alerts nobody) — the ORIGINAL audit's own non-blocking findings — were NOT part of this wave and stay open, not yet tracked as their own bullets.** (Distinct from the wave-3 integration gate's OWN NB-2/NB-3/NB-5, which already have separate `## Open` bullets dated 2026-08-09.) Per-tenant DO wedge B3's new `watchtower_state` prefix (`tenant_do_wedged:<tenantId>`) also feeds the still-open unbounded-growth NB-2 bullet above.
- [ ] 2026-08-06 [ORDER] **Dashboard-mutation idempotency audit (boundary gap 5) — FAIL, 3 BLOCKING; fix lane `dash-idem-fixes-2026-08-06` dispatched same session** (frozen `docs/adversarial/audit-dashboard-idempotency-2026-08-06.md`, all verdicts executed against real Hono/DO surfaces). **B1** `setup_infrastructure` durable buy-intent is namespaced by the CALLER's idempotency key (`engine/provisioning.ts:500`) → unkeyed-then-keyed (or k1-then-k2) retry buys a SECOND domain + unconditional duplicate billable mailbox rows — the paying customer's exact retry surface; keyless retries converge. **B2** `/remove-mailboxes` is relative ("release N") and accepts-then-IGNORES `Idempotency-Key` — identical-key double release destroyed twice (2→1→0); Stripe side of the same op is already set-to-N (`stripe-client.ts:290`). **B3** token rotate (documented as THE lost-token recovery path) leaves all dashboard sessions alive — replayed session mints are uncapped/30-day, no revoke-all; containment failure. **Elevated into the fix lane (auto-send is ARMED):** `launch_campaign` double-submit → duplicate identical campaigns = duplicate real outreach. Non-blocking ledgered: checkout sends no Stripe Idempotency-Key (4 calls→4 sessions); view create/promote/delete lack idempotency/CAS + SPA doesn't disable those buttons; thread-label LWW stale-replay clobber; webhook-subscription double-create → permanent double delivery; `/cancel` end-of-period replay misreports `alreadyCanceled:false`; concurrent rotation can hand a dead token with 200. UNVERIFIABLE from audit: live-registrar leg of B1 (sandbox ports), real-Stripe double completion, SPA button-state Playwright drive. 11 attack classes held (CSRF 27/27, layout rev-CAS, single-default invariant, magic-link single-use, cancel-teardown race — detail in doc). **NOW SHIPPED + DEPLOYED 2026-08-09 (verified in code): B1, B2, B3, and the elevated `launch_campaign` double-submit guard.** B1 — `setup_infrastructure` and `/remove-mailboxes` now accept and honor an `Idempotency-Key` header (`routes/infrastructure.ts:11-13`, `routes/checkout.ts:27-30`, code-verified). B3 — token rotate now revokes ALL dashboard sessions atomically with the token (`routes/token-rotate.ts` header comment: "the bearer token AND every dashboard session for the tenant, in one atomic write", code-verified). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). Non-blocking-ledgered items in this bullet (checkout Stripe Idempotency-Key, view create/promote/delete CAS, thread-label LWW, webhook-subscription double-create, `/cancel` replay misreport, concurrent-rotation dead-token-with-200) were NOT part of this wave and stay open.
- [ ] 2026-08-06 [ORDER] **Wave-2 re-gate follow-ups (frozen `docs/adversarial/wave2-integration-gate-2026-08-06.md` ROUND 2, none blocking):** N-1 within the dispute lane, distinct dispute OBJECTS share one watermark — a second genuine chargeback emitted before an earlier dispute's win gets refused (narrow: needs >3-day delivery delay; now ALERTS at least); fix = per-dispute-id keying or skip conflict-check for dispute.created. N-2 a REFUSED stale invoice.payment_failed still counts as a dunning strike (webhook_events claimed before the staleness check) — can suspend a recovered payer on their first genuine failure; introduced by the wave creating the first recorded≠applied divergence; fix = exclude refused events from billingFailureCount or claim after the check. N-3 CLASS HAZARD (not reachable this ship): billing_event_order changed PRIMARY KEY mid-wave and CREATE TABLE IF NOT EXISTS never alters — no migration path exists for a DO-table RESHAPE and no fresh-DO test can catch it; add a shape-version guard convention before any future table reshape. Cosmetic: email-port.ts:60 cites a nonexistent test filename. **N-1 REFINED 2026-08-09 via wave-3's webhook-inc2 lane** — `charge.dispute.created` now skips the per-lane staleness/conflict check (the fix N-1 itself suggested), gated by a new `isDisputeSettledWithoutFreeze` check (`billing.ts:407,488`, code-verified). That exemption then surfaced ITS OWN new bug (wave-3 gate finding B-1: unconditional exemption could brick a tenant on closed(won)-before-created ordering) — bounded + fixed same wave (`414b58e`), r2 SHIP 11/11 attacks failed. Merged `main` `17e05d4`, deployed `0f197c4d`. **N-2 FIXED 2026-08-09** — refused/stale events are now explicitly marked `applied=0` (`billing.ts:598,620`, code-verified) rather than silently counting as applied; same merge/deploy. N-3 (DO-table reshape hazard, still no shape-version guard convention) stays open — not addressed this wave.
- [ ] 2026-08-06 [IDEA] **Stale-checkout subscription-id overwrite (pre-existing, surfaced by the watermark rescope):** checkout(sub A) → cancel → re-checkout(sub B) → sub-A's checkout redelivers within Stripe's 3-day window, sees active==active (no state conflict), applies, and `stripe_subscription_id = COALESCE(...)` overwrites B with A → quantity sync points at the wrong subscription. Pre-existing on main before any watermark; narrow window; refusing instead = the compliance fail-open. Needs its own small design (e.g. subscription-id-aware staleness for the checkout apply).
- [ ] 2026-08-06 [ORDER] **readDeclineCode dead on real payloads (webhook-lane finding, out of its scope):** on a real unexpanded invoice.payment_failed, `charge`/`payment_intent` are bare id STRINGS so every decline-code branch returns null — now that F1 makes the handler run, every real decline grades TRANSIENT and the A5 permanent-decline fast path (lost_card/stolen_card/fraudulent) never fires; those customers grind the full 4-strike cycle instead of immediate suspend. Real fix = a second Stripe read (retrieve the PaymentIntent/charge) — design decision, queue with the next webhook increment. Also open: audit item 7 (413 cap reads declared content-length, bypassed by streamed body) — untouched, cheap. **BOTH FIXED + SHIPPED + DEPLOYED 2026-08-09 (verified in code):** readDeclineCode now does the second Stripe read — `resolveDeclineCode`/`getDeclineCode` retrieve the PaymentIntent for `last_payment_error.decline_code` (`billing.ts:335,750-751`, `stripe-client.ts:322-353`, code-verified) — the A5 permanent-decline fast path should now fire on real payloads. Item 7 fixed via `readTextBodyWithCap`, which caps ACTUAL bytes read (not declared Content-Length) and cancels the stream at the ceiling (`validate.ts:37-79`, code-verified, docstring cites this exact audit finding by name). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). **Still open (brief-confirmed, not this wave's scope):** live-verify readDeclineCode against a REAL unexpanded `invoice.payment_failed` delivery (the shape is code-correct but unproven against a live Stripe payload) — same live-verify need as `## Open`'s webhook-inc2-residuals bullet item (b) above; the two are the same open question from two angles.
- [ ] 2026-08-06 [ASK/IDEA] **Re-buy lane residuals (2026-08-06):** (a) a permanently wedged address burns TWO plan slots with no reclaim path (both dispatches ledgered honestly; founder may want a reclaim mechanism); (b) ABSENCE_MIN_AGE_MS=15min is code-inferred, not live-validated — close with one live /mailboxes/list right after a real buy (fold into the arm-watch checklist); (c) re-buy is retry-driven, not cron-driven — a tenant whose agent stops retrying stays wedged (stuck alert still fires on the first retry).
- [x] 2026-08-06 [ORDER] **Inc-D ARM-BLOCKING handoffs (from the Inc-C build report, 2026-08-06):** (1) the provider WRITE PATH at insert (§1a) — `insertProvisionedMailbox` must record `provisioned.provider` and `connectByoMailbox` must write 'byo'; without it any mailbox provisioned AFTER a tenant's clock flip gets provider='' and Inc-D's picker makes it PERMANENTLY send-ineligible, silently. (2) tick.ts:176's hoisted `now` is broader than N7's wording — it also drives the due-window check + first_send_eligible_at comparison after awaits; fix all three uses at the claim. (3) clock-provenance guard test (no `new VirtualClock(` outside blessed sites) unbuilt. (4) NEW-4 unchanged (retired sandbox rows still feed gatherMailboxHealth; sandbox domains rows unretired). Plus the standing Inc-D list: picker predicate + R3 scope + R4 shared predicate + §1c alerts + U2 provenance array + N6 + §5 budgets/rotation + scheduled.ts reorder. **Separate residual (route to rebuy/integration): a non-finite startWarmup parse now throws NON-retryable (retryable would re-enroll a BILLED warmup subscription per retry — builder's correct deviation from the design), leaving a created-but-unmarked subscription; proper close = reorder the marker write in mailbox-provisioning.ts (rebuy lane's file).** **NOW SHIPPED 2026-08-06 (verified in code):** (1) provider write path confirmed — `insertProvisionedMailbox` writes `provisioned.provider` (`mailbox-provisioning.ts:488`), `connectByoMailbox` writes literal `'byo'` (`byo-mailbox-composition.ts:115`). (2) tick.ts's tick-start `now` fix landed, explicitly noted WIDENED per this audit (`tick.ts:258` comment cites "N7 ... WIDENED by the Inc-C audit"). (3) `clock-provenance.test.ts` exists; only 2 `new VirtualClock(` construction sites remain in source (`tenant-do.ts:221`,`:518`, both blessed). (4) NEW-4 landed with the positive-evidence correction (`clock-migration.ts:156-188`: sandbox retirement now requires proof of sandbox-era origin, not mere absence). Folded into wave-2 integration (`f7ee786`), deployed `8187d36e`.
- [ ] 2026-08-06 [ORDER] **Non-finite startWarmup parse residual (split out of the Inc-D ARM-BLOCKING handoffs entry above — status not independently confirmed this pass):** a non-finite `startWarmup` parse now throws NON-retryable (retryable would re-enroll a BILLED warmup subscription per retry — builder's correct deviation from the design), leaving a created-but-unmarked subscription; proper close = reorder the marker write in `mailbox-provisioning.ts` (rebuy lane's file). Route to rebuy/integration.
- [x] 2026-08-06 [ORDER] **Wave-gate non-blocking follow-ups (frozen `docs/adversarial/wave-integration-gate-2026-08-05.md` #2-#4):** (#2) wire A's `retry_setup` message hard-codes DNS prose while wave1's `awaitMailboxReady` retryable throw lives in the same try — REST body says step "mailbox purchase" while the agent message says "DNS registration is still completing"; make the message step-aware (fold into msgchannel Inc2 or a fast-follow; executed-proven contradiction of two customer surfaces). (#3) `mailbox_intents` at status 'intent' with the vendor already holding the mailbox → second paid buy on a narrow kill-window (kill between accepted buy and status write); the naive ask-vendor-at-'intent' fix has its own hole — design with care, pairs with the re-buy ASK below. (#4) the vendor-name source tripwire globs `../src/**` only, missing `packages/shared` whose error messages `error-response.ts` returns verbatim — currently clean, widen the glob. (R2-NEW) drop `"active"`/`"ok"` from READY_STATUS_TOKENS — in a propagation field those plausibly mean in-progress; not causally reachable like #1 was, but cheap. **Ready-token live check SATISFIED pre-deploy (2026-08-06): the workspace's propagated domain (dmhadvisor) reads dns_propagation_status="propagated" / nameserver_match_status="matched" live — both in READY_STATUS_TOKENS — so the gate's stall-risk directive is closed by observation; goauthorpitchdesk will read ready when the vendor flips the same field.** **NOW SHIPPED 2026-08-06 (verified in code):** #2 `retry_setup` message is step-aware (`tenant-messages.ts:170`, `provisioning.ts:540` comment confirms). #3 the 'intent'-status double-buy hole closed via the guarded re-buy's verify-at-vendor-before-rebuy dispatch tracking (`mailbox-provisioning.ts` intent/dispatch logic, same lane as the re-buy ruling below). #4 vendor-name tripwire glob widened to include `packages/shared` (`test/vendor-identity-leak.test.ts:34`). R2-NEW `READY_STATUS_TOKENS` no longer contains `"active"`/`"ok"` (`vendors/real/inboxkit-domain-port.ts:368-378`, confirmed 9 tokens, neither present). Folded into wave-2 integration `f7ee786`, deployed `8187d36e`.
- [x] 2026-08-06 [ORDER] **Vendor-accepts-never-creates mailbox wedge — FOUNDER RULED (in-session AskUserQuestion): auto-re-buy YES, ONCE, WITH GUARDS** — before any re-buy the system must confirm at the vendor that the first purchase produced NOTHING; max ONE automatic retry per intent; founder alert fires either way. Build = attempt cap + verify-at-vendor + single guarded re-buy + alert, designed together with wave-gate follow-up #3 (the 'intent'-status double-buy window — the naive ask-vendor-at-intent fix has its own hole, per the gate). Lane DISPATCHED 2026-08-06. **NOW SHIPPED 2026-08-06:** guarded re-buy built — durable dispatch-claim table `mailbox_buy_dispatches` (`schema.ts`, `provision-intents.ts`) tracks attempts, verifies absence at the vendor before the one authorized automatic re-buy, alerts either way (`mailbox-provisioning.ts:255-290`). Folded into wave-2 integration (`rebuy-guarded-2026-08-06` lane `bb48a31`), merged `f7ee786`, deployed `8187d36e`.
- [ ] 2026-08-06 [ORDER] **BYO teardown — FOUNDER RULED: NEVER touch a customer-connected domain at teardown.** Teardown skips the vendor domain-release for connection_type='connected'/BYO domains — remove only OUR records/mailboxes; the customer disconnects their own domain themselves. Zero live risk today (no BYO customers); BUILD AFTER the wave2/platform-cred lane lands (same file: engine/lifecycle.ts). Founder digest naming InboxKit: ops-only surface, acceptable under the customer-surface rule — no action.
- [x] 2026-08-05 [ORDER] **P0 — Dunning sweep FAIL (3 blocking, cron LIVE, frozen `docs/adversarial/audit-dunning-2026-08-05.md`):** F1 `runDunningSweep` is the ONLY per-tenant sweep in `ops-sweep.ts` (`:42-65`) with NO try/catch — siblings `runDeliverabilitySweepAllTenants`/`runWarmupCancelSweepAllTenants`/`runWebhookDeliveriesAllTenants` (`:141`/`:180`/`:208`) each wrap the body — so one wedged tenant DO aborts the whole 5-min cron sequence platform-wide (watchtower/webhooks/spend-reaper/sdnRefresh/sdnRecovery all skipped that tick); fix: wrap the per-tenant body like its 3 siblings + wrap `runScheduledOpsSweep`'s legs. F2 the idempotency guard row (`insertDunningEventIfNew`, `applied=true`) commits BEFORE `suspendForDunning()` (`ops-sweep.ts:51-63`) — a crash/RPC-failure between them leaves the dunning_events row committed but the tenant unsuspended, and since `cycle` freezes once Stripe exhausts its ~4 retries, the suspend never gets retried — permanent; fix: apply the suspend before recording the guard row, or add a reconcile pass. F3 `suspendTenant` (`engine/ops-summary.ts:69-75`) is an unconditional `UPDATE tenant_profile SET status='suspended' ... WHERE id=?` — no `WHERE billing_state='past_due'` re-check — so a recovery webhook landing in the read(`opsSummary`)/write(`suspendForDunning`) race gap gets clobbered: a PAYING customer suspended + emailed "suspended for non-payment," sending frozen, no self-heal (permanent-decline codes trigger this at cycle 1, no need to reach count 4); fix: conditional `UPDATE ... WHERE billing_state='past_due'`, the same atomic-conditional-UPDATE pattern already used by `resolveScreeningReview` + the spend-ledger reserve. Severity: the cron is ARMED and LIVE (`wrangler.toml:107-108` `crons=["*/5 * * * *"]`, despite `scheduled.ts:4-6`'s "commented-out" comment being STALE) but there is NO current victim (no tenant is past_due; Mordy is paid+active) — P0 = land the fix before ANY tenant's next failed invoice, not after one forces it. Also fix the 2 stale comments (`scheduled.ts:4-6`, `admin/dunning.ts:4`/`:39` — both misattribute the ACT half to `routes/admin-ops.ts`; it's actually `admin/ops-sweep.ts`). **NOW SHIPPED + DEPLOYED 2026-08-06:** fix committed `8fd578e` (RED-proven x5), merged to `main` `c90b972` (--no-ff, lane branch `fix/dunning-p0` preserved), deployed Worker `bb8dc830-4785-4e58-a99c-74a05841fb2e` (confirmed latest via `wrangler deployments list`), combined-diff adversary gate SHIP (`docs/adversarial/wave-integration-gate-2026-08-05.md`). Live-verified: `/status` ok both hosts, cron `*/5 * * * *` intact (`wrangler.toml:108`).
- [x] 2026-08-05 [ORDER] **Msgchannel Increment 1 (system→agent message channel) — built, gated SHIP-AFTER-FIXES, `[dark-unarmed, UNCOMMITTED]`:** full-suite-green (133 files/1215 tests, tsc clean) + both wires (`retry_setup`/`credential_ready`) behaviorally tested; adversary gate frozen `docs/adversarial/msgchannel-inc1-gate-2026-08-05.md` — **1 BLOCKING** (Finding 1: on a same-idempotency-key retry, wire A's `retry_setup` message names + dedups on the WRONG domain — a fresh loop candidate the resume branch never actually touches — so the customer-facing message can claim a domain that was never bought; the test blind spot is a single-candidate fixture that short-circuits via `ValidationError` before ever reaching the wire-A catch) that must land BEFORE deploy, plus **1 non-blocking** (Finding 2: `credential_ready` has no tenant-lifecycle gate — a canceled/suspended tenant can get a false "sending is now enabled"). Source is UNCOMMITTED in the working tree — deliberately NOT on main, NOT deployed, to avoid shipping a phantom-domain customer message on an unrelated deploy. Increments 2-4 (operator route, list_messages/ack_message tools, email mirror) NOT built. Resume: fix Finding 1 → re-verify → commit → re-gate clean → deploy. **NOW SHIPPED + DEPLOYED 2026-08-06:** gate fix (Finding 1 + Finding 2) committed `a9f57a4` (RED-proven both), merged `c90b972`, deployed `bb8dc830-4785-4e58-a99c-74a05841fb2e`, combined-diff gate SHIP. Increments 2-4 remain NOT built — founder-authorized full channel, queued as their own arc (see the msgchannel IDEA entry below / `HANDOFF.md`).
- [x] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE 1 — code-only, unblocks PROVISIONING (root cause: the happy path was only ever tested against an always-succeeds sandbox → async/timing/driver/clock realities never exercised; Mordy's domain is recoverable IN PLACE — live-confirmed vendor status="active", adopt-before-buy recovers it, no re-provision/no re-charge). Frozen: `docs/adversarial/{sweep-domain-type,sweep-vendor-leak,forward-path-audit}-2026-08-05.md`.** (a) branch `setDns` on `connection_type` purchased-vs-connected — skip the connect-existing NS flow for purchased, poll status/propagation instead (carry `connectionType` through `OwnedDomain` + record on the domains row — it's fetched at `listOwnedDomains:243` then DROPPED); (b) L1 gate `dns_status='ready'` + mailbox buy on REAL propagation flags, not on setDns merely not throwing; (c) L2 poll-until-listable/ready gate between mailbox buy and every `resolveMailboxUid` consumer + real buy-idempotency (record the bought email BEFORE `startWarmup` so a retry resumes at warmup, never re-buys — the `/already-exists/` handling was removed at gate c, and `idempotency.ts` deletes the claim on throw → today's retry re-buys/double-charges); (d) stop retryable-laundering — `setDnsWithRetry` must preserve a permanent VendorError as non-retryable; (e) L7 billing: don't count/bill a row whose vendor provisioning didn't complete; (f) vendor-identity abstraction — `customerSafeVendorFailure` abstract-label MAP replacing `CUSTOMER_SAFE_STEPS`'s raw endpoint tokens; store abstract labels in `logAction`/`vendorHealthError`/the tick failed-send event, never `err.message`; (g) honest errors — restore step + retryability signal, vendor-blind. Tests MUST exercise REAL async/purchased-domain vendor behavior (a stubbed-success fixture is what let the incident hotfix gate pass falsely) + the vendor-leak `?raw` tripwire. RED-proof, adversary gate against real entry points, deploy. **⚠️ GATE ATTACK VECTOR (live retry capture 2026-08-05, Mordy's agent):** his `domains` row was recorded by the adopt path BEFORE any connection_type discriminator existed (the local table has NO such column today; `registrar:"adopted"`) — the fix's purchased-vs-connected branch MUST correctly classify pre-existing rows (backfill or live vendor re-fetch at DNS-step time), or his post-deploy retry fails identically. His agent independently confirmed the class ("any tenant whose domain came through the registration path"). Retry at ~02:30Z returned the same vendor_error as expected; domain still safe (1 domain active, 0 mailboxes, no double-charge). **Correction to record:** the incident-hotfix-gate's "32s race" premise (`docs/adversarial/incident-hotfix-gate-2026-08-05.md`) was a MISDIAGNOSIS, and its round-2 proof stubbed `setDns` to succeed on a purchased domain — a fixture-unreal pass; the fix wave must not re-trust that gate. **Note:** DUNNING P0 (entry above) is independent of provisioning and stays its OWN P0, not folded in here, though it shares the cron surface (see L8 correction below). **NOW SHIPPED + DEPLOYED 2026-08-06:** fix committed `00a6230` + gate-fix #1 `8e55bfc` (DNS false-ready — `purchasedDomainIsReady` now reads the vendor's own verdicts only), merged `c90b972` (lane branch `fix/provisioning-wave1` preserved), deployed `bb8dc830-4785-4e58-a99c-74a05841fb2e`, adversary gate SHIP after 2 rounds (r1 SHIP-AFTER-FIXES 1 blocking → fix `8e55bfc` → r2 SHIP, `docs/adversarial/wave-integration-gate-2026-08-05.md`). The GATE ATTACK VECTOR note above (pre-existing-row classification) stays live-relevant — closed by the gate's ready-token live check (workspace's propagated domain reads both READY_STATUS_TOKENS live), confirm again on Mordy's actual retry rather than treating the gate alone as proof.
- [x] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE 2 — unblocks SENDING; 3 founder decisions RULED 2026-08-05 (gate lifted, see end of entry).** L3 OAuth mint (manual per-mailbox today; programmatic `InboxKitOAuthMinter` is an UNVERIFIED shape-guess, not wired; `showMailboxCredentials` also unverified) · L4 wire a prod driver for `runTick` (verified 2026-08-05: only reachable via the DO RPC method `tenant-do.ts:821` and the demo path — zero cron/route/MCP-tool/alarm callers; `scheduled_sends` rows never drain in prod; wiring it ALSO arms auto-send) · L5 fix the frozen VirtualClock for paid tenants (real wall-clock — cascades: warmup never ramps, `sent_today` never resets → ~25 lifetime sends then send-dead, warmup pool bills forever; L4+L5 MUST ship together) · L6 wire a prod driver for `runPollInbox` (verified 2026-08-05: same DO-RPC-only reachability as L4, `tenant-do.ts:825` — replies never fetched). **L8 CORRECTED (bookkeeper-verified 2026-08-05, contradicts forward-path-audit.md's L8 as written):** the cron trigger is ALREADY live, not commented out — `wrangler.toml:107-108` reads `[triggers]` / `crons = ["*/5 * * * *"]` and `index.ts:166`'s `scheduled()` calls `runScheduledOpsSweep` unconditionally every 5 min (matches the dunning P0 entry's own "cron is ARMED and LIVE" finding above); dunning/warmup-cancel/webhooks/digest/watchtower/deliverability/spend-reap/sdn-refresh all already run live. `reconcileMailboxCredentialPushes` is ALSO already wired — it runs inside the deliverability leg every 5 min (`tenant-do.ts:864`, config-gated no-op until the programmatic minter arms), not a separate arm-time blocker. Only the header comment in `scheduled.ts:1-6` ("the actual crons=[...] trigger is commented-out") is stale — the trigger itself isn't; do not repeat that claim. The only genuinely missing production drivers remain L4 and L6. CREDSTORE F1/F2 (frozen `docs/adversarial/audit-credstore-2026-08-05.md`, overall PASS) stay in this wave: F1 (stale keyless push reverts a rotation — the i3i4-dropped-idempotency-key regression, content-hash dedup only, no monotonic/version check) must fix BEFORE the programmatic OAuth minter arms; F2 (teardown doesn't tombstone `mailbox_cred_pushes` rows so a post-cancel reconcile can resurrect a revoked credential) — mark rows terminal on teardown. Neither F1 nor F2 is Mordy-reachable today (his path is the deterministic manual mint). **FOUNDER RULED 2026-08-05 (in-session, captured at mention time):** (1) clock = REAL wall-clock for ALL non-sandbox tenants (founder verbatim: "Why not get a real clock for any non sandbox"; VirtualClock stays sandbox/demo-only — broader than the paid-only framing); (2) auto-send = BUILD + ARM NOW (driver design cron-vs-DO-alarm delegated, adversary-gated before arm); (3) OAuth = BOTH in parallel (manual mint for Mordy the moment his mailboxes exist + independently live-verify the programmatic minter on a throwaway so customer #2 needs no manual step). Build order: after Wave 1 merges (same-file collision). **DESIGN ARC COMPLETE 2026-08-05:** design v1 (Plan lane) → adversary R1 **NO-SHIP 8 blocking** (headline: arming as-designed would have routed Mordy's first campaign to demo-era sandbox mailboxes and drained it to 'failed' in ~25 min; plus double-shift migration corruption, in-flight-saga clock hole, cron-leg starvation) → v2 delta (per-mailbox eligibility + provider provenance column + transactionSync migration w/ persisted delta + DelegatingClock + budget/rotation + tombstone-first/engine-seq-tombstone) → adversary R2 **SHIP-AFTER-FIXES, 0 blocking** (all 8 re-derived closed; transactionSync verified real at workers-types :753). Frozen: design+delta `archive/2026-08-05-provisioning-fixwave-plan/wave2-design-2026-08-05.md`, verdicts R1+R2 `docs/adversarial/wave2-design-review-2026-08-05.md`. **Build briefs MUST fold in R1-R8 + NEW-4** (R1 record-row-above-deps-guard; R2 warmup-cancel.ts:58 stale-now — the six-site enumeration; R3 tenant scope in NOT EXISTS; R4 shared eligibility predicate picker+alert; R5 budget-vs-180s-timeout ordering ladder; R6 rotation zero-guard; R7 tombstone runbook note + retention; R8 provider='' rollback coupling note; NEW-4 retire sandbox domains rows + released_at filter in gatherMailboxHealth) + U1/U2 build-lane probes (U1 constructor-context transactionSync + NaN bind; U2 pre-arm Mordy provenance read incl. every provider='google' row has a pushed credential). **NOW SHIPPED 2026-08-06 — built whole:** L4 (auto-send cron+DO driver) and L6 (poll-inbox driver) + L5 (real wall-clock for all non-sandbox tenants) SHIPPED + ARMED live (Worker `8187d36e`, kill switch `AUTOSEND_DISABLED` confirmed wired in `env.ts:210`/`admin/ops-sweep.ts:378`, unset = armed). Credstore F1 (pushSeq ordering) + F2 (teardown tombstones) shipped both sides. L3 OAuth mint PARTIAL — the manual per-mailbox path is live (unchanged); the programmatic `InboxKitOAuthMinter` verify-plan is still pending founder asks (scope answer via the InboxKit support ticket). Merged `f7ee786` (--no-ff, lane branches preserved), deployed `8187d36e`, combined-diff gate NO-SHIP r1 (2 blocking, unrelated to this entry's L3/L4/L5/L6/credstore scope) → fixes → SHIP r2 (`docs/adversarial/wave2-integration-gate-2026-08-06.md`).
- [ ] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE — deferred, not yet re-triaged into Wave 1/2 (carried forward verbatim from the pre-restructure consolidated entry so nothing is silently dropped; orchestrator to fold into Wave 1/2 or open a Wave 3):** unified `withVendorIntent`/`withSaga` durable-intent mechanism (3 sweeps converged: record-before-effect, resource-named, graded-not-deleted) · discarded sanctions re-screen verdict (F9) + mode-(b) OFAC-screening-bypass-via-checkout-partial-failure · `initTenant` never gets a Stripe customer + Stripe-webhook claim-then-throw (mode-b) · release-retry poisoning (mailbox bills forever) · `REPLACE_DOMAIN` cron lane buys unrecorded, logs the opposite (distinct from the setDns wrong-op fixed in Wave 1) · Stripe checkout no-idempotency/no-record double-session window · monthly ceiling defeat both directions · backwards cross-store orders (terminate-before-token-lock, screening-reject-before-terminate) · CLI/dashboard send has no idempotency keys · error-mapping reflection coverage test + clock-provenance guard · teardown sandbox-flip (F7) · plan-slot leak (F6, slot commits at provision, slot_counted written post-warmup) · `provisioningFailureCount` hardcoded-0 + `pendingCredentialPushes` honest-zero (F8/N6) + watchtower blind to provisioning failures · claim-outlives-teardown billable-row-no-vendor-mailbox (N4) · SANDBOX-REALISM lane (systemic root: sandbox built to always-succeed → green suite hides every live failure; make it fail/stall/crash; `ARCHITECTURE.md` falsely claims a crash-sim exists) · false-doc-claims cleanup (crash-sim, spend-ceiling real-time comment, phantom `jobId`/202 saga in SPEC/openapi/tools) · unbounded `USAGE_METERING_SKIPPED`/`MAILBOX_HEALTH_UNAVAILABLE` action rows per `infrastructure_status` poll · `findAdoptableDomain` swallows listing-failure-to-null (double-purchase trigger w/ N1) · remaining `infrastructure_status` field gaps (dns/credential-push/activationState-at-0) · defect-asserting green tests. Full text of the superseded entry (for provenance): `archive/2026-08-05-provisioning-fixwave-plan/superseded-roadmap-provisioning-class-wave.md`. Sources: `docs/adversarial/{incident-hotfix-gate,provisioning-pipeline-deep-dive}-2026-08-05.md`, `.claude/agent-memory/class-sweeper/{vendor-mutation-saga-surfaces,idempotency-replay-surfaces,error-mapping-surfaces,coverage-ledger}.md`.
- [ ] 2026-08-05 [IDEA] **Per-account system→agent message channel (founder 2026-08-05):** a lightweight inbound `messages` store per tenant that the customer’s agent reads via an MCP tool (and/or a field on `infrastructure_status`), letting our system + the operator drop STRUCTURED notices to the customer’s agent WITHOUT a full relay — activation/next-step instructions, retry-with-same-key prompts, the OAuth-mint-ready signal, incident notices. Plus an EMAIL opt-in mirror. Directly addresses the manual-handoff gaps the 08-05 incident exposed (today the founder must hand-relay "retry now" / "you can send now" to Mordy). Composes with the pipeline-adversary H-alert. **FOUNDER RULED 2026-08-05: FULL CHANNEL AUTHORIZED** (in-session AskUserQuestion) — Inc1 ratified retroactively (the prior build-before-sign-off discrepancy is resolved); fix Finding 1 → re-gate clean → deploy; then Increments 2-4 (operator route, list_messages/ack_message tools, email mirror) each adversary-gated.
- [ ] 2026-08-05 [ORDER] **Full boundary audit (founder 2026-08-05, "how do we completely get everything"):** the prior audit missed heavily because reviews ran against a sandbox built to always-succeed + inner-helper unit tests + docs claiming a nonexistent crash-sim. Method: (a) build failure-capable test doubles FIRST (the SANDBOX-REALISM lane — fakes that return errors/stall/race/already-owned) — prerequisite, nothing is visible without it; (b) enumerate EVERY external boundary (vendor mutation, cross-store write, time source, idempotency key, error path, multi-step saga) — 5 classes mapped so far (the 08-05 sweeps), confirm ALL; (c) gate each by EXECUTING the real entry point with faults injected, not code-reading; (d) reflection-based coverage guard per closed class. Founder gating the SCALE (dozens of agents) before a full multi-agent launch; runs incrementally inside the PROVISIONING FIX WAVE until then. **Progress (2026-08-05):** 2 of 7 mapped gaps closed — credstore PASS (`docs/adversarial/audit-credstore-2026-08-05.md`), dunning-sweep FAIL (`docs/adversarial/audit-dunning-2026-08-05.md`, P0 above). Boundary-map correction: `apps/engine` is NOT unswept (4 frozen reviews + a SIGKILL proof already exist for it); the registrar-arming NO-SHIP is STALE — a round-2 SHIP exists in the SAME doc (`docs/adversarial/registrar-arming-review-2026-07-28.md`, merged `c1e24a5`). 3 gaps remain, staged and ranked: (5) dashboard mutation idempotency, (6) aging OFAC + outbound-webhook lanes (cheap "did files change since review" diff-checks), (7) ops-sweep/watchtower alerting completeness. Gap (3) Stripe webhook signature-verify/replay is now CLOSED — audit FAIL 3-blocking (`docs/adversarial/audit-stripe-webhook-2026-08-06.md`, currently untracked in the `main` worktree) → all three fixed in `fix/stripe-webhook-p0` `9b432a8` → folded into wave-2 `f7ee786`, deployed `8187d36e`; 2 new non-blocking residuals ledgered (`readDeclineCode` dead-on-real-payloads; the Stripe audit's own item 7, a 413 content-length-omission bypass — see the `## Open` 2026-08-06 readDeclineCode entry). Keep the boundary-map's gap list as the fan-out plan.
- [ ] 2026-08-02 [ORDER] **Warmup-wave fast-follow (round-2 residuals, none blocking, from `docs/adversarial/warmup-wave-final-review-2026-08-02.md`):** (R1) space warmup-cancel retry attempts by ELAPSED TIME, not invocation count — 5-min cron × 5 attempts is only a 20-min budget, a transient vendor outage exhausts it; (R3) a missing `pages` field in the warmup/list response must read as INCONCLUSIVE, not absent (money-leaking direction — could silently skip a real cancel) — add multi-page + last-page tests; (R2) move the warmup-cancel cron lane below `runWatchtower` in `scheduled.ts`, per that file's own stated ordering rule; (R4) fix stale tick-era comments in `warmup-cancel.ts` (:24, :29, :31, :45, :50 — :31 contradicts the give-up-marker split) and `tick.ts:165-166`. (N-b residual) the ops digest is pull-only — a give-up alert reaches the digest + `watchdogAlerts` but no email push; consider a watchtower check. **FUSE: R1+R3 must land before the first real mailbox reaches day 29 (~4 weeks after Mordy's first provisioning) — that's the day the auto-cancel step actually has to fire correctly for the first time.** **R2 NOW SHIPPED 2026-08-06** (verified `scheduled.ts`: `runWarmupCancelSweepAllTenants` now runs after `runWatchtower`, folded into wave-2 Inc-D, merged `f7ee786`, deployed `8187d36e`) — R1/R3/R4/N-b remain OPEN, FUSE unchanged.
- [ ] 2026-08-02 [IDEA] WARMUP_* action rows appear in the customer-facing activity feed — vendor-billing/ops noise leaking onto a customer surface; consider filtering. Also: BYO-ramp tiers (primary 20/day, shortened 40/day-by-day-10) are unpublished — publish when the BYO product path ships. Also (carried from the site-audit close above): single source-of-truth status object for site claims — staged for founder ratification, not built.
- [x] 2026-08-02 [ASK] **⚠️ NO production driver calls tick() — CAMPAIGN SENDS have no scheduler (adversary A1 adjacency, PRE-EXISTING, load-bearing):** wave-gate verdict (docs/adversarial/warmup-wave-final-review-2026-08-02.md A1 note) enumerated exhaustively: runTick is a B0 directly-callable RPC awaiting B2; prod cron (scheduled.ts) drives deliverability/dunning/digest/watchtower/webhooks ONLY; no route/MCP tool/DO alarm reaches tick(). scheduled_sends rows drain ONLY via runTick ⇒ real campaign sends never fire automatically in production. Not this wave's regression (B2 backlog, ROADMAP:121) — but it must be designed + armed BEFORE Mordy's first campaign or his sends sit queued forever. The A1 fix wires ONLY the warmup-cancel sweep into cron (deliberately not full tick — arming campaign auto-send is its own founder-gated arc). NEEDS: founder-ratified design (cron-driven per-tenant tick vs DO alarms) + adversary gate + arming. **NOW SHIPPED + ARMED 2026-08-06:** the cron leg now drives `runTick` via the wave-2 auto-send drivers (5-min cron leg + DO-side 4-leg gate + per-mailbox eligibility, kill switch `AUTOSEND_DISABLED` — `env.ts:210`, `admin/ops-sweep.ts:378`). Merged `f7ee786`, deployed `8187d36e`; first post-deploy cron cycle observed quiet-correct (orchestrator-reported via `wrangler tail`: `runScheduledPoll`→`runScheduledTick` "Ok" fleet-wide, zero errors, zero unexpected sends, no alert storm — not independently re-verified this pass, the live cycle had already elapsed by the time this ledger update ran).
- [x] 2026-08-02 [ORDER] **External LLM site audit (founder-supplied 08-02) — CLOSED 2026-08-02.** All 5 confirmed factual defects FIXED + DEPLOYED in the warmup-claims wave (commit `86996ef`, site Pages deployment `09b8deb2`, adversary R2 SHIP `docs/adversarial/warmup-wave-final-review-2026-08-02.md`): security-page "paid mutations remain disabled"/"future activation" residue rewritten · `/AGENTS.md` 301→200 redirect fixed via `site/_redirects` · sitemap lastmod bumped 35/35 · version-string skew closed (server 0.2.2 / CLI 0.2.1, single-sourced) · workers.dev-vs-api.coldrig.dev split resolved (`api.coldrig.dev` now the primary/documented host, workers.dev retained). Tool-count and stale-language claims ("12/17 tools", "sandbox only", "not live") were investigated and found FALSE — no site surface actually carries stale copy; no action taken. REMAINING OPEN (not this gate): positioning/copy-redesign recommendations (above-fold rewrite, qualification section, pricing lead, app.coldrig.dev move, single source-of-truth status object) staged for founder ratification, not auto-applied — status object also re-ledgered as an IDEA below; GSC/Bing recrawl resubmission is a founder click (IndexNow already fired HTTP 200 post-deploy).
- [x] 2026-08-02 [ORDER] **Warmup = bundle + AUTO-CANCEL after ramp (founder RULED 08-02, option b) — SHIPPED + DEPLOYED 2026-08-02.** Build: `apps/platform/src/engine/warmup-cancel.ts` (146 lines, cron-swept auto-cancel at ramp completion/day-29 transition) + `apps/platform/src/scheduled.ts` cron wiring, commit `e4250e9`; claims coupling honored — site copy (warmup lifecycle truth, exact ramp schedule 5/15/25/35/40) shipped in the SAME deploy, commit `86996ef`. Adversary provenance: round-1 NO-SHIP (3 blocking) → round-2 fix wave NO-SHIP (2 blocking: A1 no-production-driver for campaign sends [now tracked separately, see `## Open` 2026-08-02 ASK], A2 per-tenant-ceiling overclaim) → fix round → **R2 SHIP**, all frozen `docs/adversarial/{warmup-claims-fix-review-2026-07-31,warmup-wave-final-review-2026-08-02}.md`. Battery at ship: platform 125 files/1157 tests, typecheck clean ×5 workspaces (adversary-re-run, not builder-claimed). Deployed + live-verified: Worker `dbc4ae45-dc63-4b63-9455-51e8ffafc09f` (both hosts, cron intact; `/account` 401, `/checkout/success` 302, `/app` 200, `tools/list` 25, `serverInfo` 0.2.2); site Pages deployment `09b8deb2` --branch main (security-page + faq warmup copy live, `/AGENTS.md` 301→200, server-card 0.2.2, sitemap 35 lastmod, zero hits of the old "AI-driven layer" fiction); IndexNow fired HTTP 200 post-site-deploy. Non-blocking round-2 residuals (R1-R4, N-b) NOT part of this ship gate — see the new `## Open` fast-follow entry below.
- [ ] 2026-07-31 [IDEA] **Volume-aware warmup ramp:** `computeWarmupDay` advances by wall-clock age, not actual sends — an idle mailbox "ages into" the full 40/day cap with zero send history (cap schedule = ceiling, not proof of reputation). Advance the ramp by cumulative real sends (or hybrid age+volume) so lazy-start customers don't get a cold 40/day ceiling. Surfaced answering the founder's warmup question 07-31; irrelevant for Mordy (continuous agent sending).
- [ ] 2026-07-29 [ORDER] **Double-buy saga risk (adversary round-2 observation, pre-existing, REACHABLE once registrar armed):** the sync setup saga commits the real domain buy BEFORE mailbox+warmup, and `RealInboxKitDomainPort.buy` ignores its idempotency key — a downstream throw + agent retry re-buys a domain (spend-ceiling-bounded, but real money). Proper home = the B2 async-saga arc; until then, watch first provisionings live. Recorded in `docs/adversarial/registrar-arming-review-2026-07-28.md` round 2.
- [ ] 2026-07-29 [ASK] **`registerDomains` rides EVERY setup call (documentation footgun, adversary round-2):** a setup call omitting it resets a prior opt-in to 0 (safe direction — 503, zero buys — but surprising). Every registrar-using setup call must carry `registerDomains:true` + full `registrant`. MCP tool description says required-when-true; consider adding the every-call semantics explicitly + dashboard/docs mention.
- [ ] 2026-07-29 [IDEA] Metric hygiene: 62 of 63 `tenants_index` rows are internal test/QA tenants (45 from the 07-09 burst wave alone; only real signup = Mordy `ten_91aab24a`, mydevpros@gmail.com; 0 organic from listings as of 07-29). Tag (`internal` flag) or purge so tenant-count ≠ signup-count in reports; until then exclude known-internal patterns (yaakovscher+*, @example.com, Burst*, *Test*, verify/probe/smoke).
- [ ] 2026-07-28 [ORDER] **"Fast Start" prewarmed SKU (founder 2026-07-28: "we should offer it to Mordy and everyone else"):** productize InboxKit prewarm inventory (POST /prewarm/buy-domain, credits; $6/$7/$9-per-mbx tiers per docs/research/inboxkit-prewarm-2026-07-21.md) as a premium one-time fast-start option. Needs: pricing design (one-time fee per prewarmed mailbox, margin over their tier price), adapter method (prewarm endpoints NOT in RealInboxKitDomainPort today) + workspace-inventory→tenant adoption path (unbuilt), honest copy (aged reputation SHORTENS the ramp, never guarantees placement — no-guarantees posture holds). Design+adversary before build. For MORDY: recommended fresh+ramp (already set in motion; prewarm-for-him = hand-driven ops on unbuilt paths + more wallet) — founder may overrule. **FOUNDER 2026-07-28: AUTHORIZED for next-session background orchestration while awaiting Mordy** — full arc (pricing design → adapter prewarm methods → inventory-adoption path → claims copy), adversary-gated per lane; deep-research verdict on warmup posture (frozen `docs/research/warmup-posture-2026-07-28.md`) feeds the design. **2026-07-29: founder said HOLD for now** — authorization stands but re-confirm with the founder before actually starting the arc.
- [ ] 2026-07-28 [ORDER] **First live warmup ramp watch:** Mordy's mailboxes are the FIRST real senders through the warmup engine (warmup.ts ramp: warmup_started_at → warmupDailyCap by age, sent_today metering, deliverability throttle/pause on top — built+tested, never live-proven). At his first sends: verify daily caps enforce at the engine boundary, warmupDay/dailyCap surfaces read correctly in infrastructure_status, and the health loop's throttle path is reachable. Part of the arming checklist.
- [ ] 2026-07-28 [ORDER] **Customer transactional email gap (Mordy post-purchase: "No email fired. No receipt, no welcome, nothing"):** (a) IMMEDIATE founder click: Stripe Dashboard → Settings → Customer emails → enable "Successful payments" receipts (Stripe sends them, zero build); (b) BUILD: welcome/activation email on checkout.session.completed via the existing ops-email substrate (needs sender-identity + copy ratification; DMARC p=reject alignment per §1.7b findings); ledger-tied to the deliverability of coldrig.dev sender rep.
- [ ] 2026-07-28 [ORDER] **Billing self-serve actions wiring (Mordy: all three say "not wired to this dashboard yet"):** change-mailbox-count (API EXISTS — remove_mailboxes + add via checkout quote path; wire the dashboard buttons), update-payment-method + cancel (build = Stripe Billing-Portal session endpoint + button; portal handles both hosted). Paying-customer ask; pairs with the spend-ceiling wiring below as one billing-UX lane.
- [ ] 2026-07-28 [ORDER] **Owner spend-ceiling wiring — priority BUMPED (paying customer's agent flagged the "not wired up yet" preview label on the live Billing page).** Truthful state: customer-facing cap = preview-only; enforced today = quote-on-every-add (§18), count changes only via explicit tenant actions, our vendor-side $150 spend ceiling + slot caps. Wire persist+enforce as the next platform lane; until then the honest label stays (never fake-wire it).
- [ ] 2026-07-27 [ORDER] **Domain-onboarding matrix → product copy (founder: "we should note that somewhere"):** publish the honest matrix on byo-domain.html/docs/faq as a fast-follow copy pass (after the claims-truth lane merges): lookalike domains we register = ZERO customer DNS (recommended default, protects the main domain) · own domain at an external registrar = ONE nameserver change, once · own domain on Cloudflare Registrar = NS change impossible (CF product lock), token-connect path only (currently walled, research in flight) · BYO mailboxes = existing mail DNS mostly suffices. Frame as best-practice, not apology. Also fold conclusions from the 2026-07-27 deep-research lanes (InboxKit connect wall + subdomain-delegation workaround) when they land.
- [ ] 2026-07-27 [IDEA] X-Coldrig-Send-Token header (intent-log build): inert until the v2 SENT-scan increment, but it's a BRAND-FINGERPRINTABLE header on every cold email once real volume flows — filters/recipients could key on it platform-wide. Before the SENT-scan arc arms it: decide neutral rename (wire-compatible, e.g. opaque header name) vs keep; also confirm brand spelling. Flagged by the intent-log build review (no verdict weight).
- [ ] 2026-07-27 [ORDER] **⚠️ CF-REGISTRAR NS-LOCK CLASS (found live during the dmhadvisor probe, 2026-07-27):** Cloudflare-registered domains cannot change nameservers → InboxKit's NS-delegation connect path is impossible for them; the `cloudflare-domains/connect` API path returns `"domain is not allowed"` even on a BARE zone with the domain entity pre-created (entity uid 787b6375-c0d0-467c-af52-f6032c51a5a1, assigned NS josh/katelyn.ns.cloudflare.com) — cause unknown (plan-gating? endpoint prerequisite? support question drafted-pending). **authorpitchdesk.com is ALSO CF-registered (RDAP-verified) → Mordy's domain-connect is blocked by the same wall.** Paths: (a) crack connect via InboxKit support/app-UI attempt (founder return); (b) PILOT SHAPE CHANGE — InboxKit-registered lookalike domain(s) for Mordy instead of his bare .com (fully automatable, wallet credits, industry-standard main-domain protection) — RECOMMENDED, needs founder/Mordy word; (c) probe switches to etisrael.com (Amazon-registered → NS delegation works; its live website DIES on delegation — founder OK pending). dmhadvisor state: mail records + kwong root CNAME deleted (snapshot in scratchpad), entity created in workspace, connect refused. **Research frozen 2026-07-28 + ✅ RESOLVED LIVE same day:** `docs/research/cf-registrar-domain-connect-2026-07-28.md` — the founder's UI attempt CONNECTED dmhadvisor.com via InboxKit's Cloudflare-token path using the ZONE-scoped token (no NS change; registrar lock moot on this path; account-scope hypothesis dead). The public `cloudflare-domains/connect` 400 is an endpoint discrepancy vs the dashboard — support ticket v2 (API parity) drafted in the doc. Lookalike default unchanged as primary; BYO-CF becomes automatable once the public API matches the dashboard. Still OPEN: send ticket v2 · revoke the CF token. (`domains/remove` DONE 2026-07-29 — dmhadvisor entity `scheduled_for_deletion`, see the 2026-07-27 InboxKit-connect entry above.)
- [ ] 2026-07-27 [ORDER] **Relevance-gap response (founder: "mark all this stuff down to address")** — Mordy's-agent critique: guardrails protect deliverability, nothing below the agent layer checks list RELEVANCE (400 clean sends to the wrong list). Full closure is impossible at the infra layer (evaluator says so too) but NARROWING is designable: list-provenance/ICP-attestation at campaign creation, first-send ramp caps on NEW lists (D4 new-tenant ramp is adjacent, per-list ramp is not), sample-audit surface in the dashboard, and OWNING the line honestly in docs the way warmup-honesty is owned (copy addition, ratification-gated). Design lane candidate post-GA-tail; do NOT bolt on without a design+adversary arc.
- [ ] 2026-07-27 [ORDER] **Organic buyer evaluation (Mordy's AI) frozen** at `tools/buyer-panel/runs/2026-07-27-mordy-agent-organic.md` — positioning + honesty posture PRAISED; two actionables: (a) "concierge activation" caveat now reads as immaturity to evaluators — soften/delete GATED on the InboxKit programmatic-mint live verification at Mordy's provisioning (truth-first, never before); (b) could not find Coldrig unaided — discovery lever (GSC/Glama clicks) re-confirmed third time.
- [ ] 2026-07-27 [IDEA] `doctl compute firewall update` REPLACES the whole spec incl. droplet attachment — omitting --droplet-ids DETACHES the firewall (bit 2026-07-27, caught+re-attached same minute; add --droplet-ids to any future update one-liner).
- [ ] 2026-07-27 [ORDER] **Citation-panel weekly cloud routine (trig_01X119P6FiTgxMkamZr2tU29, armed 07-12) has NEVER produced a run** — post-ship audit 2026-07-27 found TREND.md holds only the 07-12 baseline row; the "2 weekly runs in" ledger claim was FALSE (that was the buyer-CHOICE panel). Investigate the routine's run history / re-arm or replace with a local runner; do not trust "armed" claims for cloud routines without an output-side check.
- [ ] 2026-07-27 [IDEA] Post-ship-audit minors: sitemap `lastmod` stale (07-14/15) — bump on next site deploy · "setup-completed" funnel milestone uninstrumented (61 signups → ? paid → ? setup: only the first number exists today) · homepage headline line-break cosmetic nit (1440px) · CF zone Bot-Management API reads need a broader-scoped token (founder dashboard) — same 07-16 gap.
- [ ] 2026-07-27 [ASK] Paid-conversion blind spot: `GET /admin/ops/digest` with ADMIN_TOKEN answers "of 61 signups, how many paid" — main loop classifier-blocked from local secret probing; founder runs the one curl in `docs/research/post-ship-audit-2026-07-27.md` §(f) or shares where ADMIN_TOKEN lives.
- [ ] 2026-07-27 [ASK] **Stripe sandbox webhook failure emails (root-caused 2026-07-27):** test-mode endpoint `we_1TvzMVIb6OQwXi7wi354H35z` (enabled, livemode:false) still points at the prod Worker URL, but prod's `STRIPE_WEBHOOK_SECRET` is the LIVE endpoint's secret since 07-23 — every sandbox event now fails signature → 400 (`webhooks.ts:39-40`) → Stripe "failing webhook" warning emails. Zero prod impact. Fix = disable/delete the sandbox endpoint (recreatable anytime; one API call with the test key, or Stripe will auto-disable it on its own after continued failures).
- [ ] 2026-07-24 [IDEA] event-driven alert class (builder-flagged, distinct from the cron class): alertScreeningHit / alertScreeningListUnavailable / alertRegistrarUnarmed fire per tenant ACTION — a tenant hammering a blocked action could storm by a different mechanism; sweep when the GA hardening tail runs.
- [ ] 2026-07-23 [ORDER] **GA WAVE MERGED + DEPLOYED — battery arc ran FIVE times; live-verified.** Both GA merges + glue land on main: OFAC SDN screening `32d5b6b` (G1a+G1b, fail-closed w/ self-heal + race guards, adversary SHIP + N-OF-1 CLOSED addendum) + provisioning-core GA `8674da7` (factory InboxKit wiring, spend ledger+ceiling+reaper, slot gate, honest activationState, adversary SHIP 0-blocking, 4 deviations ratified) + integration glue `1dccd94` (screening_status wired into activationState) + gitlink cleanup `d89516c` + GA fixture fix `6299788` (battery #4 correctly FAIL-CLOSED on 5 pre-OFAC fixtures lacking a seeded SDN list — N-OF-1 working as designed, not a regression; fix = benign-list seeding via shared `seedBenignSdnList`, test-only, assertions not weakened; re-run battery #5 PASS **763/763**). Deploy sequence (`wrangler deployments list`-verified): Worker `65415e9c` (signup-auth) → secret changes (INBOXKIT_API_KEY/WORKSPACE_ID) → `a8a0b117` (OFAC_LIST_URL fix, see next entry) — `a8a0b117` is the live version as of this bookkeeping pass. D1 migrations 0009+0011+0012 ALL applied remote (`wrangler d1 migrations list --remote`: "No migrations to apply!"). Live-verified: `/status` ok, `/checkout/simulate` 404, MCP `tools/list` 24, `serverInfo.version` **0.2.2** (was stale hardcoded "0.1.0", closed by `ca072ac` — single-sourced from `apps/platform/package.json`, confirmed live via a real `initialize` call).
- [ ] 2026-07-23 [ORDER] **OFAC first live cron hit HTTP 525 — root-caused + fixed `a7f3b97`.** The cron fired its fail-loud alert exactly as designed on first activation ("SDN.CSV fetch failed: HTTP 525", prior list retained, retry ~5min — the fail-closed path worked, nothing silently broke). Root cause: treasury.gov's public download URL redirects through a front-end that rejects the Workers TLS/fetch handshake. Fix: `OFAC_LIST_URL` repointed to the direct `sanctionslistservice.ofac.treas.gov` publication-service URL (`apps/platform/wrangler.toml:49`) — verified this session: the URL 302s to a signed S3 URL that 200s with a real 12-column SDN CSV. Redeployed `a8a0b117`. ⚠️ **VERIFIED AT BOOKKEEPING TIME (2026-07-24 00:11 local / ~21:11Z, ~6 min post-redeploy): `sdn_list_meta` is still EMPTY (0 rows) on remote D1** — the first successful load has NOT yet been confirmed. Cron runs every 5 min so this reads as too-soon-to-tell rather than a fresh failure (screening still fails CLOSED to 'review' with an empty list, which is safe, just not yet the intended clear-screening steady state) — **next session MUST re-check `sdn_list_meta` row count** before treating OFAC screening as live-loaded. Lesson for the class: any Workers `fetch` against a gov/vendor download URL should be pinned to the FINAL host, never a redirect front-end — the front-end can reject the Workers TLS profile even when a normal client follows it fine.
- [ ] 2026-07-16 [gated:founder] **RETURN-QUEUE (Yaakov, remaining items):** item (1) — the full production deploy chain (engine 443 smoke, gmail_api wire-Message-ID fix, Worker-side `ENGINE_AUTH_SECRET`, the fully-live reframe deploy, IndexNow) — is **CLOSED 2026-07-19**; full evidence in `archive/ROADMAP-done.md` "Drained 2026-07-19 (ship wave)". Still open, founder-hands: (2) name "submit to mcp.so and Cline" (classifier requires founder-named public targets — twice confirmed, teammate/roadmap authority insufficient); (3) ~~re-run `mcp-publisher publish`~~ **DONE 2026-07-21** (founder re-auth + publish; registry API verified serving 0.2.1-latest with the 96-char fully-live description); (4) GSC day-2 Request-Indexing batch; (5) ChatGPT: platform identity verification + 2-min in-browser read of openai.com/policies/usage-policies; (6) MCPB form check (clau.de/desktop-extention-submission — Team-org-gated or not?). **Remaining to full TENANT-live (deliberately still unarmed):** Cloudflare Tunnel / `ENGINE_BASE_URL` (bookkeeper-verified 2026-07-19 via `wrangler secret list`: still unset — only `ENGINE_AUTH_SECRET` now present) + `ENGINE_TENANTS` allowlist arming.
- [ ] 2026-07-13 [gated:founder] Enable Cloudflare Web Analytics + verify GSC/Bing indexation (~5 min, owner action; GSC half now superseded by the 2026-07-23 "GSC STATUS CORRECTED" entry below — Bing half folded into the founder click queue in `## Now`).
- [ ] 2026-07-23 [ORDER] **🚢 SIGNUP-AUTH DEPLOYED + §1.7b GATE SPLIT-VERDICT.** Battery #3 PASS (676/676) → Worker `65415e9c` deployed (migration 0009 applied) + site deployed (282510a3, "Free sign up" live ×4 on coldrig.dev). §1.7b EXECUTED live: POST /login (identical-200) → real send → droplet-IMAP read of Authentication-Results: **dkim=pass header.i=@coldrig.dev + spf=pass + dmarc=pass (p=REJECT)** — ALIGNMENT CLEARED, the B1 silent-hard-reject risk is dead; sender stays login@coldrig.dev. **BUT placement = Gmail SPAM folder** (new-domain reputation, not config; ops@ same-domain wouldn't differ) → per gate, site email-login ADVERTISING STAYS DARK; functional recovery page live (users may need spam check). Mitigations queued: [gated:founder] click "Not spam" on "Your Coldrig sign-in link" in Gmail (trains the filter, ~10s) · Google Postmaster Tools registration (founder click, low priority) · reputation accrues with real volume + time · revisit flip at arming/pilot when send history exists. Also proven en passant: OPS_EMAIL outbound path works end-to-end — dunning/watchtower mail deliverability de-risked (same channel).
- [ ] 2026-07-23 [ORDER] **SIGNUP-AUTH BUILT (5 commits `0478d21`→`b543814`) — design-adversary build review RUNNING.** All 4 increments + all 3 adversary obligations RED-proven (atomic-consume race 4/5-fail→5/5, normalization, waitUntil 3023ms→<1000ms). Migration **0009** (was genuinely free; 0011/0012 assignments for P/O lanes unaffected). Battery: platform 676/676 (+28), dashboard 106, engine 95p3s, CLI 9/9, typecheck clean. Notable: ops-mailer gained closed-enum sender ("ops"|"auth" → login@coldrig.dev); Turnstile built DI-dark (no live widget — ACTIVATION `turnstile-spin` step); increment D honest-dark (NO email-login marketing until §1.7b gate); SetupPage "17 tools"→24 fixed. Builder discovery ledgered: OPS_EMAIL binding is TRUTHY under miniflare in tests (env.ts doc comment wrong) — adversary judging the false-confidence class one layer up. §1.7b empirical outbound gate = main loop, post-deploy: real send login@→yaakovscher@gmail.com tenant, droplet-IMAP Authentication-Results dkim=pass d=coldrig.dev + dmarc=pass; funnel flip only after PASS.
- [ ] 2026-07-23 **GSC STATUS CORRECTED (founder screenshots, supersedes stale "pending" entries):** property verified + sitemap.xml SUBMITTED 2026-07-16 (last read 07-17, Success, 35 discovered pages) — the founder DID the GSC click; ledger was stale. Homepage https://coldrig.dev/ = "URL is on Google / Page is indexed" ✓. Coverage report still "Processing data" (benign new-property lag). NOT ranking for category terms = domain age/authority (11-day-old domain), not a technical block — directory lane (Glama/registry/Smithery/awesome-list) remains the fast path per traffic-channels research. REMAINING: [gated:founder] URL-Inspect → Request Indexing on ~10 money pages (pricing, 3 agent guides, 5 comparisons) · [gated:founder] Bing Webmaster Tools verification status UNKNOWN (IndexNow fired 07-19 covers submission, not console verification).
- [ ] 2026-07-23 [ORDER] **Glama Dockerfile build (founder in-dashboard, values ratified 07-23)** — Admin→Dockerfile form: Build steps `["npm ci", "npm run build -w agent-cold-email"]` · CMD `["node", "packages/cli/dist/index.js", "mcp"]` · Placeholder `{"AGENT_COLD_EMAIL_API_KEY": "glama-build-test-dummy"}` (clears the schema-mismatch red error; stdio mcp mode starts unauthenticated by design, mcp.ts:50, tools/list bridges to the public Worker endpoint) · pinned SHA: click (sync)/clear — cached 018318b is pre-ship · click Build, then Build & Release. On release: main loop verifies score via Glama public API + posts the PR #10106 reply.
- [ ] 2026-07-23 [ORDER] **PR #10106 (awesome-mcp-servers) HELD on Glama quality score** — maintainer punkpeye 07-22: score shows "?", "must have a quality score set by Glama (any grade)" before merge. VERIFIED our side 07-23: /mcp initialize responds publicly (protocol 2025-03-26) + tools/list serves 24 — introspection is NOT the blocker. Glama listing cache also STALE (shows 21 tools, 0.2.0). [gated:founder] ONE click: Glama dashboard (listing is claimed) → agent-cold-email server page → trigger re-scan/evaluation. THEN (main loop): confirm score visible → reply on PR #10106 (draft: "Glama evaluation has now run — quality score is visible on the listing. The server's MCP introspection endpoint is public if the scorer needs it: POST https://agent-cold-email-api.yaakovscher.workers.dev/mcp. Thanks!") → maintainer merges.
- [ ] 2026-07-23 [ORDER] **⛔ INTEGRATED BATTERY FAIL — DEPLOY + KEY FLIP BLOCKED; hermetic-test-env fix lane DISPATCHED.** wave-verifier on 60275b3: typecheck 5/5 clean, build clean, sanity greps pass (credpush gone; stripe-client Coldrig 2 / ColdStart 0), dashboard 99 / engine 95p3s / CLI 9/9 green — but platform 5 FAILED / 634 pass (all checkout.test.ts): main checkout's `.dev.vars` carries the real test-mode STRIPE_SECRET_KEY (wired 07-22 for local E2E) → billing.ts:40 `Boolean(env.STRIPE_SECRET_KEY)` flips checkout to real-Stripe mode under test → simulated-mode assertions fail. CLASS (not instance): test assertions gated on ambient developer-env secrets — suite result depends on machine state (fresh worktrees pass on placeholder .dev.vars, main fails). Fix lane (hard-builder, worktree test-env-hermetic-20260723): harness-layer hermetic env (constructed/allowlist pattern so NEW vars are hermetic by default — REGISTRAR_* incoming from G5 lane), full env.ts class sweep, failing-by-construction ambient-leak guard, revert-fail-restore proof w/ fake truthy key. Battery re-runs after merge; deploy stays gated. NOT a lane regression — latent since 07-22 key wiring; surfaced now because the battery finally ran in the main checkout.
- [ ] 2026-07-23 [IDEA] tools/aeo-panel: add "Coldrig" to the measured brand terms (currently measures the retired name only — will under-count citations going forward).
- [ ] 2026-07-23 [IDEA] dev-docs brand pass: SPEC/ARCHITECTURE/ACTIVATION H1 titles + SPEC.md:60 `npx coldstart` (already ledgered) still say ColdStart — internal-facing, batch on next doc touch.
- [ ] 2026-07-23 [ORDER] **BRAND SWEEP REBUILT + ADVERSARY DISPATCHED** (prior lane died with the 07-22 session, worktree `agent-a3b6ad8f…` abandoned mid-write). New branch `worktree-brand-sweep-20260723` @ `ec73d82`: full 385-hit inventory → 2 IN-class fixes (stripe-client.ts checkout product name → "Coldrig {label}" + key-prefix-derived "(test mode)" suffix; demo-seed.ts `demo-leads.coldstart.dev`→`demo-leads.coldrig.dev` — renders in demo inbox UI) + reintroduction tripwire (brand-copy-guard.test.ts, RED/GREEN revert-proven both files) + battery green (typecheck 0 ×5 workspaces; platform 601, dashboard 99, engine 74+3s, CLI 9/9). SCOPE RULINGS (main loop, from inventory): unsubscribe-token KEY_DERIVATION_LABEL stays UNCHANGED — renaming a stateless-signature domain-separation string invalidates every already-issued unsubscribe link = CAN-SPAM break; @coldstart/* workspace scope, X-Coldstart-Client header, infra/db/fixture names, frozen docs all OUT (never customer-visible or protocol-frozen). Adversary review RUNNING → merge queued behind I3+I4 per program order.
- [ ] 2026-07-23 [IDEA] SPEC.md:60 illustrative `npx coldstart …` is factually stale independent of brand (real CLI bin = `agent-cold-email`) — internal doc, fix on next SPEC touch.
- [ ] 2026-07-23 [ORDER] **SIGNUP-AUTH DESIGN ADVERSARY R1: SHIP-AFTER-FIXES — FIXES APPLIED SAME DAY** (frozen: docs/adversarial/signup-auth-design-review-2026-07-23.md). BLOCKER B1: design claimed OPS_EMAIL outbound was "end-to-end-verified" — FALSE (07-20 Gate-4 proved INBOUND forward + cron only; no outbound send_email delivery ever proven from ANY from-address) — AND missed that DMARC is live `p=reject` (design said "add p=none if absent"): a DKIM-misaligned sender = HARD-REJECT = silently-dead login links (identical-200 posture gives zero signal). Fixed in design: §1.7 claims corrected + new **§1.7b EMPIRICAL OUTBOUND GATE** (blocking build gate: deploy send path dark from login@coldrig.dev → real magic-link request to a yaakovscher@gmail.com tenant → droplet-IMAP verify Authentication-Results dkim=pass d=coldrig.dev + dmarc=pass; on fail flip to ops@ and re-gate; site funnel flips ONLY after PASS). NB folds: ctx.waitUntil on the send (timing channel), X-Coldstart-Client required on /login/consume (login-CSRF), email lowercase normalize-on-write + backfill + index (silent-login-failure). Everything else HELD on re-derivation. Design now cleared for build per program order (queued behind I3+I4 merge + brand-sweep for increments C/D; A/B unblocked at I3+I4 merge).
- [ ] 2026-07-23 [ORDER] **I3+I4 ADVERSARY R1: SHIP-AFTER-FIXES — 1 BLOCKING-before-arm, fix round DISPATCHED** (frozen: docs/adversarial/i3i4-build-review-2026-07-23.md; suites re-run by adversary: engine 95p/3s, platform 625p — match builder claims; deployed build inert until INBOXKIT_*/ENGINE_* set, so nothing wrong ships today). BLOCKER: pushRecordedMailbox stamps every push with deterministic `credpush:${tenantId}:${email}` → engine store's keyed mode rejects differing content under a reused key → rotation/re-mint UNREACHABLE; lost-response retry (the F6 at-least-once boundary) re-mints a fresh refresh token → BadRequestError → row stuck 'pending' forever; remove() doesn't clear the idempotency record so teardown can't recover. FIX (adversary-prescribed): drop the key — content-hash replay-safety already gives same-content no-op + different-content 'replaced'. Fix round also takes NB3 (parseGrants silently coerces malformed GMAIL_OAUTH_GRANTS to {} → fail loud) + NB2 (DELETE/revoke path has zero non-test callers — wire best-effort token revoke into cancel/teardown, never blocking cancel). Re-verify by the SAME adversary (alive this session) → merge → G5 micro-lane. UNVERIFIABLE carried to arming: all InboxKit/engine wire shapes are self-labelled guesses — resolve at first live mailbox; gate-b limit:1 ranking vendor-dependent.
- [ ] 2026-07-23 [ORDER] **GA-GATES DESIGN ADVERSARY R1: SHIP-AFTER-FIXES — FIXES APPLIED SAME DAY** (frozen: docs/adversarial/ga-gates-design-review-2026-07-23.md; session relaunched all 4 dead lanes after the 07-22 session death). B1 → design gains **§G5 gate (a)** (registrarConfig decoupling + Cloudflare RealDomainPort replacing the Porkbun stub + domain.buy fail-closed hard-block; **micro-lane at I3/I4 merge — BLOCKS arming**; supersedes "in the GA-gates wave" option in the program entry). B2 → realSendPathLive corrected to `engineConfig && inboxKitConfig` + G3 guard extended (engine-armed/InboxKit-unbound must read pending, not active). 4 NB dispositions folded into the design (re-screen-on-brand-rewrite, stale-reserve reaper, review-queue match context, OFAC honesty statement). Design now cleared for build per program order. Also 07-23: Stripe live account re-verified charges_enabled TRUE + payouts TRUE + details_submitted TRUE (the flip precondition that was propagation-pending). ⚠️ watchdog cron re-arm DENIED twice by the auto-mode classifier (CronCreate blocked) — no cross-gap resume net this session; founder can allow the tool or re-arm from a session where it passes.
- [ ] 2026-07-22 [ORDER] **SIGNUP-AUTH DESIGN LANDED** (docs/research/human-signup-magic-link-design-2026-07-22.md). KEY REFRAME: human web signup + connect-your-agent onboarding ALREADY EXIST (apps/dashboard SignupPage.tsx → POST /signup → token-shown-once → session → SetupPage.tsx w/ MCP snippets) — net-new = MAGIC-LINK login replacing the dead-end RecoveryPage. Increments: A magic-link backend (M, login_links table + POST /login + /login/consume, enumeration-safe, terminates in the EXISTING session mint via shared mintDashboardSession refactor) · B SPA verify page (S-M, ‖ A) · C Turnstile on /login ONLY — never /signup, agent-drivable preserved (S; ⚠️ sequence after I3+I4's env.ts change; ⚠️ R3-1 test stays scoped to VENDOR-ARMING fields so TURNSTILE_SECRET doesn't trip it) · D one-funnel site IA/copy (M; collides ENTIRELY with brand-sweep on site/*.html + stale "17 tools" strings in SetupPage.tsx — lands AFTER brand-sweep, then re-pass). Email channel VERIFIED: OPS_EMAIL is unrestricted by design (dunning precedent, wrangler.toml:48-52), send from login@coldrig.dev. RECOMMENDED-ADOPTED under the autonomy grant (founder may override): magic-link = access factor for ALL tenants incl. paid; multi-tenant-per-email = PICKER. Design adversary DISPATCHED 07-22 (auth surface = attack before build); build queued per program.
- [ ] 2026-07-22 [ORDER] **AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED** (founder verbatim: "Stripe verified the account. Keep working autnomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere"). Stripe verified live same hour (charges_enabled TRUE, payouts TRUE, support_email set). PROGRAM (each wave: build → fresh adversary → merge → battery → deploy, per project law): (1) **DONE 2026-07-23** brand-sweep lane → adversary SHIP → merged `60275b3`; (2) **DONE 2026-07-23** I3+I4 lane → adversary r2 SHIP → merged `5b151e7` → deployed Worker `3f7acce1-bfb6-4c46-8e88-62ec4ced9fce` → LIVE Stripe keys flipped from Keychain + live session creation verified (livemode:true, line-item "Coldrig Launch", no charge completed); (3) **IN FLIGHT** signup-auth build (worktree `signup-auth-20260723`; design adversary-cleared `docs/adversarial/signup-auth-design-review-2026-07-23.md`) → adversary → merge/deploy; (4) GA-GATES wave (design adversary-cleared `docs/adversarial/ga-gates-design-review-2026-07-23.md`; G5 gate(a) micro-lane already merged `91f8227`): real OFAC v1 (SDN-list screening at activation + review-queue-on-hit via watchtower alerts — no paid provider), absolute vendor-spend ceiling (founder-tunable, conservative default), pending-activation state (paid-but-unprovisioned tenants see it explicitly), slot-capacity handling (auto-upgrade under the ceiling, not waitlist — "ready for all customers"); (5) final coordinated claim-surface/info pass everywhere + IndexNow. ARMING AUTHORIZATION 2026-07-22 (founder: "can I arm it now already so Mordy can sign up even when I'm not at the comp?"): founder-present authorization GRANTED for the arming steps — main loop executes them AUTONOMOUSLY once I3+I4 clears+deploys (wrangler secrets from Keychain, droplet/tunnel ops via SSH, gate-e throwaway-mailbox verify on existing slots); only a browser-consent click (if the programmatic OAuth path fails at first mailbox) would be queued back to the founder. NOTE: signup+sandbox need NO arming (work today); payment goes live at the Stripe key flip; arming affects real provisioning/sending only. ADDED TO PROGRAM: gate (a) separate domain-port arming flag (registrarConfig decoupling, S) is still UNBUILT and now BLOCKS credential-push activation — build it in the GA-gates wave (or a micro-lane at I3 merge). Watchdog cron ARMED 07-22: every 3h at :41, session-only, 7-day expiry (resumes the program across usage limits). NEW ARMING SECRETS introduced by the I3+I4 lane (set at arming, all inert alone): INBOXKIT_API_KEY + INBOXKIT_WORKSPACE_ID (spend-arming — extend isRealSpendArmed coverage test enforces) + GMAIL_OAUTH_GRANTS (manual-mint path, non-spend).
- [ ] 2026-07-22 [ORDER] **HUMAN SIGNUP + MAGIC-LINK LOGIN + ONE-FUNNEL RULED** (founder: "do magic link with email", "why not allow human signup also", "even for an agent, flow can be just 'free sign up' and standard is sandbox till upgraded"). Scope: /login magic-link (email → single-use hashed short-expiry link → existing dashboard-session mint; enumeration-safe, rate-limited), human /signup web form (reuses POST /signup; token-shown-once + auto dashboard session + connect-your-agent onboarding; bot protection eval), site one-funnel "Free sign up" CTA (additive claim-surface pass). Sandbox-by-default preserved — activation gate untouched, GA gates unaffected (sandbox signups = zero spend). Design lane DISPATCHED 07-22 (opus) → docs/research/human-signup-magic-link-design-2026-07-22.md; adversary on design, then build QUEUED BEHIND I3+I4 + brand-sweep (site/ + copy collision avoidance). Email path candidate = the unrestricted OPS_EMAIL Send Email binding (verify customer-facing suitability in design).
- [ ] 2026-07-22 [ORDER] **MORDY PILOT PRICE = COST (founder: "We're going to drop Mordy down to cost price for us")** — supersedes the 100%-off comp framing. Cost math (from ledger): 2 InboxKit slots ≈ $7.80/mo allocated ($39/10 slots) + warmup add-on $6/mo (2 × $3, while warming) + infra slice ≈ **$14-15/mo true cost**; **RULED 2026-07-22 (rev 2, same day): 60% OFF** (founder — "we paid 39/mo? so lets make it 60% off") → Mordy pays **$39.60/mo**, which alone covers the full $39/10-slot InboxKit subscription; supersedes the 65% ruling minutes earlier on the $99 launch plan; percent-off rides the subscription invoice so future per-mailbox charges inherit it; cost-covering on both marginal (warmup-only ~$15 at 5 boxes; slots pre-paid in the $39/10-slot plan) and allocated (~$34.50) accounting. Implementation: single-use percent_off=65 coupon, no expiry, minted in LIVE mode at the wiring pass (Stripe-Version pin gotcha). Supersedes the earlier $15/85% sketch (that was pinned to his 2-box usage; founder correctly flagged the scale case). F2 containment unchanged: single-use + launch-plan-restricted + promo entry code-gated to launch sessions. Live coupon minted at KYC-approval time (Stripe-Version pin gotcha applies). Test promo MORDYPILOTTEST (100%) already consumed by the E2E — superseded either way.
- [ ] 2026-07-22 [ORDER] **"ColdStart" stale-brand CLASS SWEEP dispatched** (founder: "make sure this gets fixed") — confirmed instance = Stripe checkout product name "ColdStart Launch (test mode)" (stripe-client.ts:89); class = retired working-name on ANY customer-visible surface; builder inventories repo-wide, fixes IN-class, derives the test-mode suffix from key prefix (live customers see "Coldrig Launch"), adds a reintroduction tripwire test. Merges + deploys with the next Worker batch (before Mordy onboards).
- [ ] 2026-07-22 [ORDER] **STRIPE TEST-MODE BILLING WIRED + E2E PROVEN LIVE** (founder created the Stripe account same day; test keys wired by main loop): Worker secrets STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET set; webhook endpoint `we_1TvzMVIb6OQwXi7wi354H35z` (6 events) created via API; .dev.vars updated. LIVE E2E EVIDENCE: /checkout/simulate → 404 (F1 guard firing in prod); signup → POST /checkout → checkout.stripe.com session; headless-browser completion with single-use 100%-off promo `MORDYPILOTTEST` (coupon z5xBGWra) → "$0.00 / 100% off" shown, NO card requested (payment_method_collection if_required working) → redirect to /checkout/success → webhook flipped tenant ten_35b69b91 to plan:launch billingState:active. Screenshots + browser script rescued to archive/2026-07-22-golive-program/scratch-rescue/promo-e2e/. Test promo now CONSUMED (max_redemptions=1) — mint fresh per run. OPEN TAILS: (a) [ORDER] stripe-client.ts product_data name reads "ColdStart {label} (test mode)" — customer-visible STALE BRAND on the checkout page, fix to Coldrig + mode-derived suffix, ride the next platform deploy; (b) [gated:founder] Stripe Settings → Public details: public business name → Coldrig, statement descriptor → COLDRIG (checkout header currently shows "Epiphany Made sandbox"); (c) gotcha: POST /v1/promotion_codes rejects `coupon` param on the account's default API version — pin `Stripe-Version: 2024-06-20` (or use dashboard) when minting the LIVE coupon; (d) live-mode repeat after KYC approval: live keys via terminal (never chat), live webhook endpoint, live single-use coupon.
- [ ] 2026-07-22 [ORDER] **I3+I4 BUILD LANE DISPATCHED** (founder: "you do what you can and I'll do stripe now"): one hard-builder arc in an isolated worktree — I3 credential path (engine authed POST /v1/mailboxes + F4 idempotency + F5 loud-corrupt-store + F6 record-before-push ordering; Worker INBOXKIT_API_KEY/WORKSPACE_ID bindings + provisioning push; isRealSpendArmed InboxKit leg + R3-1 failing-by-construction guard test; OAuth mint seam manual+programmatic-dark) + I4 gates b/c/d (exact-email-before-cancel, withRequestIdempotency provision, vendor-metric display-honesty). NO live vendor calls (fixtures; spend endpoints forbidden). Adversary re-attack follows the build. **Stripe KYC IN PROGRESS (founder, same day)** — next after: live-key wiring session + single-use 100%-off launch coupon.
- [ ] 2026-07-22 [gated:founder] mcp-publisher republish for 0.2.2 — official registry still serves 0.2.1 with the STALE "21 MCP tools" description (same one-command re-run as yesterday: cd ~/dev/coldstart && mcp-publisher publish; auth may need mcp-publisher login github first).
- [ ] 2026-07-22 [ORDER] **openapi.yaml nullable-class cleanup (whole class, not piecemeal — wave-gate round-2 ruling):** file declares openapi 3.1.0 but uses 3.0-ism `nullable: true` in 21 places (18 pre-existing + 3 new lead-schema hits matching house style) → redocly struct-rule red. Fix = pin `openapi: 3.0.3` (all 21 valid instantly) OR convert all 21 to `type: [x, 'null']`. Separate pass; not deploy-blocking.
- [ ] 2026-07-22 [IDEA] guide-mcp-tool-count.html: add the BYO row (get_byo_domains/configure_byo_domain absent from the table — pre-existing gap, page headlines 24/surfaces 22; hedged copy so not false).
- [ ] 2026-07-21 **Founder-responses batch (afternoon):** (1) Stripe account+KYC = founder-identity-only (no API path for standalone account creation; fill-sheet delivered in-chat) · (2) Mordy registrar/CF-access founder-click RETIRED — flows through product BYO intake at his signup ("he's going to onboard like a reg client") · (3) RESOLVED same day: first re-run had hit an expired Registry JWT (401) — after `mcp-publisher login github` + publish, registry API verified serving **0.2.1-latest** with the 96-char description · (4) external indexing audit re-run: sitemap VALID 42 URLs, robots permissive + sitemap-linked, no AI-crawler blocks; wall remains pure index absence; console-side clicks unreachable from here (offered headed-Playwright Google-profile setup so the main loop can drive GSC) · (6) InboxKit key persistence DECIDED: macOS Keychain (`security add-generic-password -U -a coldrig -s inboxkit-api-key`) — founder pastes once in HIS terminal, key stays out of transcripts; arming also sets Worker secret INBOXKIT_API_KEY — key STORED + LIVE-VERIFIED 2026-07-21 (Keychain read → read-only InboxKit availability call, HTTP 200) · (7) Cline test [gated:founder-hold] · (8) external-prober clarified: 07-20 armed the INTERNAL watchtower; the open item = adding /status to founder's OWN uptime service (watchtower can't report its own death, NB-2 backstop) — founder may have already done it, awaiting which service to verify+close.
- [ ] 2026-07-21 [ORDER] **Self-serve activation DESIGN LANDED** (docs/research/self-serve-activation-design-2026-07-21.md; every gap file:line-cited). Increments: I1 product-driven activation gate replacing manual ENGINE_TENANTS (M, foundational) · I2 checkout promo codes w/ allow_promotion_codes + payment_method_collection if_required so a 100%-off code needs no card (S) · I3 managed-mailbox credential path — engine POST /v1/mailboxes + durable store, Worker provisioning push, programmatic gmail_api OAuth mint w/ manual-mint fallback (L, THE LONG POLE: InboxKit client-id-request flow unverified live) · I4 InboxKit arming gates b/c/d as code (M; gate a = separate S flag item, gate e = arming-time verification) · I5 OFAC screening NET-NEW (M; ACTIVATION.md:93 "hook is built" was FALSE — corrected 2026-07-21) · I6 metered $10/mbx migration (L, deferred; pilot ships flat $99 + promo code). Credential architecture ratified in-design: PUSH-to-droplet (secrets stay on the firewalled droplet; internet-facing Worker never stores refresh tokens). Critical path to Mordy self-serve: I1 → I3(manual-mint fallback OK for one pilot) + I2 + one-time founder arming (Tunnel/ENGINE_BASE_URL/secrets, Stripe KYC, InboxKit key). Registrar question in the doc = already resolved by the 2026-07-21 registrar ruling (Cloudflare default; InboxKit per-tenant opt-in behind own flag). ADVERSARY VERDICT 2026-07-21: **SHIP-after-fixes** (docs/adversarial/selfserve-activation-design-review-2026-07-21.md, grounded at 09e73ff; both panel-03 fail-opens verified closed in current code). BLOCKING→build-scope: F1 unauthenticated GET /checkout/simulate writes billing_state='active' with no STRIPE_SECRET_KEY gate (routes/checkout.ts:24-34, billing.ts:78-119) — inert today, becomes real-spend fail-open under I1; MUST close in I1 w/ RED test. F2 promo code MUST be max_redemptions=1 + plan-restricted ("plan quota = spend ceiling" is FALSE: no tenant-count cap, per-tenant ceiling is 60 mbx not 5). Non-blocking folded: F3 replyToThread bypasses freeze+caps → adapter-cache re-evaluation is REQUIRED (fresh SQL each buildAdapters) + warm-lead #4 must never use replyToThread; F4 cred-write replay/idempotency; F5 engine-state.json corrupt-file silent discard; F6 partial-failure ordering; F7 align §2.4(a) to the Cloudflare registrar ruling. Cross-lane: schema.ts additive-merge hazard w/ warm-lead lane (coordinate at integration); warm-lead tool numbering must be 22+ (SPEC body's 20-23 is stale). I1+I2 build DISPATCHED 2026-07-21 (worktree) with F1/F2/F3 embedded as musts. **BUILT 2026-07-21:** commit `7ef372d` on branch `worktree-agent-ae74fe3cd5cdba531` — activation gate (engine/activation.ts, fresh-SQL per buildAdapters), ENGINE_TENANTS DELETED per design, F1 simulate guard (404 when STRIPE_SECRET_KEY set + DO-level defense), F2 promo flags launch-plan-only + coupon-constraint comment; builder-reported 573/573 (81 files) + typecheck 0 + F1 RED/GREEN cp-backed proof. Notable deviations: email port needs `activated && engineConfig` (builder caught a real paid-but-unarmed regression via full suite); no screening_status column (pure stub, avoids schema collision); ACTIVATION.md:26 stale ENGINE_TENANTS reference flagged for bookkeeping; builder disclosed a mid-session `pkill vitest` that may have hit sibling lanes (adversaries re-run their own batteries). Fresh-context adversary re-attack DISPATCHED 07-21 — priority question: does the ORIGINAL F1 arming-order scenario survive the STRIPE_SECRET_KEY-presence guard? **ANSWER: NO — VERDICT SHIP-after-fixes 2026-07-21** (docs/adversarial/selfserve-i1i2-build-review-2026-07-21.md; adversary's own battery 573/573 + typecheck 0). BLOCKING: F1 NARROWED not killed — guard keys on STRIPE_SECRET_KEY presence while the threat window is its ABSENCE (engine-armed-before-Stripe-keys → simulate open → activated && engineConfig → real send/spend for $0); the lane's own checkout.test.ts:191-197 asserts the hole open. FIX ROUND DISPATCHED to the same builder: simulate 404s whenever engineConfig OR inboxKitConfig present (spend-arming signals) in addition to the Stripe guard — simulate works ONLY fully-unarmed; RED-proof the exact scenario. **FIX LANDED 2026-07-21: `dc934e9`** — shared `isRealSpendArmed(env)` (STRIPE_SECRET_KEY OR ENGINE_BASE_URL&&ENGINE_AUTH_SECRET) used by route guard + DO defense-in-depth; InboxKit has ZERO env binding today (grep-confirmed by builder) → documented as a REQUIRED guard extension the moment I3/I4 add one; RED-proven on the exact adversary scenario (200→404 assertion failed on old code), battery 574/574 (81 files) + typecheck 0. Adversary re-verify (round 3) DISPATCHED — includes ruling whether a comment suffices as the systemic guard for the InboxKit-binding extension or a failing-by-construction check is required. **ROUND-3 VERDICT 2026-07-21: CLEAN-SHIP** (addendum in the verdict doc; adversary's own battery 574/574 + typecheck 0): finding-1 dies in all 4 arming permutations, guard wired at BOTH route and DO level, new test end-to-end + genuinely fails-on-old-code, simulate confirmed the ONLY unauthenticated billing_state writer. **R3-1 REQUIREMENT ON THE I3/I4 LANE:** when INBOXKIT_API_KEY/WORKSPACE_ID env bindings land, a failing-by-construction check is REQUIRED (test asserting every vendor-arming env field in env.ts is referenced by isRealSpendArmed) — a doc comment is not a systemic guard for the spend-bypass class. Carried GA gates unchanged (pending-activation state, absolute-$ ceiling). BOTH LANES NOW SHIP-CLEAN → integration branch `integration/2026-07-21-wave` (fold both, battery ONCE, 21→24 sweep + claim-surface pass, deploy batch staged for founder word). **INTEGRATION MERGED + BATTERY PASS 2026-07-21:** both lanes merged conflict-free (warm-lead `54ac82a`, self-serve `dc65d6f`); verifier's own battery on the merged tree: typecheck 0 across all 5 workspaces, **84 files / 597 tests ALL PASS** (EXIT:0, 278s), node-resolution trap ruled out (worktree-local @coldstart/shared symlink verified), adapters-rename semantic conflict ruled out (no stale `this.adapters`, ctx-wiring confirmed at tenant-do.ts:300/342). Remaining before deploy-staging: 21→24 claim sweep (builder running) → final wave adversary gate on the COMBINED diff → founder deploy word (Worker BEFORE site). **SWEEP LANDED 2026-07-22 `453e445`** (27 files; builder hit its usage limit AFTER the work, before self-reporting — orchestrator mechanically verified the full checklist: zero stale claims tree-wide, server.json 96 chars @24, server-card 24 tools incl. the 3 new, openapi +3 lead paths, og-image regenerated '24 focused tools'). **WAVE GATE VERDICT 2026-07-22: SHIP-after-fixes, 1 BLOCKING** (docs/adversarial/wave-integration-review-2026-07-22.md): openapi.yaml's 3 lead paths $ref SIX never-defined schemas (LeadInterestStatus/LeadListPage/SuppressLeadInput/SuppressLeadResult/UpdateLeadInput/LeadDispositionView) — valid YAML, INVALID OpenAPI, breaks buyer-agent codegen on exactly the new endpoints; fix = add the 6 definitions (shapes in packages/shared/src/leads.ts + routes/leads.ts), FIX DISPATCHED 07-22, redocly re-lint = the gate. **FIX LANDED `d28afe7` + GATE CLOSED 2026-07-22: SHIP (round 2)** — adversary re-ran redocly itself (0 unresolved refs), shape-checked all 6 vs code serialization (EXACT, incl. suppressed:true literal + Provenance enum), ruled the 3 new nullable-hits acceptable house-style. Wave CLEAR TO DEPLOY-STAGE. Everything else HELD: count-vs-enumeration consistent at 24 everywhere, zero stale counts on live surfaces, tool descriptions faithful, concierge caveat preserved, spend-guard independently re-verified, no merge dross. Non-blocking: guide-mcp-tool-count table omits the 2 BYO tools (PRE-EXISTING gap, optional row later); dead `followups` table = increment #4's spec'd data model shipped schema-only (adversary-#1-verified zero-writers) — KEEPING (stripping would reopen the shipped lane; #4 lands into it), noted as accepted deviation from YAGNI. Non-blocking, ledgered as PRE-PUBLIC-GA GATES: (2) paid-but-engine-unarmed tenant silently gets sandbox sends (confident-wrong; needs an explicit pending-activation state before GA — fine for the pilot where founder arms first); (3) absolute-$ owner ceiling still unbuilt + max_redemptions is Stripe-dashboard-side (documented at stripe-client.ts:56-71). Attacks held: gate formula verbatim, F3 fresh-SQL both directions, freeze/dispute/cancel/past_due flip gate off, cache tenant-scoped, ENGINE_TENANTS straggler-free. ⛔ STANDING: never arm the engine before Stripe live keys until the F1 fix lands. Merge HELD pending clean re-verify + integration with warm-lead branch (tenant-do.ts touched by both). **FOUNDER RULED YES 2026-07-21** ("yes you can run the pilot"): pilot ships with OFAC stubbed-clear; containment = the F2 promo constraints (max_redemptions=1, launch-plan-restricted); real OFAC (I5) + an absolute-$ ceiling REQUIRED before PUBLIC self-serve GA — that gate stays on the ledger. **DEPLOYED 2026-07-22:** merged to main `8a30ec0` (I1+I2, w/ F1 spend-guard fix `dc934e9`, integrated with the warm-lead lane + 21→24 claim sweep `453e445` + openapi schema fix `d28afe7`), gated by 4 adversary verdicts + integration battery 597/597 (84 files, typecheck 0 ×5 workspaces). Worker DEPLOYED version `1cb56729-91af-45ba-8e02-13de2db8624d` (live-verified `/status` ok, unauth tools/list = 24); site DEPLOYED after Worker, Pages deployment `25844cc6` (live-verified 24-tool server-card + description). Prod behavior: nothing armed — engine/Stripe/InboxKit all unbound, every tenant sandbox; `ENGINE_TENANTS` mechanism RETIRED in prod per design, activation now product-driven (paid+active+unfrozen+screening-stub); `/checkout/simulate` guarded by `isRealSpendArmed`. I3 (managed-mailbox credential path, the long pole) + I4 (InboxKit arming gates) + I5 (real OFAC) + the absolute-$ ceiling remain UNBUILT — pilot arming session still needs the founder present.
- [ ] 2026-07-21 [ORDER] **Review-sites + dogfood DECIDED (founder-delegated choice; evidence docs/research/reviewsites-dogfood-2026-07-21.md).** REVIEW SITES — list NOW on free tiers: G2 + Capterra + AlternativeTo + a free TrustRadius profile. Basis: presence is a near-hard filter for AI-agent citation (Quoleady 2026: 99-100% of ChatGPT-cited tools had G2/Capterra listings; review COUNT barely correlates), and Capterra's own policy tolerates beta products at 1 review/year — thin-at-launch is not penalized. NO paid tiers (TrustRadius ~$30k/yr rejected), NO TrustRadius incentivized-review program, NO manufactured/incentivized reviews anywhere. Product Hunt DEFERRED to a real milestone (self-serve live or first real customer) — a weak launch is uniquely visible and permanently indexed. First review ASK at N=1: Mordy, after one real completed send cycle. FOUNDER-HANDS: creating the 4 vendor listings (needs a coldrig.dev business email — add a CF Email Routing address — and his identity for claiming); a claim-class-compliant listing copy pack gets drafted before his clicks. DOGFOOD — ratified: gated behind the SAME arming wave as Mordy's pilot PLUS one clean pilot send cycle; own dedicated tenant + coldrig-branded domain (NOT Mordy's; InboxKit 10-slot plan has headroom); 5-8 sends/day staggered across the warmup ramp (never a 28-blast); Tier C (AI/MCP writers) → Tier A (cold-email creators), Tier B skip, competitor blogs excluded, Jack Clark/Import AI gated on explicit founder sign-off; success = replies + engaged "show me" responses + unprompted public mentions + zero complaints (explicitly NOT demos/signups); rides the REAL product send path (suppression/unsubscribe live) — never a side-channel send; dogfood targets are press, not review candidates.
- [ ] 2026-07-21 [ORDER] **Warm-lead Q1-Q6 RULED — build UNBLOCKED.** Founder ratified all six per docs/research/warm-lead-q1-q6-recommendations-2026-07-21.md: Q1 tenant-wide (tenant,email) disposition + tenant-wide suppress · Q2 enum+tags hybrid WIDENED with out_of_office + wrong_person; "do not contact" routes to suppress_lead, never an enum member · Q3 same-thread reply through a NEW shared guarded single-send primitive (caps + warmup ramp + deliv-pause + suppression re-check; adversary R1/R2 binding — never replyToThread as-is) · Q4 auto-classification DEFERRED (customer's agent classifies better — differentiation, not a gap) · Q5 retention TTL DEFERRED to an explicit pre-GA compliance gate; founder 07-21: NO EU/UK recipients in the pilot · Q6 JSON-only list_leads export, CSV on demand. Build increments #1-#3 (schema, 3 tools, unsubscribe webhook closure) dispatched to a spec-builder in an isolated worktree 07-21; #4 (schedule_followup + the guarded primitive) follows separately. **BUILT 2026-07-21:** 3 commits on branch `worktree-agent-ab4beea4ab3f6cced` (`9b98909` schema+tools, `7ce59dc` webhook closure, `a8c728d` memory), builder-reported 597/597 across 83 files + typecheck 0 + RED-proof on the choke-point routing; enum = 8 members (base six + out_of_office + wrong_person, do_not_contact routed to suppress); 7 deviations documented in the task report (note-not-persisted + reason-manual-only + per-campaign-row list shape = adversary judgment items). Adversary re-attack DISPATCHED 07-21 (fresh context, runs its own battery). **ADVERSARY VERDICT 2026-07-21: SHIP** (docs/adversarial/warm-lead-build-review-2026-07-21.md; own battery 83 files/597 tests ALL PASS + workspaces typecheck 0; every attack held — isolation, R3-complaint-protection verified-inert, choke-point extraction logic-identical, enum enforced both transports, followups verified zero-writers). Non-blocking notes: (1) WAVE DEPLOY GATE — 21→24 sweep must cover site/guide-mcp-tool-count.html + server.json + site/for-agents.html + README + full claim-surface re-pass at the coordinated deploy; (2) multi-campaign opt-out fans out one webhook delivery per campaign-lead (defensible; founder taste ruling optional); (3) comment-only R2→R3 cite fix. Merge HELD for the coordinated 21→24 pass + integration with the self-serve lane (its adversary still running). ⚠️ New tools lift the MCP surface 21→24/25 — the public count bump is a COORDINATED merge+deploy pass (F-wave pattern), NOT part of the build lane; lane builds dark on branch. **DEPLOYED 2026-07-22:** merged to main `8a30ec0` alongside the self-serve I1+I2 lane (`453e445` 21→24 claim sweep + `d28afe7` openapi schema fix), integration battery 597/597 (84 files, typecheck 0 ×5 workspaces). Worker DEPLOYED version `1cb56729-91af-45ba-8e02-13de2db8624d` (live-verified unauth tools/list = 24 incl. suppress_lead/update_lead/list_leads); site DEPLOYED after Worker, Pages deployment `25844cc6` (live-verified 24-tool server-card + "24 intent-level MCP/HTTP tools" on llms.txt). **CORRECTION 2026-08-02:** the guarded single-send primitive is now BUILT + DEPLOYED — `apps/platform/src/engine/guarded-send.ts` (140 lines, caps/warmup/deliv-pause/suppression re-check + structured 429/409), shipped as part of the warmup-claims wave, commit `e4250e9`, adversary R2 SHIP (`docs/adversarial/warmup-wave-final-review-2026-08-02.md`), wired into the reply path (`engine/threads.ts`). `schedule_followup` (the tool + `followups` table + tick drain) still remains UNBUILT and must reuse this primitive when built — do not write a second single-send path.
- [ ] 2026-07-21 [ORDER] **Registrar DECIDED (delegated choice): Cloudflare Registrar is the default for fresh sending-domain buys** (.com $10.44/yr at-cost; purchase API verified working, beta; renewals/transfers dashboard-only = acceptable once-a-year founder click). Namecheap = documented fallback (proven — Smartlead buys via it). Porkbun DROPPED (no purchase API). InboxKit-as-registrar = per-tenant explicit opt-in ONLY (registrarConfig.kind==="inboxkit"), for the connect-existing-domain shape — never a default, never inherited from the mailbox credential. Arming-flag policy: domain port gets its own registrarConfig distinct from inboxKitConfig in factory.ts (closes 2026-07-20 arming-gate finding a). NEW BUILD ITEM surfaced: apps/platform/src/vendors/real/domain-port.ts:4 is stubbed against Porkbun (already ruled out by SPEC §13) — needs a rewrite against Cloudflare Registrar; folds into the self-serve activation build. Evidence: docs/research/registrar-choice-2026-07-21.md.
- [ ] 2026-07-21 [gated:founder] **InboxKit prewarm — research landed** (docs/research/inboxkit-prewarm-2026-07-21.md): prewarm = buying pre-aged domain+mailbox bundles from InboxKit's OWN inventory ($6/$7/$9 per mbx by 2-4/4-8/8+wk warmed age + ~$15 domain transfer; keeps auto-warming ≤12wk); canNOT be applied to a domain we already own (no backdating warmth) — authorpitchdesk.com runs the real ~2-4wk clock regardless. PENDING FOUNDER GO: (a) pilot buy of ONE prewarmed domain + 2 mailboxes ≈ $27-29 (real vendor spend + needs his InboxKit key re-paste + wallet credits) so Mordy sends before his ~07-28 Instantly renewal — this also settles the deferred "1-2 lookalike spreads" question (answer: one, second only if volume proves insufficient); (b) "Instant Start" SKU for all customers — recommended YES as optional add-on ~$20-30/mbx (2.6-3x on wholesale), never default, 4 honesty disclosures mandatory; gate: credits→USD rate is UNDOCUMENTED — verify via small top-up before any customer-facing SKU price. **SUPERSEDED-IN-PART 2026-07-21 (founder fact-correction: "He doesn't have instantly yet he's waiting on us and can warm it on us")** — Mordy has NO active Instantly; the ~07-28 bridge deadline premise is STALE → the pilot prewarm buy (a) is WITHDRAWN, buy nothing; his domain warms on-platform after connect (native ramp ± the $3/mbx warmup add-on). Also clarified for the founder: a prewarmed LOOKALIKE of his domain is impossible by construction — warmth is accumulated sending history on a specific domain, and a freshly-registered similar name has none; prewarmed inventory = InboxKit's own aged generic names only. Prewarm survives solely as the future Instant Start SKU for deadline-pressed customers [still gated on the credits→USD verify].
- [ ] 2026-07-21 [ORDER] **SELF-SERVE ACTIVATION** (founder 07-21: "why not just set it up so he can do it himself like any customer we want to") — kill the per-customer concierge step. Gaps to close: one-time Tunnel/`ENGINE_BASE_URL` arming; replace the manual `ENGINE_TENANTS` allowlist with product activation logic (billing-active + existing freeze-states/quotas + D4 OFAC placement); arm InboxKit adapters (gates a-e, 2026-07-20 entry); dynamic per-tenant mailbox-credential plumbing engine-side (today: static `/root/mailboxes.json` on the droplet); §20.6 OAuth connect for the BYO-mailbox shape (NOT needed for Mordy's managed shape). Design lane dispatched 07-21 (opus) → `docs/research/self-serve-activation-design-2026-07-21.md`; build gated on that spec + adversary.
- [ ] 2026-07-21 [ORDER] **BILLING-FIRST PILOT** (founder 07-21: "I'd rather do billing and give him a code") — supersedes the comped-outside-billing framing: Stripe live keys (KYC = founder blocker) + promotion-code support in checkout + founder mints a 100%-off code for Mordy; checkout E2E becomes part of the pilot test. Flat $99 plan + code first; the metered $10/mbx quantity migration can follow.
- [ ] 2026-07-21 [FACT-CORRECTION] **OAuth consent screen is IN PRODUCTION** (founder read the console 07-21: "it says it's in production (It was before also)") — the ledger's "Testing status → refresh token dies ~7 days (next ~07-26)" claim is WRONG (it was never independently checkable from here). In-production apps' refresh tokens do not auto-expire weekly; the 07-19 re-mint was for the scope addition + a transit-corrupted secret, not expiry. VERIFY: if the 07-19-minted token still sends after 2026-07-26, close the weekly-re-mint chore for good and strike it from HANDOFF/queues.
- [ ] 2026-07-21 [ORDER] **server.json description trimmed to the registry's 100-char cap** (founder's `mcp-publisher publish` hit 422 "expected length <= 100"; old string was 199) — 96-char form keeps the 21-tools/live-sending/sandbox/$99/concierge claims; founder re-runs `mcp-publisher publish` (already authed — the 422 was post-auth validation).
- [ ] 2026-07-21 **Indexation re-checked from outside (founder: "you check"):** Google web search = coldrig absent; DuckDuckGo (Bing-fed) `site:coldrig.dev` = ZERO results; Bing direct = captcha-blocked. Index absence persists on BOTH engines despite the 07-19 IndexNow 200. Remaining founder accelerators unchanged: GSC sitemap listed-with-Success check + URL-Inspection Request-Indexing batch; Bing Webmaster direct sitemap submit.
- [ ] 2026-07-21 [ORDER] **Mordy onboards via the REGULAR self-serve flow** (founder 07-21: "I want mordy to sign up through regular coldrig to test the end to end process") — not concierge. He follows coldrig.dev (Claude Code guide) → signup (isolated sandbox tenant) → campaign setup. True-E2E gaps to arm before his tenant goes REAL: Cloudflare Tunnel + `ENGINE_BASE_URL` + `ENGINE_TENANTS`, InboxKit key re-paste (session-local, gone), InboxKit adapter arming gates (a)-(e) in the 2026-07-20 Mordy-pilot entry. Billing = comped pilot (Stripe live keys not wired).
- [ ] 2026-07-21 [ORDER] **authorpitchdesk.com fact-correction (live DNS probe 07-21):** zone has ZERO records — no MX/SPF/DMARC/A — and already sits on Cloudflare NS (noah/angelina.ns.cloudflare.com); founder: "he just owns the domain." SPEC §20.6's "runs Mordy's live GWS mailboxes" premise is WRONG vs live DNS → reclassify §20.6 at next SPEC touch (fresh/bare, not records-to-apply). Consequence: InboxKit validateDNS is UNBLOCKED; CF-API connect path viable — remaining ask = a CF token (Zone:Read+DNS:Edit) from whichever CF account holds the zone (or NS delegation).
- [ ] 2026-07-21 [ORDER] **Research lanes dispatched (founder 07-21):** (a) warm-lead Q1-Q6 user-POV recommendations [opus]; (b) InboxKit prewarm mechanics + offer-to-all-customers + lookalike-spread rec [sonnet]; (c) registrar choice — founder DELEGATED the choice post-research [sonnet]; (d) review-site listings + dogfood-calls scope — founder DELEGATED the choice post-research [sonnet]. Outputs → `docs/research/*-2026-07-21.md`; recommendations back to founder for ratification where product-visible.
- [ ] 2026-07-20 [ORDER] **Mordy pilot — InboxKit runway (state + runbook).** Purchased 07-20: InboxKit Professional monthly $39 (renews 08-20), **10 Google mailbox slots**, wallet 0 credits; API live (workspace "Starter" `c5188ced-33db-436f-b970-1860e6c8c66b`; auth Bearer JWT + X-Workspace-Id). **FOUNDER 07-20: `authorpitchdesk.com` IS the sending domain** — Mordy doesn't need it for landing/regular contact, so it rides the pilot directly (no lookalike purchase required to start; optional 1-2 lookalike spreads = later founder call). Connect paths (both doc-verified): (a) NS delegation — POST /v1/api/domains/nameservers returns assigned Cloudflare nameservers, Mordy sets them at his registrar, then check-nameservers-propagation; (b) CF-API connect — POST /v1/api/cloudflare-domains/connect with a CF token (Zone:Read+DNS:Edit) from whichever CF account holds the zone; validateDNS BLOCKS if the zone has ANY existing mail records (MX/SPF/DKIM/DMARC/autodiscover) — check before attempting. **⛔ Gate: needs Mordy's registrar/CF access — founder coordinates.** Then: 2 mailboxes from slots → GET show-mailbox-credentials (full smtp+imap creds) → gmail_api/443 smoke from the droplet = Gate-1 ON the pilot domain, and its warmup clock starts (the 07-28 Instantly answer). Priced/mapped this session: their warmup add-on = $3/mbx/mo (GET /warmup/pricing); prewarmed mailboxes exist (POST /prewarm/buy-domain, credits; doc example 3 mbx/2 domains = 24 credits) — candidate warmup-clock compressor, founder call; **OAuth fleet path candidate**: POST /mailboxes/client-id-request/initiate registers OUR existing Google OAuth client ID onto the provisioned Workspace domain + initiate-consent-request grants consent — likely programmatic refresh-token path for gmail_api at fleet scale (verify empirically at first mailbox; fallback = manual mint, proven 07-19). InboxKit adapter build lane DISPATCHED 07-20 (dark, fixtures, spend-endpoints forbidden). **Adapter lane COMMITTED 07-20 (`51f5015`)** — RealMailboxPort rewrite + RealInboxKitDomainPort + InboxKitClient + error mapping, 40 new tests, dark (unreachable per `factory.ts`, both call-site literal and missing config), adversary re-attack SHIP (574/574, 80 files, typecheck 0; frozen record `docs/adversarial/inboxkit-adapters-2026-07-20.md`). **REQUIRED-BEFORE-ARMING (all non-blocking for the dark merge, cited from the frozen record, do not re-derive):** (a) domain port is welded to the same `inboxKitConfig` credential as the mailbox port (`factory.ts:142`) — an armer supplying the mailbox credential silently co-arms InboxKit as registrar, contradicting ACTIVATION.md:9's mailbox-scoped ruling vs ACTIVATION.md:25's still-Namecheap/Porkbun registrar line; needs a separate explicit domain-port arming flag + FOUNDER reconciliation of the two ACTIVATION lines; (b) `resolveMailboxUid` trusts `mailboxes[0]` from a keyword search with no exact-email assertion before the destructive `/mailboxes/cancel` call — add an exact-match check before cancel; (c) `provision()` has no vendor idempotency key (double-charge retry window) + a fragile `/already exists/i` message-substring match — bring to the repo's own `withRequestIdempotency` standard; (d) vendor `reputationScore`/`placementRate` reach the customer-facing `infrastructure` tool + dashboard DISPLAY as if real (decisions already use local signals only) — display-honesty fix needed; (e) `/mailboxes/list` keyword exact-vs-fuzzy semantics is UNVERIFIABLE without a live POST — resolve with a throwaway mailbox at arming time (gates finding b).
- [ ] 2026-07-20 [ORDER] **FOUNDER RULING ("go inboxkit (fix this everywhere)"): InboxKit replaces Mailforge as the decided mailbox vendor for the Mordy pilot + first validation buy.** Basis (pre-spend research, all cited in the vendor-battery entry below + bookkeeper-propagated to ACTIVATION Gate-0): (a) Mailforge's CURRENT ToS prohibits direct resale ("not entitled to... assign the use of all or part of the Services to your own customers, even for free"); permitted shape = agency/indirect-subscriber with MANDATORY per-client sub-accounts; cancellation hostile (≥7-day notice via live chat, no refunds, full remaining term charged) — corrects the 07-12 Gate-0 premise "only vendor whose ToS explicitly permits reselling"; (b) InboxKit Professional = $31/mo 10 slots (their own pricing page), cancel-anytime effective end-of-period, REAL Google Workspace/M365 accounts with full admin access, public provisioning API (docs.inboxkit.com; Bearer + X-Workspace-Id; plaintext SMTP/IMAP credential export; bulk 10 req/min), "Connect Existing Domain" flow with Cloudflare-API full automation = Mordy's exact bare-domain case; (c) send path rides our PROD-PROVEN 443 transports (gmail_api proven 07-19; ms_graph app-only BUILT, needs its one live smoke) instead of Mailforge's unproven-from-droplet SMTP-2525. Mailforge = researched FALLBACK only. Queued consequences: InboxKit real MailboxPort/DomainPort adapters + fixture capture (build lane, post-key), Google-side DWD service-account gmail adapter IF Google mailboxes chosen at fleet scale (small build; pilot can mint per-mailbox refresh tokens manually), Google-vs-M365 mailbox choice decided at first mailbox order (not blocking the buy).
- [ ] 2026-07-20 [ORDER] **Mailforge vendor battery — empirical findings so far (test-don't-ask lane, founder mid-purchase):** (a) full provisioning API EXISTS and is spec'd at api.mailforge.ai/swagger/doc.json (base /public): domain availability/buy/DNS/autorenew/transfer/masking/forwards, mailbox buy/update/delete/bulk-forward, workspaces, api-keys; mailbox responses carry a `credentials` object with FULL smtp+imap host/port/user/pass — programmatic credential export matches our engine's mailboxes.json shape exactly; (b) API access is GATED on an active slots subscription — trial excludes it ("slots subscription is required" in-app; family-level Salesforge key authenticates on api.salesforge.ai /me+/workspaces but is rejected by api.mailforge.ai in both header styles; Mailforge needs its own in-app key post-purchase); (c) pricing: 10-slot minimum, $3/slot/mo annual or ~$60/mo monthly, domains $14/yr .com, SSL/masking +$2/domain/mo — founder advised monthly for cancelability during validation; (d) customer-provided bare domains connect via NAMESERVER DELEGATION (help-center GoDaddy/change-nameservers articles) — Mordy shape-A viable without registrar transfer, verify live at onboarding; (e) STILL OPEN empirically: real smtpHost + port-2525 reachability from the droplet (needs 1 provisioned mailbox; creation = spend, quote+confirm first), isolation shape at provisioning, current ToS resale clause re-read. Auth quirk for the adapter: raw `Authorization: <key>` header, NO Bearer prefix. (founder-supplied, out-of-instrument — NOT a CHOICE-TREND row): stale-cache kill reproduced ONE DAY post-reframe.** A shopper who searched Coldrig BY NAME ~07-20 still read pre-reframe copy ("sandbox only; real sending inactive," "17 high-level actions," "no push webhooks," "BYO only planned," "agents explicitly instructed not to recommend for production," "pricing provisional") — every one of those is off the live surfaces (live-verified this session: `tools/list`=21, agent-evaluation.md = "live in production"/webhooks/BYO live). Same run confirms 3rd consecutive zero ORGANIC surfacing (shopper's queries hit Google/MS sender requirements + incumbents only; Coldrig appeared only when named). Hardens the standing Glama re-sync click + GSC/Bing Request-Indexing orders — no new work beyond them, but treat cache PURGE/refresh (search snippets, not just Glama) as in-scope for that lane.
- [ ] 2026-07-20 [IDEA] **Email/address-verification capability gap (external buyer-run 07-20, flagged twice):** none of the 21 tools does list verification; shopper scored it the one DIY-stack advantage (Smartlead $39 Base bundles verification credits; standalone ZeroBounce 2k ≈ $39 non-expiring). Options for founder ruling: (a) zero-build — document the posture (customer's agent runs its own verifier before `launch_campaign`) in FAQ/for-agents/agent-evaluation.md; (b) verification add-on (already listed as a LATER add-on in the ratified-pricing entry); (c) built-in pre-send verification pass. At minimum do (a) with the next copy batch — "not clearly documented" is the current kill wording, and a docs line deletes it.
- [ ] 2026-07-20 [IDEA] **Portability-at-cancellation docs (external buyer-run 07-20):** shopper wants clearer domain/mailbox/data portability terms before relying on us operationally. agent-evaluation.md already honestly says domain portability "not promised until upstream transfer terms are verified" — the real answer lands with the mailbox-vendor choice at activation; interim = explicit FAQ answer (what you can export today: suppression list, threads, campaign data; what's pending: domain transfer terms). Ride the next copy batch.
- [ ] 2026-07-20 [IDEA] **EU/UK/GDPR scope (external buyer-run 07-20):** shopper listed "US-first, EU/UK processing out of scope" as a limitation. Current posture is deliberate (privacy policy scopes it out). Founder positioning decision only — no build implied; revisit when a real EU prospect appears.
- [ ] 2026-07-20 [IDEA] **Compare-page ammo from the external run's own corrected math:** fair 5-mbx DIY stack = ~$76.50/mo (5×$7 Workspace + $39 Smartlead Base + ~$2.50 amortized domains) vs our $99 all-in — a $22.50/mo premium buying setup/ops time, one interface, server-enforced guardrails, per-tenant isolation. Shopper's own words: DIY "likely more expensive once setup and ongoing operating time are counted" and Coldrig "arguably better" for run-it-for-me-end-to-end. Fold into compare-vs-diy (+ Smartlead-includes-verification-credits nuance into compare-vs-smartlead) at the next compare-pages batch, claim-class adversary before deploy.
- [ ] 2026-07-19 [ORDER] **Founder rulings batch (in-session, verbatim IDs from the decision table):** **A** = DELETE the 2¢ send-usage fee everywhere ("delete the 2 cent thing wherever it is - in all places it comes up") → remove `SEND_USAGE_FEE_CENTS` + every ledger write/test/doc that references it, whole class, no dead code; closes the ACTIVATION Gate-2 "rule the 2¢ fee" item as DELETE. **E** = "use recommendation" → the crash-after-accept-before-record double-send residual is ACCEPTED for the comped pilot ONLY; durable pre-send intent log MUST build before GA (flip the ACTIVATION Gate-2 residual (1) to founder-accepted-for-pilot). **F** = GO → byo-intake coordinated merge pass (merge + Worker deploy + 19→21 count sweep + capability copy, ONE pass; connected-mailbox PRICE stays unstated on public surfaces until ruling D). **G** = GO → buyer-panel cycle 2 (post-reframe scoreboard). **I** = founder named the public targets in-session: "submit to mcp.so and Cline". **D** = "do the same as whatever the industry does" → BYO-connected mailboxes carry NO per-mailbox fee (industry standard per frozen pricing research: Instantly/Smartlead include connected inboxes in the plan); monetization = platform fee + provisioned mailboxes; design into the quantity-billing migration (tier fair-use caps allowed); NO public connect-pricing claim until the §20.6 connect flow actually exists (it is a documented stub). (founder, in-session: "C - make the pricing work").** Design the agency shape — ONE platform fee + flat per-client-workspace add-on + per-mailbox meter — INTO the Stripe quantity-billing migration (not a separate pass). Kills the ledgered agency-scale kill-in-waiting (buyer run #3: our per-tenant model ≈ $890/mo vs winner ~$600 at 8 clients — the exact pattern the shopper rejected Instantly for; our true multi-tenant isolation is the matching strength). Numbers/design → founder ratification before public. Related open pricing ruling D (BYO-connected mailbox price vs $10 provisioned) decides before the byo-intake public merge (F), not required for this design to start.
- [ ] 2026-07-19 [ORDER] **Gmail OAuth consent screen is in Testing status → refresh tokens expire ~7 days (next expiry ~2026-07-26).** Standing operational fact surfaced during the 2026-07-19 re-mint (founder minted an upgraded token with `gmail.send`+`gmail.metadata` scopes via a session-run loopback listener) — as reported this session, not independently checkable by bookkeeper (no Google Cloud Console access from here). Re-mint before expiry (runbook `apps/engine/README.md` + `scripts/mint-gmail-token.mjs`) or publish the consent screen to Production to stop the 7-day churn; the durable fix is the §20.6 productized "Connect Google" customer-facing OAuth flow (already the design direction, not yet built).
- [ ] 2026-07-19 [IDEA] `site/status.html` Dashboard row still shows an "Early access" pill (bookkeeper-verified live) — defensible-but-optional (dashboard paid-mutation gating is real), consistent sibling wording is "Rolling out"/"Concierge step". Non-blocking per the reframe adversary's residual note (`docs/adversarial/fully-live-reframe-2026-07-19.md`); reword at next status.html touch if desired.
- [ ] 2026-07-16 [ORDER] **ChatGPT and Claude search could NOT fetch coldrig.dev even when pointed directly at it, and Google is not indexing the site.** Diagnose live (CF zone AI-bot-block / Bot Fight Mode settings, robots.txt, sitemap, GSC/Bing submission state) and FIX — this gates every findability lane already shipped. Related: `## Now` GSC/Bing item, POST-SHIP AUDIT (e) zone AI-bot-block verification. **2026-07-16 probes done** (as reported by the probing agent that session — not yet persisted as a frozen research doc on disk): `site:coldrig.dev` = 0 Google results confirmed; WebFetch CAN fetch the site directly; spoofed-UA probes all return 200; CF zone bot-management settings UNREADABLE with the current wrangler token scopes. Bookkeeper spot-check: `robots.txt` is permissive (`Allow: /`, only `/unsubscribe` + `/why-email` disallowed) and `sitemap.xml` is present/linked — so the wall is not a robots/sitemap defect. Blocked on founder clicks: CF dashboard Security→Bots allow-AI-crawlers toggle (or mint a scoped API token: Zone Settings:Edit + Bot Management:Edit), GSC domain property + sitemap submit, Bing import. **07-16 FOUNDER UPDATE: CF was ALREADY set to allow AI bots** (hypothesis dead — site was fetchable all along; direct fetches confirm) **→ revised root cause: pure index absence.** Chat assistants reach sites through their search indexes; zero Google+Bing pages = "can't access" UX. GSC domain property verified 07-15 (sitemap NOT yet submitted); Bing GSC-import done 07-15 (imported BEFORE the sitemap existed in GSC → will NOT auto-carry it). Remaining founder micro-steps: GSC→Sitemaps→submit sitemap.xml + URL-Inspection Request-Indexing on / and top pages; Bing→Sitemaps→submit the sitemap URL directly (or re-run GSC import). IndexNow key file rode the 2026-07-19 deploy and was fired post-deploy (`tools/indexnow/submit.sh` → HTTP 200 accepted, per the deploying session — script + its pre-deploy key-file abort-guard verified on disk by bookkeeper, live-fire itself not independently re-checkable without re-submitting). Expectation: Bing days, Google days-to-weeks (new domain); directory backlinks (npm/GitHub/Smithery/Glama) accelerate.
- [ ] 2026-07-16 [ORDER] **`/v1/poll` unbounded first-fetch defect (found live by the Gate-1 smoke — REAL-mailbox class) — CLOSED, committed `3d7d27d`.** `sinceCursor:0` fetched full RFC5322 source for `sinceUid+1:*` (imap.js:32) — unbounded pull, engine-OOM risk on any real mailbox with history. Fix: `-1` is the never-polled sentinel (STATUS probe, zero fetch, cursor=UIDNEXT-1); `0` and up are ordinary incremental fetches bounded by `POLL_BATCH_CAP=300`, cursor advances over the full scanned range (anti-stall past expunged UIDs); provisioning binds new mailboxes at `-1`, existing rows page history capped (no loss). **Adversary NO-SHIP** (empty-mailbox first-inbound loss + illusory greenmail-e2e priming) **→ round-2 fix → re-attack SHIP → ADDENDUM 2 re-ground SHIP after HEAD advanced mid-session** (`docs/adversarial/poll-bounded-fetch-2026-07-16.md`, 2 addenda; engine 68 passed/3 skipped, platform 407 passed/62 files, both typecheck exit 0). Carried non-blocking, both pre-existing or narrow: source-less-message cursor-advance (concurrent-expunge only, real errors throw → safe redelivery); UIDVALIDITY unhandled (present in the OLD code too, not introduced here). Also note (correct-behavior, doc it): poll only emits events for replies resolving to KNOWN send-threads — self-sends without In-Reply-To are dropped by design (classify.js:55-102). Reported: droplet IMAP 993 egress is open (reads never needed the fix) — **UNVERIFIED by bookkeeper** (no SSH/network path to test droplet egress from here; confirm before relying on it).
- [ ] 2026-07-16 [ORDER] **Become THE recommended pick inside Codex and Claude Code (founder: "whatever it takes"):** evaluate + execute connector/directory placement — Anthropic/Claude connectors & Claude Code ecosystem, OpenAI/Codex ecosystem, plus whatever placement actually drives in-agent recommendations; fold with the WIN-THE-COMPARISON program and buyer-panel cycle 2. **2026-07-16 research complete** (two agents, ranked channel table — session-only, not yet a frozen research doc on disk): Anthropic Connectors Directory is the highest-leverage channel (Claude Code docs point users there); `static_headers` beta auth reported acceptable; needs a Team/Enterprise claude.ai org (founder question OPEN) + a fully-populated demo tenant (founder runs `npx agent-cold-email demo` once, pastes the bearer token); no pre-approval email needed (automated policy scan) — as reported, not independently reverified by bookkeeper. **Directory-readiness bundle BUILT + adversary-gated** (working tree, uncommitted): all 17 MCP tools carry `title` + `readOnlyHint`/`destructiveHint` (bookkeeper-verified `apps/platform/src/mcp/tools.ts` — 12 `readOnlyHint` entries, explicit `destructiveHint` on the rest, every tool has a `title`); `.claude-plugin/plugin.json` (verified: `name: "coldrig"`, honest sandbox disclosure — "Free sandbox demo now; early access — real sending is not yet active"); `claude-code-hint` stderr marker in the CLI (verified `packages/cli/src/claude-code-hint.ts` — deliberately inert, no marketplace lookup resolves it until a `claude-plugins-official` listing exists); Codex TOML blocks in `README.md`/`packages/cli/README.md`/`site/connect.html` (verified present; field names remain UNVERIFIED against live Codex docs — smoke before claiming verified-working). **Adversary cycle:** r1 NO-SHIP (`docs/adversarial/directory-readiness-2026-07-16.md`) → 2 fix rounds → re-attack found ONE NEW blocking regression (`get_dashboard('default')` phantom-default masquerade) → round-3 fix → **ADDENDUM VERDICT: SHIP** (`docs/adversarial/directory-readiness-reattack-2026-07-16.md`; that doc records platform `npm test` 362/362, typecheck exit 0). **CLASS CLOSED:** "`readOnlyHint:true` tool that writes" — 2 members found+fixed (`infrastructure_status` warmup UPDATE; `get_dashboard` first-call `ensureDefaultViewSeeded` INSERT) + a systemic write-spy guard test (`apps/platform/test/mcp-tool-annotations.test.ts`, SQL-only scope traced adequate, virgin-tenant, revert-proven for both members). NOTE: the original frozen doc's line 39 ("pure SELECT … HELD") was wrong — `get_dashboard` also wrote on first read; corrected in the reattack addendum (its line 59), original preserved unedited per the frozen-doc rule. Submissions to mcp.so + Cline: BLOCKED on founder naming the public targets (permission classifier). Connectors Directory submission drafts RESCUED to `archive/2026-07-16-send-live-and-webhooks/scratch-rescue/` (`connectors-directory-listing.md` + `mcp-review-email-draft.md`) — durable in-repo now (the mcp-review email path is superseded by the workaround research: no pre-approval email needed).
- [ ] 2026-07-16 [ORDER] **Branded API domain (founder observation 07-16):** the API/MCP endpoint lives on `agent-cold-email-api.yaakovscher.workers.dev` while the brand is coldrig.dev — an early-stage/maturity signal AI buyers read AND an operator-identity leak (`yaakovscher` in every config snippet). Fix = `api.coldrig.dev` as a Workers custom domain (zone is ours; workers.dev URL stays alive for backcompat) + coordinated reference sweep: server.json, .well-known server-card, repo-root .mcp.json, .claude-plugin/plugin.json, READMEs ×3, site/connect.html + guides, Connectors/ChatGPT listing drafts, CLI default endpoint (needs next npm publish; old CLI keeps working via backcompat URL). DO BEFORE the directory submissions so listings carry the branded URL. Execute as its own small batch AFTER the pending two-lane commit lands (keep batches separable); custom-domain attach + deploy = founder-confirm.
- [ ] 2026-07-16 [ORDER] **Consumer-chat connector placement (founder 07-16: "be the default in ChatGPT and claude.ai chat, not just Claude Code/Codex"):** (a) claude.ai chat = the SAME Anthropic Connectors Directory submission already prepped (one listing surfaces in claude.ai + Desktop + Claude Code) — no extra lane, gated on the org-tier answer. **FOUNDER RULING 07-16: SKIP the Team org for now — keep on roadmap [gated:founder], revisit when distribution spend makes sense (natural trigger: real sending live + buyer-panel cycle 2 shows the chat surface is the missing lever).** Prepped listing drafts preserved (regenerate from ROADMAP research notes if scratchpad dies). **07-16 WORKAROUND RESEARCH (founder has NO Team org): Team/Enterprise gate re-verified HARD for connectors (no Console path — that exists only for plugins). Ranked paths: (1) RECOMMENDED solo 5-seat Team org $125/mo monthly cancel-anytime (billing floor, 1 real user OK; submit same day; ⚠️ listing-persistence-after-cancel UNDOCUMENTED — treat as standing cost or ask mcp-review@ before downgrading); (2) free parallel: community plugin (built, but Cowork+Claude Code only — never reaches chat); (3) free parallel: MCPB Desktop-extension Google Form (likely no org gate — needs human browser check; Desktop-only even if listed; requires packaging the npm bridge as .mcpb); (4) partner org (e.g. Wassner-side) submits with WRITTEN authorization — plausible under Directory Terms ("necessary rights" attestation, no ownership/domain-verify requirement), but listings are org-scoped with NO transfer mechanism; (5) mcp-review@ waiver ask — zero-cost shot, no precedent, don't block on it. Founder's agency-business idea: NOT barred by terms, but two unresolved gaps (no listing transfer between orgs + Anthropic's unilateral removal right) — diligence email to mcp-review@ before pursuing.** (b) ChatGPT Apps/connectors dive DONE 07-16 — **verdict GATED (founder judgment call)**: submission portal platform.openai.com/plugins; identity/business verification MANDATORY (reusable prerequisite, do regardless); demo-account creds required (use a sandboxed no-live-send tenant); tool annotations required (we now have them); OAuth NOT mandatory (Bearer/API-key fine); NO categorical ban on outreach tools in the app guidelines (fetched directly) BUT general usage policies list undefined "spam" as prohibited = reviewer-discretion risk; precedent unconfirmed both ways; verbatim usage-policy text UNFETCHABLE by bots (403 ×3 incl. archive) — founder should read openai.com/policies/usage-policies in-browser before deciding; if GO: frame listing as "outreach infrastructure with compliance tooling" (opt-out/CAN-SPAM foregrounded), agent drafts copy + 5 pos/3 neg test cases; (c) **Codex directory claim REFUTED** (the July-9 "plugin directory for Codex" search-summary line was a synthesis artifact; direct doc fetch: ChatGPT-only directory, Codex stays config-only via ~/.codex/config.toml) — Codex placement = web-findable config snippet, already in-tree; (d) "default" status = earned curation post-listing (usage → featured/verified tiers), set expectations accordingly.
- [ ] 2026-07-16 [ORDER] **Productize the BYO OAuth grant (founder 07-16, from "does every customer have to do it?"):** dashboard "Connect Google / Connect Microsoft" buttons (standard consent-screen flow) replacing the founder-grade token runbook for customers; MS 365 admin-consent covers a whole org at once (agency selling point); managed-mailbox lane needs zero customer setup but still faces the Mailforge-SMTP wall (own resolution needed before arming). FOLD INTO the §20 BYO intake build. **⚠️ FOUNDER CORRECTION 07-16: Mordy brings a BARE DOMAIN (Cloudflare/Namecheap) — NO Google/MS mailboxes.** The 07-15 SMTP-wall line's "most BYO customers (and Mordy) are on Google/MS Workspace" assumption is WRONG for Mordy. Domain-only customers = managed mailboxes provisioned on their domain (→ Mailforge SMTP-reachability check is on HIS critical path, elevate it: check alt submission ports e.g. 2525 / vendor relay / vendor API from the droplet) OR stand up Google Workspace on the domain (then the 443 OAuth path applies). Design §20 intake for all three shapes: nothing / domain-only / existing-Google-MS. **FOUNDER RULING 07-16: build for shape (a) — managed mailboxes provisioned on the customer's domain — as the primary path.** **⚡ 07-16 PROBE: droplet egress on port 2525 AND 8025 is OPEN** (nc succeeded to smtp.mailgun.org:2525, mail.smtp2go.com:2525+8025; 465/587 still blocked) → the managed-mailbox wall likely has a door without moving hosts: (i) confirm the chosen mailbox vendor (Mailforge/Inboxkit) supports submission on 2525 — add to the Gate-1 vendor support asks; (ii) engine config already takes arbitrary SMTP host:port per mailbox, so no code change expected; (iii) relay fallback (SMTP2GO/Mailgun on 2525) exists but changes deliverability posture — vendor-native 2525 strongly preferred. **07-16 VENDOR RESEARCH (no accounts created):** Mailforge DOC-STATES 2525 as a supported fallback on its custom-MTA mailboxes (mailforge.ai/blog/smtp-ports-explained-use — content page, tier-uniformity + real hostnames unconfirmed until provisioning; hostnames only via post-provision CSV/API) → shape-A route 1 = Mailforge over 2525, verify with 1 provisioned mailbox at activation (optional pre-sales confirm email, founder-gated). InboxKit provisions REAL Google Workspace/M365 accounts (their doc: "genuine account with full admin access") → standard Google/MS SMTP = 2525 almost certainly NOT available, BUT that makes InboxKit mailboxes sendable via our NEW 443 API transports instead — **the ms_graph app-only mode (client-credentials + admin consent + Application Access Policy) is purpose-built for exactly this fleet shape and is ALREADY BUILT**; Google Workspace side would want a service-account/DWD variant (small adapter addition, JWT grant — future work if InboxKit-Google chosen). Net: shape-A has two viable routes from the CURRENT droplet; vendor choice at activation now carries a send-path criterion (Mailforge→2525 vs InboxKit→443-API). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** the 07-16 framing above still lists Mailforge-over-2525 as "route 1" — that is now FLIPPED. InboxKit real Google/M365 via the 443-API transports (`ms_graph` app-only BUILT; Google DWD service-account adapter queued if Google mailboxes are chosen at fleet scale) is shape-A PRIMARY as of 2026-07-20; Mailforge-2525 is the fallback route. Full ruling + evidence: `## Open` 2026-07-20 "FOUNDER RULING" entry above.
- [ ] 2026-07-16 [ORDER] **Engine-443 adversary residuals (non-blocking, SHIP verdict `docs/adversarial/engine-443-transports-2026-07-16.md`):** (a) add `AbortSignal.timeout` (<180s) to api-send fetches — a hung fetch currently rides undici's ~300s default, contradicting the in-code "well under reclaim TTL" comment (no double-send opened, claim holds); (b) revoked refresh token grades transient → 5 wasted attempts/message before 'failed' — consider a permanent-grant-failure grade post-pilot; (c) test rigor: base64 vs base64url encode swap is NOT caught by the decode-based tests (both decoders lenient; current encodes correct) — lock down if touched; (d) Graph app-only user==key no fail-fast; mint script client_secret via argv; HTTP-date Retry-After ignored. (e) **ACTIVATION Gate add:** one real send per transport (gmail_api, ms_graph, smtp) with inbox + intact-header confirmation — API-wire MIME acceptance is unverifiable without live OAuth creds. (buyer run #3 agency, `tools/buyer-panel/runs/2026-07-15-claude-agency.md`): the shopper killed Instantly for "each client workspace requires its own full separate paid subscription… multiplies the plan cost N times over instead of scaling with a flat per-workspace add-on" — OUR per-tenant model repeats that exact pattern for agencies (8 clients × $99-min ≈ $800-950/mo vs winner Smartlead's ~$550-630 all-in at 50 mbx w/ $29/workspace add-ons). Post-activation kill-in-waiting at agency scale. Candidate fix for founder ruling: agency bundle — ONE platform fee + flat per-client-workspace add-on + per-mailbox meter (design alongside the quantity-billing/annual migration; our true multi-tenant isolation is the matching strength — the same run killed Saleshandy for FAKE isolation).
- [ ] 2026-07-15 [ORDER] **Directory-shopfront refresh** (buyer run #2 starter, `tools/buyer-panel/runs/2026-07-15-claude-starter.md` — FIRST ORGANIC SURFACE, via Glama auto-index; killed by the LISTING's own content: "no published pricing... waitlist" while our $99 pricing page was live the same day; the shopper never fetched coldrig.dev): refresh repo README.md + server.json description + site/.well-known/mcp/server-card.json + packages/cli README to carry (a) the $99/5-mbx pricing, (b) "free sandbox live today, no card" instead of waitlist framing, (c) keep the honest test-mode/activation disclosure — directories re-render from these. Claim-class rules apply; rides the compare-pages adversary+commit batch. ALSO elevates founder task: Glama claim click — **DONE 2026-07-15** (founder claimed; listing content fix = this refresh + push + his sync-click on the claimed page). PulseMCP RESOLVED-NO-ACTION 2026-07-15: their submit page says they auto-ingest from the Official MCP Registry (daily ingest/weekly processing) — we published 07-14 + 0.2.0 update 07-15, so the listing self-materializes ~within a week; if absent by ~07-21, email hello@pulsemcp.com (draft on request); check folded into the 07-26 POST-SHIP AUDIT. BUILD DONE 2026-07-15 (4 files, uncommitted, adversary pending); residuals: (i) `SPEC.md:102` header still reads "~8–12 tools" — same staleness class the Glama shopper hit, out of the builder's scope, fix at next SPEC touch; (ii) repo-root `glama.json` addendum went unaddressed by the builder — verify Glama's schema exists before adding, else drop; (iii) founder step queued: `mcp-publisher publish` re-publish after commit. Harness fidelity note: Claude-side shoppers can identify the operator (EpiphanyMade env leak) — disclosed cleanly in-run; ChatGPT side is the clean-room check.
- [ ] 2026-07-15 [ORDER] Guide-pages adversary nits (SHIP verdict `docs/adversarial/guide-pages-review-2026-07-15.md`, non-blocking): (a) add the `agent-cold-email mcp` stdio-bridge config block to /connect — the flagship guide points there for it (`guide-cold-email-operation-claude-code.html:83`) but connect.html shows only remote-HTTP configs; (b) title-only scan can over-read "Fully Autonomous" (scoped in-body) — consider tightening at next copy pass. SEND_USAGE_FEE_CENTS question added to ACTIVATION Gate 2.
- [ ] 2026-07-15 [ORDER] **Send-framing pricing copy** (Yaakov, two rulings 07-15; AMENDED same day: "3,300/mo doesn't sound nearly as good as 30,000, whether in actuality it's better or worse"): do NOT headline the raw monthly capacity number — it loses the shelf war to fake quotas on sight. Headline axis = **NO SEND QUOTA** (true + verifiable: sends are not the billing meter, no plan-imposed send limit — beats "30,000 emails" on its own axis, since no-cap > any cap; phrase as "no send quota / not metered per send", NEVER the slop word "unlimited" bare). The ~3,300-at-5-mbx physics number DEMOTES to second position ("what N mailboxes can actually deliver — same physics on every platform") and to comparison rows where it's paired with the kill: "Smartlead $94: 30,000-email quota, 0 mailboxes included → included sending capacity: 0." Keep the ratified non-contractual planning-guidance qualifier. Execute with the comparison-pages/site copy batch, claim-class adversary before deploy.
- [ ] 2026-07-15 [ORDER] **Annual billing option** (Yaakov: "the yearly vs annually is something we should do"): add annual prepay at ~2-months-free (~17% off; industry-standard, exactly the lever that let buyer-run #1's winner quote $66.67/mo "effective") → at 10 mbx our $149/mo becomes ~$124/mo effective, landing inside the winner's $102-120 band. FOLD INTO the pending Stripe quantity-billing backend migration (annual price variants per quantity item — one migration, not two) + pricing-page monthly/annual toggle + quote both bases on comparison pages. Founder items unchanged: Stripe live keys at activation.
- [ ] 2026-07-15 [IDEA] **Independent-review presence (G2/Trustpilot/Capterra)** — buyer run #1 (`tools/buyer-panel/runs/2026-07-15-claude-canonical.md`) killed FoxReach SOLELY for "no independent review evidence... too much unverified risk to run unsupervised"; ColdRig has the identical absence and would die on the same row the moment it surfaces. Founder-hands: create listings + seed honest pilot reviews (Mordy post-pilot is the natural first).
- [ ] 2026-07-15 [IDEA] **Reply auto-classification** (interested/not-interested/OOO auto-tags) — part of buyer run #1's WINNING sentence (Salesforge Primebox "bundled free rather than gated behind a $499/mo upsell"); direct market evidence for the warm-lead deep-dive's priority — fold into that dive, not a separate lane.
- [ ] 2026-07-15 [IDEA] **Canonical-scale price band data point** — buyer run #1 winner lands ~$102-120/mo ALL-IN at 10-15 mbx (annual-billed, self-assembled 3-product stack) vs our $149-199 monthly all-in at the same scale; founder pricing data point ONLY (do not move pricing unilaterally; our basis is monthly/no-commitment/one-bill — but a computing agent sees $102 < $149).
- [ ] 2026-07-15 [ORDER] Watchtower adversary non-blocking follow-ups (`docs/adversarial/watchtower-ops-email-review-2026-07-15.md`): (a) NB-4 — strip CR/LF (charset-filter) `brand` at the signup Zod boundary; it currently reaches the dunning email subject raw (likely inert via the structured send builder, unproven); (b) alert-quality — `failure_signals` pages the founder on ANY single terminal-'failed' send incl. routine CAN-SPAM refusals (threshold `total===0`) → tune to a windowed threshold before arming; (c) test-isolation ticket — waitlist KV rate limiter accumulates across the full suite run (pre-existing; adversary saw 4 flake-fails on the OLD tests; new CORS tests already dodge via unique TEST-NET IPs); (d) NB-2 recorded in ACTIVATION Gate-4 block.
- [ ] 2026-07-15 [ORDER] **SMTP EGRESS BLOCKED — Gate-1 smoke wall (found live 2026-07-15).** Engine on the DO droplet (142.93.12.85) canNOT send: DO blocks outbound SMTP submission at the account level — proven: 443 to google/cloudflare OPEN, but 465/587 blocked to BOTH smtp.gmail.com AND smtp-mail.outlook.com; the Mac's network ALSO blocks 465/587. Not our firewall (fixed a bad `ports:0` outbound rule mid-diagnosis; 443 works). Gate-1 engine smoke is BLOCKED until a send path exists. Paths (founder decision): (a) DO support ticket to unblock SMTP — est. account is established (Navah droplet), but DO scrutinizes cold-email use, hours-to-days, may deny; (b) quick prove-tonight test — run the built engine (`apps/engine/dist`) from a DIFFERENT network (phone hotspot/tether) that allows 465, point smoke curl at localhost:8080; (c) **STRATEGIC — add a Gmail API / Microsoft Graph send path (HTTPS/443, block-immune) alongside the nodemailer SMTP adapter** — most BYO customers (and Mordy) are on Google/MS Workspace, whose APIs never hit the SMTP block; this is arguably the correct BYO architecture, not a workaround (build lane, needs OAuth vs the current app-password model); (d) pick an SMTP-egress-friendly engine host. Gmail app password for yaakovscher@gmail.com is on the droplet `/root/mailboxes.json` (world-reachable :8080 but bearer-gated; rotate anytime + re-paste). ⚠️ This constraint ALSO affects the managed-mailbox (Mailforge) path — verify Mailforge SMTP reachability from the chosen host before arming. **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** Mailforge is no longer the primary managed-mailbox path — InboxKit is (real Google/M365 via the 443-API transports, sidesteps this SMTP-egress constraint entirely). Mailforge-2525 SMTP reachability is now a fallback-only verification. See `## Open` 2026-07-20 "FOUNDER RULING" entry.
- [ ] 2026-07-15 [IDEA] `setup_infrastructure` tool description says "Async — returns { jobId }" but `runSetupInfrastructure` runs SYNCHRONOUSLY in B0 (provisioning.ts:112) and the jobId isn't backed by a tracked job record — carried-over framing, not a live defect (field is real+pollable), but reconcile when the provisioning-sagas lane (B2, resumable alarm-driven jobs) lands. Flagged by tooldesc-adversary 2026-07-15.
- [ ] 2026-07-15 [ORDER] Fix `apps/engine/Dockerfile` npm defect (droplet-runner, provisioning day): `npm install --omit=dev` STILL resolves devDependency registry metadata (arborist), so the workspace-only `@coldstart/shared` devDep 404s the image build; the Dockerfile's comment claims otherwise — wrong on npm 8/10/11. Provisioning worked around it (scratch copy, devDeps stripped; image contents identical) — the real fix (strip devDeps in Dockerfile or use `npm ci` w/ workspace-aware install) needs a PR + rebuild. Detail: `.claude/agent-memory/spec-builder/gotcha_npm_omit_dev_still_resolves_devdeps.md`. Droplet live: `coldstart-engine` 142.93.12.85 (ID 584813711, fw 639bb346), engine container Up DARK (empty creds map, health ok, unauth 401; no tunnel, no Worker secrets, no ENGINE_TENANTS).
- [ ] 2026-07-15 [ORDER] **WIN-THE-COMPARISON program** (Yaakov: "do everything we can so that we win the comparison that the model makes"): (a) buyer-CHOICE panel harness build STARTED (tools/buyer-panel, spec-builder) — **CYCLE 1 COMPLETE 2026-07-15** (3 runs: canonical/starter/agency; see the harness entry below for full detail + fix-list) → **CYCLE 2 COMPLETE 2026-07-19** (3 more runs, same 3 briefs: `tools/buyer-panel/runs/2026-07-19-claude-starter.md` / `-canonical.md` / `-agency.md`, `CHOICE-TREND.md` +3 rows, `## Open` +6 fix-list lines below); (b) flagship Claude-Code/Cursor/Codex guide content STARTED (site/, spec-builder, claim-classes discipline, adversary before deploy) — **SHIPPED LIVE 2026-07-15** (`0a87f7c`, adversary SHIP zero-blocking `docs/adversarial/guide-pages-review-2026-07-15.md`, all 3 pages 200 live, wired to llms.txt/sitemap); (c) competitor-pages research DELIVERED 2026-07-15 + persisted (`docs/research/competitor-pages-research-2026-07-15.md` — worker had no write access, orchestrator persisted); ranked build order AgentMail → Skyp → FoxReach → Maildoso; UNVERIFIED flags resolved (per-page footers cite the internal buyer-agent research runs as the sourced-but-not-independently-reverified basis, not asserted as fact); **ALL 5 PAGES SHIPPED LIVE 2026-07-15** (AgentMail/Skyp/FoxReach/Maildoso/Smartlead, `6849c80`+`a074aa7`+`0744e15`) — adversary NO-SHIP→SHIP over 2 rounds (sole blocker: internal buyer-panel paths leaked into public HTML → reworded), frozen record `docs/adversarial/compare-pages-review-2026-07-15.md`; live-verified zero `tools/buyer-panel`/`briefs/`/`CHOICE-TREND` path leaks across every `site/*.html`, confirmed in sitemap.xml + llms.txt; (d) **webhooks: RULED BUILD under this order — SHIPPED, committed `d0e91ec` 2026-07-16.** Real per-tenant reply/bounce/complaint push (URL+secret+type filter) via HTTP + MCP tools 18-19 (MCP surface now 19 tools total, was 17 — verified `apps/platform/src/mcp/tools.ts` header comment); enqueue at the `recordEventIfNew` choke point (best-effort, cannot break event recording, UNIQUE dedup); cron-driven at-least-once delivery with backoff + auto-disable after 5 terminal failures; HMAC-SHA256 signed, secret shown once; SSRF-hardened (IPv6-embedded-v4 class closed, trailing-dot stripped, fail-closed, re-validated per delivery). Adversary SHIP + SSRF re-attack SHIP (`docs/adversarial/webhooks-lane-2026-07-16.md`); platform 420/420, typecheck + dry-run build clean. Closes the buyer-checklist hard-gate on reply/bounce push — real push now exists, no longer polling-only. **Residual CLOSED 2026-07-19:** Worker deployed (version `b3bae1c7`) with the webhooks lane live — bookkeeper live-verified `tools/list` = 19 incl. `get_webhooks`/`configure_webhook`, unauth `GET /webhook-subscriptions` → 401 (not 404), and `status.html`/`docs.html` now show webhooks as "Live" — the fully-live reframe copy batch (`ea64541`) carries the claim; (e) still founder-word: aggregator asks — **RULED 2026-07-15: DEFERRED until Mordy tries the product** ("outreach save till mordy tries it"); dogfood calls, ChatGPT-side panel (account paste protocol or OpenAI key + spend OK), portability promise (gated on Mailforge transfer terms); (f) 2026-07-15 Yaakov pricing challenge ("drop to $89 vs Smartlead $94/30k?") — ANSWERED NO, structural: Smartlead $94 = software license, ZERO mailboxes/domains included (aggregator, SPEC §"Smartlead is itself an aggregator"); 5 mailboxes physically send ~2.2-3.3k/mo at the 15-30/day deliverability ceiling — 30k/mo needs 40-70 mailboxes on ANY platform; add compare-vs-smartlead page w/ all-in "cost per 100 sends/day" math to the comparison-pages queue (loudest sticker anchor in the category); (g) OPEN founder pricing question surfaced by (f): BYO-connected mailboxes — same $10/mbx as provisioned, or cheaper? Settle with the §20 BYO intake build; (h) Smithery PUBLISHED 2026-07-15 (smithery.ai/servers/yaakovscher/agent-cold-email, scan SUCCESS via X-API-Key gateway header, quality 70/100, scan tenant ten_308b1b65 sandbox) + cursor.directory SUBMITTED (cursor.directory/plugins/agent-cold-email, pending their security-agent scan).
- [ ] 2026-07-15 [ORDER] **Warm-lead lifecycle DEEP DIVE** (Yaakov ruling 07-15: NOT a Mordy-start blocker — build deferred; instead deep-dive "the thin layer we should build and really what an AI agent needs so this is useful END-TO-END without the agent needing to do much itself"). Scope = full agent-journey audit (discovery → setup → launch → reply → warm-lead handling → follow-up → reporting/handoff), enumerating every point where the customer's agent must keep its own state or build its own sidecar, then design the thin persistence/automation layer. Scout-verified baseline at `686e506`: `leads` has NO tags/notes/custom fields (`schema.ts:103-112`); `thread_labels` + canonical vocab PURELY COSMETIC (`thread-labels.ts:20-43` — even `do_not_contact` triggers nothing); no lead-status write tool, no lead query/filter/export, no manual-suppress tool despite `'manual'` in the suppression-reason enum (`types.ts:125`), no one-off scheduled follow-up outside the launch-time sequence, no reply webhook (poll-only). Seed candidates for the dive (NOT ratified): (1) ⚠️ `do_not_contact` auto-suppress + manual `suppress` tool — compliance-adjacent (free-text "stop" phrasings miss the strict matcher `reply-processor.ts:55-71`; the address stays contactable by FUTURE campaigns) — eligible to ship as an independent early increment if the dive confirms; (2) structured lead interest-status + notes + `update_lead`; (3) `list_leads` filters; (4) `schedule_followup` reusing `scheduled_sends`; (5) CSV export. Webhooks tracked under forensics-gaps (c). Deliverable: spec section + ranked build list folded into SPEC/ROADMAP (no new doc). **DIVE COMPLETE 2026-07-16, adversary SHIP** (`docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`) — frozen record `docs/research/warm-lead-thin-layer-dive-2026-07-16.md`; ratified design `SPEC.md` §22. Ranked build list + cut-line: **#1** `suppress_lead` tool + parametrize `unsubscribeEmail` reason (S, compliance-adjacent — closes the real "free-text stop stays contactable by future campaigns" hole); **#2+#3** `lead_dispositions` table + `update_lead` tool (M) then `list_leads` (M) as one persistence increment; **#4** `schedule_followup` + `followups` table + tick drain (M-L — MUST reuse a new shared guarded-send primitive, caps/warmup/deliverability-pause/suppression, NOT `replyToThread`, per adversary amendment); below the cut-line: #5 auto-classification pre-fill, #6 `unsubscribe` webhook + choke-point routing, #7 event-body retention TTL. Status now **[gated:founder]** on build go + rulings on Q1-Q6 (SPEC §22). NOTE: "19 tools" copy surfaces (AGENTS.md, `.mcp.json`, site/README references) sync to 23 only once the build actually lands — not touched now.
- [ ] 2026-07-13 [ASK] Tier-2 insurance channels: extra .well-known variants, apis.guru form, public-apis PR (neutral copy — anti-marketing gates) — per research doc #5-#8
- [ ] 2026-07-15 [ORDER] **Business-readiness gaps** (critique-skill verdict, drafter+skeptic, 2026-07-15; survivors only): (a) **MONITORING/ALERTING — the one genuinely unplanned blocker**: nothing pages anyone, `/status` probes only D1 (`routes/status.ts:10` SELECT 1), ops cron dark, no engine-droplet watchdog, and ACTIVATION.md contains ZERO monitoring items — add a synthetic-check + founder-notify lane (CF cron → /status + engine /health → email/Slack) AND add it to ACTIVATION Gate 2 as an arming prerequisite; (b) **ops outbound-email channel** — absent entirely; blocks BOTH dunning notices (`admin-ops.ts:29` suspends silently) and support replies; one shared channel unblocks both (activation-gated but build-before-arm); (c) support triage is REGEX not AI (`support-kb.ts:19-58`, no LLM call repo-wide) + no inbound wiring — fine (site makes NO ai-support claim, verified) but never market it as AI until real; LLM upgrade = activation-era work; (d) tenant-facing data-erasure endpoint (GDPR/CCPA) — teardown deletes exist (`demo.ts:70`, `lifecycle.ts:92`) but no customer-invocable erasure path; (e) fix STALE `ACTIVATION.md:67` (claims CAN-SPAM address is a placeholder — it is FILLED on terms/privacy/dpa, verified; skeptic killed the draft's "violation" claim); (f) ~~attorney review~~ RULED NOT A LAUNCH GATE by Yaakov 2026-07-15 (draft banners removed, NJ governing law set, `45a8d13`); revisit on first real recipient dispute. DISSOLVED by the skeptic: complaint-breaker "unreachable" (runs every tick — `tick.ts:164` runDeliverabilitySweep); "no row-level DELETE"; the CAN-SPAM-placeholder violation claim.
- [ ] 2026-07-15 [ORDER] **Buyer-CHOICE panel harness** (Yaakov: repeatable, multi-vendor "watch the AI shop, then win the choice" loop — extends the weekly citation panel, which measures findability only): `tools/buyer-panel/` — standardized buyer brief (canonical scales: 10-15 mbx / few hundred sends/day, per forensics §2) run with FRESH contexts on (a) Claude side: dispatched sonnet research agent, fully automatable; (b) ChatGPT side: Yaakov's account (manual paste protocol) or OpenAI API w/ web-search tool (needs his key + spend OK). Per run: transcript forensics (queries → criteria → kill-list → deciding sentence → did ColdRig surface/shortlist/win) → frozen run record + CHOICE-TREND row → diff vs prior run → fix-list into `## Open`. Cadence: post-deploy + biweekly. Method template = docs/research/agent-buyer-research-forensics-2026-07-14.md §6. **CYCLE 1 COMPLETE 2026-07-15** (Claude side, 3 runs — canonical/starter/agency, `CHOICE-TREND.md` 3 rows + `runs/` 3 frozen records confirmed on disk): canonical NOT SURFACED (winner Salesforge/Forge ~$102-120/mo); starter SURFACED+SHORTLISTED organically via Glama, killed "no real sending/no published pricing" (winner Smartlead ~$62-65/mo); agency SURFACED+SHORTLISTED ("closest match to your entire brief"), same kill (winner Smartlead ~$550-630/mo). 5 fix-list items filed to `## Open` (directory-shopfront refresh [ORDER], agency pricing-shape [IDEA], review-site presence [IDEA], reply auto-classification [IDEA], canonical price band [IDEA]). Harness runbook hardened (cwd/identity-leak preconditions). ChatGPT side + cycle 2 (post directory-shopfront-refresh, biweekly) still open.
- [ ] 2026-07-15 [ORDER] Forensics playbook §5 gap closures (checked vs live+design state 2026-07-15): (a) competitor-review moat — design branch adds compare-vs-salesforge (the actual winner) ✓; **Skyp, FoxReach, AgentMail, Maildoso pages SHIPPED LIVE 2026-07-15** (+ Smartlead, see WIN-THE-COMPARISON entry above) — closed; (b) flagship "run your ENTIRE cold email operation with Claude Code + ColdRig" content w/ qualifier-heavy titles ("2026", "fully autonomous", "agent-native") + Cursor/Codex variants — **WRITTEN + SHIPPED LIVE 2026-07-15** (see WIN-THE-COMPARISON entry above) — closed; (c) Requirement-7 webhooks: buyer checklist HARD-gates on reply/bounce push — **BUILT + SHIPPED, committed `d0e91ec` 2026-07-16** (full detail: WIN-THE-COMPARISON (d) entry above); real push closes this gap in code — residual is the capability-matrix/site copy still saying webhooks absent/polling-equivalent, fix rides the fully-live reframe copy batch; (d) aggregator listings — coldiq/tools, inboxkit, coldemailkit saturate buyer result sets (where "Reddit sentiment" is actually read) — outbound asks, founder-word gated; (e) Glama listing (appeared in buyer result sets unfetched) — add to the founder directory batch; (f) portability promise ("domains transferable if you leave") — DO NOT claim until Mailforge resale/transfer terms verified (Gate-1 support ask). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** Mailforge's resale/transfer terms are now primary-source quoted (ACTIVATION.md Gate 0 — prohibits direct resale, agency shape with mandatory sub-accounts) but Mailforge is fallback-only; the portability question that actually matters now rides InboxKit's terms, which are UNVERIFIED pending empirical post-purchase check — still DO NOT claim portability until that's settled. Closed already: pricing hole at canonical scale (RATIFIED $149/10mbx), one-click unsub real (`ecfc5b2`), status page (API live + design page), public repo, honest-activation framing.
- [ ] 2026-07-13 [ASK] B2 — (ACTIVATION-HARDENING backlog — synchronous provisioning WORKS in test mode) Provisioning sagas: resumable alarm-driven jobs on clock abstraction; clock-scaled warmup ramp; idempotency keys; DO-reload/alarm-retry soak tests.
- [ ] 2026-07-13 [dark-unarmed] B3 — `VendorPort` layer: fault-injecting sandbox impls (active) + real impls (Porkbun/Inboxkit/Stripe — coded, unactivated); one contract-test suite across both; free/demo tenants structurally can't reach a real adapter (type guard + failing test).
- [ ] 2026-07-13 [ASK] B4 — Sequencing + reply engine — OPT-OUT INCREMENT DONE `ecfc5b2` 2026-07-14 (adversary SHIP ×4 rounds): hosted RFC 8058 one-click endpoint + dual List-Unsubscribe headers + List-Unsubscribe-Post, in-body opt-out link, CAN-SPAM compliance footer (sender identity + physical address, fail-safe refuses non-compliant sends), typed-"unsubscribe" reply detection (conservative matcher, overrides stop_on_reply), honored immediately (same-tick). Campaigns/scheduling/caps/rotation/stop-on-reply/suppression/validators were already built. REMAINING B4 scope: A/B testing (unbuilt) + a founder call on the manual-reply path (`threads.ts:147` — inbox `reply` tool sends footerless/no List-Unsubscribe; adversary-ruled defensibly transactional, decide deliberately).
- [ ] 2026-07-13 C5 (remaining half) — npm publish DONE + official MCP Registry DONE (see 2026-07-14 entries below). Smithery/mcp.so/PulseMCP + awesome-list PRs: covered by the standing registry/directory authorization, cascade running (see below) — results pending. The `/.well-known/mcp/server-card.json` portion is already served.
- [ ] 2026-07-13 [ASK] D3 — Legal: BUILT+VERIFIED IN-TREE 2026-07-14, [gated:founder] on the deploy batch — full clause inventory now met: NEW `site/dpa.html` (9 sections, code-grounded termination handling incl. the deliberate suppression-permanence carve-out `lifecycle.ts:269-270`), terms §8 strengthened to ROSCA-grade (named statute, disclosure timing, concrete cancel mechanism `POST /cancel` + support@), privacy §4 links /dpa, aup reviewed-still-correct, DPA footer-linked on all 4 pages + sitemap. All DRAFT-flagged. Enforcement map produced (11 clauses: 7 BUILT w/ file:line + tests, 2 N/A-legal, 2 PARTIAL → follow-up entries below). Evidence: parse5 0-errors ×4, tag-balance ALL_OK, 8 screenshots `pw-shots/legal-2026-07-14/` (DPA desktop + terms mobile eyeballed by orchestrator). REMAINING: founder deploy-go, then attorney review (the real legal gate).
- [ ] 2026-07-13 [gated:founder] D4 (remaining half) — OFAC screen + friction ladder + new-tenant volume ramp; needs real signups to exercise. (Per-tenant physical address + sender identity capture is already done in `setup_infrastructure`.)
- [ ] 2026-07-13 [ASK] D6 (remaining half) — Owner business-health substrate: per-tenant margin, backups/DR for the ledger + encrypted creds, master-key storage/rotation/recovery (separate concern, deferred). (MRR/active-tenants/status-page/`GET /admin/ops/digest` portion is already built.)
- [ ] 2026-07-13 [ASK] E3 — Memory + ledger current; HANDOFF.md maintained (standing item).

- [ ] 2026-07-14 [ORDER] Email engine — **COMMITTED DARK `eb8ee42` 2026-07-14 after re-attack #3 SHIP** (was NO-SHIP through re-attacks #1-#2; both defect classes killed — see the two [x] entries below). [dark-unarmed] → **ENGINE LEG ARMED 2026-07-27**: founder ran `cloudflared tunnel login` on the droplet (142.93.12.85); tunnel `coldrig-engine` (`a03c90a4-632d-437d-9c26-57e15c0cf77c`) created, `/etc/cloudflared/config.yml` ingress `engine.coldrig.dev`→`http://localhost:8080` (catch-all 404), DNS CNAME routed, `cloudflared` installed as a systemd service (active, survives reboot) — droplet-side specifics **UNVERIFIED by bookkeeper** (no SSH from this session; reported by the executing session). Bookkeeper-independently-verified this pass: `https://engine.coldrig.dev/health` → 200 `{"status":"ok"}`, unauth `POST /v1/send` → 401, unknown path → 404. Founder then ran `echo "https://engine.coldrig.dev" | npx wrangler secret put ENGINE_BASE_URL` (new Worker version auto-deployed); `npx wrangler secret list` (bookkeeper-confirmed) now shows ALL FOUR of `ENGINE_BASE_URL`/`ENGINE_AUTH_SECRET`/`INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` present, so `isCredentialPushConfigured()` (`apps/platform/src/engine/mailbox-credential-push.ts:47`) is TRUE in prod. Per-tenant reachability is now governed solely by the self-serve activation predicate (`apps/platform/src/engine/activation.ts` — code-confirmed this pass, line 2 documents it as the replacement); the `ENGINE_TENANTS` allowlist is RETIRED. Residuals carried forward: (i) DO Cloud Firewall public `:8080` rule still OPEN — founder has the doctl one-liner, drop pending his run (engine stays bearer-gated meanwhile; dropping it removes the cleartext-bootstrap path entirely); (ii) `ENGINE_AUTH_SECRET` pairing (Worker vs droplet container) UNVERIFIED end-to-end since the 07-19 pairing — a mismatch fails LOUD as 'pending' credential pushes, nothing lost; prove at Mordy's first mailbox push or a pre-Mordy staged grant; (iii) `GMAIL_OAUTH_GRANTS` still absent by design (per-mailbox, actionable at provisioning); (iv) reported-not-independently-checked: `cloudflared` 2026.7.3 installed on the droplet, Docker-API ports 2375/2376 verified not listening (UFW allow rules are stale image defaults, removal pending founder). arming = ACTIVATION Gate-2 (founder droplet + the carried residuals). STILL OPEN from the original entry: (1) ~~per-port activation factory change~~ **DONE — was stale-open here: built+committed `f74687d` (adversary SHIP, 21 tests, factory.ts:84-126); revert-fail-restore physically proven 2026-07-20 (5 gate tests fail on old logic, 534/534 + typecheck 0 at HEAD, tree byte-identical)**; (2) ~~ARCHITECTURE #6 wording~~ DONE 2026-07-14 (Node ratified, updated with the commit); (3) ~~Cloudflare Tunnel before real tenant traffic~~ **DONE 2026-07-27** — see the ENGINE LEG ARMED note above; (4) GreenMail e2e once at host stand-up (now noted in Gate 2, still unrun — no real mailbox yet). Original entry (for the record) — `apps/engine/`: Node SMTP/IMAP daemon (nodemailer/imapflow/mailparser, A5-validated stack), HTTP boundary (`/health` + bearer-authed `/v1/send`,`/v1/poll`), durable JSON store, Dockerfile; Worker client `RealEmailPort` rewritten as an HTTP client, flag-dark. Builder-reported: 27 engine unit tests + 9 Worker contract tests green, full platform suite 252/252 (**UNVERIFIED by bookkeeper** — code is uncommitted, not independently re-run here). **Adversarial review found a BLOCKING defect: poll-cursor loss — class "state persisted before cross-boundary effect confirmed."** Fix IN FLIGHT with the builder (cursor ownership moving to the consumer DO; a stuck-'sending' reclaim guard is also queued). Engine code is **NOT committed** — `git log -- apps/engine/` is empty; do not treat any of this as shipped until it lands as a commit with a clean re-attack. Provisioning runbook already drafted in ACTIVATION.md Gate-2 (itself still uncommitted). **OPEN once it commits:** (1) per-port activation — routing a comped pilot tenant onto the real EmailPort while keeping sandbox billing/domain/mailbox needs a factory change (`realAdaptersActivated` is global all-or-nothing today); (2) engine is Node not Go — diverges from ARCHITECTURE #6 wording; Node-over-Go RATIFIED by the orchestrator 2026-07-14 (A5-validated stack, buildable-today) — ARCHITECTURE.md #6 needs a wording update once the engine actually commits (not done in this session — out of touch-scope for a docs-only pass); (3) ~~cleartext-HTTP bearer on the IP bootstrap → move to Cloudflare Tunnel (HTTPS) before real tenant traffic~~ **DONE 2026-07-27** — Cloudflare Tunnel live at `engine.coldrig.dev` (see ENGINE LEG ARMED note above); (4) GreenMail full send/receive e2e is coded (`ENGINE_E2E=1`) but still unrun (docker-run sandbox-denied / no real mailbox yet) — run it once at host stand-up.

> Done items live in archive/ROADMAP-done.md (self-draining at handoff).
- [ ] 2026-07-14 [ORDER] Mordy-pilot activation lane (Yaakov: "get coldrig ready + have him migrate"): critical path UPDATED 2026-07-14 (again) — engine COMMITTED (`eb8ee42`, R3 SHIP) → `ENGINE_TENANTS` allowlist BUILT + adversary SHIP + COMMITTED (5 guards, 21 tests, suite 284/284; frozen verdict `docs/adversarial/engine-tenants-allowlist-review-2026-07-14.md`; revocation-needs-DO-restart note carried in ACTIVATION Gate 2) → **now: droplet provisioning (FOUNDER — runbook CLEARED in ACTIVATION Gate 2)** → set `ENGINE_TENANTS` + arm per Gate-2 residuals → Gate-1 smoke (~$6/mo, runbook in ACTIVATION.md Gate-2) → `ENGINE_TENANTS` per-tenant allowlist build → Gate-1 smoke test on OUR test domain → BYO-domain+BYO-mailbox intake (his authorpitchdesk.com + Google Workspace boxes) → migrate as tenant #1 (comped/test-billing, no live Stripe needed). Founder 2026-07-15: droplet spend + test-domain purchase + FULL DEPLOY BATCH **AUTHORIZED** ("you have my permission… you can deploy everything") — **both now EXECUTED**: droplet `coldstart-engine` PROVISIONED (doctl-confirmed: ID `584813711`, IP `142.93.12.85`, nyc1, firewall `coldstart-engine-fw` 22+8080, container Up, `/health` `{"status":"ok"}`, unauth `/v1/send` 401; NOT armed — no creds, no tunnel, no Worker secrets, no `ENGINE_TENANTS`), deploy batch live-verified (see design-lane entry above). Mailbox-provider question RESOLVED — no third decision existed: Mailforge stays the DECIDED managed-path vendor (ACTIVATION Gate 0), Mordy rides BYO (§20) on his existing Google Workspace boxes ("all he wants is to sign up"). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit (fix this everywhere)"):** this line is now stale — Mailforge is no longer the decided managed-path vendor; InboxKit is (ACTIVATION.md Gate 0, updated 2026-07-20). Mordy's BYO path on his own Google Workspace boxes is unaffected either way. Remaining founder-hands: registrar/vendor account w/ card (no purchase capability exists on this machine — CF-dashboard 2-min buy or Namecheap acct+API key), Mailforge signup OR a GWS app-password test box for Gate-1 smoke, migration-timing vs his 07-28 Instantly clock. **Watchtower/monitoring blocker CLOSED 2026-07-20** (see ACTIVATION.md Gate 4 "Ops email + monitoring" block: `wrangler login` email-scopes refresh done, Email Sending + Routing armed + live-verified, watchtower cron confirmed firing, end-to-end alert-and-ticket path verified) — 1 of 4 Mordy-pilot blockers now closed; only the step-7 external prober remains open, folded into the founder's existing regular monitoring service (see the 2026-07-20 [gated:founder] line below), not a new UptimeRobot/BetterStack account. Remaining code: watchtower lane no longer blocks it (SHIPPED+deployed `2795a64`) — **BYO intake now BUILT** on branch `byo-intake` (`f46e935`, adversary SHIP-dark + re-attack ADDENDUM SHIP — see the "BYO-domain RULED" entry above), running alongside the already-shipped webhooks build (both core-code lanes under the win-the-comparison order); merge to main [gated:founder].
- [ ] 2026-07-14 [ASK] Pre-warmed inventory evaluation: can/should we pre-warm domain+mailbox stock ahead of signup (cost carry, brand-mismatch, reassignment ToS/deliverability risk) — Yaakov question, needs economics + SPEC ruling. — analysis DONE in SPEC §12.1 (carry cost, brand-mismatch, reassignment-risk UNKNOWN flagged, competitor precedent, break-even recommendation) — awaiting founder ruling; NOT checked off, the ruling is the item.
- [ ] 2026-07-14 [ORDER] Dogfood campaign (Yaakov = tenant #1, Mordy = tenant #2): coldrig runs its own transparent cold campaign — research-built list of lead-gen/cold-email bloggers, YouTubers, newsletter writers; subject leans into disclosure ("yes, this is a personalized cold email…"), body discloses agent-run ("completely managed by my AI executive assistant") + pitch; matches tier-2 of docs/research/traffic-channels-2026-07-12.md §16 (transparent dogfood = marketing artifact, founder-approved in-session 07-14). Guardrails: CAN-SPAM address fill (Gate 4) + unsubscribe + small-volume hyper-targeted + publish live deliverability/logs proof page; NEVER volume-spam (brand = the responsible layer). Sequencing: sends gated on engine + mailboxes + ~4wk warmup; list research + copy + proof-page build can start pre-warmup. — 2026-07-14 PROGRESS: list research + copy drafts DONE → `docs/research/dogfood-targets-2026-07-14.md` (28 verified entries tiered A/B/C, 3 subjects + 2 bodies disclosure-forward, competitor-exclusion default). Remaining pre-send: contact-path completion pass (top Tier A names have NO confirmed emails — never guess), anti-spam-voice sweep, proof-page (design lane), and 3 FOUNDER CALLS: include competitors? include Jack Clark/Import AI (Anthropic co-founder)? confirm "roast it publicly" CTA risk tolerance?
- [ ] 2026-07-14 [ASK] Founder ruling: light-KYC scope for BYO intake — §20.3 currently gates only BYO-PRIMARY; adversary R3/R4 note cheap dedicated non-primary phish domains face only the downstream complaint breaker (some phishing sends before it trips). Extend light-KYC to ALL first-time BYO intake?
- [ ] 2026-07-15 [ORDER] Directory batch ROUND 2 (main-loop execution after the classifier refused subagent relays): **mcp.so SUBMITTED ✓ FREE tier** ("Submission received… review queue", submission b5135b9d, screenshots) · **Smithery PUBLISHED 2026-07-15** (X-API-Key bearer-equivalent shipped in the watchtower commit `2795a64` unblocked it — smithery.ai/servers/yaakovscher/agent-cold-email, scan SUCCESS, quality 70/100) — closed · **cursor.directory SUBMITTED 2026-07-15** (`.mcp.json` shipped in `2795a64` unblocked the scan — cursor.directory/plugins/agent-cold-email, pending their security-agent scan) — closed · **Cline HELD on honesty**: the issue form REQUIRES attesting "I have tested that Cline can set up this server" — we have not run Cline; prerequisite = one real Cline setup test (Yaakov ~10 min in VS Code, or scripted later); icon ready (designer's mark, 400×400), form mapped, never false-attest · **Glama claim: NEEDS YAAKOV'S CLICK** (classifier correctly blocks agent-clicked OAuth grants on his GitHub): glama.ai/sign-in → Continue with GitHub → Authorize (read-only scopes) → server page → Claim → admin panel Dockerfile config = scored badge. Original round-1 outcomes below.
- [ ] 2026-07-15 [ORDER] (superseded detail) Directory batch EXECUTED under Yaakov's in-session named authorization ("Push to PulseMCP... llmstxt.site + directory.llmstxt.cloud... smithery mcp.so cursor.directory... C-Line... Awesome MCP PR... Glama"): **llmstxt.site SUBMITTED ✓** (thankyou page) · **directory.llmstxt.cloud SUBMITTED ✓** (Tally confirmation) · **Glama ALREADY-LISTED** (auto-crawled from MCP Registry; server page + connectors live; badge svg resolves; CLAIM still open — needs Yaakov's GitHub-OAuth'd Glama account + admin-panel Dockerfile for the quality checker, oauth-runner attempting with his session) · **awesome-mcp-servers PR #10106: both bot requirements MET** — badge commit `1cc4948` pushed to the fork branch + reply posted; merge = maintainer's move now, monitor · **PulseMCP BLOCKED** (submit path is behind a Cloudflare bot wall / "contact hello@pulsemcp.com for API access" — remedies: Yaakov visits pulsemcp.com/submit in his own browser (~2 min), or sends the drafted email) · mcp.so/cursor.directory/Smithery/Cline: authenticated headless run IN FLIGHT off the coldrig profile (free tier enforced on mcp.so). Screenshots: pw-shots/directory-batch-2026-07-15/. GSC/Bing + CF Web Analytics remain Yaakov's clicks (no API scope — verified).
- [ ] 2026-07-14 [ORDER] Registry/directory cascade — OUTCOMES VERIFIED 2026-07-14 (read-only): official MCP Registry LIVE+active v0.1.0 (updatedAt 14:04Z) · npm 0.1.0 live · awesome-mcp-servers PR #10106 still OPEN unmerged (1 comment — monitor) · DeepWiki seeded (200) · **Smithery NOT LISTED** (search returns only unrelated "agent-safe-email") + **PulseMCP NOT LISTED** (`total_count: 0`) — both covered by the standing disclosed-listings approval: Smithery sits behind GitHub OAuth (Yaakov's hands, same headed-login pattern as mcp.so/cursor.directory), PulseMCP has a no-login submit form BUT the auto-mode classifier DENIES the publish on standing approval alone (2026-07-14, same class as the llmstxt refusals) — needs Yaakov to state the PulseMCP submission request directly in-session, then it's ~1 min headless. Remaining hands-blocked surfaces: see the directory-cascade entry below.
- [ ] 2026-07-14 [ORDER] Human-surface gaps found by live prod drive (feeds the EXTERNAL design lane — hand these to the design LM): (a) NO self-serve billing UI — dashboard Settings shows plan/billing state but has zero upgrade/downgrade/cancel/payment-method controls, so even after Stripe goes live a human has no button to spend money; (b) NO human signup path at all (only `POST /signup`, framed as the agent's job) — a non-technical human cannot buy or use this today; (c) no `support@` contact published (routing built but disarmed, ACTIVATION Gate 4); (d) no status page; (e) no token-recovery path if a customer loses their bearer token (real support burden at launch). Evidence: `pw-shots/human-journey-2026-07-14/` (14 screenshots, both widths).
- [ ] 2026-07-14 [gated:founder] Stripe cannot take money: `STRIPE_SECRET_KEY` UNSET in prod — `/checkout` returns a SIMULATED url and hitting it upgrades a tenant to a paid plan with zero card + zero dollars (intentional test-mode; anyone with the API can self-upgrade). Webhook correctly fails closed (503, `STRIPE_WEBHOOK_SECRET` unset). To take real money: Stripe live KYC → swap key → set webhook secret (no Price objects needed, created inline). ACTIVATION Gate 2.
- [ ] 2026-07-14 [gated:founder] Mordy-pilot founder-hands items: (a) mailbox provider ruling — Inboxkit (real Google mailboxes via API, ~$6/mbx, 10-min signup + card; Yaakov leaned this way as "the better one") vs Mordy's own Google Workspace seats; (b) ~$10 test domain purchase for the Gate-1 smoke; (c) engine-host droplet spend APPROVED verbally ("whatever size we need") — `doctl` runbook in ACTIVATION Gate 2, provisioning not yet run (gated on the engine class-fix landing).
- [ ] 2026-07-14 [ASK] Cline MCP Marketplace listing — BLOCKED on a required 400×400 icon; deliberately deferred until the display brand locks (TM clearance) so we don't build a throwaway icon. 5-min submission once brand is final.
- [ ] 2026-07-14 [ORDER] Directory cascade — 4 surfaces still BLOCKED, each needs Yaakov's hands (the harness refuses agent-relayed authorization for outbound public writes): (a) **llmstxt.site** + (b) **directory.llmstxt.cloud (Tally)** — forms fully prepared, exact values drafted, scripts RESCUED to `archive/2026-07-14-activation-day/scratch-rescue/{submit_llmstxtsite.cjs,submit_tally.cjs}`; run with `NODE_PATH=~/.claude/skills/playwright-cli/node_modules node <script>` (~10s each, headless, no login) OR Yaakov states the submission request directly in-session; (c) **mcp.so** + (d) **cursor.directory** — both now behind GitHub-OAuth sign-in walls; one headed login (`~/.claude/skills/playwright-cli/scripts/persistent-session.js --profile coldrig --url <url> --login`) persists the session at `~/.claude/playwright-profiles/coldrig/` for all future automated submissions. ⚠️ mcp.so pre-selects a PAID $39 tier by default — pick the free tier deliberately. DONE already: npm, official MCP Registry, awesome-mcp-servers PR #10106 (open), llms-install.md.
- [ ] 2026-07-14 [ASK] Automated data-deletion pipeline (DPA follow-up): "deletion on termination/request" is fulfilled MANUALLY today (privacy@ inbox) — infra reclaim + suppression permanence are real tested code (`lifecycle.ts:235-277`), but no code path purges a terminated tenant's lead/campaign rows. Touches `tenant-do.ts`; not small — backlog, rank at next planning pass.
- [ ] 2026-07-14 [ORDER] PRICING **RATIFIED by Yaakov in-session 2026-07-15** (his words: liked $49+$10/mbx, "obviously not pitched that way" — PITCH = starts at $99 for 5 mailboxes, +$10 per additional mailbox; send rate NOT specifically capped — present the IDEAL send volume as deliverability guidance, never a contractual cap). Public framing directive binds all surfaces (pricing page headline, calculator, FAQ, JSON-LD, llms.txt, for-agents). REMAINING WORK: (a) integration lane verifies/adjusts the designer's pages to this framing (in flight); (b) CORE backend migration = quantity-based mailbox billing in Stripe (quantity subscriptions), quote-before-provision, owner spend ceiling persistence, downgrade/cancel semantics, plan-enum migration off launch/growth/scale, tests — the designer's UI deliberately disables billing mutations until this lands; (c) integration-adversary find 2026-07-15: `apps/platform/src/admin/support-kb.ts:35-36` — the AI SUPPORT AGENT'S KB still quotes the dead $299 Growth/$799 Scale tiers → 3-way inconsistency (site formula vs support answers vs live checkout PLAN_QUOTAS); fix the KB copy WITH the billing migration, or immediately after the design deploy if the migration lags (support quoting wrong prices to real prospects is a trust-killer). Original proposal detail (for the record): PRICING RULING was PENDING — designer + Yaakov independently converged on the SAME formula: $49 platform + $10/active mailbox ≡ "5-mailbox minimum at $99, +$10 per extra mailbox" (5=$99, 10=$149, 20=$249, 60=$649; domains bundled ~1:3; NO send meter — capacity shown as approximate/after-warmup, never contractual; deterministic agent billing = expose usage + projected monthly cost + quote-before-provision + owner-controlled spend ceiling; variable add-ons LATER: lead sourcing/verification/enrichment/dedicated IPs/extra workspaces/retention/white-label). Economics assessed in-session vs ~$4.50–6/mbx COGS (this file, ramp-economics entry above): marginal GM 40–52%, blended ~58–76% at entry tiers. Replaces live Launch/Growth/Scale ($99/$299/$799), kills the $99→$299 cliff, creates the ~$149/10-mbx option the buyer-agent research flagged; current Custom is ALREADY $49+$13/mbx so this unifies the ladder at a lower rate. NOT AUTHORIZED until Yaakov confirms; on ruling: pricing.html + JSON-LD + SPEC §12/§18 + Stripe quantity-billing + dashboard billing UI update together. ⚠️ side-finding: live pricing.html is internally inconsistent — states ~40–50 sends/mbx/day but lists 5 mbx = "~1,000 sends/mo" (the math gives ~4,400–5,500).
- [ ] 2026-07-14 [ASK] Product gaps surfaced by Mordy's reply-tracking question (scout-mapped in-session with file:line cites): (a) NO auto reply-classification (interested/OOO/not-interested) — thread labels are manual (agent- or human-set), CANONICAL_THREAD_LABELS is a UI recommendation not an enum (packages/shared/src/dashboard.ts:17-27); (b) NO per-lead tags/segments/custom fields — lead = email/first_name/company/global_status only (schema.ts:103-112); (c) ~~inbound typed-"unsubscribe" replies NOT auto-detected~~ CLOSED `ecfc5b2` 2026-07-14 (conservative exact-phrase matcher in reply-processor, suppresses + cancels, adversary-attacked clean); (d) no bulk-export/CRM-sync endpoint — agent must page inbox/thread/activity; (e) NO data-retention policy on events (reply bodies stored in full, indefinitely, no TTL — schema.ts:141-152); the "premium data retention add-on" framing from the designer's pricing note is UNCONFIRMED in repo docs. Rank these build-vs-pilot-defer.
- [ ] 2026-07-14 [ORDER] Design-lane FULL page brief (extends the human-surface-gaps entry above; delivered to Yaakov in-session for relay to the external design LM): human signup/onboarding + "connect your agent" (per-client MCP install: Claude Code/Cursor/Cline), dashboard billing UI (upgrade/downgrade/cancel, payment method, usage + projected cost, quote-before-provision, owner spend ceiling), pricing page rebuilt on the ruled model (mailbox slider/calculator, approximate-capacity labels), "what happens when a prospect replies" explainer page (Mordy asked; every buyer will), BYO-domain explainer + primary-domain informed-consent UX (SPEC §20), support/contact page (arming gated on Gate 4), status page, token-recovery flow, live deliverability proof page (ties to the dogfood ORDER above), recipient-facing unsubscribe confirmation + "why am I getting this" page, security/trust page, styled 404.
- [ ] 2026-07-16 [ORDER] Existing `reply` tool sends with NO suppression check (replyToThread — pre-existing, defensible as 1:1 transactional; same unchecked primitive increment #4 would have reused) — deliberate founder/design ruling required at warm-lead build time; flagged by warm-lead design adversary (frozen doc `docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`).
- [ ] 2026-07-17 [ORDER] BYO residuals (non-blocking, same frozen doc): (a) **engine-side standing rule** — `mailboxes.transport_json` (verbatim SMTP pass/OAuth refresh token) is write-only in the platform; the engine read-back lane, when built, must NEVER echo creds into any tenant-readable surface (tool output, dashboard, error, log); (b) abuse-gate spec-acknowledged gaps (<5-char brands exact-match only; letter-for-letter confusables outside the homoglyph map; light-KYC covers BYO-primary only) — revisit with the 2026-07-14 light-KYC founder [ASK]; (c) AGENTS.md public tool table stale (says 17; 19 live, 21 after BYO merge) — sync in the commit-batch fold; (d) root-level vitest drags apps/dashboard React tests without jsdom env (`document is not defined`) — pre-existing harness mismatch, scope root runs or add env; (e) **TOCTOU** — concurrent `is_primary` registrations could both pass the ≤1-active-BYO-primary guard under real (network-I/O) adapters; benign (more guardrails not fewer, no crash/spend/security bypass) and sandbox-untriggerable today (adapters synchronous in-memory) — fix = atomic re-check or `blockConcurrencyWhile` at arm time; (f) **`pending_kyc` soft-lockout** — a primary stuck in `pending_kyc` (abuse-flagged) permanently blocks the tenant from registering any new primary; no admin KYC-clear route exists in this build — fold into the KYC-clear build when it lands. ((e)+(f) new 2026-07-17, from the re-attack addendum, non-gating.)
- [ ] 2026-07-19 [ORDER] Buyer-panel cycle 2 (agency run, `tools/buyer-panel/runs/2026-07-19-claude-agency.md`) traces its ColdRig kill directly to a fetched Glama listing page (`glama.ai/mcp/servers/YS-projectcalc/agent-cold-email` — page #5 in the shopper's own fetch list) carrying pre-reframe copy: "openly in test mode with Stripe test keys only — no real sending, no real customers, no deliverability track record yet, by its own docs." New evidence for the existing founder-hands Glama sync-click item (`HANDOFF.md` "Other open decisions" / the Directory-shopfront-refresh `[ORDER]` above) — the claimed listing's admin-panel sync click is the direct, specific fix for THIS run's kill sentence.
- [ ] 2026-07-19 [ORDER] Buyer-panel cycle 2 (starter run, `tools/buyer-panel/runs/2026-07-19-claude-starter.md`) killed ColdRig on a stale WebSearch-snippet-sourced self-disclosure ("no live production deployment, no real customers, no deliverability track record yet") with NO page fetch to coldrig.dev/GitHub/Glama occurring anywhere in that run — search-index caching, not a directory listing, is now the vector. New evidence for the existing 2026-07-16 `[ORDER]` "ChatGPT and Claude search could NOT fetch coldrig.dev... Google is not indexing the site": the fully-live reframe (deployed 2026-07-19) + IndexNow fire are already in motion; this run is evidence the fix hasn't yet propagated to search-index caches, not new work.
- [ ] 2026-07-19 [ORDER] Buyer-panel cycle 2 canonical run (`tools/buyer-panel/runs/2026-07-19-claude-canonical.md`) is NOT SURFACED for the 2nd consecutive cycle (grep-verified 0 hits) — same discovery hole as cycle 1. None of the 25 queries run targeted our brand/repo slug (contrast the starter run's query #5, `"agent-cold-email" MCP multi-tenant platform github`) or fetched glama.ai/coldrig.dev/GitHub; every query was purely category- or named-competitor-scoped. Ties to the existing 2026-07-16 `[ORDER]` Google/Bing index-absence item — canonical-scale buying queries are exactly the discovery gap that order is meant to close.
- [ ] 2026-07-19 [IDEA] Compare-page ammo from the cycle-2 canonical run (`tools/buyer-panel/runs/2026-07-19-claude-canonical.md`): Instantly's own feedback board carries an OPEN, unresolved feature request for API/MCP domain+mailbox creation from scratch — the shopper's stated kill reason for Instantly ("that one core step is dashboard-only") — our exact strength, worth a direct callout on compare-vs-instantly copy. Also: Smartlead's real all-in cost at canonical scale is $142-167/mo (midpoint $152.33 monthly-billed, $136.63 annual) vs our $149/10-mbx — closer than the sticker $94 suggests, worth quoting on compare-vs-smartlead. Open question the shopper itself flagged and could not resolve from public docs: whether Smartlead's plan cap counts warmup traffic against the send limit — worth an explicit answer on our own compare page if we can source it.
- [ ] 2026-07-19 [IDEA] Both cycle-2 canonical and agency runs (`tools/buyer-panel/runs/2026-07-19-claude-canonical.md`, `-agency.md`) independently cite Smartlead's 116-tool MCP server + Master Inbox API + G2 4.6/5 across 306 reviews as deciding-sentence evidence — confirms Smartlead is THE agent-operability reference competitor buyer agents measure against. Checked `site/guide-mcp-tool-count.html` against this: it already argues the 21-vs-116 tradeoff head-on (intent-level tools vs per-entity-CRUD granularity, token/context cost, a lifecycle-coverage table mapping Smartlead's reported categories to our tools) — no new build needed. One verified discrepancy found while checking: the page's own copy says "21 intent-level tools" (title, H1, callout, `dateModified` 2026-07-14) but the live MCP surface is currently 19 tools (19→21 only after the byo-intake merge, per the `## Open` byo-intake entry above) — same staleness class as the `SPEC.md` "~8-12 tools" note; fix at the byo-intake merge's coordinated tool-count-copy pass, not before. **RESOLVED same-day:** the F-wave coordinated pass shipped later on 2026-07-19 (Worker `d7b300e7` serves 21, site swept to 21, live-verified) — the discrepancy this line describes no longer exists; kept for the forensics record.
- [ ] 2026-07-19 [IDEA] Cycle-2 starter run's own winner (Salesforge) carries a shopper-recorded caveat verbatim: "no product I found does 100% of 'domains + mailboxes + warmup + sending + replies' as one single native SKU with real agent/API access, no forced-capacity tax, and no ownership lock-in, at this budget" (`tools/buyer-panel/runs/2026-07-19-claude-starter.md` §5). That's the exact empty slot ColdRig's single-SKU $99 fits once live-status propagates through search indexes (see the `[ORDER]` above on stale search-cache). Candidate quote for future compare/positioning copy — NEVER attribute it to "a buyer-panel shopper run" or name the internal harness publicly; that's an internal-leak class per the compare-pages adversary precedent (`docs/adversarial/compare-pages-review-2026-07-15.md`).
- [ ] 2026-07-20 [gated:founder] **Watchtower external prober (ACTIVATION Gate 4 step 7) — the one open piece of the ops-email/monitoring block.** Ops email + monitoring itself is ARMED + VERIFIED 2026-07-20 (Email Sending + Routing live-verified, cron confirmed firing, end-to-end alert + support-ticket path verified — full evidence in ACTIVATION.md Gate 4). Founder's ruling: add `https://agent-cold-email-api.yaakovscher.workers.dev/status` (5-min interval, alert → `yaakovscher@gmail.com`) to his EXISTING regular monitoring service, not a new UptimeRobot/BetterStack account. Not yet added.
