# ColdStart — ROADMAP

> Build order + status. Canonical; updated every session that touches the project.
> Program started 2026-07-09. Owner interview complete (SPEC §0). Adversarial panel #1 complete → amendments adopted (`docs/adversarial/panel-01/SYNTHESIS.md`). Autonomous to the finish line; open questions HELD for the final report, never asked mid-build.

## Definition of done — "test-mode CORE complete" (rescoped by panel #1)

The core business exists and runs itself in **test mode**: the pipe (provision→warm→send→reply, sandbox adapters) + control plane + Stripe test-mode billing/metering + agent surface (MCP+CLI+demo) + real-but-minimal compliance guardrails + the business-ops lanes (dunning, teardown, abuse-drop, metrics) + legal docs + a publish-now distribution shell that's already indexing — all adversarially hardened on the REAL surfaces — plus **ACTIVATION.md**, the single checklist of owner-hands steps that flips it live. Un-sandbox-testable *tuning* (live deliverability tuning against real Gmail, warmup optimization) is built-to-contract now and tuned at activation.

## Hardening-budget rule (panel #1)
Adversarial panels apply to what is REAL: control plane, auth, tenancy, billing, isolation logic, compliance, legal, distribution. Deliverability/warmup/AI-ops get contract-level review only until activation — don't burn budget polishing synthetic behavior.

## Phases (B and C-shell run in PARALLEL)

### Phase A — Foundation & plan hardening — DONE
- [x] A1. Canonical home: git repo; SPEC §0 locked; ROADMAP/README/CLAUDE/ARCHITECTURE/HANDOFF/MEMORY
- [x] A2. Adversarial panel #1 (4 opus lenses) — 4× CONDITIONAL_GO; amendments synthesized + adopted
- [x] A2.5 Architecture settled (ARCHITECTURE.md — hybrid topology, DO-SQLite ledger, injected clock, idempotency, engine-as-adapter)
- [x] A3. cold-cli license = MIT (verified via GitHub API 2026-07-09) — clean to fork; reference for engine contract
- [x] A4. $0 vendor-ToS resale + isolation + unit-economics research DONE (docs/research/vendor-tos-economics-2026-07-09.md) → pricing designed (SPEC §18); resale-permission gate recorded (SPEC §13 — Mailforge ToS-clean, Inboxkit needs enterprise deal, or management-service model); Porkbun buy-domain undocumented → Namecheap fallback. NO-GO branch = facade swap, does not block sandbox.
- [x] A5. Local-mailserver engine spike (GreenMail 2.1.3, real SMTP+IMAP in Docker, $0) — DONE 2026-07-12: all 5 contract behaviors (send / reply-detect / threading / bounce-classify / list-unsubscribe round-trip) validated satisfiable against a real server; spike code + README at `spikes/a5-engine-imap/`; root suite stayed 130/130. Surfaced 4 pre-`VendorPort`-freeze findings — see session log.

### Phase B — Core product
- [x] B0. **Walking skeleton DONE:** npm workspaces monorepo (`packages/shared` + `apps/platform`); Hono facade over the ~12 SPEC §6 intents; `TenantDO` (SQLite runtime state + money ledger) behind Workers RPC; injected `RealClock`/`VirtualClock`; sandbox `VendorPort` bundle (deterministic reply/bounce simulator) + `real/` stubs + factory with a demo-can't-reach-real guard; native sandbox engine (warmup ramp, tick, poll/reply-processor, threads, reporting). 9/9 tests green (E2E pipe + cap-enforcement + pause + tenant-isolation + demo-adapter-guard), typecheck clean, `wrangler deploy --dry-run` clean. See session log.
- [x] B1. Monorepo scaffold; control plane: tenants, auth/tokens, Stripe test-mode billing+metering, quotas + per-tenant spend/provisioning caps (distinct from usage quotas)
- [ ] B2. (ACTIVATION-HARDENING backlog — synchronous provisioning WORKS in test mode) Provisioning sagas: resumable alarm-driven jobs on clock abstraction; clock-scaled warmup ramp; idempotency keys; DO-reload/alarm-retry soak tests
- [ ] B3. `VendorPort` layer: fault-injecting sandbox impls (active) + real impls (Porkbun/Inboxkit/Stripe — coded, unactivated); one contract-test suite across both; free/demo tenants structurally can't reach a real adapter (type guard + failing test)
- [ ] B4. Sequencing + reply engine (contract, native sandbox impl): campaigns, scheduling, per-mailbox caps, rotation, stop-on-reply, suppression, **full CAN-SPAM opt-out flow** (in-body + RFC 8058 header + honor windows), subject-honesty + lookalike third-party-brand hard-reject validators, A/B
- [x] B5. Agent surface: hosted MCP (direct JSON-RPC 2.0 handler, ~12 tools) + CLI twin + AGENTS.md + no-signup demo (`POST /demo/run`, sandbox-only, structurally gated to demo/free plans)
- [x] B6. Deliverability control-loop LOGIC + tests (monitor→throttle/pause/rotate/replace, domain-burn auto-replace) — built to contract, tuned at activation. `engine/deliverability.ts` (pure `evaluate` + signal gather) + `engine/deliverability-actions.ts` (apply/sweep/replace), wired into the tick BEFORE scheduling; surfaced in `account().deliverability` + `infrastructure_status` per-mailbox health. 78/78 tests (13 new). NOT deployed (owner's call).
- [x] B7. Unified inbox + reply management — UI half absorbed into **B7.1 Dashboard + Unified Inbox (SPEC §19)** below (founder-directed 2026-07-12); resolved via B7.1's M1-M5 completion, not built standalone.
- [x] B7.1 **Dashboard + Unified Inbox (SPEC §19)** — **DEPLOYED LIVE 2026-07-13**: merged to main (`77741ae`) and shipped to production via `npm run deploy` (worker version `e9734c42-984a-47f3-805a-3c261db4d06c`); live at https://agent-cold-email-api.yaakovscher.workers.dev/app (same-origin via the Worker's `[assets]` config). Founder-directed 2026-07-12, absorbs/implements B7's UI half. Spec SHIPPED (adversarial verdict SHIP after 2 rounds — `docs/adversarial/dashboard-spec-review-2026-07-12.md`). M1-M5 all DONE 2026-07-13, committed `c9e5e27` on branch `feat/dashboard-inbox-spec-19`, merged and deployed same day — remaining open item is pushing `main` to GitHub (see 2026-07-13 (deploy) session-log entry). Milestones (§19.8):
  - [x] M1 backend — serving spike (`[assets]` config + wrangler-dev gate) → TenantDO schema bootstrap + `last_polled_at` → shared zod layout schema/registry → inbox v2 (composite cursor) → listCampaigns/activity → views CRUD + rev CAS → D1 `0006_dashboard_sessions` cookie session + global CSRF middleware → MCP tools 13–15. DONE 2026-07-12, 190/190 tests (148→190, +42), serving spike proven (401-JSON API / SPA fallback / 307 on /app / JSON 404), cookie-session + global CSRF + inbox v2 (composite cursor, no-N+1 asserted) + views CRUD w/ rev CAS + MCP tools 13-15.
  - [x] M2 dashboard SPA shell + widgets (tokens, grid renderer, widget registry v1, provenance badges, Settings) — DONE 2026-07-13.
  - [x] M3 inbox (virtualized paginated list, filters, thread detail, composer, labels, keyboard + swipe + undo, Cmd+K) — DONE 2026-07-13.
  - [x] M4 agent-control e2e + saved views + dark mode + a11y pass — DONE 2026-07-13.
  - [x] M5 perfection loop (screenshot 1440/390 → design-review → fixes → repeat; verifier battery; adversary re-attack gate) — DONE 2026-07-13, two rounds (R1 defects A-I; R2 7 more items) closed on top of a fresh-context adversarial security re-attack (SHIP) + fresh-context UX review (SHIP-AFTER-FIXES, fixes landed in R2) + a full verification battery, all confirmed clean before commit.

### Phase C — Distribution machine (C-shell parallel with B; deep content after product is describable)
- [x] C0. **Identity vs brand split:** keyword-permanent slug `agent-cold-email` for repo/npm/registry (never renamed); coldrig/coldpipe/coldloop = domain/display brand only, at activation
- [x] C1. Public GitHub repo (LIVE https://github.com/YS-projectcalc/agent-cold-email, public, 11 topics) (keyword slug, under YS-projectcalc; AGENTS.md + README at agents' literal queries; guardrails prominent so abuse-scanners read it as compliance-first infra)
- [x] C2. Docs + AEO content on Pages/github.io (crawlable, authority-accruing): AGENTS.md + OpenAPI + JSON-LD (the real assets) + llms.txt (convenience); not-yet-available + no-deliverability-guarantee disclosures; privacy policy BEFORE any waitlist email
- [x] C3. Agent-facing free first-use (npx demo works vs live sandbox via /demo/run): `npx agent-cold-email demo` runs full pipeline vs live sandbox, no signup; lead capture + conversion handoff at the end
- [ ] C4. SEO/AEO comparison + how-to pages targeting agent queries (after product describable)
- [~] C5. (server-card served; registry submissions + npm publish + awesome-PRs = ACTIVATION, identity-gated)  `/.well-known/mcp/server-card.json` served; MCP registry listings (Smithery/mcp.so/PulseMCP) submitted once identity exists + scan is clean; npm publish + awesome-list PRs = ACTIVATION.md (identity-gated)
- [x] C6. Waitlist + early-access flow: `POST /api/waitlist` (KV-backed, dedupe, per-IP rate limit, CORS for the Pages origin) live, wired to the site form built earlier. Distribution validation harness (fresh agent + query → discovers us?) still open — not yet baselined.

### Phase D — Business automation & the missing lanes (panel #1 add)
- [x] D1. AI support lane: support inbox → agent triage → auto-answer/escalate → daily digest (armed at activation). `POST /admin/support/triage` (classify billing/deliverability/how-to/abuse-report/other, draft an FAQ answer from a built-in KB grounded in SPEC §6/§18/§7/§10, or escalate) + `GET /admin/support/digest`. Real inbound email (Cloudflare Email Routing → this endpoint) is the ACTIVATION step, per the line above.
- [x] D2. Ops routines: deliverability loop, metrics/watchdog digest, **dunning/failed-payment sweep**. `POST /admin/ops/dunning-sweep` (scans `past_due` tenants, retry→escalate→suspend by failure-count cycle, idempotent per cycle, `suspend` is a real local state transition now) + a `scheduled()` Cron Trigger export (`src/scheduled.ts`: deliverability sweep for every tenant → dunning sweep → digest) — the `[triggers]` block is commented-out in `wrangler.toml`, armed at activation; `scheduled()` itself is built and independently callable now.
- [ ] D3. Legal: ToS/Privacy/AUP to the specified clause inventory (customer-is-sender + reps, indemnification, vendor-AUP pass-through, monitoring consent, no-deliverability-warranty+LoL, prohibited-use, ROSCA/state-ARL auto-renewal, DPA) — enforcement path built+tested, not paper; DRAFT-flagged for attorney review
- [~] D4. (address/sender-identity capture DONE in setup_infrastructure; OFAC screen + friction ladder = ACTIVATION, needs real signups) Onboarding: self-serve signup → Stripe test checkout → token → agent instructions; **OFAC screen + friction ladder + new-tenant volume ramp**; per-tenant physical address + sender identity capture
- [x] D5. Lifecycle ops: voluntary cancellation + infra teardown/reclaim (+ annual-domain liability accounting); abuse-offboarding ops executing the abuse-drop ToS (terminate + reclaim + honor suppression); chargeback/dispute lane. BUILT: `POST /cancel` (tenant-authed — end-of-period `canceling` / immediate `canceled`, releases domains+mailboxes via `DomainPort`/`MailboxPort.release`, stops campaigns, books per-domain annual liability, idempotent) + `POST /admin/tenants/:id/terminate` (ADMIN_TOKEN — suspend + same teardown path + retain suppressions + D1 `enforcement_actions` audit + token lockout so a terminated tenant can't re-provision) + `charge.dispute.created/.closed` webhook cases (freeze `disputed` → tick send-freeze; won→unfreeze/resume; idempotent by event id via `webhook_events`) + digest lifecycle counts (canceled/terminated/disputed + total annual-domain liability). Deferred to ACTIVATION.md: real registrar/mailbox release calls + real Stripe dispute→tenant routing (sandbox ports execute the release now; test payloads carry `metadata.tenantId`).
- [~] D6. Owner business-health substrate: MRR, active tenants, margin/tenant, provisioning-failure + incident rate; status page; backups/DR for the ledger + encrypted creds; master-key storage/rotation/recovery. BUILT: `GET /admin/ops/digest` (MRR estimate, active-tenants-by-plan, total usage, provisioning-failure count [honestly 0 — B2 sagas not built], windowed deliverability-action counts, open/escalated support tickets, past_due count, watchdog alerts) + public `GET /status`. NOT built (separate concern, deferred): per-tenant margin, ledger backups/DR, encrypted-creds/master-key storage+rotation.

### Phase E — Hardening & finish
- [x] E1. Adversarial panels: #1 (plan) ✔, #2 (live surfaces) ✔, #3 (final/whole-system) ✔ — all 3 remediated + redeployed + live-verified. Records in docs/adversarial/panel-0{1,2,3}/.
- [x] E2. Full verification battery: 130/130 tests green; every lane deployed + live-smoked; 3 adversarial panels all clean-after-fix.
- [ ] E3. Memory + ledger current; HANDOFF.md maintained
- [x] E4. Final report: name-candidate presentation, pricing rationale, ACTIVATION.md (first activation gate = the real-world deliverability smoke test), held-questions list

## Session log
- 2026-07-09: Program start. Interview (10 Qs) locked; SPEC §0 written; repo + governance docs (CLAUDE/ARCHITECTURE/HANDOFF/MEMORY) initialized.
- 2026-07-09: Adversarial panel #1 (4 opus lenses vs SPEC+ROADMAP) → 4× CONDITIONAL_GO. Synthesis in `docs/adversarial/panel-01/`. Adopted: hybrid topology (engine off-Worker), DO-SQLite money ledger, injected clock + fault-injecting sandbox, idempotency keys, compliance-as-code (per-tenant address/identity, CA 17529.5, full opt-out, OFAC/friction ladder, demo type-guard), identity-vs-brand split (keyword-permanent slug), DoD rescoped to test-mode CORE, C-shell parallel with B, hardening-budget rule, +11 missing business lanes (dunning/teardown/abuse-ops/chargeback/health-metrics/DR/key-rotation). A3: cold-cli = MIT ✓. Next: A4 vendor research (running) + A5 engine spike, then B0 skeleton.
- 2026-07-09: **B0 walking skeleton built + verified.** Monorepo: `packages/shared` (domain types, `Clock`/`VendorPort` interfaces, zod intent schemas) + `apps/platform` (Hono facade, `TenantDO` w/ SQLite ledger via Workers RPC, `RealClock`/`VirtualClock`, sandbox `VendorPort` bundle + `real/` NotActivatedError stubs + demo-guarded factory, native engine: warmup ramp, tick, reply-processor, threads, reporting). D1 `coldstart-platform-db` created (`wrangler d1 create`, id `119be39d-05c3-4608-b628-b2cc7bddfac1`). Full flow proven end-to-end (signup→setup_infrastructure→warmup ramp via injected clock→launch_campaign→tick sends→sandbox poll returns 1 reply+1 bounce→stop-on-reply+bounce-suppression→inbox/thread/reply→metrics/account) plus per-mailbox daily-cap enforcement, pause/pause_all, tenant isolation (separate TenantDO per tenant), and the demo-can't-reach-real-adapter guard. `npm test`: **9/9 passed** (3 files). `npm run typecheck`: clean. `npx wrangler deploy --dry-run`: clean (TENANT DO + DB bindings resolved). Deliberately deferred (documented in code comments, in-scope for later phases): async resumable provisioning sagas/DO alarms (B2), Stripe test-mode wiring beyond a stub `BillingPort` (B1), full IANA timezone send-window handling (later), MCP/CLI surface (B5). Noted mid-build: this worktree is shared/live — a concurrent commit (`74e4e9f`) swept up in-progress B0 files via an automated `git add -A`; per standing git-discipline rules this agent left the final `git add`/`commit` to the orchestrator rather than committing itself. Next: A5 engine spike, B1 control-plane hardening (real auth/quotas/billing), B2 sagas.
- 2026-07-09: **B0 DEPLOYED LIVE (test mode)** → https://agent-cold-email-api.yaakovscher.workers.dev (Cloudflare free tier, sandbox vendors, remote D1 migrated, TOKEN_HASH_PEPPER secret set). Live smoke green: signup→provision(2 domains/4 mbx warming, day-1 caps)→campaign→metrics→account; tenant isolation verified on live URL (cross-tenant=404, no-token=401). NOTES for later lanes: (1) sandbox tick/clock/pollInbox are DO-RPC-only, NOT HTTP — C3 demo needs a sandbox-safe run endpoint to drive the full pipe over HTTP; (2) B0 signup only mints plan:demo — paid-plan + Stripe path is B1.
- 2026-07-09: **D1/D2/D6 business-ops automation built + verified (NOT deployed, owner's call).** New `src/admin/` surface (support-kb, dunning, db, ops-sweep, schemas + README) + `src/routes/admin-support.ts`/`admin-ops.ts`/`status.ts` + `src/require-admin-auth.ts` (timing-safe `ADMIN_TOKEN` bearer check, fails closed) + `src/scheduled.ts` (Cron Trigger entry point, `wrangler.toml` `[triggers]` commented-out) + `migrations/0002_admin_ops.sql` (`support_tickets`, `dunning_events`) + `TenantDO.opsSummary()`/`deliverabilitySweep()`/`suspendForDunning()` RPCs (`src/engine/ops-summary.ts`). Caught+fixed live during build: Hono composes EVERY registered handler matching a request path into one chain regardless of "sub-app" — an unscoped `admin.use("*", requireAdminAuth)` 401'd every tenant route too; fixed by scoping to `admin.use("/admin/*", ...)` (see `src/index.ts` comment; reproduced red via a temporary cp-backed revert of `require-admin-auth.ts`, confirmed green after restore). `npm test`: **102/102 passed** (23 files, 24 new: `admin-auth`/`admin-support`/`admin-dunning`/`admin-digest`/`admin-unit`/`scheduled`/`status`.test.ts). `npm run typecheck` (root + platform): clean. `npx wrangler deploy --dry-run`: clean. Deferred to ACTIVATION.md: real inbound support email (Cloudflare Email Routing), the cron trigger arm, real dunning retry emails. Deferred to a later session (separate D6 sub-scope, not in this brief): per-tenant margin, ledger backups/DR, master-key rotation.
- 2026-07-09: **B5 agent surface + C6 waitlist SHIPPED + DEPLOYED LIVE.** (1) `POST /mcp` — a direct hand-rolled JSON-RPC 2.0 handler (NOT the Agents SDK `McpAgent`; ARCHITECTURE.md #7 superseded by this brief's "thin facade" call for the transport only, tool contract unchanged): `initialize`/`notifications/initialized`/`tools/list`/`tools/call`, 12 tools matching AGENTS.md exactly, `inputSchema` via zod v4's native `z.toJSONSchema()` (no new dependency). Auth resolves the tenant FRESH from the bearer token on every `tools/call` (`resolveTenantFromToken`, shared with the HTTP `requireAuth` middleware) — no caching, proven by an interleaved two-token isolation test. (2) `POST /demo/run` — synchronous sandbox-only pipeline run (advance clock past warmup → launch a canned deterministic-reply/bounce campaign → tick → poll → tick/poll again to prove stop-on-reply), structurally gated to demo/free plans via `TenantIsolationError` thrown inside `TenantDO.demoRun()` (403), not an HTTP-layer check. (3) `packages/cli` (`agent-cold-email`, not yet published — builds locally via `npm run build -w agent-cold-email`, plain tsc→NodeNext ESM, zero runtime deps): `demo` hero command (signup→setup→`/demo/run`→human-readable summary+honest sandbox disclaimer) + signup/setup/status/campaign/inbox/metrics/pause/account thin wrappers, `--token`/`AGENT_COLD_EMAIL_TOKEN`, exponential-backoff polling. (4) `POST /api/waitlist` — KV-backed (`WAITLIST` namespace created via `wrangler kv namespace create`), email dedupe, per-IP sliding-window rate limit (5/60s), CORS for `https://agent-cold-email.pages.dev`. AGENTS.md updated (MCP no longer "once live"; CLI publish status disclosed honestly). Tests: 28/28 green (`npm test`), up from 9/9 — new: `mcp.test.ts` (9, incl. two-token interleaved isolation), `demo-run.test.ts` (4, incl. non-demo-plan 403 via a test-only `mintTenant` helper), `waitlist.test.ts` (6, incl. dedupe-doesn't-overwrite + rate-limit-then-403). `npm run typecheck`: clean, all 3 workspaces (`@coldstart/platform`, `agent-cold-email`, `@coldstart/shared`). `npx wrangler deploy` → live, Total Upload 666.41 KiB / gzip 109.45 KiB, bindings TENANT/WAITLIST/DB all resolved. Live-smoke (curl) green: initialize→serverInfo, tools/list→12 tools, tools/call setup_infrastructure→jobId, `/demo/run`→sent:4/replies:1/bounces:1/stopOnReplyProof true, waitlist POST→ok:true + OPTIONS preflight→CORS headers. CLI `demo` run against the live deployed API: 2 domains/4 mailboxes provisioned, sent:4/replies:1/bounces:1, stop-on-reply proof, honest "ran in a sandbox" closing line. Deliberately deferred (B5 was scoped to the agent surface, not real sending): Stripe/paid-tier wiring (B1), DO-alarm-driven scheduling (B2), MCP registry submissions + npm publish (both identity-gated, C5/ACTIVATION.md).
- 2026-07-09: **B5 + distribution SHIPPED LIVE.** Site LIVE https://agent-cold-email.pages.dev (AEO shell, all assets 200). Public repo LIVE https://github.com/YS-projectcalc/agent-cold-email (11 topics, AGENTS.md/README/LICENSE/site). Worker redeployed with /mcp (12 tools, JSON-RPC streamable-HTTP, fresh-per-request tenant resolution), /demo/run (sandbox-only accelerated pipe), /api/waitlist (KV+CORS+rate-limit), packages/cli 'agent-cold-email' (demo verified live: sent:4/replies:1). 28/28 tests. Commit 6187f14. Adversarial panel #2 (4 opus lenses vs live surfaces) RUNNING.
- 2026-07-09: **Panel #2 fully remediated + shipped.** Wave 1 (11 platform fixes, 50/50 tests, 5 revert-proven) + Wave 2 (site honesty). Worker redeployed (v 278497e6, +RateLimiterDO); site redeployed; repo pushed. Live-verified fixes: brand="Google"→400 (was provisioning trygoogle.com), token cs_test_, 10MB body→413, signup 3×201→429 rate limit, demo throttle. security-isolation lens was CLEAN (no cross-tenant leak). Records: docs/adversarial/panel-02/.
- 2026-07-09: **B1 money path built (NOT yet deployed — pending review).** `TenantPlan` grew concrete paid tiers (`"demo" | "free" | "launch" | "growth" | "scale"`, replacing the placeholder `"paid"`); `packages/shared/src/pricing.ts` is the single `PLAN_QUOTAS` source (SPEC §18: Launch 2dom/5mbx $99, Growth 6/20 $299, Scale 18/60 $799). (1) **Checkout:** `POST /checkout {plan}` (authed) — real Stripe TEST-mode Checkout Session (subscription mode, inline `price_data`, tenantId in `client_reference_id` + `metadata`/`subscription_data.metadata`) when `env.STRIPE_SECRET_KEY` is set (`src/billing/stripe-client.ts`, coded fully, inert/UNVERIFIED — no key anywhere); else a simulated session (`checkout_sessions` table) completed via unauthenticated `GET /checkout/simulate?tenant&session` (session id is the credential, tenant-scoped, idempotent-on-replay, cross-tenant hijack → 404). (2) **Quotas + spend cap:** `engine/quota.ts` `assertWithinProvisioningCap` — paid tiers rejected (400) past PLAN_QUOTAS cumulative domains/mailboxes; demo/free get a separate flat `SANDBOX_PROVISIONING_CAP` (5 domains/15 mailboxes) as the "distinct from usage quota" runaway guard (demo/free real spend is already 0 via the existing vendor-factory structural guard). `account()` now returns `billingState`, `sends`, and `quota`. (3) **Metering:** per-mailbox/mo fee (600c, paid tiers only, idempotent on the mailbox's provisioning key) added alongside the existing per-send fee (2c); both aggregate into `account().usageCents`; `engine/billing.ts#reportUsageToStripeIfConfigured` codes the Stripe usage-record reporting call (inert without a key + a stored subscription id). (4) **Webhook:** `POST /webhooks/stripe` (unauthenticated — HMAC `Stripe-Signature` verification when `env.STRIPE_WEBHOOK_SECRET` is set, else test-sim accept) handles `checkout.session.completed` / `customer.subscription.updated` / `.deleted` / `invoice.payment_failed`, idempotent per Stripe event id (`webhook_events` table, `INSERT OR IGNORE` claim). New tables: `checkout_sessions`, `webhook_events`; new `tenant_profile` columns `billing_state`/`stripe_customer_id`/`stripe_subscription_id` (backfilled via the existing `ensureColumnMigrations` pattern for already-deployed DOs). New dir `src/billing/` (+README). Tests: **65/65 green** (`npm test`, 14 files, up from 50/50) — new: `checkout.test.ts` (5), `webhook.test.ts` (4), `quota.test.ts` (4), `metering.test.ts` (2); fixed 3 pre-existing `"paid"` literal usages (now `"launch"`) broken by the TenantPlan type change. `npm run typecheck`: clean, all 3 workspaces. `npx wrangler deploy --dry-run`: clean (same 4 bindings, no new resources needed — Stripe keys are plain secrets, not bindings). Deliberately deferred/UNVERIFIED: the real-Stripe checkout/webhook-signature/usage-report code paths are coded-to-docs but never exercised against a live Stripe account (no test key in any environment here — that's the ACTIVATION.md step); resolving a Stripe subscription's line-item id for usage reporting (currently reports against the stored subscription id with a code comment flagging the activation-time gap); MCP tool surface NOT extended with a `checkout` tool (out of this brief's explicit HTTP-facade scope). Left uncommitted per shared-worktree git discipline — orchestrator's call.
- 2026-07-09: **B1 money path SHIPPED LIVE** (v ba24f7d3, 65/65 tests). Live-verified: signup→demo (sandbox quota 5/15), POST /checkout growth→simulated session→complete→plan upgraded to growth (quota 6 domains/20 mbx), over-quota setup→400. Real Stripe test-mode path coded+inert (STRIPE_SECRET_KEY=activation). Idempotent webhook. Commit 1a05959.
- 2026-07-09: **B6 deliverability control loop built (NOT deployed — owner's call).** SPEC §10 monitor→decide→act as sandbox LOGIC + tests. (1) `engine/deliverability.ts` — a PURE `evaluate(mailboxHealth[], domainStats[], thresholds) -> Action[]` (THROTTLE/PAUSE/ROTATE/REPLACE_DOMAIN) + `gatherMailboxHealth`/`gatherDomainStats` that compute first-party per-mailbox/per-domain bounce+complaint RATES from the event log, in FRACTION units (Gmail 0.30%=0.003, NOT 0.30 — the 100x trap is documented + guarded). Thresholds grounded in SPEC (Gmail 0.30% ineligibility; §7 caps; §7/§15 domain burn 8-18%/mo normal → auto-retire-replace at the top of that band). (2) `engine/deliverability-actions.ts` — `applyActions` mutates DO state: THROTTLE via a `cap_override` column (survives the per-tick warmup-cap recompute), PAUSE via a `deliv_status` column DISTINCT from warmup status (so a warmup refresh can't resurrect it), REPLACE_DOMAIN marks the domain 'burning' + pauses its mailboxes + auto-provisions a replacement via the shared `provisionDomainWithMailboxes` helper (extracted from setup_infrastructure, DRY) + re-parks sends; every action audited to a new `deliverability_actions` table. `runDeliverabilitySweep` runs in the tick BEFORE scheduling; the send picker excludes `deliv_status='paused'` (this realizes ROTATE — pending sends aren't pre-bound to a mailbox, so the picker reroutes them). (3) **Fault injection:** sandbox EmailPort emits a spam-complaint for "complaint"-tagged recipients (mirrors its reply/bounce triggers); reply-processor `processComplaint` suppresses + attributes per-mailbox via the original send's message id. (4) **Safety:** re-pausing a paused mailbox is a no-op (conditional UPDATE, no dup audit); domain-replace is per-window rate-capped (MAX 3/30d) so a replacement that also burns can't spawn forever (capped ones still retire, logged REPLACE_DOMAIN_CAPPED for owner intervention). (5) **Surfaced:** `account().deliverability` (paused/throttled/burning/replaced counts + recentActions[]) + `infrastructure_status` per-mailbox health gains delivStatus/complaint+bounce rates/vendor reputation+placement (getHealth, on-demand not on the hot path). 12-tool surface UNCHANGED (extended `account`+`infrastructure_status` responses + their AGENTS.md rows). Tests: **78/78 green** (up from 65/65) — new `deliverability.test.ts` (8 pure-`evaluate` units: throttle/pause/replace/rotate/idempotent-repause/idempotent-rethrottle/burn-by-bounce/min-sample-gate) + `deliverability-loop.test.ts` (5 integration: headline end-to-end complaint→pause→burn→replace→rotate through the REAL send/poll pipe; throttle-survives-warmup-refresh; idempotent re-pause no-dup; replacement cap 3+1-capped; ROTATE routes to healthy box). Revert-fail-restore PROVEN on 2 interaction guards: without `cap_override` handling the throttle test fails "expected 40 to be 5"; without the tick's paused-exclusion the headline+rotate tests fail (paused box still sends). typecheck clean (3 workspaces); `wrangler deploy --dry-run` clean (same 4 bindings, no new resources). **Deferred to activation (real-signal tuning, per hardening-budget rule):** threshold VALUES tuned against live Gmail (built-to-contract now); vendor getHealth batching/caching if moved onto the hot path; a mailbox/domain RECOVERY/un-pause path (loop only degrades today — recovery is a live-signal concern). Left uncommitted per shared-worktree git discipline.
- 2026-07-09: **D5 tenant lifecycle built + verified (NOT deployed — owner's call).** Four coherent pieces. (1) **Voluntary cancel** `POST /cancel` (tenant-authed, `engine/lifecycle.ts`): end-of-period `canceling` default / immediate `canceled`; shared `teardownTenant` releases every domain (`DomainPort.release`) + mailbox (`MailboxPort.release`, sets `released_at` + `deliv_status='paused'`), stops campaigns (reuses `pauseAllCampaigns`), books a per-domain annual-domain liability as its own idempotent `kind='liability'` ledger row (SPEC §12 .com $11.08/yr, prorated by remaining term); `account().teardown` surfaces the summary; idempotent via a `teardown_records` anchor (re-cancel = no-op). (2) **Abuse offboarding** `POST /admin/tenants/:id/terminate` (ADMIN_TOKEN, reuses `require-admin-auth`): suspend + same teardown, **retains suppressions** (CAN-SPAM opt-outs survive), logs reason+evidence to D1 `enforcement_actions` (idempotent per tenant, migration 0003), and locks the D1 `tenants_index` token so a terminated tenant can't re-provision to undo the reclaim. (3) **Chargeback lane** `charge.dispute.created/.closed` in `engine/billing.ts`: freeze `billing_state='disputed'` (a new `runTick` guard stops ALL sends for suspended/disputed tenants — reversible), record to a per-DO `disputes` table (keyed on Stripe dispute id), idempotent by event id via the existing `webhook_events` dedupe; `.closed(won)` unfreezes + resumes, `.closed(lost)` stays frozen. (4) **Digest** gains canceled/terminated/disputed counts + total annual-domain liability. New `release()` on `DomainPort`/`MailboxPort` (sandbox executes, real stubs throw `NotActivatedError`). Tests **111/111** (up from 102, 4 new files: `lifecycle-cancel`/`admin-terminate`/`dispute`/`lifecycle-digest`), incl. the required cross-tenant teardown-isolation (A terminate leaves B's infra untouched), chargeback idempotency, and re-cancel idempotency. Revert-fail-restore PROVEN (cp-backed) on 2: chargeback idempotency (disable the `webhook_events` dedupe early-return → redelivered dispute returns applied:true not duplicate:true → fail → restore→pass) and teardown reclaim (disable the release UPDATEs → A's `liveDomains` 2≠0 ground-truth assertion fails → restore→pass; cross-DO isolation itself is architectural — a DO cannot reach another tenant's SqlStorage). `npm install`/`npm run typecheck` (3 workspaces): clean. `npx wrangler deploy --dry-run`: clean (same 4 bindings, no new resources). **Deferred to ACTIVATION.md:** real registrar/mailbox release HTTP calls (sandbox ports execute the release now) + real Stripe dispute→tenant routing (test payloads carry `metadata.tenantId`; the live charge→payment_intent→subscription-metadata resolution is the activation step). Left uncommitted per shared-worktree git discipline.
- 2026-07-09: **D5 lifecycle SHIPPED LIVE** (v a59ed5bb, 111/111). Cancel+teardown (release infra + annual-domain liability + idempotent), admin terminate (suspend+teardown+retain suppressions+lock D1 token — caught cross-store enforcement gap), chargeback freeze (idempotent + stops sends). migration 0003 applied. Owner digest +lifecycle. Commit 47e3382.
- 2026-07-09: **Business now runs itself end-to-end in test mode.** Live surfaces: platform pipe + deliverability AI loop + billing/quotas + agent surface (MCP/CLI/demo) + support triage + dunning + owner digest + lifecycle/abuse/chargeback + distribution machine (repo/site/AEO/5 guides) + legal. 111 tests. Next: final adversarial panel #3, then FINAL-REPORT close.
- 2026-07-09: **Panel #3 platform fixes built + verified (NOT deployed — owner's call).** Remediated all money/security/correctness findings (`docs/adversarial/panel-03/`) as a coherent billing-state machine, not one-offs — new `engine/billing-state.ts` centralizes the FROZEN set (`disputed`/`canceling`/`canceled`) + `isLifecycleFrozen(status,billing_state)` shared by the tick, deliverability sweep, and setup/launch guards. Fixes: **#1** `/webhooks/stripe` now FAILS CLOSED with no `STRIPE_WEBHOOK_SECRET` (503) instead of trusting an unsigned event's `metadata.tenantId` (was an anonymous free-plan-upgrade + freeze-any-tenant vector); the test suite gained a configured test secret + signed fixtures (`postWebhook`). **#2** `disputed`/`canceled`/`canceling` are STICKY — `checkout.session.completed`/`subscription.updated(active)`/`invoice.payment_failed`/simulated-checkout guard their UPDATEs so a routine event can't lift a chargeback freeze; only a won dispute clears `disputed`, only an explicit re-checkout clears `canceled`. **#3** `runDeliverabilitySweep` gained the freeze guard (protects the standalone RPC + cron lane, not just the tick — a frozen tenant's burning domain no longer buys a REPLACE_DOMAIN). **#4** quota counts only LIVE resources (`domains status='active'`, `mailboxes released_at IS NULL`) so a re-subscriber isn't locked out; `clearTeardownRecord` on re-checkout re-arms teardown so a second cancel releases the NEW infra + books its liability (was a permanent tombstone leaking vendor spend). **#5** `canceled`/`canceling` added to the tick freeze AND `setup_infrastructure`/`launch_campaign` reject when lifecycle-frozen (a canceled tenant can't re-provision/relaunch/send). **#6** billing-recovery (`subscription.updated(active)`/`checkout.completed`/dispute won) un-suspends a DUNNING suspension via a new `suspend_reason` column, never an abuse TERMINATE. **#7** end-of-period cancel DEFERS teardown (infra stays live through the paid period; reclaim-at-boundary is a DO-alarm/cron reaper = B2/activation, noted); immediate teardown only for `immediate:true`. **#8** Content-Length body cap on `/webhooks/stripe` + `/cancel` (413). **#9** waitlist emails moved from TTL'd KV to a durable D1 `waitlist` table (migration **0004**, no expiry) + owner visibility (`buildOpsDigest.waitlist.count` + `GET /admin/ops/waitlist`). **#10** `startCheckout` reuses a pending session per (tenant,plan) instead of unbounded INSERTs. **#11** MCP `tools/list` uses `z.toJSONSchema(..., { io: "input" })` so `.default()` fields (company/timezone/sendWindow/stopOnReply) are optional, matching runtime. New `tenant_profile.suspend_reason` column (via `ensureColumnMigrations`) + D1 migration 0004. Tests: **130/130 green** (`npm test`, 30 files, up from 111) — new `webhook-security`/`lifecycle-freeze`/`lifecycle-reactivation`.test.ts + finding tests folded into `checkout`/`mcp`/`waitlist`/`lifecycle-cancel`/`lifecycle-digest`. **Revert-fail-restore PROVEN (cp-backed)** on the LIVE-PROVEN findings: #1 (old→200 not 503), #2 (old→freeze lifted to active/past_due, ×3), #3 (old→REPLACE_DOMAIN emitted), #4a (old→re-provision 400) + #4b (old→2nd cancel alreadyCanceled=true, infra leaks), #5 (old→setup 202 not 400). `npm run typecheck` (3 workspaces): clean. `npx wrangler deploy --dry-run`: clean (same 4 bindings). **Deferred (noted, not built):** the end-of-period teardown reaper + the tick send→bill saga reaper are both B2/activation (money-billing.json LOW finding #6). Left uncommitted per shared-worktree git discipline — orchestrator's call.

## PROGRAM COMPLETE (test-mode CORE) — 2026-07-09
The entire business is built, deployed, and running in test mode, hardened through 3 adversarial opus panels (all remediated + live-verified). 130/130 tests. Live: API https://agent-cold-email-api.yaakovscher.workers.dev · site https://agent-cold-email.pages.dev · repo https://github.com/YS-projectcalc/agent-cold-email. Owner go-live path = ACTIVATION.md; owner summary = FINAL-REPORT.md.
Remaining = ACTIVATION-hardening backlog (owner-gated or refinement of working systems): B2 resumable sagas + end-of-period/send-bill reapers, D4 OFAC, A5 engine spike, D6 DR/key-rotation, real Stripe+vendor+engine wiring. All documented; none block the test-mode business.
- 2026-07-09: **Panel #3 (final gate) remediated + shipped** (v 13f8ee36, 130/130). Wave 1: billing/lifecycle state machine (webhook fail-closed, sticky disputed/canceled, deliverability-sweep freeze, quota-live-only, teardown-epoch, cancel-blocks-writes, dunning reactivation, body caps, waitlist-durable+digest, MCP input-mode) — 5 live-proven HIGHs revert-proven. Wave 2: clean-URL AEO (no 308 hops). Live-verified: webhook forge→503, admin gated→401. Records docs/adversarial/panel-03/.
- 2026-07-12: **Agent search-behavior panel run** (8 fresh-context Claude probes, 5 sonnet + 3 opus, real web searches) to ground what a coding agent naturally queries when asked to stand up cold email. Record: `docs/research/agent-search-queries-2026-07-12.md`. Headline: agents search category keywords + incumbent comparisons, not brand names — "cold email MCP server" was a universal query (8/8) and an "Instantly vs Smartlead" comparison was also universal (8/8), while 0/8 discovered agent-cold-email (unindexed, expected). Keyword-name beats brand-name for agent-driven traffic, reinforcing the existing keyword-permanent-slug naming decision.
- 2026-07-12: **Resale legal model DECIDED** (verbal, in-session): start with Mailforge (option b) as the activation-time real mailbox vendor while pursuing an Inboxkit enterprise/reseller agreement (option a) in parallel; management-service (option c) stays under active evaluation as a possible additional offering tier. Recorded in ACTIVATION.md Gate 0/1/2 + HANDOFF.md. Three analysis lanes launched in parallel: brand-name adversarial review of "agentcoldemail as the brand" (name still undecided), a competitor pricing sweep, and a Mailforge/Inboxkit cost deep-dive feeding the still-pending pricing sign-off.
- 2026-07-12: **Three analysis lanes completed.** (1) Brand-name adversarial review → **NO-SHIP** on the pure-keyword "agentcoldemail" candidate; hybrid structure (distinct brand + permanent `agent-cold-email` keyword slug) confirmed as the correct — and already-shipped — design. Record: `docs/adversarial/name-review-2026-07-12.md`. (2) Competitor pricing landscape frozen. Record: `docs/research/pricing-landscape-2026-07-12.md`. (3) Mailforge/Inboxkit COGS frozen — headline finding: Mailforge's advertised $3/mbx excludes warmup (separate Warmforge product, $10/mbx), pushing standalone all-in to ~$13.5/mbx; a Salesforge-bundle "unlimited warmup slots" escape hatch is unverified at reseller scale (new ACTIVATION.md Gate 1 item). Record: `docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md`. ACTIVATION.md and HANDOFF.md updated accordingly.
- 2026-07-12: **Two conversation-level design conclusions folded here as the canonical record** (support the pending founder decisions; not yet built): (a) **Two-tier vendor positioning** — if option-c ships as an additional tier, the menu is a genuine good/better ladder, not arbitrage: Standard (Mailforge under the hood) = fastest setup, one signup, one blended price, budget shared-IP infra; Dedicated/Pro (customer-owned Inboxkit account, we operate) = real Google/M365 mailboxes, own reputation universe, customer owns the assets (anti-lock-in), transparent pass-through infra + explicit management fee — priced at a PREMIUM (its buyers are placement-sensitive, not price-sensitive). Build prerequisite: per-tenant vendor credentials in the VendorPort facade (currently single platform-owned account). (b) **Warmup COGS strategy ladder** for the Mailforge path — never default to $10/mbx Warmforge forever: (1) if the Salesforge $80/mo "unlimited Warmforge slots" bundle verifies at reseller scale (Gate-1 item), take it — warmup ≈ $0 marginal; (2) else use vendor warmup as a RAMP ONLY (~first 3-4 weeks per mailbox, slots rotated) since research says warmup value is front-loaded and pool-based warmup is increasingly detected via graph topology (SPEC §9); (3) at tenant critical mass, run the internal tenant-to-tenant warmup pool under our AI mimicry layer (the warmbly-fork L2 option pulled forward) — zero vendor cost. Real replies remain the durable reputation engine either way.
- 2026-07-12 (later): **Option-c APPROVED as the additional premium "Dedicated" tier** (founder, verbal: "that dedicated tier makes sense") — recorded in ACTIVATION.md Gate 0; fast-follow build prereq = per-tenant VendorPort credentials. **Option-B blast-radius rationale (why shared-IP Mailforge is an acceptable start, the analysis behind the resale decision):** one killed email / complaint = survivable background noise (Google's threshold is a 0.3% spam RATE, not a count); one burned domain = contained to that tenant (8–18%/mo burn priced in, B6 loop auto-replaces); the real exposure is shared-IP pool reputation from OTHER Mailforge customers we don't control — degradation-not-termination, i.e. a churn/quality risk, not existential; mitigations = B6 per-mailbox monitoring/rotate/replace + enforcement ladder + VendorPort migration path + the Dedicated tier as the in-house upgrade for placement-sensitive customers. Related L2 note: private warmup pools showed a +20–30% edge in the 07-09 research — the number backing "verticalize the pool later." **SEO/AEO backlog now has its own frozen derivation record** with the per-item arguments (tool-count-heuristic counter, assembly-question guide, maintenance signals): `docs/research/aeo-backlog-2026-07-12.md` — still pending owner green-light. **Correction pointer:** `docs/research/agent-search-queries-2026-07-12.md` line "These 44 issued queries" undercounts — the frozen per-probe lists actually total **46** (6+5+7+6+6+5+5+6); 46 is the correct harness count (per this log + HANDOFF); the frozen doc body is left unedited per frozen-record discipline. **Salesforge→Warmforge bundle desk-verification launched** (public ToS/docs pass; support-inquiry draft if unresolvable) — result lands in this repo when done.
- 2026-07-12 (close): **Warmforge-bundle desk-verification RESOLVED: BUNDLE LIMITED** (`docs/research/warmforge-bundle-verification-2026-07-12.md`) — ToS Art. 3.5 reserves a 99-connected-accounts/workspace warmup cap and the Salesforge Whitelabel FAQ explicitly excludes Warmforge from the reseller option. Pricing basis for the Mailforge path switches to the **ramp-only warmup model**: warmup fleet sized to onboarding rate (front-loaded ~3-4 wk/mailbox), slots rotated → ~$1-1.50/active-mbx amortized → **all-in ~$4.50-5.00/mbx, retail $13-15 clears ~2.6-3.3×** — the margin model survives without the bundle. Optional owner upside: drafted 3-question support inquiry (warm-only connections) in the frozen record. ACTIVATION Gate-1 item marked resolved. **Pricing sign-off is now UNBLOCKED** — sign off against ramp-only COGS + `docs/research/pricing-landscape-2026-07-12.md`.
- 2026-07-12: **A5 engine spike complete** — validated the `EmailPort` contract (`packages/shared/src/vendor-ports.ts`) against GreenMail 2.1.3 (real SMTP+IMAP in Docker, $0); all 5 contract behaviors (send / reply-detect / threading / bounce-classify / list-unsubscribe round-trip) proven satisfiable against a real server. Spike code + README at `spikes/a5-engine-imap/` (isolated package, outside the root npm workspace). Root suite stayed 130/130. Surfaced four findings that must resolve BEFORE the `VendorPort` freeze — recorded here as a checklist so the freeze can't happen without closing them:
  - [ ] F1 (blocks B4/RFC 8058): `SendEmailInput` (`packages/shared/src/vendor-ports.ts:71-78`) has no header field — cannot express `List-Unsubscribe`/`List-Unsubscribe-Post` through the port. Add `listUnsubscribe`/`listUnsubscribePost` (or generic headers) before freeze.
  - [ ] F2 (correctness): `PolledBounce` (`vendor-ports.ts:95-103`) has no hard/soft flag, and `processBounce` (`apps/platform/src/engine/reply-processor.ts:45-67`) permanently suppresses on EVERY bounce — a real soft 4.x.x bounce would permanently kill a deliverable address; the sandbox only emits hard-shaped bounces so this is invisible today. Needs a `permanent: boolean` field + engine suppress-only-on-hard.
  - [ ] F3 (adapter contract to document): real IMAP has no `threadId` — the adapter must reconstruct it from `In-Reply-To`/`References`, which requires outbound sends to set a real RFC 5322 `Message-ID` and store that SAME value in `scheduled_sends.message_id` (`tick.ts:181` currently stores the sandbox's opaque `msg_<uuid>`).
  - [ ] F4 (correctness): IMAP polling is at-least-once, but the `events` table (`apps/platform/src/schema.ts:114-122`) has no dedupe key on `message_id` — a re-poll after an adapter crash double-inserts reply/bounce/complaint events, inflating metrics/inbox. Needs event dedupe on inbound `Message-ID` before freeze.
- 2026-07-12 (decisions round, cont.): **Pricing sign-off DECIDED — tiers unchanged, deferred-paywall onboarding model adopted.** Founder decision (verbal, in-session): Launch $99 / Growth $299 / Scale $799 (quotas `packages/shared/src/pricing.ts`) signed off unchanged on the ramp-only COGS basis (~$4.50–5.00/mbx all-in, clears ~2.6–3.3× — `docs/research/warmforge-bundle-verification-2026-07-12.md`), decided TOGETHER WITH the deferred-paywall onboarding model: the free no-card sandbox setup stage is the marketed onboarding path (not a "demo"), payment gate sits at the go-live `/checkout` moment only — live-verified against the deployed API 2026-07-12 (no-card signup → setup on demo plan → checkout simulate → same tenant flips to launch with pre-payment state intact; junk tenant `ten_d1707c7c-69f3-4704-8b88-8b8f134981b0`). Recorded in `ACTIVATION.md` Gate 0. Three build items queued onto the activation-lane backlog (not started): (a) demo→paid "graduation" pass — on upgrade, re-provision the tenant's declared infrastructure through the real adapters (campaign/lead state already carries via the TenantDO); (b) checkout-response value framing — machine-readable what's-included in the `/checkout` response + pricing surfaces, since the customer's agent is the messenger that relays price to the human owner at the card moment; (c) site copy reframe from "demo" to "start free — pay when you flip live".
- 2026-07-12: **Engine-correctness increment GREEN-LIT** by founder ("fix whatever needs fixing — do it right") — the A5 spike findings F1/F2/F4 above (List-Unsubscribe header, hard/soft bounce flag, inbound event dedupe) plus F3 (real-IMAP `threadId` reconstruction, landing as an adapter contract doc, not a code change) move from backlog checklist to active build. Per the Bug Response protocol, class-sweeps are dispatched to inventory sibling defects in each finding's class BEFORE the fix is written; the fix itself lands as its own session-log entry when done.

  Also built this session in `site/` (uncommitted, under fresh-context adversarial review, deploy founder-gated): SEO/AEO backlog items 1 (incumbent comparison pages), 2 (tool-coverage matrix), 3 (assembly-question guide), 4 (educational follow-ups + domain calculator), 6 (vendor-blog-style content retargeting) — per `docs/research/aeo-backlog-2026-07-12.md`. Item 5 (maintenance signals) deliberately excluded from this build — orchestrator-owned.
- 2026-07-12 (close): **Brand word DECIDED — coldrig.** Founder decision (verbal, in-session): brand word = **coldrig**; founder registered `coldrig.dev` on Cloudflare the same day. Structure unchanged from the shipped design — distinct display brand Coldrig + the permanent `agent-cold-email` keyword slug on every discovery surface, never collapsed (`docs/adversarial/name-review-2026-07-12.md`). Recorded in `ACTIVATION.md` Gate 0, now fully closed (all three items decided). Public display-brand rollout held pending attorney trademark clearance on "coldrig"; domain + infrastructure wiring may proceed now. Follow-up items this decision unlocks: (a) Gate 3 custom-domain work can now target `coldrig.dev` — Cloudflare Pages custom domain, branded API/MCP host, `server-card.json` + OpenAPI `servers` update, sitemap/canonical-URL pass on `site/` (absolute URLs must move to the canonical host), 301 continuity from `agent-cold-email.pages.dev`; (b) display-brand token swap (site `brand.js` `{{BRAND}}` mechanism, `site/assets/brand.js`) is queued but GATED on attorney TM clearance; (c) sequencing recommendation: wire `coldrig.dev` as the canonical host BEFORE deploying the pending AEO content, so search equity accrues to the permanent domain from day one.
- 2026-07-12 (distribution research): **Zero-founder-touch traffic-channel scout COMPLETE** — frozen at `docs/research/traffic-channels-2026-07-12.md` (17 channels researched + inventory + exclusions). Top-10 ranked by impact×automatability; the 3 highest-leverage moves: (1) the **registry cascade** — one founder npm-auth session (Gate 3, ~10 min) unlocks npm → official MCP Registry → GitHub MCP Registry → Smithery/Glama/PulseMCP/mcp.so auto-propagation → Cline/Cursor submissions → awesome-mcp-servers PR (the 8/8 "cold email MCP server" query surface, where the incumbent artifact is an archived third-party repo); (2) a **Claude Code plugin** (skill + MCP bundle, self-hosted marketplace.json — no gatekeeper) = in-harness discovery before the agent ever searches; (3) **deploy the AEO site on coldrig.dev domain-first** — llms.txt directories, the standalone calculator URL, syndication canonicals, and directory backlinks all hang off the permanent domain. Ethics exclusions recorded in the frozen doc (no astroturf/fake reviews/PBNs/undisclosed placements; brand never spams — dogfood tier-3 rejected). Standing-authorization ask queued for founder: disclosed brand-account external actions (PRs, listings, posts) without per-action sign-off.
- 2026-07-12 (ship): **AEO content SHIPPED + LIVE.** Commit `3477e7f` on main, pushed to GitHub (verified `HEAD`==`origin/main`; engine-correctness increment deliberately excluded — its round-2 fix + adversarial gate still in flight). Site deployed to Cloudflare Pages — prod https://agent-cold-email.pages.dev, Production deployment `96a55be8` (source `3477e7f`), verified via `wrangler pages deployment list`. Live-verified: all 4 new pages return HTTP 200 via extensionless routing (compare-vs-smartlead-instantly, guide-mcp-tool-count, guide-cold-email-with-ai-agent, guide-domains-inboxes-warmup-compliance); calculator live-driven in production (500/day → 17 mailboxes/6 domains, matches worked examples); zero console errors; desktop+mobile screenshots captured. Included the RFC 8058 honesty fix across 12 surfaces (11 HTML instances per `docs/adversarial/aeo-content-review-2026-07-12.md` ROUND 2 + the `site/.well-known/mcp/server-card.json` guardrails line — confirmed via `git show 3477e7f` diff: server-card.json's overclaim "suppression list + RFC 8058 one-click unsubscribe" rewritten to the honest form; restore-item recorded in the same ROUND-2 record). Weekly citation-panel cloud routine ARMED — `coldrig-aeo-citation-panel-weekly` (`trig_01X119P6FiTgxMkamZr2tU29`), Mondays 06:00 UTC, first run 2026-07-13, reports the TREND row + per-query JSON (**UNVERIFIED**: external cloud scheduler, not checkable from this repo); the orchestrating session appends rows to `tools/aeo-panel/runs/TREND.md` (baseline row confirmed present: 46/46 queries live, 0% mention/cite/clickable, pre-publication).
  - [ ] **2026-07-26 — POST-SHIP AUDIT (founder-requested 07-12)**: (a) citation-panel trend review (2 weekly runs in — `tools/aeo-panel/runs/TREND.md`); (b) Cloudflare crawler analytics (AI-bot fetches) + human traffic review; (c) GSC/Bing indexation status; (d) design-pass QA — human-visitor polish holding the extraction structure; (e) coldrig.dev wiring + zone AI-bot-block settings verification; (f) deferred-paywall funnel numbers (signups → setup-completed → checkout); (g) engine defect-class guards re-check (soft-bounce streak + idempotency ledger holding).

  Remaining/queued from this ship: domain wiring = OWNER-PENDING (the CNAME + Pages custom-domain attach was permission-gated in-session; one command or dashboard click, then the host-swap redeploy — ~20 absolute pages.dev URLs already inventoried); design polish pass = queued next session; CF Web Analytics enable + GSC/Bing verification = owner (~5 min); npm auth session = owner (~10 min, unlocks the registry cascade per `docs/research/traffic-channels-2026-07-12.md`); standing brand-account authorization (disclosed PRs/listings/posts) = open founder question.
- 2026-07-12 (close): **Engine correctness increment SHIPPED — commit `d342cd0` pushed.** Both defect classes closed whole (severity/temporality-blind handling; non-idempotent at-least-once processing) via the full Bug Response loop: A5 spike → 2 class-sweeps → build (20 scope items) → verifier COMPLETE (revert-fail-restore proven on both anchors) → adversarial re-attack **FAIL** (round-1 blocker: soft-bounce streak reset by its own triggering send — threshold unreachable; fixture-unreal test hid it) → round-2 fix (streak resets ONLY on reply; +6 NB items) → re-attack **CLEAN**. Suite 130→148, typecheck clean, dry-run clean. Orchestrator rulings recorded: NB3 scope-grow to `idx_ledger_source_send` approved (same class); NB5 NULL-tenant dedupe nuance accepted (revisit on activation ops noise); stale-'pending' claim reclaim = new ACTIVATION Gate-2 item. Fold-ins post-CLEAN (mirror-of-attacked-pattern, suite re-verified 148/148): `sent_message_keys` 30d eviction + corrected idempotency liveness docstring. Standing lesson (hard-builder memory): async-tally-reset-on-triggering-action — a tally incremented by an ASYNC signal must never be reset by the action that triggers the signal; tests must model the real over-time flow, not single-tick stacked fixtures. Known suite flake: `npm test` wrapper intermittently fails under load while direct `npx vitest run` is stable green (3rd occurrence today) — investigate wrapper/pipe interaction someday.
- 2026-07-12: **Dashboard + Unified Inbox lane spec'd (SPEC §19), founder-directed** — a net-new build lane absorbing B7's UI half (Phase B, ROADMAP B7/B7.1). Research fan-out grounded the design: 3 workers (unibox UX, OSS email UI, agent-controlled/generative UI) + a repo map; priorart frozen at `~/.claude/priorart-archive/agent-controlled-dashboard-unified-inbox-coldrig-2026-07-12.md`. Spec drafted (agent-layout-control via MCP/API parity law; httpOnly opaque-cookie session auth + global CSRF middleware; one sanitization pipeline for email HTML and agent-authored content; inbox v2 cursor-paginated single-JOIN API killing the N+1; views/layout CRUD with rev-CAS + default-view lifecycle; dashboard SPA + 3-pane keyboard-first inbox UX) then adversarially reviewed twice (repo grounded @ `e1d1f94`): round 1 **NO-SHIP** (10 findings, 2 blocking — unspecified agent-content sanitization risking stored XSS on the credential origin, and an unbuildable inbox UX on the existing unpaginated N+1 `/inbox`); round 2 all 8 fix-list items closed → **SHIP** with 2 build-gating conditions (global `X-Coldstart-Client` CSRF header on every cookie-authed mutation incl. `/cancel`; composite `(lastEventTs, rowid)` same-ts cursor tiebreak) + 3 notes — folded into the shipped spec as SPEC §19.0–19.8. Record: `docs/adversarial/dashboard-spec-review-2026-07-12.md`. M1 backend build dispatched same day (serving spike → schema → inbox v2 → views CRUD → cookie session → MCP tools 13–15, per §19.8 build order); M2–M5 (SPA shell, inbox UX, agent-control e2e, perfection loop) queued behind M1, gated on a UI/UX screenshot loop + a fresh adversarial re-attack + verification battery before any commit.

  **M1 backend DONE 2026-07-12.** 190/190 tests (148→190, +42). Serving spike proven live via `wrangler dev`: unauthed API paths 401 JSON, unknown paths 404 JSON, `/app/*` deep links 200 (SPA fallback), bare `/app` 307-redirects to `/app/`. Delivered per the build order above: TenantDO schema bootstrap + `last_polled_at`, shared zod layout schema/registry, inbox v2 (composite `(lastEventTs, rowid)` cursor, single-JOIN query, dedicated no-N+1 test), listCampaigns/activity, views CRUD + rev-CAS, D1 `0006_dashboard_sessions` cookie session + global CSRF middleware, MCP tools 13–15 (`get_dashboard`/`configure_dashboard`/`label_thread`). Two notable implementation decisions: (a) the authed-route mount switched from a blanket `authed.use("*")` to an explicit `AUTHED_PATH_PATTERNS` array (`apps/platform/src/index.ts:77-93`) after live-proving a real, previously-undetected gap — every path, including unknown ones, 401'd before this fix instead of 404ing; any future authed route MUST register its path pattern in that array (documented in `src/routes/README.md`); (b) `apps/platform/public/index.html` is a deliberate duplicate of `public/app/index.html` — Cloudflare's `not_found_handling = "single-page-application"` was empirically proven (via `wrangler dev`, not assumed from docs) to serve the assets directory's ROOT `index.html` for any unmatched `/app/*` path rather than a per-directory walk-up, so both copies must exist; keeping them in sync is M2's build-step responsibility once the Vite build starts populating `app/` (documented in `public/README.md`). M2 dashboard SPA shell dispatched to design-builder same day. M1 remains uncommitted in the working tree by design — commit is gated on arc-end verification + a fresh adversarial re-attack.
- 2026-07-13: **M2-M5 built, gates clean, whole lane COMMITTED — `c9e5e27` on `feat/dashboard-inbox-spec-19`.** M2 (SPA shell: token-gate auth with no JS-readable token, desktop nav rail / mobile bottom tabs, 12-col grid renderer + 8-widget registry, view management, Settings) → M3 (virtualized keyboard-first inbox: j/k nav, Cmd+K palette, thread detail + reply composer, mobile swipe + 5s undo) → M4 (agent-control e2e proof, server-side archived filter on inbox v2 + MCP, saved-view rename, dark mode, a11y pass) → M5 two-round perfection loop (R1 closed defects A-I; R2 closed 7 more — mailbox-health failsafe banner, grid dead-gap fix for content-fit widgets via `measuredRowSpan` row-unit collapsing, default seeded view redesign, widget overflow row-clip/fade, `ProvenanceBadge` mobile truncation, keyboard archive-undo parity, mailbox-chip long-local-part truncation) followed by a full final 1440/390 screenshot matrix.
  Both gates run fresh-context, both clean: **security re-attack → SHIP** (CSRF coverage on every cookie-authed mutation, opaque httpOnly cookie session, sanitization sinks confirmed confined to `AgentNote.tsx`/`EmailHtmlFrame.tsx` via `check:dangerous-html`, tenant isolation, MCP cookie-rejection all held); **UX review → SHIP-AFTER-FIXES**, all of which landed in M5 R2 above. Verification battery this session (re-run and confirmed, not just trusted from the commit message): `npm test` root workspace **233/233 passed (47 files)**, `apps/dashboard` **93/93 passed (21 files)**, `npm run typecheck` clean across all 4 workspaces, `npm run build` clean (Vite build + `wrangler deploy --dry-run` + CLI `tsc`), `npm run check:dangerous-html` clean. Live-driven this session: cookie-session auth flow, CSRF 403 on a missing `X-Coldstart-Client` header, MCP `configure_dashboard` stale-`rev` 409 conflict, and the serving criteria from M1's spike all re-confirmed against the built app. Commit `c9e5e27` (105 files) excludes unrelated in-flight `.claude/agent-memory/` files; `pw-shots/` screenshots are gitignored, not in the commit.
  **State at commit time: BUILT + COMMITTED, NOT merged to main, NOT pushed, NOT deployed** — founder-gated per SPEC §0 (test-mode/no-real-spend discipline extends to not shipping a net-new authed surface without a founder look). **Superseded same day — see the 2026-07-13 (deploy) entry below: merged to main and deployed live.**
  Known non-blocking follow-ups for a future session (from this session's gates, not new backlog docs): (1) no MCP `list_campaigns`/`activity` tools yet — verified via `apps/platform/src/mcp/tools.ts`, those two remain HTTP-only; (2) `site/openapi.yaml` still documents 13 operations (verified via `operationId` count) — stale against the 3 new dashboard MCP tools (`get_dashboard`/`configure_dashboard`/`label_thread`) and the new HTTP routes; (3) the content-fit grid gap (`apps/dashboard/src/dashboard/Grid.tsx`) collapses close to but not exactly 0 — `measuredRowSpan` quantizes to whole `ROW_HEIGHT_PX + ROW_GAP_PX` (76+16=92px) row units by design, so a residual gap up to just under one row unit is expected, not a bug; the specific "~82px" figure from this session's live UX pass is **UNVERIFIED** by this bookkeeping pass (not independently re-measured); (4) the mobile thread-detail overlay covers the failsafe banner by design (single-pane mobile UX tradeoff, not yet reconciled); (5) `parseJsonBody`'s Content-Length pre-check (`apps/platform/src/validate.ts:41`) is skippable when the header is absent/non-numeric (`Number.isFinite(NaN)` is false) — verified in the code; pre-existing platform-wide, not dashboard-specific, and bounded downstream by each route's zod schema caps; (6) login-CSRF on `/dashboard/session` is low-severity (an attacker can only plant their OWN session token, not hijack another's).
- 2026-07-13 (deploy): **Dashboard + Unified Inbox lane (SPEC §19) MERGED + DEPLOYED LIVE.** Merged `feat/dashboard-inbox-spec-19` → `main` as `77741ae` ("Merge dashboard + unified inbox (SPEC §19) into main", merging `e1d1f94` + `f3f4187` — confirmed via `git show --stat`). Deployed via `npm run deploy` (`apps/platform`): D1 remote migrations `0005_support_dedupe` + `0006_dashboard_sessions` both applied (confirmed via `wrangler d1 migrations list coldstart-platform-db --remote` → "No migrations to apply!"), then `wrangler deploy` → Worker version `e9734c42-984a-47f3-805a-3c261db4d06c` (confirmed as the newest entry in `wrangler deployments list`, created 2026-07-13T11:29:20Z). Dashboard is now LIVE at https://agent-cold-email-api.yaakovscher.workers.dev/app, served same-origin by the Worker's `[assets]` config (`wrangler.toml`: `directory = "public"`, `not_found_handling = "single-page-application"`) — no separate deploy target. Live-verified this session: `GET /app/inbox` → 200 `text/html`, title `coldrig dashboard`; `GET /app` → 307 (redirects to `/app/`); `GET /inbox` with no auth → 401 JSON `{"error":"missing bearer token","code":"invalid_token"}` (API auth intact); `GET /status` → 200 `{"status":"ok"}`. The full signup → `POST /dashboard/session` → `Set-Cookie: cs_dashboard_session` (opaque, httpOnly/Secure/SameSite=Strict, distinct from the bearer token) → `GET /dashboard/views` (seeded default view present) path was run and confirmed by the deploying session; not independently re-run in this bookkeeping pass to avoid creating a duplicate prod signup. **Still TEST-MODE**: no real vendor spend or real sending — SPEC §0 unchanged; this deploy only ships the dashboard onto the already-live test-mode API. **Open item: `main` is NOT pushed to GitHub** — confirmed via `git rev-parse main` (`77741ae...`) vs `git rev-parse origin/main` (`e1d1f94...`), `git status -sb` shows `ahead 3`; `origin/main` is behind the deployed HEAD by the 3 commits `c9e5e27`/`f3f4187`/`77741ae` (founder chose deploy-only this session) — push when ready.
