# ColdStart — ROADMAP

> Build order + status. Canonical; updated every session that touches the project.
> Program started 2026-07-09. Owner interview complete (SPEC §0). Adversarial panel #1 complete → amendments adopted (`docs/adversarial/panel-01/SYNTHESIS.md`). Autonomous to the finish line; open questions HELD for the final report, never asked mid-build.

## Definition of done — "test-mode CORE complete" (rescoped by panel #1)

The core business exists and runs itself in **test mode**: the pipe (provision→warm→send→reply, sandbox adapters) + control plane + Stripe test-mode billing/metering + agent surface (MCP+CLI+demo) + real-but-minimal compliance guardrails + the business-ops lanes (dunning, teardown, abuse-drop, metrics) + legal docs + a publish-now distribution shell that's already indexing — all adversarially hardened on the REAL surfaces — plus **ACTIVATION.md**, the single checklist of owner-hands steps that flips it live. Un-sandbox-testable *tuning* (live deliverability tuning against real Gmail, warmup optimization) is built-to-contract now and tuned at activation.

## Hardening-budget rule (panel #1)
Adversarial panels apply to what is REAL: control plane, auth, tenancy, billing, isolation logic, compliance, legal, distribution. Deliverability/warmup/AI-ops get contract-level review only until activation — don't burn budget polishing synthetic behavior.

## Now
- [ ] 2026-07-13 [gated:founder] Domain wiring: attach coldrig.dev as Cloudflare Pages custom domain (CNAME + dashboard click), then the host-swap redeploy — ~20 pages.dev URLs already inventoried; permission-gated in-session (`docs/research/traffic-channels-2026-07-12.md`, ACTIVATION.md Gate 3).
- [ ] 2026-07-13 [ASK] Design polish pass on the shipped AEO site — queued for next session (human-visitor polish holding the extraction structure).
- [ ] 2026-07-13 [gated:founder] Enable Cloudflare Web Analytics + verify GSC/Bing indexation (~5 min, owner action).
- [ ] 2026-07-13 [gated:founder] Run one npm auth session (~10 min) to unlock the registry cascade (npm → official MCP Registry → GitHub MCP Registry → Smithery/Glama/PulseMCP/mcp.so → Cline/Cursor → awesome-mcp-servers PR) — also closes the open half of C5 below.
- [ ] 2026-07-13 [gated:founder] Standing brand-account authorization: founder sign-off to let disclosed brand-account external actions (PRs, registry listings, posts) proceed without per-action approval.

## Open
- [x] 2026-07-13 (evening) MCP parity: `list_campaigns`+`activity` tools added (15→17 tools, SPEC §19.0 parity, byte-identical to existing HTTP routes) + `site/openapi.yaml` 13→24 `operationId`s — commit `b8d57e4`, 6 new tests, platform suite green (239/239), typecheck clean.
- [x] 2026-07-13 (evening) Discoverability wave: OG+Twitter cards on all 16 site pages + `site/assets/og-image.png` (1200×630, verified on disk) + JSON-LD on docs/pricing/compare + GitHub repo `homepageUrl` set to the live Pages URL (verified via `gh repo view`) — commit `b8d57e4`. NOTE: gh repo **description** still reads "~12 MCP/CLI tools" — not yet updated, tracked below.
- [x] 2026-07-13 (evening) Tool-count staleness swept as a CLASS: `site/llms.txt` + `site/.well-known/mcp/server-card.json` were stale at "~12 tools"; every 12/15-tool reference across `site/`, `AGENTS.md`, READMEs re-grepped post-fix — zero survivors confirmed. Fresh-context adversary verdict **SHIP** on the full diff, with one gate carried forward: DEPLOY ORDERING — Worker must deploy before/with the site (else `server-card.json` advertises tools the live Worker lacks) + verify live `tools/list` count == 17 pre-deploy (see push/deploy item below).
- [x] 2026-07-13 (evening) Deep research frozen: `docs/research/traffic-channels-selfserve-2026-07-13.md` — 13 ranked zero-founder-touch channels (Tier-1 verified: IndexNow, GitHub code-search metadata, grep.app on-demand indexing, DeepWiki mirrors; 5 refuted-dropped: deno.land/x, Common Crawl submission, GitHub Packages npm, JSR zero-touch, Postman); extends the 07-12 channel-scout doc — commit `1e84672`.
- [x] 2026-07-13 (evening) IndexNow built fire-ready: key file live under `site/` + `tools/indexnow/submit.sh` (pre-deploy abort guard verified — aborts if key file isn't live at the target host; 16-URL sitemap payload validated) — commit `fd13aa0`. Inert until the site actually deploys.
- [ ] 2026-07-13 [ORDER] push main (ahead 3) + deploy Worker THEN site [gated:founder]
- [ ] 2026-07-13 [ORDER] post-deploy: run tools/indexnow/submit.sh + Wayback-snapshot key URLs + curl live tools/list == 17 + update gh repo description 12→17 tools
- [ ] 2026-07-13 [ORDER] post-push: seed grep.app/<org>/<repo> + deepwiki.com/<org>/<repo> on-demand indexing
- [ ] 2026-07-13 [ASK] Tier-2 insurance channels: extra .well-known variants, apis.guru form, public-apis PR (neutral copy — anti-marketing gates) — per research doc #5-#8
- [ ] 2026-07-26 [ORDER] POST-SHIP AUDIT (founder-requested 2026-07-12): (a) citation-panel trend review (`tools/aeo-panel/runs/TREND.md`, 2 weekly runs in); (b) Cloudflare crawler analytics (AI-bot fetches) + human traffic review; (c) GSC/Bing indexation status; (d) design-pass QA — human-visitor polish holding the extraction structure; (e) coldrig.dev wiring + zone AI-bot-block settings verification; (f) deferred-paywall funnel numbers (signups → setup-completed → checkout); (g) engine defect-class guards re-check (soft-bounce streak + idempotency ledger holding).
- [ ] 2026-07-13 [ASK] B2 — (ACTIVATION-HARDENING backlog — synchronous provisioning WORKS in test mode) Provisioning sagas: resumable alarm-driven jobs on clock abstraction; clock-scaled warmup ramp; idempotency keys; DO-reload/alarm-retry soak tests.
- [ ] 2026-07-13 [dark-unarmed] B3 — `VendorPort` layer: fault-injecting sandbox impls (active) + real impls (Porkbun/Inboxkit/Stripe — coded, unactivated); one contract-test suite across both; free/demo tenants structurally can't reach a real adapter (type guard + failing test).
- [ ] 2026-07-13 [ASK] B4 — Sequencing + reply engine (contract, native sandbox impl): campaigns, scheduling, per-mailbox caps, rotation, stop-on-reply, suppression, full CAN-SPAM opt-out flow (in-body + RFC 8058 header + honor windows), subject-honesty + lookalike third-party-brand hard-reject validators, A/B.
- [ ] 2026-07-13 [ASK] C4 — SEO/AEO comparison + how-to pages targeting agent queries (after product describable). NOTE: this checklist line was never checked off, but 2026-07-12 session-log entries in archive/ROADMAP-done.md describe AEO content as "SHIPPED + LIVE" with matching comparison/guide pages (compare-vs-smartlead-instantly, guide-mcp-tool-count, etc.) — verify whether this item is actually closed and the checkbox is just stale before doing more work here.
- [ ] 2026-07-13 [gated:founder] C5 (remaining half) — MCP registry listings (Smithery/mcp.so/PulseMCP) submitted once identity exists + scan is clean; npm publish + awesome-list PRs = ACTIVATION.md (identity-gated). The `/.well-known/mcp/server-card.json` portion is already served.
- [ ] 2026-07-13 [ASK] C6 (remaining half) — Distribution validation harness (fresh agent + query → discovers us?) still open, not yet baselined. (The waitlist/early-access flow itself — `POST /api/waitlist`, KV-backed dedupe/rate-limit/CORS — is done.)
- [ ] 2026-07-13 [ASK] D3 — Legal: ToS/Privacy/AUP to the specified clause inventory (customer-is-sender + reps, indemnification, vendor-AUP pass-through, monitoring consent, no-deliverability-warranty+LoL, prohibited-use, ROSCA/state-ARL auto-renewal, DPA) — enforcement path built+tested, not paper; DRAFT-flagged for attorney review.
- [ ] 2026-07-13 [gated:founder] D4 (remaining half) — OFAC screen + friction ladder + new-tenant volume ramp; needs real signups to exercise. (Per-tenant physical address + sender identity capture is already done in `setup_infrastructure`.)
- [ ] 2026-07-13 [ASK] D6 (remaining half) — Owner business-health substrate: per-tenant margin, backups/DR for the ledger + encrypted creds, master-key storage/rotation/recovery (separate concern, deferred). (MRR/active-tenants/status-page/`GET /admin/ops/digest` portion is already built.)
- [ ] 2026-07-13 [ASK] E3 — Memory + ledger current; HANDOFF.md maintained (standing item).
- [ ] 2026-07-13 [ASK] A5 engine-spike findings F1-F4 (List-Unsubscribe header on `SendEmailInput`, hard/soft bounce flag on `PolledBounce`, real-IMAP `threadId` reconstruction via Message-ID, inbound event dedupe on `message_id`) — recorded unchecked in the 2026-07-12 session-log entry (archive/ROADMAP-done.md), but a later 2026-07-12 entry ("Engine correctness increment SHIPPED — commit `d342cd0`") appears to close these without the checklist ever being updated — verify and reconcile before treating as open work.

> Done items live in archive/ROADMAP-done.md (self-draining at handoff).
- [ ] 2026-07-14 [ORDER] BYO-domain RULED (Yaakov, in-session): customers may bring ANY domain they want to send from, INCLUDING their primary (with explicit informed-consent risk acknowledgment). Automation: delegation-first (nameservers/zone to our CF = fully automatic DNS) > agent-applied records (MCP returns records, customer's agent sets them, we poll-verify) > human walkthrough fallback; subdomain delegation (mail.x.com) as middle path. Needs: SPEC § addition + adversarial spec review + engine intake build (flag-dark). — SPEC §20 drafted, awaiting adversarial re-review
- [ ] 2026-07-14 [ORDER] Mordy-pilot activation lane (Yaakov: "get coldrig ready + have him migrate"): critical path = stale-claim reclaim (in flight) → Go-engine host stood up + EmailPort wired → Gate-1 smoke test on OUR test domain → BYO-domain+BYO-mailbox intake (his authorpitchdesk.com + Google Workspace boxes) → migrate as tenant #1 (comped/test-billing, no live Stripe needed). Founder-hands: engine-host spend confirm (~$6/mo droplet or CF Containers) + 1 test domain (~$10) + migration-timing call vs his 07-28 Instantly clock.
- [ ] 2026-07-14 [ASK] Pre-warmed inventory evaluation: can/should we pre-warm domain+mailbox stock ahead of signup (cost carry, brand-mismatch, reassignment ToS/deliverability risk) — Yaakov question, needs economics + SPEC ruling.
- [x] 2026-07-14 [ASK] Competitive check vs outreach-gigs gig layer: Mordy decided Instantly-over-Smartlead 07-13 (human-ops DFY gig, authorpitchdesk.com, 2-3 mbx, ~$60-80/mo, live ~07-28). VERDICT: same category, no conflict today (coldrig test-mode-locked + gig is human-UI-operated); post-activation the gig is the natural FIRST-TENANT/dogfood-migration candidate, not a rival — evidence in outreach-gigs `gig-layer/sending-infra-setup.md`.
- [ ] 2026-07-14 [IDEA] Fold 07-12 ramp-only warmup economics into canonical SPEC §12/§18 (tables still show stale $7/mbx Inboxkit basis; real Mailforge-path all-in ~$4.50-5/mbx) — scout-found gap.
- [ ] 2026-07-14 [ORDER] Dogfood campaign (Yaakov = tenant #1, Mordy = tenant #2): coldrig runs its own transparent cold campaign — research-built list of lead-gen/cold-email bloggers, YouTubers, newsletter writers; subject leans into disclosure ("yes, this is a personalized cold email…"), body discloses agent-run ("completely managed by my AI executive assistant") + pitch; matches tier-2 of docs/research/traffic-channels-2026-07-12.md §16 (transparent dogfood = marketing artifact, founder-approved in-session 07-14). Guardrails: CAN-SPAM address fill (Gate 4) + unsubscribe + small-volume hyper-targeted + publish live deliverability/logs proof page; NEVER volume-spam (brand = the responsible layer). Sequencing: sends gated on engine + mailboxes + ~4wk warmup; list research + copy + proof-page build can start pre-warmup.
- [ ] 2026-07-14 [ORDER] Mordy live-discovery test (~1hr deadline): his agent runs the exact target search ("agent-first complete cold email campaign"); funnel live-drive + discovery simulation dispatched; grep.app reseeded. Surprise gate: funnel must survive a stranger-agent run.
