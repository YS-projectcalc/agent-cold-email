# ColdStart — ROADMAP

> Build order + status. Canonical; updated every session that touches the project.
> Program started 2026-07-09. Owner interview complete (SPEC §0). Adversarial panel #1 complete → amendments adopted (`docs/adversarial/panel-01/SYNTHESIS.md`). Autonomous to the finish line; open questions HELD for the final report, never asked mid-build.

## Definition of done — "test-mode CORE complete" (rescoped by panel #1)

The core business exists and runs itself in **test mode**: the pipe (provision→warm→send→reply, sandbox adapters) + control plane + Stripe test-mode billing/metering + agent surface (MCP+CLI+demo) + real-but-minimal compliance guardrails + the business-ops lanes (dunning, teardown, abuse-drop, metrics) + legal docs + a publish-now distribution shell that's already indexing — all adversarially hardened on the REAL surfaces — plus **ACTIVATION.md**, the single checklist of owner-hands steps that flips it live. Un-sandbox-testable *tuning* (live deliverability tuning against real Gmail, warmup optimization) is built-to-contract now and tuned at activation.

## Hardening-budget rule (panel #1)
Adversarial panels apply to what is REAL: control plane, auth, tenancy, billing, isolation logic, compliance, legal, distribution. Deliverability/warmup/AI-ops get contract-level review only until activation — don't burn budget polishing synthetic behavior.

## Now
- [ ] 2026-08-23 [ORDER] **Agent-recommendation wave SHIPPED + DEPLOYED 2026-08-23** — Claude Code skill + plugin marketplace, Cursor/Codex integration files, site AEO fixes (homepage comparison table, `Organization.sameAs` expansion, table CSS at all widths, stale-claim sweep), claim-guard coverage. Merged `main` `7ffba30` (`integ/visibility-2026-08-23`), deployed Cloudflare Pages `211d6cf7`. **Live-verified this pass** (curl against `coldrig.dev`): homepage has `id="compare"`; `style.css` carries `th:has(code), td:has(code) { overflow-wrap: anywhere; }`; `llms.txt` has the Option-D plugin/skill bullet; `/connect` names the plugin path (3 mentions); homepage JSON-LD `Organization.sameAs` = 4 entries (GitHub/npm/Smithery/Glama); `compare-vs-maildoso.html` reads "No refunds on the unused portion…"; the stale `support.html` "must be owner-verified before paid activation" line is gone (0 matches). **Gate `docs/adversarial/agent-skill-gate-2026-08-23.md`, 4 rounds → SHIP:** r1 SHIP-AFTER-FIX (5 blocking — incl. B1, the plugin's `source: "./"` rooted it at the monorepo root so Claude Code copied the whole repo + ran `npm ci` there, measured 413 MB/45s for a payload that was one 8.6 KB skill + a 10-line `.mcp.json`; fixed to a dedicated `plugins/coldrig/` root with the skill dir symlinked in, cache now 28 KB/~1s) → r2 combined-diff SHIP-AFTER-FIX (B6, table CSS caused horizontal overflow 621–1023px on 10 pages) → r3 NO-SHIP (B7, the B6 fix's own global `min-width` then clipped the last column of 2 tables at desktop — the reviewer's own r3 "0 new scroll" verification was later found to be a vacuous pass: a stale variable name in its harness skipped every page-comparison) → r4 **SHIP** (B7 closed; the builder's diagnosis was right, the reviewer's r3 counter-patch was not; 296/296 page×width pairs clean, 0 skipped, plus a 35-instance pre-existing mid-word-break defect on `main` fixed as a side effect). **Battery GREEN on the merged tree:** typecheck 5/5 workspaces; platform 239 files/2407 tests (1 skipped); dashboard 165; engine 157; CLI 12/12; claim-surface guards 377 (incl. 33 new cases covering the new skill/plugin/integration surfaces). **GitHub release `v0.2.3`** tagged at `7ffba30` (first tagged release; confirmed `git ls-remote --tags origin` + `gh release view`) — https://github.com/YS-projectcalc/agent-cold-email/releases/tag/v0.2.3. Install paths verified live from GitHub: `npx skills add YS-projectcalc/agent-cold-email` → "Found 1 skill"; raw `skills/coldrig/SKILL.md` → 200; `.claude-plugin/marketplace.json` → plugin `coldrig` source `./plugins/coldrig`. **Registry republish NOT needed** (`server.json`'s registry-facing block unchanged) and **npm CLI stays `0.2.1`** (registry `dist-tags.latest` confirmed 0.2.1, matches the existing pin — `npm login` is expired and only needed if a new CLI version is actually cut). **Founder ORDER 2026-08-23 status** (full order text below, now annotated): (0) DONE — cleanup executed (commit `2dac5a7`: "234 → 116 open"); (1) BUILT + PUBLISHED — this wave; (2) dogfood PARTIAL — runbook delivered (`archive/2026-08-23-roadmap-cleanup/dogfood-tenant-runbook.md`), execution [gated:founder] (no tenant exists yet; needs a live 100%-off Stripe coupon + a card + 2-4 Gmail OAuth grants; only 4 of 14 outreach targets are email-able; `pendingCredentialPushes:4` platform-wide, all Mordy's, zero real sends ever completed on this platform); (3) directories PARTIAL — mcpservers.org submitted, `YuzeHao2023/Awesome-MCP-Servers` PR #433 opened, `punkpeye` PR #10106 nudged with the now-evaluated Glama score (83%/A, the maintainer's stated blocker), `chatmcp/mcpso` issue #3602 monitored; several more prepared-not-sent because the auto-mode classifier blocks subagent-driven multi-site form/PR automation (Salestools Club, MCP.Directory, Oryndex, claudeskills.info, cursor.directory — see the new `## Open` [gated:founder] entries below); founder-present items (Directory for AI, Glama claim, PulseMCP email) and dropped items (parked domain, archived repo, signup/payment rule, star-count gate) unchanged; (4) trust floor unchanged [gated:founder-present]; (5) on-site nits DONE (homepage table, sameAs, stale claims, table CSS at all widths — items verified live above). Full per-send record: `archive/2026-08-23-roadmap-cleanup/send-ledger-2026-08-23.md`.
- [ ] 2026-08-20 [ORDER] **TRAINS PROGRAM BUILD PHASE COMPLETE 2026-08-20** — trains 1-6 (channel-truth/loop-isolation, vendor-truth, claim-surface, dedup-semantics, scale, monitoring) plus the alert-state increment plus the sweep-calibration fix have ALL shipped and deployed (supersedes the Wave B.1 `## Now` entry that was here, preserved verbatim at `archive/2026-08-20-b2-sweepcal-ship/superseded-roadmap-wave-b1-now-entry.md`; Wave B.1's own facts unchanged, live in `HANDOFF.md`/`MEMORY.md`). Last two to land, same day: **Wave B.2 (alert-state build)** merged `ce6218b` + N8 correction `9d31e26`, gate `docs/adversarial/alert-state-build-gate-2026-08-20.md` r1 FAIL-SHIP-AFTER-FIX (B1 prose-fed key classifier; F1 fold-collision enumerated) → r2 SHIP; and **sweep-calibration** (lane `fix/sweep-calibration-2026-08-20`, merged `23fe40d`), gate `docs/adversarial/sweep-calibration-gate-2026-08-20.md` PASS (SHIP), 0 blocking, 7 non-blocking. Merged-tree battery GREEN at `9d31e26`: typecheck 0; platform 239f/2373p/1skip; dashboard 31/165; engine 18+2s/153+4s; cli 12/12; wrangler dry-run zero binding delta. Deployed 2026-08-21 (local 2026-08-20 evening): migration `0021_watchtower_alert_state.sql` applied remote (confirmed `wrangler d1 migrations list coldstart-platform-db --remote` → "No migrations to apply"); Worker `fca0c4d9-b116-4b98-8b43-2159bda6f117` both hosts, confirmed LATEST via `wrangler deployments list` (created 2026-08-21T02:13:21Z) and live-reconfirmed `/status` ok both hosts this pass; no site deploy (no claim-surface change, both gates verified). Full detail: the two entries below (§9.13 RATIFY, sweep-capacity follow-up) plus the fresh 2026-08-20 `## Open` entries this pass added/updated. Two design decisions remain owed, confirmed unsettled by BOTH the B.1 and B.2 gates — see the two `[ORDER]` entries below (immortal one-shot `*_FAILED` clearer; per-tenant staleness signal).
- [ ] 2026-08-20 [ORDER] **Queued next: sweep-capacity follow-up increment (design-then-build, one lane).** Per the sweep-calibration gate's finding 6 ruling (`docs/adversarial/sweep-calibration-gate-2026-08-20.md:201-212`): the shipped remedy prose tells the operator "the read-model is DUE" but the lane's OWN measurement (DO `cpuTime` at 1-3% of `wallTime`, the textbook signature of a dispatch-bound workload) points at a cheaper unevaluated option first. Orchestrator ruling: **measure bounded-concurrency fan-out (subject to Workers' simultaneous-connection ceiling, never measured in this repo) BEFORE committing to the D1/Analytics read-model** (`apps/platform/src/admin/README.md` remedies list, read-model = remedy #2). Bundle into the same lane: `opsSummary` dedupe, send-pipeline-off-slice, paying-tenant-first priority. Live baseline this pass (re-verified via `GET /admin/ops/checks?unhealthy=1`): `sweep_coverage` is CURRENTLY UNHEALTHY under the new honest grading — "63 tenant(s)... a full pass every 21 tick(s) (~105 min)" — 2 unhealthy total incl. `customer_progress_agent:ten_91aab24a…`; this is the calibration fix reporting truthfully, not a regression.
- [ ] 2026-08-17 → 2026-08-20 [ORDER] **msgchannel Inc4 (email mirror) still queued** — founder-authorized 2026-08-05 alongside Inc2/3/5 (all shipped), never got its own build lane; queued behind the sweep-capacity lane above now that the trains program's build phase is complete. Full detail: the 2026-08-17 `## Open` entry (backlog-inventory promotion).

## Open
- [ ] 2026-08-23 [NOTE] Cleanup pass executed 2026-08-23 per founder ORDER: 82 items archived DONE → archive/ROADMAP-done.md, 37 dropped → archive/ROADMAP-dropped-2026-08-23.md, 112 kept (90 agent-doable / 22 founder-owned). Pass 2 (2026-08-23) resolved the 29 stale-KEEP items flagged at pass 1: 2 DONE, 9 dropped, 5 edited-in-place, 13 unchanged (incl. 1 conditional-KEEP — Warm-lead Q1-Q6, increment #4 `schedule_followup` still unbuilt). Inventory + rulings: archive/2026-08-23-roadmap-cleanup/inventory.md.
- [ ] 2026-08-13 [ASK] **Provisioning-architecture design pass DELIVERED + C3 FOUNDER-APPROVED & BUILT (direction-1 staged) — priorart `~/.claude/priorart-archive/coldrig-async-provisioning-and-resource-reconcile-2026-08-13.md`.** Two questions the P0 fix deliberately left open; both well-trodden distributed-systems patterns. **Founder ruled 2026-08-13: BUILD C3 NOW (approved); STAGE direction-1 after C3.** **C3 SHIPPED + DEPLOYED 2026-08-13 (part (b) ARMED, part (d) DARK).** Adversary gate SHIP-dark 0-blocking-for-merge (frozen `docs/adversarial/c3-gate-2026-08-13.md`; the (b) money-boundary held on every dangerous-direction attack; battery 172 files/1612 tests). Merged `main` `6c5140b` (--no-ff, lane preserved), pushed, deployed Worker `52a65306`, live-verified `/status` 200 both hosts / `/account` 401 / cron intact; `PROVISIONING_RECONCILE_ENABLED` confirmed UNSET (d is dark). **Part (b) now live:** a benign terminal purchased-domain-propagation-wait returns success-pending instead of a thrown `retryable:true` — so Mordy's next retry gets "provisioning in progress" not the scary error (no spend). **🔴 ARM-GATE (BLOCKING BEFORE ARMING (d) — gate Finding 1, cannot fire while dark):** the reconcile reads the FIRST persisted spec (`INSERT OR IGNORE`) while the DIRECT resume path honors the LATEST call's persona/count — they DIVERGE, so an armed (d) OVER-PROVISIONS (gate reproduced: call1 alpha/5 pending → direct retry bravo/2 provisions 2 → armed reconcile re-drives alpha to 5 → 7 mailboxes for a customer whose last ask was 2, real spend+bill; also same-persona reduced-count silently bumps back up). Also: reconcile mailbox buys pass `withSpendCeiling` (dollar cap) but NOT `assertWithinProvisioningCap` (count cap). **DO NOT arm `PROVISIONING_RECONCILE_ENABLED` until fixed** — options (founder/next-session to scope): direct resume also reads the persisted spec; OR reconcile refuses a different-persona set; OR persona/count immutable-per-ordinal at the boundary; + add the count cap to the reconcile buy path. **POST-SHIP RE-ATTACK 2026-08-14 (Finding 2) EXTENDS THIS ARM-GATE:** the reconcile also skips `assertNotLifecycleFrozen` — executed proof (3/3 green): frozen tenants (suspended/disputed/canceled) get provisioned with phantom `mailboxes` rows + false `dns_status='ready'` that turn BILLABLE the moment the tenant reactivates (no live spend today — `buildAdapters()` re-reads activation per call and frozen tenants get the sandbox bundle). **The class: the reconcile inherits only the guards INSIDE the shared primitive, never the on-demand caller's wrappers — before arming, port the WHOLE wrapper set, not item-by-item.** Frozen: `docs/adversarial/c3-postship-reattack-2026-08-14.md`. **⚠️ Builder design decision for the gate to rule on:** `persona`/`inboxesEach` are durable nowhere, so it persists them at first-buy via 2 NEW nullable `domain_intents` columns (`persona_slug`/`inboxes_each`, INSERT-only) + a DO-schema migration, and the (d) reconcile SKIPS any NULL-spec intent. **Consequence (CORRECTED — supersedes the earlier "arming spends on Mordy" note): arming `PROVISIONING_RECONCILE_ENABLED` does NOT auto-complete/auto-spend on Mordy's 2 pending domains** — they sit on P0-rebound LEGACY intents (pre-column → NULL spec → skipped); Mordy completes via an agent RETRY (supplies real persona/count), and C3 part (b) makes that retry a non-error (success-pending, not a thrown `retryable:true`). Arming (d) only auto-completes NEW post-C3-stranded domains (still a deliberate deploy-time founder decision — it spends on an armed tenant out-of-band). direction-1 (resume-from-resource) remains STAGED after C3 ships. **2026-08-14 vendor-verdict wave:** the reconcile's scope predicate now also excludes `dns_gave_up_at IS NOT NULL` rows (`provisioning-reconcile.ts:112-124`, confirmed in code) — a domain the vendor-verdict fix has already given up on is not re-driven by the dark reconcile; arm-gate blockers unchanged (spec divergence + count cap + lifecycle-freeze guard still open).
  - **C3 (first-buy latency): compose (b)+(d), reject (a) parking, defer (c) webhook.** CONFIRMED by code trace: every first-time domain buy ends in a customer-facing `vendor_error retryable:true` because the in-call DNS wait (~2.3s, `SET_DNS_BACKOFF_MS=[2000]`) can't outlast the vendor's ~32s async registration — this is what made Mordy's agent perceive a broken loop (post-P0 the spend is safe, but the CONTRACT reports "in progress" as an error). Prior art (Stripe `processing`, async request-reply 202+status-resource, Temporal/Restate durable-await) converges: "still working" is never an error. RECOMMENDATION: **(b)** reclassify the benign propagation-wait from a thrown `retryable:true` VendorError to a non-error "provisioning in progress" result (surgically scoped — real failures still surface); **(d)** add a per-tenant provisioning-reconcile cron leg (`scheduled.ts`/`ops-sweep.ts`, mirrors the existing `runLeg` sweeps) that re-drives the already-idempotent `provisionDomainWithMailboxes` for `dns_status='pending'` domains so the PLATFORM completes provisioning out-of-band within ≤1 cron cycle, agent never retries. The code's own anti-parking comment (`domain-dns.ts:30-37`) is SOUND but only refutes option (a) synchronous parking — it doesn't reach out-of-band reconciliation. **Founder decisions:** approve the agent-facing contract change (propagation-wait → "in progress, completes automatically", not an error); approve the platform spending on a timer (cron re-drive; every step already spend-guarded); optional: authorize a spike to check whether InboxKit emits a domain-active webhook (OPEN VENDOR QUESTION — a later optimization ON TOP of (d), never a replacement).
  - **Direction-1 (resume from the RESOURCE not the record): level-triggered reconciliation, HIGH blast radius.** CONFIRMED: `planProvisioning` resumes only off `domain_intents`; `domains` rows are consulted ONLY to exclude — so a paid domain with a row but no committed intent is permanently stranded (the P0's root class C2). Prior art (Kubernetes controllers, Terraform refresh, Crossplane) converges: source of truth for "what exists" is the observed RESOURCE, not an action log. RECOMMENDATION: make `planProvisioning` a reconciler over the `domains` table (`status != 'released'`, keyed by ordinal) + vendor-owned list as source of truth; demote `domain_intents` to buy-idempotency hint. **Crux:** the `domains` row must carry its ordinal + PROVENANCE (a `domain_index` + setup-vs-replacement binding) so the reconciler maps resource→slot without the log AND a burned REPLACE_DOMAIN domain can never satisfy a setup slot. Cleaner than today (ONE reconciler over ONE table replaces three disagreeing "live" definitions — the P0's root cause). Makes the one-shot `legacy-domain-intent-keys.ts` rebind obsolete post-migration; narrows the C1 guard's scope. **Founder decisions:** approve a schema change + migration to the money-critical `domains` table; approve demoting `domain_intents` from resume-authority; decide the provenance model. Needs a full class-sweep + fresh-context adversary gate (correctness/money change). Honesty caveat: C3's "every first buy errors" is code-trace-confirmed + stated in the code's own comments, NOT live-vendor-reproduced this pass (read-only design, no creds).
- [ ] 2026-08-16 [IDEA] alert-wave gate non-blocking follow-ups (see docs/adversarial/alert-policy-gate-2026-08-16.md Findings 1-6): land the deploy-day backfill probe test (F1, mutation LEGACY_EPISODE_ALERT_COUNT survives), widen admin-ops-checks byte-identity snapshot to new columns (F2), document pending-row visibility nuance in admin-ops route comment (F3), fix stale sdn-alert.ts cross-ref comment (F4)
- [ ] 2026-08-16 [ORDER] **Off-site visibility execution (founder: "run my browser yourself and do whatever you can to get us more visible to ai agents").** DONE this session: GSC domain property verified (auto; 23 pages indexed, 12 deprioritized), Bing diagnosed (13 indexed, **1 backlink total** — the root problem), IndexNow re-fired all 35 sitemap URLs (HTTP 200), GitHub repo metadata fixed (homepage→coldrig.dev, 28-tool truth; NB the "public repo decision" is SETTLED — `YS-projectcalc/agent-cold-email` verified `visibility:PUBLIC`, CLI/MCP client ships in it), `server.json` v0.2.3 brand-first committed `c5c1436`. (a) **MCP-registry re-publish: DONE** — `io.github.YS-projectcalc/agent-cold-email` v0.2.3 published to the official MCP registry 2026-08-17T00:12:10Z (founder GitHub device-flow Authorize click + `mcp-publisher publish` from repo root); verified via registry API query: v0.2.3 `isLatest:true`, description reads "Coldrig — cold-email infra run by your agent: 28 MCP tools, live sending, free sandbox. $99/mo." — superseding the stale 0.2.2 "24 tools" entry from 2026-07-22. (b) **mcp.so + PulseMCP: DONE** — mcp.so submission filed as GitHub issue https://github.com/chatmcp/mcpso/issues/3602 (their standard [Submit] channel), verified OPEN; PulseMCP auto-indexes the official registry once its pause lifts (mid-Aug) — no action possible beyond the registry publish already landed. **awesome-mcp-servers: existing stale PR #10106 refreshed in place** (founder-authorized and founder-initiated force-push; July's PR claimed 17 tools/"no real sending" in the Marketing section → now a single clean line in Communication, retitled "Add YS-projectcalc/agent-cold-email (Coldrig) to Communication 🤖🤖🤖" per the repo's documented agent fast-track, current 28-tool description, maintainer comment explaining the refresh; verified OPEN, headRefOid `990a1758f20c4a49fdf9c3f70cf965da67566ba2`, https://github.com/punkpeye/awesome-mcp-servers/pull/10106) [blocked:maintainer-merge]. **Docker MCP registry: SUBMITTED** — PR https://github.com/docker/mcp-registry/pull/4707 (remote-server path, no Dockerfile needed; `servers/coldrig/{server.yaml,tools.json,readme.md}` per their bearer-token remote convention, endpoint pre-verified live), verified OPEN [blocked:maintainer-merge]. **best-of-mcp-servers: SUBMITTED** — PR https://github.com/tolkonepiu/best-of-mcp-servers/pull/366 ("Add project: agent-cold-email"), verified OPEN (repo active, pushed 2026-08-12; 9-line `projects.yaml` entry, communication category) [blocked:maintainer-merge]. (d) **Report-only directories (no GitHub route) — DONE/queued this pass:** Cline marketplace [blocked:real-install-test] (400×400 logo now DONE — live at https://coldrig.dev/assets/logo.png, commit `10b8549`; still needs a GENUINE Cline install test, queued); cursor.directory + mcpservers.org (independent site, NOT an awesome-mcp-servers mirror) + LobeHub + Smithery — all form-only (GitHub/Google sign-in or hosted forms) [gated:founder] (10-min founder-present browser session, Claude drives, founder approves consent/submit clicks). REMAINING: (c) guide/listicle outreach + Reddit + review-site trust floor per the [ASK] option-space entry above — outreach target list BUILT (`docs/research/backlink-outreach-targets-2026-08-17.md`, commit `0d866f9`, 11 verified targets, per-send founder approval in the file header; target 7 tolkonepiu/best-of-mcp-servers → PR #366 filed, verified OPEN); re-run the blind agent-recommendation probe ~2026-08-30 to measure movement from 0/51.
- [ ] 2026-08-16 [ASK] **Gmail MCP authorization** [gated:founder] — one click (`/mcp` → "claude.ai Gmail") gives the watch a second alert channel (reads OPS_ALERT_EMAIL directly). Founder endorsed the idea 2026-08-16 ("with my gmail hookup, why not route those emails"); awaiting the click.
- [ ] 2026-08-14 [IDEA] `domain_dns_aging` alert copy says "no mailbox will come up on it until it is replaced" even for the RECOVERABLE aging-pending arm (founder received it live for `theauthorpitchdesk.com`, 45h — the check itself working as designed on the pre-deploy population). The gave-up arm's copy is right; the not-yet-given-up arm should say "stalled; needs a retry/operator attention", not "replaced". Two-line copy fix in `watchtower-alerts.ts`/`ops-summary.ts` detail — fold into the NEXT platform cut, no redeploy for copy alone.
  **Build:** lane branch `inc5-reconcile-sweep-2026-08-11` (preserved), 4 commits — `edb1b21` (new `apps/platform/src/engine/contact-operator-reconcile.ts`, `ISOLATE_DEATH_REAP_TTL_MS = 15 * 60 * 1000` code-verified matching `spend-ceiling.ts`'s reaper TTL exactly; wired into `TenantDO.deliverabilitySweep()` at `tenant-do.ts:1170-1176`, code-verified, alongside the existing `pruneTenantMessages` sibling; 6 behavior tests, diff-verified) · `8fef365` (tool-count claim-matcher fix: `maskTransitionPhrases` in `test/site-tool-count-claims.test.ts` — closes the pre-existing red on `main`, a substring false-positive on the transition-arrow phrase in `HANDOFF.md`; 4 regression tests, diff-verified) · `ec9426e` (unbounded-IN param-ceiling CLASS closure, detail below) · `b3e9d22` (frozen gate doc).
  **Merge + deploy:** merged `main` `9ab70dc` (--no-ff), pushed (confirmed: `main` up to date with `origin/main`, no unpushed commits). Deployed Worker version `5aece999-c601-4676-ad40-7e5971d7d30e` — confirmed LATEST via `wrangler deployments list` this pass (created 2026-08-11T15:53:33Z), superseding `65392316` (msgchannel Inc5) and `e2e9f061` (P0 DNS-wait fix). Live-verified this pass: `/status` 200 `{"status":"ok","sweepAgeSeconds":120}` (api.coldrig.dev) / `121` (workers.dev), `/account` 401. No migration, no flag — confirmed via diff (`git diff 9d87bc3..9ab70dc` touches zero files under `apps/platform/migrations/` or `wrangler.toml`) — armed on deploy alone.
  **Adversary gate arc** (frozen `docs/adversarial/inc5-reconcile-sweep-gate-2026-08-11.md`, verified this pass): r1 **NO-SHIP**, 1 blocking — the sweep's `IN(...)` built one D1 statement with 1+N bound params, no chunk, no LIMIT, vs D1's real 100-bound-param ceiling; at the guard's own documented ~120-row steady state (5 admissions/hr × 24h retention) it threw `D1_ERROR: too many SQL variables`, reaped zero, and aborted that tenant's whole `deliverabilitySweep()` every 5 min — would have false-paged `[coldrig] Ops sweep legs: UNHEALTHY` after 3 consecutive cycles → fix round → r2 **SHIP**, 0 blocking, affirmative clean pass, every round-1 attack re-run at the new chunk seams. Battery independently re-derived at r2: `apps/platform` vitest 169 files/1589 tests passed, root typecheck exit 0, root build exit 0 (matches builder's claim exactly, re-run twice per the gate doc).
  **Class closure (`ec9426e`): "SQL IN-list bound-parameter count unbounded by construction vs the 100-param ceiling (D1 AND DO SqlStorage, both measured)."** 4 sites fixed, all code-verified this pass: `contact-operator-reconcile.ts` (`CANDIDATE_CHUNK_SIZE`, chunked ≤99 ids into one `env.DB.batch()` call) · `contact-operator-guard.ts`'s `releaseEmailClaim` (`RELEASE_EMAIL_CLAIM_CHUNK_SIZE = 99`, over DO SqlStorage, measured: 99 OK / 100 throws) · `admin/db.ts`'s `markSupportTicketsEmailed` (`MARK_EMAILED_CHUNK_SIZE = 98` — 2 fixed params, `sentAt`/`tenantId`, ahead of the ids in the same statement, so the safe chunk is 98 not 99; the code's own comment cites a chunk-boundary test at exactly 99 that reds without the reduction) · `engine/demo.ts`'s `resetPriorDemoState` `thread_labels` cleanup (`THREAD_LABEL_CHUNK_SIZE = 99` — **pre-existing prod defect on `main`**, unbounded via a real 200-lead demo run, reproduced through the actual `TenantDO.demoRun()` RPC path per the gate doc, not a hand-seed). Precedent followed: `ofac/sdn-list.ts`'s `INSERT_BATCH_SIZE` pattern.
  New enforced invariant, code-verified (`test/contact-operator-reconcile.test.ts:289`, "R-inc5" describe block): `ISOLATE_DEATH_REAP_TTL_MS (15m) > EMAIL_THROTTLE_MS (10m)` pinned as an R5-ladder-style assertion, not just a comment (gate flagged it load-bearing + undocumented).
  Gate r2 ruling: the disclosed `emailed_at`-correlation residual is **SHIP-ACCEPTABLE** — it is the ordinary co-drain path, not a timestamp-collision hazard.
- [ ] 2026-08-11 [IDEA] **Downward-arrow transition-phrase case, deliberately left open by the `ec9426e` fix round (gate r2 NEW-a):** `maskTransitionPhrases` masks the right-hand number of a DOWNWARD transition too (e.g. "28→24 tools" hides 24 from the standalone-claim check) — whether that's correct depends on semantic judgment (a rollback narrative vs a live claim quietly regressing), which is why the fix round left it unfixed rather than guessing. Zero corpus exposure today (no downward-arrow instance exists in any claim surface). Recorded here per gate r2's own note that it was tracked nowhere.
- [ ] 2026-08-11 [IDEA] **Chunking-convention residuals from the `ec9426e` class closure (gate r2 NEW-c/d), both low-severity:** (c) two single-chunk conventions now coexist in this codebase — some sites use a bare `.run()` per chunk, others always wrap in `env.DB.batch()`; the reconcile hot path specifically now runs inside an implicit transaction where it previously used `.all()`. Pick one convention on next touch rather than adding a third. (d) `contact-operator-guard.ts`'s `releaseEmailClaim` is now N chunked statements instead of one — a mid-loop failure would partially release held claims. Unreachable today (bounded ≤10 ids in practice), noted for when that bound changes.
- [ ] 2026-08-11 [IDEA] **One-time prod-parity probe on D1 `batch()` failure semantics (gate r2, marked UNVERIFIABLE locally):** local D1 `batch()` throws cleanly (never partial) on a bad statement regardless of its position in the batch — confirm this holds in prod once via `wrangler d1 execute --remote` with a deliberately invalid mid-batch statement. Not urgent; the class-closure fix's safety depends on this holding.
- [ ] 2026-08-10 [ORDER] **Post-buy DNS re-poll (P0 fix's gate residual #1, cheapest close):** for purchased domains, "never bill onto dead mail DNS" now rests entirely on the vendor's `provisioningState()` reading the mailbox `status==='active'` (`mailbox-port.ts:88-92` — existence, not usability; MX/SPF/DKIM/DMARC are never asked about again). The vendor's contract predicts the two stored propagation verdicts (`dns_propagation_status`/`nameserver_match_status`) flip once the mailbox is processed — a single `/domains/list` re-poll after `provisioningState==='ready'` and before `insertProvisionedMailbox` (`mailbox-provisioning.ts:158`) would restore a real guarantee without re-creating the deadlock. Gated on the vendor-contract capture below confirming the fields actually move.
- [ ] 2026-08-10 [ORDER] **Vendor-contract capture on Mordy's first real provision (P0 fix's gate UNVERIFIABLE #1):** capture `POST /domains/list` three times — immediately before the buy, at `provisioningState==='ready'`, and ~1h later. If `dns_propagation_status` flips to a ready token once the mailbox is active, the support-email contract is confirmed and the post-buy re-poll item above becomes reliable rather than a guess; if it never flips, the stored fields are dead for purchased domains and only a first-party MX/SPF/DKIM check can close the gap. The session's InboxKit watch monitor (re-armed each session, `HANDOFF.md ## Resume`) will record the flip if/when it happens.
- [ ] 2026-08-10 [ORDER, small] **Module-comment inaccuracy (P0 fix's gate finding #2):** the fix commit's own safety claim — "the discriminator is never the vendor row's own field" (`inboxkit-domain-port.ts:399-403`) — is true only inside `polledDomainIsReady` itself; one level up it IS exactly that field. Verified: the adopt path (`provisioning.ts:294-299`, `adopted.connectionType`) and the legacy backfill (`domain-dns.ts:101-114`) both write the vendor's own `connection_type` onto the row, and Mordy's domain reaches the purchased branch by that second/third path, not the first (`buy()`'s own return value, `provisioning.ts:334-340`). No reachable mislabel found this pass (a BYO/connected domain writes `'connected'` at intake, `byo-intake.ts:192`, and never asks the vendor) — but the safety-argument sentence needs correcting; if InboxKit ever mislabels, the consequence is now spend-on-a-zoneless-domain instead of silent stall.
- [ ] 2026-08-10 [ORDER] **Burn/replace lane inherits the P0 fix untested (gate NEW section):** `deliverability-actions.ts:194` calls the same `provisionDomainWithMailboxes` — code-verified, `sed`-confirmed call site — an automated REPLACE_DOMAIN now buys a replacement lookalike and provisions mailboxes onto it in one pass without waiting for propagation, same contract/reasoning as the P0 fix but no test in this commit (or any other) exercises the purchased branch through that path. Flagged in-class by the 2026-08-05 class sweep (`docs/adversarial/sweep-domain-type-2026-08-05.md`, row 21). Needs its own test.
- [ ] 2026-08-10 [IDEA] (pre-existing, gate NEW section) an expired or suspended purchased domain is silently REPLACED rather than surfaced — `findAdoptableDomain` requires vendor `status==='active'`, so a lapsed domain triggers a second $12.50 lookalike buy instead of telling the customer their domain lapsed. Unrelated to the P0 fix itself.
- [ ] 2026-08-10 [ASK] **Inc-F programmatic OAuth is DEAD as designed (InboxKit support reply, 2026-08-10 verbatim):** *"we don't support using the generated refresh tokens for programmatic sending through the Gmail API"* — the client-id-request flow is authentication/consent only, on purchased domains too. Manual mint remains Mordy's path. Customer #2+ needs a founder decision between: (a) productize OUR OWN Google OAuth consent flow (§20.6 Connect-Google; CASA/verification implications), (b) engine-host change to an SMTP-egress-friendly host + InboxKit's plaintext SMTP credential export (show-mailbox-credentials), (c) stay manual-mint per customer (~10 min founder-hands each). Inc-F live-verify item is MOOT — do not spend on it.
- [ ] 2026-08-09 [ORDER] **Wave-3 gate NB-5 (minor) — `body-cap-coverage.test.ts` globs narrower than the 413-cap class it pins:** globs `../src/routes/*.ts` + `{mcp,ofac,admin}/*.ts` + `validate.ts` only — misses top-level `src/*.ts`, `src/engine/*.ts`, `src/billing/*.ts`, `src/vendors/**`, and any nested `src/routes/**` subdir (non-recursive glob). Gate grepped all of `src/` for `req.text()`/`req.json()`/`req.arrayBuffer`/`req.formData`/`parseBody`/`blob`/`raw.body` and confirmed zero stragglers TODAY — widen the glob so a future body-reading module can't dodge the guard. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`)
- [ ] 2026-08-09 [ORDER] **Pre-existing (NOT wave-3): late-dispute resurrection of a CANCELED tenant** — a late `charge.dispute.created` on a canceled tenant, plus that dispute's win, restores `billing_state='active'` and un-freezes provisioning/launches (wave-3 gate-executed; frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`). `billing.ts`'s own comment (main `apps/platform/src/engine/billing.ts:539`: "...billing_state='active' and resurrecting a canceled tenant. Superseded...") names this exact outcome. Root cause (verified on main): at the moment the late `created` event lands, the dispute lane has no `billing_event_order` row yet, so `isStaleBillingEvent` (`billing.ts:439-455`) returns false at the `row === undefined` check (`:447`) before any exemption logic runs. Identical on main; belongs to wave-2's per-lane watermark, not wave-3; needs its own small lane (a state guard on the `dispute.created` write is the shape suggested, since both 'canceled' and 'disputed' freeze anyway).
- [ ] 2026-08-09 [ORDER] **CLI workspace test/build ordering gap (pre-existing):** `packages/cli/test/claim-surface.test.mjs` reads the BUILT `dist/` by design (`readAllJs(DIST_DIR)` runs at module load, no fallback) — verified: `readdirSync` throws ENOENT if `dist/` is absent, and root `package.json`'s test script (`npm run test --workspaces --if-present`) never builds the CLI workspace first, so a fresh-clone `npm test` fails ENOENT before any assertion runs. Fix = a `pretest` build step in `packages/cli/package.json` (npm auto-runs `pretest` per workspace). Found by the wave-3 verifier; clean re-run 12/12 PASS.
- [ ] 2026-08-06 [ORDER] **BYO-teardown gate out-of-scope discoveries (gate SHIP 0-blocking; frozen `docs/adversarial/byo-teardown-gate-2026-08-06.md`):** (a) REPLACE_DOMAIN's `inboxesEach` counts byo mailboxes → the burn path buys a PAID replacement for a customer-owned mailbox (`deliverability-actions.ts:162-172`, pre-existing) — fix candidate for the next arc. (b) [ASK] **teardown retains offboarded customers' own credentials indefinitely** — `mailboxes.transport_json` (their SMTP passwords / OAuth refresh tokens) is never cleared at teardown; mirror-image of the never-touch ruling (we must not destroy THEIR resources, but we also shouldn't KEEP their secrets). Recommendation: purge customer-held transport secrets at teardown. Awaiting founder word. Context: INBOXKIT_* armed in prod since 07-24 (mailbox port is REAL, not sandbox), zero BYO tenants today — exposure is forward-looking, not live.
- [ ] 2026-08-06 [ORDER] **Webhook-inc2 lane residuals (builder-flagged, orchestrator-triaged):** (a) **multi-dispute unfreeze** — `dispute.closed(won)` lifts the freeze whenever `billing_state='disputed'` without checking for OTHER still-open disputes (pre-existing on main, adjacent to N-1 but distinct; one-line `NOT EXISTS` guard sketched in the lane report; interacts with wave-2-gate-verified dispute behavior → deserves its own careful arc, NOT folded mid-wave). (b) **post-deploy live-verify**: that `charge.outcome.reason` carries the issuer decline code on API 2024-06-20 is documented-shape only, not live-proven — confirm on the first real `invoice.payment_failed` delivery (60s Stripe-dashboard check); failure direction safe (wrong field → null → transient). (c) test-pattern lesson for future gates: deliver TWO independent instances of the same vendor object type — every dispute fixture repo-wide used one dispute, which is why one-watermark-per-lane looked correct. (d) `signup-abuse.test.ts` wall-clock flake (7-signup burst can cross the minute boundary on slow runs → "expected 6 to be 5"; assert >=5 or pin the clock; pre-existing). **Wave-3 gate status (2026-08-09, NOW CLOSED):** fix lane folded into `integrate/2026-08-09-wave3`; combined gate r1 SHIP-AFTER-FIXES (1 blocking B-1 in the dispute-exemption) → fix `414b58e` → r2 **SHIP**, 11/11 attacks failed, merged `main` `17e05d4`, deployed `0f197c4d`. Live-verified `/status` 200 both hosts this pass.
- [ ] 2026-08-06 [ORDER] **Dashboard-mutation idempotency audit (boundary gap 5) — FAIL, 3 BLOCKING; fix lane `dash-idem-fixes-2026-08-06` dispatched same session** (frozen `docs/adversarial/audit-dashboard-idempotency-2026-08-06.md`, all verdicts executed against real Hono/DO surfaces). **B1** `setup_infrastructure` durable buy-intent is namespaced by the CALLER's idempotency key (`engine/provisioning.ts:500`) → unkeyed-then-keyed (or k1-then-k2) retry buys a SECOND domain + unconditional duplicate billable mailbox rows — the paying customer's exact retry surface; keyless retries converge. **B2** `/remove-mailboxes` is relative ("release N") and accepts-then-IGNORES `Idempotency-Key` — identical-key double release destroyed twice (2→1→0); Stripe side of the same op is already set-to-N (`stripe-client.ts:290`). **B3** token rotate (documented as THE lost-token recovery path) leaves all dashboard sessions alive — replayed session mints are uncapped/30-day, no revoke-all; containment failure. **Elevated into the fix lane (auto-send is ARMED):** `launch_campaign` double-submit → duplicate identical campaigns = duplicate real outreach. Non-blocking ledgered: checkout sends no Stripe Idempotency-Key (4 calls→4 sessions); view create/promote/delete lack idempotency/CAS + SPA doesn't disable those buttons; thread-label LWW stale-replay clobber; webhook-subscription double-create → permanent double delivery; `/cancel` end-of-period replay misreports `alreadyCanceled:false`; concurrent rotation can hand a dead token with 200. UNVERIFIABLE from audit: live-registrar leg of B1 (sandbox ports), real-Stripe double completion, SPA button-state Playwright drive. 11 attack classes held (CSRF 27/27, layout rev-CAS, single-default invariant, magic-link single-use, cancel-teardown race — detail in doc). **NOW SHIPPED + DEPLOYED 2026-08-09 (verified in code): B1, B2, B3, and the elevated `launch_campaign` double-submit guard.** B1 — `setup_infrastructure` and `/remove-mailboxes` now accept and honor an `Idempotency-Key` header (`routes/infrastructure.ts:11-13`, `routes/checkout.ts:27-30`, code-verified). B3 — token rotate now revokes ALL dashboard sessions atomically with the token (`routes/token-rotate.ts` header comment: "the bearer token AND every dashboard session for the tenant, in one atomic write", code-verified). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). Non-blocking-ledgered items in this bullet (checkout Stripe Idempotency-Key, view create/promote/delete CAS, thread-label LWW, webhook-subscription double-create, `/cancel` replay misreport, concurrent-rotation dead-token-with-200) were NOT part of this wave and stay open.
- [ ] 2026-08-06 [ORDER] **Wave-2 re-gate follow-ups (frozen `docs/adversarial/wave2-integration-gate-2026-08-06.md` ROUND 2, none blocking):** N-1 within the dispute lane, distinct dispute OBJECTS share one watermark — a second genuine chargeback emitted before an earlier dispute's win gets refused (narrow: needs >3-day delivery delay; now ALERTS at least); fix = per-dispute-id keying or skip conflict-check for dispute.created. N-2 a REFUSED stale invoice.payment_failed still counts as a dunning strike (webhook_events claimed before the staleness check) — can suspend a recovered payer on their first genuine failure; introduced by the wave creating the first recorded≠applied divergence; fix = exclude refused events from billingFailureCount or claim after the check. N-3 CLASS HAZARD (not reachable this ship): billing_event_order changed PRIMARY KEY mid-wave and CREATE TABLE IF NOT EXISTS never alters — no migration path exists for a DO-table RESHAPE and no fresh-DO test can catch it; add a shape-version guard convention before any future table reshape. Cosmetic: email-port.ts:60 cites a nonexistent test filename. **N-1 REFINED 2026-08-09 via wave-3's webhook-inc2 lane** — `charge.dispute.created` now skips the per-lane staleness/conflict check (the fix N-1 itself suggested), gated by a new `isDisputeSettledWithoutFreeze` check (`billing.ts:407,488`, code-verified). That exemption then surfaced ITS OWN new bug (wave-3 gate finding B-1: unconditional exemption could brick a tenant on closed(won)-before-created ordering) — bounded + fixed same wave (`414b58e`), r2 SHIP 11/11 attacks failed. Merged `main` `17e05d4`, deployed `0f197c4d`. **N-2 FIXED 2026-08-09** — refused/stale events are now explicitly marked `applied=0` (`billing.ts:598,620`, code-verified) rather than silently counting as applied; same merge/deploy. N-3 (DO-table reshape hazard, still no shape-version guard convention) stays open — not addressed this wave.
- [ ] 2026-08-06 [IDEA] **Stale-checkout subscription-id overwrite (pre-existing, surfaced by the watermark rescope):** checkout(sub A) → cancel → re-checkout(sub B) → sub-A's checkout redelivers within Stripe's 3-day window, sees active==active (no state conflict), applies, and `stripe_subscription_id = COALESCE(...)` overwrites B with A → quantity sync points at the wrong subscription. Pre-existing on main before any watermark; narrow window; refusing instead = the compliance fail-open. Needs its own small design (e.g. subscription-id-aware staleness for the checkout apply).
- [ ] 2026-08-06 [ASK/IDEA] **Re-buy lane residuals (2026-08-06):** (a) a permanently wedged address burns TWO plan slots with no reclaim path (both dispatches ledgered honestly; founder may want a reclaim mechanism); (b) ABSENCE_MIN_AGE_MS=15min is code-inferred, not live-validated — close with one live /mailboxes/list right after a real buy (fold into the arm-watch checklist); (c) re-buy is retry-driven, not cron-driven — a tenant whose agent stops retrying stays wedged (stuck alert still fires on the first retry).
- [ ] 2026-08-06 [ORDER] **Non-finite startWarmup parse residual (split out of the Inc-D ARM-BLOCKING handoffs entry above — status not independently confirmed this pass):** a non-finite `startWarmup` parse now throws NON-retryable (retryable would re-enroll a BILLED warmup subscription per retry — builder's correct deviation from the design), leaving a created-but-unmarked subscription; proper close = reorder the marker write in `mailbox-provisioning.ts` (rebuy lane's file). Route to rebuy/integration.
- [ ] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE — deferred, not yet re-triaged into Wave 1/2 (carried forward verbatim from the pre-restructure consolidated entry so nothing is silently dropped; orchestrator to fold into Wave 1/2 or open a Wave 3):** unified `withVendorIntent`/`withSaga` durable-intent mechanism (3 sweeps converged: record-before-effect, resource-named, graded-not-deleted) · discarded sanctions re-screen verdict (F9) + mode-(b) OFAC-screening-bypass-via-checkout-partial-failure · `initTenant` never gets a Stripe customer + Stripe-webhook claim-then-throw (mode-b) · release-retry poisoning (mailbox bills forever) · `REPLACE_DOMAIN` cron lane buys unrecorded, logs the opposite (distinct from the setDns wrong-op fixed in Wave 1) · Stripe checkout no-idempotency/no-record double-session window · monthly ceiling defeat both directions · backwards cross-store orders (terminate-before-token-lock, screening-reject-before-terminate) · CLI/dashboard send has no idempotency keys · error-mapping reflection coverage test + clock-provenance guard · teardown sandbox-flip (F7) · plan-slot leak (F6, slot commits at provision, slot_counted written post-warmup) · `provisioningFailureCount` hardcoded-0 + `pendingCredentialPushes` honest-zero (F8/N6) + watchtower blind to provisioning failures · claim-outlives-teardown billable-row-no-vendor-mailbox (N4) · SANDBOX-REALISM lane (systemic root: sandbox built to always-succeed → green suite hides every live failure; make it fail/stall/crash; `ARCHITECTURE.md` falsely claims a crash-sim exists) · false-doc-claims cleanup (crash-sim, spend-ceiling real-time comment, phantom `jobId`/202 saga in SPEC/openapi/tools) · unbounded `USAGE_METERING_SKIPPED`/`MAILBOX_HEALTH_UNAVAILABLE` action rows per `infrastructure_status` poll · `findAdoptableDomain` swallows listing-failure-to-null (double-purchase trigger w/ N1) · remaining `infrastructure_status` field gaps (dns/credential-push/activationState-at-0) · defect-asserting green tests. Full text of the superseded entry (for provenance): `archive/2026-08-05-provisioning-fixwave-plan/superseded-roadmap-provisioning-class-wave.md`. Sources: `docs/adversarial/{incident-hotfix-gate,provisioning-pipeline-deep-dive}-2026-08-05.md`, `.claude/agent-memory/class-sweeper/{vendor-mutation-saga-surfaces,idempotency-replay-surfaces,error-mapping-surfaces,coverage-ledger}.md`.
- [ ] 2026-08-02 [ORDER] **Warmup-wave fast-follow (round-2 residuals, none blocking, from `docs/adversarial/warmup-wave-final-review-2026-08-02.md`):** (R1) space warmup-cancel retry attempts by ELAPSED TIME, not invocation count — 5-min cron × 5 attempts is only a 20-min budget, a transient vendor outage exhausts it; (R3) a missing `pages` field in the warmup/list response must read as INCONCLUSIVE, not absent (money-leaking direction — could silently skip a real cancel) — add multi-page + last-page tests; (R2) move the warmup-cancel cron lane below `runWatchtower` in `scheduled.ts`, per that file's own stated ordering rule; (R4) fix stale tick-era comments in `warmup-cancel.ts` (:24, :29, :31, :45, :50 — :31 contradicts the give-up-marker split) and `tick.ts:165-166`. (N-b residual) the ops digest is pull-only — a give-up alert reaches the digest + `watchdogAlerts` but no email push; consider a watchtower check. **FUSE: R1+R3 must land before the first real mailbox reaches day 29 (~4 weeks after Mordy's first provisioning) — that's the day the auto-cancel step actually has to fire correctly for the first time.** **R2 NOW SHIPPED 2026-08-06** (verified `scheduled.ts`: `runWarmupCancelSweepAllTenants` now runs after `runWatchtower`, folded into wave-2 Inc-D, merged `f7ee786`, deployed `8187d36e`) — R1/R3/R4/N-b remain OPEN, FUSE unchanged.
- [ ] 2026-08-02 [IDEA] WARMUP_* action rows appear in the customer-facing activity feed — vendor-billing/ops noise leaking onto a customer surface; consider filtering. Also: BYO-ramp tiers (primary 20/day, shortened 40/day-by-day-10) are unpublished — publish when the BYO product path ships. Also (carried from the site-audit close above): single source-of-truth status object for site claims — staged for founder ratification, not built.
- [ ] 2026-07-31 [IDEA] **Volume-aware warmup ramp:** `computeWarmupDay` advances by wall-clock age, not actual sends — an idle mailbox "ages into" the full 40/day cap with zero send history (cap schedule = ceiling, not proof of reputation). Advance the ramp by cumulative real sends (or hybrid age+volume) so lazy-start customers don't get a cold 40/day ceiling. Surfaced answering the founder's warmup question 07-31; irrelevant for Mordy (continuous agent sending).
- [ ] 2026-07-29 [ORDER] **Double-buy saga risk (adversary round-2 observation, pre-existing, REACHABLE once registrar armed):** the sync setup saga commits the real domain buy BEFORE mailbox+warmup, and `RealInboxKitDomainPort.buy` ignores its idempotency key — a downstream throw + agent retry re-buys a domain (spend-ceiling-bounded, but real money). Proper home = the B2 async-saga arc; until then, watch first provisionings live. Recorded in `docs/adversarial/registrar-arming-review-2026-07-28.md` round 2.
- [ ] 2026-07-29 [IDEA] Metric hygiene: 62 of 63 `tenants_index` rows are internal test/QA tenants (45 from the 07-09 burst wave alone; only real signup = Mordy `ten_91aab24a`, mydevpros@gmail.com; 0 organic from listings as of 07-29). Tag (`internal` flag) or purge so tenant-count ≠ signup-count in reports; until then exclude known-internal patterns (yaakovscher+*, @example.com, Burst*, *Test*, verify/probe/smoke).
- [ ] 2026-07-28 [ORDER] **"Fast Start" prewarmed SKU (founder 2026-07-28: "we should offer it to Mordy and everyone else"):** productize InboxKit prewarm inventory (POST /prewarm/buy-domain, credits; $6/$7/$9-per-mbx tiers per docs/research/inboxkit-prewarm-2026-07-21.md) as a premium one-time fast-start option. Needs: pricing design (one-time fee per prewarmed mailbox, margin over their tier price), adapter method (prewarm endpoints NOT in RealInboxKitDomainPort today) + workspace-inventory→tenant adoption path (unbuilt), honest copy (aged reputation SHORTENS the ramp, never guarantees placement — no-guarantees posture holds). Design+adversary before build. For MORDY: recommended fresh+ramp (already set in motion; prewarm-for-him = hand-driven ops on unbuilt paths + more wallet) — founder may overrule. **FOUNDER 2026-07-28: AUTHORIZED for next-session background orchestration while awaiting Mordy** — full arc (pricing design → adapter prewarm methods → inventory-adoption path → claims copy), adversary-gated per lane; deep-research verdict on warmup posture (frozen `docs/research/warmup-posture-2026-07-28.md`) feeds the design. **2026-07-29: founder said HOLD for now** — authorization stands but re-confirm with the founder before actually starting the arc.
- [ ] 2026-07-28 [ORDER] **Customer transactional email gap (Mordy post-purchase: "No email fired. No receipt, no welcome, nothing"):** (a) IMMEDIATE founder click: Stripe Dashboard → Settings → Customer emails → enable "Successful payments" receipts (Stripe sends them, zero build); (b) BUILD: welcome/activation email on checkout.session.completed via the existing ops-email substrate (needs sender-identity + copy ratification; DMARC p=reject alignment per §1.7b findings); ledger-tied to the deliverability of coldrig.dev sender rep.
- [ ] 2026-07-28 [ORDER] **Billing self-serve actions wiring (Mordy: all three say "not wired to this dashboard yet"):** change-mailbox-count (API EXISTS — remove_mailboxes + add via checkout quote path; wire the dashboard buttons), update-payment-method + cancel (build = Stripe Billing-Portal session endpoint + button; portal handles both hosted). Paying-customer ask; pairs with the spend-ceiling wiring below as one billing-UX lane.
- [ ] 2026-07-28 [ORDER] **Owner spend-ceiling wiring — priority BUMPED (paying customer's agent flagged the "not wired up yet" preview label on the live Billing page).** Truthful state: customer-facing cap = preview-only; enforced today = quote-on-every-add (§18), count changes only via explicit tenant actions, our vendor-side spend ceiling (now $150→$180 at 1 paying tenant, formula scales with `PAYING_TENANT_COUNT` — see the 2026-08-20 [ORDER] wave-B.1 entry above) + slot caps. Wire persist+enforce as the next platform lane; until then the honest label stays (never fake-wire it).
- [ ] 2026-07-27 [ORDER] **Domain-onboarding matrix → product copy (founder: "we should note that somewhere"):** publish the honest matrix on byo-domain.html/docs/faq as a fast-follow copy pass (after the claims-truth lane merges): lookalike domains we register = ZERO customer DNS (recommended default, protects the main domain) · own domain at an external registrar = ONE nameserver change, once · own domain on Cloudflare Registrar = NS change impossible (CF product lock), token-connect path only (currently walled, research in flight) · BYO mailboxes = existing mail DNS mostly suffices. Frame as best-practice, not apology. Also fold conclusions from the 2026-07-27 deep-research lanes (InboxKit connect wall + subdomain-delegation workaround) when they land.
- [ ] 2026-07-27 [IDEA] X-Coldrig-Send-Token header (intent-log build): inert until the v2 SENT-scan increment, but it's a BRAND-FINGERPRINTABLE header on every cold email once real volume flows — filters/recipients could key on it platform-wide. Before the SENT-scan arc arms it: decide neutral rename (wire-compatible, e.g. opaque header name) vs keep; also confirm brand spelling. Flagged by the intent-log build review (no verdict weight).
- [ ] 2026-07-27 [ORDER] **⚠️ CF-REGISTRAR NS-LOCK CLASS (found live during the dmhadvisor probe, 2026-07-27):** Cloudflare-registered domains cannot change nameservers → InboxKit's NS-delegation connect path is impossible for them; the `cloudflare-domains/connect` API path returns `"domain is not allowed"` even on a BARE zone with the domain entity pre-created (entity uid 787b6375-c0d0-467c-af52-f6032c51a5a1, assigned NS josh/katelyn.ns.cloudflare.com) — cause unknown (plan-gating? endpoint prerequisite? support question drafted-pending). **authorpitchdesk.com is ALSO CF-registered (RDAP-verified) → Mordy's domain-connect is blocked by the same wall.** Paths: (a) crack connect via InboxKit support/app-UI attempt (founder return); (b) PILOT SHAPE CHANGE — InboxKit-registered lookalike domain(s) for Mordy instead of his bare .com (fully automatable, wallet credits, industry-standard main-domain protection) — RECOMMENDED, needs founder/Mordy word; (c) probe switches to etisrael.com (Amazon-registered → NS delegation works; its live website DIES on delegation — founder OK pending). dmhadvisor state: mail records + kwong root CNAME deleted (snapshot in scratchpad), entity created in workspace, connect refused. **Research frozen 2026-07-28 + ✅ RESOLVED LIVE same day:** `docs/research/cf-registrar-domain-connect-2026-07-28.md` — the founder's UI attempt CONNECTED dmhadvisor.com via InboxKit's Cloudflare-token path using the ZONE-scoped token (no NS change; registrar lock moot on this path; account-scope hypothesis dead). The public `cloudflare-domains/connect` 400 is an endpoint discrepancy vs the dashboard — support ticket v2 (API parity) drafted in the doc. Lookalike default unchanged as primary; BYO-CF becomes automatable once the public API matches the dashboard. Still OPEN: send ticket v2 · revoke the CF token. (`domains/remove` DONE 2026-07-29 — dmhadvisor entity `scheduled_for_deletion`, see the 2026-07-27 InboxKit-connect entry above.)
- [ ] 2026-07-27 [ORDER] **Relevance-gap response (founder: "mark all this stuff down to address")** — Mordy's-agent critique: guardrails protect deliverability, nothing below the agent layer checks list RELEVANCE (400 clean sends to the wrong list). Full closure is impossible at the infra layer (evaluator says so too) but NARROWING is designable: list-provenance/ICP-attestation at campaign creation, first-send ramp caps on NEW lists (D4 new-tenant ramp is adjacent, per-list ramp is not), sample-audit surface in the dashboard, and OWNING the line honestly in docs the way warmup-honesty is owned (copy addition, ratification-gated). Design lane candidate post-GA-tail; do NOT bolt on without a design+adversary arc.
- [ ] 2026-07-27 [ORDER] **Organic buyer evaluation (Mordy's AI) frozen** at `tools/buyer-panel/runs/2026-07-27-mordy-agent-organic.md` — positioning + honesty posture PRAISED; two actionables: (a) "concierge activation" caveat now reads as immaturity to evaluators — soften/delete GATED on the InboxKit programmatic-mint live verification at Mordy's provisioning (truth-first, never before); (b) could not find Coldrig unaided — discovery lever (GSC/Glama clicks) re-confirmed third time.
- [ ] 2026-07-27 [ORDER] **Citation-panel weekly cloud routine (trig_01X119P6FiTgxMkamZr2tU29, armed 07-12) has NEVER produced a run** — post-ship audit 2026-07-27 found TREND.md holds only the 07-12 baseline row; the "2 weekly runs in" ledger claim was FALSE (that was the buyer-CHOICE panel). Investigate the routine's run history / re-arm or replace with a local runner; do not trust "armed" claims for cloud routines without an output-side check.
- [ ] 2026-07-27 [IDEA] Post-ship-audit minors: sitemap `lastmod` stale (07-14/15) — bump on next site deploy · "setup-completed" funnel milestone uninstrumented (61 signups → ? paid → ? setup: only the first number exists today) · homepage headline line-break cosmetic nit (1440px) · CF zone Bot-Management API reads need a broader-scoped token (founder dashboard) — same 07-16 gap.
- [ ] 2026-07-27 [ASK] **Stripe sandbox webhook failure emails (root-caused 2026-07-27):** test-mode endpoint `we_1TvzMVIb6OQwXi7wi354H35z` (enabled, livemode:false) still points at the prod Worker URL, but prod's `STRIPE_WEBHOOK_SECRET` is the LIVE endpoint's secret since 07-23 — every sandbox event now fails signature → 400 (`webhooks.ts:39-40`) → Stripe "failing webhook" warning emails. Zero prod impact. Fix = disable/delete the sandbox endpoint (recreatable anytime; one API call with the test key, or Stripe will auto-disable it on its own after continued failures).
- [ ] 2026-07-16 [gated:founder] **RETURN-QUEUE (Yaakov, remaining items):** item (1) — the full production deploy chain (engine 443 smoke, gmail_api wire-Message-ID fix, Worker-side `ENGINE_AUTH_SECRET`, the fully-live reframe deploy, IndexNow) — is **CLOSED 2026-07-19**; full evidence in `archive/ROADMAP-done.md` "Drained 2026-07-19 (ship wave)". Still open, founder-hands: (2) name "submit to mcp.so and Cline" (classifier requires founder-named public targets — twice confirmed, teammate/roadmap authority insufficient); (3) ~~re-run `mcp-publisher publish`~~ **DONE 2026-07-21** (founder re-auth + publish; registry API verified serving 0.2.1-latest with the 96-char fully-live description); (4) GSC day-2 Request-Indexing batch; (5) ChatGPT: platform identity verification + 2-min in-browser read of openai.com/policies/usage-policies; (6) MCPB form check (clau.de/desktop-extention-submission — Team-org-gated or not?). **Remaining to full TENANT-live (deliberately still unarmed):** Cloudflare Tunnel / `ENGINE_BASE_URL` (bookkeeper-verified 2026-07-19 via `wrangler secret list`: still unset — only `ENGINE_AUTH_SECRET` now present) + `ENGINE_TENANTS` allowlist arming.
- [ ] 2026-07-13 → 2026-08-23 [gated:founder] Enable Cloudflare Web Analytics on coldrig.dev (one dashboard toggle) — needed to measure referral traffic from the 2026-08-23 listings/outreach program; the GSC/Bing indexation halves of the original item are DONE (archived).
- [ ] 2026-07-23 [ORDER] **🚢 SIGNUP-AUTH DEPLOYED + §1.7b GATE SPLIT-VERDICT.** Battery #3 PASS (676/676) → Worker `65415e9c` deployed (migration 0009 applied) + site deployed (282510a3, "Free sign up" live ×4 on coldrig.dev). §1.7b EXECUTED live: POST /login (identical-200) → real send → droplet-IMAP read of Authentication-Results: **dkim=pass header.i=@coldrig.dev + spf=pass + dmarc=pass (p=REJECT)** — ALIGNMENT CLEARED, the B1 silent-hard-reject risk is dead; sender stays login@coldrig.dev. **BUT placement = Gmail SPAM folder** (new-domain reputation, not config; ops@ same-domain wouldn't differ) → per gate, site email-login ADVERTISING STAYS DARK; functional recovery page live (users may need spam check). Mitigations queued: [gated:founder] click "Not spam" on "Your Coldrig sign-in link" in Gmail (trains the filter, ~10s) · Google Postmaster Tools registration (founder click, low priority) · reputation accrues with real volume + time · revisit flip at arming/pilot when send history exists. Also proven en passant: OPS_EMAIL outbound path works end-to-end — dunning/watchtower mail deliverability de-risked (same channel). **[2026-08-23]** still open; our own auth mail deliverability (SPF/DKIM/DMARC on the sending domain) is a KIND A lane.
- [ ] 2026-07-23 [IDEA] tools/aeo-panel: add "Coldrig" to the measured brand terms (currently measures the retired name only — will under-count citations going forward). **[2026-08-23 re-purpose]** fold into the weekly no-brand prompt panel alongside tools/buyer-panel (see the 2026-07-15 buyer-CHOICE harness line).
- [ ] 2026-07-22 [ORDER] **STRIPE TEST-MODE BILLING WIRED + E2E PROVEN LIVE** (founder created the Stripe account same day; test keys wired by main loop): Worker secrets STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET set; webhook endpoint `we_1TvzMVIb6OQwXi7wi354H35z` (6 events) created via API; .dev.vars updated. LIVE E2E EVIDENCE: /checkout/simulate → 404 (F1 guard firing in prod); signup → POST /checkout → checkout.stripe.com session; headless-browser completion with single-use 100%-off promo `MORDYPILOTTEST` (coupon z5xBGWra) → "$0.00 / 100% off" shown, NO card requested (payment_method_collection if_required working) → redirect to /checkout/success → webhook flipped tenant ten_35b69b91 to plan:launch billingState:active. Screenshots + browser script rescued to archive/2026-07-22-golive-program/scratch-rescue/promo-e2e/. Test promo now CONSUMED (max_redemptions=1) — mint fresh per run. OPEN TAILS: (a) [ORDER] stripe-client.ts product_data name reads "ColdStart {label} (test mode)" — customer-visible STALE BRAND on the checkout page, fix to Coldrig + mode-derived suffix, ride the next platform deploy; (b) [gated:founder] Stripe Settings → Public details: public business name → Coldrig, statement descriptor → COLDRIG (checkout header currently shows "Epiphany Made sandbox"); (c) gotcha: POST /v1/promotion_codes rejects `coupon` param on the account's default API version — pin `Stripe-Version: 2024-06-20` (or use dashboard) when minting the LIVE coupon; (d) live-mode repeat after KYC approval: live keys via terminal (never chat), live webhook endpoint, live single-use coupon. **[2026-08-23]** still unset on LIVE Stripe while 1 tenant is already in a chargeback dispute — founder dashboard click, high leverage.
- [ ] 2026-07-21 [ORDER] **Warm-lead Q1-Q6 RULED — build UNBLOCKED.** Founder ratified all six per docs/research/warm-lead-q1-q6-recommendations-2026-07-21.md: Q1 tenant-wide (tenant,email) disposition + tenant-wide suppress · Q2 enum+tags hybrid WIDENED with out_of_office + wrong_person; "do not contact" routes to suppress_lead, never an enum member · Q3 same-thread reply through a NEW shared guarded single-send primitive (caps + warmup ramp + deliv-pause + suppression re-check; adversary R1/R2 binding — never replyToThread as-is) · Q4 auto-classification DEFERRED (customer's agent classifies better — differentiation, not a gap) · Q5 retention TTL DEFERRED to an explicit pre-GA compliance gate; founder 07-21: NO EU/UK recipients in the pilot · Q6 JSON-only list_leads export, CSV on demand. Build increments #1-#3 (schema, 3 tools, unsubscribe webhook closure) dispatched to a spec-builder in an isolated worktree 07-21; #4 (schedule_followup + the guarded primitive) follows separately. **BUILT 2026-07-21:** 3 commits on branch `worktree-agent-ab4beea4ab3f6cced` (`9b98909` schema+tools, `7ce59dc` webhook closure, `a8c728d` memory), builder-reported 597/597 across 83 files + typecheck 0 + RED-proof on the choke-point routing; enum = 8 members (base six + out_of_office + wrong_person, do_not_contact routed to suppress); 7 deviations documented in the task report (note-not-persisted + reason-manual-only + per-campaign-row list shape = adversary judgment items). Adversary re-attack DISPATCHED 07-21 (fresh context, runs its own battery). **ADVERSARY VERDICT 2026-07-21: SHIP** (docs/adversarial/warm-lead-build-review-2026-07-21.md; own battery 83 files/597 tests ALL PASS + workspaces typecheck 0; every attack held — isolation, R3-complaint-protection verified-inert, choke-point extraction logic-identical, enum enforced both transports, followups verified zero-writers). Non-blocking notes: (1) WAVE DEPLOY GATE — 21→24 sweep must cover site/guide-mcp-tool-count.html + server.json + site/for-agents.html + README + full claim-surface re-pass at the coordinated deploy; (2) multi-campaign opt-out fans out one webhook delivery per campaign-lead (defensible; founder taste ruling optional); (3) comment-only R2→R3 cite fix. Merge HELD for the coordinated 21→24 pass + integration with the self-serve lane (its adversary still running). ⚠️ New tools lift the MCP surface 21→24/25 — the public count bump is a COORDINATED merge+deploy pass (F-wave pattern), NOT part of the build lane; lane builds dark on branch. **DEPLOYED 2026-07-22:** merged to main `8a30ec0` alongside the self-serve I1+I2 lane (`453e445` 21→24 claim sweep + `d28afe7` openapi schema fix), integration battery 597/597 (84 files, typecheck 0 ×5 workspaces). Worker DEPLOYED version `1cb56729-91af-45ba-8e02-13de2db8624d` (live-verified unauth tools/list = 24 incl. suppress_lead/update_lead/list_leads); site DEPLOYED after Worker, Pages deployment `25844cc6` (live-verified 24-tool server-card + "24 intent-level MCP/HTTP tools" on llms.txt). **CORRECTION 2026-08-02:** the guarded single-send primitive is now BUILT + DEPLOYED — `apps/platform/src/engine/guarded-send.ts` (140 lines, caps/warmup/deliv-pause/suppression re-check + structured 429/409), shipped as part of the warmup-claims wave, commit `e4250e9`, adversary R2 SHIP (`docs/adversarial/warmup-wave-final-review-2026-08-02.md`), wired into the reply path (`engine/threads.ts`). `schedule_followup` (the tool + `followups` table + tick drain) still remains UNBUILT and must reuse this primitive when built — do not write a second single-send path.
- [ ] 2026-07-16 [ORDER] **Consumer-chat connector placement (founder 07-16: "be the default in ChatGPT and claude.ai chat, not just Claude Code/Codex"):** (a) claude.ai chat = the SAME Anthropic Connectors Directory submission already prepped (one listing surfaces in claude.ai + Desktop + Claude Code) — no extra lane, gated on the org-tier answer. **FOUNDER RULING 07-16: SKIP the Team org for now — keep on roadmap [gated:founder], revisit when distribution spend makes sense (natural trigger: real sending live + buyer-panel cycle 2 shows the chat surface is the missing lever).** Prepped listing drafts preserved (regenerate from ROADMAP research notes if scratchpad dies). **07-16 WORKAROUND RESEARCH (founder has NO Team org): Team/Enterprise gate re-verified HARD for connectors (no Console path — that exists only for plugins). Ranked paths: (1) RECOMMENDED solo 5-seat Team org $125/mo monthly cancel-anytime (billing floor, 1 real user OK; submit same day; ⚠️ listing-persistence-after-cancel UNDOCUMENTED — treat as standing cost or ask mcp-review@ before downgrading); (2) free parallel: community plugin (built, but Cowork+Claude Code only — never reaches chat); (3) free parallel: MCPB Desktop-extension Google Form (likely no org gate — needs human browser check; Desktop-only even if listed; requires packaging the npm bridge as .mcpb); (4) partner org (e.g. Wassner-side) submits with WRITTEN authorization — plausible under Directory Terms ("necessary rights" attestation, no ownership/domain-verify requirement), but listings are org-scoped with NO transfer mechanism; (5) mcp-review@ waiver ask — zero-cost shot, no precedent, don't block on it. Founder's agency-business idea: NOT barred by terms, but two unresolved gaps (no listing transfer between orgs + Anthropic's unilateral removal right) — diligence email to mcp-review@ before pursuing.** (b) ChatGPT Apps/connectors dive DONE 07-16 — **verdict GATED (founder judgment call)**: submission portal platform.openai.com/plugins; identity/business verification MANDATORY (reusable prerequisite, do regardless); demo-account creds required (use a sandboxed no-live-send tenant); tool annotations required (we now have them); OAuth NOT mandatory (Bearer/API-key fine); NO categorical ban on outreach tools in the app guidelines (fetched directly) BUT general usage policies list undefined "spam" as prohibited = reviewer-discretion risk; precedent unconfirmed both ways; verbatim usage-policy text UNFETCHABLE by bots (403 ×3 incl. archive) — founder should read openai.com/policies/usage-policies in-browser before deciding; if GO: frame listing as "outreach infrastructure with compliance tooling" (opt-out/CAN-SPAM foregrounded), agent drafts copy + 5 pos/3 neg test cases; (c) **Codex directory claim REFUTED** (the July-9 "plugin directory for Codex" search-summary line was a synthesis artifact; direct doc fetch: ChatGPT-only directory, Codex stays config-only via ~/.codex/config.toml) — Codex placement = web-findable config snippet, already in-tree; (d) "default" status = earned curation post-listing (usage → featured/verified tiers), set expectations accordingly.
- [ ] 2026-07-16 [ORDER] **Productize the BYO OAuth grant (founder 07-16, from "does every customer have to do it?"):** dashboard "Connect Google / Connect Microsoft" buttons (standard consent-screen flow) replacing the founder-grade token runbook for customers; MS 365 admin-consent covers a whole org at once (agency selling point); managed-mailbox lane needs zero customer setup but still faces the Mailforge-SMTP wall (own resolution needed before arming). FOLD INTO the §20 BYO intake build. **⚠️ FOUNDER CORRECTION 07-16: Mordy brings a BARE DOMAIN (Cloudflare/Namecheap) — NO Google/MS mailboxes.** The 07-15 SMTP-wall line's "most BYO customers (and Mordy) are on Google/MS Workspace" assumption is WRONG for Mordy. Domain-only customers = managed mailboxes provisioned on their domain (→ Mailforge SMTP-reachability check is on HIS critical path, elevate it: check alt submission ports e.g. 2525 / vendor relay / vendor API from the droplet) OR stand up Google Workspace on the domain (then the 443 OAuth path applies). Design §20 intake for all three shapes: nothing / domain-only / existing-Google-MS. **FOUNDER RULING 07-16: build for shape (a) — managed mailboxes provisioned on the customer's domain — as the primary path.** **⚡ 07-16 PROBE: droplet egress on port 2525 AND 8025 is OPEN** (nc succeeded to smtp.mailgun.org:2525, mail.smtp2go.com:2525+8025; 465/587 still blocked) → the managed-mailbox wall likely has a door without moving hosts: (i) confirm the chosen mailbox vendor (Mailforge/Inboxkit) supports submission on 2525 — add to the Gate-1 vendor support asks; (ii) engine config already takes arbitrary SMTP host:port per mailbox, so no code change expected; (iii) relay fallback (SMTP2GO/Mailgun on 2525) exists but changes deliverability posture — vendor-native 2525 strongly preferred. **07-16 VENDOR RESEARCH (no accounts created):** Mailforge DOC-STATES 2525 as a supported fallback on its custom-MTA mailboxes (mailforge.ai/blog/smtp-ports-explained-use — content page, tier-uniformity + real hostnames unconfirmed until provisioning; hostnames only via post-provision CSV/API) → shape-A route 1 = Mailforge over 2525, verify with 1 provisioned mailbox at activation (optional pre-sales confirm email, founder-gated). InboxKit provisions REAL Google Workspace/M365 accounts (their doc: "genuine account with full admin access") → standard Google/MS SMTP = 2525 almost certainly NOT available, BUT that makes InboxKit mailboxes sendable via our NEW 443 API transports instead — **the ms_graph app-only mode (client-credentials + admin consent + Application Access Policy) is purpose-built for exactly this fleet shape and is ALREADY BUILT**; Google Workspace side would want a service-account/DWD variant (small adapter addition, JWT grant — future work if InboxKit-Google chosen). Net: shape-A has two viable routes from the CURRENT droplet; vendor choice at activation now carries a send-path criterion (Mailforge→2525 vs InboxKit→443-API). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** the 07-16 framing above still lists Mailforge-over-2525 as "route 1" — that is now FLIPPED. InboxKit real Google/M365 via the 443-API transports (`ms_graph` app-only BUILT; Google DWD service-account adapter queued if Google mailboxes are chosen at fleet scale) is shape-A PRIMARY as of 2026-07-20; Mailforge-2525 is the fallback route. Full ruling + evidence: `## Open` 2026-07-20 "FOUNDER RULING" entry above. **[2026-08-23 ELEVATED]** the 2026-08-23 dogfood runbook found the platform has NEVER completed a credential push or real send (pendingCredentialPushes: 4, all Mordy's); the manual per-mailbox OAuth grant is the single real blocker between provisioning and sending — this item is the product fix.
- [ ] 2026-07-16 [ORDER] **Engine-443 adversary residuals (non-blocking, SHIP verdict `docs/adversarial/engine-443-transports-2026-07-16.md`):** (a) add `AbortSignal.timeout` (<180s) to api-send fetches — a hung fetch currently rides undici's ~300s default, contradicting the in-code "well under reclaim TTL" comment (no double-send opened, claim holds); (b) revoked refresh token grades transient → 5 wasted attempts/message before 'failed' — consider a permanent-grant-failure grade post-pilot; (c) test rigor: base64 vs base64url encode swap is NOT caught by the decode-based tests (both decoders lenient; current encodes correct) — lock down if touched; (d) Graph app-only user==key no fail-fast; mint script client_secret via argv; HTTP-date Retry-After ignored. (e) **ACTIVATION Gate add:** one real send per transport (gmail_api, ms_graph, smtp) with inbox + intact-header confirmation — API-wire MIME acceptance is unverifiable without live OAuth creds. (buyer run #3 agency, `tools/buyer-panel/runs/2026-07-15-claude-agency.md`): the shopper killed Instantly for "each client workspace requires its own full separate paid subscription… multiplies the plan cost N times over instead of scaling with a flat per-workspace add-on" — OUR per-tenant model repeats that exact pattern for agencies (8 clients × $99-min ≈ $800-950/mo vs winner Smartlead's ~$550-630 all-in at 50 mbx w/ $29/workspace add-ons). Post-activation kill-in-waiting at agency scale. Candidate fix for founder ruling: agency bundle — ONE platform fee + flat per-client-workspace add-on + per-mailbox meter (design alongside the quantity-billing/annual migration; our true multi-tenant isolation is the matching strength — the same run killed Saleshandy for FAKE isolation). **[2026-08-23]** still zero real sends in production; pair with the OAuth-grant item above.
- [ ] 2026-07-15 [ORDER] **Send-framing pricing copy** (Yaakov, two rulings 07-15; AMENDED same day: "3,300/mo doesn't sound nearly as good as 30,000, whether in actuality it's better or worse"): do NOT headline the raw monthly capacity number — it loses the shelf war to fake quotas on sight. Headline axis = **NO SEND QUOTA** (true + verifiable: sends are not the billing meter, no plan-imposed send limit — beats "30,000 emails" on its own axis, since no-cap > any cap; phrase as "no send quota / not metered per send", NEVER the slop word "unlimited" bare). The ~3,300-at-5-mbx physics number DEMOTES to second position ("what N mailboxes can actually deliver — same physics on every platform") and to comparison rows where it's paired with the kill: "Smartlead $94: 30,000-email quota, 0 mailboxes included → included sending capacity: 0." Keep the ratified non-contractual planning-guidance qualifier. Execute with the comparison-pages/site copy batch, claim-class adversary before deploy.
- [ ] 2026-07-15 [ORDER] Watchtower adversary non-blocking follow-ups (`docs/adversarial/watchtower-ops-email-review-2026-07-15.md`): (a) NB-4 — strip CR/LF (charset-filter) `brand` at the signup Zod boundary; it currently reaches the dunning email subject raw (likely inert via the structured send builder, unproven); (b) alert-quality — `failure_signals` pages the founder on ANY single terminal-'failed' send incl. routine CAN-SPAM refusals (threshold `total===0`) → tune to a windowed threshold before arming; (c) test-isolation ticket — waitlist KV rate limiter accumulates across the full suite run (pre-existing; adversary saw 4 flake-fails on the OLD tests; new CORS tests already dodge via unique TEST-NET IPs); (d) NB-2 recorded in ACTIVATION Gate-4 block.
- [ ] 2026-07-15 [ORDER] **Business-readiness gaps** (critique-skill verdict, drafter+skeptic, 2026-07-15; survivors only): (a) **MONITORING/ALERTING — the one genuinely unplanned blocker**: nothing pages anyone, `/status` probes only D1 (`routes/status.ts:10` SELECT 1), ops cron dark, no engine-droplet watchdog, and ACTIVATION.md contains ZERO monitoring items — add a synthetic-check + founder-notify lane (CF cron → /status + engine /health → email/Slack) AND add it to ACTIVATION Gate 2 as an arming prerequisite; (b) **ops outbound-email channel** — absent entirely; blocks BOTH dunning notices (`admin-ops.ts:29` suspends silently) and support replies; one shared channel unblocks both (activation-gated but build-before-arm); (c) support triage is REGEX not AI (`support-kb.ts:19-58`, no LLM call repo-wide) + no inbound wiring — fine (site makes NO ai-support claim, verified) but never market it as AI until real; LLM upgrade = activation-era work; (d) tenant-facing data-erasure endpoint (GDPR/CCPA) — teardown deletes exist (`demo.ts:70`, `lifecycle.ts:92`) but no customer-invocable erasure path; (e) fix STALE `ACTIVATION.md:67` (claims CAN-SPAM address is a placeholder — it is FILLED on terms/privacy/dpa, verified; skeptic killed the draft's "violation" claim); (f) ~~attorney review~~ RULED NOT A LAUNCH GATE by Yaakov 2026-07-15 (draft banners removed, NJ governing law set, `45a8d13`); revisit on first real recipient dispute. DISSOLVED by the skeptic: complaint-breaker "unreachable" (runs every tick — `tick.ts:164` runDeliverabilitySweep); "no row-level DELETE"; the CAN-SPAM-placeholder violation claim.
- [ ] 2026-07-15 [ORDER] **Buyer-CHOICE panel harness** (Yaakov: repeatable, multi-vendor "watch the AI shop, then win the choice" loop — extends the weekly citation panel, which measures findability only): `tools/buyer-panel/` — standardized buyer brief (canonical scales: 10-15 mbx / few hundred sends/day, per forensics §2) run with FRESH contexts on (a) Claude side: dispatched sonnet research agent, fully automatable; (b) ChatGPT side: Yaakov's account (manual paste protocol) or OpenAI API w/ web-search tool (needs his key + spend OK). Per run: transcript forensics (queries → criteria → kill-list → deciding sentence → did ColdRig surface/shortlist/win) → frozen run record + CHOICE-TREND row → diff vs prior run → fix-list into `## Open`. Cadence: post-deploy + biweekly. Method template = docs/research/agent-buyer-research-forensics-2026-07-14.md §6. **CYCLE 1 COMPLETE 2026-07-15** (Claude side, 3 runs — canonical/starter/agency, `CHOICE-TREND.md` 3 rows + `runs/` 3 frozen records confirmed on disk): canonical NOT SURFACED (winner Salesforge/Forge ~$102-120/mo); starter SURFACED+SHORTLISTED organically via Glama, killed "no real sending/no published pricing" (winner Smartlead ~$62-65/mo); agency SURFACED+SHORTLISTED ("closest match to your entire brief"), same kill (winner Smartlead ~$550-630/mo). 5 fix-list items filed to `## Open` (directory-shopfront refresh [ORDER], agency pricing-shape [IDEA], review-site presence [IDEA], reply auto-classification [IDEA], canonical price band [IDEA]). Harness runbook hardened (cwd/identity-leak preconditions). ChatGPT side + cycle 2 (post directory-shopfront-refresh, biweekly) still open. **[2026-08-23 re-purpose]** the `tools/buyer-panel` harness is the base for the weekly versioned no-brand prompt panel in the agent-recommendation program (em-aeo method) — do not retire.
- [ ] 2026-07-13 [ASK] B2 — (ACTIVATION-HARDENING backlog — synchronous provisioning WORKS in test mode) Provisioning sagas: resumable alarm-driven jobs on clock abstraction; clock-scaled warmup ramp; idempotency keys; DO-reload/alarm-retry soak tests.
- [ ] 2026-07-13 [ASK] D6 (remaining half) — Owner business-health substrate: per-tenant margin, backups/DR for the ledger + encrypted creds, master-key storage/rotation/recovery (separate concern, deferred). (MRR/active-tenants/status-page/`GET /admin/ops/digest` portion is already built.)

- [ ] 2026-07-14 [ORDER] Email engine — **COMMITTED DARK `eb8ee42` 2026-07-14 after re-attack #3 SHIP** (was NO-SHIP through re-attacks #1-#2; both defect classes killed — see the two [x] entries below). [dark-unarmed] → **ENGINE LEG ARMED 2026-07-27**: founder ran `cloudflared tunnel login` on the droplet (142.93.12.85); tunnel `coldrig-engine` (`a03c90a4-632d-437d-9c26-57e15c0cf77c`) created, `/etc/cloudflared/config.yml` ingress `engine.coldrig.dev`→`http://localhost:8080` (catch-all 404), DNS CNAME routed, `cloudflared` installed as a systemd service (active, survives reboot) — droplet-side specifics **UNVERIFIED by bookkeeper** (no SSH from this session; reported by the executing session). Bookkeeper-independently-verified this pass: `https://engine.coldrig.dev/health` → 200 `{"status":"ok"}`, unauth `POST /v1/send` → 401, unknown path → 404. Founder then ran `echo "https://engine.coldrig.dev" | npx wrangler secret put ENGINE_BASE_URL` (new Worker version auto-deployed); `npx wrangler secret list` (bookkeeper-confirmed) now shows ALL FOUR of `ENGINE_BASE_URL`/`ENGINE_AUTH_SECRET`/`INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` present, so `isCredentialPushConfigured()` (`apps/platform/src/engine/mailbox-credential-push.ts:47`) is TRUE in prod. Per-tenant reachability is now governed solely by the self-serve activation predicate (`apps/platform/src/engine/activation.ts` — code-confirmed this pass, line 2 documents it as the replacement); the `ENGINE_TENANTS` allowlist is RETIRED. Residuals carried forward: (i) DO Cloud Firewall public `:8080` rule still OPEN — founder has the doctl one-liner, drop pending his run (engine stays bearer-gated meanwhile; dropping it removes the cleartext-bootstrap path entirely); (ii) `ENGINE_AUTH_SECRET` pairing (Worker vs droplet container) UNVERIFIED end-to-end since the 07-19 pairing — a mismatch fails LOUD as 'pending' credential pushes, nothing lost; prove at Mordy's first mailbox push or a pre-Mordy staged grant; (iii) `GMAIL_OAUTH_GRANTS` still absent by design (per-mailbox, actionable at provisioning); (iv) reported-not-independently-checked: `cloudflared` 2026.7.3 installed on the droplet, Docker-API ports 2375/2376 verified not listening (UFW allow rules are stale image defaults, removal pending founder). arming = ACTIVATION Gate-2 (founder droplet + the carried residuals). STILL OPEN from the original entry: (1) ~~per-port activation factory change~~ **DONE — was stale-open here: built+committed `f74687d` (adversary SHIP, 21 tests, factory.ts:84-126); revert-fail-restore physically proven 2026-07-20 (5 gate tests fail on old logic, 534/534 + typecheck 0 at HEAD, tree byte-identical)**; (2) ~~ARCHITECTURE #6 wording~~ DONE 2026-07-14 (Node ratified, updated with the commit); (3) ~~Cloudflare Tunnel before real tenant traffic~~ **DONE 2026-07-27** — see the ENGINE LEG ARMED note above; (4) GreenMail e2e once at host stand-up (now noted in Gate 2, still unrun — no real mailbox yet). Original entry (for the record) — `apps/engine/`: Node SMTP/IMAP daemon (nodemailer/imapflow/mailparser, A5-validated stack), HTTP boundary (`/health` + bearer-authed `/v1/send`,`/v1/poll`), durable JSON store, Dockerfile; Worker client `RealEmailPort` rewritten as an HTTP client, flag-dark. Builder-reported: 27 engine unit tests + 9 Worker contract tests green, full platform suite 252/252 (**UNVERIFIED by bookkeeper** — code is uncommitted, not independently re-run here). **Adversarial review found a BLOCKING defect: poll-cursor loss — class "state persisted before cross-boundary effect confirmed."** Fix IN FLIGHT with the builder (cursor ownership moving to the consumer DO; a stuck-'sending' reclaim guard is also queued). Engine code is **NOT committed** — `git log -- apps/engine/` is empty; do not treat any of this as shipped until it lands as a commit with a clean re-attack. Provisioning runbook already drafted in ACTIVATION.md Gate-2 (itself still uncommitted). **OPEN once it commits:** (1) per-port activation — routing a comped pilot tenant onto the real EmailPort while keeping sandbox billing/domain/mailbox needs a factory change (`realAdaptersActivated` is global all-or-nothing today); (2) engine is Node not Go — diverges from ARCHITECTURE #6 wording; Node-over-Go RATIFIED by the orchestrator 2026-07-14 (A5-validated stack, buildable-today) — ARCHITECTURE.md #6 needs a wording update once the engine actually commits (not done in this session — out of touch-scope for a docs-only pass); (3) ~~cleartext-HTTP bearer on the IP bootstrap → move to Cloudflare Tunnel (HTTPS) before real tenant traffic~~ **DONE 2026-07-27** — Cloudflare Tunnel live at `engine.coldrig.dev` (see ENGINE LEG ARMED note above); (4) GreenMail full send/receive e2e is coded (`ENGINE_E2E=1`) but still unrun (docker-run sandbox-denied / no real mailbox yet) — run it once at host stand-up.

> Done items live in archive/ROADMAP-done.md (self-draining at handoff).
- [ ] 2026-07-14 [ASK] Founder ruling: light-KYC scope for BYO intake — §20.3 currently gates only BYO-PRIMARY; adversary R3/R4 note cheap dedicated non-primary phish domains face only the downstream complaint breaker (some phishing sends before it trips). Extend light-KYC to ALL first-time BYO intake?
- [ ] 2026-07-14 [ASK] Automated data-deletion pipeline (DPA follow-up): "deletion on termination/request" is fulfilled MANUALLY today (privacy@ inbox) — infra reclaim + suppression permanence are real tested code (`lifecycle.ts:235-277`), but no code path purges a terminated tenant's lead/campaign rows. Touches `tenant-do.ts`; not small — backlog, rank at next planning pass.
- [ ] 2026-07-17 [ORDER] BYO residuals (non-blocking, same frozen doc): (a) **engine-side standing rule** — `mailboxes.transport_json` (verbatim SMTP pass/OAuth refresh token) is write-only in the platform; the engine read-back lane, when built, must NEVER echo creds into any tenant-readable surface (tool output, dashboard, error, log); (b) abuse-gate spec-acknowledged gaps (<5-char brands exact-match only; letter-for-letter confusables outside the homoglyph map; light-KYC covers BYO-primary only) — revisit with the 2026-07-14 light-KYC founder [ASK]; (c) AGENTS.md public tool table stale (says 17; 19 live, 21 after BYO merge) — sync in the commit-batch fold; (d) root-level vitest drags apps/dashboard React tests without jsdom env (`document is not defined`) — pre-existing harness mismatch, scope root runs or add env; (e) **TOCTOU** — concurrent `is_primary` registrations could both pass the ≤1-active-BYO-primary guard under real (network-I/O) adapters; benign (more guardrails not fewer, no crash/spend/security bypass) and sandbox-untriggerable today (adapters synchronous in-memory) — fix = atomic re-check or `blockConcurrencyWhile` at arm time; (f) **`pending_kyc` soft-lockout** — a primary stuck in `pending_kyc` (abuse-flagged) permanently blocks the tenant from registering any new primary; no admin KYC-clear route exists in this build — fold into the KYC-clear build when it lands. ((e)+(f) new 2026-07-17, from the re-attack addendum, non-gating.)
- [ ] 2026-07-20 [gated:founder] **Watchtower external prober (ACTIVATION Gate 4 step 7) — the one open piece of the ops-email/monitoring block.** Ops email + monitoring itself is ARMED + VERIFIED 2026-07-20 (Email Sending + Routing live-verified, cron confirmed firing, end-to-end alert + support-ticket path verified — full evidence in ACTIVATION.md Gate 4). Founder's ruling: add `https://agent-cold-email-api.yaakovscher.workers.dev/status` (5-min interval, alert → `yaakovscher@gmail.com`) to his EXISTING regular monitoring service, not a new UptimeRobot/BetterStack account. Not yet added.
- [ ] 2026-08-19 [IDEA] **Fill remedy can re-buy a deliberately-released address inside its fill prefix** (members: `domain_dns_incomplete`, `ordinal_incomplete` — r3 BLOCKING-1 path B first surfaced this for `ordinal_slot_shortfall`, closed there by r4's B1 `released_at` discriminator; r5 gate proved the other two setup-family reasons still lack that discriminator — `fillDistribution` packs by slot index up to the billed quantity, so a released slot inside that prefix gets re-bought). Ruled non-blocking twice (r3: restoring the exactness-withhold gate would wrongly withhold the DNS remedy from ordinary fill shapes — the worse trade; r5: same ruling, reconfirmed by execution) — the priced `effect` now states the cost when it bites (r5 N4, comment corrected in `0cc9feb`; the corrected comment in `next-steps.ts:1221-1227` now explicitly cross-references this ROADMAP item, so it must stay findable here). Candidate fix: extend the r4 B1 `released_at`-based discriminator to these two reasons, or accept as documented behavior.
- [ ] 2026-08-19 [IDEA] **Platform-wide domain-liveness mismatch — CLASS SWEEP candidate.** `readProvisioningSnapshot` treats any `domains.status != 'released'` as live (`provisioning-plan.ts:132`), but the deliverability subsystem's terminal `'retired'` status (set on an autonomous burn, `deliverability-actions.ts:218`) is a DISTINCT status — so a burned domain still reads as live everywhere the planner is consulted, and a `setup_infrastructure` retry will happily provision new mailboxes onto a retired domain (r3 BLOCKING-1 path A root cause; the specific `ordinal_slot_shortfall` symptom was closed in r4 by keying off `released_at` at the MAILBOX level, but the DOMAIN-level status mismatch itself is untouched and other planner consumers inherit it — r3's own words: "worth a class sweep of 'what counts as a live domain,' independent of this branch"). Frozen: `docs/adversarial/customer-continuity-build-gate-r3-2026-08-19.md`.
- [ ] 2026-08-19 [IDEA] **Burned ordinal (intent `committed`, domain `retired`) is covered by NO next-step reason**, and the customer is affirmatively told "nothing is required." `ordinal_incomplete` requires status `intent`/`dangling`; post-B1-fix `ordinal_slot_shortfall` no longer fires on it either (r3 NEW finding; r4 NEW-2 proved it live — PROBE 6: after a platform-initiated burn destroys 3 of 5 paid mailboxes, `infrastructure_status` reads `ready_to_launch:available, seat_headroom_free:available` and tells the customer "Filling them costs nothing… Nothing is blocked and nothing is required"). Ruled non-blocking (the pre-fix behavior was a FALSE `owed` that would have re-bought onto the burned domain — restoring it is strictly worse; `seat_headroom_free`'s `owedCount`-keyed E1 guard means closing the coverage hole closes both the false-silence and the false-owed halves at once). Fix shape: a new reason/branch for `committed`+`retired` that recommends `REPLACE_DOMAIN` rather than silence. Frozen: `docs/adversarial/customer-continuity-build-gate-r4-2026-08-19.md`.
- [ ] 2026-08-19 [IDEA] **Legacy NULL-`inboxes_each` intents: the shortfall/setup-family abstention covers the WHOLE pre-column population, not just the live tenant.** The abstention at `next-steps.ts:820` (disclosed r3 residual (b)) is doing more work than credited — it is the ONLY thing keeping the `ordinal_slot_shortfall`/BLOCKING-1-family false-positives off every tenant provisioned before the `domain_intents.persona_slug`/`inboxes_each` columns landed (2026-08-14, `43ad313`), and it means a raised/lowered ask stays invisible for those tenants until their next fresh `setup_infrastructure` call writes a real spec. Closure is tied to the existing `PROVISIONING_RECONCILE_ENABLED` arm-gate work item (`## Open` 2026-08-13 [ASK], R5/R6) — gate-ratified ROADMAP-grade commentary, not a new order on its own.
- [ ] 2026-08-19 [IDEA] **next-steps: route the three setup-family pricing sites through the cap conjunct** (r5 N1) — `paid_seats_unprovisioned`/`ordinal_incomplete`/`ordinal_slot_shortfall`/`domain_dns_incomplete`'s pricing bypasses the cap check `planRecommendation` enforces (`MAX_SELF_SERVE_MAILBOXES`), so a priced call the request boundary REFUSES with a 400 can state a large bill projection as definite (proved by execution: a $709.00/mo projection stated as certain on a call the boundary rejects; no spend occurs — the call never executes — and reachability needs ≥~31 live mailboxes with a persona change or slot-index gaps AND a DNS-pending domain; the live paying tenant holds 5). Fix shape: add the cap conjunct at the three sites, fall back to the withheld/unpriced posture; the guard should then assert `provisionedAfter <= MAX_SELF_SERVE_MAILBOXES` for every audited step. Frozen: `docs/adversarial/customer-continuity-build-gate-r5-2026-08-19.md`.
- [ ] 2026-08-19 [IDEA] **next-steps: three-way `billMoveSentence`'s comparison so a bill DECREASE under billing drift is not reported as "the same monthly total"** (r5 N2) — its "same monthly total" branch fires on `≤`, not `===`, so e.g. billed 8/billable 5 with `billed_quantity_drift` owed, a call landing at 6 ($109.00) against a $129.00 current charge gets narrated as unchanged. One-directional (never hides a raise — proven monotonic by the r5 gate) and the true number is co-emitted via `billed_quantity_drift` in the same response, so the error is cosmetic, not a surprise-charge risk; reachable whenever `syncMailboxQuantity` failed to advance `mailbox_qty_synced` (`billing.ts:896-902`: non-active billing state, unarmed/simulated Stripe, or a push throw). Fix shape: split the branch into raises / unchanged / decreases.
- [ ] 2026-08-19 [IDEA] **next-steps class guard: derive the audited-reason set from the reason universe, add a BYO-composition fleet; downgrade the docstring's future-proofing overclaim** (r5 N3) — the guard's docstring claims a future bill-raising reason that forgets its `effect` reddens automatically; r5 proved that false (a hypothetical BYO-gated reason with `effect: null` stayed green, 4/4) because coverage is derivation-driven within a hand-picked 17-fleet list, not over the reason universe. Currently benign — all 5 live reasons ARE reached by the fleets. Fix shape: assert audited-reasons ⊇ (`NEXT_STEP_REASONS` minus a justified skip-list), the way the sibling runtime array already does; or, at minimum, correct the docstring's claim to what it actually delivers.
- [ ] 2026-08-19 [IDEA] **`billClaimSentence` (seat_headroom_free's bill prose) anchors on the fixed 5-seat minimum, not on what the tenant is actually billed — verified by direct code read, `next-steps.ts:787-797`.** The sibling `billMoveSentence` (added for NEW-1) anchors on `mailbox_qty_synced` specifically to avoid this — its own docstring says so at `next-steps.ts:810-814` — but `billClaimSentence` was never updated to match, and it is reachable at any billed quantity (`seatHeadroomStep` fills toward `max(billedQuantity, 5)`, not toward 5). Example: a tenant billed 8 and holding 2 gets its fill distribution targeted at 8; if the fill lands at 7, `billClaimSentence` sees `7 > MINIMUM_BILLABLE_MAILBOXES(5)` and states "This particular call would take you to 7 mailboxes… DOES change your bill," even though 7 is still inside the 8 already paid for. Fix: same anchor swap `billMoveSentence` already got — compare against `billedQuantity`/`mailbox_qty_synced`, not the fixed minimum.
- [ ] 2026-08-19 [IDEA] **Withheld-remedy persona-split state pages the founder indefinitely via operator-blame with no operator action defined** (r5 gate R4 residual). Two ordinals under two different personas is a normal history (`recordDomainIntent` is INSERT-OR-IGNORE per ordinal; the global persona follows the newest `updated_at`), and it makes the topping-up call inexpressible (any single call carries one persona) — so the step correctly abstains (`via:"none", waitingOn:"operator"`), but an operator email alert with no defined remedy action doesn't actually resolve a customer-caused state. Per the gate: "Worth a ROADMAP line, not a fix in this round." Frozen: `docs/adversarial/customer-continuity-build-gate-r4-2026-08-19.md`.
- [ ] 2026-08-19 [IDEA] **r2-gate non-blocking leftovers, not previously ledgered.** `GET /admin/ops/digest` mutates tenant state (an expiry write) despite being a read-shaped endpoint (r2 N-B-2). The liveness stamp only fires AFTER the zod parse (`mcp/handler.ts:146-170`), so an agent hammering a real tool with one bad field for 24h still scores `agentStalled` — and every `readOnlyHint: true` tool now performs a write on the hosted MCP path; the stub-level write-spy test still passes, but the guard no longer covers the transport path where the write actually happens (r2 N-B-4). The nudge no longer requires a CONFIRMED alert — the gate is `!result.healthy`, not a confirmed transition (r2 N-B-5) — inert at the 24h `CONTINUITY_NUDGE_DELAY_MS` default, but would fire on an unconfirmed flap if that delay is ever lowered. Frozen: `docs/adversarial/customer-continuity-build-gate-r2-2026-08-19.md`.
- [ ] 2026-08-19 [IDEA] **Emit-coverage guard, generalized:** for every (kind × severity) an auto-resolution rule can silence, test that SOME reason's predicate covers the emit site's surviving state — not just that the resolution rule's own reason-set is internally consistent. This is the class the r3 BLOCKING-1 and burned-ordinal-no-reason findings both trace to ("no reason in set S is owed" used as "RESOLVED" silences every emit-state S fails to cover — see hard-builder memory `resolution-predicate-inherits-its-reason-sets-coverage`). A reusable tripwire in this shape, built once, would have caught the burned-ordinal-no-reason gap (see the entry above) before a gate had to find it by hand.
- [ ] 2026-08-19 [IDEA] **Post-deploy watch items from the customer-continuity ship, not yet confirmed live:** (a) `expires_at` stamping on the stale `retry_setup` rows via the 5-min ops fan-out (r1-B2 fix's live proof); (b) priced-effect spot check — no admin next-steps surface exists to read it directly (404 today), so this lands passively on the customer's next `infrastructure_status` poll; (c) one real Stripe quantity-change vs the Stripe dashboard (r4-gate/r5-gate UNVERIFIABLE item #2 in both rounds); (d) **the ack-loop live proof** — `customer_progress_agent:ten_91aab24a…` clearing when his agent finally `ack_message`s the owed backlog is the first-of-its-kind live proof of the continuity wave's read/ack loop (the nudge instructing the ack fired exactly-once 2026-08-21 and is verified; the CLEAR is the half still unobserved — and if it never clears, that silence is itself a signal: the nudge was read-without-ack like every prior notice).
- [ ] 2026-08-19 [IDEA] **(moved from `## Now` at the 2026-07-29 re-triage; originally dated 2026-07-29, content unchanged)** **BYO-Cloudflare connect path now UNBLOCKED (Q1 root cause, research doc):** the real `cloudflare-domains/connect` schema is `{auth_type, api_token, domains:[…]}` (array) or `connect_all:true` — the two-step dashboard flow = connect(save-credential) → GET /zones → connect(domains). When we build the BYO-CF product path (not on Mordy's critical path — he uses lookalikes), wire this corrected schema; end-to-end live-connect confirmation on a genuine throwaway CF domain still pending (dmhadvisor is scheduled_for_deletion, unsuitable).
- [ ] 2026-08-19 [ORDER] **Sub-ask split from the 2026-07-28 hardening-wave item above: pre-send intent log arming NEVER confirmed verified in prod.** `apps/engine/src/send-log.ts` (WAL) merged `608c80a`, but the `[dark-unarmed]` residual (crash-after-SMTP-accept-before-record double-send, founder-accepted-for-pilot 2026-07-19) clears only after a real power-loss `fsync` check on the actual DO volume, done at host stand-up — no evidence this has run. Separately, `ACTIVATION.md`'s Gate-2 residual (1) line still literally reads "uncommitted, pending review + adversary re-gate + merge," which is stale text (the chain merged) — needs a wording fix at the next ACTIVATION.md touch (out of this bookkeeping pass's file scope).
- [ ] 2026-08-19 [gated:founder] **Sub-ask split from the 2026-07-28/29 click-queue + stray-CF-credential items above: revoke the CF token (Keychain `cf-dns-dmhadvisor`) in the Cloudflare dashboard, then clear the Keychain entry** — unblocks clearing the `dmhadvisor` zone/credential from InboxKit workspace `c5188ced…` (the stray-credential half of the 2026-07-29 [ASK]; the tenant-isolation half was RULED 2026-07-31, see the original text above).
- [ ] 2026-08-19 [gated:founder] **Sub-ask split from the 2026-07-28 click-queue item above: `listings@` routing rule** still not confirmed set up.
- [ ] 2026-08-19 [gated:founder] **Sub-ask split from the 2026-07-28 click-queue item above: Stripe dashboard `support_email`** still not confirmed set (dashboard-only field, not API-settable per the 2026-07-22 Stripe gotcha in `MEMORY.md`).
- [ ] 2026-08-19 [ORDER] **Sub-ask split from the 2026-07-28 MORDY-PAID item above: fleet-mint OAuth verification for Mordy's mailboxes has NO confirming citation anywhere in the ledger.** His mailboxes are live and warming (see the drained warmup-watch entry), so credential push succeeded via SOME path (fleet-mint or the 10-min manual grant fallback), but which one, and whether fleet-mint's first live test actually passed, is not recorded — worth a explicit confirmation next time credentials are touched, not claimed done here.
- [ ] 2026-08-19 [ASK] **Sub-ask split from the 2026-07-28 unit-economics item above: part (b), the build-our-own-infra study (Google Workspace reseller/API, domain registrar API, warmup, deliverability vs InboxKit resale, real breakeven customer count) — Research-lane candidate; still NOT started.** Part (a), slot-headroom economics, is substantially answered by the 2026-08-17 scale-readiness audit (S2 InboxKit workspace ceiling; founder ruling 2026-08-18: DEFERRED until more than ~1 customer at scale) — see the 2026-08-17 `[ORDER]` TRAINS entry.
- [ ] 2026-08-19 [ORDER] **Sub-ask split from the 2026-07-24 post-GA-follow-ons item above: baseline-reset recovery doc for a >10% legit SDN delisting** (post-arm; see the SDN-relay entry) — no evidence this doc was ever written; still open.
- [ ] 2026-08-19 [ORDER] **Sub-ask split from the 2026-07-24 post-GA-follow-ons item above: agency-bundle billing design** (flat per-workspace add-on) — the 2026-07-19 founder-rulings entry committed to designing this INTO the Stripe quantity-billing migration, but item text as of 2026-07-24 (after that migration shipped) still listed it as deferred; no later evidence it was built. Still open.
- [ ] 2026-08-19 [ORDER] **Sub-ask split from the 2026-07-24 post-GA-follow-ons item above: buyer-panel cycle 3** (after the founder's Glama re-scan — the Glama manual description edit was DONE + founder-confirmed 2026-07-29) — no evidence cycle 3 has run since. Still open. (The 2026-07-24 item's 4th sub-item, event-driven alert class sweep, is NOT duplicated here — it already has its own standalone `[IDEA]` line dated 2026-07-24 elsewhere in `## Open`.)
- [ ] 2026-08-20 [IDEA] **`sendWithGuards`/`EmailPort` returns `{messageId, sentAt}` with no field saying whether the vendor collapsed the call** — every `deduplicated` value on the reply path is inferred from our own table rather than observed, the same substitution the wave-A B1 fix removed on the `remove_mailboxes` release path (gate `docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md`, IN-7/NB1). Fix needs a port-return-shape widening, own design increment.
- [ ] 2026-08-20 [IDEA] **Generic `deduplicated: true` stamp belongs at the single idempotency replay site, `engine/idempotency.ts:126-127`** (`const recorded = JSON.parse(existing.response_json)`) — stamping there on any replayed response that carries the field fixes all three `Collapsed<T>` consumers at once (wave-A r2 gate ruling, N1). A change to the money-path idempotency wrapper — Wave B, not an end-of-wave patch.
- [ ] 2026-08-20 [IDEA] **IN-7 residual, qualifies (does not close) the class-closure claim:** `replyToThread`'s 30-day row eviction (`SENT_MESSAGE_KEY_TTL_MS`, `threads.ts:224`) resets `epoch` to 0 tenant-wide on every reply, re-minting the ORIGINAL vendor key the first send already spent — a genuine >30-day repeat reply on the same thread is silently swallowed and reported `deduplicated: false`. Graded non-blocking (customer-visible harm pre-existing and unchanged in magnitude, needs a >30-day gap + identical body + intervening reply); real fix is the generic stamp above (item directly above this one).
- [ ] 2026-08-20 [NOTE] **`syncMailboxQuantity` push failure does NOT throw — it catches, logs `"stripe mailbox quantity sync failed (non-fatal — the reconcile sweep will retry)"`, and returns `{pushed: false}`** (`billing.ts:922-925`). Corrects a wave-A builder-report claim that failures throw (they do not — wave-A r2 gate finding N2). Disposition still SHIP-ACCEPTABLE on grounds the gate verified directly: the customer-facing `billing` field never claims Stripe accepted anything, `mailbox_qty_synced` is not advanced on failure, `tenant-do.ts:1442` re-pushes on drift, and `next-steps.ts:702` surfaces `billed_quantity_drift` to the customer — but a failed downgrade push does leave the customer billed at the old higher quantity until the reconcile sweep catches it.
- [ ] 2026-08-20 [ASK] **Is `support@epiphanymade.com` (the cancellation contact in `site/terms.html:75`) actually monitored/routed?** Every other coldrig surface publishes `support@coldrig.dev` (claim-drift sweep flagged this UNCERTAIN 2026-08-18; TRAIN 3 shipped 2026-08-20 without resolving it — the wave-A r2 gate re-raised it: the page's text is internally correct, the only open question is the operational fact). Founder confirm, or `terms.html` changes to `support@coldrig.dev`.
- [ ] 2026-08-20 [ASK] **`AGENTS.md:85` ("Poll `infrastructure_status` until ready") contradicts `setup_infrastructure`'s own tool description**, which documents a `capacity_pending` state where "polling will NOT progress until an operator raises it — call `contact_operator` instead of waiting" (`apps/platform/src/mcp/tools.ts:74`). Wording pass on `AGENTS.md`'s demo-path section.
- [ ] 2026-08-21 [gated:founder] **GitHub 2FA on `YS-projectcalc` before 2026-08-28** (GitHub banner since 2026-08-16) — hard external deadline; without it, account actions get restricted (repo pushes, registry publishes). Founder keystrokes only; device-flow buttons reject synthetic clicks.
- [ ] 2026-08-21 [ASK] **The founder was OFFERED (in-session 2026-08-20, unanswered) a way to quiet the daily `sweep_coverage` alert sooner — "accept a higher tick threshold as the calibrated bound for a demo-heavy fleet".** ⚠️ CONSTRAINT before acting on any "yes, do that": the sweep-calibration gate's deploy requirements say the re-alert cadence *"is deliberate and correct; it must not be 'fixed' by raising `COVERAGE_TICKS_ALERT_AFTER`"* (`docs/adversarial/sweep-calibration-gate-2026-08-20.md`, Deploy requirements §). A ruling to quiet it must either re-litigate that gate line explicitly (a founder-authorized calibration change with its own reasoning, e.g. a demo-vs-paying-weighted bound) or wait for the queued sweep-capacity lane, which clears the condition honestly. Never a silent threshold raise.
- [ ] 2026-08-20 [IDEA] **`/checkout` has no `openapi.yaml` path** (verified: zero matches for `checkout` in `site/openapi.yaml`'s path list) — same gap class TRAIN 3 just closed for `/remove-mailboxes` (now present at `site/openapi.yaml:211`), left un-swept for `/checkout`.
- [ ] 2026-08-20 [IDEA] **`contact_operator` dedup replay reportedly returns HTTP 201, not 200 — wire-behavior question, deferred from the sweep's G1 tool-claim-binding guard.** UNVERIFIED this pass: could not locate the source of this specific claim in `docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md` on a full-text search; carried forward from the brief as given. Needs a direct code/test check before acting on it.
- [ ] 2026-08-20 [NOTE] **`syncMailboxQuantity`'s return value is discarded at 5 call sites** (`tenant-do.ts:1446`, `provisioning.ts:708`, `deliverability-actions.ts:382`, `billing.ts:1071`, `byo-mailbox-composition.ts:63` — verified by grep this pass, definition at `billing.ts:888`). Informational; not the B1 dedup-inversion class (wave-A r2 gate N2) — the `{pushed: false}` disposition itself is accepted per the NOTE above, this just documents the call-site count for any future audit.
- [ ] 2026-08-20 [NOTE] **Wave-B deferral set CONFIRMED as ONE alert-state design increment, not six separate items:** IN-9 + IN-10 + IN-11 + IN-12 + IN-17 + U-2 (wave-A r2 gate, finding 5) — per-episode announced-set + materiality keys, `admin/db.ts`'s `subsequentActions` is a member of this same increment (NB3), not a settled item. All six are ops-side under-alerting (founder learns late), never customer-facing, none a regression from Wave A. Design pass dispatched 2026-08-20 alongside the two Wave B build lanes; its BUILD waits for the watchtower-seam lane. **BUILT + SHIPPED 2026-08-20 (Wave B.2, merged `ce6218b` + N8 correction `9d31e26`), with two disclosed carve-outs, neither a full close:** (a) **IN-9 is ruled INERT for `send_starved:`** — an accepted disposition, not a defect (`docs/research/alert-state-design-2026-08-20.md` §3.2/§8: "the alternative is costed and rejected"); (b) **IN-17's own site — the SDN ledger — is NOT closed by this increment**, deferred behind the P0 delivery-outcome plumbing (design §2.4/line 829: "deferred behind the P0 delivery-outcome plumbing"). U-2's polarity fix and NB3 (`admin/db.ts` `subsequentActions`) both verified closed by the B.2 build gate.
- [ ] 2026-08-20 [ORDER] **The three one-shot `*_FAILED` check families have NO clearer — route into the alert-state increment.** `mailbox_release_failed:` / `domain_ordinal_failed:` / `mailbox_slot_failed:` (wave B.1, `engine/isolated-failure-alerts.ts`) have three producers and, grepped, zero clearers: nothing ever reports those names healthy. `retireHealthyCheckRows` deletes `status='healthy'` rows only — correctly, per the frozen design's §7.4 — so those rows are immortal by construction and the unhealthy set grows monotonically with real provisioning/teardown failures. Gate N4 (`docs/adversarial/wave-b1-scale-monitoring-gate-2026-08-20.md`), executed: `10-YEAR-OLD one-shots: retired=1 rowsLeft=3 unhealthyLeft=3`. §7.3's `recoverAfter: 1` does not help — recovery needs a healthy observation. DELIBERATELY NOT fixed in the B.1 fix round: one-shot lifecycle is the alert-state increment's territory and a second opinion about it in the codebase is worse than the growth. The shipped claims were corrected instead (`CHECK_RETENTION_MS` docstring + the `/admin/ops/checks` route comment now say 'the per-entity families', not 'the table'). Bounded meanwhile by real failures rather than tenant count, and visible: the rows are unhealthy, so they sort to the top of `GET /admin/ops/checks`. Also partially reopens audit S10 (the four quadratic `reported` loops), whose NOT-A-DEFECT-NOW disposition rests on S5 having removed the precondition. **B.2 gate CONFIRMED still unsettled 2026-08-20** (`docs/adversarial/alert-state-build-gate-2026-08-20.md`, "The two STOP-AND-REPORT items — honestly out of scope, and not worsened by this diff" — "Immortal one-shot rows"): the frozen design's §7.4 requires only that the GC exclude `status='unhealthy'` and specifies NO clearing path; B.2 shipped exempt budget + `recoverAfter: 1` around the immortality (so a *second* failure at the same address now escalates once, budget-exempt — the correct direction for money-bearing checks) but did not add a clearer. Still an owed design decision, not yet routed to a lane.
- [ ] 2026-08-20 [ORDER] **A single persistently-wedged tenant reaches NO watchtower check — route into the alert-state increment.** Wave B.1 gate round 2, NEW-4. `sweep_coverage`'s deferral arm is calibrated at one full slice (`DEFERRED_LEG_VISITS_ALERT_AFTER = SWEEP_TENANT_SLICE`), so a wholly wedged engine trips it exactly and 43-of-44 tenants lost is silent; and one wedged tenant (`deferred = 1`) matches nothing: `send_starved:` needs zero eligible mailboxes (a slow engine's are healthy), `tenant_do_wedged:` needs `opsSummary` to THROW, `customer_progress_*` is about owed setup steps. Softened by the fact that `withItemBudget` abandons the WAIT not the work (the RPC keeps running, effects idempotent) — a latency/observability gap, not a stuck tenant. The calibration itself is a defensible judgment and was NOT changed; what is owed is a per-tenant staleness signal ("this tenant has not completed a poll+tick in N cycles"), which is the alert-state increment's shape rather than a threshold tweak. The `sweep-signals.ts` module docstring now names the gap instead of claiming the coverage. **CORRECTED 2026-08-20, twice, same day:** (1) the sweep-calibration lane DELETES `DEFERRED_LEG_VISITS_ALERT_AFTER` entirely (`fix/sweep-calibration-2026-08-20`, merged `23fe40d`) — this line's own named mechanism no longer exists, and per that gate's ruling C the owed per-tenant staleness signal is now WIDER than described here (`docs/adversarial/sweep-calibration-gate-2026-08-20.md` ruling C: "the per-tenant staleness signal already on the ROADMAP... is now WIDER than ROADMAP:242 describes"). (2) **B.2 gate N8 CONFIRMED the design does NOT settle it** (`docs/adversarial/alert-state-build-gate-2026-08-20.md`, N8 — "A cross-lane attribution that points at a section the frozen design does not contain" / "Single-wedged-tenant staleness. Not in the design (N8)"): this ROADMAP line's own claim that the signal "is the alert-state increment's shape... where the frozen design already put it" was WRONG — the `sweep-signals.ts` docstring said the same wrong thing until corrected `9d31e26` ("post-gate ordered correction: per-tenant staleness attribution states the truth — the design does NOT settle it"). Still an owed design decision, not yet routed to a lane; B.2 makes the adjacent single-wedged-tenant hazard better (requires 3 rotation-skipped results to matter, not 1) but does not close it.
- [ ] 2026-08-20 [RATIFY:founder] **Alert-state design §9.13's alert-volume ceiling, stated in inbox units, awaits explicit founder sign-off before the increment is ARMED (not before it's built).** `docs/research/alert-state-design-2026-08-20.md` §9.13 + `docs/adversarial/alert-state-design-gate-2026-08-20.md` round-4 "§9.13 in inbox units — CLOSED": ratified baseline **~2/day today** (one per stuck Mordy domain on the existing debounce ladder); **~30/day measured** in a pure per-entity storm simulated over 7 days (15 announcements + 15 recoveries, the 15/5 reserved-split sub-cap binding); **~42/day theoretical ceiling** (20 announcements + up to 20 recoveries + up to 2 `alert_budget_exceeded` notices, the last empirically ~1.1/day debounced over the same 7-day simulation) plus exempt-family traffic, which is unbounded and out of scope for this counter by design (pre-disclosed in §5.5, routed to an open ask in §7.8). **UPDATED 2026-08-20 — the mechanism is now BUILT, GATED SHIP, and DEPLOYED live at the designed constants** (20 total / 15 per-entity / 5 reserved — `MAX_ANNOUNCEMENT_EMAILS_PER_DAY=20`, `apps/platform/src/admin/watchtower-budget.ts:42`; migration `0021_watchtower_alert_state.sql` applied remote; Worker `fca0c4d9-b116-4b98-8b43-2159bda6f117` live both hosts). Ratification now arms the NUMBER as founder-accepted policy, not the build — the increment already runs at these constants for every tenant. The honest inbox arithmetic is now carried directly in the constant's own docstring (`watchtower-budget.ts:37`: "...exempt traffic, ~30/day measured, ~42/day theoretical..."), not just this ledger line. **New disclosure from the B.2 build gate (N2), not in the original ask:** the ≤20/day bound does NOT hold at all while the WatchtowerDO is unreachable — in that state announcements are bounded only at 5 per 5-minute tick, and the 24h total is measured UNBOUNDED (200/24h at 100 fault instances, identical to the pre-B.2 unbounded measurement); the platform now SAYS SO (`alert_budget_exceeded` announces `materiality: "unreadable"` after two consecutive unreadable ticks, `watchtower.ts:1216`) rather than staying silent in that state, per `docs/adversarial/alert-state-build-gate-2026-08-20.md:463`. Building is done (Wave B.2, merged `ce6218b`+`9d31e26`); do not treat the increment as "live policy" for real tenants until this number, including the DO-outage carve-out, is explicitly ratified.
- [ ] 2026-08-20 [ORDER] **Founder-visible money-guard change DEPLOYED 2026-08-20: default monthly spend ceiling moved $150 → $180 at 1 paying tenant.** `apps/platform/src/engine/spend-ceiling.ts`: `spendCeilingCents()` = `env.SPEND_CEILING_CENTS` if set (absolute override, FREEZES the formula), else `PLATFORM_BASE_CENTS (6000¢/$60) + PER_PAYING_TENANT_CENTS (12000¢/$120) × PAYING_TENANT_COUNT` (unset `PAYING_TENANT_COUNT` defaults to 1 — the pilot, matching today's live state, confirmed via `wrangler.toml` having no override). The in-force ceiling (`vendor_spend_ledger.ceiling_cents`) is written by an INSERT-OR-IGNORE seed and a raise-only reconcile — **a raise is durable for the calendar month and nothing currently walks it back**: no lowering SQL statement and no admin route exist (wave-B.1 gate N1, stepped down from BLOCKING to non-blocking — operator-error-only, backstop-only, remedy exists). Manual lowering path (documented in the `readInForceCeilingCents` docstring, not previously written down anywhere): `wrangler d1 execute coldstart-platform-db --remote --command "UPDATE vendor_spend_ledger SET ceiling_cents = <cents> WHERE period_key = '<YYYY-MM>'"` — check `reserved_cents + committed_cents` first, since setting the ceiling below that total blocks all further provisioning for the month without refunding anything already spent. Founder informed in-session 2026-08-20; explicit ack still outstanding.
- [ ] 2026-08-20 [IDEA] **Admin route to lower the in-force monthly spend ceiling (today: `wrangler d1 execute` only, no supported UI/API path).** Wave-B.1 gate r1 N1 (stepped down from BLOCKING — see the money-guard [ORDER] entry above for the full mechanism and the manual command). Worth building once a mistaken raise actually needs walking back rather than waiting for the next incident to motivate it.
- [ ] 2026-08-20 [IDEA] **The engine store's freeze-to-JSON persistence stops being ROADMAP-grade at roughly 50 paying tenants — write the number down now, before it's an incident.** Wave-B.1 gate ruling 4 / finding N8 (`docs/adversarial/wave-b1-scale-monitoring-gate-2026-08-20.md`): at ~40 sends/mailbox/day (assumed, unverified live) and 90-day retention, ~20k sends/day accumulates to **~1.8M retained sends** — the regime the prior scale-readiness audit measured at 358MB and multi-second freeze times. S7's decision to revert rather than fix (not-picked options: incremental serialization, which is durability-core surgery; a real datastore, which needs a >50MB store) is correctly deferred for now — the ruling is that this stays ROADMAP-grade, not fixed in-wave, but the ~50-paying-tenant arming point needs to be explicit rather than left implicit for the next session to rediscover.
- [ ] 2026-08-20 [IDEA] **Send-pipeline `rotationOffset` is now redundant with the slice cursor — delete it in a follow-up increment, not this wave.** Wave-B.1 gate ruling 1: simulated the two windows' composition over 400-4,000 ticks across 4 scenarios; no configuration starves any tenant, and the wedged-engine worst case (538 ticks) is within ~8% of the pre-wave number (499 ticks) — the composition is not a regression. Deleting tested rotation behavior to fix a non-issue was judged the worse trade inside a combined wave, so it shipped as-is. The cleanup itself is safe and cheap; it's housekeeping, not a fix for a live defect.
- [ ] 2026-08-20 [IDEA] **The `Collapsed<T>` idempotency-response disclosure guard verifies its known call sites, not the space of possible ones — a future DTO could silently defeat it.** Wave-B.1 gate ruling 7 (`apps/platform/src/engine/idempotency.ts:84-104`): `withRequestIdempotency` has exactly 5 non-test call sites today; only `reply` and `remove_mailboxes` return a `Collapsed<T>` with a `deduplicated` flag, and no field-name collision exists at this ref (verified — the recorded payload is the raw `T`, not the `Settled` envelope). Residual: nothing enumerates the wrapper's call sites against their return types, so a sixth site whose DTO happens to grow an unrelated boolean `deduplicated` field later would silently mint a false disclosure. A one-assertion guard (site count × return-type check) would close it; not built this wave.
- [ ] 2026-08-20 [IDEA] **`LEG_SUBREQUEST_COSTS` (`apps/platform/src/admin/sweep-budget.ts:231`) is a hand-maintained per-leg cost table — the wave-B.1 dual-oracle guard proves internal consistency, not real-world truth.** The guard (gate round 3, NEW-2, 5 planted-defect probes: A/B/C/D red, residual E green) proves every declared subrequest term is summed into the aggregate and every leg the scheduler actually runs has a declared cost — but it cannot prove any individual constant (e.g. `AUDIT_SWEEP_SUBREQUESTS = 300×3`) matches the Worker's REAL subrequest cost for that leg. The counting-proxy test suite is the empirical backstop for that; both remain UNVERIFIED against real Cloudflare metering (see the UNVERIFIABLE note below).
- [ ] 2026-08-20 [IDEA] **Harmonize `count`/`total`/`returned` field naming across the three paged admin endpoints — each is internally unambiguous today, but the same field name means opposite things across two of them.** `GET /admin/ops/checks` (`routes/admin-ops.ts:140`) and `GET /admin/screening/reviews` (`routes/admin-screening.ts:35`) both use `count` = this page's size, `total` = the full count. `GET /admin/ops/waitlist` (`routes/admin-ops.ts:163`) uses `count` = the full count and `returned` = this page's size — the OPPOSITE convention, deliberately fixed that way in 2026-08-20's own docstring to correct a prior bug where `count` had meant "this page" and silently under-reported past 1000 rows. Orchestrator ruling 2026-08-20: naming wart, not a bug — no response currently misleads a caller reading its own docstring, but a caller who assumes one endpoint's shape from another's would get burned.
- [ ] 2026-08-20 [NOTE] **UNVERIFIABLE constants load-bearing for the sweep-budget math — PARTIALLY CLOSED 2026-08-20 by the sweep-calibration lane, one piece still open.** `ASSUMED_DO_RPC_MS = 25` is GONE — the sweep-calibration lane (merged `23fe40d`) replaced the hardcoded guess with a real production measurement, `MEASURED_DO_RPC_MS = {mean 414, p50 350, p75 450, p90 531, samples: 77}` captured via `wrangler tail` against Worker `133fc911` (`apps/platform/src/admin/sweep-budget.ts:283`), with `ASSUMED_DO_RPC_MS = MEASURED_DO_RPC_MS.p75Ms` now DERIVED from it (`:329`) and a guard that reds if the constant drifts from the record (`test/sweep-budget.test.ts:93-121`). `SWEEP_SUBREQUEST_BUDGET = 1000` (`:70`) unchanged. **Still genuinely UNVERIFIABLE per the calibration gate's own §7** (`docs/adversarial/sweep-calibration-gate-2026-08-20.md`): (1) whether DO RPCs count toward Workers' subrequest limit at all — still asserted, not proven; miniflare does not enforce it (`sweep-budget.ts:63`); (2) the 1-3% `cpuTime`/`wallTime` ratio — same `wrangler tail` capture, load-bearing for the queued sweep-capacity increment's bounded-concurrency question; (3) Workers' simultaneous-connection ceiling for DO RPC — never measured anywhere in this repo, also load-bearing for that same increment. **New provenance risk surfaced by the gate (finding 5, NOT yet fixed):** the guard only checks `samples > 0` and a date FORMAT — a fabricated 1-sample `MEASURED_DO_RPC_MS` record with a plausible `capturedAt` silently restores the old slice-37 behavior; see the fresh 2026-08-20 `[IDEA]` entry below.
- [ ] 2026-08-20 [IDEA] **Sweep-calibration oracle-strength — the measurement guard is only as strong as the record it checks, not the reality it claims (gate finding 5).** `apps/platform/test/sweep-budget.test.ts:93-121`'s provenance test only checks a date FORMAT, `samples > 0`, and `p75 ≥ mean × 0.5` — nothing pins the slice value itself or floors the sample count. A falsified `MEASURED_DO_RPC_MS = {mean 22, p50 20, p75 25, p90 30, samples: 1, capturedAt: "<today>"}` passes 20/20 green and silently restores the dangerous slice-37 behavior. Not a false claim today (the docstring only says the guard is *capable* of failing, which is true) — but the next re-calibration is the obvious attack surface. Cheap fix (2 lines): a `samples ≥ 20` floor and an explicit slice pin in the provenance test.
- [ ] 2026-08-20 [NOTE] **Sweep-calibration bookkeeping residual (gate finding 7c, NOT a code defect, low severity, not fixed this pass):** `apps/platform/src/engine/spend-ceiling.ts:567` and `test/spend-ceiling.test.ts:485` narrate the tick's subrequest budget as "592 and a tail reserve of 408" — historically true (the pre-calibration slice-37 figures) but present-tense misleading now that the real numbers are 218/782 (slice 3, per the sweep-calibration gate's own arithmetic). Low severity because both are past-tense citations of a prior gate finding, not live claims about current behavior — a future prose-only fix, no test or ledger risk.
- [ ] 2026-08-20 [ASK] **§7.8 open ask, routed to whoever next owns `engine/isolated-failure-alerts.ts`: one email per FAILED BATCH, not per FAILED item, for isolated-failure alerts.** Raised in the alert-state design's §9.13 disclosure (`docs/research/alert-state-design-2026-08-20.md` §7.8) alongside the exempt-family-traffic-is-unbounded-by-design disclosure — the three one-shot `*_FAILED` check families (`mailbox_release_failed:`/`domain_ordinal_failed:`/`mailbox_slot_failed:`) are exempt-budget and immortal by construction (see the immortal-one-shot `[ORDER]` entry above), so a batch of N simultaneous provisioning/teardown failures currently mints N separate alert emails rather than one batched notice. Not built this wave; not blocking.
- [ ] 2026-08-23 [ASK] Founder asked "what more can we do so people's agents recommend us" — answered in-session 2026-08-23 (live probe: site fully crawlable + llms.txt/JSON-LD/compare+guide pages clean; registries 4/6 present — PulseMCP absent, Glama blurb stale "~12 tools", awesome-mcp-servers 0/3; web search 0/5 unbranded category queries; GitHub 1 star). Candidate lanes, none dispatched: (1) official Claude Code SKILL + plugin-marketplace entry + Cursor rule + Codex AGENTS snippet — the agent reads our install instructions directly (skills hubs rank for "best cold email tool for Claude Code"); (2) expand `docs/research/backlink-outreach-targets-2026-08-17.md` with the pages ranking today (snov.io 17-best-infra, explorium best-skill-for-Claude, claudeskills.info, claudemarketplaces.com, mcp.directory/skills, saleshandy/reply.io/woodpecker comparisons) — still one send = one founder approval; (3) directory gaps: PulseMCP direct submit, Glama claim+refresh, awesome-mcp-servers PRs x3, mcp-publisher republish (server.json pins npm 0.2.1 vs 0.2.3); (4) trust floor: Mordy testimonial/star ask, Show HN, G2/Capterra listing, organic disclosed Reddit; (5) on-site nits: homepage 0 tables → add comparison table, Organization.sameAs only GitHub → add npm/registry/Smithery/Glama/mcp.so; (6) convert the 51-query blind probe into a versioned 30-prompt no-brand panel run weekly across Claude Code/Cursor/Codex/ChatGPT/Perplexity, mention/citation/clickable reported separately (re-run still ~08-30). Also: 229 open items, 110 (48%) ≥30d stale + 3 already-done-unchecked (`:159` engine ARMED, `:153`/`:168` directory snapshots, `:79` signup-auth) → closure/archive pass owed.
- [ ] 2026-08-23 [ORDER] Founder orders (given 2026-08-23, session f6d82067), all authorized for autonomous execution except (4): (0) ROADMAP cleanup — close done items, drop not-really-necessary ones (archive, never delete); (1) BUILD + PUBLISH the official Claude Code skill + plugin-marketplace entry + Cursor rule + Codex snippet; (2) run the outreach campaign to the 14 targets THROUGH coldrig using one of our open mailbox slots, then expand; "also expand the outreach list"; (3) close the directory gaps (PulseMCP, Glama, awesome-mcp-servers PRs, mcp-publisher republish, cursor.directory/mcpservers.org/LobeHub) — drive the browser if necessary; (4) trust-floor items (Mordy testimonial/star ask, Show HN, Reddit, G2/Capterra) [gated:founder-present] — "when I come back"; (5) fix ALL on-site nits (homepage comparison table, sameAs expansion, server.json npm pin / CLI 0.2.3 publish, stale tool-count blurbs). **Status 2026-08-23 EOD:** (0) DONE; (1) DONE — built + published (see `## Now` top entry); (2) PARTIAL — runbook delivered, execution [gated:founder] (see the new `## Open` dogfood-tenant-#1 entry below); (3) PARTIAL — 4 items filed/monitored (mcpservers.org, PR #433, PR #10106 nudge, mcp.so issue watched), several more prepared-not-sent [gated:founder] (see the new `## Open` directory-submissions entry below) or [gated:founder-present] (Directory for AI/Glama/PulseMCP, see the new `## Open` entry below), some dropped (parked domain, archived repo, signup/payment rule); (4) unchanged [gated:founder-present]; (5) DONE — homepage table, sameAs ×4, stale claims, table CSS at all widths, all live-verified (see `## Now` top entry). Full detail: `## Now` 2026-08-23 top entry.

- [ ] 2026-08-23 [gated:founder] **Gmail OAuth grants for Mordy's 4 mailboxes** — `pendingCredentialPushes:4` live platform-wide (all his; the platform has never completed a credential push or a real send). Manual, ~10 min/mailbox by vendor policy (`ROADMAP.md` 2026-08-10 entry — InboxKit support confirmed programmatic minting is dead by design). Must land before his warmup ramp ripens ~Sep 14-15. Runbook with commands staged: `archive/2026-08-23-roadmap-cleanup/dogfood-tenant-runbook.md` §2 step 4.
- [ ] 2026-08-23 [ASK] **InboxKit auto-topup is ON** — live wallet read this pass: `auto_topup_enabled:true`, `<10 credits → +25` charged to the card on file, no approval prompt. Wallet holds 40 credits; Mordy's 4 mailboxes alone draw ≈26/mo, so the floor is reached within roughly a month even with zero dogfood activity. Founder to decide: keep it (expect an automatic charge) or disable it in favor of manual top-ups. Detail: `archive/2026-08-23-roadmap-cleanup/dogfood-tenant-runbook.md` §3.
- [ ] 2026-08-23 [gated:founder] **Dogfood tenant #1 (EpiphanyMade) execution checklist** — no tenant exists yet (verified read-only: zero rows at `yaakovscher@gmail.com`; one unrelated `@epiphanymade.com` sandbox tenant from 2026-07-15, nothing to reuse). Needs founder-hands: (1) a live 100%-off `duration:forever` Stripe coupon (MORDYPILOT is exhausted/inactive) — else checkout is $99/mo self-charge with a card required; (2) signup + checkout with a card; (3) `setup_infrastructure` over HTTP/MCP (not the CLI, which cannot send `registerDomains`) at `domains:1, inboxesEach:2, registerDomains:true` ≈25.5 InboxKit credits — the 5-mailbox checkout-floor shape draws ≈57.5, over the 40 available; (4) 2 more Gmail OAuth grants (same sitting as the item above); (5) approve the 3 email-able Wave-1 sends (Fastio `help@fast.io`, Crustdata `info@crustdata.com`, Noded `hello@getnoded.ai` — the only 3 of 14 outreach targets with a direct email route). Full runbook incl. risks, costs, and a 9-item STOP list of founder-only actions: `archive/2026-08-23-roadmap-cleanup/dogfood-tenant-runbook.md`.
- [ ] 2026-08-23 [gated:founder] **Prepared directory/skill-hub submissions, one founder word each to send** — the auto-mode classifier blocks subagent-driven multi-site form/PR automation, so these are staged (field values ready) but not sent: Salestools Club (`https://salestools.club/submit`), MCP.Directory `/submit` (+ `/submit-skill` once the skill hub picks it up), Oryndex `/submit-a-tool`, cursor.directory repo-scan (pre-filled once already, publish click not rendered — likely rate-limited, retry), claudeskills.info `/submit/`, mcpservers.org Agent Skills. Exact field values + per-target status: `archive/2026-08-23-roadmap-cleanup/send-ledger-2026-08-23.md`.
- [ ] 2026-08-23 [gated:founder-present] **Directory for AI / Glama claim / PulseMCP email** — three directory actions that need the founder's own account or a browser click: Directory for AI `/submit-tool/` requires "Continue with Google" account creation; Glama listing claim (`https://glama.ai/mcp/servers/YS-projectcalc/agent-cold-email`, GitHub login) to refresh the stale "~12 tools" blurb (profile completion 83%); PulseMCP bot-walls automation and routes to emailing `hello@pulsemcp.com`. Detail: `archive/2026-08-23-roadmap-cleanup/send-ledger-2026-08-23.md`.
- [ ] 2026-08-23 [NOTE] **Star-threshold curated lists stay blocked until the trust-floor item lands.** `tolkonepiu/best-of-mcp-servers` PR #366 was closed 2026-08-18 for "at least 50 GitHub stars" (repo is at 1 star, 0 forks today). Re-file once the `[gated:founder-present]` trust-floor item (Mordy testimonial/star ask, Show HN, disclosed Reddit posts — see the 2026-08-23 founder ORDER entry, item (4)) has moved the star count.
