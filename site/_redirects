# Cloudflare Pages redirects. Docs: https://developers.cloudflare.com/pages/configuration/redirects/
# Keep the well-known MCP path discoverable even if someone types the plain path.
/mcp-server-card  /.well-known/mcp/server-card.json  200
/start  /signup  302
/help  /support  302
# AGENTS.md is published in the repo root, not on the site — redirect the
# convention path an agent guesses instead of leaving it a 404.
/AGENTS.md  https://raw.githubusercontent.com/YS-projectcalc/agent-cold-email/main/AGENTS.md  301
