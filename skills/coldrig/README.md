# skills/coldrig

The Coldrig Claude Code skill (`SKILL.md`) — auto-loaded by the `.claude-plugin/plugin.json` plugin (default `skills/` component location) and by `.claude-plugin/marketplace.json`'s `coldrig` entry. Also installable standalone as a project skill by copying this folder to `.claude/skills/coldrig/`, or via `npx skills add YS-projectcalc/agent-cold-email`.

Depends on nothing outside this file; its content is grounded in `site/llms.txt`, `site/for-agents.html`, `AGENTS.md`, `llms-install.md`, `site/.well-known/mcp/server-card.json`, and `site/pricing.html` at the repo root — update this file if any of those change materially.
