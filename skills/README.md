# skills

Claude Code Agent Skills shipped by this repo, installable standalone (`.claude/skills/<name>/`) or via the `coldrig` plugin (`plugins/coldrig/skills/coldrig` symlinks here).

- [`coldrig/`](./coldrig/) — the Coldrig cold-email infrastructure skill. See its own [README](./coldrig/README.md).
