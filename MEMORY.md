# ColdStart — MEMORY (durable project facts)

- Program start date: 2026-07-09.
- Owner interview answers are in SPEC.md §0 and are FINAL (no further questions until the final report).
- Auth landscape (verified 2026-07-09): GitHub CLI authed as `YS-projectcalc`; wrangler authed as `yaakovscher@gmail.com`; npm NOT authed on this machine (SUPERSEDED 2026-07-14: authed as `yakscher`, 2FA on, `agent-cold-email@0.1.0` published).
- Name candidates verified available 2026-07-09: `coldrig` / `coldpipe` — GitHub + npm + .dev/.sh/.io all free; `coldloop` — GitHub + npm + .dev free.
- Prior-art archive: `~/.claude/priorart-archive/ai-agent-controllable-cold-email-platform-2026-06-25.md`; dashboard/inbox lane: `~/.claude/priorart-archive/agent-controlled-dashboard-unified-inbox-coldrig-2026-07-12.md`.
- Test-suite lesson (2026-07-13): `@cloudflare/vitest-pool-workers` runs ALL test files against ONE shared per-project Miniflare; vitest's default `fileParallelism` at ~40+ files causes intermittent ECONNRESET/timeout flakes that vanish in isolation. Fix is `test.fileParallelism: false` in `apps/platform/vitest.config.ts` — do not remove it, and do not chase such flakes as test-logic bugs first.
- Render-at-action-time lesson (2026-07-13): any templated value shown later (email subjects/bodies) must be substituted AND STORED RENDERED at the action (send) time — the send path originally never substituted `{{vars}}`, so every stored artifact carried literal templates and no read path could repair it (`engine/template.ts` + rendered `sent`-event metadata is the fix; inbox v2 prefers the sent event's rendered subject).
- Workers static-assets lesson (2026-07-12, detail in `apps/platform/public/README.md`): `not_found_handling = "single-page-application"` serves the assets-dir ROOT `index.html` (not per-directory walk-up) — hence the deliberate duplicate `public/index.html` ↔ `public/app/index.html`, synced by the dashboard build's postbuild copy.
- Workflow-model-inheritance lesson (2026-07-13): named/stock Workflow scripts pin NO models by default — their agents silently inherit the main-loop model. The `model-lint` hook only catches `workflow({scriptPath})`-style invocations; a named-workflow first run slips through unlinted. This produced a 2.3M-token Fable-main-loop incident this session (the stock deep-research workflow ran its fan-out agents on Fable before the session hit a usage limit). Salvage path when this happens: read `journal.jsonl` for the partial fan-out results, then run ONE pinned sonnet synthesis worker over the salvage — don't re-run the whole workflow.
- Tool-count staleness is a recurring CLASS (2026-07-13, second occurrence): any change to the MCP/HTTP tool surface must sweep `site/llms.txt`, `site/.well-known/mcp/server-card.json`, `AGENTS.md`, every README, site HTML pages, and the `gh repo` description — grep for the OLD tool count repo-wide before committing a tool-surface change — **including SPELLED-OUT word forms ("Twenty-five"), not just numerals** (train 3, 2026-08-20, found three site surfaces the numeral grep missed; a guard now pins retired cardinals as words in `apps/platform/test/site-tool-count-claims.test.ts`). The 15→17 tool bump this session found stale "~12 tools" copy on `llms.txt`+`server-card.json` even after a documented 12→15 sweep last session; the class recurs because each new discoverability surface (OG cards, gh description, etc.) is itself a new place the count can drift.
- npm-login lesson (2026-07-14): running `npm login` backgrounded (via `!`/non-interactive shell) fails silently-ish — without a TTY, npm falls back to its legacy interactive Username/Password/Email prompt, which never receives input in a backgrounded shell and just hangs or times out. Run `npm login` in a real foreground terminal. Separately, `npm publish` on this account additionally requires 2FA — a human-present step regardless of how login happens.
- Cloudflare Pages detached-HEAD deploy lesson (2026-07-14): `wrangler pages deploy` run from an agent's detached-HEAD worktree treats that ref as a "head" branch, not `main` — the deploy lands as a PREVIEW environment, not production, even though the command reports success. Always pass `--branch main` explicitly when deploying to production from an agent session; a "successful" deploy with the live site unchanged is the symptom.
- Device-flow auth code timing lesson (2026-07-14, MCP Registry publish via `mcp-publisher`'s GitHub device-flow): the displayed device code expires in a short window (well under a minute), and a backgrounded command's stdout can flush too late for the code to still be valid by the time it's read. Run device-flow auth steps in the foreground and surface the code to the user immediately — don't batch or defer reading the output.
- Front-end conversion paths need a REAL browser drive, not a backend curl (2026-07-14): the site's only CTA (waitlist) was silently dead for every visitor since launch — the backend returned `200 {"ok":true}` and looked healthy, but `form.querySelector(".form-status")` returned null because the status `<p>` is a SIBLING of the `<form>` (index.html), so `setStatus` threw a TypeError before `fetch()` ever ran: no request, no message, no cleared field. Nothing in the test suite or an API smoke test can see this class. Any page with a form gets a Playwright drive that asserts the request actually fires (`pw-scripts/waitlist-form-drive.js` is the pattern; it revert-fails on the old code).
- Vendor ruling (2026-07-20): founder ruled "go inboxkit (fix this everywhere)" — InboxKit replaces Mailforge as the decided activation-time mailbox vendor (Mailforge's ToS was misread on 07-12: it prohibits direct resale, not permits it). Full basis: `ROADMAP.md ## Open` 2026-07-20 "FOUNDER RULING" entry; propagated to `ACTIVATION.md` Gate 0/1/2 and `SPEC.md` §12/§13.
- Milestone (2026-07-20, `53effa9`): stale-claim-surface class round 2 fixed+committed+pushed — 6 repo-metadata files (server.json, llms-install.md, plugin.json, SECURITY.md, CONTRIBUTING.md, server-card.json tools[] 19→21), adversary SHIP-after-fixes.
- Milestone (2026-07-20, `51f5015`): InboxKit vendor adapters committed dark (RealMailboxPort, RealInboxKitDomainPort, InboxKitClient, error mapping, 40 new tests), adversary SHIP with a REQUIRED-BEFORE-ARMING findings list (`ROADMAP.md ## Open` 2026-07-20 "Mordy pilot" entry).
- Stripe gotchas (2026-07-22, live-verified): the account's default API version rejects `coupon` on POST /v1/promotion_codes — pin `Stripe-Version: 2024-06-20` (or use the dashboard) when minting promotion codes. Own-account `business_profile` fields are NOT updatable via API ("connected accounts only") — support_email etc. are dashboard clicks. Checkout uses INLINE price_data by design (SPEC §18): the dashboard Products page is expected to be empty until metered billing creates durable Prices.
- Spend-guard pattern (2026-07-22): every vendor-arming env binding MUST be referenced by `isRealSpendArmed` — enforced failing-by-construction by apps/platform/test/spend-armed-env-coverage.test.ts (parses env.ts + billing.ts source). When adding a vendor key, wire the guard or that test goes RED.
- Fresh-worktree gotcha (2026-07-23, brand-sweep lane): a new `git worktree add` checkout lacks the gitignored `apps/platform/.dev.vars` (→ 73 misleading platform test failures, `HMAC key length (0)`) and the built `packages/cli/dist/` (→ CLI MODULE_NOT_FOUND). Fix before judging a battery: `cp apps/platform/.dev.vars.example apps/platform/.dev.vars` + `npm run build -w agent-cold-email`. Environmental, not code failures.
- Milestone (2026-07-23, `485a4fe`/`116a058`): go-live wave shipped LIVE — Worker `3f7acce1-bfb6-4c46-8e88-62ec4ced9fce` deployed (founder in-session deploy authorization), then LIVE Stripe key flip executed and verified two ways: Stripe API read-back showed `livemode:true` on the created checkout session, and its line-item description read "Coldrig Launch" (confirming the brand-sweep fix on the real money surface, not just in tests).
- Milestone (2026-07-23/24, GA wave): OFAC SDN screening (`32d5b6b`) + provisioning-core GA (`8674da7`, wires `inboxKitConfig` into the DO factory — real provisioning now REACHABLE for paid+screened tenants) merged and deployed (Worker `a8a0b117`); battery arc ran 5 times, with run #4's 5 failures being the N-OF-1 fail-closed gate correctly rejecting pre-OFAC fixtures (not a regression) — fixed via benign-list fixture seeding to PASS 763/763 on run #5. `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` armed as Worker secrets (workspace uid discovered live via `GET /workspaces/list`).
- Gov/vendor-download-URL lesson (2026-07-23/24, OFAC): a Cloudflare Workers `fetch()` against treasury.gov's public SDN-list download URL failed with **HTTP 525** — the fail-loud cron alert worked exactly as designed and no data was silently lost — but the underlying cause is that treasury.gov's redirect front-end rejects the Workers TLS/fetch handshake even though ordinary clients follow the redirect fine. Fix: point `OFAC_LIST_URL` at the FINAL direct host (`sanctionslistservice.ofac.treas.gov/api/publicationpreview/exports/sdn.csv`), not the redirecting front-end. General lesson: any Workers `fetch` against a gov/vendor download endpoint should be pinned to the final resolved host, not a redirector — the redirector can reject Workers' TLS profile independent of whether the final host is reachable.
- Milestone (2026-07-24, `3b29fc5`→`71bfa830`): SDN screening went fully LIVE against the real Treasury list via a new droplet-relay ingest architecture (a droplet curls the feed and POSTs it to `/admin/sdn/ingest`, sidestepping Workers-side fetch fragility entirely) — `sdn_entries` verified holding 19,241 real rows on remote D1, `sdn_list_meta.active_version = "sdn-relay-1784888169248"`. Two more real wire-shape gotchas surfaced and were fixed the same hour the ingest first went live (see the two lessons below) — the OFAC feed has now broken Workers-side assumptions three separate times (525 redirect, HEAD/GET mismatch, trailing DOS-EOF byte), which argues for treating ANY new government CSV/data feed as "assume the wire format lies about something" rather than a one-off.
- curl HEAD-vs-GET lesson (2026-07-24, SDN relay): a droplet `curl` against the Treasury SDN feed got a 200 on a HEAD probe but a 302 redirect on the actual GET — `curl` invocations that don't pass `-L`/`--location` (the push script originally didn't) will silently fetch a redirect-page body instead of the real file. Always test the ACTUAL request verb+redirect-following behavior you'll use in production, not a HEAD-only preflight — they can disagree.
- Trailing DOS-EOF-byte lesson (2026-07-24, SDN relay): the real Treasury SDN.CSV ends with a lone `0x1A` (legacy DOS EOF/SUB marker) after the last data row. A fail-loud CSV parser correctly rejected this as malformed on the very first live push. Fix: strip a trailing run of blank/whitespace lines and/or a single trailing `0x1A`, anchored strictly to the end of the file — an `0x1A` or blank line ANYWHERE else (i.e., touching real data) must still be treated as fatal corruption. Lesson: "tolerate the known-benign tail artifact, keep failing loud on everything else" is the right shape for real-world feed quirks — don't loosen the whole parser to work around one specific trailing byte.
- Milestone (2026-07-24, `4e53e2d`→`7f197f7d`): the ARM-FOR-EVERYONE program's build phase is COMPLETE — quantity billing shipped live (durable Stripe Prices `coldrig_platform_monthly_v1` $49 + `coldrig_mailbox_monthly_v1` $10/mailbox, `lookup_key`-addressed, replacing the old inline `price_data`/tiered-plan checkout), migration 0015 applied, deployed `7f197f7d`, 817/817 tests + typecheck clean across all 5 workspaces, zero active worktrees. `POST /checkout` now requires a `mailboxes` count (5-60) and has no `plan` field — a breaking shape change from the old tiered API; `openapi.yaml`/MCP descriptions already updated, but any externally-cached example of the old shape will now 400.
- Typecheck-vs-vitest divergence lesson (2026-07-24, quantity-billing battery #7): a plan-tier collapse (deleting the old `launch`/`growth`/`agency` enum in favor of a single `managed` plan) left 2 stale `"launch"` string literals in a test file. `npm test` (vitest) ran GREEN — nothing in the test's runtime path ever compared against the type, so the stale literals never executed a failing assertion. `npm run typecheck` (`tsc`) caught it immediately, because the literal no longer matched the narrowed union type. Lesson: a full battery is BOTH `npm test` AND `npm run typecheck` — a green vitest run alone does not prove a refactor is complete when the change narrows/removes string-literal types; the two tools catch genuinely different defect classes here.
- doctl firewall gotcha (2026-07-27): `doctl compute firewall update` REPLACES the whole firewall spec, including droplet attachment — omitting `--droplet-ids` DETACHES the firewall from the droplet. Caught and re-attached the same minute this time. Always include `--droplet-ids` on any future firewall-update one-liner.

## 2026-07-28 standing lessons (Mordy go-live day)
- `tenants_index.plan` is a signup-time snapshot — NEVER updated afterward (only `status` is; db.ts). Paid/plan detection must read the tenant DO (/account) or Stripe, never that column.
- `wrangler.toml` `routes` addition implicitly DISABLES the workers.dev host unless `workers_dev = true` is set explicitly — took prod down ~3min on 07-28 (Stripe webhook + customer MCP configs point at workers.dev). Both hosts now serve: api.coldrig.dev (branded) + workers.dev (legacy, keep forever).
- Stripe checkout `success_url`/`cancel_url` (billing.ts:176) must have ROUTED targets — they 404'd from first wiring until 2026-07-28 (first real customer found it). Now 302 → /app/billing.
- Token prefix history: minted `cs_test_` from sandbox era until 2026-07-28; new mints `cr_live_`; legacy tokens grandfathered forever (hash lookup is prefix-blind). Never add prefix validation.
- InboxKit: domain registration $12.50/.com via wallet dollars; `cloudflare-domains/connect` returns "domain is not allowed" (unresolved — see docs/research/cf-registrar-domain-connect-2026-07-28.md); CF-Registrar domains (incl. authorpitchdesk.com) can never NS-delegate.
- Milestone (2026-07-29, `4a7bbf0d`): registrar arming SHIPPED LIVE + ARMED (`REGISTRAR_PROVIDER=inboxkit` secret confirmed present) alongside the `cr_live_` token-rotation lane (merged `b843963`/`c1e24a5`, pushed `a68361c`) — both adversary round-2 SHIP after round-1 catches (token: a false-green concurrency test; registrar: B1 stale-registrant-on-opt-out). Mordy's critical path to his first send is now unblocked; awaiting his agent's `setup_infrastructure` calls.
- Milestone (2026-08-02, `e4250e9`/`86996ef`): warmup-claims wave SHIPPED + DEPLOYED — Worker `dbc4ae45-dc63-4b63-9455-51e8ffafc09f`, site Pages `09b8deb2` --branch main, adversary R2 SHIP after two NO-SHIP rounds (`docs/adversarial/{warmup-claims-fix-review-2026-07-31,warmup-wave-final-review-2026-08-02}.md`), platform 125 files/1157 tests, typecheck clean ×5 workspaces.
- `runTick` has NO production driver (B0 RPC awaiting B2) — anything wired only into `tick()` never actually runs in prod. `warmup-cancel` got its OWN dedicated cron lane 2026-08-02 (`scheduled.ts`, alongside deliverability/dunning/digest/watchtower/webhooks) so it does NOT depend on `tick()`; campaign sends still have no scheduler and remain fully driverless — this is now the critical-path gap before Mordy's first campaign send.
- InboxKit warmup add-on = $3/mbx/MONTH recurring (COST_MAILBOX_CENTS=690 = $3.90 slot + $3.00 warmup). Founder ruling 2026-08-02 (option b): platform auto-activates it on every provisioned mailbox, bundled into price, then auto-cancels it at ramp completion (day 29) — built+deployed via `e4250e9`'s `warmup-cancel.ts` cron sweep, so real COGS stays ~1 month/mbx instead of running indefinitely.

## 2026-08-05 incident + hotfix
- Milestone (2026-08-05, `fad3a3e`→`7ab780f`): Mordy's first `setup_infrastructure` call (08-04) stranded an orphan vendor-registered domain — diagnosed via 7 read-only lanes (diagnosis + 5 class-sweeps + pipeline-adversary), fixed via a 14-member hotfix, adversary R2 SHIP after an R1 NO-SHIP (3 blocking: setDns-not-re-driven, adopt-filtered-by-availability, DO-brick via a raw unique index in the constructor). SHIPPED + DEPLOYED same day: Worker `451a1876-c49a-4fcd-905a-505489509528` (both hosts live), site Pages `60ad814c` --branch main; battery platform 129 files/1189 tests, typecheck clean ×5. Frozen `docs/adversarial/{incident-hotfix-gate,provisioning-pipeline-deep-dive}-2026-08-05.md`.
- Provisioning root cause: `setDns` races InboxKit's ~30s async domain registration — a downstream throw during that race leaves the domain vendor-registered but never persisted locally (orphan). Fix pattern: persist-domain-immediately (before DNS) + adopt-before-buy (check for an already-owned domain via `/domains/list` before registering a new one) + treat `setDns` as a retryable follow-up rather than part of the atomic registration step.
- The sandbox is built to always-succeed — green suites hide live provisioning failures. All 3 adversary rounds this incident (hotfix-gate R1, hotfix-gate R2, pipeline-adversary) ran against REAL/simulated-real entry points rather than the sandbox-only test suite, and each round caught a test-invisible blocker (opaque 500s, a DO-brick path, a silently-discarded opt-in field). Any future provisioning-path review must gate against real entry points, not a green sandbox-backed suite.
- Frozen VirtualClock: `ctx.clock` never advances for paid tenants post-signup (only demo/test paths move it) — a real vendor timestamp (e.g. InboxKit's `startedAt`) compared against a paid tenant's frozen `now` produces a negative warmup day, breaking the day-29 auto-cancel math and any other cross-checked ledger timestamp. Sandbox adapters mask it by construction (both sides read the same virtual clock). Class-wave fix pending — ledgered `ROADMAP.md ## Open` PROVISIONING FIX WAVE 2 (L5), 2026-08-05.
- Full boundary audit 2026-08-05: execution-gated audits against REAL entry points caught a LIVE dunning-sweep FAIL (can wrong-suspend a paying customer) that months of green suites + standard review missed — the method is: fault-inject the real path, assert durable state, never trust a green sandbox.
- Dunning cron is LIVE (`wrangler.toml` `crons=["*/5 * * * *"]`) despite a stale `scheduled.ts` "commented-out" comment.
- provisioning happy-path was only ever tested against an always-succeeds sandbox → the incident + 8 forward landmines + a falsely-passing adversary gate (stubbed setDns) all trace to that; fix-wave tests must exercise REAL async/purchased-domain/failure behavior.
- founder rule 2026-08-05: customer/agent surfaces NEVER reveal the upstream vendor (InboxKit) — abstract "provider" only.
- Milestone (2026-08-09, `17e05d4`): wave 3 (5 lanes — msgchannel Inc2/Inc3, webhook inc2, dash-idem fixes, watchtower hardening, byo-teardown) SHIPPED + DEPLOYED — Worker `0f197c4d-ffe8-4da6-a1f4-94c3f23063d6`, site Pages `20a4a523`, combined adversary gate SHIP after one blocking fix (`414b58e`: bounded the `dispute.created` staleness exemption to the dispute's own resolution — see the new `exemption-inherits-none-of-the-guards-reasoning` class, `.claude/agent-memory/hard-builder/`), 11/11 re-attacks failed, 227 files/1810 tests 0 real failures. New `WatchtowerDO` dead-man: `/status`'s `sweepAgeSeconds` field only becomes meaningful after the first post-deploy cron tick (healthy `<900`), not at deploy time.
- Milestone (2026-08-10, `bc1a927`→`bfce3a9`): P0 purchased-domain DNS-wait deadlock fixed + deployed same day it was diagnosed — InboxKit support confirmed (verbatim) that on a domain it registered itself, the propagation checker (`dns_propagation_status`/`nameserver_match_status`) never runs before a mailbox exists, so wave-1's both-verdicts gate had Mordy's provisioning waiting 6 days on state only the wait's own resolution could produce. Fix: `polledDomainIsReady` (`vendors/real/inboxkit-domain-port.ts:453`) short-circuits `connection_type='purchased'`+`status='active'` past the propagation wait; `connected`/`unknown` unchanged. Adversary gate SHIP 0-blocking (`docs/adversarial/purchased-dns-wait-gate-2026-08-10.md`), deployed Worker `e2e9f061-e7e0-4999-bdb3-258d36582002`. CLASS lesson (new hard-builder memory, `gate-waits-on-state-the-gated-action-produces.md`): a "safe-direction" wait argument silently assumes the awaited signal eventually arrives — when the gated action is itself the only thing that causes the vendor to write that signal, the wait is a permanent deadlock, not a conservative delay; state the arrival assumption out loud for every readiness gate. Same session: InboxKit also confirmed programmatic OAuth refresh-token sending is unsupported by design — Inc-F's live-verify plan is dead, replaced by a 3-way founder decision (`ROADMAP.md ## Open` 2026-08-10 ASK).
- Milestone (2026-08-18, `2cef30d`): product-hardening TRAINS 1+2 (channel-truth + loop-isolation) SHIPPED + DEPLOYED — Worker `3c549e5e` live both hosts, site Pages `1ea77c9e`, live `openapi.yaml` byte-identical to committed. 4-round adversary gate (frozen `docs/adversarial/wave-1-2-integration-gate-2026-08-18.md`): R1 NO-SHIP (B1 merge-semantic, B2 member-5 2-of-3-sites, B3 partial-release-replays-forever) → R2 NO-SHIP (N1 unbounded over-release, measured 12-destroyed-for-ask-3) → R3 SHIP-AFTER-FIXES (R3-1 100-bound-param ceiling at count≥21) → R4 SHIP (21/60 drives verified at the ceiling). Battery 186f/1766p/1skip + typecheck clean, all real (non-piped) exit codes. **Gate's own lesson, worth repeating: in 3 of 4 rounds the previous round's fix bred the next blocker — a fix to a correctness invariant deserves the same attack budget as the original code, and the last fix is always the prime suspect.** Two new mechanical gotchas this gate surfaced: `npx vitest run | tail -N` (or any pipe) silently discards vitest's real exit code — a backgrounded run can report "exit code 0" while its own output shows failures; verify from the printed summary line, never a piped `$?`. And: InboxKit's `/domains/list` + `/mailboxes/list` are **POST with a JSON body, never GET** — a bare GET 404s and mimics a vendor outage.
- Milestone (2026-08-18, `a0fd314`, gate re-emitted `629a20e`): vendor-truth wave (classes A-F — wallet exhaustion, duplicate/orphan detection, scope, billed-effect ordering, adapter live-contract drift) SHIPPED + DEPLOYED — Worker `54b00702` both hosts, site Pages `62115561`. Gate SHIP round 1, 0 blocking, 6 non-blocking, battery 190f/1831p/1skip exit 0 (`docs/adversarial/vendor-truth-gate-2026-08-18.md`; its first untracked copy was destroyed by a worktree reap, re-emitted verbatim same day, unpushed on local `main` as of this ledger pass). The gate's own live-state probe is the authoritative incident timeline: the Mordy InboxKit-wallet-empty incident (tickets sup_8a486bb0/sup_1b6746fc, root cause = empty wallet + fundable refusals graded permanent) resolved UNDER THE OLD CODE when his agent's retry settled `apd-setup-a-2mbx` to `done` at 2026-08-18T18:22:37Z (founder wallet top-up unblocked it) — with zero human-to-human relay on either side, the first fully agent-to-agent customer operation on the platform. A further claimed setup-B (~19:14Z, 2 mailboxes on goauthorpitchdesk.com) postdates the gate's probe and is orchestrator-reported only, not independently re-verified this pass.
- Milestone (2026-08-11, `1130344`→`0a350cd`): msgchannel Inc5 (agent→operator direction — new `contact_operator` MCP tool + REST route, 27→28 tools) SHIPPED + DEPLOYED — Worker `65392316-db03-4587-8191-fdca1a893146`, site Pages `5e9df792`, migration `0017_support_ticket_agent_source.sql`. 3-round adversary gate (frozen `docs/adversarial/msgchannel-inc5-gate-2026-08-11.md`): r1 NO-SHIP → r2 NO-SHIP → r3 SHIP 0-blocking, 1 accepted-risk residual, platform 167f/1561t. **CLASS lesson — DO input-gate atomicity does NOT extend across an external binding.** `rate-limiter-do.ts`'s own doc comment states the DO input gate stays closed across DO-storage awaits, serializing read-modify-write; this repo's storm guard read-then-wrote via `env.DB` (D1) instead, which is an external binding — the input gate opens at every `await`, so 100 concurrent calls all read the pre-write count and all passed, producing 96 tickets/96 ops emails against a 5/hr cap (r1). Lesson: "the DO serializes my logic" is only true if every step of that logic stays inside DO storage; one D1/KV/fetch await anywhere in a read-modify-write reopens the race. **Second CLASS lesson — admitting before the record you authorize creates a phantom on partial failure.** The r1 fix (a DO-local synchronous admission guard) committed the admission before the awaited D1 ticket-insert, so a D1 write failure on the record leg left a committed admission with no ticket; every retry then deduped against that phantom and returned a fake success (4/4 phantom retries, r2). Fix pattern: synchronous compensation (`revokeAdmission`) that undoes the admission + releases any held side-effects when the downstream record write fails, inside the same request, before rethrowing — closes the common case but NOT a mid-flight isolate death between admission-commit and record-write (needs an out-of-band reconcile sweep) NOR a concurrent identical-input twin that deduped against the doomed row in the sub-ms window before revocation (narrow triple-conjunct: concurrency + partial-write-degradation + identical input; self-healing; accepted as ship-worthy risk here). True closure of the concurrent-twin case needs a two-phase confirmed flag on the dedup check, not a reconcile sweep — the sweep only catches orphaned rows, and revocation already leaves none.
- Milestone (2026-08-19, `cf9b323`→`0cc9feb`): customer-continuity wave (I1-I15 + 4 fix rounds) SHIPPED + DEPLOYED — Worker `319e15c4-1de7-4690-be8b-61a68082a226` both hosts, site Pages `40fe9f0c`. 5-round adversary build gate (`docs/adversarial/customer-continuity-build-gate{,-r2,-r3,-r4,-r5}-2026-08-19.md`): r1 SHIP-AFTER-FIXES → r2 SHIP-AFTER-FIXES → r3 NO-SHIP (`ordinal_slot_shortfall` couldn't tell a real failure from a deliberate/autonomous/persona-driven live-set change) → r4 SHIP-AFTER-FIXES (NEW-1: three bill-raising setup calls shipped `effect: null`) → r5 SHIP (NEW-1 closed 3/3). Merged-tree battery GREEN: typecheck 0, platform 211f/2063p/1skip, dashboard 29/143, engine 17+2s/140+4s, cli 12/12. Live-verified on `ten_91aab24a`: persona backfill worked in production (both `domainIntents` recovered from NULL `personaSlug` to `"mordytee"`). Root class this wave closed: a resolution predicate built from "is any reason in set S still owed" silently inherits set S's OWN coverage gaps as its resolved-state (new hard-builder memory `resolution-predicate-inherits-its-reason-sets-coverage`) — surfaced a pre-existing domain-liveness status mismatch (`'retired'` != `'released'`, both read as "not released") and a burned ordinal covered by no next-step reason at all; both ledgered `ROADMAP.md ## Open`, neither fixed this wave. `PROVISIONING_RECONCILE_ENABLED` arm-gate (3 blockers) UNCHANGED and still shut.
- Milestone (2026-08-20, `2c07add`→`69ec655`): Wave A (trains 3+4 — claim-surface truth sweep + dedup-semantics wave) SHIPPED + DEPLOYED — Worker `a74ae0ab-14ae-42cb-8bff-31ef247c2831` both hosts, site Pages `8a8333f0`. 2-round adversary gate (`docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md`): r1 FAIL (B1 — `remove_mailboxes` reported `deduplicated: true` on a call that had just irreversibly released real mailboxes, the wave's own headline field lying on the money path) → fix `8582c3d` → r2 SHIP, 0 blocking, 0 new. Battery green at merged HEAD: typecheck 0, platform 223f/2156p/1skip, dashboard 31f/165p, engine 17f+2s/144p+4s, cli 12/12. **CLASS lesson (new, per the gate's own reasoning):** a "was this response produced fresh, or handed back" disclosure field can be honest at one collapse layer (the intent/idempotency-key layer, which B1 fixed) while a SECOND, lower collapse layer (the request-idempotency replay wrapper, `engine/idempotency.ts:126-127`) remains invisible to it — the fix must be scoped to whichever layer actually produced the response, and a field that only sees one layer needs its blind spot documented, not assumed closed. Same session dispatched Wave B (trains 5+6 split into a watchtower-seam lane + a disjoint-modules lane, plus a parallel alert-state design increment) — see `ROADMAP.md`/`HANDOFF.md` for current state.
- Milestone (2026-08-20, `b260bd0`): Wave B.1 (trains 5+6 core — scale + monitoring) SHIPPED + DEPLOYED — Worker `133fc911-defd-4713-beee-1654ee53d7e2` both hosts, D1 migrations `0019_sweep_cursor`+`0020_sdn_entries_name_index` applied remote, no site deploy needed. 3-round adversary gate (`docs/adversarial/wave-b1-scale-monitoring-gate-2026-08-20.md`): r1 FAIL (B1 — the `sdnRecovery` leg could spend the whole invocation budget ahead of the dead-man heartbeat and silently kill every trailing leg, exactly the class this wave exists to prevent) → r2 checklist-SHIP (4 NEW) → r3 FINAL SHIP (dual-oracle budget guard, bounded reaper, `ABSENCE_MIN_AGE` 45min). Battery green: typecheck 0, platform 233f/2268p/1skip, dashboard 31f/165p, engine 18f+2s(20)/153p+4s(157), cli 12/12, wrangler dry-run zero binding delta. **Founder-visible money-guard change, live:** default monthly spend ceiling moved $150→$180 at 1 paying tenant (formula-driven, `PAYING_TENANT_COUNT`-scaled; `spend-ceiling.ts`). Same day, the ALERT-STATE increment's design FROZE at v4 after a 4-round design gate (`docs/adversarial/alert-state-design-gate-2026-08-20.md`, blocking 6→2→1→0), consolidated into an 18-constraint build brief; Wave B.2 (the build against it) dispatched immediately, gated on a [RATIFY:founder] alert-volume-ceiling number (§9.13, ~2/day today / ~30/day worst-case / ~42/day theoretical) before ARMING.
- Milestone (2026-08-20, `ce6218b`+`9d31e26` / `23fe40d`): **TRAINS PROGRAM BUILD PHASE COMPLETE** — Wave B.2 (alert-state build) and the sweep-calibration fix both SHIPPED + DEPLOYED same day, closing trains 1-6 end to end. Wave B.2: gate `docs/adversarial/alert-state-build-gate-2026-08-20.md` r1 FAIL-SHIP-AFTER-FIX (B1 prose-fed key classifier; F1 fold collision enumerated) → r2 SHIP; content = 26-family materiality-key table, announced-key ledger (cap 5), ladder-first cap precedence, §5.5 daily budget (20 total/15 per-entity/5 reserved, decide/send split so a withheld email never withholds a state transition, fail-open bounded at 5/tick with the 24h total honestly UNBOUNDED during a DO outage — the platform now announces `unreadable` rather than staying silent), migration `0021_watchtower_alert_state.sql`. Sweep-calibration: gate `docs/adversarial/sweep-calibration-gate-2026-08-20.md` PASS (SHIP), 0 blocking, 7 non-blocking; content = measured 450ms p75 DO RPC in production (`wrangler tail` against Worker `133fc911`, n=77) replacing a hardcoded 25ms guess, slice 37→3, `sweep_coverage`'s reported rotation corrected from a 31×-optimistic claim to the honest number, cadence redistribution disclosed (mean 12.0→10.0 ticks, worst 3× better, median slower for 36/63 tenants — a real trade-off, not a pure win). Merged-tree battery GREEN at `9d31e26`: typecheck 0, platform 239f/2373p/1skip, dashboard 31/165, engine 18+2s/153+4s, cli 12/12. Deployed: migration 0021 applied remote (confirmed `wrangler d1 migrations list coldstart-platform-db --remote` → clean); Worker `fca0c4d9-b116-4b98-8b43-2159bda6f117` both hosts, confirmed latest via `wrangler deployments list`; no site deploy (no claim-surface change). **Two design decisions confirmed owed by BOTH the B.1 and B.2 gates, still unrouted to a lane:** the immortal one-shot `*_FAILED` check families have no clearing path (design §7.4 excludes only `status='unhealthy'` from GC, specifies no clearer); a per-tenant staleness signal is NOT in the frozen design at all (B.2 gate N8) — a `sweep-signals.ts` docstring wrongly claimed otherwise until corrected same day (`9d31e26`). **Live baseline this pass (re-verified, NOT the same as the pre-deploy baseline):** `GET /admin/ops/checks?unhealthy=1` shows 2 unhealthy — `sweep_coverage` (now honestly reporting "a full pass every 21 tick(s) (~105 min)" under the new grading, not a regression) + `customer_progress_agent:ten_91aab24a…`. **Queued next:** msgchannel Inc4 (email mirror, authorized 2026-08-05, never built) + a sweep-capacity follow-up lane — per the calibration gate's own finding 6, measure bounded-concurrency fan-out (DO `cpuTime` at 1-3% of `wallTime`, dispatch-bound, never load-tested) BEFORE committing to the larger D1/Analytics read-model rebuild.
