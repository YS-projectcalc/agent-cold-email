# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE, FIRST PAYING CUSTOMER Mordy (2026-07-28).** Site https://coldrig.dev · API https://api.coldrig.dev + https://agent-cold-email-api.yaakovscher.workers.dev · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`. **Standing autonomy (founder, 2026-07-22): merge/push/deploy/arm to customer-ready is authorized; browser-consent clicks excepted.**

> **You are resuming `coldrig` with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (deploy version, C3 build state, whether Mordy's agent retried). If they hold and the step is non-destructive, proceed. **STOP and confirm before any destructive/founder-owned step** — EXCEPT where the standing autonomy grant covers it (merge/push/deploy/arm).

## Where we are right now (2026-08-14)
**A live P0 was found, root-caused, fixed, gated, and DEPLOYED this session — and a follow-on (C3) is building.**

1. **🔥 P0 — SHIPPED + DEPLOYED + ARMED (Worker `d8055e22`).** Mordy's agent used the new `contact_operator` channel for the first time (ticket `sup_3ca260e4`, 2026-08-12) to report that his `setup_infrastructure` retry **MINTED + BOUGHT a second lookalike domain** (`theauthorpitchdesk.com`) instead of resuming the first — idempotency key not gating domain generation — and still failed at `step: "domain DNS setup"`. Root cause CONFIRMED: wave-3 commit `85f48af` changed the domain-intent key derivation with no backfill, orphaning Mordy's committed intent (`apd-setup-a-2mbx#0`), so resume never fired and `findAdoptableDomain` excludes any already-recorded domain — his paid domain was "too owned to adopt, not committed enough to resume." A structural second defect (C3, below) guarantees the retryable error that triggered the mint. Fix = dated one-shot constructor reconciliation (`engine/legacy-domain-intent-keys.ts`) rebinding the orphan to ordinal 1, zero new buys; excludes the deliverability `replace:` writer; + C1 literal-derivation guard + C4 previous-build-state fixture. Adversary gate SHIP 0-blocking, money invariant RED-proven ($15 mint reproduced then vanishes); frozen `docs/adversarial/intentfix-gate-2026-08-13.md`. Merged `main` `8a66976`, deployed, live-verified. **The fix ARMS on Mordy's DO's next touch** (the reconciliation runs in the `TenantDO` constructor) — his next retry rebinds + provisions with zero buys.
2. **C3 + direction-1 design pass DELIVERED, founder-ruled.** C3 (first-buy latency: every first domain buy ends in a customer-facing `retryable:true` because the ~2.3s in-call DNS wait can't outlast the vendor's ~32s registration — what made Mordy's agent feel the loop was broken) → **BUILD C3 NOW** (approved). Direction-1 (resume-from-resource root redesign) → **STAGE AFTER C3** (approved). Priorart `~/.claude/priorart-archive/coldrig-async-provisioning-and-resource-reconcile-2026-08-13.md`; both folded into `ROADMAP.md ## Open` 2026-08-13 [ASK].
3. **Founder-ordered post-ship adversarial re-attack (2026-08-14) → BLOCKING defect found → class-swept → fixed → shipped → deployed → CLOSED, same day.** The re-attack found the one part that was LIVE and ARMED (C3 part (b)) failing the exact criterion its own gate passed it on: a permanently-dead vendor registration read to the customer as "provisioning in progress" forever, with nothing bounding or escalating the pending state (Finding 1 BLOCKING + Finding 3 amplifier); frozen `docs/adversarial/c3-postship-reattack-2026-08-14.md`. Class-swept to 15 members across domain + mailbox lifecycle handling; frozen `docs/adversarial/vendor-verdict-class-sweep-2026-08-14.md`. Fixed and taken through a 3-round fresh-context adversary gate — `a776b29` SHIP-WITH-NOTES → `0bd5ec4` SHIP → `fcf845a` SHIP, frozen `docs/adversarial/vendor-verdict-gate-2026-08-14.md`. Merged `main` `e5598da` (--no-ff, src byte-identical to the gated tree), deployed Worker `71ec2003-c0bd-4dc3-931c-597265031b4e` (100%), live-verified (both hosts `/status` 200, unauth `/account` 401, `PROVISIONING_RECONCILE_ENABLED` still absent from the manifest). Live now: terminal vendor states (incl. `scheduled_for_deletion`) fail non-retryably on first poll instead of spinning; purchased-domain pending is bounded at 6h with a `connected`-domain exemption; pre-deploy stalled rows (Mordy's shape) surface via the new `domain_dns_aging` watchtower check with correct ages; a `200 {error:true}` vendor body can no longer authorize a mailbox re-buy or silently stall nameservers.

## In flight / next
- **C3 SHIPPED + DEPLOYED 2026-08-13 (part (b) ARMED, part (d) DARK) — Worker `52a65306`.** Adversary gate SHIP-dark (frozen `docs/adversarial/c3-gate-2026-08-13.md`), merged `main` `6c5140b` (--no-ff, lane preserved), pushed, live-verified, `PROVISIONING_RECONCILE_ENABLED` confirmed unset. Part (b) is LIVE: a benign terminal purchased-domain propagation-wait now returns success-pending, not the scary `retryable:true` — so Mordy's next retry reads "in progress" (no spend). Full detail + the arm-gate finding: `ROADMAP.md ## Open` 2026-08-13 [ASK].
- **🔴 ARM-GATE (must fix BEFORE arming (d); cannot fire while dark) — THREE open blockers:** (1) spec divergence — the (d) reconcile reads the FIRST persisted spec while the DIRECT resume path honors the LATEST call's persona/count, so an armed (d) OVER-PROVISIONS (7 mailboxes for a customer whose last ask was 2); (2) count cap — the reconcile buy path lacks `assertWithinProvisioningCap`; (3) lifecycle-freeze guard — the reconcile also skips `assertNotLifecycleFrozen`, so frozen tenants get phantom `mailboxes` rows + false `dns_status='ready'` that turn billable on reactivation (no live spend today; sandbox adapters while frozen). **DO NOT arm `PROVISIONING_RECONCILE_ENABLED`** until all three are fixed — options in `ROADMAP.md ## Open` 2026-08-13 [ASK] entry.
- **The Finding-1 fix wave is CLOSED, not in flight** — shipped, gated (3 rounds, SHIP), merged `e5598da`, deployed Worker `71ec2003`, live-verified 2026-08-14. No builders or gates currently running. The 2-hourly Mordy cron is still active session-scoped (see Landmines).
- **✅ Reply to Mordy's agent SENT 2026-08-14 ~21:55Z** — unblocked by founder-ordered `ADMIN_TOKEN` rotation (fresh mint → **Keychain `coldrig/admin-token`** → `wrangler secret put`; the secret-put redeploy makes the current Worker version **`7d41d1ae`**, same code as `71ec2003`; /status 200, admin digest 200). `POST /admin/tenants/ten_91aab24a…/messages` → 201 `emitted:true`, `regarding: sup_3ca260e4`, severity `action_required`; body = one-shot-defect correction + "retry the SAME key `apd-setup-a-2mbx`, spend-safe" + the three honest outcomes. His agent sees it on its next `infrastructure_status`/`list_messages` poll. The watch now looks for his REACTION.
- **✅ `GET /admin/ops/checks` SHIPPED + DEPLOYED 2026-08-14 (Worker `ba8d0fec`, merge `babf287`, gate SHIP-WITH-NOTES)** — the watch's leg 3 now reads the same checks that email the founder; two-store caveat + evidence in `ROADMAP.md` (the checked 2026-08-14 [ORDER] line).
- **Open decisions / blockers (all founder-gated):** (a) the C3 (d) arm-gate fix, now three items (before any arming); (b) direction-1 (STAGED after C3, founder ask); (c) mailbox W7.T5/T6 (separate project — see `~/dev/agent-mailbox/HANDOFF.md`); (d) Gmail MCP authorization (`/mcp` → "claude.ai Gmail") — founder-endorsed second alert channel, awaiting the founder's one click.

## Landmines / gotchas
- **2-hourly coldrig watch is SESSION-SCOPED — it dies when this Claude session ends.** Now job `be956d32` (fires at :41; supersedes `9d4130d0`/`0d6e9bc2`/`300c0b41`; 2026-08-16 rework: ALL legs via HTTPS — leg 1 now uses `GET /admin/support/digest` instead of wrangler D1, eliminating the transient-7403 watch noise) with FOUR legs: (1) support tickets via admin API, (2) InboxKit provisioning, (3) **`GET /admin/ops/checks?unhealthy=1`** (LIVE since 2026-08-14, Worker `ba8d0fec`) via Keychain `coldrig/admin-token` — known-unhealthy baseline = the 2 Mordy `domain_dns_aging` checks; stale `updatedAt` across rows = dead-cron tell, (4) `GET /status` (catches `d1`/`cron_sweep`, which leg 3 structurally cannot see). NEW issues are handled under the standing grant, not just observed. Baselines (`watchtowerChecks` + `opsDigest` blocks) live in the state file. Re-create all four legs in a resuming session. It checks the `support_tickets` D1 table + InboxKit workspace `c5188ced-33db-436f-b970-1860e6c8c66b` for new activity, state in `~/.claude/mordy-watch/last-seen.json`. Re-create it in the resuming session if still watching Mordy. (⚠️ `support_tickets` has NO `urgency` column — schema is `id/from_email/subject/body/tenant_id/category/draft/status/created_at/message_id/source/email_sent_at`.)
- **Mordy's live state as of last cron:** 2 purchased domains both DNS-pending, 0 mailboxes, `last_nameserver_check=null` (the vendor-contract capture the 08-10 item wanted — the vendor's checker genuinely never runs pre-mailbox). $30 booked domain spend, 0 mailboxes. His agent is HOLDING (asked us whether to retry same-key or reconcile first). **2026-08-14 vendor-verdict wave implication:** his 2 stuck domains are exactly the pre-deploy NULL-anchor population the new `domain_dns_aging` check now surfaces (ages derived from `purchased_at`, per `docs/adversarial/vendor-verdict-gate-2026-08-14.md` Finding 2) — his agent's next retry now gets either honest bounded pending, a proper terminal error, or success, never eternal fake-pending. The fake-pending state question (which branch he's actually in) still needs the founder-held `ADMIN_TOKEN` to confirm directly, but is now LOW-stakes since every branch behaves correctly.
- **The P0 fix is ARMED but not yet APPLIED to Mordy's DO** — it applies on his DO's next construct (his next retry, OR the C3 (d) sweep touching his DO). No live proof it applied yet (no read path to live `domain_intents`).
- **Standing autonomy grant covers the C3 deploy** — merge/push/deploy is authorized on a clean gate. Browser-consent clicks are NOT.
- **caffeinate re-armed (pid ~37722, 4h)** to fight idle-sleep, but the sleeps this session were LID-CLOSE (caffeinate can't override) — long agents died mid-response and were resumed. Expect the same.

## Key files
- `apps/platform/src/engine/legacy-domain-intent-keys.ts` — the shipped P0 one-shot reconciliation.
- `apps/platform/src/engine/provision-intents.ts` — the intent-key derivations (setup + `replacementDomainIntentKey`).
- `apps/platform/src/engine/domain-dns.ts` — `SET_DNS_BACKOFF_MS` (the C3 timing defect) + the anti-parking comment.
- `docs/adversarial/intentfix-gate-2026-08-13.md` — frozen P0 gate SHIP verdict.
- `docs/adversarial/vendor-verdict-gate-2026-08-14.md` — 3-round fresh-context adversary gate (SHIP-WITH-NOTES → SHIP → SHIP) + ship record for the vendor-verdict class fix.
- `apps/platform/src/vendors/real/vendor-lifecycle.ts` — the terminal-token vocabulary (`TERMINAL_TOKENS`) the vendor-verdict fix grades registrations against.
- `ROADMAP.md ## Open` — the P0 close-out + the C3/direction-1 [ASK] entries; `## Now` — active work.

## Resume — founder-gated items lead (KIND B); the fix wave that used to occupy slot 1 is CLOSED
1. **C3 (d) arm-gate fix (do this BEFORE ever arming `PROVISIONING_RECONCILE_ENABLED`) — now THREE blockers:** (i) spec divergence — direct-resume and reconcile paths disagree on persona/count for the same ordinal, so an armed (d) over-provisions; (ii) count cap — the reconcile buy path lacks `assertWithinProvisioningCap`; (iii) lifecycle-freeze guard — the reconcile skips `assertNotLifecycleFrozen` (2026-08-14 vendor-verdict wave finding). Options in `ROADMAP.md ## Open` 2026-08-13 [ASK]. Build → adversary gate → deploy under the standing grant. Only THEN is arming (d) a live founder decision.
2. **Watch for Mordy's agent's REACTION to the sent reply** (SENT 2026-08-14 ~21:55Z — see In flight): a new ticket, provisioning progress (mailboxes appearing / DNS flipping), or silence. The cron's legs cover it; on progress, confirm the P0 rebind applied (zero new buys) and notify the founder. `ADMIN_TOKEN` is no longer a blocker — it lives at Keychain `coldrig/admin-token` (rotated 2026-08-14; the Worker holds the same fresh value).
3. **direction-1** (resume-from-resource redesign) — founder-approved to STAGE after C3; scope it as its own wave (schema change, full gate). `ROADMAP.md ## Open`.
4. **Mailbox W7 tail** — separate project, `~/dev/agent-mailbox/HANDOFF.md` (T5 interactive check / T6 Mordy round-trip).
5. **Post-deploy smoke against the REAL vendor happens organically** on Mordy's next retry — no separate drive needed; the new `domain_dns_aging` watchtower check will page if his domains are genuinely stuck.

Prior HANDOFF (reconcile-sweep era): `archive/2026-08-13-p0-c3-mailbox/prior-HANDOFF.md`.
