# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE (test mode):** site https://coldrig.dev · API + dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` · MCP Registry `io.github.YS-projectcalc/agent-cold-email` (v0.2.0, hosted remote + npm package) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold before acting.** If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** (deploy · push · npm publish · real vendor spend · external send · DO support ticket) — SPEC §0 locks NO real vendor spend until the owner works `ACTIVATION.md`.

## Where we are right now (2026-07-20 late-session close-out, on top of the 2026-07-19 post-ship wave below)

**2026-07-22 — the 2026-07-21/22 wave DEPLOYED on the founder's word "deploy."** Merge to main `8a30ec0` (warm-lead #1-3 + self-serve I1+I2, w/ F1 spend-guard fix `dc934e9` + 21→24 claim sweep `453e445` + openapi schema fix `d28afe7`), gated by 4 adversary verdicts (warm-lead SHIP; self-serve CLEAN-SHIP r3; wave gate SHIP r2) + integration battery 597/597 (84 files, typecheck 0 ×5 workspaces). Worker DEPLOYED version `1cb56729-91af-45ba-8e02-13de2db8624d` (live-verified `/status` ok, unauth `tools/list` = **24** incl. `suppress_lead`/`update_lead`/`list_leads`). Site DEPLOYED after Worker (ordering gate held): Pages deployment `25844cc6` (live-verified server-card 24 tools + 24-claiming description, `llms.txt` "24 intent-level MCP/HTTP tools", `openapi.yaml` serving the 3 lead paths + 6 schemas, homepage 200). IndexNow fired post-deploy (HTTP 200 accepted). `main` pushed through `f8fd3d9` (server.json 0.2.1→0.2.2 bump carrying the 24-tool description). Worktrees pruned: `integration-2026-07-21`, both lane worktrees, `byo-intake` (all merged; branches preserved). **What's live:** 24 tools incl. the full lead lifecycle (suppress/update/list), product-driven activation (paid+active+unfrozen+screening-stub replacing `ENGINE_TENANTS`), promo checkout dark until Stripe live keys land. **Nothing armed** — engine/Stripe/InboxKit all unbound in prod, every tenant sandbox; `/checkout/simulate` guarded by `isRealSpendArmed`. **What's NEXT:** I3 managed-mailbox credential path (long pole, InboxKit client-id-request flow unverified live) + I4 InboxKit arming gates + the R3-1 guard-test (failing-by-construction check on `isRealSpendArmed` env coverage); warm-lead #4 (schedule_followup + guarded single-send primitive); the pilot arming session needs the founder present. **Founder queue:** Stripe account creation/KYC, `mcp-publisher publish` re-run for 0.2.2 (registry still serves stale 0.2.1/"21 tools"), GSC/Bing Request-Indexing clicks, review-site listings + a coldrig.dev business email, naming the uptime service for the external prober, Cline test (held).

**2026-07-21 update:** founder-confirmed site deploy batch executed from `main@2cfccb6` (clean tree, in sync with origin) — Cloudflare Pages deployment `e9fd72a3`. Live server-card verified 19→21 tools + fully-live copy. No code changes; `main` unchanged at `2cfccb6`.

**2026-07-21, three decisions ruled/landed:** (1) Warm-lead Q1-Q6 RULED (`SPEC.md` §22, `docs/research/warm-lead-q1-q6-recommendations-2026-07-21.md`) — build UNBLOCKED, increments #1-#3 dispatched dark to a spec-builder in an isolated worktree (#4 follows separately); (2) Registrar DECIDED (delegated choice): Cloudflare Registrar is the default (Namecheap fallback, Porkbun dropped) — `ACTIVATION.md`:25 corrected, `domain-port.ts` rewrite (currently Porkbun-stubbed) surfaced as a new build item; (3) InboxKit prewarm research landed (`docs/research/inboxkit-prewarm-2026-07-21.md`) — pilot buy ~$27-29 awaiting founder GO. Reviews-dogfood and self-serve-activation design lanes remain in flight, unchanged by this update.

**Session close-out — `main` at `51f5015`, in sync with `origin/main`, working tree clean.** Two commits landed this touch: `53effa9` (claim-surface class round 2 — 6 files fixed, adversary SHIP-after-fixes, frozen record `docs/adversarial/claim-surface-round2-2026-07-20.md`) and `51f5015` (InboxKit vendor adapters, dark — RealMailboxPort rewrite + RealInboxKitDomainPort + InboxKitClient + error mapping, 40 new tests, suite 574/574 across 80 files, typecheck 0, adversary SHIP, frozen record `docs/adversarial/inboxkit-adapters-2026-07-20.md`).

- **Watchtower armed + verified** — 1 of the Mordy-pilot blockers CLOSED. ACTIVATION.md Gate 4 is armed and end-to-end verified (cron firing, real inbound-alert path proven). Only the external-prober step remains, folded into the founder's existing monitoring service.
- **InboxKit ruled, propagated, PURCHASED** — Professional tier, $39/mo monthly, 10 Google mailbox slots, renews 08-20. API live; workspace uid recorded in `ROADMAP.md` (2026-07-20 "Mordy pilot" entry). `authorpitchdesk.com` (founder's) is the sending domain; connect is gated on Mordy's registrar/CF access — full runbook (NS-delegation vs CF-API-connect paths, validateDNS mail-record block) lives in that same ROADMAP entry.
- **Adapter lane committed dark, `51f5015`**, with a REQUIRED-BEFORE-ARMING gate list carried in `ROADMAP.md` (2026-07-20 "Mordy pilot" entry): (a) separate domain-port arming flag + ACTIVATION.md:9/:25 reconciliation, (b) exact-email assertion before `/mailboxes/cancel`, (c) provisioning idempotency, (d) display-honesty pass on vendor reputation/placement fields, (e) resolve keyword-search exact-vs-fuzzy semantics with a throwaway mailbox at arming.
- **Claim-surface class round 2 committed+pushed `53effa9`** — Glama re-sync verification pending its next daily sync. **Live server-card DEPLOYED+VERIFIED 2026-07-21** (Cloudflare Pages deployment `e9fd72a3`, from `main@2cfccb6`) — `curl -sL https://coldrig.dev/.well-known/mcp/server-card.json` now enumerates 21 tools with the fully-live description (was 19).
- **Per-port activation** — confirmed already-shipped at `f74687d`, revert-proof done (5 gate tests fail on old logic).
- **FOUNDER QUEUE on return, in leverage order:** (1) Mordy domain access (NS change or CF token, zone must be mail-record-free); (2) `mcp-publisher publish` re-run (official registry still serves stale 0.2.0 "17 tools / early access" description); (3) site deploy batch (live server-card fix rides it) — **DONE 2026-07-21** (Pages deployment `e9fd72a3`; live server-card verified 21 tools + fully-live copy); (4) prewarmed-mailboxes + warmup add-on ($3/mbx/mo) decisions; (5) domain-registrar reconciliation ruling (InboxKit vs Porkbun/Namecheap as registrar default); (6) external prober on his regular monitoring service; (7) OAuth consent-screen publish before ~07-26 expiry.
- **⚠️ InboxKit API key is session-local only** (scratchpad env, dies with this session, per secret rule g — never ledgered) — founder re-pastes it from the InboxKit dashboard next session before any adapter-arming work resumes.

## Prior wave (2026-07-19, post-ship)

**THE FULL SHIP WAVE IS CLOSED.** Everything staged/in-flight in the prior snapshot (below, "Where we were") landed, merged, deployed, and was live-re-verified this pass. `main` is at `cd515e1`, in sync with `origin/main` (bookkeeper-verified `git status -sb`, working tree clean). Four commits landed since the 07-17 snapshot: `e704e4b` (Dockerfile devDeps fix, real fix for the 07-15 npm-defect item), `ea64541` (early-access → fully-live site reframe, 43 files), `1a64e79` (docs ledger), `cd515e1` (merge of `msgid-wire-fix` = `60ff693`, the wire-Message-ID fix). Branch `msgid-wire-fix` and its worktree are gone (deleted/pruned post-merge, bookkeeper-verified). `byo-intake` branch/worktree (`~/dev/coldstart-worktrees/byo-intake` @ `f46e935`) is UNTOUCHED — deliberately excluded from this wave (see below).

- **gmail_api wire-Message-ID bug — fixed, merged, deployed, re-smoked, CLOSED.** `60ff693` records the Gmail-rewritten wire Message-ID (best-effort read-back, dual-recorded with the minted id) so replies resolve threads correctly. Adversary SHIP (`docs/adversarial/msgid-wire-fix-2026-07-19.md`). The re-smoke on the redeployed droplet returned a wire-shaped Message-ID (`<CAMc35PTDteHVgqB86Q2expaHiRP2C42+Z0wAsJtTgf7KhkpQqQ@mail.gmail.com>`) that an independent IMAP search then found on the delivered message (`[Gmail]/All Mail` uid 247031) — recorded==wire proven live. As reported this session, not independently re-verified by bookkeeper (no droplet SSH). Full detail: `archive/ROADMAP-done.md` "Drained 2026-07-19 (ship wave)".
- **Platform Worker DEPLOYED** — version `b3bae1c7-f859-4e8a-9f05-a048466f38bc`, ships the webhooks lane live. Bookkeeper live-verified: `tools/list` = 19 (incl. `get_webhooks`/`configure_webhook`), unauth `GET /webhook-subscriptions` → 401, `/status` → `{"status":"ok"}`.
- **Site DEPLOYED** — Cloudflare Pages deployment `ca4eecaa`, sourced from `cd515e1`, deployed AFTER the Worker (deploy-ordering gate held). Bookkeeper live-verified: zero "early access" text on `/`, `/pricing`, `/docs` (only a defensible pill remains on `/status`'s Dashboard row — non-blocking, own `ROADMAP.md ## Open` line); "concierge step" posture and "Gmail API (HTTPS/443)" transport naming present across every claim surface; `llms.txt` reads 19 tools + npm v0.2.0; `openapi.yaml` serves `/webhook-subscriptions` + `/webhook-subscriptions/{id}`; `og-image.png` regenerated (55386 bytes). Adversary record: `docs/adversarial/fully-live-reframe-2026-07-19.md` (round 1 + round-2 re-attack addendum, gated on the `llms.txt` v0.2.0 line which landed in `ea64541`).
- **IndexNow fired post-deploy** (`tools/indexnow/submit.sh` → reported HTTP 200 accepted) — closes the "run ONLY post-deploy" precondition.
- **Worker-side `ENGINE_AUTH_SECRET` SET** (founder `wrangler secret put`) — bookkeeper-verified live `wrangler secret list` now shows `ADMIN_TOKEN`, `ENGINE_AUTH_SECRET`, `TOKEN_HASH_PEPPER`. `ENGINE_BASE_URL` remains unset — the Cloudflare Tunnel step is still deliberately not done, unchanged from before.
- **THE SMTP WALL IS ROUTED AROUND IN PRODUCTION, END TO END, FOR THE DURABLE-FIX PATH** — the 443 API transports work live, the one defect the production smoke surfaced (wire-Message-ID mismatch) is fixed and re-proven.

**Deliberately NOT done this wave:** `byo-intake` merge — merging would lift the live Worker's MCP tool count 19→21, making the just-shipped "19 tools" claims stale-on-arrival (the exact buyer-agent stale-listing kill class this reframe exists to avoid). Next merge must be a coordinated pass: merge + Worker deploy + site/shopfront count bump + capability copy, together. Cloudflare Tunnel / `ENGINE_BASE_URL` + `ENGINE_TENANTS` allowlist arming (full TENANT-facing traffic, as opposed to the droplet-level smoke proven above) also remain unarmed, unchanged.

## Second wave (2026-07-19, same day)

**The founder ruled on the pending decision queue (batch A/C/D/E/F/G/I) and the byo-intake coordinated merge — deferred from the morning wave above — executed the same day.** `main` is now at `2cd7a51`, in sync with `origin/main`, working tree CLEAN (bookkeeper-verified `git status -sb`).

- **Founder rulings, verbatim IDs** (full text ledgered in `ROADMAP.md ## Open`, 2026-07-19 [ORDER] "Founder rulings batch" — not duplicated here): **A** = delete the 2¢ send-usage fee everywhere; **E** = accept the crash-after-accept-before-record double-send residual for the comped pilot only, durable intent log still required before GA; **F** = GO on the byo-intake coordinated merge; **G** = GO on buyer-panel cycle 2; **I** = founder named mcp.so + Cline as the public submission targets; **D** = BYO-connected mailboxes carry no per-mailbox fee (industry-standard), fold into the quantity-billing migration, no public connect-pricing claim until §20.6 exists; **C** = design the agency-bundle shape (platform fee + per-client-workspace add-on + per-mailbox meter) into that same quantity-billing migration. C and D are design work, not yet built; I (mcp.so/Cline submissions) is not yet executed.
- **`byo-intake` MERGED + DEPLOYED LIVE — ruling F executed as ONE coordinated pass.** Merge commit `8175623` (`byo-intake` → `main`), F-wave copy commit `f214c7c` (19→21 tool-count sweep + BYO capability copy), fee-deletion commit `f86ce6e` (ruling A, same pass — see below). Platform Worker deployed version `d7b300e7-380e-45bd-a831-90ffde19cabc` BEFORE the site — bookkeeper live-verified: `tools/list` = 21 (including `get_byo_domains`/`configure_byo_domain`), unauth `GET /byo-domains` → 401 (exists), `/status` → `{"status":"ok"}`. Site deployed (Cloudflare Pages deployment `3a5cc229`) — bookkeeper live-verified on `coldrig.dev`: `llms.txt` reads "Twenty-one" tools + npm v0.2.0, `openapi.yaml` serves the 6 real BYO paths / 13 BYO schemas, `/byo-domain` page carries live-intake copy, `og-image.png` re-rendered ("21 focused tools"). Adversary round-3 addendum: SHIP after 2 one-word fixes (`docs/adversarial/fully-live-reframe-2026-07-19.md`).
- **Internal 2¢ send-usage fee DELETED, whole class (ruling A) — commit `f86ce6e`, same coordinated pass.** `SEND_USAGE_FEE_CENTS` + its ledger write + Stripe usage-report call removed entirely. Bookkeeper-verified independently: `grep -rn SEND_USAGE_FEE_CENTS apps/platform` = zero matches; full platform suite re-run 534/534 passed (76 files), confirming the builder's reported 537→534 delta. "$0 per-send fees" on the public site is now true by construction. `ACTIVATION.md` Gate-2 checkbox flipped to DONE-as-DELETE; the crash-after-accept-before-record residual (ruling E) is annotated FOUNDER-ACCEPTED-FOR-PILOT 2026-07-19 in the same file.
- **Buyer-panel cycle 2** — already fully ledgered and committed as part of `2cd7a51` (3 fresh runs, `CHOICE-TREND.md` +3 rows, `tools/buyer-panel/runs/2026-07-19-claude-{canonical,starter,agency}.md`); reference only, not redone here. Headline: our $99 SKU now undercuts the cycle's winner (~$112 effective) on at least one run; the remaining kills trace to stale third-party search/directory caches and the billing cold-start, not to product gaps.
- **Still-open founder queue after this wave** (unchanged in kind from the morning-wave list, now updated): (1) Glama re-sync click — highest-leverage single click, a stale pre-reframe Glama listing page was cited as a direct kill sentence in the cycle-2 agency run; (2) GSC/Bing sitemap submission + CF Web Analytics; (3) Stripe live KYC + the quantity-billing migration, which now also carries rulings C (agency bundle shape) and D (BYO-mailbox no-fee) as design inputs; (4) OAuth consent-screen publish to Production (stop the 7-day Testing-status refresh-token churn, next expiry ~2026-07-26); (5) Cline — run one real 10-minute Cline setup test before the honesty-gated submission; (6) mcp.so + Cline submissions (ruling I named the targets, not yet executed); (7) Cloudflare Tunnel / `ENGINE_TENANTS` allowlist arming, carrying the ruling-E-accepted residual, for full TENANT-facing traffic.
- **Warm-lead lifecycle build (SPEC §22, increments #1-#4) remains BLOCKED, unchanged.** Its unblock condition is founder rulings on Q1-Q6 in `SPEC.md` §22 — checked against the 2026-07-19 rulings-batch text (`ROADMAP.md ## Open`) and those six questions are **not** among the IDs the founder ruled on (A/C/D/E/F/G/I only). No warm-lead ruling happened this wave; do not treat any warm-lead build increment as unblocked.
- **Worktree note:** `byo-intake` branch is merged (`f46e935` == the worktree's current HEAD, bookkeeper-verified via `git -C ~/dev/coldstart-worktrees/byo-intake log --oneline -1`). The worktree at `~/dev/coldstart-worktrees/byo-intake` may now be safely pruned — bookkeeper did NOT prune it; that's the next session's or the founder's call.

## Where we were (2026-07-17)

**COMMITTED + PUSHED:** local `main` = `c30cd68`, in sync with origin (verified `git diff origin/main main` empty). Two docs commits landed on top of the 07-16 four-lane arc (`f406132`→`d0e91ec`, unchanged this pass): `8cca0c6` (ledger — Gate-1 smoke PASS, poll+webhooks lanes closed, integration green) and `c30cd68` (docs — corrected OAuth-token provenance, rescued submission drafts, pruned a stale worktree), pushed to origin this session on founder confirmation ("orchestrate everything"). No new code commit landed on `main` this session — this pass is docs + a staged-but-unrun deploy + a completed-but-unmerged branch, three lanes:

1. **Deploy staged, not run.** Engine re-verified at HEAD `c30cd68` (typecheck exit 0; 68 passed/3 skipped). `apps/engine/Dockerfile`'s npm defect (`ROADMAP.md` 2026-07-15 `## Open` item) got its REAL fix, uncommitted in the working tree (`npm pkg delete devDependencies` before `npm install --omit=dev`) — reproduced the 404 on the unfixed Dockerfile, applied the fix, clean rebuild, ran it, `/health` ok, confirmed no dev deps leaked into `node_modules`. Deploy artifact staged at `/private/tmp/claude-503/-Users-yaakovscher/2d80a426-039c-47e2-8ff4-c6ebcf240703/scratchpad/deploy-stage/`: `coldstart-engine.tar` (61MB, linux/amd64, `docker load` round-trip proven — loaded into a fresh tag, ran, `/health` ok, unauth `POST /v1/send` 401'd, torn down) + `DEPLOY-RUNBOOK.md` (folded into `ACTIVATION.md` Gate-2 this pass). ⚠️ **SESSION-MORTAL:** the tarball is in scratchpad and disappears if this session dies — durable fallback is a cheap rebuild from HEAD with the fixed Dockerfile (command in `ACTIVATION.md` Gate-2 step 1). Droplet `/health` re-verified this session (~24.7h uptime, still dark/unarmed — no creds, no tunnel, no Worker secrets). **Correction to the staged runbook, verified this pass:** `npx wrangler secret list` (`apps/platform`) shows the Worker has NEVER had `ENGINE_AUTH_SECRET`/`ENGINE_BASE_URL` set (only `ADMIN_TOKEN`/`TOKEN_HASH_PEPPER` exist) — there is no existing Worker-side secret to reuse; `ACTIVATION.md` Gate-2 now says generate a fresh one and set it on the new container and the Worker together at deploy time.
2. **Warm-lead lifecycle deep dive CLOSED** (already ledgered in `ROADMAP.md ## Open`, not duplicated here): adversary SHIP, frozen dive `docs/research/warm-lead-thin-layer-dive-2026-07-16.md`, ratified design `SPEC.md` §22 (4 new tools, `lead_dispositions` + `followups` tables, webhook/poll split, adversary amendments on the send mechanism). Build gated on founder rulings Q1-Q6 (§22).
3. **BYO domain/mailbox intake BUILT COMPLETE, on branch, unmerged.** Branch `byo-intake` (`~/dev/coldstart-worktrees/byo-intake`), 6 commits off `main@c30cd68`: `aeda46f`→`62b692c`→`217ca2b`→`011576e`→`8dc3305`→`f46e935`. Shape-A (managed mailboxes on the customer's own domain) is the primary path; §20.6 connect seam; OAuth consent UI is a documented stub; real DNS/reputation/domain ports are `NotActivatedError` stubs. Adversary SHIP-dark (`docs/adversarial/byo-intake-2026-07-17.md`) found one must-fix-before-arm design hazard — a burned `fresh_standalone` BYO domain would auto-BUY a lookalike of the customer's brand once real adapters activate — fixed same session (`8dc3305` source-keyed `HARD_PAUSE_DOMAIN` routing + `f46e935` ≤1-active-BYO-primary guard) and re-attacked clean (addendum SHIP, all four attack angles held, RED test proven). Battery at `f46e935`: typecheck 0, platform 537/537 (was 420 pre-lane). **The safe default the adversary prescribed is now built: a burned BYO domain hard-pauses and alerts, it is never auto-replaced/bought.** Yaakov may override that product decision later, but the built behavior is the conservative one. Merge to main [gated:founder].

All three lanes' evidence is frozen on disk (`docs/adversarial/byo-intake-2026-07-17.md`, the staged `DEPLOY-RUNBOOK.md`, `SPEC.md` §22) — nothing above is asserted without a pointer.

## In flight / next

- Still running: **none** — every session agent (builders, adversaries, bookkeepers, panel shoppers) delivered and its output is committed; no background tasks, no in-progress git ops.
- **This ship wave closed the entire prior decision queue** (commit-confirm, OAuth mint + deploy "go", the fully-live reframe) — see `## Where we are right now`. **The second wave, same day, then closed item 1 below** (byo-intake coordinated merge, ruling F) **and rulings A/D/C/E** — see `## Second wave (2026-07-19, same day)`. Nothing is staged-but-unrun anymore; the working tree is clean.
- **No non-destructive build lane is currently open.** Everything buildable without a founder ruling has shipped. Remaining founder queue:
  1. ~~`byo-intake` coordinated merge pass~~ **DONE 2026-07-19** — merged + deployed live as one coordinated pass (ruling F); see `## Second wave`.
  2. **Warm-lead Q1-Q6 rulings** (`SPEC.md` §22) — unblocks build increments #1-#4. **Still open** — the 2026-07-19 rulings batch (A/C/D/E/F/G/I) did not touch Q1-Q6; do not treat warm-lead as unblocked.
  3. **Stripe live keys + quantity-billing migration** — ACTIVATION Gate 2. Now also carries rulings **C** (agency-bundle shape: platform fee + per-client-workspace add-on + per-mailbox meter) and **D** (BYO-connected mailboxes carry no per-mailbox fee) as design inputs for the same migration — design work not yet started.
  4. **Tunnel / `ENGINE_TENANTS` pilot arming** — Cloudflare Tunnel + `ENGINE_BASE_URL` + the tenant allowlist, carrying its already-ledgered residuals (revocation-needs-DO-restart, per-port activation factory change) plus the newly-accepted ruling-E residual (crash-after-accept-before-record, FOUNDER-ACCEPTED-FOR-PILOT). This is the last step to full TENANT-facing traffic; the droplet-level 443 smoke is proven but no tenant has been routed through it yet.
  5. **Older directory/findability items:** **founder named the mcp.so + Cline public-listing targets 2026-07-19 (ruling I)** — Cline submission still needs one real 10-minute Cline setup test first (honesty-gated attestation), mcp.so/Cline submissions not yet executed · re-run `mcp-publisher publish` (server.json 0.2.1) · GSC day-2 Request-Indexing batch · ChatGPT platform identity verification + in-browser policy read · MCPB form check (Team-org-gated?) · enable Cloudflare Web Analytics.
  6. **Weekly Gmail OAuth token re-mint** — the consent screen is in Testing status, refresh tokens expire ~7 days (next expiry ~2026-07-26; as reported this session, not independently checkable — no Google Cloud Console access from here); re-mint or publish the consent screen, or build the §20.6 productized "Connect Google" flow to stop the churn permanently.
- **Other open decisions:** agency-bundle (ruling C, GO — design not yet started) and BYO-mailbox (ruling D, GO — no per-mailbox fee, design not yet started) pricing are now RULED, folding into item 3 above · review-site listings · dogfood-calls scope · **Glama listing sync-click — elevated to highest-leverage single click**, a stale pre-reframe Glama listing page was cited as a direct kill sentence in the cycle-2 agency buyer-panel run (`ROADMAP.md ## Open`, 2026-07-19 agency-run entry).

## Landmines / gotchas

- **`site` pages 308-redirect from `.html` to their clean extensionless form** (`/byo-domain.html` → 308, `/byo-domain` → 200) — found during the byo-intake second-wave live-verification. A bare `curl` against a `.html` URL reads the 308, not the page; use `-L` or hit the extensionless path directly.
- **⚠️ SMTP egress is blocked on the DO droplet** (465/587; 2525/8025 open) — the Mac's network is UNBLOCKED and Gate-1 smoke-proven (real self-send over 465, 07-16). Do NOT assume the DROPLET can send SMTP until it's deployed + smoked; the 443 API transports (gmail_api/ms_graph) route around the droplet block entirely and are the durable fix — gmail_api PROVEN in production 2026-07-19 (two droplet-side live sends, wire-Message-ID recorded==delivered); ms_graph still needs its one Gate-2 live smoke. SMTP itself remains relevant only for vendor mailboxes over port 2525 (Mailforge path, verify at provisioning) — **CORRECTED 2026-07-20:** Mailforge-2525 is now fallback only; InboxKit (2026-07-20 founder ruling) routes via the 443 API transports above, not 2525.
- **`site/` deploys whatever is on disk**, not what's committed — `wrangler pages deploy site --branch main` uploads the local dir as-is (no git-based Pages integration). Tree is clean now, but re-check `git status` before any future site redeploy (bit twice before).
- **support-kb.ts:35-36 still quotes dead $299/$799 tiers** — contradicts the ratified $99 pricing; fix with the billing migration (`ROADMAP.md ## Open`).
- **`SPEC.md` header may still read "~8–12 tools"** — stale (actual **19** after the webhooks lane added tools 18-19; was 17); same staleness class the Glama shopper hit; verify/fix at next SPEC touch (`ROADMAP.md ## Open` shopfront residual i).
- `apps/platform/vitest.config.ts fileParallelism:false` is REQUIRED; fresh-worktree verification needs `apps/platform/.dev.vars` + local D1 migrations or the suite shows phantom failures.
- **Gmail app password for yaakovscher@gmail.com sits on the droplet** `/root/mailboxes.json` (bearer-gated, world-reachable :8080 but can't even send) — rotate anytime and re-paste; nothing depends on it staying.
- Worktrees at this handoff: `main` (`c30cd68`, working tree dirty — see `## Resume`), the designer's own `~/Documents/Codex/2026-07-14/.../coldrig-design` (branch `coldrig-human-interface`, the user's, unrelated to main — leave it), and **`~/dev/coldstart-worktrees/byo-intake`** (branch `byo-intake` @ `f46e935`, 6 commits, adversary SHIP-dark + re-attack ADDENDUM SHIP, unmerged — ⚠️ do NOT prune, it holds real unmerged work pending the founder merge-gate).
- **OAuth secret hygiene:** the Gmail refresh token minted this session (`gmail-oauth.json`) + the smoke's app-password file were DELETED from scratchpad at handoff (rule g — no secret persists). The droplet still holds its own app-password at `/root/mailboxes.json` (see below). Re-mint the OAuth token before the droplet 443 deploy.
- **This is a shared/live worktree with multiple parallel-agent lanes** — the 07-16 four-lane batch (engine-443, directory-readiness, poll fix, webhooks) is now fully committed, but treat this as a standing pattern, not a one-off: before any `git add`, run `git status` and check what else is dirty — do not `git add -A` or commit one lane's files blind.

## Key files

- `archive/2026-07-20-post-two-waves/scratch-rescue/render-og-image.js` — the Playwright SVG→PNG renderer used to regenerate `site/assets/og-image.png` from `og-image.svg` (run via `~/.claude/skills/playwright-cli/run.sh <script> <svg> <png>`); the repo has NO other og-image generator — use this next time the SVG changes.

- `SPEC.md` (§0 locks · §12/§12.1 economics · §18 pricing · §19 dashboard · §20 BYO) · `ROADMAP.md` (`## Now`/`## Open` ledger — source of truth for all open items) · `ACTIVATION.md` (owner-hands gates; Gate-2 engine host = the SMTP-wall context) · `CLAUDE.md` (project law).
- `apps/engine/` — the Node SMTP/IMAP daemon (committed `eb8ee42`). `apps/engine/dist/` — built; runnable from any SMTP-capable network for a local smoke (SMTP-wall path (b)).
- `tools/buyer-panel/` — the blind-shopper CHOICE harness; `runs/2026-07-15-claude-{canonical,starter,agency}.md` — the frozen evidence that we surface but get killed on "not live".
- `docs/adversarial/*-2026-07-15.md` — frozen SHIP verdicts (shopfront, tool-descriptions, guide-pages, compare-pages, watchtower-ops-email).
- `archive/2026-07-16-handoff-smtp-wall/prior-HANDOFF.md` — the resume state this replaced.

## Resume — KIND B: founder decision queue, no unblocked build lane left

**First verify state still holds:** `git -C ~/dev/coldstart status -sb` — expect HEAD at `2cd7a51`, in sync with origin, working tree CLEAN. Also check `git -C ~/dev/coldstart-worktrees/byo-intake log --oneline -1` — expect `f46e935`, MERGED into main as of `8175623` (the worktree/branch is safe to prune but not yet pruned). Also spot-check the live state hasn't drifted: `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`, and a live `tools/list` call → **21** tools (was 19 before the second wave). If any of these differ, a deploy or another session's write happened since this handoff — re-derive state from `git log` + `wrangler deployments list`/`wrangler pages deployment list` before proceeding.

**Step 1 — the founder decision queue** (present all five remaining items from `## In flight / next`, do NOT act unilaterally):
1. Warm-lead Q1-Q6 rulings (`SPEC.md` §22) — still fully open, not touched by the 2026-07-19 rulings batch.
2. Stripe live keys + quantity-billing migration — now also carries rulings C (agency-bundle shape) and D (BYO-mailbox no-fee) as design inputs.
3. Tunnel / `ENGINE_TENANTS` pilot arming (the last step to real tenant-facing traffic), carrying the ruling-E FOUNDER-ACCEPTED-FOR-PILOT residual.
4. The older directory/findability items — mcp.so + Cline targets now NAMED (ruling I) but submission not yet executed (Cline needs one real 10-min setup test first) · registry re-publish · GSC batch · ChatGPT verification · MCPB check · CF Web Analytics · **Glama listing sync-click, now the single highest-leverage item** (a stale Glama listing directly killed the cycle-2 agency buyer-panel run).
5. Gmail OAuth token re-mint before ~2026-07-26 (7-day Testing-status expiry) — a standing chore until the §20.6 productized flow replaces it.

**Meanwhile:** no non-destructive build lane is currently open — everything buildable without a founder ruling has shipped through both waves. A resuming session with no founder input available should NOT start new build scope; the productive move is chasing down one of the five queue items, or the standing OAuth re-mint if it's close to expiry.

**Then, only with Yaakov's explicit confirmation:** any of the five queue items above · review-site listings · dogfood-calls scope.

Do NOT deploy, push, publish, spend, or send without confirming with Yaakov first. (`byo-intake` is already merged — no merge-gate remains on it.)
