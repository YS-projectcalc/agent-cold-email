# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE + has its FIRST PAYING CUSTOMER (Mordy, 2026-07-28).** Site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · branded API https://api.coldrig.dev (new 2026-07-28) · npm `agent-cold-email@0.2.1` (published 2026-07-28) · MCP Registry 0.2.2 (25 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (merge/push/deploy/arm to customer-ready; browser-consent clicks excepted) covers it.

## Where we are right now (2026-07-29 morning — MORDY PAID, registrar ARMED, awaiting his first provisioning)

**🎉 First real customer, path now clear to his first send.** Mordy Tokayer paid 2026-07-28 17:06Z: tenant "Press Outreach" (`ten_91aab24a-43a8-45c1-bf43-af88ef633221`), Stripe sub `sub_1TyESe…` ACTIVE (platform×1 + mailbox×5), MORDYPILOT redeemed, invoice amount **$39.60** (Stripe-verified). ⚠️ **`tenants_index.plan` still reads `demo` — that column is NEVER updated post-signup by design (db.ts only writes `status`); read the tenant DO `/account` or Stripe for real plan/billing state.**

**SHIPPED LIVE + ARMED this session:** (1) Token prefix + rotation lane (`cr_live_` mint, legacy `cs_test_` grandfathered, `POST /token/rotate` + Settings rotate card) — adversary round-1 NO-SHIP (its own concurrency test falsely asserted both-200 on a shared credential) → fixed to an exactly-one-alive invariant → round-2 SHIP (frozen `docs/adversarial/token-rotation-review-2026-07-28.md`), merged `b843963`. (2) Registrar arming (`REGISTRAR_PROVIDER` env leg + per-tenant `registerDomains` opt-in + structured registrant capture) — adversary round-1 NO-SHIP on B1 (domain port selected from a pre-UPDATE profile: fresh opt-in 503'd single-call + false founder alert; opt-out-after-persisted-opt-in still bought a stale registrant) → fixed (setup re-selects ONLY the domain port from THIS call's validated input; env leg inviolable) → round-2 SHIP with 5 independent repros (frozen `docs/adversarial/registrar-arming-review-2026-07-28.md`), merged `c1e24a5`. Verifier battery GREEN at `c1e24a5` (typecheck clean all 5 workspaces — bookkeeper-reconfirmed this pass; platform 1122/1122 bookkeeper-reconfirmed exact match, dashboard 143/143 + engine 126+4skip + cli 12/12 per integration-verifier), pushed `a68361c`. **DEPLOYED + ARMED by founder 2026-07-29:** Worker `4a7bbf0d-299b-4db4-b949-0bf25e841209` (bookkeeper-confirmed via `wrangler deployments list`), both hosts live (workers.dev + api.coldrig.dev), no new migrations, cron intact; `REGISTRAR_PROVIDER=inboxkit` armed — bookkeeper-confirmed present via `wrangler secret list` alongside `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` (the arm is real, not a sandbox no-op). Post-arm live checks bookkeeper-reconfirmed: `GET /account` → 401, `GET /checkout/success` → 302, `GET /app` → 307→200.

**CF-registrar wall resolved live (2026-07-28/29):** InboxKit's UI connected dmhadvisor.com via a ZONE-scoped Cloudflare token (no NS change needed — registrar lock is moot on this path); the public `cloudflare-domains/connect` API still 400s (endpoint discrepancy vs the dashboard — support ticket v2 drafted in `docs/research/cf-registrar-domain-connect-2026-07-28.md`). The re-created dmhadvisor entity is now `scheduled_for_deletion` in the InboxKit workspace (founder ran `domains/remove` 2026-07-29, verified via `domains/list`) — clears the path for Mordy's own setup.

**Standing authorization (founder, verbatim):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done."* Arming executes autonomously (Keychain + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder.

## In flight / next

- **No lanes in flight.** Both the token-rotation and registrar-arming build lanes are merged, deployed, and (registrar) armed. Their worktrees — `.claude/worktrees/agent-a2cb852d0853de94a` and `.claude/worktrees/agent-ae2d6f14e41fd6a02` — hold only already-merged commits (safe to reap, not yet reaped). The warmup-posture research lane also finished (`docs/research/warmup-posture-2026-07-28.md`, untracked — out of this bookkeeping pass's file scope; feeds the Fast-Start SKU design).
- **Founder sends Mordy the go message (drafted in-chat)** — unblocks his agent to run the two `setup_infrastructure` calls: `{primaryDomain:"authorpitchdesk.com", domains:1, inboxesEach:3, registerDomains:true, registrant:{...}}` then `{..., domains:1, inboxesEach:2, registerDomains:true, registrant:{...}}` (registrant + `registerDomains:true` required on EACH call — see Landmines). = 2 domains × (3+2) mailboxes, all 5 paid seats, bill stays $39.60.
- **Watch live once he runs them:** the first real domain buy + mailbox provisioning + fleet-mint OAuth attempt (his mailboxes are its FIRST live test; 10-min manual grant is the fallback) → first-live warmup-ramp watch. Known watch item: double-buy saga risk on downstream throw + retry (`ROADMAP.md ## Open` 2026-07-29, proper home = the B2 async-saga arc).
- **Founder click queue:** InboxKit support ticket v2 (paste from `docs/research/cf-registrar-domain-connect-2026-07-28.md`) → revoke the CF token (Keychain `cf-dns-dmhadvisor`) → Stripe receipts toggle (Settings → Customer emails → "Successful payments") → Glama manual description edit (text in Landmines) → G2/Capterra listing packs (`docs/research/review-site-listing-packs-2026-07-27.md`, 25-gate cleared) → `listings@coldrig.dev` routing rule → PulseMCP word → uptime-prober.
- **Open decisions / blockers (ledgered `ROADMAP.md`, none block Mordy's setup):** unit-economics + build-our-own-infra study ([ASK], NOT started) · Fast-Start prewarmed SKU (founder-authorized for next-session background orchestration) · agency-bundle billing · owner spend-ceiling wiring + billing self-serve actions (both flagged by Mordy's agent) · metric hygiene (63 `tenants_index` rows = 62 internal test tenants + 1 real signup — tenant-count ≠ signup-count in any report).

## Landmines / gotchas

- **Live money is real.** Worker runs LIVE Stripe keys — NEVER complete a live checkout in testing. `MORDYPILOT` already redeemed (single-use, now inactive). Never print Keychain values (`security find-generic-password -a coldrig -s <service> -w`).
- **`tenants_index.plan` is a signup snapshot, NEVER updated** — three payment-detection watches this session built polled it and were blind. Read the DO `/account` or Stripe.
- **`wrangler.toml` `routes` implicitly disables workers.dev** unless `workers_dev=true` is explicit — this took prod down ~3 min on 07-28 (Stripe webhook + every customer MCP config point at workers.dev). Both hosts must stay live: api.coldrig.dev + workers.dev.
- **Registrar arming is DECOUPLED from mailbox arming by design** (`apps/platform/src/vendors/factory.ts` comment ~L56-67) — the arming lane must preserve it: `RegistrarUnarmedDomainPort` throws until BOTH `REGISTRAR_PROVIDER` env AND the tenant `registerDomains` opt-in are present. Do not collapse them.
- **`registerDomains` + `registrant` must ride EVERY `setup_infrastructure` call that uses the registrar** — omitting either on a later call resets the tenant's opt-in to 0 (safe direction: 503, zero buys — but surprising). The MCP tool description says required-when-true; every registrar-using call needs the full registrant object, not just the first one.
- **`tenants_index` row count ≠ signup count** — 63 rows as of 2026-07-29 (bookkeeper-confirmed via D1 `SELECT COUNT(*)`), of which 62 are internal test/QA tenants; only 1 (Mordy) is a real signup. Never report the tenant-table row count as a business metric without filtering known-internal patterns.
- **Pre-send intent log is `[dark-unarmed]`** — merged `608c80a`, engine-side, NOT armed; don't treat the double-send residual as closed until arming is verified live. `ACTIVATION.md` Gate-2 residual (1) line is STALE ("uncommitted") — fix on next doc-touch.
- **SDN "unchanged" fix** (weekend false-alarm): a byte-identical re-push is `ok:true reason:"unchanged"` 200 and advances `fetched_at` (so the 5-min direct-refresh cron doesn't 525-storm); a >10% entry-count drop is still `stale`/422. Never hand-load bypassing `swapInSdnList`.
- **Glama API stale at "~12 tools"** independent of our surfaces (confirmed 25) — needs a manual Glama dashboard description edit, NOT a rebuild. Paste: *"Agent-run cold-email infrastructure: 25 MCP/HTTP tools for domains, mailboxes, warmup, campaigns, and replies behind one bearer token. Free self-serve sandbox, no card required. Real sending is live in production. $99/month all-in for 5 mailboxes (+$10/mailbox after) bundles mailbox seats, domains, warmup, and compliance infrastructure, $0 per-send fees. No inbox-placement, deliverability, or reply-rate guarantees, ever."*
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20`.
- **Site deploys whatever is on disk;** Worker before site when counts/claims change; IndexNow fires post-site-deploy.
- **dmhadvisor.com probe residue:** its mail DNS + kwong root CNAME were deleted and its InboxKit workspace entity removed; full pre-probe DNS snapshot for restore at `archive/2026-07-28-mordy-golive-wave/scratch-rescue/dmhadvisor-dns-snapshot-pre-probe.json`.
- **Uncommitted at handoff:** this pass's `ROADMAP.md`/`HANDOFF.md`/`MEMORY.md` edits are committed via this bookkeeping pass but NOT pushed (push is founder/main-loop cadence) — `origin/main` was confirmed in sync with local `main` (`a68361c`, 0 ahead/0 behind) before this commit. The token-rotation and registrar-arming worktrees (`agent-a2cb852d0853de94a`, `agent-ae2d6f14e41fd6a02`) hold only already-merged commits — reap candidates, not active work. A third worktree, `agent-a25bfeea71119a691`, predates both merges and holds no unmerged work either — also a reap candidate (found this pass via `git worktree list` + `git diff`).

## Key files

- `apps/platform/src/vendors/factory.ts` — adapter arming decisions (sandbox/real/unarmed); the registrar-decouple comment block.
- `apps/platform/src/engine/warmup.ts` — the ramp engine (Mordy's mailboxes = first live run).
- `apps/platform/src/routes/checkout.ts` — checkout + the new success/cancel return routes.
- `docs/research/cf-registrar-domain-connect-2026-07-28.md` — domain-connect wall research + support ticket.
- `docs/adversarial/registrar-arming-review-2026-07-28.md` / `docs/adversarial/token-rotation-review-2026-07-28.md` — frozen adversary verdicts for this milestone's two shipped lanes.
- `docs/research/review-site-listing-packs-2026-07-27.md` — paste-ready G2/Capterra/AlternativeTo packs.
- `ROADMAP.md ## Now` — the live ledger; `## Open` — everything outstanding; `archive/2026-07-28-mordy-golive-wave/` — this session's scratch + prior HANDOFF.

## Resume — await Mordy's setup calls; watch first provisioning live

**KIND B — the immediate next step is founder-owned (send Mordy the go message), then a live-watch, not a build.** Both build lanes from the prior pass are done: verify the preconditions still hold before proceeding — `npx wrangler secret list` should show `REGISTRAR_PROVIDER`, and `npx wrangler deployments list` should show `4a7bbf0d…` (or later) as the latest deploy.

1. **If the founder hasn't sent Mordy's go message yet:** nothing to watch yet — the registrar-arming lane is merged, deployed, and armed (see `## Where we are right now` for the evidence chain). No further build is needed to unblock him.
2. **Once Mordy's agent runs its two `setup_infrastructure` calls:** watch the first real domain buy + mailbox provisioning + fleet-mint OAuth attempt live. Watch for the double-buy saga risk (`ROADMAP.md ## Open` 2026-07-29) — a downstream throw + agent retry could re-buy a domain; it's spend-ceiling-bounded but real money.
3. **After his first send lands:** verify the warmup ramp engine enforces daily caps at the boundary (first-live-proof, per `ROADMAP.md ## Now` warmup entry).
4. **Next session, background-orchestrate the Fast-Start prewarmed SKU arc** (founder-authorized 2026-07-28: pricing design → adapter prewarm methods → inventory-adoption path → claims copy, adversary-gated per lane) while awaiting Mordy — don't let it block watching his provisioning.

Do NOT push past founder-owned clicks (InboxKit ticket, CF token revoke, Stripe toggle, Glama edit — see `## In flight / next`) without the founder doing them; those are outside the 2026-07-22 autonomy grant. Prior HANDOFF: `archive/2026-07-28-mordy-golive-wave/prior-HANDOFF.md`.
