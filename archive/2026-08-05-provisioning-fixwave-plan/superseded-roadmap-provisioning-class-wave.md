# Superseded ROADMAP.md entry — PROVISIONING CLASS WAVE (as of `0d48fbe`)

Replaced 2026-08-05 by the two-wave restructure (`## Open` "PROVISIONING FIX WAVE 1/2") once the new class-sweeps (`docs/adversarial/{sweep-domain-type,sweep-vendor-leak,forward-path-audit}-2026-08-05.md`) superseded the incident-hotfix-gate's diagnosis. Kept verbatim for provenance — several sub-items below are NOT yet re-triaged into Wave 1/2 and were carried forward as a "Deferred" bullet in the new entry; do not treat this file as current.

---

- [ ] 2026-08-05 [ORDER] **PROVISIONING CLASS WAVE (next big arc) — deferred inventory from the 08-05 incident's 5 class-sweeps + pipeline-adversary, consolidated + priority-tagged; NOT started.** Unified `withVendorIntent`/`withSaga` durable-intent mechanism (3 sweeps converged: record-before-effect, resource-named, graded-not-deleted) · ⚠️ **FUSE, PRIORITIZE — frozen VirtualClock full fix:** defeats the shipped day-29 warmup auto-cancel (real vendor `startedAt` vs frozen tenant `now` = negative warmup day, ramp never advances, $3/mo warmup add-on never cancels); sandbox masks it; must land before any real mailbox completes month 1 (compounds the existing R1/R3 warmup fast-follow fuse, `## Open` 2026-08-02 entry) · OAuth minter zero production construction (pipeline-adversary F5) — blocks real SENDING, prioritize for Mordy · discarded sanctions re-screen verdict (F9) + mode-(b) OFAC-screening-bypass-via-checkout-partial-failure — OFAC-adjacent, prioritize · `initTenant` never gets a Stripe customer + Stripe-webhook claim-then-throw (mode-b) · release-retry poisoning (mailbox bills forever) · `REPLACE_DOMAIN` cron lane buys unrecorded, logs the opposite · Stripe checkout no-idempotency/no-record double-session window · monthly ceiling defeat both directions · backwards cross-store orders (terminate-before-token-lock, screening-reject-before-terminate) · CLI/dashboard send has no idempotency keys · error-mapping reflection coverage test + clock-provenance guard · teardown sandbox-flip (F7) · plan-slot leak (F6, slot commits at provision, slot_counted written post-warmup) · `provisioningFailureCount` hardcoded-0 + `pendingCredentialPushes` honest-zero (F8/N6) + watchtower blind to provisioning failures · claim-outlives-teardown billable-row-no-vendor-mailbox (N4) · SANDBOX-REALISM lane (systemic root: sandbox built to always-succeed → green suite hides every live failure; make it fail/stall/crash; `ARCHITECTURE.md` falsely claims a crash-sim exists) · false-doc-claims cleanup (crash-sim, spend-ceiling real-time comment, phantom `jobId`/202 saga in SPEC/openapi/tools) · gate's 2 NEW items: unbounded `USAGE_METERING_SKIPPED`/`MAILBOX_HEALTH_UNAVAILABLE` action rows per `infrastructure_status` poll, `findAdoptableDomain` swallows listing-failure-to-null (double-purchase trigger w/ N1) · remaining `infrastructure_status` field gaps (dns/credential-push/activationState-at-0) · defect-asserting green tests · credstore audit findings (`docs/adversarial/audit-credstore-2026-08-05.md`, overall PASS): F1 stale keyless push reverts a rotation (the i3i4-dropped-idempotency-key regression — content-hash dedup only, no monotonic/version check; add one BEFORE the programmatic `InboxKitOAuthMinter` arms) + F2 teardown doesn't tombstone `mailbox_cred_pushes` rows so a post-cancel reconcile can resurrect a revoked credential (mark rows terminal on teardown) — neither Mordy-reachable today (his path is the deterministic manual mint). Sources: `docs/adversarial/incident-hotfix-gate-2026-08-05.md`, `docs/adversarial/provisioning-pipeline-deep-dive-2026-08-05.md`, `docs/adversarial/audit-credstore-2026-08-05.md`, `.claude/agent-memory/class-sweeper/{vendor-mutation-saga-surfaces,idempotency-replay-surfaces,error-mapping-surfaces,coverage-ledger}.md`.
