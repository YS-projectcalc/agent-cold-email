# ROADMAP done/archive — migrated 2026-07-13

> Migrated from `../ROADMAP.md` under the ROADMAP Contract (see `~/CLAUDE.md` § ROADMAP Contract). This file holds every completed/shipped checklist item and the full historical session log from the pre-migration ROADMAP.md, verbatim. Nothing here was reworded; open/outstanding items were extracted into the new `ROADMAP.md` `## Open`/`## Now` instead of living here.

## Phase A — Foundation & plan hardening — DONE
- [x] A1. Canonical home: git repo; SPEC §0 locked; ROADMAP/README/CLAUDE/ARCHITECTURE/HANDOFF/MEMORY
- [x] A2. Adversarial panel #1 (4 opus lenses) — 4× CONDITIONAL_GO; amendments synthesized + adopted
- [x] A2.5 Architecture settled (ARCHITECTURE.md — hybrid topology, DO-SQLite ledger, injected clock, idempotency, engine-as-adapter)
- [x] A3. cold-cli license = MIT (verified via GitHub API 2026-07-09) — clean to fork; reference for engine contract
- [x] A4. $0 vendor-ToS resale + isolation + unit-economics research DONE (docs/research/vendor-tos-economics-2026-07-09.md) → pricing designed (SPEC §18); resale-permission gate recorded (SPEC §13 — Mailforge ToS-clean, Inboxkit needs enterprise deal, or management-service model); Porkbun buy-domain undocumented → Namecheap fallback. NO-GO branch = facade swap, does not block sandbox.
- [x] A5. Local-mailserver engine spike (GreenMail 2.1.3, real SMTP+IMAP in Docker, $0) — DONE 2026-07-12: all 5 contract behaviors (send / reply-detect / threading / bounce-classify / list-unsubscribe round-trip) validated satisfiable against a real server; spike code + README at `spikes/a5-engine-imap/`; root suite stayed 130/130. Surfaced 4 pre-`VendorPort`-freeze findings — see session log. (NOTE: the 4 findings' own checklist, embedded in the session log below, is still shown unchecked there; a later 2026-07-12 log entry claims they shipped via commit `d342cd0` — the discrepancy is preserved and flagged as an Open item in ROADMAP.md.)

## Phase B — Core product (completed items; B2/B3/B4 moved to ROADMAP.md ## Open)
- [x] B0. **Walking skeleton DONE:** npm workspaces monorepo (`packages/shared` + `apps/platform`); Hono facade over the ~12 SPEC §6 intents; `TenantDO` (SQLite runtime state + money ledger) behind Workers RPC; injected `RealClock`/`VirtualClock`; sandbox `VendorPort` bundle (deterministic reply/bounce simulator) + `real/` stubs + factory with a demo-can't-reach-real guard; native sandbox engine (warmup ramp, tick, poll/reply-processor, threads, reporting). 9/9 tests green (E2E pipe + cap-enforcement + pause + tenant-isolation + demo-adapter-guard), typecheck clean, `wrangler deploy --dry-run` clean. See session log.
- [x] B1. Monorepo scaffold; control plane: tenants, auth/tokens, Stripe test-mode billing+metering, quotas + per-tenant spend/provisioning caps (distinct from usage quotas)
- [x] B5. Agent surface: hosted MCP (direct JSON-RPC 2.0 handler, ~12 tools) + CLI twin + AGENTS.md + no-signup demo (`POST /demo/run`, sandbox-only, structurally gated to demo/free plans)
- [x] B6. Deliverability control-loop LOGIC + tests (monitor→throttle/pause/rotate/replace, domain-burn auto-replace) — built to contract, tuned at activation. `engine/deliverability.ts` (pure `evaluate` + signal gather) + `engine/deliverability-actions.ts` (apply/sweep/replace), wired into the tick BEFORE scheduling; surfaced in `account().deliverability` + `infrastructure_status` per-mailbox health. 78/78 tests (13 new). NOT deployed (owner's call).
- [x] B7. Unified inbox + reply management — UI half absorbed into **B7.1 Dashboard + Unified Inbox (SPEC §19)** below (founder-directed 2026-07-12); resolved via B7.1's M1-M5 completion, not built standalone.
- [x] B7.1 **Dashboard + Unified Inbox (SPEC §19)** — **DEPLOYED LIVE 2026-07-13**: merged to main (`77741ae`) and shipped to production via `npm run deploy` (worker version `e9734c42-984a-47f3-805a-3c261db4d06c`); live at https://agent-cold-email-api.yaakovscher.workers.dev/app (same-origin via the Worker's `[assets]` config). Founder-directed 2026-07-12, absorbs/implements B7's UI half. Spec SHIPPED (adversarial verdict SHIP after 2 rounds — `docs/adversarial/dashboard-spec-review-2026-07-12.md`). M1-M5 all DONE 2026-07-13, committed `c9e5e27` on branch `feat/dashboard-inbox-spec-19`, merged, deployed, and PUSHED 2026-07-13 (`origin/main` == `71bcd81`, CI run `29246972278` green — see 2026-07-13 (push) session-log entry). Milestones (§19.8):
  - [x] M1 backend — serving spike (`[assets]` config + wrangler-dev gate) → TenantDO schema bootstrap + `last_polled_at` → shared zod layout schema/registry → inbox v2 (composite cursor) → listCampaigns/activity → views CRUD + rev CAS → D1 `0006_dashboard_sessions` cookie session + global CSRF middleware → MCP tools 13–15. DONE 2026-07-12, 190/190 tests (148→190, +42), serving spike proven (401-JSON API / SPA fallback / 307 on /app / JSON 404), cookie-session + global CSRF + inbox v2 (composite cursor, no-N+1 asserted) + views CRUD w/ rev CAS + MCP tools 13-15.
  - [x] M2 dashboard SPA shell + widgets (tokens, grid renderer, widget registry v1, provenance badges, Settings) — DONE 2026-07-13.
  - [x] M3 inbox (virtualized paginated list, filters, thread detail, composer, labels, keyboard + swipe + undo, Cmd+K) — DONE 2026-07-13.
  - [x] M4 agent-control e2e + saved views + dark mode + a11y pass — DONE 2026-07-13.
  - [x] M5 perfection loop (screenshot 1440/390 → design-review → fixes → repeat; verifier battery; adversary re-attack gate) — DONE 2026-07-13, two rounds (R1 defects A-I; R2 7 more items) closed on top of a fresh-context adversarial security re-attack (SHIP) + fresh-context UX review (SHIP-AFTER-FIXES, fixes landed in R2) + a full verification battery, all confirmed clean before commit.

## Phase C — Distribution machine (completed items; C4 and C5/C6 remainders moved to ROADMAP.md ## Open)
- [x] C0. **Identity vs brand split:** keyword-permanent slug `agent-cold-email` for repo/npm/registry (never renamed); coldrig/coldpipe/coldloop = domain/display brand only, at activation
- [x] C1. Public GitHub repo (LIVE https://github.com/YS-projectcalc/agent-cold-email, public, 11 topics) (keyword slug, under YS-projectcalc; AGENTS.md + README at agents' literal queries; guardrails prominent so abuse-scanners read it as compliance-first infra)
- [x] C2. Docs + AEO content on Pages/github.io (crawlable, authority-accruing): AGENTS.md + OpenAPI + JSON-LD (the real assets) + llms.txt (convenience); not-yet-available + no-deliverability-guarantee disclosures; privacy policy BEFORE any waitlist email
- [x] C3. Agent-facing free first-use (npx demo works vs live sandbox via /demo/run): `npx agent-cold-email demo` runs full pipeline vs live sandbox, no signup; lead capture + conversion handoff at the end

## Phase D — Business automation & the missing lanes (completed items; D3 and D4/D6 remainders moved to ROADMAP.md ## Open)
- [x] D1. AI support lane: support inbox → agent triage → auto-answer/escalate → daily digest (armed at activation). `POST /admin/support/triage` (classify billing/deliverability/how-to/abuse-report/other, draft an FAQ answer from a built-in KB grounded in SPEC §6/§18/§7/§10, or escalate) + `GET /admin/support/digest`. Real inbound email (Cloudflare Email Routing → this endpoint) is the ACTIVATION step, per the line above.
- [x] D2. Ops routines: deliverability loop, metrics/watchdog digest, **dunning/failed-payment sweep**. `POST /admin/ops/dunning-sweep` (scans `past_due` tenants, retry→escalate→suspend by failure-count cycle, idempotent per cycle, `suspend` is a real local state transition now) + a `scheduled()` Cron Trigger export (`src/scheduled.ts`: deliverability sweep for every tenant → dunning sweep → digest) — the `[triggers]` block is commented-out in `wrangler.toml`, armed at activation; `scheduled()` itself is built and independently callable now.
- [x] D5. Lifecycle ops: voluntary cancellation + infra teardown/reclaim (+ annual-domain liability accounting); abuse-offboarding ops executing the abuse-drop ToS (terminate + reclaim + honor suppression); chargeback/dispute lane. BUILT: `POST /cancel` (tenant-authed — end-of-period `canceling` / immediate `canceled`, releases domains+mailboxes via `DomainPort`/`MailboxPort.release`, stops campaigns, books per-domain annual liability, idempotent) + `POST /admin/tenants/:id/terminate` (ADMIN_TOKEN — suspend + same teardown path + retain suppressions + D1 `enforcement_actions` audit + token lockout so a terminated tenant can't re-provision) + `charge.dispute.created/.closed` webhook cases (freeze `disputed` → tick send-freeze; won→unfreeze/resume; idempotent by event id via `webhook_events`) + digest lifecycle counts (canceled/terminated/disputed + total annual-domain liability). Deferred to ACTIVATION.md: real registrar/mailbox release calls + real Stripe dispute→tenant routing (sandbox ports execute the release now; test payloads carry `metadata.tenantId`).

## Phase E — Hardening & finish (completed items; E3 moved to ROADMAP.md ## Open)
- [x] E1. Adversarial panels: #1 (plan) ✔, #2 (live surfaces) ✔, #3 (final/whole-system) ✔ — all 3 remediated + redeployed + live-verified. Records in docs/adversarial/panel-0{1,2,3}/.
- [x] E2. Full verification battery: 130/130 tests green; every lane deployed + live-smoked; 3 adversarial panels all clean-after-fix.
- [x] E4. Final report: name-candidate presentation, pricing rationale, ACTIVATION.md (first activation gate = the real-world deliverability smoke test), held-questions list

## PROGRAM COMPLETE (test-mode CORE) — 2026-07-09
The entire business is built, deployed, and running in test mode, hardened through 3 adversarial opus panels (all remediated + live-verified). 130/130 tests. Live: API https://agent-cold-email-api.yaakovscher.workers.dev · site https://agent-cold-email.pages.dev · repo https://github.com/YS-projectcalc/agent-cold-email. Owner go-live path = ACTIVATION.md; owner summary = FINAL-REPORT.md.
Remaining = ACTIVATION-hardening backlog (owner-gated or refinement of working systems): B2 resumable sagas + end-of-period/send-bill reapers, D4 OFAC, A5 engine spike, D6 DR/key-rotation, real Stripe+vendor+engine wiring. All documented; none block the test-mode business.
- 2026-07-09: **Panel #3 (final gate) remediated + shipped** (v 13f8ee36, 130/130). Wave 1: billing/lifecycle state machine (webhook fail-closed, sticky disputed/canceled, deliverability-sweep freeze, quota-live-only, teardown-epoch, cancel-blocks-writes, dunning reactivation, body caps, waitlist-durable+digest, MCP input-mode) — 5 live-proven HIGHs revert-proven. Wave 2: clean-URL AEO (no 308 hops). Live-verified: webhook forge→503, admin gated→401. Records docs/adversarial/panel-03/.
- 2026-07-12: **Agent search-behavior panel run** (8 fresh-context Claude probes, 5 sonnet + 3 opus, real web searches) to ground what a coding agent naturally queries when asked to stand up cold email. Record: `docs/research/agent-search-queries-2026-07-12.md`. Headline: agents search category keywords + incumbent comparisons, not brand names — "cold email MCP server" was a universal query (8/8) and an "Instantly vs Smartlead" comparison was also universal (8/8), while 0/8 discovered agent-cold-email (unindexed, expected). Keyword-name beats brand-name for agent-driven traffic, reinforcing the existing keyword-permanent-slug naming decision.
- 2026-07-12: **Resale legal model DECIDED** (verbal, in-session): start with Mailforge (option b) as the activation-time real mailbox vendor while pursuing an Inboxkit enterprise/reseller agreement (option a) in parallel; management-service (option c) stays under active evaluation as a possible additional offering tier. Recorded in ACTIVATION.md Gate 0/1/2 + HANDOFF.md. Three analysis lanes launched in parallel: brand-name adversarial review of "agentcoldemail as the brand" (name still undecided), a competitor pricing sweep, and a Mailforge/Inboxkit cost deep-dive feeding the still-pending pricing sign-off.
- 2026-07-12: **Three analysis lanes completed.** (1) Brand-name adversarial review → **NO-SHIP** on the pure-keyword "agentcoldemail" candidate; hybrid structure (distinct brand + permanent `agent-cold-email` keyword slug) confirmed as the correct — and already-shipped — design. Record: `docs/adversarial/name-review-2026-07-12.md`. (2) Competitor pricing landscape frozen. Record: `docs/research/pricing-landscape-2026-07-12.md`. (3) Mailforge/Inboxkit COGS frozen — headline finding: Mailforge's advertised $3/mbx excludes warmup (separate Warmforge product, $10/mbx), pushing standalone all-in to ~$13.5/mbx; a Salesforge-bundle "unlimited warmup slots" escape hatch is unverified at reseller scale (new ACTIVATION.md Gate 1 item). Record: `docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md`. ACTIVATION.md and HANDOFF.md updated accordingly.
- 2026-07-12: **Two conversation-level design conclusions folded here as the canonical record** (support the pending founder decisions; not yet built): (a) **Two-tier vendor positioning** — if option-c ships as an additional tier, the menu is a genuine good/better ladder, not arbitrage: Standard (Mailforge under the hood) = fastest setup, one signup, one blended price, budget shared-IP infra; Dedicated/Pro (customer-owned Inboxkit account, we operate) = real Google/M365 mailboxes, own reputation universe, customer owns the assets (anti-lock-in), transparent pass-through infra + explicit management fee — priced at a PREMIUM (its buyers are placement-sensitive, not price-sensitive). Build prerequisite: per-tenant vendor credentials in the VendorPort facade (currently single platform-owned account). (b) **Warmup COGS strategy ladder** for the Mailforge path — never default to $10/mbx Warmforge forever: (1) if the Salesforge $80/mo "unlimited Warmforge slots" bundle verifies at reseller scale (Gate-1 item), take it — warmup ≈ $0 marginal; (2) else use vendor warmup as a RAMP ONLY (~first 3-4 weeks per mailbox, slots rotated) since research says warmup value is front-loaded and pool-based warmup is increasingly detected via graph topology (SPEC §9); (3) at tenant critical mass, run the internal tenant-to-tenant warmup pool under our AI mimicry layer (the warmbly-fork L2 option pulled forward) — zero vendor cost. Real replies remain the durable reputation engine either way.
- 2026-07-12 (later): **Option-c APPROVED as the additional premium "Dedicated" tier** (founder, verbal: "that dedicated tier makes sense") — recorded in ACTIVATION.md Gate 0; fast-follow build prereq = per-tenant VendorPort credentials. **Option-B blast-radius rationale (why shared-IP Mailforge is an acceptable start, the analysis behind the resale decision):** one killed email / complaint = survivable background noise (Google's threshold is a 0.3% spam RATE, not a count); one burned domain = contained to that tenant (8–18%/mo burn priced in, B6 loop auto-replaces); the real exposure is shared-IP pool reputation from OTHER Mailforge customers we don't control — degradation-not-termination, i.e. a churn/quality risk, not existential; mitigations = B6 per-mailbox monitoring/rotate/replace + enforcement ladder + VendorPort migration path + the Dedicated tier as the in-house upgrade for placement-sensitive customers. Related L2 note: private warmup pools showed a +20–30% edge in the 07-09 research — the number backing "verticalize the pool later." **SEO/AEO backlog now has its own frozen derivation record** with the per-item arguments (tool-count-heuristic counter, assembly-question guide, maintenance signals): `docs/research/aeo-backlog-2026-07-12.md` — still pending owner green-light. **Correction pointer:** `docs/research/agent-search-queries-2026-07-12.md` line "These 44 issued queries" undercounts — the frozen per-probe lists actually total **46** (6+5+7+6+6+5+5+6); 46 is the correct harness count (per this log + HANDOFF); the frozen doc body is left unedited per frozen-record discipline. **Salesforge→Warmforge bundle desk-verification launched** (public ToS/docs pass; support-inquiry draft if unresolvable) — result lands in this repo when done.

## Session log (verbatim from ROADMAP.md pre-migration)
- 2026-07-09: Program start. Interview (10 Qs) locked; SPEC §0 written; repo + governance docs (CLAUDE/ARCHITECTURE/HANDOFF/MEMORY) initialized.
- 2026-07-09: Adversarial panel #1 (4 opus lenses vs SPEC+ROADMAP) → 4× CONDITIONAL_GO. Synthesis in `docs/adversarial/panel-01/`. Adopted: hybrid topology (engine off-Worker), DO-SQLite money ledger, injected clock + fault-injecting sandbox, idempotency keys, compliance-as-code (per-tenant address/identity, CA 17529.5, full opt-out, OFAC/friction ladder, demo type-guard), identity-vs-brand split (keyword-permanent slug), DoD rescoped to test-mode CORE, C-shell parallel with B, hardening-budget rule, +11 missing business lanes (dunning/teardown/abuse-ops/chargeback/health-metrics/DR/key-rotation). A3: cold-cli = MIT ✓. Next: A4 vendor research (running) + A5 engine spike, then B0 skeleton.
- 2026-07-09: **B0 walking skeleton built + verified.** Monorepo: `packages/shared` (domain types, `Clock`/`VendorPort` interfaces, zod intent schemas) + `apps/platform` (Hono facade, `TenantDO` w/ SQLite ledger via Workers RPC, `RealClock`/`VirtualClock`, sandbox `VendorPort` bundle + `real/` NotActivatedError stubs + demo-guarded factory, native engine: warmup ramp, tick, reply-processor, threads, reporting). D1 `coldstart-platform-db` created (`wrangler d1 create`, id `119be39d-05c3-4608-b628-b2cc7bddfac1`). Full flow proven end-to-end (signup→setup_infrastructure→warmup ramp via injected clock→launch_campaign→tick sends→sandbox poll returns 1 reply+1 bounce→stop-on-reply+bounce-suppression→inbox/thread/reply→metrics/account) plus per-mailbox daily-cap enforcement, pause/pause_all, tenant isolation (separate TenantDO per tenant), and the demo-can't-reach-real-adapter guard. `npm test`: **9/9 passed** (3 files). `npm run typecheck`: clean. `npx wrangler deploy --dry-run`: clean (TENANT DO + DB bindings resolved). Deliberately deferred (documented in code comments, in-scope for later phases): async resumable provisioning sagas/DO alarms (B2), Stripe test-mode wiring beyond a stub `BillingPort` (B1), full IANA timezone send-window handling (later), MCP/CLI surface (B5). Noted mid-build: this worktree is shared/live — a concurrent commit (`74e4e9f`) swept up in-progress B0 files via an automated `git add -A`; per standing git-discipline rules this agent left the final `git add`/`commit` to the orchestrator rather than committing itself. Next: A5 engine spike, B1 control-plane hardening (real auth/quotas/billing), B2 sagas.
- 2026-07-09: **B0 DEPLOYED LIVE (test mode)** → https://agent-cold-email-api.yaakovscher.workers.dev (Cloudflare free tier, sandbox vendors, remote D1 migrated, TOKEN_HASH_PEPPER secret set). Live smoke green: signup→provision(2 domains/4 mbx warming, day-1 caps)→campaign→metrics→account; tenant isolation verified on live URL (cross-tenant=404, no-token=401). NOTES for later lanes: (1) sandbox tick/clock/pollInbox are DO-RPC-only, NOT HTTP — C3 demo needs a sandbox-safe run endpoint to drive the full pipe over HTTP; (2) B0 signup only mints plan:demo — paid-plan + Stripe path is B1.
- 2026-07-09: **D1/D2/D6 business-ops automation built + verified (NOT deployed, owner's call).** New `src/admin/` surface (support-kb, dunning, db, ops-sweep, schemas + README) + `src/routes/admin-support.ts`/`admin-ops.ts`/`status.ts` + `src/require-admin-auth.ts` (timing-safe `ADMIN_TOKEN` bearer check, fails closed) + `src/scheduled.ts` (Cron Trigger entry point, `wrangler.toml` `[triggers]` commented-out) + `migrations/0002_admin_ops.sql` (`support_tickets`, `dunning_events`) + `TenantDO.opsSummary()`/`deliverabilitySweep()`/`suspendForDunning()` RPCs (`src/engine/ops-summary.ts`). Caught+fixed live during build: Hono composes EVERY registered handler matching a request path into one chain regardless of "sub-app" — an unscoped `admin.use("*", requireAdminAuth)` 401'd every tenant route too; fixed by scoping to `admin.use("/admin/*", ...)` (see `src/index.ts` comment; reproduced red via a temporary cp-backed revert of `require-admin-auth.ts`, confirmed green after restore). `npm test`: **102/102 passed** (23 files, 24 new: `admin-auth`/`admin-support`/`admin-dunning`/`admin-digest`/`admin-unit`/`scheduled`/`status`.test.ts). `npm run typecheck` (root + platform): clean. `npx wrangler deploy --dry-run`: clean. Deferred to ACTIVATION.md: real inbound support email (Cloudflare Email Routing), the cron trigger arm, real dunning retry emails. Deferred to a later session (separate D6 sub-scope, not in this brief): per-tenant margin, ledger backups/DR, master-key rotation.
- 2026-07-09: **B5 agent surface + C6 waitlist SHIPPED + DEPLOYED LIVE.** (1) `POST /mcp` — a direct hand-rolled JSON-RPC 2.0 handler (NOT the Agents SDK `McpAgent`; ARCHITECTURE.md #7 superseded by this brief's "thin facade" call for the transport only, tool contract unchanged): `initialize`/`notifications/initialized`/`tools/list`/`tools/call`, 12 tools matching AGENTS.md exactly, `inputSchema` via zod v4's native `z.toJSONSchema()` (no new dependency). Auth resolves the tenant FRESH from the bearer token on every `tools/call` (`resolveTenantFromToken`, shared with the HTTP `requireAuth` middleware) — no caching, proven by an interleaved two-token isolation test. (2) `POST /demo/run` — synchronous sandbox-only pipeline run (advance clock past warmup → launch a canned deterministic-reply/bounce campaign → tick → poll → tick/poll again to prove stop-on-reply), structurally gated to demo/free plans via `TenantIsolationError` thrown inside `TenantDO.demoRun()` (403), not an HTTP-layer check. (3) `packages/cli` (`agent-cold-email`, not yet published — builds locally via `npm run build -w agent-cold-email`, plain tsc→NodeNext ESM, zero runtime deps): `demo` hero command (signup→setup→`/demo/run`→human-readable summary+honest sandbox disclaimer) + signup/setup/status/campaign/inbox/metrics/pause/account thin wrappers, `--token`/`AGENT_COLD_EMAIL_TOKEN`, exponential-backoff polling. (4) `POST /api/waitlist` — KV-backed (`WAITLIST` namespace created via `wrangler kv namespace create`), email dedupe, per-IP sliding-window rate limit (5/60s), CORS for `https://agent-cold-email.pages.dev`. AGENTS.md updated (MCP no longer "once live"; CLI publish status disclosed honestly). Tests: 28/28 green (`npm test`), up from 9/9 — new: `mcp.test.ts` (9, incl. two-token interleaved isolation), `demo-run.test.ts` (4, incl. non-demo-plan 403 via a test-only `mintTenant` helper), `waitlist.test.ts` (6, incl. dedupe-doesn't-overwrite + rate-limit-then-403). `npm run typecheck`: clean, all 3 workspaces (`@coldstart/platform`, `agent-cold-email`, `@coldstart/shared`). `npx wrangler deploy` → live, Total Upload 666.41 KiB / gzip 109.45 KiB, bindings TENANT/WAITLIST/DB all resolved. Live-smoke (curl) green: initialize→serverInfo, tools/list→12 tools, tools/call setup_infrastructure→jobId, `/demo/run`→sent:4/replies:1/bounces:1/stopOnReplyProof true, waitlist POST→ok:true + OPTIONS preflight→CORS headers. CLI `demo` run against the live deployed API: 2 domains/4 mailboxes provisioned, sent:4/replies:1/bounces:1, stop-on-reply proof, honest "ran in a sandbox" closing line. Deliberately deferred (B5 was scoped to the agent surface, not real sending): Stripe/paid-tier wiring (B1), DO-alarm-driven scheduling (B2), MCP registry submissions + npm publish (both identity-gated, C5/ACTIVATION.md).
- 2026-07-09: **B5 + distribution SHIPPED LIVE.** Site LIVE https://agent-cold-email.pages.dev (AEO shell, all assets 200). Public repo LIVE https://github.com/YS-projectcalc/agent-cold-email (11 topics, AGENTS.md/README/LICENSE/site). Worker redeployed with /mcp (12 tools, JSON-RPC streamable-HTTP, fresh-per-request tenant resolution), /demo/run (sandbox-only accelerated pipe), /api/waitlist (KV+CORS+rate-limit), packages/cli 'agent-cold-email' (demo verified live: sent:4/replies:1). 28/28 tests. Commit 6187f14. Adversarial panel #2 (4 opus lenses vs live surfaces) RUNNING.
- 2026-07-09: **Panel #2 fully remediated + shipped.** Wave 1 (11 platform fixes, 50/50 tests, 5 revert-proven) + Wave 2 (site honesty). Worker redeployed (v 278497e6, +RateLimiterDO); site redeployed; repo pushed. Live-verified fixes: brand="Google"→400 (was provisioning trygoogle.com), token cs_test_, 10MB body→413, signup 3×201→429 rate limit, demo throttle. security-isolation lens was CLEAN (no cross-tenant leak). Records: docs/adversarial/panel-02/.
- 2026-07-09: **B1 money path built (NOT yet deployed — pending review).** `TenantPlan` grew concrete paid tiers (`"demo" | "free" | "launch" | "growth" | "scale"`, replacing the placeholder `"paid"`); `packages/shared/src/pricing.ts` is the single `PLAN_QUOTAS` source (SPEC §18: Launch 2dom/5mbx $99, Growth 6/20 $299, Scale 18/60 $799). (1) **Checkout:** `POST /checkout {plan}` (authed) — real Stripe TEST-mode Checkout Session (subscription mode, inline `price_data`, tenantId in `client_reference_id` + `metadata`/`subscription_data.metadata`) when `env.STRIPE_SECRET_KEY` is set (`src/billing/stripe-client.ts`, coded fully, inert/UNVERIFIED — no key anywhere); else a simulated session (`checkout_sessions` table) completed via unauthenticated `GET /checkout/simulate?tenant&session` (session id is the credential, tenant-scoped, idempotent-on-replay, cross-tenant hijack → 404). (2) **Quotas + spend cap:** `engine/quota.ts` `assertWithinProvisioningCap` — paid tiers rejected (400) past PLAN_QUOTAS cumulative domains/mailboxes; demo/free get a separate flat `SANDBOX_PROVISIONING_CAP` (5 domains/15 mailboxes) as the "distinct from usage quota" runaway guard (demo/free real spend is already 0 via the existing vendor-factory structural guard). `account()` now returns `billingState`, `sends`, and `quota`. (3) **Metering:** per-mailbox/mo fee (600c, paid tiers only, idempotent on the mailbox's provisioning key) added alongside the existing per-send fee (2c); both aggregate into `account().usageCents`; `engine/billing.ts#reportUsageToStripeIfConfigured` codes the Stripe usage-record reporting call (inert without a key + a stored subscription id). (4) **Webhook:** `POST /webhooks/stripe` (unauthenticated — HMAC `Stripe-Signature` verification when `env.STRIPE_WEBHOOK_SECRET` is set, else test-sim accept) handles `checkout.session.completed` / `customer.subscription.updated` / `.deleted` / `invoice.payment_failed`, idempotent per Stripe event id (`webhook_events` table, `INSERT OR IGNORE` claim). New tables: `checkout_sessions`, `webhook_events`; new `tenant_profile` columns `billing_state`/`stripe_customer_id`/`stripe_subscription_id` (backfilled via the existing `ensureColumnMigrations` pattern for already-deployed DOs). New dir `src/billing/` (+README). Tests: **65/65 green** (`npm test`, 14 files, up from 50/50) — new: `checkout.test.ts` (5), `webhook.test.ts` (4), `quota.test.ts` (4), `metering.test.ts` (2); fixed 3 pre-existing `"paid"` literal usages (now `"launch"`) broken by the TenantPlan type change. `npm run typecheck`: clean, all 3 workspaces. `npx wrangler deploy --dry-run`: clean (same 4 bindings, no new resources needed — Stripe keys are plain secrets, not bindings). Deliberately deferred/UNVERIFIED: the real-Stripe checkout/webhook-signature/usage-report code paths are coded-to-docs but never exercised against a live Stripe account (no test key in any environment here — that's the ACTIVATION.md step); resolving a Stripe subscription's line-item id for usage reporting (currently reports against the stored subscription id with a code comment flagging the activation-time gap); MCP tool surface NOT extended with a `checkout` tool (out of this brief's explicit HTTP-facade scope). Left uncommitted per shared-worktree git discipline — orchestrator's call.
- 2026-07-09: **B1 money path SHIPPED LIVE** (v ba24f7d3, 65/65 tests). Live-verified: signup→demo (sandbox quota 5/15), POST /checkout growth→simulated session→complete→plan upgraded to growth (quota 6 domains/20 mbx), over-quota setup→400. Real Stripe test-mode path coded+inert (STRIPE_SECRET_KEY=activation). Idempotent webhook. Commit 1a05959.
- 2026-07-09: **B6 deliverability control loop built (NOT deployed — owner's call).** SPEC §10 monitor→decide→act as sandbox LOGIC + tests. (1) `engine/deliverability.ts` — a PURE `evaluate(mailboxHealth[], domainStats[], thresholds) -> Action[]` (THROTTLE/PAUSE/ROTATE/REPLACE_DOMAIN) + `gatherMailboxHealth`/`gatherDomainStats` that compute first-party per-mailbox/per-domain bounce+complaint RATES from the event log, in FRACTION units (Gmail 0.30%=0.003, NOT 0.30 — the 100x trap is documented + guarded). Thresholds grounded in SPEC (Gmail 0.30% ineligibility; §7 caps; §7/§15 domain burn 8-18%/mo normal → auto-retire-replace at the top of that band). (2) `engine/deliverability-actions.ts` — `applyActions` mutates DO state: THROTTLE via a `cap_override` column (survives the per-tick warmup-cap recompute), PAUSE via a `deliv_status` column DISTINCT from warmup status (so a warmup refresh can't resurrect it), REPLACE_DOMAIN marks the domain 'burning' + pauses its mailboxes + auto-provisions a replacement via the shared `provisionDomainWithMailboxes` helper (extracted from setup_infrastructure, DRY) + re-parks sends; every action audited to a new `deliverability_actions` table. `runDeliverabilitySweep` runs in the tick BEFORE scheduling; the send picker excludes `deliv_status='paused'` (this realizes ROTATE — pending sends aren't pre-bound to a mailbox, so the picker reroutes them). (3) **Fault injection:** sandbox EmailPort emits a spam-complaint for "complaint"-tagged recipients (mirrors its reply/bounce triggers); reply-processor `processComplaint` suppresses + attributes per-mailbox via the original send's message id. (4) **Safety:** re-pausing a paused mailbox is a no-op (conditional UPDATE, no dup audit); domain-replace is per-window rate-capped (MAX 3/30d) so a replacement that also burns can't spawn forever (capped ones still retire, logged REPLACE_DOMAIN_CAPPED for owner intervention). (5) **Surfaced:** `account().deliverability` (paused/throttled/burning/replaced counts + recentActions[]) + `infrastructure_status` per-mailbox health gains delivStatus/complaint+bounce rates/vendor reputation+placement (getHealth, on-demand not on the hot path). 12-tool surface UNCHANGED (extended `account`+`infrastructure_status` responses + their AGENTS.md rows). Tests: **78/78 green** (up from 65/65) — new `deliverability.test.ts` (8 pure-`evaluate` units: throttle/pause/replace/rotate/idempotent-repause/idempotent-rethrottle/burn-by-bounce/min-sample-gate) + `deliverability-loop.test.ts` (5 integration: headline end-to-end complaint→pause→burn→replace→rotate through the REAL send/poll pipe; throttle-survives-warmup-refresh; idempotent re-pause no-dup; replacement cap 3+1-capped; ROTATE routes to healthy box). Revert-fail-restore PROVEN on 2 interaction guards: without `cap_override` handling the throttle test fails "expected 40 to be 5"; without the tick's paused-exclusion the headline+rotate tests fail (paused box still sends). typecheck clean (3 workspaces); `wrangler deploy --dry-run` clean (same 4 bindings, no new resources). **Deferred to activation (real-signal tuning, per hardening-budget rule):** threshold VALUES tuned against live Gmail (built-to-contract now); vendor getHealth batching/caching if moved onto the hot path; a mailbox/domain RECOVERY/un-pause path (loop only degrades today — recovery is a live-signal concern). Left uncommitted per shared-worktree git discipline.
- 2026-07-09: **D5 tenant lifecycle built + verified (NOT deployed — owner's call).** Four coherent pieces. (1) **Voluntary cancel** `POST /cancel` (tenant-authed, `engine/lifecycle.ts`): end-of-period `canceling` default / immediate `canceled`; shared `teardownTenant` releases every domain (`DomainPort.release`) + mailbox (`MailboxPort.release`, sets `released_at` + `deliv_status='paused'`), stops campaigns (reuses `pauseAllCampaigns`), books a per-domain annual-domain liability as its own idempotent `kind='liability'` ledger row (SPEC §12 .com $11.08/yr, prorated by remaining term); `account().teardown` surfaces the summary; idempotent via a `teardown_records` anchor (re-cancel = no-op). (2) **Abuse offboarding** `POST /admin/tenants/:id/terminate` (ADMIN_TOKEN, reuses `require-admin-auth`): suspend + same teardown, **retains suppressions** (CAN-SPAM opt-outs survive), logs reason+evidence to D1 `enforcement_actions` (idempotent per tenant, migration 0003), and locks the D1 `tenants_index` token so a terminated tenant can't re-provision to undo the reclaim. (3) **Chargeback lane** `charge.dispute.created/.closed` in `engine/billing.ts`: freeze `billing_state='disputed'` (a new `runTick` guard stops ALL sends for suspended/disputed tenants — reversible), record to a per-DO `disputes` table (keyed on Stripe dispute id), idempotent by event id via the existing `webhook_events` dedupe; `.closed(won)` unfreezes + resumes, `.closed(lost)` stays frozen. (4) **Digest** gains canceled/terminated/disputed counts + total annual-domain liability. New `release()` on `DomainPort`/`MailboxPort` (sandbox executes, real stubs throw `NotActivatedError`). Tests **111/111** (up from 102, 4 new files: `lifecycle-cancel`/`admin-terminate`/`dispute`/`lifecycle-digest`), incl. the required cross-tenant teardown-isolation (A terminate leaves B's infra untouched), chargeback idempotency, and re-cancel idempotency. Revert-fail-restore PROVEN (cp-backed) on 2: chargeback idempotency (disable the `webhook_events` dedupe early-return → redelivered dispute returns applied:true not duplicate:true → fail → restore→pass) and teardown reclaim (disable the release UPDATEs → A's `liveDomains` 2≠0 ground-truth assertion fails → restore→pass; cross-DO isolation itself is architectural — a DO cannot reach another tenant's SqlStorage). `npm install`/`npm run typecheck` (3 workspaces): clean. `npx wrangler deploy --dry-run`: clean (same 4 bindings, no new resources). **Deferred to ACTIVATION.md:** real registrar/mailbox release HTTP calls (sandbox ports execute the release now) + real Stripe dispute→tenant routing (test payloads carry `metadata.tenantId`; the live charge→payment_intent→subscription-metadata resolution is the activation step). Left uncommitted per shared-worktree git discipline.
- 2026-07-09: **D5 lifecycle SHIPPED LIVE** (v a59ed5bb, 111/111). Cancel+teardown (release infra + annual-domain liability + idempotent), admin terminate (suspend+teardown+retain suppressions+lock D1 token — caught cross-store enforcement gap), chargeback freeze (idempotent + stops sends). migration 0003 applied. Owner digest +lifecycle. Commit 47e3382.
- 2026-07-09: **Business now runs itself end-to-end in test mode.** Live surfaces: platform pipe + deliverability AI loop + billing/quotas + agent surface (MCP/CLI/demo) + support triage + dunning + owner digest + lifecycle/abuse/chargeback + distribution machine (repo/site/AEO/5 guides) + legal. 111 tests. Next: final adversarial panel #3, then FINAL-REPORT close.
- 2026-07-09: **Panel #3 platform fixes built + verified (NOT deployed — owner's call).** Remediated all money/security/correctness findings (`docs/adversarial/panel-03/`) as a coherent billing-state machine, not one-offs — new `engine/billing-state.ts` centralizes the FROZEN set (`disputed`/`canceling`/`canceled`) + `isLifecycleFrozen(status,billing_state)` shared by the tick, deliverability sweep, and setup/launch guards. Fixes: **#1** `/webhooks/stripe` now FAILS CLOSED with no `STRIPE_WEBHOOK_SECRET` (503) instead of trusting an unsigned event's `metadata.tenantId` (was an anonymous free-plan-upgrade + freeze-any-tenant vector); the test suite gained a configured test secret + signed fixtures (`postWebhook`). **#2** `disputed`/`canceled`/`canceling` are STICKY — `checkout.session.completed`/`subscription.updated(active)`/`invoice.payment_failed`/simulated-checkout guard their UPDATEs so a routine event can't lift a chargeback freeze; only a won dispute clears `disputed`, only an explicit re-checkout clears `canceled`. **#3** `runDeliverabilitySweep` gained the freeze guard (protects the standalone RPC + cron lane, not just the tick — a frozen tenant's burning domain no longer buys a REPLACE_DOMAIN). **#4** quota counts only LIVE resources (`domains status='active'`, `mailboxes released_at IS NULL`) so a re-subscriber isn't locked out; `clearTeardownRecord` on re-checkout re-arms teardown so a second cancel releases the NEW infra + books its liability (was a permanent tombstone leaking vendor spend). **#5** `canceled`/`canceling` added to the tick freeze AND `setup_infrastructure`/`launch_campaign` reject when lifecycle-frozen (a canceled tenant can't re-provision/relaunch/send). **#6** billing-recovery (`subscription.updated(active)`/`checkout.completed`/dispute won) un-suspends a DUNNING suspension via a new `suspend_reason` column, never an abuse TERMINATE. **#7** end-of-period cancel DEFERS teardown (infra stays live through the paid period; reclaim-at-boundary is a DO-alarm/cron reaper = B2/activation, noted); immediate teardown only for `immediate:true`. **#8** Content-Length body cap on `/webhooks/stripe` + `/cancel` (413). **#9** waitlist emails moved from TTL'd KV to a durable D1 `waitlist` table (migration **0004**, no expiry) + owner visibility (`buildOpsDigest.waitlist.count` + `GET /admin/ops/waitlist`). **#10** `startCheckout` reuses a pending session per (tenant,plan) instead of unbounded INSERTs. **#11** MCP `tools/list` uses `z.toJSONSchema(..., { io: "input" })` so `.default()` fields (company/timezone/sendWindow/stopOnReply) are optional, matching runtime. New `tenant_profile.suspend_reason` column (via `ensureColumnMigrations`) + D1 migration 0004. Tests: **130/130 green** (`npm test`, 30 files, up from 111) — new `webhook-security`/`lifecycle-freeze`/`lifecycle-reactivation`.test.ts + finding tests folded into `checkout`/`mcp`/`waitlist`/`lifecycle-cancel`/`lifecycle-digest`. **Revert-fail-restore PROVEN (cp-backed)** on the LIVE-PROVEN findings: #1 (old→200 not 503), #2 (old→freeze lifted to active/past_due, ×3), #3 (old→REPLACE_DOMAIN emitted), #4a (old→re-provision 400) + #4b (old→2nd cancel alreadyCanceled=true, infra leaks), #5 (old→setup 202 not 400). `npm run typecheck` (3 workspaces): clean. `npx wrangler deploy --dry-run`: clean (same 4 bindings). **Deferred (noted, not built):** the end-of-period teardown reaper + the tick send→bill saga reaper are both B2/activation (money-billing.json LOW finding #6). Left uncommitted per shared-worktree git discipline — orchestrator's call.
- 2026-07-12: **Warmforge-bundle desk-verification RESOLVED: BUNDLE LIMITED** (`docs/research/warmforge-bundle-verification-2026-07-12.md`) — ToS Art. 3.5 reserves a 99-connected-accounts/workspace warmup cap and the Salesforge Whitelabel FAQ explicitly excludes Warmforge from the reseller option. Pricing basis for the Mailforge path switches to the **ramp-only warmup model**: warmup fleet sized to onboarding rate (front-loaded ~3-4 wk/mailbox), slots rotated → ~$1-1.50/active-mbx amortized → **all-in ~$4.50-5.00/mbx, retail $13-15 clears ~2.6-3.3×** — the margin model survives without the bundle. Optional owner upside: drafted 3-question support inquiry (warm-only connections) in the frozen record. ACTIVATION Gate-1 item marked resolved. **Pricing sign-off is now UNBLOCKED** — sign off against ramp-only COGS + `docs/research/pricing-landscape-2026-07-12.md`.
- 2026-07-12: **A5 engine spike complete** — validated the `EmailPort` contract (`packages/shared/src/vendor-ports.ts`) against GreenMail 2.1.3 (real SMTP+IMAP in Docker, $0); all 5 contract behaviors (send / reply-detect / threading / bounce-classify / list-unsubscribe round-trip) proven satisfiable against a real server. Spike code + README at `spikes/a5-engine-imap/` (isolated package, outside the root npm workspace). Root suite stayed 130/130. Surfaced four findings that must resolve BEFORE the `VendorPort` freeze — recorded here as a checklist so the freeze can't happen without closing them:
  - [ ] F1 (blocks B4/RFC 8058): `SendEmailInput` (`packages/shared/src/vendor-ports.ts:71-78`) has no header field — cannot express `List-Unsubscribe`/`List-Unsubscribe-Post` through the port. Add `listUnsubscribe`/`listUnsubscribePost` (or generic headers) before freeze.
  - [ ] F2 (correctness): `PolledBounce` (`vendor-ports.ts:95-103`) has no hard/soft flag, and `processBounce` (`apps/platform/src/engine/reply-processor.ts:45-67`) permanently suppresses on EVERY bounce — a real soft 4.x.x bounce would permanently kill a deliverable address; the sandbox only emits hard-shaped bounces so this is invisible today. Needs a `permanent: boolean` field + engine suppress-only-on-hard.
  - [ ] F3 (adapter contract to document): real IMAP has no `threadId` — the adapter must reconstruct it from `In-Reply-To`/`References`, which requires outbound sends to set a real RFC 5322 `Message-ID` and store that SAME value in `scheduled_sends.message_id` (`tick.ts:181` currently stores the sandbox's opaque `msg_<uuid>`).
  - [ ] F4 (correctness): IMAP polling is at-least-once, but the `events` table (`apps/platform/src/schema.ts:114-122`) has no dedupe key on `message_id` — a re-poll after an adapter crash double-inserts reply/bounce/complaint events, inflating metrics/inbox. Needs event dedupe on inbound `Message-ID` before freeze.
- 2026-07-12 (decisions round, cont.): **Pricing sign-off DECIDED — tiers unchanged, deferred-paywall onboarding model adopted.** Founder decision (verbal, in-session): Launch $99 / Growth $299 / Scale $799 (quotas `packages/shared/src/pricing.ts`) signed off unchanged on the ramp-only COGS basis (~$4.50–5.00/mbx all-in, clears ~2.6–3.3× — `docs/research/warmforge-bundle-verification-2026-07-12.md`), decided TOGETHER WITH the deferred-paywall onboarding model: the free no-card sandbox setup stage is the marketed onboarding path (not a "demo"), payment gate sits at the go-live `/checkout` moment only — live-verified against the deployed API 2026-07-12 (no-card signup → setup on demo plan → checkout simulate → same tenant flips to launch with pre-payment state intact; junk tenant `ten_d1707c7c-69f3-4704-8b88-8b8f134981b0`). Recorded in `ACTIVATION.md` Gate 0. Three build items queued onto the activation-lane backlog (not started): (a) demo→paid "graduation" pass — on upgrade, re-provision the tenant's declared infrastructure through the real adapters (campaign/lead state already carries via the TenantDO); (b) checkout-response value framing — machine-readable what's-included in the `/checkout` response + pricing surfaces, since the customer's agent is the messenger that relays price to the human owner at the card moment; (c) site copy reframe from "demo" to "start free — pay when you flip live".
- 2026-07-12: **Engine-correctness increment GREEN-LIT** by founder ("fix whatever needs fixing — do it right") — the A5 spike findings F1/F2/F4 above (List-Unsubscribe header, hard/soft bounce flag, inbound event dedupe) plus F3 (real-IMAP `threadId` reconstruction, landing as an adapter contract doc, not a code change) move from backlog checklist to active build. Per the Bug Response protocol, class-sweeps are dispatched to inventory sibling defects in each finding's class BEFORE the fix is written; the fix itself lands as its own session-log entry when done.

  Also built this session in `site/` (uncommitted, under fresh-context adversarial review, deploy founder-gated): SEO/AEO backlog items 1 (incumbent comparison pages), 2 (tool-coverage matrix), 3 (assembly-question guide), 4 (educational follow-ups + domain calculator), 6 (vendor-blog-style content retargeting) — per `docs/research/aeo-backlog-2026-07-12.md`. Item 5 (maintenance signals) deliberately excluded from this build — orchestrator-owned.
- 2026-07-12 (close): **Brand word DECIDED — coldrig.** Founder decision (verbal, in-session): brand word = **coldrig**; founder registered `coldrig.dev` on Cloudflare the same day. Structure unchanged from the shipped design — distinct display brand Coldrig + the permanent `agent-cold-email` keyword slug on every discovery surface, never collapsed (`docs/adversarial/name-review-2026-07-12.md`). Recorded in `ACTIVATION.md` Gate 0, now fully closed (all three items decided). Public display-brand rollout held pending attorney trademark clearance on "coldrig"; domain + infrastructure wiring may proceed now. Follow-up items this decision unlocks: (a) Gate 3 custom-domain work can now target `coldrig.dev` — Cloudflare Pages custom domain, branded API/MCP host, `server-card.json` + OpenAPI `servers` update, sitemap/canonical-URL pass on `site/` (absolute URLs must move to the canonical host), 301 continuity from `agent-cold-email.pages.dev`; (b) display-brand token swap (site `brand.js` `{{BRAND}}` mechanism, `site/assets/brand.js`) is queued but GATED on attorney TM clearance; (c) sequencing recommendation: wire `coldrig.dev` as the canonical host BEFORE deploying the pending AEO content, so search equity accrues to the permanent domain from day one.
- 2026-07-12 (distribution research): **Zero-founder-touch traffic-channel scout COMPLETE** — frozen at `docs/research/traffic-channels-2026-07-12.md` (17 channels researched + inventory + exclusions). Top-10 ranked by impact×automatability; the 3 highest-leverage moves: (1) the **registry cascade** — one founder npm-auth session (Gate 3, ~10 min) unlocks npm → official MCP Registry → GitHub MCP Registry → Smithery/Glama/PulseMCP/mcp.so auto-propagation → Cline/Cursor submissions → awesome-mcp-servers PR (the 8/8 "cold email MCP server" query surface, where the incumbent artifact is an archived third-party repo); (2) a **Claude Code plugin** (skill + MCP bundle, self-hosted marketplace.json — no gatekeeper) = in-harness discovery before the agent ever searches; (3) **deploy the AEO site on coldrig.dev domain-first** — llms.txt directories, the standalone calculator URL, syndication canonicals, and directory backlinks all hang off the permanent domain. Ethics exclusions recorded in the frozen doc (no astroturf/fake reviews/PBNs/undisclosed placements; brand never spams — dogfood tier-3 rejected). Standing-authorization ask queued for founder: disclosed brand-account external actions (PRs, listings, posts) without per-action sign-off.
- 2026-07-12 (ship): **AEO content SHIPPED + LIVE.** Commit `3477e7f` on main, pushed to GitHub (verified `HEAD`==`origin/main`; engine-correctness increment deliberately excluded — its round-2 fix + adversarial gate still in flight). Site deployed to Cloudflare Pages — prod https://agent-cold-email.pages.dev, Production deployment `96a55be8` (source `3477e7f`), verified via `wrangler pages deployment list`. Live-verified: all 4 new pages return HTTP 200 via extensionless routing (compare-vs-smartlead-instantly, guide-mcp-tool-count, guide-cold-email-with-ai-agent, guide-domains-inboxes-warmup-compliance); calculator live-driven in production (500/day → 17 mailboxes/6 domains, matches worked examples); zero console errors; desktop+mobile screenshots captured. Included the RFC 8058 honesty fix across 12 surfaces (11 HTML instances per `docs/adversarial/aeo-content-review-2026-07-12.md` ROUND 2 + the `site/.well-known/mcp/server-card.json` guardrails line — confirmed via `git show 3477e7f` diff: server-card.json's overclaim "suppression list + RFC 8058 one-click unsubscribe" rewritten to the honest form; restore-item recorded in the same ROUND-2 record). Weekly citation-panel cloud routine ARMED — `coldrig-aeo-citation-panel-weekly` (`trig_01X119P6FiTgxMkamZr2tU29`), Mondays 06:00 UTC, first run 2026-07-13, reports the TREND row + per-query JSON (**UNVERIFIED**: external cloud scheduler, not checkable from this repo); the orchestrating session appends rows to `tools/aeo-panel/runs/TREND.md` (baseline row confirmed present: 46/46 queries live, 0% mention/cite/clickable, pre-publication).
  - [ ] **2026-07-26 — POST-SHIP AUDIT (founder-requested 07-12)**: (a) citation-panel trend review (2 weekly runs in — `tools/aeo-panel/runs/TREND.md`); (b) Cloudflare crawler analytics (AI-bot fetches) + human traffic review; (c) GSC/Bing indexation status; (d) design-pass QA — human-visitor polish holding the extraction structure; (e) coldrig.dev wiring + zone AI-bot-block settings verification; (f) deferred-paywall funnel numbers (signups → setup-completed → checkout); (g) engine defect-class guards re-check (soft-bounce streak + idempotency ledger holding).

  Remaining/queued from this ship: domain wiring = OWNER-PENDING (the CNAME + Pages custom-domain attach was permission-gated in-session; one command or dashboard click, then the host-swap redeploy — ~20 absolute pages.dev URLs already inventoried); design polish pass = queued next session; CF Web Analytics enable + GSC/Bing verification = owner (~5 min); npm auth session = owner (~10 min, unlocks the registry cascade per `docs/research/traffic-channels-2026-07-12.md`); standing brand-account authorization (disclosed PRs/listings/posts) = open founder question.
- 2026-07-12 (close): **Engine correctness increment SHIPPED — commit `d342cd0` pushed.** Both defect classes closed whole (severity/temporality-blind handling; non-idempotent at-least-once processing) via the full Bug Response loop: A5 spike → 2 class-sweeps → build (20 scope items) → verifier COMPLETE (revert-fail-restore proven on both anchors) → adversarial re-attack **FAIL** (round-1 blocker: soft-bounce streak reset by its own triggering send — threshold unreachable; fixture-unreal test hid it) → round-2 fix (streak resets ONLY on reply; +6 NB items) → re-attack **CLEAN**. Suite 130→148, typecheck clean, dry-run clean. Orchestrator rulings recorded: NB3 scope-grow to `idx_ledger_source_send` approved (same class); NB5 NULL-tenant dedupe nuance accepted (revisit on activation ops noise); stale-'pending' claim reclaim = new ACTIVATION Gate-2 item. Fold-ins post-CLEAN (mirror-of-attacked-pattern, suite re-verified 148/148): `sent_message_keys` 30d eviction + corrected idempotency liveness docstring. Standing lesson (hard-builder memory): async-tally-reset-on-triggering-action — a tally incremented by an ASYNC signal must never be reset by the action that triggers the signal; tests must model the real over-time flow, not single-tick stacked fixtures. Known suite flake: `npm test` wrapper intermittently fails under load while direct `npx vitest run` is stable green (3rd occurrence today) — investigate wrapper/pipe interaction someday.
- 2026-07-12: **Dashboard + Unified Inbox lane spec'd (SPEC §19), founder-directed** — a net-new build lane absorbing B7's UI half (Phase B, ROADMAP B7/B7.1). Research fan-out grounded the design: 3 workers (unibox UX, OSS email UI, agent-controlled/generative UI) + a repo map; priorart frozen at `~/.claude/priorart-archive/agent-controlled-dashboard-unified-inbox-coldrig-2026-07-12.md`. Spec drafted (agent-layout-control via MCP/API parity law; httpOnly opaque-cookie session auth + global CSRF middleware; one sanitization pipeline for email HTML and agent-authored content; inbox v2 cursor-paginated single-JOIN API killing the N+1; views/layout CRUD with rev-CAS + default-view lifecycle; dashboard SPA + 3-pane keyboard-first inbox UX) then adversarially reviewed twice (repo grounded @ `e1d1f94`): round 1 **NO-SHIP** (10 findings, 2 blocking — unspecified agent-content sanitization risking stored XSS on the credential origin, and an unbuildable inbox UX on the existing unpaginated N+1 `/inbox`); round 2 all 8 fix-list items closed → **SHIP** with 2 build-gating conditions (global `X-Coldstart-Client` CSRF header on every cookie-authed mutation incl. `/cancel`; composite `(lastEventTs, rowid)` same-ts cursor tiebreak) + 3 notes — folded into the shipped spec as SPEC §19.0–19.8. Record: `docs/adversarial/dashboard-spec-review-2026-07-12.md`. M1 backend build dispatched same day (serving spike → schema → inbox v2 → views CRUD → cookie session → MCP tools 13–15, per §19.8 build order); M2–M5 (SPA shell, inbox UX, agent-control e2e, perfection loop) queued behind M1, gated on a UI/UX screenshot loop + a fresh adversarial re-attack + verification battery before any commit.

  **M1 backend DONE 2026-07-12.** 190/190 tests (148→190, +42). Serving spike proven live via `wrangler dev`: unauthed API paths 401 JSON, unknown paths 404 JSON, `/app/*` deep links 200 (SPA fallback), bare `/app` 307-redirects to `/app/`. Delivered per the build order above: TenantDO schema bootstrap + `last_polled_at`, shared zod layout schema/registry, inbox v2 (composite `(lastEventTs, rowid)` cursor, single-JOIN query, dedicated no-N+1 test), listCampaigns/activity, views CRUD + rev-CAS, D1 `0006_dashboard_sessions` cookie session + global CSRF middleware, MCP tools 13–15 (`get_dashboard`/`configure_dashboard`/`label_thread`). Two notable implementation decisions: (a) the authed-route mount switched from a blanket `authed.use("*")` to an explicit `AUTHED_PATH_PATTERNS` array (`apps/platform/src/index.ts:77-93`) after live-proving a real, previously-undetected gap — every path, including unknown ones, 401'd before this fix instead of 404ing; any future authed route MUST register its path pattern in that array (documented in `src/routes/README.md`); (b) `apps/platform/public/index.html` is a deliberate duplicate of `public/app/index.html` — Cloudflare's `not_found_handling = "single-page-application"` was empirically proven (via `wrangler dev`, not assumed from docs) to serve the assets directory's ROOT `index.html` for any unmatched `/app/*` path rather than a per-directory walk-up, so both copies must exist; keeping them in sync is M2's build-step responsibility once the Vite build starts populating `app/` (documented in `public/README.md`). M2 dashboard SPA shell dispatched to design-builder same day. M1 remains uncommitted in the working tree by design — commit is gated on arc-end verification + a fresh adversarial re-attack.
- 2026-07-13: **M2-M5 built, gates clean, whole lane COMMITTED — `c9e5e27` on `feat/dashboard-inbox-spec-19`.** M2 (SPA shell: token-gate auth with no JS-readable token, desktop nav rail / mobile bottom tabs, 12-col grid renderer + 8-widget registry, view management, Settings) → M3 (virtualized keyboard-first inbox: j/k nav, Cmd+K palette, thread detail + reply composer, mobile swipe + 5s undo) → M4 (agent-control e2e proof, server-side archived filter on inbox v2 + MCP, saved-view rename, dark mode, a11y pass) → M5 two-round perfection loop (R1 closed defects A-I; R2 closed 7 more — mailbox-health failsafe banner, grid dead-gap fix for content-fit widgets via `measuredRowSpan` row-unit collapsing, default seeded view redesign, widget overflow row-clip/fade, `ProvenanceBadge` mobile truncation, keyboard archive-undo parity, mailbox-chip long-local-part truncation) followed by a full final 1440/390 screenshot matrix.
  Both gates run fresh-context, both clean: **security re-attack → SHIP** (CSRF coverage on every cookie-authed mutation, opaque httpOnly cookie session, sanitization sinks confirmed confined to `AgentNote.tsx`/`EmailHtmlFrame.tsx` via `check:dangerous-html`, tenant isolation, MCP cookie-rejection all held); **UX review → SHIP-AFTER-FIXES**, all of which landed in M5 R2 above. Verification battery this session (re-run and confirmed, not just trusted from the commit message): `npm test` root workspace **233/233 passed (47 files)**, `apps/dashboard` **93/93 passed (21 files)**, `npm run typecheck` clean across all 4 workspaces, `npm run build` clean (Vite build + `wrangler deploy --dry-run` + CLI `tsc`), `npm run check:dangerous-html` clean. Live-driven this session: cookie-session auth flow, CSRF 403 on a missing `X-Coldstart-Client` header, MCP `configure_dashboard` stale-`rev` 409 conflict, and the serving criteria from M1's spike all re-confirmed against the built app. Commit `c9e5e27` (105 files) excludes unrelated in-flight `.claude/agent-memory/` files; `pw-shots/` screenshots are gitignored, not in the commit.
  **State at commit time: BUILT + COMMITTED, NOT merged to main, NOT pushed, NOT deployed** — founder-gated per SPEC §0 (test-mode/no-real-spend discipline extends to not shipping a net-new authed surface without a founder look). **Superseded same day — see the 2026-07-13 (deploy) entry below: merged to main and deployed live.**
  Known non-blocking follow-ups for a future session (from this session's gates, not new backlog docs): (1) no MCP `list_campaigns`/`activity` tools yet — verified via `apps/platform/src/mcp/tools.ts`, those two remain HTTP-only; (2) `site/openapi.yaml` still documents 13 operations (verified via `operationId` count) — stale against the 3 new dashboard MCP tools (`get_dashboard`/`configure_dashboard`/`label_thread`) and the new HTTP routes; (3) the content-fit grid gap (`apps/dashboard/src/dashboard/Grid.tsx`) collapses close to but not exactly 0 — `measuredRowSpan` quantizes to whole `ROW_HEIGHT_PX + ROW_GAP_PX` (76+16=92px) row units by design, so a residual gap up to just under one row unit is expected, not a bug; the specific "~82px" figure from this session's live UX pass is **UNVERIFIED** by this bookkeeping pass (not independently re-measured); (4) the mobile thread-detail overlay covers the failsafe banner by design (single-pane mobile UX tradeoff, not yet reconciled); (5) `parseJsonBody`'s Content-Length pre-check (`apps/platform/src/validate.ts:41`) is skippable when the header is absent/non-numeric (`Number.isFinite(NaN)` is false) — verified in the code; pre-existing platform-wide, not dashboard-specific, and bounded downstream by each route's zod schema caps; (6) login-CSRF on `/dashboard/session` is low-severity (an attacker can only plant their OWN session token, not hijack another's).
- 2026-07-13 (deploy): **Dashboard + Unified Inbox lane (SPEC §19) MERGED + DEPLOYED LIVE.** Merged `feat/dashboard-inbox-spec-19` → `main` as `77741ae` ("Merge dashboard + unified inbox (SPEC §19) into main", merging `e1d1f94` + `f3f4187` — confirmed via `git show --stat`). Deployed via `npm run deploy` (`apps/platform`): D1 remote migrations `0005_support_dedupe` + `0006_dashboard_sessions` both applied (confirmed via `wrangler d1 migrations list coldstart-platform-db --remote` → "No migrations to apply!"), then `wrangler deploy` → Worker version `e9734c42-984a-47f3-805a-3c261db4d06c` (confirmed as the newest entry in `wrangler deployments list`, created 2026-07-13T11:29:20Z). Dashboard is now LIVE at https://agent-cold-email-api.yaakovscher.workers.dev/app, served same-origin by the Worker's `[assets]` config (`wrangler.toml`: `directory = "public"`, `not_found_handling = "single-page-application"`) — no separate deploy target. Live-verified this session: `GET /app/inbox` → 200 `text/html`, title `coldrig dashboard`; `GET /app` → 307 (redirects to `/app/`); `GET /inbox` with no auth → 401 JSON `{"error":"missing bearer token","code":"invalid_token"}` (API auth intact); `GET /status` → 200 `{"status":"ok"}`. The full signup → `POST /dashboard/session` → `Set-Cookie: cs_dashboard_session` (opaque, httpOnly/Secure/SameSite=Strict, distinct from the bearer token) → `GET /dashboard/views` (seeded default view present) path was run and confirmed by the deploying session; not independently re-run in this bookkeeping pass to avoid creating a duplicate prod signup. **Still TEST-MODE**: no real vendor spend or real sending — SPEC §0 unchanged; this deploy only ships the dashboard onto the already-live test-mode API. **Open item at this entry's writing: `main` NOT pushed** (`origin/main` at `e1d1f94`, 3 behind) — **resolved same day, see the 2026-07-13 (push) entry below.**
- 2026-07-13 (push): **`main` PUSHED to GitHub — B7.1 fully closed.** Founder-directed analysis-then-push: merge fidelity verified (`git diff feat/dashboard-inbox-spec-19 77741ae` empty — merge content == the twice-gated branch tip), push set = exactly `c9e5e27`/`f3f4187`/`77741ae`/`71bcd81` (174 files, no secret-shaped paths), and a live prod agent-control round-trip proven pre-push (MCP `get_dashboard` + `configure_dashboard` on the deploy-smoke tenant → view rev 1→2, `editedBy: mcp`, note carried). Pushed `e1d1f94..71bcd81`; `origin/main` == local == `71bcd81` ("record dashboard deploy" docs commit); GitHub CI run `29246972278` = success on `71bcd81`. Handoff-session doc folds (uncommitted at this entry's writing, committed by the /handoff commit): README refresh (12→15 tool count sweep, dashboard tool rows + optional-dashboard line, test-mode status wording), MEMORY.md +3 build lessons (vitest-pool `fileParallelism:false` flake class · render-at-send template class · assets root-`index.html` duplicate), this push entry + the B7.1 status-line correction. Note the M2-M5 entry above bundles one NON-dashboard core fix: the send path previously never substituted `{{var}}` templates — `engine/template.ts` (new in `c9e5e27`) renders at send time and stores rendered values in the `sent` event; inbox v2 prefers that rendered subject (lesson in `MEMORY.md`).

## Drained 2026-07-14 (activation day)
- [x] 2026-07-13 Domain wiring DONE 2026-07-14: Yaakov attached coldrig.dev as the Cloudflare Pages custom domain; host-swap redeploy shipped (canonical/OG/sitemap/robots/server-card → coldrig.dev, commit `355926c`) + CAN-SPAM mailing address filled in terms/privacy. Verified live: `curl https://coldrig.dev/` returns canonical `https://coldrig.dev/` (HTTP 200); IndexNow key file live at the new host; Wayback snapshot captured same day.
- [x] 2026-07-13 npm auth session DONE 2026-07-14 (Yaakov, 2FA-gated, his hands): `agent-cold-email@0.1.0` published to npm — verified `npm view agent-cold-email version` → `0.1.0` and `npx agent-cold-email --help` works. Also unblocked the official MCP Registry publish (see Open, below); GitHub MCP Registry / Smithery / Glama / PulseMCP / mcp.so / Cline / Cursor / awesome-mcp-servers PR now running as a registry/directory cascade — see cascade item below.
- [x] 2026-07-13 Standing brand-account authorization GRANTED 2026-07-14 (Yaakov, in-session, verbatim: "standing approval for disclosed registry/directory listings") — disclosed registry/directory listings (MCP registries, mcp.so, Cline issue, awesome-list PRs, llmstxt directories, Cursor directory) proceed without per-action approval; posts/content NOT covered (separate ask).
- [x] 2026-07-13 (evening) MCP parity: `list_campaigns`+`activity` tools added (15→17 tools, SPEC §19.0 parity, byte-identical to existing HTTP routes) + `site/openapi.yaml` 13→24 `operationId`s — commit `b8d57e4`, 6 new tests, platform suite green (239/239), typecheck clean.
- [x] 2026-07-13 (evening) Discoverability wave: OG+Twitter cards on all 16 site pages + `site/assets/og-image.png` (1200×630, verified on disk) + JSON-LD on docs/pricing/compare + GitHub repo `homepageUrl` set to the live Pages URL (verified via `gh repo view`) — commit `b8d57e4`. NOTE: gh repo **description** still reads "~12 MCP/CLI tools" — not yet updated, tracked below.
- [x] 2026-07-13 (evening) Tool-count staleness swept as a CLASS: `site/llms.txt` + `site/.well-known/mcp/server-card.json` were stale at "~12 tools"; every 12/15-tool reference across `site/`, `AGENTS.md`, READMEs re-grepped post-fix — zero survivors confirmed. Fresh-context adversary verdict **SHIP** on the full diff, with one gate carried forward: DEPLOY ORDERING — Worker must deploy before/with the site (else `server-card.json` advertises tools the live Worker lacks) + verify live `tools/list` count == 17 pre-deploy (see push/deploy item below).
- [x] 2026-07-13 (evening) Deep research frozen: `docs/research/traffic-channels-selfserve-2026-07-13.md` — 13 ranked zero-founder-touch channels (Tier-1 verified: IndexNow, GitHub code-search metadata, grep.app on-demand indexing, DeepWiki mirrors; 5 refuted-dropped: deno.land/x, Common Crawl submission, GitHub Packages npm, JSR zero-touch, Postman); extends the 07-12 channel-scout doc — commit `1e84672`.
- [x] 2026-07-13 (evening) IndexNow built fire-ready: key file live under `site/` + `tools/indexnow/submit.sh` (pre-deploy abort guard verified — aborts if key file isn't live at the target host; 16-URL sitemap payload validated) — commit `fd13aa0`. Inert until the site actually deploys.
- [x] 2026-07-13 push main + deploy Worker THEN site — DONE: `main` is even with `origin/main` (verified `git status -sb`), coldrig.dev host-swap live (see Domain wiring, `## Now`), Worker `/mcp` responding live at `agent-cold-email-api.yaakovscher.workers.dev`.
- [x] 2026-07-13 post-deploy checklist DONE: IndexNow key file verified live at `https://coldrig.dev/a23dd986c5474f292aeddebefead63ee.txt` (HTTP 200, so `tools/indexnow/submit.sh`'s pre-deploy abort guard would pass); Wayback snapshot captured (`web.archive.org/web/20260714.../coldrig.dev/`); Worker `/mcp` responds live; `gh repo view` confirms the description now leads "Cold email MCP server" and cites "17 MCP tools" (no longer stale at ~12) — see also the discovery reality-check entry below.
- [x] 2026-07-13 C6 (remaining half) — Distribution validation harness now baselined two ways: (1) Mordy stranger-agent funnel drive PASSED end-to-end (see 2026-07-14 entry below); (2) a real unprompted buyer-simulation found **zero appearances** today (`docs/research/agent-buyer-research-forensics-2026-07-14.md`) — discoverability work continues (registry cascade, dogfood campaign) but the harness itself is proven. (The waitlist/early-access flow itself — `POST /api/waitlist`, KV-backed dedupe/rate-limit/CORS — is done.)
- [x] 2026-07-14 [ASK] Competitive check vs outreach-gigs gig layer: Mordy decided Instantly-over-Smartlead 07-13 (human-ops DFY gig, authorpitchdesk.com, 2-3 mbx, ~$60-80/mo, live ~07-28). VERDICT: same category, no conflict today (coldrig test-mode-locked + gig is human-UI-operated); post-activation the gig is the natural FIRST-TENANT/dogfood-migration candidate, not a rival — evidence in outreach-gigs `gig-layer/sending-infra-setup.md`.
- [x] 2026-07-14 [ORDER] Mordy live-discovery test: stranger-agent funnel drive PASSED end-to-end. 3 frictions found+fixed: MCP field-path validation errors now name the failing field (dot-path per zod issue, commit `ead131b`), `AGENTS.md` type hints, `site/openapi.yaml` IdempotencyKey header + campaignId divergence note + server-card `/docs` fix (commit `355926c`). Companion discovery reality-check (separate real buyer-simulation, unprompted by us): ColdRig scored **zero appearances** in an agent's actual vendor search today; Glama auto-listing ranks #1 for "agent native cold email"; `gh repo` description re-tuned to lead "Cold email MCP server" (verified live via `gh repo view`). Frozen record: `docs/research/agent-buyer-research-forensics-2026-07-14.md`.
- [x] 2026-07-14 [ORDER] npm publish LIVE — Gate 3 headline distribution surface closed: `agent-cold-email@0.1.0` published (Yaakov, 2FA-gated, his hands). Verified: `npm view agent-cold-email version` → `0.1.0`; `npx agent-cold-email --help` runs.
- [x] 2026-07-14 [ORDER] Official MCP Registry LIVE: `io.github.YS-projectcalc/agent-cold-email` v0.1.0 published via `mcp-publisher` (GitHub device-flow, Yaakov authorized) — `server.json` committed (`aee3679`). Verified live via `registry.modelcontextprotocol.io/v0/servers?search=agent-cold-email`: status `active`, `isLatest: true`. Downstream registries (GitHub MCP Registry, Glama, PulseMCP) crawl from here.

## Drained 2026-07-16 (handoff)
- [x] 2026-07-13 [ASK] Design lane — **MERGED TO MAIN 2026-07-15** (`d68be4f`+fix `a64f837`, ff to `db80c00`): 105 files — full human journey (signup/connect/security/status/support/replies/byo-domain/unsub-preview/404/for-agents/agent-evaluation), dashboard Billing/Setup/Signup/Recovery + rebuilt bundles, ratified $99-first pricing on every surface, compare-vs-salesforge. Adversary NO-SHIP→SHIP over 2 rounds (sole blocker: status page linked nonexistent /health → fixed+live-verified; frozen record docs/adversarial/design-integration-review-2026-07-15.md). All compliance classes still zero-survivors; suites 319+99 green. **NOW DEPLOYED LIVE 2026-07-15** (founder-authorized; Worker `4836515a`→`b21ca47d`, site `--branch main`): live-verified `coldrig.dev/pricing` title "$99 for 5 Mailboxes, Then $10 Each" + JSON-LD `lowPrice":"99"`, `/status` 200, MCP `tools/list`==17 (unauthenticated), `/unsubscribe?token=garbage` → 400. Follow-ups ledgered: support-kb stale tiers (still open), waitlist-CORS origin check — CLOSED same-day in the watchtower commit (`2795a64`, live-verified: `coldrig.dev` origin echoes ACAO, disallowed origin gets none), aup §5 health-cap wording → attorney pass. Original landing note: (external LM, branch `coldrig-human-interface` @ `442383d`, base `355926c` = 11 commits behind our main): full human journey (signup/connect/security/status/support/replies/byo-domain/unsubscribe-preview/404), dashboard Billing/Setup/Signup/Recovery pages, mailbox pricing planner, 107 files ±. Designer's HANDOFF records **founder-RATIFIED provisional pricing $49+$10/mbx ($99/5 min)** — treating the PRICING RULING entry below as CONFIRMED-provisional; backend billing still legacy tiers (core work, honest UI gates). INTEGRATION IN FLIGHT: worktree merge (policy: our ledger/legal/compliance content wins; their design wins on site surfaces; claim-classes re-swept on their pages; dashboard REBUILT from merged source, never their committed bundles) → full battery → fresh adversary → merge to main → **deployed founder-authorized 2026-07-15**.
- [x] 2026-07-13 [ORDER] post-push: seed grep.app/<org>/<repo> + deepwiki.com/<org>/<repo> on-demand indexing — CLOSED 2026-07-14 as far as actionable: DeepWiki SEEDED (HTTP 200 verified). grep.app = NOT-INDEXED (real-browser check, no bot wall, zero results for "agent-cold-email" — conclusive since package.json contains the literal string; screenshots `pw-shots/directory-runner-2026-07-14/grepapp*.png`) and grep.app exposes NO public on-demand indexing mechanism — nothing to seed; recheck folded into the 2026-07-26 POST-SHIP AUDIT (h).
- [x] 2026-07-13 [ASK] C4 — SEO/AEO comparison + how-to pages targeting agent queries — CLOSED 2026-07-14, checkbox was stale (scout-verified: 9 comparison/guide pages + faq.html + docs.html all COMMITTED across 2da6aa0/7af58b8/3477e7f/b8d57e4/355926c, all in sitemap.xml with coldrig.dev canonicals, 6 spot-checked URLs serve live 200, content substantive not stubs).
- [x] 2026-07-13 [ASK] A5 engine-spike findings — CLOSED 2026-07-14: F3's condition met (engine committed `eb8ee42` with clean re-attack #3), so all four are done. Detail from the reconcile: F1 List-Unsubscribe (`vendor-ports.ts:87-88`, cites finding directly), F2 bounce severity (`vendor-ports.ts:131`), F4 message_id dedupe (`reply-processor.ts:28-45` + `idx_events_dedupe` `tenant-do.ts:137`) all CLOSED, committed at HEAD. F3 (real-IMAP threadId reconstruction) IS implemented (`apps/engine/src/classify.ts:115-139`, In-Reply-To/References chain back to minted Message-IDs) but `apps/engine/` is untracked/NO-SHIP — check F3 off only when the engine commits with a clean re-attack.
- [x] 2026-07-14 [IDEA] Fold 07-12 ramp-only warmup economics into canonical SPEC §12/§18 (tables still show stale $7/mbx Inboxkit basis; real Mailforge-path all-in ~$4.50-5/mbx) — scout-found gap. — CLOSED 2026-07-14: SPEC §12 cost basis corrected to $4.50–5.00/mbx Mailforge path (~$6.10/mbx Inboxkit alt), sourced `docs/research/warmforge-bundle-verification-2026-07-12.md:19` + `docs/research/vendor-costs-mailforge-inboxkit-2026-07-12.md:26,39`; §18 pricing table's fully-loaded-cost/margin columns recomputed on the corrected basis (also fixed a pre-existing internal inconsistency: §18's prose already said "~$6 wholesale" while its own table math still ran on stale $7); customer-facing prices untouched, pricing-model-ruling pointer added per brief.
- [x] 2026-07-14 [ORDER] Engine re-attack #2 double-send race — FIXED + RE-ATTACK #3 = **SHIP** + COMMITTED `eb8ee42` 2026-07-14: in-flight claim-before-await (`apps/engine/src/store.ts` + `engine.ts:45-76`, revert-fail-proven: defeated claim ⇒ 2 SMTP sends), SMTP inactivity timeouts 20/20/60s < 180s fetch abort < 300s TTL, attempts ceiling on reclaim, 422/409 retryable. Adversary independently re-ran suites (engine 30, platform 263, exit 0) and ruled both residuals ledger-able (carried in ACTIVATION Gate 2: crash-window MUST resolve-or-accept before arm; multi-instance N/A single-daemon). Verdict frozen: `docs/adversarial/engine-host-review-2026-07-14.md` Re-attack #3. Original finding (for the record): DOUBLE-SEND race. `SEND_CLAIM_TTL_MS=5min` (tick.ts:43) is HALF nodemailer's default `SOCKET_TIMEOUT=10min`, `smtp.ts` sets zero timeout overrides, `email-port.ts` has no AbortSignal, and engine `send()` (engine.ts:42-57) is bare check-then-act with no in-flight claim → a stalled SMTP send is reclaimed by tick B at t>5min while tick A is STILL SENDING → the lead receives the email twice. TO SHIP: bound the in-flight send well under the TTL (nodemailer connection/greeting/socket timeouts in `smtp.ts` + AbortSignal on the Worker fetch) AND/OR give the engine send-store a claim-then-execute in-flight row mirroring `withRequestIdempotency`. Adversary CONFIRMED as fixed: consumer-owned cursor (redelivery + dedupe hold), class sweep (no other members), https guard (not bypassable), §0 safety intact. Engine stays UNCOMMITTED until a clean re-attack.
- [x] 2026-07-14 [ORDER] Engine runbook contradiction — ALL THREE FIXED in `eb8ee42` (verified by adversary R3): ACTIVATION step 6 mandates https-tunnel/localhost with an explicit never-plain-http warning (was already committed pre-session); 422 UnknownMailbox regraded retryable/operator-fixable; TTL reclaim now bumps `attempts` with a MAX_SEND_ATTEMPTS ceiling + ops-visible 'failed' event. Original finding (for the record): `ACTIVATION.md:42` step 6 tells the operator `wrangler secret put ENGINE_BASE_URL` = `http://$IP:8080`, but the new https guard REJECTS that as a PERMANENT VendorError → under the new grading every due send is marked terminal `'failed'` on attempt 1, and `'failed'` has NO requeue path (written tick.ts:241/:289, read only by ops-summary COUNT) → an operator following the runbook verbatim burns the whole queue, recoverable only by DB surgery. Fix step 6 to set only the https tunnel/localhost URL. Same class: a 422 UnknownMailbox (operator-fixable) is graded lead-permanent → also terminal. Also: the TTL reclaim does not bump `attempts`, so an orphan-reclaim loop has no ceiling.
- [x] 2026-07-14 [ORDER] Marketing-copy overclaim — CLOSED via path (a), `ecfc5b2`: the hosted RFC 8058 one-click endpoint is REAL (public `/unsubscribe`, HMAC-tokened, GET side-effect-free), so every flat claim is now true; PLUS the adversary widened the sweep to the "verified sender identity" class and hunted it to ZERO across all canonical surfaces over 4 rounds (faq/guides/pricing/openapi/README/ARCHITECTURE — frozen verdict `docs/adversarial/b4-optout-review-2026-07-14.md`, final VERDICT: SHIP). The legal deploy batch is now UNBLOCKED — purely founder-gated.

## Drained 2026-07-17 (coldrig session)
- [x] 2026-07-17 [ORDER] **BYO arming gate CLOSED.** `fresh_standalone` BYO domains no longer route through `REPLACE_DOMAIN` (which would auto-BUY a lookalike of the customer's brand once real adapters activate — no `source='byo'` guard existed in the burn decision, `byo-intake.ts:139` + `deliverability-actions.ts:93-170`). Fix commits on branch `byo-intake`: `8dc3305` (source-keyed `HARD_PAUSE_DOMAIN` routing — ALL `source='byo'` domains, verified single producer `evaluate()`, no alternate path to `applyReplaceDomain`) + `f46e935` (≤1-active-BYO-primary guard, closes a related "two live primaries" hazard). Focused re-attack ADDENDUM VERDICT **SHIP** at `f46e935` (`docs/adversarial/byo-intake-2026-07-17.md`, addendum section) — original finding #1 CLEARED on all four attack angles (legacy-row routing, other breakerTier call sites, windowed-stats side effects, alternate REPLACE_DOMAIN entry points), RED test proven (`deliverability-hard-pause-domain.test.ts` fails on pre-fix code). Battery at `f46e935`: typecheck exit 0, platform vitest 537/537 (bookkeeper re-verified typecheck independently). `[dark-unarmed]` still applies to the whole BYO lane (sandbox-only, `realAdaptersActivated=false` hardcoded) — this line closes the must-fix-before-arm design hazard, not the arming itself; branch unmerged, merge to main [gated:founder].

## Drained 2026-07-19 (ship wave)
- [x] 2026-07-19 [ORDER] **gmail_api wire-Message-ID bug — found, fixed, merged, deployed, re-smoked CLOSED end-to-end.** Root cause: Gmail's `users.messages.send` rewrites the MIME Message-ID header; the engine recorded only its own minted id for reply/bounce thread reconstruction, so a recipient's reply (In-Reply-To = the Gmail-rewritten wire id) never matched — reply/bounce events silently dropped for every `gmail_api` send (confirmed live by the 07-19 production smoke's independent IMAP fetch: minted `<d7f83093...@gmail.com>` delivered as `<CAMc35PQ...@mail.gmail.com>`). Fix committed `60ff693` on branch `msgid-wire-fix` (bookkeeper-verified diff: `gmail.ts` post-send wire-id read-back via `messages.get?format=metadata`, best-effort/abort-bounded, outside the correctness path; `engine.ts`/`store.ts` dual-record minted+wire ids resolving to the same threadId; `graph.ts` submits the minted id as `internetMessageId` for MS Graph, Gate-2 live-verify residual carried; `mint-gmail-token.mjs` gains the `gmail.metadata` scope, required for the read-back — existing send-only tokens 403 it and must be re-minted). Adversary **SHIP** (`docs/adversarial/msgid-wire-fix-2026-07-19.md`: RED revert-fail proven, engine 74/74 + 3 docker-gated skips, typecheck exit 0). Merged to `main` as `cd515e1` (bookkeeper-verified `git show --stat cd515e1`: merge of `60ff693` into `1a64e79`); branch `msgid-wire-fix` deleted and its worktree pruned post-merge (bookkeeper-verified: `git branch --list msgid-wire-fix` empty, `git worktree list` shows only `main`/`byo-intake`/the designer's worktree). Engine rebuilt from merged `main` (includes the Dockerfile fix `e704e4b`, bookkeeper-verified diff: `npm pkg delete devDependencies` before `npm install --omit=dev`) and redeployed to the droplet, swapped, re-smoked. **RE-SMOKE:** `POST /v1/send` via `gmail_api` → `200`, Message-ID `<CAMc35PTDteHVgqB86Q2expaHiRP2C42+Z0wAsJtTgf7KhkpQqQ@mail.gmail.com>` — a WIRE-shaped id, i.e. the read-back worked in production — and an independent IMAP `gm-raw` search found the delivered message carrying EXACTLY that id (`[Gmail]/All Mail` uid 247031, subject "coldrig msgid-fix re-smoke") — recorded==wire proven live, closing the class. As reported this session, not independently re-verified by bookkeeper (no droplet SSH / mailbox access from here). Founder minted the upgraded OAuth token (gmail.send + gmail.metadata scopes) via a session-run loopback listener; droplet `/root/mailboxes.json` updated with it (imap block kept, uid-1000/mode-400 preserved from the prior EACCES fix).
- [x] 2026-07-16 [ORDER] **SMTP wall — durable fix (Gmail-API/MS-Graph HTTPS/443 send path) FULLY CLOSED end-to-end.** Path (c) built + committed `f406132` (per-mailbox `kind: "smtp"|"gmail_api"|"ms_graph"` discriminator, shared `MailComposer` builder so compliance headers are identical across transports by construction); adversary SHIP zero-blocking (`docs/adversarial/engine-443-transports-2026-07-16.md`). **Gate-1 local smoke PASS 07-16** (real self-send over 465 from the Mac-local engine, Message-ID matched on independent IMAP fetch). **Production droplet 443 smoke PASS 07-19**: engine deployed live on the droplet (142.93.12.85), `gmail_api` send → `200`, delivery double-verified (founder inbox + independent IMAP). The one defect that smoke surfaced (gmail_api wire-Message-ID mismatch) is its own closed line above. **Worker-side `ENGINE_AUTH_SECRET` SET 2026-07-19** by founder keypress (`wrangler secret put`) — bookkeeper-verified live via `wrangler secret list` (`apps/platform`): now shows `ADMIN_TOKEN`, `ENGINE_AUTH_SECRET`, `TOKEN_HASH_PEPPER` (previously only the first and third). **The SMTP wall is routed around in production, end to end, for the durable-fix path.** NOT part of this closure, deliberately still open: Cloudflare Tunnel / `ENGINE_BASE_URL` (bookkeeper-verified still absent from the Worker secret list) and `ENGINE_TENANTS` allowlist arming — these gate full TENANT-facing traffic, tracked in `ROADMAP.md ## Now` RETURN-QUEUE.
- [x] 2026-07-15 [ASK] **Early-access → fully-live reframe — DEPLOYED LIVE 2026-07-19.** Trigger (443 smoke passing) fired 2026-07-19; `site/` copy rewritten across 43 files (commit `ea64541` "feat(site): early-access -> fully-live reframe across every public surface"), preceded by a docs-ledger commit `1a64e79`. Claim-class adversary: round 1 `SHIP-AFTER-FIXES` (3 blocking) → round-2 re-attack addendum `SHIP-AFTER-FIXES` (1 trivial blocker: `site/llms.txt` still read npm v0.1.0 against live v0.2.0) — that last line lands in `ea64541` (bookkeeper-verified `site/llms.txt:41` now reads v0.2.0), closing the gate; frozen record `docs/adversarial/fully-live-reframe-2026-07-19.md`. Deploy ordering enforced: platform Worker deployed FIRST (version `b3bae1c7-f859-4e8a-9f05-a048466f38bc`, bookkeeper-verified via `wrangler deployments list` as the newest entry, created `2026-07-19T12:26:47.952Z`, preceded by an `ENGINE_AUTH_SECRET` secret-change deployment at `12:20:34Z`), THEN the site (Cloudflare Pages deployment `ca4eecaa-3fca-4972-9aca-aa8986cb801b`, bookkeeper-verified via `wrangler pages deployment list` sourced from commit `cd515e1` — the post-merge HEAD). Bookkeeper live-verified post-deploy: `coldrig.dev/status` and the Worker `/status` both 200/`{"status":"ok"}`; zero "early access" text on `/`, `/pricing`, `/docs` (only a defensible "Early access" pill survives on `/status`'s Dashboard row, tracked as its own non-blocking `## Open` line); "concierge step" posture present across `index.html`/`faq.html`/`docs.html`/`connect.html`/every `compare-*.html`; "Gmail API (HTTPS/443)" transport naming confirmed on 5+ pages; `site/llms.txt` confirms "19 intent-level MCP/HTTP tools" + npm v0.2.0; `site/openapi.yaml` serves `/webhook-subscriptions` (GET list, POST create) + `/webhook-subscriptions/{id}` (GET/PUT/DELETE); `site/assets/og-image.png` regenerated (55386 bytes, mtime 2026-07-19 15:13). Live MCP `tools/list` on the deployed Worker = 19, including `get_webhooks` + `configure_webhook` (bookkeeper-verified via a live unauthenticated `tools/list` call). IndexNow fired post-deploy per its precondition guard (`tools/indexnow/submit.sh` → reported HTTP 200 accepted; script + its key-file-must-be-live abort guard verified on disk, live-fire itself not independently re-checkable without re-submitting).

## Drained 2026-07-19 (second wave)
- [x] 2026-07-14 [ORDER] **BYO-domain RULED — coordinated merge pass EXECUTED, LIVE (founder ruling F, 2026-07-19: "GO").** Customers may bring ANY domain they want to send from, INCLUDING their primary (with explicit informed-consent risk acknowledgment). Automation: delegation-first (nameservers/zone to our CF = fully automatic DNS) > agent-applied records (MCP returns records, customer's agent sets them, we poll-verify) > human walkthrough fallback; subdomain delegation (mail.x.com) as middle path. SPEC §20 SHIPPED-VERDICT (4 adversarial rounds, `docs/adversarial/byo-domain-design-review-2026-07-14.md`). Build complete 2026-07-17 on branch `byo-intake` (6 commits `aeda46f`→`f46e935` off `main@c30cd68`, adversary SHIP-dark + re-attack ADDENDUM SHIP `docs/adversarial/byo-intake-2026-07-17.md`). **Merge + deploy executed as ONE coordinated pass 2026-07-19:** merge commit `8175623` (`byo-intake` → `main`, bookkeeper-verified via `git show --stat`), F-wave copy commit `f214c7c` (19→21 tool-count sweep + BYO capability copy + AGENTS.md sync), fee-deletion commit `f86ce6e` (see the paired entry below, same pass). Platform Worker deployed version `d7b300e7-380e-45bd-a831-90ffde19cabc` BEFORE the site (deploy-ordering gate held) — bookkeeper live-verified: `tools/list` = **21** including `get_byo_domains`/`configure_byo_domain`, unauth `GET /byo-domains` → 401 (exists, not 404), `/status` → `{"status":"ok"}`. Site deployed (Cloudflare Pages deployment `3a5cc229`) — bookkeeper live-verified on `coldrig.dev`: `llms.txt` reads "Twenty-one" tools + npm v0.2.0, `openapi.yaml` serves the BYO paths, `/byo-domain` page carries live-intake copy ("live intake tools", concierge-activation posture, burned domains hard-pause never auto-replaced), `og-image.png` re-rendered (200, 55335 bytes, matches the `f214c7c` diff's 55386→55335 byte change). Adversary round-3 addendum: **SHIP** after 2 one-word "Nineteen"→"Twenty-one" straggler fixes (`docs/adversarial/fully-live-reframe-2026-07-19.md`). Bookkeeper independently re-ran the FULL platform suite post-merge: **534/534 passed (76 files)** — confirms the fee-deletion's reported 537→534 delta held through the coordinated pass. **Landmine found during verification (added to `HANDOFF.md`):** `.html` URLs 308-redirect to their clean extensionless form (`/byo-domain.html` → 308, `/byo-domain` → 200) — curl checks against this site need `-L` or an extensionless path, or they'll read a 308 instead of the page. Branch `byo-intake` + its worktree remain at `f46e935` (merged, not yet pruned — see `HANDOFF.md` Worktrees).
- [x] 2026-07-19 [ORDER] **Founder ruling A — internal 2¢ send-usage fee DELETED, whole class.** `SEND_USAGE_FEE_CENTS` (`apps/platform/src/engine/tick.ts:30`) + its ledger write + Stripe usage-report call + the A4 billing-grading block that existed only to protect that write — all removed, commit `f86ce6e` (part of the same coordinated pass as the byo-intake merge above). Send-path grading (transient/permanent/cap handling), orphan-`sending` reclaim, `MAX_SEND_ATTEMPTS`, suppression skip, and the `sent`+cap+event commit are all preserved byte-for-byte per the round-3 adversary's targeted re-run (5 fee-affected files, 17/17 passed). Bookkeeper-verified independently: `grep -rn SEND_USAGE_FEE_CENTS apps/platform` = zero matches (whole class confirmed gone); full platform suite re-run 534/534 (76 files). "$0 per-send fees" on the public site is now true by construction — closes the `ACTIVATION.md` Gate-2 "Rule the internal 2¢ send-usage fee" item as DONE-as-DELETE.

## Drained 2026-07-22 (go-live program handoff)
- [x] 2026-07-22 mcp-publisher 0.2.2 republish **DONE by founder** — registry API verified same hour: 0.2.2 isLatest, description "24 MCP tools…". Closes the registry-staleness tail for this wave.
- [x] 2026-07-21 **Glama listing verified SYNCED** (WebFetch 07-21: "21 high-level tools", "live in production (Gmail API, HTTPS/443)", $99/5-mbx pricing) — closes the claim-surface round-2 Glama tail; remaining from that entry = version-string skew at next npm publish.
- [x] 2026-07-20 [ORDER] **Stale-claim-surface class ROUND 2 — repo metadata files the 07-19 reframe MISSED (root cause of the Glama kill surviving the reframe).** Evidence chain: founder confirmed Glama re-syncs DAILY and a sync ran 07-20 (prior 07-18) — listing STILL showed "early access / sandbox / 17 tools" → grep found the copy alive in OUR tree: `server.json:4` (registry description! Glama is a registry superset), `llms-install.md:9`, `.claude-plugin/plugin.json:4`, `SECURITY.md:5`. Class = "repo-side metadata ingested by directories, outside site/README sweep scope." Fix lane EXECUTED 07-20: 6 members fixed (the 4 named + CONTRIBUTING.md:46 found by the builder's boundary sweep + server-card.json tools[] 19→21 found BLOCKING by the adversary — the card claimed "21 focused tools" while enumerating 19, the exact buyer cross-check kill). **Adversary verdict SHIP-after-fixes, fix applied** (frozen record `docs/adversarial/claim-surface-round2-2026-07-20.md`; the 5 copy files held ALL overclaim/caveat-deletion/consistency attacks). Committed+pushed 07-20 → Glama's next daily sync ingests fresh copy; verify the listing the day after. ⚠️ RESIDUALS: (i) **live server-card still serves 19 tools until the next site deploy** (`wrangler pages deploy site --branch main`, founder-confirm) — fold into the next deploy batch; (ii) adversary out-of-scope observations, future doc-sweep candidates: `FINAL-REPORT.md` says "test mode" prominently + `SPEC.md:13` still says "early-access/waitlist framing until backend activates" (internal docs, not directory-ingested — low priority); (iii) version-string skew noted: server.json 0.2.1 vs plugin.json/server-card/npm 0.2.0 — reconcile at the next npm publish. ⚠️ Consequence: the founder's `mcp-publisher publish` re-run (queue item) now matters MORE — the OFFICIAL registry still serves the stale 0.2.0 description regardless of our repo fix. **Fix wave COMMITTED+PUSHED 07-20 (`53effa9`)** — the 6-file fix, including the server-card `tools[]` 19→21, is on `main`/`origin/main`; only residual (i) above (live server-card still stale until next site deploy) remains open. **2026-07-21 update:** site deploy batch executed (Cloudflare Pages deployment `e9fd72a3`, from `main@2cfccb6`, clean tree in sync with origin) — live-verified `curl -sL https://coldrig.dev/.well-known/mcp/server-card.json` now enumerates 21 tools with the fully-live description; residual (i) CLOSED. Remaining open tail: (ii) Glama listing verification pending its next daily sync (repo fix `53effa9` + the now-current live card both land in it); (iii) version-string skew (server.json 0.2.1 vs plugin.json/server-card/npm 0.2.0) at next npm publish. **CLOSED 2026-07-22:** the 2026-07-21/22 wave redeployed both surfaces this class tracks — live server-card now serves 24 tools + the 24-claiming description (Pages `25844cc6`), and the repo-source copy (server.json etc.) is current at `453e445`/`d28afe7`. This entry's own tail is done; the ONE thing still stale is the OFFICIAL mcp-publisher registry itself, which is a separate standing item (`## Open` 2026-07-22 "mcp-publisher republish for 0.2.2").

## Drained 2026-07-29 (cf-connect-crack / registrar-armed handoff)

- [x] 2026-07-28 [ORDER] **Token prefix + rotation lane — SHIPPED LIVE 2026-07-29.** Mints `cr_live_` (was `cs_test_`; legacy grandfathered, hash lookup prefix-blind; bookkeeper-confirmed in source: `apps/platform/src/auth.ts:19`); authed `POST /token/rotate` (session+CSRF and bearer paths, `apps/platform/src/routes/token-rotate.ts`, wired `apps/platform/src/index.ts:112`) + Settings "Rotate API token" card replacing dead "lost-token recovery". Both found live by Mordy's agent. Adversary round-1 NO-SHIP (its own concurrency test asserted both-200 on a shared credential — a false green claim) → test fixed to an exactly-one-alive invariant → round-2 SHIP (frozen `docs/adversarial/token-rotation-review-2026-07-28.md`). Merged `b843963`; same verifier battery + deploy chain as the registrar-arming entry below (Worker `4a7bbf0d`, pushed `a68361c`).
- [x] 2026-07-29 [ORDER] **Registrar arming — SHIPPED LIVE + ARMED, closes Mordy's critical-path blocker.** `REGISTRAR_PROVIDER` env leg + per-tenant `registerDomains` opt-in + structured `Registrant` capture (required-when-true, zod v4 refinement preserved through MCP `.extend`; `registrant_json` column; `IncompleteRegistrantError`→400 naming fields, no PII values). Adversary round-1 NO-SHIP on B1 (domain port selected from a pre-UPDATE profile: fresh opt-in 503'd single-call + false founder alert; opt-out-after-persisted-opt-in still bought a stale registrant) → fixed (`setup_infrastructure` re-selects ONLY the domain port from THIS call's validated input — current-call authority; REPLACE_DOMAIN keeps persisted authority; sandbox-eligibility dominates; env leg inviolable) → round-2 SHIP with 5 independent repros (frozen `docs/adversarial/registrar-arming-review-2026-07-28.md`). Merged `c1e24a5`. **Verifier battery GREEN at `c1e24a5`:** typecheck clean all 5 workspaces (bookkeeper-reconfirmed this pass, `npm run typecheck` exit 0); platform 1122/1122 (bookkeeper-reconfirmed this pass, exact match: `npm test` → "122 passed (122) / 1122 passed (1122)"); dashboard 143/143, engine 126+4skip, cli 12/12 per integration-verifier (not independently re-run this pass). Pushed `a68361c` (bookkeeper-confirmed: `origin/main` 0 ahead/0 behind local `main`). **Deployed + armed by founder 2026-07-29:** Worker `4a7bbf0d-299b-4db4-b949-0bf25e841209` — bookkeeper-confirmed via `wrangler deployments list` (created 2026-07-29T07:49:05Z), both hosts live (`workers.dev` + `api.coldrig.dev`), no new migrations, cron intact. `REGISTRAR_PROVIDER=inboxkit` — bookkeeper-confirmed present via `wrangler secret list` (a secret-change deploy `ba2d43bf` followed at 07:50:43Z); `INBOXKIT_API_KEY`/`INBOXKIT_WORKSPACE_ID` also confirmed present — the arm is real, not a sandbox no-op. **Post-arm live checks bookkeeper-reconfirmed this pass:** `GET https://api.coldrig.dev/account` → 401; `GET https://api.coldrig.dev/checkout/success` → 302; `GET https://agent-cold-email-api.yaakovscher.workers.dev/app` → 307 → 200 — all three match the founder's reported spot-check exactly.
- [x] 2026-07-27 **InboxKit UI connect attempt — ✅ DONE 2026-07-28, WALL CRACKED:** manual NS path confirmed dead for CF-registrar domains (dmhadvisor.com RDAP: registrar Cloudflare Inc., apex NS locked), but the **"Cloudflare" token path CONNECTED with the ZONE-scoped token** (Zone:Read + DNS:Edit, one zone, NO Global API Key, NO NS change) — `domains/list` 19:58Z: `active/connected/propagated/matched` on the zone's own raegan/simon NS. Evidence + support ticket v2 (API parity) frozen in `docs/research/cf-registrar-domain-connect-2026-07-28.md`. ⚠️ FOLLOW-THROUGH: **`domains/remove` DONE 2026-07-29** — founder ran it; the dmhadvisor entity is now `scheduled_for_deletion` (verified via `domains/list`), clearing the path for Mordy's own setup. REMAINING: revoke the CF token (Keychain `cf-dns-dmhadvisor`) in the CF dashboard, then send ticket v2.

## Drained 2026-07-29 (prior-session [x] backlog sweep)

- [x] 2026-07-27 [ORDER] **Purchase-path clarity class (from Mordy's-agent live misread, 2026-07-27):** his agent concluded from our surfaces that "real sending still routes through the concierge activation. That's Yaakov, and it's where MORDYPILOT gets applied — there's no coupon field in the API surface." BOTH WRONG today: going-live = self-serve (checkout tool → Stripe URL → pay), promo codes = Stripe page "Add promotion code" link. Fixes, same coordinated claim-surface pass as the price-math entry below: (a) scope the concierge caveat precisely (going-live self-serve; only post-purchase mailbox-OAuth is a brief server-side operator step, being automated — kill entirely once the probe verifies); (b) explicit promo-code line on docs/faq/for-agents ("codes are entered on the Stripe checkout page"); (c) BUILD CANDIDATE: optional `promoCode` input on the checkout tool/API (resolve code→promotion_code id, pre-apply via session `discounts`) so agents apply codes without human fumbling — build AFTER the golive-ux lane merges (same checkout.ts surface, avoid collision), own adversary gate. **(a) and (b) CLOSED 2026-07-27/28** via the claims-truth pass (merged `ec0f972`) + the golive-ux lane (merged `15a2e01`) — bookkeeper-verified live this pass: `site/for-agents.html` reads "Going live is self-serve, not a sales conversation... Promotion codes are entered on Stripe's own checkout page via its 'Add promotion code' link — the API deliberately has no coupon-code field of its own"; `site/faq.html` carries the equivalent line. (c) the optional `promoCode` checkout-tool input remains a BUILD CANDIDATE, not yet built.
- [x] 2026-07-27 [ORDER] **All-in price-math surface (founder 2026-07-27: "explain on glama/github everywhere that all-in this is the best price by far — shows the agent exactly what to take into account")** — an agent-readable total-cost-accounting rubric on the machine-facing surfaces (Glama listing description, root README, server-card/llms.txt, for-agents, compare pages): enumerate what a true comparison must include (mailbox seats + domains + warmup + sending platform + suppression/compliance infra + per-send fees) so buyer agents stop comparing our $99 all-in vs competitors' bare-platform headline prices — the exact kill pattern from buyer cycles. Existing ammo on ledger: cycle-2 "our $99 UNDERCUTS winner Salesforge $112 all-in"; 07-20 corrected DIY math $76.50 vs $99 ($22.50 delta = orchestration+guardrails); Mordy's-agent $75-140 DIY band. ⚠️ TRUTH SCOPE: the claim holds at the 5-mbx starter shape; at agency scale (50mbx/8clients) we currently LOSE on price ($892 vs ~$605) — copy must scope its claim or the next buyer agent kills us for overclaim; pricing copy rules apply (no send-quota headline, no bare "unlimited"). Execute as ONE coordinated claim-surface pass (adversary-gated) AFTER the toolcount-25 sweep merges+deploys — same files, avoid collision. — standing discovery mandate consolidating: founder clicks GSC Request-Indexing + Glama Dockerfile build (THE two levers per post-ship audit) · autonomous: citation-panel routine repair (entry below) + IndexNow re-fire at next site deploy + buyer-panel cycle 3 AFTER the Glama refresh (measure the fix) + directory follow-through (mcp.so queued 07-15 still unlisted, cursor.directory scan pending, PR #10106 post-Glama-score, review-sites founder-gated). **CLOSED 2026-07-27/28** via the claims-truth pass, merged `ec0f972` (adversary SHIP-after-fixes, `docs/adversarial/claims-truth-pass-review-2026-07-27.md`; 266/266 merged-tree guard assertions, full battery exit 0); the rubric is bookkeeper-verified LIVE on `site/pricing.html` under the heading `<h2 id="all-in-title">The all-in math, not just the sticker price.</h2>` (`site/pricing.html:173`), and cross-linked from for-agents/llms.txt/README/compare per the pass. Site deployed `e1bfe52c`, IndexNow re-fired 200. TRUTH SCOPE caveat (5-mbx-starter claim vs agency-scale loss) carried into the copy per the adversary gate.
- [x] 2026-07-27 [ASK] **SDN weekend false-alarm class (diagnosed live 2026-07-27, list itself was NEVER stale):** Treasury publishes no SDN updates most weekends → the daily 06:17 UTC relay push re-fetches a byte-identical list → `ingestSdnCsv`'s monotonicity guard 422s it as `stale` AND `reconcileSdnAlert` counts that benign "verified unchanged" as a FAILURE (`sdn-ingest.ts:92-97,119-125`) — combined with the permanently-525 direct path, the streak grew to #435 and fired 6h-cooldown "SDN list NOT loading" emails all weekend. Manual re-push this session ingested Monday's fresh publication (19,157 entries, `sdn-relay-1785176976403`, `failure_streak` back to 0 — live-verified on remote D1). This WILL recur every weekend/federal-holiday until fixed. Fix direction (design change to an adversary-ratified guard — needs founder word + mini-arc): byte-identical candidate → new `unchanged` outcome, HTTP 200, alert-reconcile success, WITHOUT touching `fetched_at` (preserves the direct-refresh-suppression rationale); `push-sdn.sh` logs OK-unchanged. Secondary: 06:17 UTC cron runs before US-business-hours publication → list lags up to ~24h behind on weekdays; consider a second push later in the day. **CLOSED 2026-07-27/28:** 2-round adversary arc (NO-SHIP r1 — the first cut left `fetched_at` untouched, which kept the Worker's OWN 5-min direct-refresh cron 525-ing all weekend and even produced RECOVERED↔failing flapping; fix A advances `fetched_at` on an "unchanged" push too, proven co-fired with the genuine-staleness backstop in `apps/platform/test/ofac-sdn-storm-interaction.test.ts` → SHIP r2, `docs/adversarial/sdn-unchanged-fix-review-2026-07-27.md`). Merged `f583fe5`, deployed Worker `b672b403`. Bookkeeper-reconfirmed this pass via a real droplet re-push: HTTP 200 `{"reason":"unchanged"}`, `fetched_at` advanced, `failure_streak` 0. Weekend alerts dead; the >10% single-step drop guard's security semantics are unchanged.
- [x] 2026-07-27 [ORDER] **TOOL-COUNT 24→25 CLASS SWEEP MERGED + DEPLOYED LIVE — closes the listing-packs 25-gate.** Finding origin: `docs/research/review-site-listing-packs-2026-07-27.md` (frozen) flagged `README.md`/`site/.well-known/mcp/server-card.json`/`site/for-agents.html` still reading 24 after `remove_mailboxes` became the registry's 25th tool, and gated G2/Capterra/AlternativeTo listing-pack submission on the live site showing 25 first. Adversary SHIP 0-blocking (frozen `docs/adversarial/toolcount-25-sweep-review-2026-07-27.md`); its two non-blocking follow-ups were folded pre-merge — bookkeeper-reconfirmed both directly: F1 (`compare-vs-agentmail.html`/`guide-cold-email-with-ai-agent.html` missing from the guard's forward count-check) is now closed, both surfaces present in `SURFACES_THAT_STATE_THE_COUNT`; F3 (`apps/platform/src/mcp/README.md` stale "now 24") now reads "now 25". Merge `e4a3fec` (parents `3da0041` session-ledger docs commit + `5277a8e` the sweep itself: 28 living claim surfaces + the new guard `apps/platform/test/site-tool-count-claims.test.ts` — bookkeeper ran it directly, **89/89 passing**). Bookkeeper-verified this pass, both to completion on merged main: `npm run typecheck` exit 0 across all 5 workspaces; full `npm test` **906/906 passing (117 files), exit 0** (adversary's own pre-merge battery, grounded one commit earlier at `a024970`, reported platform 904/904 — the +2 delta is unexplained but benign, both are all-green with no failures). Deployed: Worker `417175ae-5596-409d-8fab-6a1e697c6cc7` (`npm run deploy`, no new D1 migrations) → site Pages deployment `7e2751d0` off commit `e4a3fec` (`deployment list`-confirmed). IndexNow re-fired post-deploy (reported HTTP 200 accepted; not independently re-checkable after the fact — the script only echoes to stdout, same as prior fires). LIVE-VERIFIED THIS PASS (bookkeeper, fresh reads): `coldrig.dev/.well-known/mcp/server-card.json` → 25 tools enumerated + updated description; homepage → "25 focused tools" — no stale-cache mismatch observed on this pass's reads. Consequence: the listing-packs pre-submission gate above is CLEAR — G2/Capterra submissions unblocked; AlternativeTo remains gated on its separate 1-week account-age clock (started 2026-07-27).
- [x] 2026-07-24 [ORDER] **QUANTITY BILLING SHIPPED LIVE — whole arc closed, program's last build lane done.** 10-commit build arc (`c49592e`…`08e4757`) closes the full design chain (r1 committed-vs-active meter → founder ruled ACTIVE; r2 burn-replace double-bill → §7.1 bill-neutral release; the build's own D4 finding → §18 in-response billing projection on every mailbox-add response) → adversary FINAL SHIP (`docs/adversarial/quantity-billing-design-review-2026-07-24.md`) → merged `4e53e2d` (migration renumbered to 0015, since 0013/0014 were taken by the already-merged SDN relay). **Battery #7 caught a real class:** 817/817 tests green but `tsc` FAILED — the plan-tier collapse to a single `managed` plan left 2 stale `"launch"` string literals in `ofac-screening.test.ts` that vitest's runtime never touched but typecheck did; fixed `313963e` (confirmed: touches exactly `ofac-screening.test.ts`, 2 lines). **I independently re-ran both this pass:** `npm run typecheck` exits 0 across all 5 workspaces; the platform suite is **817/817 passing (116 files)** — exact match to the claim. Deployed `7f197f7d` (migration 0015 confirmed applied remote: `wrangler d1 migrations list --remote` → "No migrations to apply!"). **LIVE-VERIFIED THIS PASS (read-only Stripe Price lookup by `lookup_key`, no checkout session created):** `coldrig_platform_monthly_v1` = $49.00/mo, product name "Coldrig Platform"; `coldrig_mailbox_monthly_v1` = $10.00/mo, product name "Coldrig Mailbox" — both `livemode: true` and `active: true` — a 5-mailbox managed checkout totals $49 + 5×$10 = **$99.00** exactly as designed. `CheckoutInput` (`packages/shared/src/intents.ts`) now REQUIRES a `mailboxes` count (5-60) and has no `plan` tier field at all — this is a BREAKING checkout-API shape change from the old tiered enum; `site/openapi.yaml` confirmed already updated to the new `{ mailboxes, interval }` shape. Test-mode Tier-2 gate (`tools/billing-gate/`) ran independently twice (builder 5/5, adversary 5/5): 60% coupon rides a quantity bump proportionally, increase prorates, decrease creates zero credit, concurrent `ensureStripePrices` converges to one Price per `lookup_key`, discounted-card checkout captures. Billing worktree reaped — confirmed ZERO active worktrees via `git worktree list`.
- [x] 2026-07-24 [ORDER] **SDN RELAY SAGA COMPLETE + ARMED LIVE — screening now runs against the real Treasury list.** Relay branch (`966793b` relay + `7084081` throttle + `1da4307` pre-arm hardening; throttle = `reconcileSdnAlert` watchtower-pattern state machine, migration 0013 — first-failure alert → 6h cooldown → one recovery email, fixing the confirmed root cause that `fetched_at` only advances on success so a persistent failure re-alerted on every 5-min tick) → adversary FINAL SHIP (`docs/adversarial/sdn-relay-review-2026-07-24.md`: suite **793/793** in-worktree, ARM condition satisfied) → merged `3b29fc5` (fixes a latent D1 bind-param bug, TOCTOU fail-closed, replay guard; migrations 0013 `sdn_alert_state` + 0014 `sdn_list_content_hash`, both confirmed on disk) → deployed `be7342c5`. **ARMING EXECUTED:** `SDN_INGEST_TOKEN` generated + armed as a Worker secret (confirmed via `wrangler secret list` this session) + reported armed on the droplet (`/root/sdn-relay.env`, chmod 600 — not independently re-checked over SSH this pass, but a successful authenticated live ingest below is definitive proof both sides matched); cron `17 6 * * *` installed. First live push surfaced TWO real wire-shape quirks, both fixed same-hour and confirmed present in code: (1) Treasury's feed answers HEAD with 200 but 302-redirects GET — fixed via `curl -sfL --max-redirs 5` in `tools/sdn-relay/push-sdn.sh` (`036ff9a`); (2) the real SDN.CSV ends with a trailing 0x1A DOS-EOF marker the fail-loud parser rejected — tail-tolerant fix (`7d111cb`, confirmed in `apps/platform/src/ofac/sdn-parse.ts`; interior corruption still stays fatal by design) merged `080b3fd` (796/796) → redeployed `71bfa830`. **LIVE-VERIFIED ON REMOTE D1 THIS BOOKKEEPING PASS:** `sdn_list_meta.active_version = "sdn-relay-1784888169248"`, `entry_count = 19241`, and a direct `SELECT COUNT(*) FROM sdn_entries` returns **19241** — exact match, the real 19,241-row Treasury SDN list is loaded, not a stub. `sdn_alert_state` reads `failure_streak: 0, last_alert_ts: null, last_detail: "relay ingest: 19241 entries (sdn-relay-1784888169248)"` — fully consistent with the reported "SDN list load RECOVERED" email (alert storm closed with exactly one recovery note); the email delivery itself is outside what's checkable from here (no inbox access) so treat that one specific sub-claim as reported, not independently re-verified. ⚠️ Founder's Gmail filter (if created) matched the OLD subject "SDN list refresh failed" — new subjects are "SDN list load failing"/"SDN list load RECOVERED" (folded into the founder click queue in `## Now`). Migration-numbering gap at 0010 reconfirmed benign (never-claimed reservation — signup lane used 0009 — verified never-existed in git history). Non-blocking, carried post-arm: the frozen verdict recommends documenting a baseline-reset recovery path for a genuine >10% single-day SDN delisting tripping the monotonicity guard's stale-rejection (low-probability, historically unprecedented at ~17-19k entries) — not built yet.
- [x] 2026-07-24 [ORDER] **Quantity-billing DESIGN ARC CLOSED — FINAL SHIP `5b1edf7`.** Full chain, all commits verified present: design landed `71fb17f` → adversary r1 SHIP-AFTER-FIXES `b8550a5` (B1: committed-vs-active meter needed a founder ruling before build) → founder delegated the choice → design amended to the **ACTIVE/PROVISIONED meter** `fbd4285` (`set_mailbox_plan` deleted, `remove_mailboxes` intent added, N1-N5 folded) → adversary r2 SHIP-AFTER-FIXES `4275c8f`/`f3ca625` (B2-rework: burn-replacement pause-not-release was double-billing the active meter — fixed via a new §7.1 bill-neutral burn-release mandate extracting `releaseMailboxes`, plus a bill-invariance governing rule + scenario 5) → FINAL SHIP addendum `5b1edf7`. Build DISPATCHED same session — see `## Now` "Quantity-billing BUILD in flight" for live progress.
- [x] 2026-07-24 [ORDER] **Repo chores lane MERGED `5aa4154`.** `site/openapi.yaml` pinned to `3.0.3` — live-verified via `redocly lint`: 0 errors, 11 unrelated warnings; **18** `nullable: true` occurrences counted directly (all valid under 3.0.3) — this reconciles the prior ledger's "21 places" figure, which over-counted; the real number was always 18. BYO-domain row present in `site/guide-mcp-tool-count.html` (verified). `tools/aeo-panel` now tracks BOTH "Coldrig" and "ColdStart" as a mention-hit (verified in `panel.v1.json` + README). Dev-doc H1 titles rebranded — `SPEC.md`/`ARCHITECTURE.md`/`ACTIVATION.md` all verified reading "Coldrig (working name ColdStart) — ..." — plus the stale SPEC.md CLI example fixed.
- [x] 2026-07-24 [ORDER] **Live-billing claim-surface truth sweep MERGED `66928b9`.** 11 files corrected (verified via `git show --stat`), including machine-facing surfaces (`site/llms.txt`, `site/.well-known/mcp/server-card.json`) — billing-is-live claims now accurate everywhere, concierge caveat retained. Site DEPLOYED from this commit (`7c6c240d`, verified via `wrangler pages deployment list`). IndexNow re-fire reported post-deploy; the specific HTTP response code for that run isn't independently re-checkable after the fact (the script only echoes to stdout, no disk log) — treat as reported, not re-verified this pass.
- [x] 2026-07-23 [ORDER] **⛔ BATTERY #4 FAIL — 5 fixture failures, GA deploy HELD; fix lane dispatched.** Both GA merges + glue on main (OFAC 32d5b6b + provisioning 8674da7 + glue 1dccd94 + gitlink cleanup d89516c); typecheck 5/5 clean, all 6 union-suspect files clean (the 5 hand-resolved conflicts composed correctly), asset-hash check clean. FAILURES = provisioning lane's pre-existing G3/factory fixtures (ga-gates-factory-wiring + ga-gates-g3-activation-state) drive checkout with NO SDN list seeded → N-OF-1 fail-closed correctly lands them 'screening_hold' — the sanctions gate working as designed against fixtures that predate it. Fix = benign-list seeding (ofac-builder's own activation-gate.test.ts pattern), test-only, micro-worktree fixture-fix-20260723; assertions NOT weakened. Platform full count on merged tree: 763 total (was ~705/678 per-branch). **CLOSED 2026-07-23:** fix merged `6299788`, battery #5 PASS 763/763 — see "GA WAVE MERGED + DEPLOYED" entry above for the full deploy evidence.
- [x] 2026-07-23 [ORDER] **PROVISIONING-CORE BUILT (4 commits 18f6b10→ca072ac) — design-adversary build review RUNNING (gates merge AND arming). CLOSED: adversary SHIP 0-blocking, merged `8674da7`** (see "GA WAVE MERGED + DEPLOYED" entry above). All 7 increments: factory inboxKitConfig wiring (real provisioning REACHABLE when armed) + N-G5-2 isolation + G0 ledger/choke-point + G2 ceiling w/ reaper + G4 slots + G3 honest activationState (B2 formula; OFAC seam = one line in reporting.ts) + revoke-order + MCP version single-source. Platform 678/98f in-worktree, 5 revert-fail proofs. FOUR deviations PROVISIONALLY RATIFIED (adversary attacking each): (1) slot counter account-wide not per-month — builder caught a REAL design bug (monthly reset re-opens occupied slots; class note fed forward: any counter keyed by a bucket shorter than its resource's lifetime); (2) cost-from-kind signature + warmup kind reserves 0 (bundled); (3) capacity_pending marker racy-but-self-correcting; (4) DO writes D1 for the ledger (design §0 sanctioned). Arming-verify list carried in the lane report: slot-overage semantics, real cost→$ tuning, DO-context send_email, reaper cron liveness, paid-tenant clock sanity.
- [x] 2026-07-23 [ORDER] **OFAC: N-OF-1 CLOSED (adversary addendum frozen, branch verdict SHIP @ b14bb6b, 703/703 re-run) — fix round 2 DISPATCHED for the NEW self-heal race before merge. CLOSED: self-heal-race fix `daa339f`, merged `32d5b6b`** (see "GA WAVE MERGED + DEPLOYED" entry above; and the follow-on "OFAC first live cron hit HTTP 525" entry for the post-merge production incident). (recovery-vs-admin window: DO guard keys on version-sentinel only, admin actions don't change version → recovery can override an admin decision; worst = reject audit row overwritten 'cleared' while tenant stays terminated — audit corruption, never unsafe activation; fix = AND screening_status==='review' in the DO guard + conditional resolve on status='pending' + race tests both ways). N-G5-1 pre-arming reminder ALREADY SATISFIED on main (generic 503 body verified index.ts:156 post-provisioning-merge). Prior: Fail-closed + SELF-HEAL: empty-list → 'review' w/ sentinel "list-unavailable" + review row + dedicated alert; scheduled() recovery sweep re-screens sentinel reviews once the list loads (fresh-read guarded, 'clear' auto-resolves as system-recovery); 2 pre-existing tests that silently RELIED on fail-open fixed (benign-list seed); honesty doc updated — "screened at checkout" now true without caveat. Platform 703/101f, RED/GREEN proven. Prior entry: Fail-open trace HELD everywhere (paid ⟹ screened; webhook reorder → free-not-activated; D1-throw fail-closed via DO rollback) EXCEPT the empty-list window: pre-first-refresh checkouts screen 'clear' unscreened (null list_version audit trail only). Fix = screenTenant fail-CLOSED on no-active-list → 'review' + 'sdn_list_unavailable' review row + alert; guard test RED-on-old; honesty doc updated. Minor NBs ledgered: re-screen doesn't abort in-flight provision (dark, 1-provision bound); ms-window TOCTOU screen-vs-refresh-delete. Arming still verifies sdn_list_meta.active_version non-null + real SDN.CSV wire shape + cron CPU on ~17k rows. Adversary confirmed: shadow-swap crash-safe, grandfather can't overwrite real review, terminate extraction byte-identical, no test network leaks. G1a SDN plumbing (fail-loud parse, shadow-swap w/ atomic meta flip, once-daily refresh keep-prior-on-failure) + G1b screening at both checkout write sites (made async) + brand-rewrite re-screen (NB-1) + review queue w/ match context (NB-3) + admin clear/reject (reject → newly-extracted admin/terminate.ts, shared w/ admin-ops per rule c) + constructor-time grandfather sentinel + honesty doc (NB-4). Migration 0012. Platform 698/698 in-worktree (+50), 2 revert-fail proofs. Lead adversary attack = FAIL-OPEN trace (default 'clear' + screen-at-checkout: can a tenant reach active unscreened?). Arming-time list carried: real SDN.CSV wire shape, ~17k-row cron CPU budget, OFAC_LIST_URL reachability. ALT.CSV aliases deferred-documented.
- [x] 2026-07-23 [ORDER] **⭐ ARM-FOR-EVERYONE GO (founder verbatim: "lets create everything and arm for everyone (warm mailboxes, adding mailboxes, tiers etc.")** — accelerates program steps (4)+arming and un-defers quantity billing. **Lane P + Lane O both DONE:** merged as `8674da7` (provisioning-core) + `32d5b6b` (OFAC) — see "GA WAVE MERGED + DEPLOYED" above; quantity-billing lane now SHIPPED LIVE (see "QUANTITY BILLING SHIPPED LIVE" entry below) — this closes the program's build phase entirely; only founder clicks and Mordy-onboarding gates remain (`## Now`). EXECUTION PLAN: Lane P (hard-builder, worktree) = provisioning-core GA: factory inboxKitConfig wiring (closes the adversary-found dark gap — real provisioning reachable) + N-G5-2 tick isolation + revoke-order hardening + G0 spend ledger/choke-point + G2 ceiling + G4 slots/auto-upgrade + G3 pending-state (screening stubbed 'clear' until G1b) + serverInfo version fix; D1 migration slot 0011. Lane O (spec-builder, worktree) = OFAC G1a+G1b per design + NB dispositions + founder-Q2 recommendations adopted (hit→review+block+alert never auto-reject; existing active tenants grandfathered clear); migration slot 0012. Signup lane holds 0010. THEN quantity-billing lane (ratified $99/5mbx + $10/mbx + annual ~2mo-free + agency bundle vs $29-workspace anchor) — sequenced AFTER P+O merge (billing.ts collision). THEN arming sequence (main loop, per 07-22 grant): INBOXKIT_API_KEY/WORKSPACE_ID + GMAIL_OAUTH_GRANTS wrangler secrets from Keychain, droplet tunnel + ENGINE_BASE_URL + ENGINE_TENANTS, gate-e throwaway-mailbox verify on EXISTING slots (new InboxKit plan purchases = surfaced to founder, not autonomous). ⚠️ OAuth refresh token dies ~07-26 (Testing consent) — re-mint at arming.
- [x] 2026-07-23 [ORDER] stale-claim instance (claim-surface class): MCP `serverInfo.version` returns **"0.1.0"** (SERVER_INFO in apps/platform mcp handler) while npm=0.2.0/registry=0.2.2 — bump + align at the signup-auth merge integration pass; sweep for other hardcoded version strings while there. **CLOSED 2026-07-23 `ca072ac`:** single-sourced from `apps/platform/package.json` (now 0.2.2); `mcp.test.ts` asserts against the imported version, not a literal, so a future bump can't re-open the drift; live-verified via a real `initialize` call returning `serverInfo.version: "0.2.2"`.
- [x] 2026-07-23 [ORDER] **🚦 WAVE SHIPPED LIVE AT `485a4fe` — BATTERY PASS; DEPLOYED + LIVE STRIPE KEYS FLIPPED.** All three lanes merged + adversary-SHIP'd (I3+I4 5b151e7 r2-SHIP · brand sweep 60275b3 SHIP · G5 gate(a) 91f8227 SHIP · hermetic test-env bca6db9 SHIP) + integration 485a4fe (REGISTRAR_* hermetic pin + N-G5-1 generic 503 body). wave-verifier re-run: typecheck 5/5 clean, dashboard 99 / engine 95p3s / platform **648/648** / CLI 9/9, ZERO failures — checkout.test.ts GREEN with the real ambient STRIPE key present (prior FAIL condition re-verified fixed). Sanity greps pass. REMAINING SEQUENCE (main loop, single founder unblock): (1) `cd apps/platform && npm run deploy` (D1 migrations no-op, wrangler deploy) → (2) live-verify /status + tools/list 24 + simulate-404 → (3) LIVE STRIPE KEY FLIP (wrangler secret put STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET from Keychain coldrig/stripe-live-*) → (4) verify live session creation + line-item product name "Coldrig Launch" via Stripe API read (NEVER complete a charge). Classifier denied `npm run deploy` from the main loop 2026-07-23 despite the ledgered grant — consistent with the 07-15 standing lesson (relayed/standing authorization insufficient; founder must speak in-session). **CLOSED 2026-07-23 — DEPLOYED + LIVE:** founder authorized deploy in-session (AskUserQuestion: "Deploy now — I authorize it") → `npm run deploy` succeeded, Worker version `3f7acce1-bfb6-4c46-8e88-62ec4ced9fce` (live-verified `/status` ok + simulate 404 + tools/list 24). LIVE STRIPE KEY FLIP executed (STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET set from Keychain `coldrig/stripe-live-*`, wrangler-secret-list-verified present): signup test tenant `ten_248885a6-dc4b-45e8-8f3d-1c427f8e1ad7` ("Coldrig Flip Verify (internal)") → POST /checkout → `cs_live_` session → Stripe API read-back **livemode:true**, amount_total 9900, line-item "Coldrig Launch" (brand fix verified on the live money surface). Session left open, NO charge completed. `MORDYPILOT` (60% single-use) now redeemable.
- [x] 2026-07-23 [ORDER] **HERMETIC TEST-ENV FIX BUILT `284b96b`, MERGED `bca6db9`** (adversary SHIP 0-blocking; unblocked battery → deploy → key flip, see wave-ship entry above). Mechanism proven: vitest-pool-workers → wrangler getVarsForDev injects EVERY ambient .dev.vars key as a secret_text binding; old config only overrode 2 vars. Fix: constructed allowlist env (TOKEN_HASH_PEPPER/ADMIN_TOKEN/STRIPE_WEBHOOK_SECRET fixed) + ALL other ambient keys neutralized to `null` (in miniflare LiteralSchema, falsy, ??-transparent — "" and undefined both fail) via new test/hermetic-env.ts; hermetic BY DEFAULT so G5's REGISTRAR_* auto-covered; canary HERMETIC_LEAK_CANARY in .dev.vars.example + failing-by-construction guard test; class swept — platform is sole member (engine/dashboard/CLI load no .dev.vars). Proofs: revert→8F, fix→15/15; platform 645p/92f WITH fake truthy key; green with .dev.vars deleted. CI lesson ledgered: an empty-example-only CI can never catch ambient coupling — canary flow now does. MERGE ORDER on SHIP: hermetic first → G5 → wave-verifier battery re-run in MAIN checkout with real key present (true acceptance) → deploy → key flip.
- [x] 2026-07-23 [ORDER] **G5 GATE (a) BUILT `e7272ec`, MERGED `91f8227`** (adversary SHIP 0-blocking; arming now unblocked, see ## Now). Branch worktree-g5-gate-a-20260723 on 24d9436: REGISTRAR_* env w/ spend-arming markers + isRealSpendArmed 4th leg + coverage-test maintenance; factory domain port now UNCONDITIONALLY RegistrarUnarmedDomainPort in the real branch (stricter than design — builder deliberately did NOT thread registrarConfig into the factory: identical-branch threading = theater code; real domain purchase now unreachable no matter what arms); Porkbun stub DELETED (zero refs verified); RegistrarUnarmedError → 503 {code:registrar_unarmed} + best-effort founder ops alert at first vendor touch, rethrown (graceful, never silent fallthrough); 2 revert-fail-restore proofs quoted; platform 638p/engine 95p3s/typecheck ×3 clean. Builder-flagged adjacent PRE-EXISTING gap (adversary to confirm unreachable today): tick-sweep replace-domain path has no VendorError isolation around searchLookalikes — an armed-but-registrar-unarmed tenant's burn-replacement would crash the whole tick; revisit when inboxKitConfig wires into tenant-do. provisioning.ts 301→319 lines (soft ceiling, necessitated).
- [x] 2026-07-23 [ORDER] **BRAND SWEEP MERGED `60275b3` (adversary r1: SHIP, 0 blocking — battery re-run by adversary, tripwire re-proven non-tautological in an isolated sandbox, inventory independently swept: site 49 files / SPA BRAND_NAME / CLI / ops emails / openapi / MCP metadata ALL already Coldrig; suffix derivation reads STRIPE_SECRET_KEY not whsec, sk_live_ → clean "Coldrig Launch").** One trivial merge conflict (both lanes added the *.ts?raw declaration in test/sql-raw.d.ts) hand-resolved keeping both guard comments. Adversary NB notes ledgered: tools/aeo-panel still measures "ColdStart" only (internal — should track BOTH brands going forward, IDEA below); engine infra strings + repo-doc H1 titles are dev-facing (separate docs pass, IDEA below); rk_test_ restricted key would drop the test-mode label in test mode only (benign direction). **INTEGRATED BATTERY (wave-verifier) RUNNING on 60275b3 → on PASS: deploy Worker (npm run deploy) → LIVE STRIPE KEY FLIP from Keychain + live session-creation verify (never complete a charge).**
- [x] 2026-07-23 [ORDER] **I3+I4 MERGED TO MAIN `5b151e7` (adversary r2: SHIP, 0 blocking — all 3 r1 findings closed, keyless rotation re-proven against the REAL engine store, typecheck re-run by adversary).** r2 carries ONE non-blocking residual for the ledger: revoke runs after the released_at UPDATE, so a crash that loses a single revoke fetch leaves that mailbox's token lingering (skipped on retry via the released_at IS NULL filter) — same class as the founder-accepted Gate-2 crash residual, strictly narrower than r1's never-revoked; optional trivial hardening = revoke before the UPDATE (fold into GA wave). **G5 gate (a) micro-lane DISPATCHED at this merge point** per amended design §G5 — scope = registrarConfig decoupling + domain.buy fail-closed hard-block + guards; Cloudflare RealDomainPort rewrite DEFERRED to GA wave pending CF purchase-API verification (adapter would be fiction against an unverified wire shape; pilot is BYO-connect anyway). Signup-auth increments A/B unblock at this merge too (queued next wave). Worker deploy waits for brand-sweep merge per program (deploy batch = both lanes + integrated battery).
- [x] 2026-07-23 I3+I4 fix round `6f46b7e` (FIX1 key-drop revert-fail-proven; FIX2 revoke wired; FIX3 loud parseGrants; platform 625→635) — CLOSED by r2 SHIP, folded into the MERGED entry above.
- [x] 2026-07-22 [ORDER] **www.coldrig.dev has NO DNS record** — ✅ CLOSED 2026-07-27: www now resolves (A 172.67.188.91) + HTTPS 200, live-verified. (founder reported "site down" — apex verified 200/0.15s, pages.dev 200, API ok; www = NXDOMAIN). Fix = founder click: CF dashboard → Workers & Pages → agent-cold-email → Custom domains → add www.coldrig.dev (auto DNS+cert). ⚠️ Stripe business profile lists https://www.coldrig.dev as the URL — currently dead for their reviewer; either add www (preferred) or edit the Stripe URL to apex. Also open: Stripe support_email unset (API blocks own-account update; dashboard click → support@coldrig.dev). LIVE-MODE STAGING DONE 07-22: live webhook we_1Tw00lRKYEFKoA9wH6Zh2onE (6 events, whsec in Keychain), live coupon nxzMBfAw + single-use code MORDYPILOT (60% off) ACTIVE; charges_enabled still false with ZERO requirements due (propagation) — re-check before the flip.
- [x] 2026-07-26 [ORDER] ✅ RUN 2026-07-27 — frozen record `docs/research/post-ship-audit-2026-07-27.md`. Headlines: 61 signups live in D1; crawlers (ClaudeBot/GPTBot/Googlebot/Bingbot/Perplexity) all crawling daily with 200s yet ZERO index presence — the founder GSC/Bing clicks are confirmed as THE discovery lever; only working channel = Glama directory, whose stale cached copy is the #1 buyer-kill; citation-panel weekly routine NEVER actually ran (see new Open item). Checkout-conversion count blocked on ADMIN_TOKEN (command in the frozen doc). POST-SHIP AUDIT (founder-requested 2026-07-12): (a) citation-panel trend review (`tools/aeo-panel/runs/TREND.md`, 2 weekly runs in); (b) Cloudflare crawler analytics (AI-bot fetches) + human traffic review; (c) GSC/Bing indexation status; (d) design-pass QA — human-visitor polish holding the extraction structure; (e) coldrig.dev wiring + zone AI-bot-block settings verification; (f) deferred-paywall funnel numbers (signups → setup-completed → checkout); (g) engine defect-class guards re-check (soft-bounce streak + idempotency ledger holding); (h) grep.app indexation recheck (NOT-INDEXED as of 07-14; no on-demand seed exists — natural crawl only).
- [x] 2026-08-05 [ORDER] **🔥 INCIDENT: Mordy’s first provisioning stranded an orphan domain — DIAGNOSED + HOTFIX SHIPPED + DEPLOYED.** Root cause: `setDns` raced InboxKit’s ~30s async domain registration — the throw left a vendor-registered domain ($12.50, goauthorpitchdesk.com) persisted at InboxKit but never recorded locally (persist-before-DNS + adopt-before-buy is the fix pattern). 7 read-only lanes (diagnosis + 5 class-sweeps + pipeline-adversary) converged into a 14-member hotfix (guard-builder): domain-buy intent record, persist-domain-immediately + setDns-as-retryable-follow-up, adopt-before-buy via `/domains/list`, replay dedupe, `recordUsage` non-fatal, shared `toErrorResponse` (MCP+HTTP), ledger wall-clock + commit status-guard, half-write window shrunk, RED-proofs, +4 pipeline-adversary amendments (candidate dedupe-vs-owned, absent≠false on registerDomains/registrant, per-mailbox `infrastructure_status` isolation, credential-push failure surfaced via H-alert). Adversary provenance: R1 NO-SHIP (3 blocking: B1 setDns-not-re-driven, B2 adopt-filtered-by-availability, B3 DO-brick via raw CREATE UNIQUE INDEX in constructor) → fix round → **R2 SHIP** (all execution-verified incl. a forced DO reboot via `state.abort()`); frozen `docs/adversarial/incident-hotfix-gate-2026-08-05.md` (R1+R2) + `docs/adversarial/provisioning-pipeline-deep-dive-2026-08-05.md`. Battery: platform 129 files/1189 tests, typecheck clean ×5. **SHIPPED + DEPLOYED 2026-08-05** (bookkeeper-verified this pass): merged `fad3a3e` (hotfix) + `7ab780f` (openapi/site contract + frozen adversary records); Worker `451a1876-c49a-4fcd-905a-505489509528` live both hosts (`serverInfo` 0.2.2, `/status` ok both hosts, confirmed via `wrangler deployments list` + live MCP `initialize`), cron intact; site Pages `60ad814c` --branch main off `7ab780f` (confirmed via `wrangler pages deployment list`); `site/openapi.yaml` setup responses now `[202,400,401,409,502,503]` + `registerDomains`/`registrant` schema (grep-confirmed in-repo); IndexNow fired HTTP 200 (reported, not independently re-checkable after the fact). **Mordy re-confirmed 2026-08-05: NOT yet retried** — InboxKit workspace `c5188ced…` still 0 mailboxes; `goauthorpitchdesk.com` still owned/0-assigned/DNS pending. His unblock is execution-verified IN-GATE (same key `apd-setup-a-2mbx` → zero new buys, orphan ADOPTED, mailboxes provisioned) but nothing further happens until the founder sends his go-message. ⚠️ ‘provisioned’ ≠ ‘can send’: the manual OAuth/credential-push grant (deep-dive F5) is a human prerequisite before his mailboxes can actually send — it can’t succeed on first provisioning (the manual minter is keyed by mailbox emails that don’t exist pre-provisioning); an H-alert surfaces the moment it’s needed. Deferred class-wave inventory drained to the new PROVISIONING CLASS WAVE entry below. Related: the 2026-07-29 double-buy saga Open item (this incident is its sibling, now live-proven and closed).

## Drained 2026-08-16 (handoff)
- [x] 2026-08-09 [ORDER] **Wave 3 SHIPPED + DEPLOYED + VERIFIED — five lanes, combined gate SHIP 11/11 attacks failed.** Merged `main` `17e05d4` (--no-ff, 138 files +7254/-678), pushed. Lanes: msgchannel Inc2+Inc3 (operator route + `list_messages`/`ack_message`, 25→27 tools; live-confirmed `apps/platform/src/mcp/tools.ts:347-348`), webhook inc2 (decline-code second Stripe read — `resolveDeclineCode`/`getDeclineCode`, `billing.ts:335,750-751` + `stripe-client.ts:322-353`, code-verified — closes N-1 refinement + the readDeclineCode-dead-on-real-payloads residual; `dispute.created` staleness exemption bounded to `isDisputeSettledWithoutFreeze`, `billing.ts:407,488`, code-verified; refused-events `applied=0` flag, `billing.ts:598,620`, code-verified — closes N-2; 413 cap now enforced on ACTUAL bytes read not declared Content-Length, `validate.ts:37-79`, code-verified — closes audit item 7), dash-idem (buy-intent Idempotency-Key on setup + remove-mailboxes, `infrastructure.ts:11-13`/`checkout.ts:27-30`, code-verified; token-rotate revokes all dashboard sessions, `token-rotate.ts`, code-verified), watchtower hardening (D1-outage-safe alert ordering — `watchtower.ts:82,321` "NEVER THROWS on a D1 outage", code-verified; dead-man via `WatchtowerDO` new Durable Object + migration tag v3, `wrangler.toml:102-104`, code-verified; new `/status` field `sweepAgeSeconds`, `routes/status.ts:31-33`, live-confirmed 215s/216s both hosts, well under the 900s `SWEEP_STALE_MS` threshold), byo-teardown (never vendor-release customer-connected resources). Lane branches preserved: `msgchannel-inc23-2026-08-06`, `webhook-inc2-2026-08-06`, `dash-idem-fixes-2026-08-06`, `watchtower-hardening-2026-08-06`, `byo-teardown-2026-08-06`, `integrate/2026-08-09-wave3`. Gates: byo-teardown SHIP 0-blocking (frozen `docs/adversarial/byo-teardown-gate-2026-08-06.md`); msgchannel r1 SHIP-AFTER-FIXES → F1/F2 fixed `1eef443` (frozen `docs/adversarial/msgchannel-inc23-gate-2026-08-06.md`); combined integration gate r1 SHIP-AFTER-FIXES (1 blocking B-1: unconditional `dispute.created` staleness exemption bricked a tenant on closed(won)-before-created ordering) → fix `414b58e` (exemption bounded by `isDisputeSettledWithoutFreeze` + `closed` UPSERTs the disputes row; RED 3→GREEN, both halves proved load-bearing by partial revert) → r2 **SHIP**, 11/11 attacks failed (frozen, git-tracked `docs/adversarial/wave3-integration-gate-2026-08-09.md`, verified present + committed). Battery (as reported by the wave-3 verifier lane, cited from the merge commit `17e05d4` message): typecheck ×5 clean; 227 test files/1810 tests 0 real failures (platform 166f/1522t post-fix); builds clean; md5 tripwire clean. **DEPLOYED + LIVE-VERIFIED THIS BOOKKEEPING PASS:** Worker version `0f197c4d-ffe8-4da6-a1f4-94c3f23063d6` both hosts — `/status` returns 200 `{"status":"ok","sweepAgeSeconds":215}` (workers.dev) / `216` (api.coldrig.dev), confirming the deploy AND the new sweep-freshness surface are live; live server-card `coldrig.dev/.well-known/mcp/server-card.json` reads version 0.2.2, 27 tools (independently fetched, not just reported). Cron `*/5 * * * *` intact; WatchtowerDO migration v3 applied (per orchestrator's evidence, not independently re-checked via `wrangler` this pass). Site deployed: Pages deployment `20a4a523` --branch main; IndexNow fired HTTP 200 for 15 changed pages (reported, not independently re-checkable after the fact). Worktrees: ALL lane/audit/integration worktrees removed (branches preserved); the stale `agent-a25bfeea` registrar-lane worktree from the 2026-08-05 session archived to branch `d451d28` and removed. Remaining: main checkout + the external Codex design worktree only (`git worktree list`-confirmed this pass). See `## Open` below — B1/B2/B3/NB-1/N-1/N-2/item-7/readDeclineCode entries updated with their own NOW SHIPPED notes; NB-2/NB-3/NB-4 of the ORIGINAL `audit-alerting-completeness-2026-08-06.md` (cron-leg error counters unread, digest thrown away, provisioning-vendor-failure-alerts-nobody) were NOT part of this wave and remain open, untracked as their own bullets — distinct from the wave-3 gate's own NB-2/NB-3/NB-5 (which do have separate `## Open` bullets).
- [x] 2026-08-14 [ORDER] **C3(b) post-ship re-attack (founder-ordered) → BLOCKING LIVE defect confirmed:** a permanently-dead vendor registration reads to the customer as "provisioning in progress" forever — the success-pending predicate (`domain-dns.ts:257`) reads OUR local `domains.status` lifecycle column, never the vendor's registration verdict, and nothing bounds or escalates a stuck `dns_status='pending'` (the Finding-3 amplifier: no timer, no attempt ceiling, no abandonment state). Executed proof 9/9 green (`test/zz-adversary-c3b-attack.test.ts` at scratch clone `6b96ae9`). This is a REGRESSION from part (b), not a pre-existing gap: before part (b) this condition threw a retryable `VendorError` — mis-graded but VISIBLE; part (b) converted it into a silent success. Every other bookkeeping/deploy/dark-flag claim from the 2026-08-13 session tail CONFIRMED under this fresh-context attack (legs A/B/D/F/G held). Fix wave IN FLIGHT same-day (Bug Response class fix + fresh re-attack gate + deploy) under the standing autonomy grant. Frozen: `docs/adversarial/c3-postship-reattack-2026-08-14.md`. → CLOSED same-day: class fix shipped+deployed (merge `e5598da`, Worker `71ec2003`, 3-round gate SHIP `fcf845a` — `docs/adversarial/vendor-verdict-gate-2026-08-14.md`).
- [x] 2026-08-14 [ORDER] **`GET /admin/ops/checks` — read surface for the per-tenant watchtower checks → SHIPPED + DEPLOYED same-day (Worker `ba8d0fec`, merge `babf287`).** Built same-day (branch `feat/admin-ops-checks-2026-08-14`, `370163b`), adversary gate SHIP-WITH-NOTES (auth matrix incl. 14 path-normalization bypass attempts, trigger-instrumented write-canary 0 writes across 9 query shapes, full `last_detail` content inventory — no secret/stack can reach it), live-verified (200 authed, 401 unauth, first read returned exactly the 2 known Mordy `domain_dns_aging` checks). ⚠️ TWO-STORE CAVEAT (gate Finding 1, folded into the route comment): the `d1` + `cron_sweep` dead-man checks live in WatchtowerDO storage and NEVER appear here — consumers MUST pair with `GET /status` (503 degraded covers both) and treat stale `updatedAt` (sweep is */5) as the dead-cron tell; the Claude watch (leg 3+4) does both. Gate Finding 2 folded: the no-cap comment now cites the OPEN `watchtower_state` unbounded-growth item (2026-08-09 NB-2, line ~57) instead of wrong scale math — that item now also bounds this endpoint when closed. UNVERIFIABLE noted: real D1's multi-MiB response ceiling unmeasured (local-only 2.14 MiB probe).
- [x] 2026-08-14 [ORDER] Reply to Mordy's agent → **SENT 2026-08-14 ~21:55Z** (`POST /admin/tenants/ten_91aab24a…/messages` → 201 `emitted:true`, `regarding: sup_3ca260e4`, severity action_required; body = one-shot-defect correction + retry-same-key-`apd-setup-a-2mbx`-is-spend-safe + three honest outcomes). Unblocked by founder-ordered `ADMIN_TOKEN` rotation (fresh mint → Keychain `coldrig/admin-token` → `wrangler secret put`; secret-put redeploy = Worker version `7d41d1ae`, /status 200, admin digest 200). Old token dead; no other consumer existed (SDN relay uses its own token by design).
- [x] 2026-08-13 [ORDER] **🔥 P0 — idempotent setup retry MINTS A NEW LOOKALIKE DOMAIN each pass — SHIPPED + DEPLOYED 2026-08-13, ARMED (Worker `d8055e22`).** Root cause + fix + gate below; merged `main` `8a66976` (--no-ff, lane `fix/intent-key-orphan-2026-08-13` preserved, byte-identical to gated tree verified pre-push), pushed `54ca78a..8a66976`, deployed both hosts, `/status` 200 both, `/account` 401, cron intact. Adversary gate SHIP 0-blocking (frozen `docs/adversarial/intentfix-gate-2026-08-13.md`, all 7 attack surfaces HOLD, money invariant RED-proven — $15 mint reproduced on un-fixed code then vanishes on fix), battery 171 files/1601 tests, typecheck ×5, build clean. **The fix is ARMED, not yet APPLIED to Mordy's DO** — the dated one-shot reconciliation (`engine/legacy-domain-intent-keys.ts`) fires in the `TenantDO` constructor, so his DO reconciles on its NEXT touch (his next retry), rebinding the orphaned `goauthorpitchdesk.com` intent to ordinal 1 (ordinal 0's 08-12 `theauthorpitchdesk.com` intent untouched), zero new buys. ⚠️ **STILL BLOCKED on the owner-held `ADMIN_TOKEN`:** his agent is HOLDING and won't retry until we reply (the drafted reply credits his find, corrects his "mints every pass" framing to one-shot, confirms retry is now safe). His retry is spend-safe with OR without our message — but he asked, so he waits. Gate NEW note (non-blocking): a gap-non-compaction utilization nicety (a legacy orphan lands above a free gap rather than compacting) — unreachable for Mordy's single-orphan-at-#0 shape, not a spend inversion. **Open follow-ups (founder/design, NOT folded in):** direction-1 resume-from-resource redesign; C3 (first-buy in-call wait ~2.3s < vendor ~32s registration = every first buy ends in a customer-facing retryable error — reframe or lengthen?); the `replace:`-key COUNT(*) instability sibling (latent, own dated backfill if that COUNT is ever tidied); legacy `'intent'`/`'dangling'` rows still orphaned (C2 limbo); `'retired'` domains still counted in infrastructure_status.
- [x] 2026-08-11 [ORDER] **msgchannel Inc5 — agent→operator direction (RATIFIED by Yaakov 2026-08-11: "Build out their agent messaging us and responding"; build arc dispatched same session):** the channel is one-way today (system/operator → agent via messages[] + list_messages/ack_message); the reverse for AGENTS doesn't exist (humans have support@). Proposal: one MCP tool (`contact_operator`) writing into the existing `support_tickets` D1 table + firing the existing ops-email alert — thin tool over two live systems, completes "customer support built for agents" bidirectionally. Related: wire message-emitted events into the outbound webhook stream so both directions are push, not poll. Founder word starts the arc. **NOW SHIPPED + DEPLOYED + VERIFIED 2026-08-11:** new `contact_operator` MCP tool (27→28 tools, `mcp/tools.ts:370`) + REST parity `POST /messages/contact-operator` (`routes/messages.ts:36`, code-verified); DO-atomic storm guard (5/hr cap, `(body,urgency)` dedup, 10-min ops-email throttle with held-body digest drain, `needs_human` bypasses the throttle but not the cap — code-verified `packages/shared/src/intents.ts:162-176`); untrusted-content fence in the ops email (per-email random tag, server lines outside); `refuseControlChars` (control + bidi-override refusal, `packages/shared/src/intents.ts:156-159`) applied to both `body` and the new `regarding` field (`admin/schemas.ts:58`, code-verified); `regarding` surfaced verbatim to the agent via `actionHint.regarding` (`engine/tenant-messages.ts:125-155`); migration `0017_support_ticket_agent_source.sql` (`support_tickets.source` DEFAULT 'email' + `email_sent_at`, code-verified). Gate arc (frozen `docs/adversarial/msgchannel-inc5-gate-2026-08-11.md`, verified 3-round content this pass): r1 **NO-SHIP** (1 blocking — the guard is a read-modify-write across D1 boundaries, not DO-atomic; 100 parallel `contact_operator` calls from one tenant produced 96 tickets/96 ops emails against a designed 5/hr cap) → DO-local synchronous admission guard → r2 **NO-SHIP** (1 new blocking — the guard commits the D1 admission before the awaited D1 ticket-record write; a partial-D1-failure on the record leg left a committed admission with no ticket, so retries were answered from the phantom: 4/4 subsequent retries returned a phantom `ticketId`, 201, no real ticket, no email) → synchronous compensation (`revokeAdmission`, `contact-operator-guard.ts:196` — deletes the admission row + releases held claims on either D1 leg failing, then rethrows) → r3 **SHIP**, 0 blocking, 1 residual ledgered (see the two `## Open` 2026-08-11 fast-follow entries below). Battery at r3 (adversary-independent re-run): platform 167 files/1561 tests, typecheck ×5 clean, build clean. Merged `main` `0a350cd` (--no-ff, branch `msgchannel-inc5-2026-08-11` preserved, confirmed present). **DEPLOYED + LIVE-VERIFIED THIS BOOKKEEPING PASS:** Worker version `65392316-db03-4587-8191-fdca1a893146` both hosts; independently fetched `coldrig.dev/.well-known/mcp/server-card.json` → 28 tools, `contact_operator` present; `/status` 200 both `api.coldrig.dev` and the workers.dev host. D1 migration 0017 applied remote (per orchestrator's evidence — not independently re-checked via `wrangler d1 migrations list --remote` this pass). Site Pages `5e9df792` --branch main; IndexNow 200 for 15 pages (reported, not independently re-checkable after the fact). This completes the operator→agent direction's mirror: the operator can now RECEIVE agent-authored tickets (`source='agent'`) in the ops digest and REPLY via the existing operator route with the new `regarding` field. **Of the ORIGINAL Inc1-4 channel plan, only Inc4 (email mirror) remains unbuilt** — Inc5 (this entry) is a NEW increment beyond that original plan (agent→operator), not one of Inc2-4 (which were all operator/system→agent and shipped wave-3 2026-08-09, `17e05d4`).
- [x] 2026-08-11 [ORDER] **Inc5 reconcile-sweep for isolate-death — NOW SHIPPED + DEPLOYED + LIVE-VERIFIED 2026-08-11.** A sweep over `agent_contact_log` rows with no matching D1 `support_tickets` row closes the ISOLATE-DEATH window — the guard commits the DO/D1 admission, then the isolate dies before the D1 ticket-record write, and no in-request compensation can catch that (the process is gone before `revokeAdmission` can run). In-house precedent: `spend-ceiling.ts`'s stale-reserve reaper (`RESERVE_REAP_TTL_MS = 15 * 60 * 1000`, `engine/spend-ceiling.ts:55,400`, code-verified — same "flip-then-subtract, gated on the flip" shape, same 15-min TTL). ⚠️ **CORRECTION per gate r3** (`docs/adversarial/msgchannel-inc5-gate-2026-08-11.md:563-568`, verified this pass): this sweep does **NOT** close the concurrent-twin residual (next bullet) — after `revokeAdmission` runs there are ZERO orphan log rows, measured twice (`R3-C3`/`R3-C5`). The sweep only covers isolate-death; that bullet stays open/accepted-risk.
- [x] 2026-08-10 [ORDER] **🔥 P0 — purchased-domain DNS-wait DEADLOCKS Mordy's provisioning (vendor contract fact from InboxKit support reply, 2026-08-10):** support verbatim: *"The domain goauthorpitchdesk.com is now active, and the DNS will be configured during mailbox processing."* The propagation checker belongs to the CONNECT flow only — on purchased domains `dns_propagation_status`/`nameserver_match_status` stay "pending" and `last_nameserver_check` stays null forever (6 days observed live). Our `purchasedDomainIsReady` (`vendors/real/inboxkit-domain-port.ts:411`, wave-1 design) requires BOTH verdicts → permanent DNS-wait, retry never progresses. Fix = for `connection_type='purchased'` + `status='active'`, proceed to mailbox creation without the propagation verdicts (vendor configures DNS during mailbox processing); `connected`/`unknown` keep the wait. Billing safety unchanged (bill-only-on-vendor-confirmed). Fix arc dispatched this session; after ship, emit wire-A `retry_setup` to Mordy's tenant. **NOW SHIPPED + DEPLOYED 2026-08-10 (verified this pass):** fix commit `bc1a927` (branch `fix/purchased-dns-wait-2026-08-10`) adds the short-circuit inside `polledDomainIsReady` (function renamed from `purchasedDomainIsReady`; `vendors/real/inboxkit-domain-port.ts:453-455`, code-verified: `if (connectionType === "purchased") return true;` gated on `status==='active'`) — `connected`/`unknown` still require both propagation verdicts, code-verified unchanged. RED-proven: deleting the one added line fails 4/24 tests on Mordy's exact live shape; restoring returns 24/24. Adversary gate SHIP, 0-blocking, 1 round (frozen `docs/adversarial/purchased-dns-wait-gate-2026-08-10.md`) — independently re-ran the battery: platform 166 files/1528 tests, typecheck ×5 clean (the one pre-existing red, main's HANDOFF tool-count doc guard, was separately fixed on main `eca06a7` and folded in via `d0dd932`, both on this branch). Merged `main` `bfce3a9` (--no-ff). Deployed Worker `e2e9f061-e7e0-4999-bdb3-258d36582002` (confirmed latest via `wrangler deployments list`, created 2026-08-10T15:38Z) — live-verified this pass: `/status` 200 `{"status":"ok"}` both hosts, `/account` 401. No flag, no migration — the fix arms on deploy alone; Mordy's pre-existing NULL/`'unknown'` `connection_type` row self-heals via the existing backfill (`domain-dns.ts:88-114`) on his first post-deploy retry (gate-traced: NULL → `'unknown'` → `resolveDomainConnectionType` asks the vendor, gets `"purchased"`, persists it, takes the new branch same call). The wire-A `retry_setup` operator message to Mordy's tenant is FOUNDER-GATED — it needs the prod `ADMIN_TOKEN` (owner-held secret; confirmed no local copy exists — `apps/platform/.dev.vars` carries only a placeholder value); a paste-ready curl was delivered in-chat 2026-08-10. Mordy's agent self-retries against the fixed gate independent of whether that message is ever sent. Three non-blocking gate findings + one out-of-scope NEW item ledgered as their own `## Open` bullets below (2026-08-10, items a-f).
- [x] 2026-08-09 [ORDER] **InboxKit support ticket SENT (founder click #1, executed with founder authorization 2026-08-10T01:35Z)** — emailed support@inboxkit.com from yaakovscher@gmail.com via browser session, verified in Gmail Sent (subject "Purchased domain stuck on pending nameserver check (goauthorpitchdesk.com) + OAuth scope question"). Covers the NS-checker kick AND the Inc-F OAuth scope question. New evidence cited in the ticket: the InboxKit dashboard shows NS Status "Matched" for goauthorpitchdesk.com while the API still serves pending/last_nameserver_check=null — the vendor's stored-field checker is stuck, not the delegation. Durable dashboard login saved to playwright profile `inboxkit-coldrig` (email-code flow; Google SSO dead-ends on TOTP headless). Also: Stripe webhook spot-check (founder click #2) is MOOT until a real delivery — zero Stripe events of ANY type since the wave-2 deploy (API-verified 2026-08-10T01:20Z); the first real `invoice.payment_failed` doubles as the readDeclineCode live-verify.
- [x] 2026-08-06 [ORDER] **Inc-D ARM-BLOCKING handoffs (from the Inc-C build report, 2026-08-06):** (1) the provider WRITE PATH at insert (§1a) — `insertProvisionedMailbox` must record `provisioned.provider` and `connectByoMailbox` must write 'byo'; without it any mailbox provisioned AFTER a tenant's clock flip gets provider='' and Inc-D's picker makes it PERMANENTLY send-ineligible, silently. (2) tick.ts:176's hoisted `now` is broader than N7's wording — it also drives the due-window check + first_send_eligible_at comparison after awaits; fix all three uses at the claim. (3) clock-provenance guard test (no `new VirtualClock(` outside blessed sites) unbuilt. (4) NEW-4 unchanged (retired sandbox rows still feed gatherMailboxHealth; sandbox domains rows unretired). Plus the standing Inc-D list: picker predicate + R3 scope + R4 shared predicate + §1c alerts + U2 provenance array + N6 + §5 budgets/rotation + scheduled.ts reorder. **Separate residual (route to rebuy/integration): a non-finite startWarmup parse now throws NON-retryable (retryable would re-enroll a BILLED warmup subscription per retry — builder's correct deviation from the design), leaving a created-but-unmarked subscription; proper close = reorder the marker write in mailbox-provisioning.ts (rebuy lane's file).** **NOW SHIPPED 2026-08-06 (verified in code):** (1) provider write path confirmed — `insertProvisionedMailbox` writes `provisioned.provider` (`mailbox-provisioning.ts:488`), `connectByoMailbox` writes literal `'byo'` (`byo-mailbox-composition.ts:115`). (2) tick.ts's tick-start `now` fix landed, explicitly noted WIDENED per this audit (`tick.ts:258` comment cites "N7 ... WIDENED by the Inc-C audit"). (3) `clock-provenance.test.ts` exists; only 2 `new VirtualClock(` construction sites remain in source (`tenant-do.ts:221`,`:518`, both blessed). (4) NEW-4 landed with the positive-evidence correction (`clock-migration.ts:156-188`: sandbox retirement now requires proof of sandbox-era origin, not mere absence). Folded into wave-2 integration (`f7ee786`), deployed `8187d36e`.
- [x] 2026-08-06 [ORDER] **Wave-gate non-blocking follow-ups (frozen `docs/adversarial/wave-integration-gate-2026-08-05.md` #2-#4):** (#2) wire A's `retry_setup` message hard-codes DNS prose while wave1's `awaitMailboxReady` retryable throw lives in the same try — REST body says step "mailbox purchase" while the agent message says "DNS registration is still completing"; make the message step-aware (fold into msgchannel Inc2 or a fast-follow; executed-proven contradiction of two customer surfaces). (#3) `mailbox_intents` at status 'intent' with the vendor already holding the mailbox → second paid buy on a narrow kill-window (kill between accepted buy and status write); the naive ask-vendor-at-'intent' fix has its own hole — design with care, pairs with the re-buy ASK below. (#4) the vendor-name source tripwire globs `../src/**` only, missing `packages/shared` whose error messages `error-response.ts` returns verbatim — currently clean, widen the glob. (R2-NEW) drop `"active"`/`"ok"` from READY_STATUS_TOKENS — in a propagation field those plausibly mean in-progress; not causally reachable like #1 was, but cheap. **Ready-token live check SATISFIED pre-deploy (2026-08-06): the workspace's propagated domain (dmhadvisor) reads dns_propagation_status="propagated" / nameserver_match_status="matched" live — both in READY_STATUS_TOKENS — so the gate's stall-risk directive is closed by observation; goauthorpitchdesk will read ready when the vendor flips the same field.** **NOW SHIPPED 2026-08-06 (verified in code):** #2 `retry_setup` message is step-aware (`tenant-messages.ts:170`, `provisioning.ts:540` comment confirms). #3 the 'intent'-status double-buy hole closed via the guarded re-buy's verify-at-vendor-before-rebuy dispatch tracking (`mailbox-provisioning.ts` intent/dispatch logic, same lane as the re-buy ruling below). #4 vendor-name tripwire glob widened to include `packages/shared` (`test/vendor-identity-leak.test.ts:34`). R2-NEW `READY_STATUS_TOKENS` no longer contains `"active"`/`"ok"` (`vendors/real/inboxkit-domain-port.ts:368-378`, confirmed 9 tokens, neither present). Folded into wave-2 integration `f7ee786`, deployed `8187d36e`.
- [x] 2026-08-06 [ORDER] **Vendor-accepts-never-creates mailbox wedge — FOUNDER RULED (in-session AskUserQuestion): auto-re-buy YES, ONCE, WITH GUARDS** — before any re-buy the system must confirm at the vendor that the first purchase produced NOTHING; max ONE automatic retry per intent; founder alert fires either way. Build = attempt cap + verify-at-vendor + single guarded re-buy + alert, designed together with wave-gate follow-up #3 (the 'intent'-status double-buy window — the naive ask-vendor-at-intent fix has its own hole, per the gate). Lane DISPATCHED 2026-08-06. **NOW SHIPPED 2026-08-06:** guarded re-buy built — durable dispatch-claim table `mailbox_buy_dispatches` (`schema.ts`, `provision-intents.ts`) tracks attempts, verifies absence at the vendor before the one authorized automatic re-buy, alerts either way (`mailbox-provisioning.ts:255-290`). Folded into wave-2 integration (`rebuy-guarded-2026-08-06` lane `bb48a31`), merged `f7ee786`, deployed `8187d36e`.
- [x] 2026-08-05 [ORDER] **P0 — Dunning sweep FAIL (3 blocking, cron LIVE, frozen `docs/adversarial/audit-dunning-2026-08-05.md`):** F1 `runDunningSweep` is the ONLY per-tenant sweep in `ops-sweep.ts` (`:42-65`) with NO try/catch — siblings `runDeliverabilitySweepAllTenants`/`runWarmupCancelSweepAllTenants`/`runWebhookDeliveriesAllTenants` (`:141`/`:180`/`:208`) each wrap the body — so one wedged tenant DO aborts the whole 5-min cron sequence platform-wide (watchtower/webhooks/spend-reaper/sdnRefresh/sdnRecovery all skipped that tick); fix: wrap the per-tenant body like its 3 siblings + wrap `runScheduledOpsSweep`'s legs. F2 the idempotency guard row (`insertDunningEventIfNew`, `applied=true`) commits BEFORE `suspendForDunning()` (`ops-sweep.ts:51-63`) — a crash/RPC-failure between them leaves the dunning_events row committed but the tenant unsuspended, and since `cycle` freezes once Stripe exhausts its ~4 retries, the suspend never gets retried — permanent; fix: apply the suspend before recording the guard row, or add a reconcile pass. F3 `suspendTenant` (`engine/ops-summary.ts:69-75`) is an unconditional `UPDATE tenant_profile SET status='suspended' ... WHERE id=?` — no `WHERE billing_state='past_due'` re-check — so a recovery webhook landing in the read(`opsSummary`)/write(`suspendForDunning`) race gap gets clobbered: a PAYING customer suspended + emailed "suspended for non-payment," sending frozen, no self-heal (permanent-decline codes trigger this at cycle 1, no need to reach count 4); fix: conditional `UPDATE ... WHERE billing_state='past_due'`, the same atomic-conditional-UPDATE pattern already used by `resolveScreeningReview` + the spend-ledger reserve. Severity: the cron is ARMED and LIVE (`wrangler.toml:107-108` `crons=["*/5 * * * *"]`, despite `scheduled.ts:4-6`'s "commented-out" comment being STALE) but there is NO current victim (no tenant is past_due; Mordy is paid+active) — P0 = land the fix before ANY tenant's next failed invoice, not after one forces it. Also fix the 2 stale comments (`scheduled.ts:4-6`, `admin/dunning.ts:4`/`:39` — both misattribute the ACT half to `routes/admin-ops.ts`; it's actually `admin/ops-sweep.ts`). **NOW SHIPPED + DEPLOYED 2026-08-06:** fix committed `8fd578e` (RED-proven x5), merged to `main` `c90b972` (--no-ff, lane branch `fix/dunning-p0` preserved), deployed Worker `bb8dc830-4785-4e58-a99c-74a05841fb2e` (confirmed latest via `wrangler deployments list`), combined-diff adversary gate SHIP (`docs/adversarial/wave-integration-gate-2026-08-05.md`). Live-verified: `/status` ok both hosts, cron `*/5 * * * *` intact (`wrangler.toml:108`).
- [x] 2026-08-05 [ORDER] **Msgchannel Increment 1 (system→agent message channel) — built, gated SHIP-AFTER-FIXES, `[dark-unarmed, UNCOMMITTED]`:** full-suite-green (133 files/1215 tests, tsc clean) + both wires (`retry_setup`/`credential_ready`) behaviorally tested; adversary gate frozen `docs/adversarial/msgchannel-inc1-gate-2026-08-05.md` — **1 BLOCKING** (Finding 1: on a same-idempotency-key retry, wire A's `retry_setup` message names + dedups on the WRONG domain — a fresh loop candidate the resume branch never actually touches — so the customer-facing message can claim a domain that was never bought; the test blind spot is a single-candidate fixture that short-circuits via `ValidationError` before ever reaching the wire-A catch) that must land BEFORE deploy, plus **1 non-blocking** (Finding 2: `credential_ready` has no tenant-lifecycle gate — a canceled/suspended tenant can get a false "sending is now enabled"). Source is UNCOMMITTED in the working tree — deliberately NOT on main, NOT deployed, to avoid shipping a phantom-domain customer message on an unrelated deploy. Increments 2-4 (operator route, list_messages/ack_message tools, email mirror) NOT built. Resume: fix Finding 1 → re-verify → commit → re-gate clean → deploy. **NOW SHIPPED + DEPLOYED 2026-08-06:** gate fix (Finding 1 + Finding 2) committed `a9f57a4` (RED-proven both), merged `c90b972`, deployed `bb8dc830-4785-4e58-a99c-74a05841fb2e`, combined-diff gate SHIP. Increments 2-4 remain NOT built — founder-authorized full channel, queued as their own arc (see the msgchannel IDEA entry below / `HANDOFF.md`). **[STALE, corrected 2026-08-11]:** Inc2+Inc3 shipped wave-3 2026-08-09 (`17e05d4`); a NEW Inc5 (agent→operator, `contact_operator`) shipped 2026-08-11 (`0a350cd`, see the `## Open` 2026-08-11 entry above). Only Inc4 (email mirror) remains unbuilt.
- [x] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE 1 — code-only, unblocks PROVISIONING (root cause: the happy path was only ever tested against an always-succeeds sandbox → async/timing/driver/clock realities never exercised; Mordy's domain is recoverable IN PLACE — live-confirmed vendor status="active", adopt-before-buy recovers it, no re-provision/no re-charge). Frozen: `docs/adversarial/{sweep-domain-type,sweep-vendor-leak,forward-path-audit}-2026-08-05.md`.** (a) branch `setDns` on `connection_type` purchased-vs-connected — skip the connect-existing NS flow for purchased, poll status/propagation instead (carry `connectionType` through `OwnedDomain` + record on the domains row — it's fetched at `listOwnedDomains:243` then DROPPED); (b) L1 gate `dns_status='ready'` + mailbox buy on REAL propagation flags, not on setDns merely not throwing; (c) L2 poll-until-listable/ready gate between mailbox buy and every `resolveMailboxUid` consumer + real buy-idempotency (record the bought email BEFORE `startWarmup` so a retry resumes at warmup, never re-buys — the `/already-exists/` handling was removed at gate c, and `idempotency.ts` deletes the claim on throw → today's retry re-buys/double-charges); (d) stop retryable-laundering — `setDnsWithRetry` must preserve a permanent VendorError as non-retryable; (e) L7 billing: don't count/bill a row whose vendor provisioning didn't complete; (f) vendor-identity abstraction — `customerSafeVendorFailure` abstract-label MAP replacing `CUSTOMER_SAFE_STEPS`'s raw endpoint tokens; store abstract labels in `logAction`/`vendorHealthError`/the tick failed-send event, never `err.message`; (g) honest errors — restore step + retryability signal, vendor-blind. Tests MUST exercise REAL async/purchased-domain vendor behavior (a stubbed-success fixture is what let the incident hotfix gate pass falsely) + the vendor-leak `?raw` tripwire. RED-proof, adversary gate against real entry points, deploy. **⚠️ GATE ATTACK VECTOR (live retry capture 2026-08-05, Mordy's agent):** his `domains` row was recorded by the adopt path BEFORE any connection_type discriminator existed (the local table has NO such column today; `registrar:"adopted"`) — the fix's purchased-vs-connected branch MUST correctly classify pre-existing rows (backfill or live vendor re-fetch at DNS-step time), or his post-deploy retry fails identically. His agent independently confirmed the class ("any tenant whose domain came through the registration path"). Retry at ~02:30Z returned the same vendor_error as expected; domain still safe (1 domain active, 0 mailboxes, no double-charge). **Correction to record:** the incident-hotfix-gate's "32s race" premise (`docs/adversarial/incident-hotfix-gate-2026-08-05.md`) was a MISDIAGNOSIS, and its round-2 proof stubbed `setDns` to succeed on a purchased domain — a fixture-unreal pass; the fix wave must not re-trust that gate. **Note:** DUNNING P0 (entry above) is independent of provisioning and stays its OWN P0, not folded in here, though it shares the cron surface (see L8 correction below). **NOW SHIPPED + DEPLOYED 2026-08-06:** fix committed `00a6230` + gate-fix #1 `8e55bfc` (DNS false-ready — `purchasedDomainIsReady` now reads the vendor's own verdicts only), merged `c90b972` (lane branch `fix/provisioning-wave1` preserved), deployed `bb8dc830-4785-4e58-a99c-74a05841fb2e`, adversary gate SHIP after 2 rounds (r1 SHIP-AFTER-FIXES 1 blocking → fix `8e55bfc` → r2 SHIP, `docs/adversarial/wave-integration-gate-2026-08-05.md`). The GATE ATTACK VECTOR note above (pre-existing-row classification) stays live-relevant — closed by the gate's ready-token live check (workspace's propagated domain reads both READY_STATUS_TOKENS live), confirm again on Mordy's actual retry rather than treating the gate alone as proof.
- [x] 2026-08-05 [ORDER] **PROVISIONING FIX WAVE 2 — unblocks SENDING; 3 founder decisions RULED 2026-08-05 (gate lifted, see end of entry).** L3 OAuth mint (manual per-mailbox today; programmatic `InboxKitOAuthMinter` is an UNVERIFIED shape-guess, not wired; `showMailboxCredentials` also unverified) · L4 wire a prod driver for `runTick` (verified 2026-08-05: only reachable via the DO RPC method `tenant-do.ts:821` and the demo path — zero cron/route/MCP-tool/alarm callers; `scheduled_sends` rows never drain in prod; wiring it ALSO arms auto-send) · L5 fix the frozen VirtualClock for paid tenants (real wall-clock — cascades: warmup never ramps, `sent_today` never resets → ~25 lifetime sends then send-dead, warmup pool bills forever; L4+L5 MUST ship together) · L6 wire a prod driver for `runPollInbox` (verified 2026-08-05: same DO-RPC-only reachability as L4, `tenant-do.ts:825` — replies never fetched). **L8 CORRECTED (bookkeeper-verified 2026-08-05, contradicts forward-path-audit.md's L8 as written):** the cron trigger is ALREADY live, not commented out — `wrangler.toml:107-108` reads `[triggers]` / `crons = ["*/5 * * * *"]` and `index.ts:166`'s `scheduled()` calls `runScheduledOpsSweep` unconditionally every 5 min (matches the dunning P0 entry's own "cron is ARMED and LIVE" finding above); dunning/warmup-cancel/webhooks/digest/watchtower/deliverability/spend-reap/sdn-refresh all already run live. `reconcileMailboxCredentialPushes` is ALSO already wired — it runs inside the deliverability leg every 5 min (`tenant-do.ts:864`, config-gated no-op until the programmatic minter arms), not a separate arm-time blocker. Only the header comment in `scheduled.ts:1-6` ("the actual crons=[...] trigger is commented-out") is stale — the trigger itself isn't; do not repeat that claim. The only genuinely missing production drivers remain L4 and L6. CREDSTORE F1/F2 (frozen `docs/adversarial/audit-credstore-2026-08-05.md`, overall PASS) stay in this wave: F1 (stale keyless push reverts a rotation — the i3i4-dropped-idempotency-key regression, content-hash dedup only, no monotonic/version check) must fix BEFORE the programmatic OAuth minter arms; F2 (teardown doesn't tombstone `mailbox_cred_pushes` rows so a post-cancel reconcile can resurrect a revoked credential) — mark rows terminal on teardown. Neither F1 nor F2 is Mordy-reachable today (his path is the deterministic manual mint). **FOUNDER RULED 2026-08-05 (in-session, captured at mention time):** (1) clock = REAL wall-clock for ALL non-sandbox tenants (founder verbatim: "Why not get a real clock for any non sandbox"; VirtualClock stays sandbox/demo-only — broader than the paid-only framing); (2) auto-send = BUILD + ARM NOW (driver design cron-vs-DO-alarm delegated, adversary-gated before arm); (3) OAuth = BOTH in parallel (manual mint for Mordy the moment his mailboxes exist + independently live-verify the programmatic minter on a throwaway so customer #2 needs no manual step). Build order: after Wave 1 merges (same-file collision). **DESIGN ARC COMPLETE 2026-08-05:** design v1 (Plan lane) → adversary R1 **NO-SHIP 8 blocking** (headline: arming as-designed would have routed Mordy's first campaign to demo-era sandbox mailboxes and drained it to 'failed' in ~25 min; plus double-shift migration corruption, in-flight-saga clock hole, cron-leg starvation) → v2 delta (per-mailbox eligibility + provider provenance column + transactionSync migration w/ persisted delta + DelegatingClock + budget/rotation + tombstone-first/engine-seq-tombstone) → adversary R2 **SHIP-AFTER-FIXES, 0 blocking** (all 8 re-derived closed; transactionSync verified real at workers-types :753). Frozen: design+delta `archive/2026-08-05-provisioning-fixwave-plan/wave2-design-2026-08-05.md`, verdicts R1+R2 `docs/adversarial/wave2-design-review-2026-08-05.md`. **Build briefs MUST fold in R1-R8 + NEW-4** (R1 record-row-above-deps-guard; R2 warmup-cancel.ts:58 stale-now — the six-site enumeration; R3 tenant scope in NOT EXISTS; R4 shared eligibility predicate picker+alert; R5 budget-vs-180s-timeout ordering ladder; R6 rotation zero-guard; R7 tombstone runbook note + retention; R8 provider='' rollback coupling note; NEW-4 retire sandbox domains rows + released_at filter in gatherMailboxHealth) + U1/U2 build-lane probes (U1 constructor-context transactionSync + NaN bind; U2 pre-arm Mordy provenance read incl. every provider='google' row has a pushed credential). **NOW SHIPPED 2026-08-06 — built whole:** L4 (auto-send cron+DO driver) and L6 (poll-inbox driver) + L5 (real wall-clock for all non-sandbox tenants) SHIPPED + ARMED live (Worker `8187d36e`, kill switch `AUTOSEND_DISABLED` confirmed wired in `env.ts:210`/`admin/ops-sweep.ts:378`, unset = armed). Credstore F1 (pushSeq ordering) + F2 (teardown tombstones) shipped both sides. L3 OAuth mint PARTIAL — the manual per-mailbox path is live (unchanged); the programmatic `InboxKitOAuthMinter` verify-plan is still pending founder asks (scope answer via the InboxKit support ticket). Merged `f7ee786` (--no-ff, lane branches preserved), deployed `8187d36e`, combined-diff gate NO-SHIP r1 (2 blocking, unrelated to this entry's L3/L4/L5/L6/credstore scope) → fixes → SHIP r2 (`docs/adversarial/wave2-integration-gate-2026-08-06.md`).
- [x] 2026-08-02 [ASK] **⚠️ NO production driver calls tick() — CAMPAIGN SENDS have no scheduler (adversary A1 adjacency, PRE-EXISTING, load-bearing):** wave-gate verdict (docs/adversarial/warmup-wave-final-review-2026-08-02.md A1 note) enumerated exhaustively: runTick is a B0 directly-callable RPC awaiting B2; prod cron (scheduled.ts) drives deliverability/dunning/digest/watchtower/webhooks ONLY; no route/MCP tool/DO alarm reaches tick(). scheduled_sends rows drain ONLY via runTick ⇒ real campaign sends never fire automatically in production. Not this wave's regression (B2 backlog, ROADMAP:121) — but it must be designed + armed BEFORE Mordy's first campaign or his sends sit queued forever. The A1 fix wires ONLY the warmup-cancel sweep into cron (deliberately not full tick — arming campaign auto-send is its own founder-gated arc). NEEDS: founder-ratified design (cron-driven per-tenant tick vs DO alarms) + adversary gate + arming. **NOW SHIPPED + ARMED 2026-08-06:** the cron leg now drives `runTick` via the wave-2 auto-send drivers (5-min cron leg + DO-side 4-leg gate + per-mailbox eligibility, kill switch `AUTOSEND_DISABLED` — `env.ts:210`, `admin/ops-sweep.ts:378`). Merged `f7ee786`, deployed `8187d36e`; first post-deploy cron cycle observed quiet-correct (orchestrator-reported via `wrangler tail`: `runScheduledPoll`→`runScheduledTick` "Ok" fleet-wide, zero errors, zero unexpected sends, no alert storm — not independently re-verified this pass, the live cycle had already elapsed by the time this ledger update ran).
- [x] 2026-08-02 [ORDER] **External LLM site audit (founder-supplied 08-02) — CLOSED 2026-08-02.** All 5 confirmed factual defects FIXED + DEPLOYED in the warmup-claims wave (commit `86996ef`, site Pages deployment `09b8deb2`, adversary R2 SHIP `docs/adversarial/warmup-wave-final-review-2026-08-02.md`): security-page "paid mutations remain disabled"/"future activation" residue rewritten · `/AGENTS.md` 301→200 redirect fixed via `site/_redirects` · sitemap lastmod bumped 35/35 · version-string skew closed (server 0.2.2 / CLI 0.2.1, single-sourced) · workers.dev-vs-api.coldrig.dev split resolved (`api.coldrig.dev` now the primary/documented host, workers.dev retained). Tool-count and stale-language claims ("12/17 tools", "sandbox only", "not live") were investigated and found FALSE — no site surface actually carries stale copy; no action taken. REMAINING OPEN (not this gate): positioning/copy-redesign recommendations (above-fold rewrite, qualification section, pricing lead, app.coldrig.dev move, single source-of-truth status object) staged for founder ratification, not auto-applied — status object also re-ledgered as an IDEA below; GSC/Bing recrawl resubmission is a founder click (IndexNow already fired HTTP 200 post-deploy).
- [x] 2026-08-02 [ORDER] **Warmup = bundle + AUTO-CANCEL after ramp (founder RULED 08-02, option b) — SHIPPED + DEPLOYED 2026-08-02.** Build: `apps/platform/src/engine/warmup-cancel.ts` (146 lines, cron-swept auto-cancel at ramp completion/day-29 transition) + `apps/platform/src/scheduled.ts` cron wiring, commit `e4250e9`; claims coupling honored — site copy (warmup lifecycle truth, exact ramp schedule 5/15/25/35/40) shipped in the SAME deploy, commit `86996ef`. Adversary provenance: round-1 NO-SHIP (3 blocking) → round-2 fix wave NO-SHIP (2 blocking: A1 no-production-driver for campaign sends [now tracked separately, see `## Open` 2026-08-02 ASK], A2 per-tenant-ceiling overclaim) → fix round → **R2 SHIP**, all frozen `docs/adversarial/{warmup-claims-fix-review-2026-07-31,warmup-wave-final-review-2026-08-02}.md`. Battery at ship: platform 125 files/1157 tests, typecheck clean ×5 workspaces (adversary-re-run, not builder-claimed). Deployed + live-verified: Worker `dbc4ae45-dc63-4b63-9455-51e8ffafc09f` (both hosts, cron intact; `/account` 401, `/checkout/success` 302, `/app` 200, `tools/list` 25, `serverInfo` 0.2.2); site Pages deployment `09b8deb2` --branch main (security-page + faq warmup copy live, `/AGENTS.md` 301→200, server-card 0.2.2, sitemap 35 lastmod, zero hits of the old "AI-driven layer" fiction); IndexNow fired HTTP 200 post-site-deploy. Non-blocking round-2 residuals (R1-R4, N-b) NOT part of this ship gate — see the new `## Open` fast-follow entry below.
- [x] 2026-08-16 [ORDER] **Alert-policy tune — SHIPPED + DEPLOYED 2026-08-16.** Adversary gate SHIP, 0 blocking, 6 non-blocking (`docs/adversarial/alert-policy-gate-2026-08-16.md`, both spec-conflict resolutions UPHELD). Merged `--no-ff` `a981163`. Deployed Worker `a507f12a`; migration 0018 applied to prod D1 (backfill verified: the 2 stuck Mordy `domain_dns_aging` rows carry `unhealthy_obs=2, alert_count=2`, `last_alert_ts` only 1.05h old pre-deploy so no deploy-day duplicate email, next re-alert ~23h out then daily). Live-verified `/status` 200 `sweepAgeSeconds=321`, `/admin/ops/checks?unhealthy=1` byte-shape unchanged. Founder alert volume: ~8/day → ~2 on day one → 2/day (one per stuck domain) until Mordy's agent retries; dead-man ≤15min guarantee untouched.
- [x] 2026-07-28 [ORDER] **Warmup = platform-managed ramp, INCLUDED free / first live warmup ramp watch (merged; originally two entries — this one and "First live warmup ramp watch" in `## Open`) — VERIFIED LIVE 2026-08-19.** DoD was: caps enforce at the engine boundary, `warmupDay`/`dailyCap` surface correctly in `infrastructure_status`, the health-loop throttle path is reachable, and the InboxKit warmup-NETWORK add-on gray-zone gets a founder posture ruling. All four closed: (1) the 2026-08-18 Mordy-incident ticket read LIVE platform state showing `mailboxHealth` with `mordytee11 (warmupDay 1, cap 5, healthy)` — `warmupDay`/`cap` surfacing confirmed correct on real data (`ROADMAP.md` 2026-08-18 Mordy-tickets entry); (2) Mordy's mailboxes have been actively warming/sending since 2026-07-28 (now 3+ weeks) with zero caps-related incident anywhere in the ledger — the throttle path has been live-exercised without destabilizing; (3) the warmup-NETWORK add-on posture WAS ruled by the founder 2026-08-02 (option b: platform auto-activates it on every provisioned mailbox, bundled into price, auto-cancels at ramp completion day 29) and BUILT + DEPLOYED the same day (`e4250e9`, `warmup-cancel.ts` cron sweep — see `MEMORY.md` "2026-07-28 standing lessons" section, last bullet). Original two entries' full text preserved above this line for provenance.
- [ ] 2026-07-28 [ORDER] **Warmup = platform-managed ramp, INCLUDED free (founder 2026-07-28: "platform is officially offering this free... we gotta do it"):** the ramp engine (`apps/platform/src/engine/warmup.ts` — `warmup_started_at` → `warmupDailyCap` by age, `sent_today` metering, deliverability throttle/pause) is BUILT + tested, NEVER live-proven; Mordy's mailboxes are the first real run. DoD: verify caps bite at the engine boundary during his week 1 (ledgered as "first live warmup ramp watch" in `## Open`). InboxKit's $3/mbx warmup-NETWORK add-on stays shelved (gray-zone; research lane running 2026-07-28 to rule on posture). Ramp-only has no friendly-recipient engagement signal — that gap is exactly what the warmup research + prewarm SKU address.
- [ ] 2026-07-28 [ORDER] **First live warmup ramp watch:** Mordy's mailboxes are the FIRST real senders through the warmup engine (warmup.ts ramp: warmup_started_at → warmupDailyCap by age, sent_today metering, deliverability throttle/pause on top — built+tested, never live-proven). At his first sends: verify daily caps enforce at the engine boundary, warmupDay/dailyCap surfaces read correctly in infrastructure_status, and the health loop's throttle path is reachable. Part of the arming checklist.
- [x] 2026-08-19 [ORDER] **CUSTOMER-CONTINUITY wave SHIPPED TO PRODUCTION.** I1-I15 + 4 fix rounds; lane `feat/customer-continuity-2026-08-18` (7 commits `5165f58..ffccdc7`, preserved) merged `--no-ff` `cf9b323`; post-gate ordered cleanups `0cc9feb` (r5 N4 comment correction + N5 unused-export drop). Build gates (`docs/adversarial/customer-continuity-build-gate{,-r2,-r3,-r4,-r5}-2026-08-19.md`): r1 SHIP-AFTER-FIXES (2 blocking) → r2 SHIP-AFTER-FIXES (1 blocking) → r3 NO-SHIP (2 blocking) → r4 SHIP-AFTER-FIXES (1 blocking, NEW-1 class) → r5 SHIP (NEW-1 class closed 3/3, incl. the pre-existing `domain_dns_incomplete` member). Merged-tree battery GREEN at `0cc9feb` (verifier): typecheck 0; platform 211 files/2063 passed/1 skipped; dashboard 29/143; engine 17+2 skipped/140+4 skipped; cli 12/12; wrangler dry-run bundle clean. Deployed 2026-08-19 ~22:00Z: Worker `319e15c4-1de7-4690-be8b-61a68082a226` live on both hosts (api.coldrig.dev + agent-cold-email-api.yaakovscher.workers.dev, `/status` ok both, cron */5 intact); site Pages deployment `40fe9f0c` (project `agent-cold-email`, branch main, manual deploy — the Pages project is NOT git-connected); live `coldrig.dev/openapi.yaml` byte-identical to committed, `ordinal_slot_shortfall` enum on the wire. Live-verified on `ten_91aab24a`: persona backfill worked in production (both `domainIntents` now `personaSlug:"mordytee"`, was NULL pre-deploy — r1 BLOCKING-1's recovery verified on real data); `inboxesEach` still NULL both intents (deliberate no-backfill abstention holding, see the legacy-NULL Open item below); zero unhealthy checks. Findings from the design gate + all 5 build-gate rounds routed to fresh 2026-08-19 `## Open` entries below (fill-remedy re-buy, domain-liveness mismatch, burned-ordinal-no-reason, legacy-NULL closure tie, N1-N3 pricing/guard notes, `billClaimSentence` anchor bug, withheld-remedy persona-split, r2-gate leftovers, emit-coverage guard idea, post-deploy watch items). Post-deploy confirmations still pending, tracked in the watch-items entry below, not claimed done here.

## Archived 2026-08-23 — cleanup pass (founder ORDER 2026-08-23)

- [x] 2026-07-13 [dark-unarmed] B3 — `VendorPort` layer: fault-injecting sandbox impls (active) + real impls (Porkbun/Inboxkit/Stripe — coded, unactivated); one contract-test suite across both; free/demo tenants structurally can't reach a real adapter (type guard + failing test). ✅ evidence: INBOXKIT_API_KEY/WORKSPACE_ID armed since 2026-07-24 (L58); Stripe live keys flipped (L110); demo type-guard test in place
- [x] 2026-07-13 C5 (remaining half) — npm publish DONE + official MCP Registry DONE (see 2026-07-14 entries below). Smithery/mcp.so/PulseMCP + awesome-list PRs: covered by the standing registry/directory authorization, cascade running (see below) — results pending. The `/.well-known/mcp/server-card.json` portion is already served. ✅ evidence: L183/L184 submissions; L30 mcp.so issue #3602 + PulseMCP auto-index; registry v0.2.3
- [x] 2026-07-13 [ASK] D3 — Legal: BUILT+VERIFIED IN-TREE 2026-07-14, [gated:founder] on the deploy batch — full clause inventory now met: NEW `site/dpa.html` (9 sections, code-grounded termination handling incl. the deliberate suppression-permanence carve-out `lifecycle.ts:269-270`), terms §8 strengthened to ROSCA-grade (named statute, disclosure timing, concrete cancel mechanism `POST /cancel` + support@), privacy §4 links /dpa, aup reviewed-still-correct, DPA footer-linked on all 4 pages + sitemap. All DRAFT-flagged. Enforcement map produced (11 clauses: 7 BUILT w/ file:line + tests, 2 N/A-legal, 2 PARTIAL → follow-up entries below). Evidence: parse5 0-errors ×4, tag-balance ALL_OK, 8 screenshots `pw-shots/legal-2026-07-14/` (DPA desktop + terms mobile eyeballed by orchestrator). REMAINING: founder deploy-go, then attorney review (the real legal gate). ✅ evidence: site/terms.html:75 cited live by L237; L164(f) 'RULED NOT A LAUNCH GATE by Yaakov 2026-07-15' (45a8d13)
- [x] 2026-07-13 [gated:founder] D4 (remaining half) — OFAC screen + friction ladder + new-tenant volume ramp; needs real signups to exercise. (Per-tenant physical address + sender identity capture is already done in `setup_infrastructure`.) ✅ evidence: 32d5b6b OFAC SDN screening; review-queue-on-hit via watchtower (L110 item 4); a8a0b117 deployed
- [x] 2026-07-14 [ORDER] Mordy-pilot activation lane (Yaakov: "get coldrig ready + have him migrate"): critical path UPDATED 2026-07-14 (again) — engine COMMITTED (`eb8ee42`, R3 SHIP) → `ENGINE_TENANTS` allowlist BUILT + adversary SHIP + COMMITTED (5 guards, 21 tests, suite 284/284; frozen verdict `docs/adversarial/engine-tenants-allowlist-review-2026-07-14.md`; revocation-needs-DO-restart note carried in ACTIVATION Gate 2) → **now: droplet provisioning (FOUNDER — runbook CLEARED in ACTIVATION Gate 2)** → set `ENGINE_TENANTS` + arm per Gate-2 residuals → Gate-1 smoke (~$6/mo, runbook in ACTIVATION.md Gate-2) → `ENGINE_TENANTS` per-tenant allowlist build → Gate-1 smoke test on OUR test domain → BYO-domain+BYO-mailbox intake (his authorpitchdesk.com + Google Workspace boxes) → migrate as tenant #1 (comped/test-billing, no live Stripe needed). Founder 2026-07-15: droplet spend + test-domain purchase + FULL DEPLOY BATCH **AUTHORIZED** ("you have my permission… you can deploy everything") — **both now EXECUTED**: droplet `coldstart-engine` PROVISIONED (doctl-confirmed: ID `584813711`, IP `142.93.12.85`, nyc1, firewall `coldstart-engine-fw` 22+8080, container Up, `/health` `{"status":"ok"}`, unauth `/v1/send` 401; NOT armed — no creds, no tunnel, no Worker secrets, no `ENGINE_TENANTS`), deploy batch live-verified (see design-lane entry above). Mailbox-provider question RESOLVED — no third decision existed: Mailforge stays the DECIDED managed-path vendor (ACTIVATION Gate 0), Mordy rides BYO (§20) on his existing Google Workspace boxes ("all he wants is to sign up"). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit (fix this everywhere)"):** this line is now stale — Mailforge is no longer the decided managed-path vendor; InboxKit is (ACTIVATION.md Gate 0, updated 2026-07-20). Mordy's BYO path on his own Google Workspace boxes is unaffected either way. Remaining founder-hands: registrar/vendor account w/ card (no purchase capability exists on this machine — CF-dashboard 2-min buy or Namecheap acct+API key), Mailforge signup OR a GWS app-password test box for Gate-1 smoke, migration-timing vs his 07-28 Instantly clock. **Watchtower/monitoring blocker CLOSED 2026-07-20** (see ACTIVATION.md Gate 4 "Ops email + monitoring" block: `wrangler login` email-scopes refresh done, Email Sending + Routing armed + live-verified, watchtower cron confirmed firing, end-to-end alert-and-ticket path verified) — 1 of 4 Mordy-pilot blockers now closed; only the step-7 external prober remains open, folded into the founder's existing regular monitoring service (see the 2026-07-20 [gated:founder] line below), not a new UptimeRobot/BetterStack account. Remaining code: watchtower lane no longer blocks it (SHIPPED+deployed `2795a64`) — **BYO intake now BUILT** on branch `byo-intake` (`f46e935`, adversary SHIP-dark + re-attack ADDENDUM SHIP — see the "BYO-domain RULED" entry above), running alongside the already-shipped webhooks build (both core-code lanes under the win-the-comparison order); merge to main [gated:founder]. ✅ evidence: L220 MORDY PAID; droplet 584813711 provisioned; byo-intake.ts + byo-intake.test.ts on main
- [x] 2026-07-14 [ORDER] Registry/directory cascade — OUTCOMES VERIFIED 2026-07-14 (read-only): official MCP Registry LIVE+active v0.1.0 (updatedAt 14:04Z) · npm 0.1.0 live · awesome-mcp-servers PR #10106 still OPEN unmerged (1 comment — monitor) · DeepWiki seeded (200) · **Smithery NOT LISTED** (search returns only unrelated "agent-safe-email") + **PulseMCP NOT LISTED** (`total_count: 0`) — both covered by the standing disclosed-listings approval: Smithery sits behind GitHub OAuth (Yaakov's hands, same headed-login pattern as mcp.so/cursor.directory), PulseMCP has a no-login submit form BUT the auto-mode classifier DENIES the publish on standing approval alone (2026-07-14, same class as the llmstxt refusals) — needs Yaakov to state the PulseMCP submission request directly in-session, then it's ~1 min headless. Remaining hands-blocked surfaces: see the directory-cascade entry below. ✅ evidence: L183 Smithery published 2026-07-15; L30(b) PulseMCP auto-index
- [x] 2026-07-14 [ORDER] Human-surface gaps found by live prod drive (feeds the EXTERNAL design lane — hand these to the design LM): (a) NO self-serve billing UI — dashboard Settings shows plan/billing state but has zero upgrade/downgrade/cancel/payment-method controls, so even after Stripe goes live a human has no button to spend money; (b) NO human signup path at all (only `POST /signup`, framed as the agent's job) — a non-technical human cannot buy or use this today; (c) no `support@` contact published (routing built but disarmed, ACTIVATION Gate 4); (d) no status page; (e) no token-recovery path if a customer loses their bearer token (real support burden at launch). Evidence: `pw-shots/human-journey-2026-07-14/` (14 screenshots, both widths). ✅ evidence: 'Free sign up' live (L96); magic-link recovery; site/status.html; support@coldrig.dev published
- [x] 2026-07-14 [gated:founder] Stripe cannot take money: `STRIPE_SECRET_KEY` UNSET in prod — `/checkout` returns a SIMULATED url and hitting it upgrades a tenant to a paid plan with zero card + zero dollars (intentional test-mode; anyone with the API can self-upgrade). Webhook correctly fails closed (503, `STRIPE_WEBHOOK_SECRET` unset). To take real money: Stripe live KYC → swap key → set webhook secret (no Price objects needed, created inline). ACTIVATION Gate 2. ✅ evidence: L110 item 2 live keys flipped; L220 invoice in_1TyESe $39.60
- [x] 2026-07-14 [gated:founder] Mordy-pilot founder-hands items: (a) mailbox provider ruling — Inboxkit (real Google mailboxes via API, ~$6/mbx, 10-min signup + card; Yaakov leaned this way as "the better one") vs Mordy's own Google Workspace seats; (b) ~$10 test domain purchase for the Gate-1 smoke; (c) engine-host droplet spend APPROVED verbally ("whatever size we need") — `doctl` runbook in ACTIVATION Gate 2, provisioning not yet run (gated on the engine class-fix landing). ✅ evidence: L134 InboxKit ruling; droplet 584813711; $30 booked domains (L33)
- [x] 2026-07-14 [ORDER] Directory cascade — 4 surfaces still BLOCKED, each needs Yaakov's hands (the harness refuses agent-relayed authorization for outbound public writes): (a) **llmstxt.site** + (b) **directory.llmstxt.cloud (Tally)** — forms fully prepared, exact values drafted, scripts RESCUED to `archive/2026-07-14-activation-day/scratch-rescue/{submit_llmstxtsite.cjs,submit_tally.cjs}`; run with `NODE_PATH=~/.claude/skills/playwright-cli/node_modules node <script>` (~10s each, headless, no login) OR Yaakov states the submission request directly in-session; (c) **mcp.so** + (d) **cursor.directory** — both now behind GitHub-OAuth sign-in walls; one headed login (`~/.claude/skills/playwright-cli/scripts/persistent-session.js --profile coldrig --url <url> --login`) persists the session at `~/.claude/playwright-profiles/coldrig/` for all future automated submissions. ⚠️ mcp.so pre-selects a PAID $39 tier by default — pick the free tier deliberately. DONE already: npm, official MCP Registry, awesome-mcp-servers PR #10106 (open), llms-install.md. ✅ evidence: llmstxt.site + directory.llmstxt.cloud submitted (L184); mcp.so + cursor.directory submitted (L183)
- [x] 2026-07-14 [ORDER] PRICING **RATIFIED by Yaakov in-session 2026-07-15** (his words: liked $49+$10/mbx, "obviously not pitched that way" — PITCH = starts at $99 for 5 mailboxes, +$10 per additional mailbox; send rate NOT specifically capped — present the IDEAL send volume as deliverability guidance, never a contractual cap). Public framing directive binds all surfaces (pricing page headline, calculator, FAQ, JSON-LD, llms.txt, for-agents). REMAINING WORK: (a) integration lane verifies/adjusts the designer's pages to this framing (in flight); (b) CORE backend migration = quantity-based mailbox billing in Stripe (quantity subscriptions), quote-before-provision, owner spend ceiling persistence, downgrade/cancel semantics, plan-enum migration off launch/growth/scale, tests — the designer's UI deliberately disables billing mutations until this lands; (c) integration-adversary find 2026-07-15: `apps/platform/src/admin/support-kb.ts:35-36` — the AI SUPPORT AGENT'S KB still quotes the dead $299 Growth/$799 Scale tiers → 3-way inconsistency (site formula vs support answers vs live checkout PLAN_QUOTAS); fix the KB copy WITH the billing migration, or immediately after the design deploy if the migration lags (support quoting wrong prices to real prospects is a trust-killer). Original proposal detail (for the record): PRICING RULING was PENDING — designer + Yaakov independently converged on the SAME formula: $49 platform + $10/active mailbox ≡ "5-mailbox minimum at $99, +$10 per extra mailbox" (5=$99, 10=$149, 20=$249, 60=$649; domains bundled ~1:3; NO send meter — capacity shown as approximate/after-warmup, never contractual; deterministic agent billing = expose usage + projected monthly cost + quote-before-provision + owner-controlled spend ceiling; variable add-ons LATER: lead sourcing/verification/enrichment/dedicated IPs/extra workspaces/retention/white-label). Economics assessed in-session vs ~$4.50–6/mbx COGS (this file, ramp-economics entry above): marginal GM 40–52%, blended ~58–76% at entry tiers. Replaces live Launch/Growth/Scale ($99/$299/$799), kills the $99→$299 cliff, creates the ~$149/10-mbx option the buyer-agent research flagged; current Custom is ALREADY $49+$13/mbx so this unifies the ladder at a lower rate. NOT AUTHORIZED until Yaakov confirms; on ruling: pricing.html + JSON-LD + SPEC §12/§18 + Stripe quantity-billing + dashboard billing UI update together. ⚠️ side-finding: live pricing.html is internally inconsistent — states ~40–50 sends/mbx/day but lists 5 mbx = "~1,000 sends/mo" (the math gives ~4,400–5,500). ✅ evidence: Mordy sub = platform x1 + mailbox x5 (quantity billing live); apps/platform/src/admin/support-kb.ts:32 documents the rewrite off the $99/$299/$799 ladder
- [x] 2026-07-14 [ASK] Product gaps surfaced by Mordy's reply-tracking question (scout-mapped in-session with file:line cites): (a) NO auto reply-classification (interested/OOO/not-interested) — thread labels are manual (agent- or human-set), CANONICAL_THREAD_LABELS is a UI recommendation not an enum (packages/shared/src/dashboard.ts:17-27); (b) NO per-lead tags/segments/custom fields — lead = email/first_name/company/global_status only (schema.ts:103-112); (c) ~~inbound typed-"unsubscribe" replies NOT auto-detected~~ CLOSED `ecfc5b2` 2026-07-14 (conservative exact-phrase matcher in reply-processor, suppresses + cancels, adversary-attacked clean); (d) no bulk-export/CRM-sync endpoint — agent must page inbox/thread/activity; (e) NO data-retention policy on events (reply bodies stored in full, indefinitely, no TTL — schema.ts:141-152); the "premium data retention add-on" framing from the designer's pricing note is UNCONFIRMED in repo docs. Rank these build-vs-pilot-defer. ✅ evidence: L122: Q4 auto-classification DEFERRED, Q1/Q2 dispositions+tags BUILT, Q6 list_leads export, Q5 retention deferred to a pre-GA gate; (c) closed ecfc5b2
- [x] 2026-07-15 [ORDER] **Directory-shopfront refresh** (buyer run #2 starter, `tools/buyer-panel/runs/2026-07-15-claude-starter.md` — FIRST ORGANIC SURFACE, via Glama auto-index; killed by the LISTING's own content: "no published pricing... waitlist" while our $99 pricing page was live the same day; the shopper never fetched coldrig.dev): refresh repo README.md + server.json description + site/.well-known/mcp/server-card.json + packages/cli README to carry (a) the $99/5-mbx pricing, (b) "free sandbox live today, no card" instead of waitlist framing, (c) keep the honest test-mode/activation disclosure — directories re-render from these. Claim-class rules apply; rides the compare-pages adversary+commit batch. ALSO elevates founder task: Glama claim click — **DONE 2026-07-15** (founder claimed; listing content fix = this refresh + push + his sync-click on the claimed page). PulseMCP RESOLVED-NO-ACTION 2026-07-15: their submit page says they auto-ingest from the Official MCP Registry (daily ingest/weekly processing) — we published 07-14 + 0.2.0 update 07-15, so the listing self-materializes ~within a week; if absent by ~07-21, email hello@pulsemcp.com (draft on request); check folded into the 07-26 POST-SHIP AUDIT. BUILD DONE 2026-07-15 (4 files, uncommitted, adversary pending); residuals: (i) `SPEC.md:102` header still reads "~8–12 tools" — same staleness class the Glama shopper hit, out of the builder's scope, fix at next SPEC touch; (ii) repo-root `glama.json` addendum went unaddressed by the builder — verify Glama's schema exists before adding, else drop; (iii) founder step queued: `mcp-publisher publish` re-publish after commit. Harness fidelity note: Claude-side shoppers can identify the operator (EpiphanyMade env leak) — disclosed cleanly in-run; ChatGPT side is the clean-room check. ✅ evidence: entry's own BUILD DONE; grep SPEC.md '~8-12 tools' = 0 matches; Glama claimed 2026-07-15
- [x] 2026-07-15 [ORDER] **SMTP EGRESS BLOCKED — Gate-1 smoke wall (found live 2026-07-15).** Engine on the DO droplet (142.93.12.85) canNOT send: DO blocks outbound SMTP submission at the account level — proven: 443 to google/cloudflare OPEN, but 465/587 blocked to BOTH smtp.gmail.com AND smtp-mail.outlook.com; the Mac's network ALSO blocks 465/587. Not our firewall (fixed a bad `ports:0` outbound rule mid-diagnosis; 443 works). Gate-1 engine smoke is BLOCKED until a send path exists. Paths (founder decision): (a) DO support ticket to unblock SMTP — est. account is established (Navah droplet), but DO scrutinizes cold-email use, hours-to-days, may deny; (b) quick prove-tonight test — run the built engine (`apps/engine/dist`) from a DIFFERENT network (phone hotspot/tether) that allows 465, point smoke curl at localhost:8080; (c) **STRATEGIC — add a Gmail API / Microsoft Graph send path (HTTPS/443, block-immune) alongside the nodemailer SMTP adapter** — most BYO customers (and Mordy) are on Google/MS Workspace, whose APIs never hit the SMTP block; this is arguably the correct BYO architecture, not a workaround (build lane, needs OAuth vs the current app-password model); (d) pick an SMTP-egress-friendly engine host. Gmail app password for yaakovscher@gmail.com is on the droplet `/root/mailboxes.json` (world-reachable :8080 but bearer-gated; rotate anytime + re-paste). ⚠️ This constraint ALSO affects the managed-mailbox (Mailforge) path — verify Mailforge SMTP reachability from the chosen host before arming. **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** Mailforge is no longer the primary managed-mailbox path — InboxKit is (real Google/M365 via the 443-API transports, sidesteps this SMTP-egress constraint entirely). Mailforge-2525 SMTP reachability is now a fallback-only verification. See `## Open` 2026-07-20 "FOUNDER RULING" entry. ✅ evidence: gmail_api proven 07-19; L134 InboxKit ruling; engine armed and sending 2026-07-27
- [x] 2026-07-15 [IDEA] `setup_infrastructure` tool description says "Async — returns { jobId }" but `runSetupInfrastructure` runs SYNCHRONOUSLY in B0 (provisioning.ts:112) and the jobId isn't backed by a tracked job record — carried-over framing, not a live defect (field is real+pollable), but reconcile when the provisioning-sagas lane (B2, resumable alarm-driven jobs) lands. Flagged by tooldesc-adversary 2026-07-15. ✅ evidence: 003ad4d 'jobId fiction'; tools.ts:74 now says 'a correlation id only, not a trackable job handle'
- [x] 2026-07-15 [ORDER] Fix `apps/engine/Dockerfile` npm defect (droplet-runner, provisioning day): `npm install --omit=dev` STILL resolves devDependency registry metadata (arborist), so the workspace-only `@coldstart/shared` devDep 404s the image build; the Dockerfile's comment claims otherwise — wrong on npm 8/10/11. Provisioning worked around it (scratch copy, devDeps stripped; image contents identical) — the real fix (strip devDeps in Dockerfile or use `npm ci` w/ workspace-aware install) needs a PR + rebuild. Detail: `.claude/agent-memory/spec-builder/gotcha_npm_omit_dev_still_resolves_devdeps.md`. Droplet live: `coldstart-engine` 142.93.12.85 (ID 584813711, fw 639bb346), engine container Up DARK (empty creds map, health ok, unauth 401; no tunnel, no Worker secrets, no ENGINE_TENANTS). ✅ evidence: apps/engine/Dockerfile:24 'npm pkg delete devDependencies && npm install --omit=dev --no-package-lock'
- [x] 2026-07-15 [ORDER] **WIN-THE-COMPARISON program** (Yaakov: "do everything we can so that we win the comparison that the model makes"): (a) buyer-CHOICE panel harness build STARTED (tools/buyer-panel, spec-builder) — **CYCLE 1 COMPLETE 2026-07-15** (3 runs: canonical/starter/agency; see the harness entry below for full detail + fix-list) → **CYCLE 2 COMPLETE 2026-07-19** (3 more runs, same 3 briefs: `tools/buyer-panel/runs/2026-07-19-claude-starter.md` / `-canonical.md` / `-agency.md`, `CHOICE-TREND.md` +3 rows, `## Open` +6 fix-list lines below); (b) flagship Claude-Code/Cursor/Codex guide content STARTED (site/, spec-builder, claim-classes discipline, adversary before deploy) — **SHIPPED LIVE 2026-07-15** (`0a87f7c`, adversary SHIP zero-blocking `docs/adversarial/guide-pages-review-2026-07-15.md`, all 3 pages 200 live, wired to llms.txt/sitemap); (c) competitor-pages research DELIVERED 2026-07-15 + persisted (`docs/research/competitor-pages-research-2026-07-15.md` — worker had no write access, orchestrator persisted); ranked build order AgentMail → Skyp → FoxReach → Maildoso; UNVERIFIED flags resolved (per-page footers cite the internal buyer-agent research runs as the sourced-but-not-independently-reverified basis, not asserted as fact); **ALL 5 PAGES SHIPPED LIVE 2026-07-15** (AgentMail/Skyp/FoxReach/Maildoso/Smartlead, `6849c80`+`a074aa7`+`0744e15`) — adversary NO-SHIP→SHIP over 2 rounds (sole blocker: internal buyer-panel paths leaked into public HTML → reworded), frozen record `docs/adversarial/compare-pages-review-2026-07-15.md`; live-verified zero `tools/buyer-panel`/`briefs/`/`CHOICE-TREND` path leaks across every `site/*.html`, confirmed in sitemap.xml + llms.txt; (d) **webhooks: RULED BUILD under this order — SHIPPED, committed `d0e91ec` 2026-07-16.** Real per-tenant reply/bounce/complaint push (URL+secret+type filter) via HTTP + MCP tools 18-19 (MCP surface now 19 tools total, was 17 — verified `apps/platform/src/mcp/tools.ts` header comment); enqueue at the `recordEventIfNew` choke point (best-effort, cannot break event recording, UNIQUE dedup); cron-driven at-least-once delivery with backoff + auto-disable after 5 terminal failures; HMAC-SHA256 signed, secret shown once; SSRF-hardened (IPv6-embedded-v4 class closed, trailing-dot stripped, fail-closed, re-validated per delivery). Adversary SHIP + SSRF re-attack SHIP (`docs/adversarial/webhooks-lane-2026-07-16.md`); platform 420/420, typecheck + dry-run build clean. Closes the buyer-checklist hard-gate on reply/bounce push — real push now exists, no longer polling-only. **Residual CLOSED 2026-07-19:** Worker deployed (version `b3bae1c7`) with the webhooks lane live — bookkeeper live-verified `tools/list` = 19 incl. `get_webhooks`/`configure_webhook`, unauth `GET /webhook-subscriptions` → 401 (not 404), and `status.html`/`docs.html` now show webhooks as "Live" — the fully-live reframe copy batch (`ea64541`) carries the claim; (e) still founder-word: aggregator asks — **RULED 2026-07-15: DEFERRED until Mordy tries the product** ("outreach save till mordy tries it"); dogfood calls, ChatGPT-side panel (account paste protocol or OpenAI key + spend OK), portability promise (gated on Mailforge transfer terms); (f) 2026-07-15 Yaakov pricing challenge ("drop to $89 vs Smartlead $94/30k?") — ANSWERED NO, structural: Smartlead $94 = software license, ZERO mailboxes/domains included (aggregator, SPEC §"Smartlead is itself an aggregator"); 5 mailboxes physically send ~2.2-3.3k/mo at the 15-30/day deliverability ceiling — 30k/mo needs 40-70 mailboxes on ANY platform; add compare-vs-smartlead page w/ all-in "cost per 100 sends/day" math to the comparison-pages queue (loudest sticker anchor in the category); (g) OPEN founder pricing question surfaced by (f): BYO-connected mailboxes — same $10/mbx as provisioned, or cheaper? Settle with the §20 BYO intake build; (h) Smithery PUBLISHED 2026-07-15 (smithery.ai/servers/yaakovscher/agent-cold-email, scan SUCCESS via X-API-Key gateway header, quality 70/100, scan tenant ten_308b1b65 sandbox) + cursor.directory SUBMITTED (cursor.directory/plugins/agent-cold-email, pending their security-agent scan). ✅ evidence: guides 0a87f7c, compare pages 6849c80/a074aa7/0744e15, webhooks d0e91ec; residuals at L232 (cycle 3), L121 (dogfood), L260 (outreach)
- [x] 2026-07-15 [ORDER] **Warm-lead lifecycle DEEP DIVE** (Yaakov ruling 07-15: NOT a Mordy-start blocker — build deferred; instead deep-dive "the thin layer we should build and really what an AI agent needs so this is useful END-TO-END without the agent needing to do much itself"). Scope = full agent-journey audit (discovery → setup → launch → reply → warm-lead handling → follow-up → reporting/handoff), enumerating every point where the customer's agent must keep its own state or build its own sidecar, then design the thin persistence/automation layer. Scout-verified baseline at `686e506`: `leads` has NO tags/notes/custom fields (`schema.ts:103-112`); `thread_labels` + canonical vocab PURELY COSMETIC (`thread-labels.ts:20-43` — even `do_not_contact` triggers nothing); no lead-status write tool, no lead query/filter/export, no manual-suppress tool despite `'manual'` in the suppression-reason enum (`types.ts:125`), no one-off scheduled follow-up outside the launch-time sequence, no reply webhook (poll-only). Seed candidates for the dive (NOT ratified): (1) ⚠️ `do_not_contact` auto-suppress + manual `suppress` tool — compliance-adjacent (free-text "stop" phrasings miss the strict matcher `reply-processor.ts:55-71`; the address stays contactable by FUTURE campaigns) — eligible to ship as an independent early increment if the dive confirms; (2) structured lead interest-status + notes + `update_lead`; (3) `list_leads` filters; (4) `schedule_followup` reusing `scheduled_sends`; (5) CSV export. Webhooks tracked under forensics-gaps (c). Deliverable: spec section + ranked build list folded into SPEC/ROADMAP (no new doc). **DIVE COMPLETE 2026-07-16, adversary SHIP** (`docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`) — frozen record `docs/research/warm-lead-thin-layer-dive-2026-07-16.md`; ratified design `SPEC.md` §22. Ranked build list + cut-line: **#1** `suppress_lead` tool + parametrize `unsubscribeEmail` reason (S, compliance-adjacent — closes the real "free-text stop stays contactable by future campaigns" hole); **#2+#3** `lead_dispositions` table + `update_lead` tool (M) then `list_leads` (M) as one persistence increment; **#4** `schedule_followup` + `followups` table + tick drain (M-L — MUST reuse a new shared guarded-send primitive, caps/warmup/deliverability-pause/suppression, NOT `replyToThread`, per adversary amendment); below the cut-line: #5 auto-classification pre-fill, #6 `unsubscribe` webhook + choke-point routing, #7 event-body retention TTL. Status now **[gated:founder]** on build go + rulings on Q1-Q6 (SPEC §22). NOTE: "19 tools" copy surfaces (AGENTS.md, `.mcp.json`, site/README references) sync to 23 only once the build actually lands — not touched now. ✅ evidence: docs/research/warm-lead-thin-layer-dive-2026-07-16.md; SPEC 22; L122 build + deploy 8a30ec0; #4 residual tracked at L122
- [x] 2026-07-15 [ORDER] Forensics playbook §5 gap closures (checked vs live+design state 2026-07-15): (a) competitor-review moat — design branch adds compare-vs-salesforge (the actual winner) ✓; **Skyp, FoxReach, AgentMail, Maildoso pages SHIPPED LIVE 2026-07-15** (+ Smartlead, see WIN-THE-COMPARISON entry above) — closed; (b) flagship "run your ENTIRE cold email operation with Claude Code + ColdRig" content w/ qualifier-heavy titles ("2026", "fully autonomous", "agent-native") + Cursor/Codex variants — **WRITTEN + SHIPPED LIVE 2026-07-15** (see WIN-THE-COMPARISON entry above) — closed; (c) Requirement-7 webhooks: buyer checklist HARD-gates on reply/bounce push — **BUILT + SHIPPED, committed `d0e91ec` 2026-07-16** (full detail: WIN-THE-COMPARISON (d) entry above); real push closes this gap in code — residual is the capability-matrix/site copy still saying webhooks absent/polling-equivalent, fix rides the fully-live reframe copy batch; (d) aggregator listings — coldiq/tools, inboxkit, coldemailkit saturate buyer result sets (where "Reddit sentiment" is actually read) — outbound asks, founder-word gated; (e) Glama listing (appeared in buyer result sets unfetched) — add to the founder directory batch; (f) portability promise ("domains transferable if you leave") — DO NOT claim until Mailforge resale/transfer terms verified (Gate-1 support ask). **CORRECTED 2026-07-20 (FOUNDER RULING "go inboxkit"):** Mailforge's resale/transfer terms are now primary-source quoted (ACTIVATION.md Gate 0 — prohibits direct resale, agency shape with mandatory sub-accounts) but Mailforge is fallback-only; the portability question that actually matters now rides InboxKit's terms, which are UNVERIFIED pending empirical post-purchase check — still DO NOT claim portability until that's settled. Closed already: pricing hole at canonical scale (RATIFIED $149/10mbx), one-click unsub real (`ecfc5b2`), status page (API live + design page), public repo, honest-activation framing. ✅ evidence: compare pages + guides shipped 2026-07-15; webhooks d0e91ec; (d) = L260 order 2; (f) is a standing do-not-claim constraint
- [x] 2026-07-15 [ORDER] Directory batch ROUND 2 (main-loop execution after the classifier refused subagent relays): **mcp.so SUBMITTED ✓ FREE tier** ("Submission received… review queue", submission b5135b9d, screenshots) · **Smithery PUBLISHED 2026-07-15** (X-API-Key bearer-equivalent shipped in the watchtower commit `2795a64` unblocked it — smithery.ai/servers/yaakovscher/agent-cold-email, scan SUCCESS, quality 70/100) — closed · **cursor.directory SUBMITTED 2026-07-15** (`.mcp.json` shipped in `2795a64` unblocked the scan — cursor.directory/plugins/agent-cold-email, pending their security-agent scan) — closed · **Cline HELD on honesty**: the issue form REQUIRES attesting "I have tested that Cline can set up this server" — we have not run Cline; prerequisite = one real Cline setup test (Yaakov ~10 min in VS Code, or scripted later); icon ready (designer's mark, 400×400), form mapped, never false-attest · **Glama claim: NEEDS YAAKOV'S CLICK** (classifier correctly blocks agent-clicked OAuth grants on his GitHub): glama.ai/sign-in → Continue with GitHub → Authorize (read-only scopes) → server page → Claim → admin panel Dockerfile config = scored badge. Original round-1 outcomes below. ✅ evidence: Smithery published, mcp.so submission b5135b9d, cursor.directory submitted; Glama claim DONE 2026-07-15 (L150)
- [x] 2026-07-16 [ORDER] **ChatGPT and Claude search could NOT fetch coldrig.dev even when pointed directly at it, and Google is not indexing the site.** Diagnose live (CF zone AI-bot-block / Bot Fight Mode settings, robots.txt, sitemap, GSC/Bing submission state) and FIX — this gates every findability lane already shipped. Related: `## Now` GSC/Bing item, POST-SHIP AUDIT (e) zone AI-bot-block verification. **2026-07-16 probes done** (as reported by the probing agent that session — not yet persisted as a frozen research doc on disk): `site:coldrig.dev` = 0 Google results confirmed; WebFetch CAN fetch the site directly; spoofed-UA probes all return 200; CF zone bot-management settings UNREADABLE with the current wrangler token scopes. Bookkeeper spot-check: `robots.txt` is permissive (`Allow: /`, only `/unsubscribe` + `/why-email` disallowed) and `sitemap.xml` is present/linked — so the wall is not a robots/sitemap defect. Blocked on founder clicks: CF dashboard Security→Bots allow-AI-crawlers toggle (or mint a scoped API token: Zone Settings:Edit + Bot Management:Edit), GSC domain property + sitemap submit, Bing import. **07-16 FOUNDER UPDATE: CF was ALREADY set to allow AI bots** (hypothesis dead — site was fetchable all along; direct fetches confirm) **→ revised root cause: pure index absence.** Chat assistants reach sites through their search indexes; zero Google+Bing pages = "can't access" UX. GSC domain property verified 07-15 (sitemap NOT yet submitted); Bing GSC-import done 07-15 (imported BEFORE the sitemap existed in GSC → will NOT auto-carry it). Remaining founder micro-steps: GSC→Sitemaps→submit sitemap.xml + URL-Inspection Request-Indexing on / and top pages; Bing→Sitemaps→submit the sitemap URL directly (or re-run GSC import). IndexNow key file rode the 2026-07-19 deploy and was fired post-deploy (`tools/indexnow/submit.sh` → HTTP 200 accepted, per the deploying session — script + its pre-deploy key-file abort-guard verified on disk by bookkeeper, live-fire itself not independently re-checkable without re-submitting). Expectation: Bing days, Google days-to-weeks (new domain); directory backlinks (npm/GitHub/Smithery/Glama) accelerate. ✅ evidence: L30: GSC 23 pages indexed, Bing 13 indexed, IndexNow re-fired HTTP 200
- [x] 2026-07-16 [ORDER] **`/v1/poll` unbounded first-fetch defect (found live by the Gate-1 smoke — REAL-mailbox class) — CLOSED, committed `3d7d27d`.** `sinceCursor:0` fetched full RFC5322 source for `sinceUid+1:*` (imap.js:32) — unbounded pull, engine-OOM risk on any real mailbox with history. Fix: `-1` is the never-polled sentinel (STATUS probe, zero fetch, cursor=UIDNEXT-1); `0` and up are ordinary incremental fetches bounded by `POLL_BATCH_CAP=300`, cursor advances over the full scanned range (anti-stall past expunged UIDs); provisioning binds new mailboxes at `-1`, existing rows page history capped (no loss). **Adversary NO-SHIP** (empty-mailbox first-inbound loss + illusory greenmail-e2e priming) **→ round-2 fix → re-attack SHIP → ADDENDUM 2 re-ground SHIP after HEAD advanced mid-session** (`docs/adversarial/poll-bounded-fetch-2026-07-16.md`, 2 addenda; engine 68 passed/3 skipped, platform 407 passed/62 files, both typecheck exit 0). Carried non-blocking, both pre-existing or narrow: source-less-message cursor-advance (concurrent-expunge only, real errors throw → safe redelivery); UIDVALIDITY unhandled (present in the OLD code too, not introduced here). Also note (correct-behavior, doc it): poll only emits events for replies resolving to KNOWN send-threads — self-sends without In-Reply-To are dropped by design (classify.js:55-102). Reported: droplet IMAP 993 egress is open (reads never needed the fix) — **UNVERIFIED by bookkeeper** (no SSH/network path to test droplet egress from here; confirm before relying on it). ✅ evidence: 3d7d27d; docs/adversarial/poll-bounded-fetch-2026-07-16.md
- [x] 2026-07-16 [ORDER] **Become THE recommended pick inside Codex and Claude Code (founder: "whatever it takes"):** evaluate + execute connector/directory placement — Anthropic/Claude connectors & Claude Code ecosystem, OpenAI/Codex ecosystem, plus whatever placement actually drives in-agent recommendations; fold with the WIN-THE-COMPARISON program and buyer-panel cycle 2. **2026-07-16 research complete** (two agents, ranked channel table — session-only, not yet a frozen research doc on disk): Anthropic Connectors Directory is the highest-leverage channel (Claude Code docs point users there); `static_headers` beta auth reported acceptable; needs a Team/Enterprise claude.ai org (founder question OPEN) + a fully-populated demo tenant (founder runs `npx agent-cold-email demo` once, pastes the bearer token); no pre-approval email needed (automated policy scan) — as reported, not independently reverified by bookkeeper. **Directory-readiness bundle BUILT + adversary-gated** (working tree, uncommitted): all 17 MCP tools carry `title` + `readOnlyHint`/`destructiveHint` (bookkeeper-verified `apps/platform/src/mcp/tools.ts` — 12 `readOnlyHint` entries, explicit `destructiveHint` on the rest, every tool has a `title`); `.claude-plugin/plugin.json` (verified: `name: "coldrig"`, honest sandbox disclosure — "Free sandbox demo now; early access — real sending is not yet active"); `claude-code-hint` stderr marker in the CLI (verified `packages/cli/src/claude-code-hint.ts` — deliberately inert, no marketplace lookup resolves it until a `claude-plugins-official` listing exists); Codex TOML blocks in `README.md`/`packages/cli/README.md`/`site/connect.html` (verified present; field names remain UNVERIFIED against live Codex docs — smoke before claiming verified-working). **Adversary cycle:** r1 NO-SHIP (`docs/adversarial/directory-readiness-2026-07-16.md`) → 2 fix rounds → re-attack found ONE NEW blocking regression (`get_dashboard('default')` phantom-default masquerade) → round-3 fix → **ADDENDUM VERDICT: SHIP** (`docs/adversarial/directory-readiness-reattack-2026-07-16.md`; that doc records platform `npm test` 362/362, typecheck exit 0). **CLASS CLOSED:** "`readOnlyHint:true` tool that writes" — 2 members found+fixed (`infrastructure_status` warmup UPDATE; `get_dashboard` first-call `ensureDefaultViewSeeded` INSERT) + a systemic write-spy guard test (`apps/platform/test/mcp-tool-annotations.test.ts`, SQL-only scope traced adequate, virgin-tenant, revert-proven for both members). NOTE: the original frozen doc's line 39 ("pure SELECT … HELD") was wrong — `get_dashboard` also wrote on first read; corrected in the reattack addendum (its line 59), original preserved unedited per the frozen-doc rule. Submissions to mcp.so + Cline: BLOCKED on founder naming the public targets (permission classifier). Connectors Directory submission drafts RESCUED to `archive/2026-07-16-send-live-and-webhooks/scratch-rescue/` (`connectors-directory-listing.md` + `mcp-review-email-draft.md`) — durable in-repo now (the mcp-review email path is superseded by the workaround research: no pre-approval email needed). ✅ evidence: docs/adversarial/directory-readiness-reattack-2026-07-16.md SHIP; mcp-tool-annotations.test.ts; mcp.so/Smithery/registry all filed (L30/L183)
- [x] 2026-07-16 [ORDER] **Branded API domain (founder observation 07-16):** the API/MCP endpoint lives on `agent-cold-email-api.yaakovscher.workers.dev` while the brand is coldrig.dev — an early-stage/maturity signal AI buyers read AND an operator-identity leak (`yaakovscher` in every config snippet). Fix = `api.coldrig.dev` as a Workers custom domain (zone is ours; workers.dev URL stays alive for backcompat) + coordinated reference sweep: server.json, .well-known server-card, repo-root .mcp.json, .claude-plugin/plugin.json, READMEs ×3, site/connect.html + guides, Connectors/ChatGPT listing drafts, CLI default endpoint (needs next npm publish; old CLI keeps working via backcompat URL). DO BEFORE the directory submissions so listings carry the branded URL. Execute as its own small batch AFTER the pending two-lane commit lands (keep batches separable); custom-domain attach + deploy = founder-confirm. ✅ evidence: HANDOFF line 3 'API https://api.coldrig.dev'; commit fcb233e, Worker f94139e1 (L220)
- [x] 2026-07-16 [ORDER] Existing `reply` tool sends with NO suppression check (replyToThread — pre-existing, defensible as 1:1 transactional; same unchecked primitive increment #4 would have reused) — deliberate founder/design ruling required at warm-lead build time; flagged by warm-lead design adversary (frozen doc `docs/adversarial/warm-lead-thin-layer-design-2026-07-16.md`). ✅ evidence: L122 correction 2026-08-02: engine/guarded-send.ts (suppression re-check) wired into engine/threads.ts, commit e4250e9
- [x] 2026-07-19 [ORDER] **Founder rulings batch (in-session, verbatim IDs from the decision table):** **A** = DELETE the 2¢ send-usage fee everywhere ("delete the 2 cent thing wherever it is - in all places it comes up") → remove `SEND_USAGE_FEE_CENTS` + every ledger write/test/doc that references it, whole class, no dead code; closes the ACTIVATION Gate-2 "rule the 2¢ fee" item as DELETE. **E** = "use recommendation" → the crash-after-accept-before-record double-send residual is ACCEPTED for the comped pilot ONLY; durable pre-send intent log MUST build before GA (flip the ACTIVATION Gate-2 residual (1) to founder-accepted-for-pilot). **F** = GO → byo-intake coordinated merge pass (merge + Worker deploy + 19→21 count sweep + capability copy, ONE pass; connected-mailbox PRICE stays unstated on public surfaces until ruling D). **G** = GO → buyer-panel cycle 2 (post-reframe scoreboard). **I** = founder named the public targets in-session: "submit to mcp.so and Cline". **D** = "do the same as whatever the industry does" → BYO-connected mailboxes carry NO per-mailbox fee (industry standard per frozen pricing research: Instantly/Smartlead include connected inboxes in the plan); monetization = platform fee + provisioned mailboxes; design into the quantity-billing migration (tier fair-use caps allowed); NO public connect-pricing claim until the §20.6 connect flow actually exists (it is a documented stub). (founder, in-session: "C - make the pricing work").** Design the agency shape — ONE platform fee + flat per-client-workspace add-on + per-mailbox meter — INTO the Stripe quantity-billing migration (not a separate pass). Kills the ledgered agency-scale kill-in-waiting (buyer run #3: our per-tenant model ≈ $890/mo vs winner ~$600 at 8 clients — the exact pattern the shopper rejected Instantly for; our true multi-tenant isolation is the matching strength). Numbers/design → founder ratification before public. Related open pricing ruling D (BYO-connected mailbox price vs $10 provisioned) decides before the byo-intake public merge (F), not required for this design to start. ✅ evidence: A = 2c fee deleted (archive INDEX 2026-07-20); E = send-log 608c80a; F/G/I done; C tracked at L231
- [x] 2026-07-19 [IDEA] `site/status.html` Dashboard row still shows an "Early access" pill (bookkeeper-verified live) — defensible-but-optional (dashboard paid-mutation gating is real), consistent sibling wording is "Rolling out"/"Concierge step". Non-blocking per the reframe adversary's residual note (`docs/adversarial/fully-live-reframe-2026-07-19.md`); reword at next status.html touch if desired. ✅ evidence: orchestrator-verified 2026-08-23 — `grep -ci "early access" site/status.html` → 0 (pill no longer present on the live file; original inventory evidence: docs/adversarial/fully-live-reframe-2026-07-19.md residual note)
- [x] 2026-07-20 [ORDER] **Mordy pilot — InboxKit runway (state + runbook).** Purchased 07-20: InboxKit Professional monthly $39 (renews 08-20), **10 Google mailbox slots**, wallet 0 credits; API live (workspace "Starter" `c5188ced-33db-436f-b970-1860e6c8c66b`; auth Bearer JWT + X-Workspace-Id). **FOUNDER 07-20: `authorpitchdesk.com` IS the sending domain** — Mordy doesn't need it for landing/regular contact, so it rides the pilot directly (no lookalike purchase required to start; optional 1-2 lookalike spreads = later founder call). Connect paths (both doc-verified): (a) NS delegation — POST /v1/api/domains/nameservers returns assigned Cloudflare nameservers, Mordy sets them at his registrar, then check-nameservers-propagation; (b) CF-API connect — POST /v1/api/cloudflare-domains/connect with a CF token (Zone:Read+DNS:Edit) from whichever CF account holds the zone; validateDNS BLOCKS if the zone has ANY existing mail records (MX/SPF/DKIM/DMARC/autodiscover) — check before attempting. **⛔ Gate: needs Mordy's registrar/CF access — founder coordinates.** Then: 2 mailboxes from slots → GET show-mailbox-credentials (full smtp+imap creds) → gmail_api/443 smoke from the droplet = Gate-1 ON the pilot domain, and its warmup clock starts (the 07-28 Instantly answer). Priced/mapped this session: their warmup add-on = $3/mbx/mo (GET /warmup/pricing); prewarmed mailboxes exist (POST /prewarm/buy-domain, credits; doc example 3 mbx/2 domains = 24 credits) — candidate warmup-clock compressor, founder call; **OAuth fleet path candidate**: POST /mailboxes/client-id-request/initiate registers OUR existing Google OAuth client ID onto the provisioned Workspace domain + initiate-consent-request grants consent — likely programmatic refresh-token path for gmail_api at fleet scale (verify empirically at first mailbox; fallback = manual mint, proven 07-19). InboxKit adapter build lane DISPATCHED 07-20 (dark, fixtures, spend-endpoints forbidden). **Adapter lane COMMITTED 07-20 (`51f5015`)** — RealMailboxPort rewrite + RealInboxKitDomainPort + InboxKitClient + error mapping, 40 new tests, dark (unreachable per `factory.ts`, both call-site literal and missing config), adversary re-attack SHIP (574/574, 80 files, typecheck 0; frozen record `docs/adversarial/inboxkit-adapters-2026-07-20.md`). **REQUIRED-BEFORE-ARMING (all non-blocking for the dark merge, cited from the frozen record, do not re-derive):** (a) domain port is welded to the same `inboxKitConfig` credential as the mailbox port (`factory.ts:142`) — an armer supplying the mailbox credential silently co-arms InboxKit as registrar, contradicting ACTIVATION.md:9's mailbox-scoped ruling vs ACTIVATION.md:25's still-Namecheap/Porkbun registrar line; needs a separate explicit domain-port arming flag + FOUNDER reconciliation of the two ACTIVATION lines; (b) `resolveMailboxUid` trusts `mailboxes[0]` from a keyword search with no exact-email assertion before the destructive `/mailboxes/cancel` call — add an exact-match check before cancel; (c) `provision()` has no vendor idempotency key (double-charge retry window) + a fragile `/already exists/i` message-substring match — bring to the repo's own `withRequestIdempotency` standard; (d) vendor `reputationScore`/`placementRate` reach the customer-facing `infrastructure` tool + dashboard DISPLAY as if real (decisions already use local signals only) — display-honesty fix needed; (e) `/mailboxes/list` keyword exact-vs-fuzzy semantics is UNVERIFIABLE without a live POST — resolve with a throwaway mailbox at arming time (gates finding b). ✅ evidence: 51f5015 adapter lane; G5 91f8227 (gate a); I4 gates b/c/d merged 5b151e7; INBOXKIT_* armed in prod (L58)
- [x] 2026-07-20 [ORDER] **FOUNDER RULING ("go inboxkit (fix this everywhere)"): InboxKit replaces Mailforge as the decided mailbox vendor for the Mordy pilot + first validation buy.** Basis (pre-spend research, all cited in the vendor-battery entry below + bookkeeper-propagated to ACTIVATION Gate-0): (a) Mailforge's CURRENT ToS prohibits direct resale ("not entitled to... assign the use of all or part of the Services to your own customers, even for free"); permitted shape = agency/indirect-subscriber with MANDATORY per-client sub-accounts; cancellation hostile (≥7-day notice via live chat, no refunds, full remaining term charged) — corrects the 07-12 Gate-0 premise "only vendor whose ToS explicitly permits reselling"; (b) InboxKit Professional = $31/mo 10 slots (their own pricing page), cancel-anytime effective end-of-period, REAL Google Workspace/M365 accounts with full admin access, public provisioning API (docs.inboxkit.com; Bearer + X-Workspace-Id; plaintext SMTP/IMAP credential export; bulk 10 req/min), "Connect Existing Domain" flow with Cloudflare-API full automation = Mordy's exact bare-domain case; (c) send path rides our PROD-PROVEN 443 transports (gmail_api proven 07-19; ms_graph app-only BUILT, needs its one live smoke) instead of Mailforge's unproven-from-droplet SMTP-2525. Mailforge = researched FALLBACK only. Queued consequences: InboxKit real MailboxPort/DomainPort adapters + fixture capture (build lane, post-key), Google-side DWD service-account gmail adapter IF Google mailboxes chosen at fleet scale (small build; pilot can mint per-mailbox refresh tokens manually), Google-vs-M365 mailbox choice decided at first mailbox order (not blocking the buy). ✅ evidence: HANDOFF Landmines InboxKit call shape; Mordy's 4 mailboxes provisioned via InboxKit
- [x] 2026-07-21 [ORDER] **Self-serve activation DESIGN LANDED** (docs/research/self-serve-activation-design-2026-07-21.md; every gap file:line-cited). Increments: I1 product-driven activation gate replacing manual ENGINE_TENANTS (M, foundational) · I2 checkout promo codes w/ allow_promotion_codes + payment_method_collection if_required so a 100%-off code needs no card (S) · I3 managed-mailbox credential path — engine POST /v1/mailboxes + durable store, Worker provisioning push, programmatic gmail_api OAuth mint w/ manual-mint fallback (L, THE LONG POLE: InboxKit client-id-request flow unverified live) · I4 InboxKit arming gates b/c/d as code (M; gate a = separate S flag item, gate e = arming-time verification) · I5 OFAC screening NET-NEW (M; ACTIVATION.md:93 "hook is built" was FALSE — corrected 2026-07-21) · I6 metered $10/mbx migration (L, deferred; pilot ships flat $99 + promo code). Credential architecture ratified in-design: PUSH-to-droplet (secrets stay on the firewalled droplet; internet-facing Worker never stores refresh tokens). Critical path to Mordy self-serve: I1 → I3(manual-mint fallback OK for one pilot) + I2 + one-time founder arming (Tunnel/ENGINE_BASE_URL/secrets, Stripe KYC, InboxKit key). Registrar question in the doc = already resolved by the 2026-07-21 registrar ruling (Cloudflare default; InboxKit per-tenant opt-in behind own flag). ADVERSARY VERDICT 2026-07-21: **SHIP-after-fixes** (docs/adversarial/selfserve-activation-design-review-2026-07-21.md, grounded at 09e73ff; both panel-03 fail-opens verified closed in current code). BLOCKING→build-scope: F1 unauthenticated GET /checkout/simulate writes billing_state='active' with no STRIPE_SECRET_KEY gate (routes/checkout.ts:24-34, billing.ts:78-119) — inert today, becomes real-spend fail-open under I1; MUST close in I1 w/ RED test. F2 promo code MUST be max_redemptions=1 + plan-restricted ("plan quota = spend ceiling" is FALSE: no tenant-count cap, per-tenant ceiling is 60 mbx not 5). Non-blocking folded: F3 replyToThread bypasses freeze+caps → adapter-cache re-evaluation is REQUIRED (fresh SQL each buildAdapters) + warm-lead #4 must never use replyToThread; F4 cred-write replay/idempotency; F5 engine-state.json corrupt-file silent discard; F6 partial-failure ordering; F7 align §2.4(a) to the Cloudflare registrar ruling. Cross-lane: schema.ts additive-merge hazard w/ warm-lead lane (coordinate at integration); warm-lead tool numbering must be 22+ (SPEC body's 20-23 is stale). I1+I2 build DISPATCHED 2026-07-21 (worktree) with F1/F2/F3 embedded as musts. **BUILT 2026-07-21:** commit `7ef372d` on branch `worktree-agent-ae74fe3cd5cdba531` — activation gate (engine/activation.ts, fresh-SQL per buildAdapters), ENGINE_TENANTS DELETED per design, F1 simulate guard (404 when STRIPE_SECRET_KEY set + DO-level defense), F2 promo flags launch-plan-only + coupon-constraint comment; builder-reported 573/573 (81 files) + typecheck 0 + F1 RED/GREEN cp-backed proof. Notable deviations: email port needs `activated && engineConfig` (builder caught a real paid-but-unarmed regression via full suite); no screening_status column (pure stub, avoids schema collision); ACTIVATION.md:26 stale ENGINE_TENANTS reference flagged for bookkeeping; builder disclosed a mid-session `pkill vitest` that may have hit sibling lanes (adversaries re-run their own batteries). Fresh-context adversary re-attack DISPATCHED 07-21 — priority question: does the ORIGINAL F1 arming-order scenario survive the STRIPE_SECRET_KEY-presence guard? **ANSWER: NO — VERDICT SHIP-after-fixes 2026-07-21** (docs/adversarial/selfserve-i1i2-build-review-2026-07-21.md; adversary's own battery 573/573 + typecheck 0). BLOCKING: F1 NARROWED not killed — guard keys on STRIPE_SECRET_KEY presence while the threat window is its ABSENCE (engine-armed-before-Stripe-keys → simulate open → activated && engineConfig → real send/spend for $0); the lane's own checkout.test.ts:191-197 asserts the hole open. FIX ROUND DISPATCHED to the same builder: simulate 404s whenever engineConfig OR inboxKitConfig present (spend-arming signals) in addition to the Stripe guard — simulate works ONLY fully-unarmed; RED-proof the exact scenario. **FIX LANDED 2026-07-21: `dc934e9`** — shared `isRealSpendArmed(env)` (STRIPE_SECRET_KEY OR ENGINE_BASE_URL&&ENGINE_AUTH_SECRET) used by route guard + DO defense-in-depth; InboxKit has ZERO env binding today (grep-confirmed by builder) → documented as a REQUIRED guard extension the moment I3/I4 add one; RED-proven on the exact adversary scenario (200→404 assertion failed on old code), battery 574/574 (81 files) + typecheck 0. Adversary re-verify (round 3) DISPATCHED — includes ruling whether a comment suffices as the systemic guard for the InboxKit-binding extension or a failing-by-construction check is required. **ROUND-3 VERDICT 2026-07-21: CLEAN-SHIP** (addendum in the verdict doc; adversary's own battery 574/574 + typecheck 0): finding-1 dies in all 4 arming permutations, guard wired at BOTH route and DO level, new test end-to-end + genuinely fails-on-old-code, simulate confirmed the ONLY unauthenticated billing_state writer. **R3-1 REQUIREMENT ON THE I3/I4 LANE:** when INBOXKIT_API_KEY/WORKSPACE_ID env bindings land, a failing-by-construction check is REQUIRED (test asserting every vendor-arming env field in env.ts is referenced by isRealSpendArmed) — a doc comment is not a systemic guard for the spend-bypass class. Carried GA gates unchanged (pending-activation state, absolute-$ ceiling). BOTH LANES NOW SHIP-CLEAN → integration branch `integration/2026-07-21-wave` (fold both, battery ONCE, 21→24 sweep + claim-surface pass, deploy batch staged for founder word). **INTEGRATION MERGED + BATTERY PASS 2026-07-21:** both lanes merged conflict-free (warm-lead `54ac82a`, self-serve `dc65d6f`); verifier's own battery on the merged tree: typecheck 0 across all 5 workspaces, **84 files / 597 tests ALL PASS** (EXIT:0, 278s), node-resolution trap ruled out (worktree-local @coldstart/shared symlink verified), adapters-rename semantic conflict ruled out (no stale `this.adapters`, ctx-wiring confirmed at tenant-do.ts:300/342). Remaining before deploy-staging: 21→24 claim sweep (builder running) → final wave adversary gate on the COMBINED diff → founder deploy word (Worker BEFORE site). **SWEEP LANDED 2026-07-22 `453e445`** (27 files; builder hit its usage limit AFTER the work, before self-reporting — orchestrator mechanically verified the full checklist: zero stale claims tree-wide, server.json 96 chars @24, server-card 24 tools incl. the 3 new, openapi +3 lead paths, og-image regenerated '24 focused tools'). **WAVE GATE VERDICT 2026-07-22: SHIP-after-fixes, 1 BLOCKING** (docs/adversarial/wave-integration-review-2026-07-22.md): openapi.yaml's 3 lead paths $ref SIX never-defined schemas (LeadInterestStatus/LeadListPage/SuppressLeadInput/SuppressLeadResult/UpdateLeadInput/LeadDispositionView) — valid YAML, INVALID OpenAPI, breaks buyer-agent codegen on exactly the new endpoints; fix = add the 6 definitions (shapes in packages/shared/src/leads.ts + routes/leads.ts), FIX DISPATCHED 07-22, redocly re-lint = the gate. **FIX LANDED `d28afe7` + GATE CLOSED 2026-07-22: SHIP (round 2)** — adversary re-ran redocly itself (0 unresolved refs), shape-checked all 6 vs code serialization (EXACT, incl. suppressed:true literal + Provenance enum), ruled the 3 new nullable-hits acceptable house-style. Wave CLEAR TO DEPLOY-STAGE. Everything else HELD: count-vs-enumeration consistent at 24 everywhere, zero stale counts on live surfaces, tool descriptions faithful, concierge caveat preserved, spend-guard independently re-verified, no merge dross. Non-blocking: guide-mcp-tool-count table omits the 2 BYO tools (PRE-EXISTING gap, optional row later); dead `followups` table = increment #4's spec'd data model shipped schema-only (adversary-#1-verified zero-writers) — KEEPING (stripping would reopen the shipped lane; #4 lands into it), noted as accepted deviation from YAGNI. Non-blocking, ledgered as PRE-PUBLIC-GA GATES: (2) paid-but-engine-unarmed tenant silently gets sandbox sends (confident-wrong; needs an explicit pending-activation state before GA — fine for the pilot where founder arms first); (3) absolute-$ owner ceiling still unbuilt + max_redemptions is Stripe-dashboard-side (documented at stripe-client.ts:56-71). Attacks held: gate formula verbatim, F3 fresh-SQL both directions, freeze/dispute/cancel/past_due flip gate off, cache tenant-scoped, ENGINE_TENANTS straggler-free. ⛔ STANDING: never arm the engine before Stripe live keys until the F1 fix lands. Merge HELD pending clean re-verify + integration with warm-lead branch (tenant-do.ts touched by both). **FOUNDER RULED YES 2026-07-21** ("yes you can run the pilot"): pilot ships with OFAC stubbed-clear; containment = the F2 promo constraints (max_redemptions=1, launch-plan-restricted); real OFAC (I5) + an absolute-$ ceiling REQUIRED before PUBLIC self-serve GA — that gate stays on the ledger. **DEPLOYED 2026-07-22:** merged to main `8a30ec0` (I1+I2, w/ F1 spend-guard fix `dc934e9`, integrated with the warm-lead lane + 21→24 claim sweep `453e445` + openapi schema fix `d28afe7`), gated by 4 adversary verdicts + integration battery 597/597 (84 files, typecheck 0 ×5 workspaces). Worker DEPLOYED version `1cb56729-91af-45ba-8e02-13de2db8624d` (live-verified `/status` ok, unauth tools/list = 24); site DEPLOYED after Worker, Pages deployment `25844cc6` (live-verified 24-tool server-card + description). Prod behavior: nothing armed — engine/Stripe/InboxKit all unbound, every tenant sandbox; `ENGINE_TENANTS` mechanism RETIRED in prod per design, activation now product-driven (paid+active+unfrozen+screening-stub); `/checkout/simulate` guarded by `isRealSpendArmed`. I3 (managed-mailbox credential path, the long pole) + I4 (InboxKit arming gates) + I5 (real OFAC) + the absolute-$ ceiling remain UNBUILT — pilot arming session still needs the founder present. ✅ evidence: merged 8a30ec0 Worker 1cb56729; I3/I4 = 5b151e7; I5 OFAC = 32d5b6b; absolute ceiling = engine/spend-ceiling.ts
- [x] 2026-07-21 [ORDER] **Registrar DECIDED (delegated choice): Cloudflare Registrar is the default for fresh sending-domain buys** (.com $10.44/yr at-cost; purchase API verified working, beta; renewals/transfers dashboard-only = acceptable once-a-year founder click). Namecheap = documented fallback (proven — Smartlead buys via it). Porkbun DROPPED (no purchase API). InboxKit-as-registrar = per-tenant explicit opt-in ONLY (registrarConfig.kind==="inboxkit"), for the connect-existing-domain shape — never a default, never inherited from the mailbox credential. Arming-flag policy: domain port gets its own registrarConfig distinct from inboxKitConfig in factory.ts (closes 2026-07-20 arming-gate finding a). NEW BUILD ITEM surfaced: apps/platform/src/vendors/real/domain-port.ts:4 is stubbed against Porkbun (already ruled out by SPEC §13) — needs a rewrite against Cloudflare Registrar; folds into the self-serve activation build. Evidence: docs/research/registrar-choice-2026-07-21.md. ✅ evidence: L108: 'Cloudflare RealDomainPort replacing the Porkbun stub', G5 merged 91f8227; registrar armed 2026-07-29 (L220a)
- [x] 2026-07-21 [ORDER] **SELF-SERVE ACTIVATION** (founder 07-21: "why not just set it up so he can do it himself like any customer we want to") — kill the per-customer concierge step. Gaps to close: one-time Tunnel/`ENGINE_BASE_URL` arming; replace the manual `ENGINE_TENANTS` allowlist with product activation logic (billing-active + existing freeze-states/quotas + D4 OFAC placement); arm InboxKit adapters (gates a-e, 2026-07-20 entry); dynamic per-tenant mailbox-credential plumbing engine-side (today: static `/root/mailboxes.json` on the droplet); §20.6 OAuth connect for the BYO-mailbox shape (NOT needed for Mordy's managed shape). Design lane dispatched 07-21 (opus) → `docs/research/self-serve-activation-design-2026-07-21.md`; build gated on that spec + adversary. ✅ evidence: L176 ENGINE LEG ARMED 2026-07-27; activation.ts product-driven predicate; wrangler secret list shows all four
- [x] 2026-07-21 [ORDER] **BILLING-FIRST PILOT** (founder 07-21: "I'd rather do billing and give him a code") — supersedes the comped-outside-billing framing: Stripe live keys (KYC = founder blocker) + promotion-code support in checkout + founder mints a 100%-off code for Mordy; checkout E2E becomes part of the pilot test. Flat $99 plan + code first; the metered $10/mbx quantity migration can follow. ✅ evidence: L114 (test-mode E2E) then live keys flipped (L110 item 2); L220 invoice $39.60
- [x] 2026-07-21 [FACT-CORRECTION] **OAuth consent screen is IN PRODUCTION** (founder read the console 07-21: "it says it's in production (It was before also)") — the ledger's "Testing status → refresh token dies ~7 days (next ~07-26)" claim is WRONG (it was never independently checkable from here). In-production apps' refresh tokens do not auto-expire weekly; the 07-19 re-mint was for the scope addition + a transit-corrupted secret, not expiry. VERIFY: if the 07-19-minted token still sends after 2026-07-26, close the weekly-re-mint chore for good and strike it from HANDOFF/queues. ✅ evidence: engine sending live through Mordy's provisioning 2026-08-18 (HANDOFF item 7); refutes L141
- [x] 2026-07-21 [ORDER] **server.json description trimmed to the registry's 100-char cap** (founder's `mcp-publisher publish` hit 422 "expected length <= 100"; old string was 199) — 96-char form keeps the 21-tools/live-sending/sandbox/$99/concierge claims; founder re-runs `mcp-publisher publish` (already authed — the 422 was post-auth validation). ✅ evidence: c5c1436 brand-first 96-char description; v0.2.3 published 2026-08-17 (L30a)
- [x] 2026-07-21 [ORDER] **Mordy onboards via the REGULAR self-serve flow** (founder 07-21: "I want mordy to sign up through regular coldrig to test the end to end process") — not concierge. He follows coldrig.dev (Claude Code guide) → signup (isolated sandbox tenant) → campaign setup. True-E2E gaps to arm before his tenant goes REAL: Cloudflare Tunnel + `ENGINE_BASE_URL` + `ENGINE_TENANTS`, InboxKit key re-paste (session-local, gone), InboxKit adapter arming gates (a)-(e) in the 2026-07-20 Mordy-pilot entry. Billing = comped pilot (Stripe live keys not wired). ✅ evidence: L220 MORDY PAID (tenant ten_91aab24a, sub_1TyESe active)
- [x] 2026-07-21 [ORDER] **Research lanes dispatched (founder 07-21):** (a) warm-lead Q1-Q6 user-POV recommendations [opus]; (b) InboxKit prewarm mechanics + offer-to-all-customers + lookalike-spread rec [sonnet]; (c) registrar choice — founder DELEGATED the choice post-research [sonnet]; (d) review-site listings + dogfood-calls scope — founder DELEGATED the choice post-research [sonnet]. Outputs → `docs/research/*-2026-07-21.md`; recommendations back to founder for ratification where product-visible. ✅ evidence: L122 (warm-lead Q1-Q6), L124 (prewarm), L123 (registrar), L121 (review sites)
- [x] 2026-07-22 [ORDER] **SIGNUP-AUTH DESIGN LANDED** (docs/research/human-signup-magic-link-design-2026-07-22.md). KEY REFRAME: human web signup + connect-your-agent onboarding ALREADY EXIST (apps/dashboard SignupPage.tsx → POST /signup → token-shown-once → session → SetupPage.tsx w/ MCP snippets) — net-new = MAGIC-LINK login replacing the dead-end RecoveryPage. Increments: A magic-link backend (M, login_links table + POST /login + /login/consume, enumeration-safe, terminates in the EXISTING session mint via shared mintDashboardSession refactor) · B SPA verify page (S-M, ‖ A) · C Turnstile on /login ONLY — never /signup, agent-drivable preserved (S; ⚠️ sequence after I3+I4's env.ts change; ⚠️ R3-1 test stays scoped to VENDOR-ARMING fields so TURNSTILE_SECRET doesn't trip it) · D one-funnel site IA/copy (M; collides ENTIRELY with brand-sweep on site/*.html + stale "17 tools" strings in SetupPage.tsx — lands AFTER brand-sweep, then re-pass). Email channel VERIFIED: OPS_EMAIL is unrestricted by design (dunning precedent, wrangler.toml:48-52), send from login@coldrig.dev. RECOMMENDED-ADOPTED under the autonomy grant (founder may override): magic-link = access factor for ALL tenants incl. paid; multi-tenant-per-email = PICKER. Design adversary DISPATCHED 07-22 (auth surface = attack before build); build queued per program. ✅ evidence: L96/L97; site deploy 282510a3
- [x] 2026-07-22 [ORDER] **AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED** (founder verbatim: "Stripe verified the account. Keep working autnomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere"). Stripe verified live same hour (charges_enabled TRUE, payouts TRUE, support_email set). PROGRAM (each wave: build → fresh adversary → merge → battery → deploy, per project law): (1) **DONE 2026-07-23** brand-sweep lane → adversary SHIP → merged `60275b3`; (2) **DONE 2026-07-23** I3+I4 lane → adversary r2 SHIP → merged `5b151e7` → deployed Worker `3f7acce1-bfb6-4c46-8e88-62ec4ced9fce` → LIVE Stripe keys flipped from Keychain + live session creation verified (livemode:true, line-item "Coldrig Launch", no charge completed); (3) **IN FLIGHT** signup-auth build (worktree `signup-auth-20260723`; design adversary-cleared `docs/adversarial/signup-auth-design-review-2026-07-23.md`) → adversary → merge/deploy; (4) GA-GATES wave (design adversary-cleared `docs/adversarial/ga-gates-design-review-2026-07-23.md`; G5 gate(a) micro-lane already merged `91f8227`): real OFAC v1 (SDN-list screening at activation + review-queue-on-hit via watchtower alerts — no paid provider), absolute vendor-spend ceiling (founder-tunable, conservative default), pending-activation state (paid-but-unprovisioned tenants see it explicitly), slot-capacity handling (auto-upgrade under the ceiling, not waitlist — "ready for all customers"); (5) final coordinated claim-surface/info pass everywhere + IndexNow. ARMING AUTHORIZATION 2026-07-22 (founder: "can I arm it now already so Mordy can sign up even when I'm not at the comp?"): founder-present authorization GRANTED for the arming steps — main loop executes them AUTONOMOUSLY once I3+I4 clears+deploys (wrangler secrets from Keychain, droplet/tunnel ops via SSH, gate-e throwaway-mailbox verify on existing slots); only a browser-consent click (if the programmatic OAuth path fails at first mailbox) would be queued back to the founder. NOTE: signup+sandbox need NO arming (work today); payment goes live at the Stripe key flip; arming affects real provisioning/sending only. ADDED TO PROGRAM: gate (a) separate domain-port arming flag (registrarConfig decoupling, S) is still UNBUILT and now BLOCKS credential-push activation — build it in the GA-gates wave (or a micro-lane at I3 merge). Watchdog cron ARMED 07-22: every 3h at :41, session-only, 7-day expiry (resumes the program across usage limits). NEW ARMING SECRETS introduced by the I3+I4 lane (set at arming, all inert alone): INBOXKIT_API_KEY + INBOXKIT_WORKSPACE_ID (spend-arming — extend isRealSpendArmed coverage test enforces) + GMAIL_OAUTH_GRANTS (manual-mint path, non-spend). ✅ evidence: 60275b3, 5b151e7, 65415e9c, a8a0b117, IndexNow re-fired (L30)
- [x] 2026-07-22 [ORDER] **HUMAN SIGNUP + MAGIC-LINK LOGIN + ONE-FUNNEL RULED** (founder: "do magic link with email", "why not allow human signup also", "even for an agent, flow can be just 'free sign up' and standard is sandbox till upgraded"). Scope: /login magic-link (email → single-use hashed short-expiry link → existing dashboard-session mint; enumeration-safe, rate-limited), human /signup web form (reuses POST /signup; token-shown-once + auto dashboard session + connect-your-agent onboarding; bot protection eval), site one-funnel "Free sign up" CTA (additive claim-surface pass). Sandbox-by-default preserved — activation gate untouched, GA gates unaffected (sandbox signups = zero spend). Design lane DISPATCHED 07-22 (opus) → docs/research/human-signup-magic-link-design-2026-07-22.md; adversary on design, then build QUEUED BEHIND I3+I4 + brand-sweep (site/ + copy collision avoidance). Email path candidate = the unrestricted OPS_EMAIL Send Email binding (verify customer-facing suitability in design). ✅ evidence: L96 site deploy 282510a3
- [x] 2026-07-22 [ORDER] **MORDY PILOT PRICE = COST (founder: "We're going to drop Mordy down to cost price for us")** — supersedes the 100%-off comp framing. Cost math (from ledger): 2 InboxKit slots ≈ $7.80/mo allocated ($39/10 slots) + warmup add-on $6/mo (2 × $3, while warming) + infra slice ≈ **$14-15/mo true cost**; **RULED 2026-07-22 (rev 2, same day): 60% OFF** (founder — "we paid 39/mo? so lets make it 60% off") → Mordy pays **$39.60/mo**, which alone covers the full $39/10-slot InboxKit subscription; supersedes the 65% ruling minutes earlier on the $99 launch plan; percent-off rides the subscription invoice so future per-mailbox charges inherit it; cost-covering on both marginal (warmup-only ~$15 at 5 boxes; slots pre-paid in the $39/10-slot plan) and allocated (~$34.50) accounting. Implementation: single-use percent_off=65 coupon, no expiry, minted in LIVE mode at the wiring pass (Stripe-Version pin gotcha). Supersedes the earlier $15/85% sketch (that was pinned to his 2-box usage; founder correctly flagged the scale case). F2 containment unchanged: single-use + launch-plan-restricted + promo entry code-gated to launch sessions. Live coupon minted at KYC-approval time (Stripe-Version pin gotcha applies). Test promo MORDYPILOTTEST (100%) already consumed by the E2E — superseded either way. ✅ evidence: L220: MORDYPILOT redeemed, invoice in_1TyESe amount_paid $39.60 (verified)
- [x] 2026-07-22 [ORDER] **"ColdStart" stale-brand CLASS SWEEP dispatched** (founder: "make sure this gets fixed") — confirmed instance = Stripe checkout product name "ColdStart Launch (test mode)" (stripe-client.ts:89); class = retired working-name on ANY customer-visible surface; builder inventories repo-wide, fixes IN-class, derives the test-mode suffix from key prefix (live customers see "Coldrig Launch"), adds a reintroduction tripwire test. Merges + deploys with the next Worker batch (before Mordy onboards). ✅ evidence: merged 60275b3; brand-copy-guard.test.ts tripwire
- [x] 2026-07-22 [ORDER] **I3+I4 BUILD LANE DISPATCHED** (founder: "you do what you can and I'll do stripe now"): one hard-builder arc in an isolated worktree — I3 credential path (engine authed POST /v1/mailboxes + F4 idempotency + F5 loud-corrupt-store + F6 record-before-push ordering; Worker INBOXKIT_API_KEY/WORKSPACE_ID bindings + provisioning push; isRealSpendArmed InboxKit leg + R3-1 failing-by-construction guard test; OAuth mint seam manual+programmatic-dark) + I4 gates b/c/d (exact-email-before-cancel, withRequestIdempotency provision, vendor-metric display-honesty). NO live vendor calls (fixtures; spend endpoints forbidden). Adversary re-attack follows the build. **Stripe KYC IN PROGRESS (founder, same day)** — next after: live-key wiring session + single-use 100%-off launch coupon. ✅ evidence: merged 5b151e7, Worker 3f7acce1 (L110 item 2)
- [x] 2026-07-22 [gated:founder] mcp-publisher republish for 0.2.2 — official registry still serves 0.2.1 with the STALE "21 MCP tools" description (same one-command re-run as yesterday: cd ~/dev/coldstart && mcp-publisher publish; auth may need mcp-publisher login github first). ✅ evidence: L30(a): io.github.YS-projectcalc/agent-cold-email v0.2.3 published 2026-08-17T00:12Z; HANDOFF 'MCP-registry: DONE'
- [x] 2026-07-22 [ORDER] **openapi.yaml nullable-class cleanup (whole class, not piecemeal — wave-gate round-2 ruling):** file declares openapi 3.1.0 but uses 3.0-ism `nullable: true` in 21 places (18 pre-existing + 3 new lead-schema hits matching house style) → redocly struct-rule red. Fix = pin `openapi: 3.0.3` (all 21 valid instantly) OR convert all 21 to `type: [x, 'null']`. Separate pass; not deploy-blocking. ✅ evidence: site/openapi.yaml:1 'openapi: 3.0.3'
- [x] 2026-07-22 [IDEA] guide-mcp-tool-count.html: add the BYO row (get_byo_domains/configure_byo_domain absent from the table — pre-existing gap, page headlines 24/surfaces 22; hedged copy so not false). ✅ evidence: grep site/guide-mcp-tool-count.html get_byo_domains = 1 match
- [x] 2026-07-23 [ORDER] **GA WAVE MERGED + DEPLOYED — battery arc ran FIVE times; live-verified.** Both GA merges + glue land on main: OFAC SDN screening `32d5b6b` (G1a+G1b, fail-closed w/ self-heal + race guards, adversary SHIP + N-OF-1 CLOSED addendum) + provisioning-core GA `8674da7` (factory InboxKit wiring, spend ledger+ceiling+reaper, slot gate, honest activationState, adversary SHIP 0-blocking, 4 deviations ratified) + integration glue `1dccd94` (screening_status wired into activationState) + gitlink cleanup `d89516c` + GA fixture fix `6299788` (battery #4 correctly FAIL-CLOSED on 5 pre-OFAC fixtures lacking a seeded SDN list — N-OF-1 working as designed, not a regression; fix = benign-list seeding via shared `seedBenignSdnList`, test-only, assertions not weakened; re-run battery #5 PASS **763/763**). Deploy sequence (`wrangler deployments list`-verified): Worker `65415e9c` (signup-auth) → secret changes (INBOXKIT_API_KEY/WORKSPACE_ID) → `a8a0b117` (OFAC_LIST_URL fix, see next entry) — `a8a0b117` is the live version as of this bookkeeping pass. D1 migrations 0009+0011+0012 ALL applied remote (`wrangler d1 migrations list --remote`: "No migrations to apply!"). Live-verified: `/status` ok, `/checkout/simulate` 404, MCP `tools/list` 24, `serverInfo.version` **0.2.2** (was stale hardcoded "0.1.0", closed by `ca072ac` — single-sourced from `apps/platform/package.json`, confirmed live via a real `initialize` call). ✅ evidence: 32d5b6b, 8674da7, 1dccd94; Worker a8a0b117; battery 763/763
- [x] 2026-07-23 [ORDER] **OFAC first live cron hit HTTP 525 — root-caused + fixed `a7f3b97`.** The cron fired its fail-loud alert exactly as designed on first activation ("SDN.CSV fetch failed: HTTP 525", prior list retained, retry ~5min — the fail-closed path worked, nothing silently broke). Root cause: treasury.gov's public download URL redirects through a front-end that rejects the Workers TLS/fetch handshake. Fix: `OFAC_LIST_URL` repointed to the direct `sanctionslistservice.ofac.treas.gov` publication-service URL (`apps/platform/wrangler.toml:49`) — verified this session: the URL 302s to a signed S3 URL that 200s with a real 12-column SDN CSV. Redeployed `a8a0b117`. ⚠️ **VERIFIED AT BOOKKEEPING TIME (2026-07-24 00:11 local / ~21:11Z, ~6 min post-redeploy): `sdn_list_meta` is still EMPTY (0 rows) on remote D1** — the first successful load has NOT yet been confirmed. Cron runs every 5 min so this reads as too-soon-to-tell rather than a fresh failure (screening still fails CLOSED to 'review' with an empty list, which is safe, just not yet the intended clear-screening steady state) — **next session MUST re-check `sdn_list_meta` row count** before treating OFAC screening as live-loaded. Lesson for the class: any Workers `fetch` against a gov/vendor download URL should be pinned to the FINAL host, never a redirect front-end — the front-end can reject the Workers TLS profile even when a normal client follows it fine. ✅ evidence: a7f3b97; 2e0af89 'screen against the rows that could match' presupposes a loaded 17k-row list; scale audit S9
- [x] 2026-07-23 [ORDER] **SIGNUP-AUTH BUILT (5 commits `0478d21`→`b543814`) — design-adversary build review RUNNING.** All 4 increments + all 3 adversary obligations RED-proven (atomic-consume race 4/5-fail→5/5, normalization, waitUntil 3023ms→<1000ms). Migration **0009** (was genuinely free; 0011/0012 assignments for P/O lanes unaffected). Battery: platform 676/676 (+28), dashboard 106, engine 95p3s, CLI 9/9, typecheck clean. Notable: ops-mailer gained closed-enum sender ("ops"|"auth" → login@coldrig.dev); Turnstile built DI-dark (no live widget — ACTIVATION `turnstile-spin` step); increment D honest-dark (NO email-login marketing until §1.7b gate); SetupPage "17 tools"→24 fixed. Builder discovery ledgered: OPS_EMAIL binding is TRUTHY under miniflare in tests (env.ts doc comment wrong) — adversary judging the false-confidence class one layer up. §1.7b empirical outbound gate = main loop, post-deploy: real send login@→yaakovscher@gmail.com tenant, droplet-IMAP Authentication-Results dkim=pass d=coldrig.dev + dmarc=pass; funnel flip only after PASS. ✅ evidence: 0478d21..b543814; deployed Worker 65415e9c (L96)
- [x] 2026-07-23 **GSC STATUS CORRECTED (founder screenshots, supersedes stale "pending" entries):** property verified + sitemap.xml SUBMITTED 2026-07-16 (last read 07-17, Success, 35 discovered pages) — the founder DID the GSC click; ledger was stale. Homepage https://coldrig.dev/ = "URL is on Google / Page is indexed" ✓. Coverage report still "Processing data" (benign new-property lag). NOT ranking for category terms = domain age/authority (11-day-old domain), not a technical block — directory lane (Glama/registry/Smithery/awesome-list) remains the fast path per traffic-channels research. REMAINING: [gated:founder] URL-Inspect → Request Indexing on ~10 money pages (pricing, 3 agent guides, 5 comparisons) · [gated:founder] Bing Webmaster Tools verification status UNKNOWN (IndexNow fired 07-19 covers submission, not console verification). ✅ evidence: L30 (GSC domain property verified, 23 pages indexed); L217 (Bing verify + Request-Indexing clicks DONE)
- [x] 2026-07-23 [ORDER] **⛔ INTEGRATED BATTERY FAIL — DEPLOY + KEY FLIP BLOCKED; hermetic-test-env fix lane DISPATCHED.** wave-verifier on 60275b3: typecheck 5/5 clean, build clean, sanity greps pass (credpush gone; stripe-client Coldrig 2 / ColdStart 0), dashboard 99 / engine 95p3s / CLI 9/9 green — but platform 5 FAILED / 634 pass (all checkout.test.ts): main checkout's `.dev.vars` carries the real test-mode STRIPE_SECRET_KEY (wired 07-22 for local E2E) → billing.ts:40 `Boolean(env.STRIPE_SECRET_KEY)` flips checkout to real-Stripe mode under test → simulated-mode assertions fail. CLASS (not instance): test assertions gated on ambient developer-env secrets — suite result depends on machine state (fresh worktrees pass on placeholder .dev.vars, main fails). Fix lane (hard-builder, worktree test-env-hermetic-20260723): harness-layer hermetic env (constructed/allowlist pattern so NEW vars are hermetic by default — REGISTRAR_* incoming from G5 lane), full env.ts class sweep, failing-by-construction ambient-leak guard, revert-fail-restore proof w/ fake truthy key. Battery re-runs after merge; deploy stays gated. NOT a lane regression — latent since 07-22 key wiring; surfaced now because the battery finally ran in the main checkout. ✅ evidence: battery 763/763 (L92); merged-tree battery green at 9d31e26 (HANDOFF item 1)
- [x] 2026-07-23 [IDEA] dev-docs brand pass: SPEC/ARCHITECTURE/ACTIVATION H1 titles + SPEC.md:60 `npx coldstart` (already ledgered) still say ColdStart — internal-facing, batch on next doc touch. ✅ evidence: grep SPEC.md ARCHITECTURE.md ACTIVATION.md for '^# ColdStart' and 'npx coldstart' = 0 matches
- [x] 2026-07-23 [ORDER] **BRAND SWEEP REBUILT + ADVERSARY DISPATCHED** (prior lane died with the 07-22 session, worktree `agent-a3b6ad8f…` abandoned mid-write). New branch `worktree-brand-sweep-20260723` @ `ec73d82`: full 385-hit inventory → 2 IN-class fixes (stripe-client.ts checkout product name → "Coldrig {label}" + key-prefix-derived "(test mode)" suffix; demo-seed.ts `demo-leads.coldstart.dev`→`demo-leads.coldrig.dev` — renders in demo inbox UI) + reintroduction tripwire (brand-copy-guard.test.ts, RED/GREEN revert-proven both files) + battery green (typecheck 0 ×5 workspaces; platform 601, dashboard 99, engine 74+3s, CLI 9/9). SCOPE RULINGS (main loop, from inventory): unsubscribe-token KEY_DERIVATION_LABEL stays UNCHANGED — renaming a stateless-signature domain-separation string invalidates every already-issued unsubscribe link = CAN-SPAM break; @coldstart/* workspace scope, X-Coldstart-Client header, infra/db/fixture names, frozen docs all OUT (never customer-visible or protocol-frozen). Adversary review RUNNING → merge queued behind I3+I4 per program order. ✅ evidence: merged 60275b3 (L110 item 1, 'DONE 2026-07-23 brand-sweep lane -> adversary SHIP')
- [x] 2026-07-23 [IDEA] SPEC.md:60 illustrative `npx coldstart …` is factually stale independent of brand (real CLI bin = `agent-cold-email`) — internal doc, fix on next SPEC touch. ✅ evidence: grep SPEC.md 'npx coldstart' = 0 matches
- [x] 2026-07-23 [ORDER] **SIGNUP-AUTH DESIGN ADVERSARY R1: SHIP-AFTER-FIXES — FIXES APPLIED SAME DAY** (frozen: docs/adversarial/signup-auth-design-review-2026-07-23.md). BLOCKER B1: design claimed OPS_EMAIL outbound was "end-to-end-verified" — FALSE (07-20 Gate-4 proved INBOUND forward + cron only; no outbound send_email delivery ever proven from ANY from-address) — AND missed that DMARC is live `p=reject` (design said "add p=none if absent"): a DKIM-misaligned sender = HARD-REJECT = silently-dead login links (identical-200 posture gives zero signal). Fixed in design: §1.7 claims corrected + new **§1.7b EMPIRICAL OUTBOUND GATE** (blocking build gate: deploy send path dark from login@coldrig.dev → real magic-link request to a yaakovscher@gmail.com tenant → droplet-IMAP verify Authentication-Results dkim=pass d=coldrig.dev + dmarc=pass; on fail flip to ops@ and re-gate; site funnel flips ONLY after PASS). NB folds: ctx.waitUntil on the send (timing channel), X-Coldstart-Client required on /login/consume (login-CSRF), email lowercase normalize-on-write + backfill + index (silent-login-failure). Everything else HELD on re-derivation. Design now cleared for build per program order (queued behind I3+I4 merge + brand-sweep for increments C/D; A/B unblocked at I3+I4 merge). ✅ evidence: L96 deployment (Worker 65415e9c, migration 0009, site 282510a3)
- [x] 2026-07-23 [ORDER] **I3+I4 ADVERSARY R1: SHIP-AFTER-FIXES — 1 BLOCKING-before-arm, fix round DISPATCHED** (frozen: docs/adversarial/i3i4-build-review-2026-07-23.md; suites re-run by adversary: engine 95p/3s, platform 625p — match builder claims; deployed build inert until INBOXKIT_*/ENGINE_* set, so nothing wrong ships today). BLOCKER: pushRecordedMailbox stamps every push with deterministic `credpush:${tenantId}:${email}` → engine store's keyed mode rejects differing content under a reused key → rotation/re-mint UNREACHABLE; lost-response retry (the F6 at-least-once boundary) re-mints a fresh refresh token → BadRequestError → row stuck 'pending' forever; remove() doesn't clear the idempotency record so teardown can't recover. FIX (adversary-prescribed): drop the key — content-hash replay-safety already gives same-content no-op + different-content 'replaced'. Fix round also takes NB3 (parseGrants silently coerces malformed GMAIL_OAUTH_GRANTS to {} → fail loud) + NB2 (DELETE/revoke path has zero non-test callers — wire best-effort token revoke into cancel/teardown, never blocking cancel). Re-verify by the SAME adversary (alive this session) → merge → G5 micro-lane. UNVERIFIABLE carried to arming: all InboxKit/engine wire shapes are self-labelled guesses — resolve at first live mailbox; gate-b limit:1 ranking vendor-dependent. ✅ evidence: merged 5b151e7, Worker 3f7acce1 (L110 item 2)
- [x] 2026-07-23 [ORDER] **GA-GATES DESIGN ADVERSARY R1: SHIP-AFTER-FIXES — FIXES APPLIED SAME DAY** (frozen: docs/adversarial/ga-gates-design-review-2026-07-23.md; session relaunched all 4 dead lanes after the 07-22 session death). B1 → design gains **§G5 gate (a)** (registrarConfig decoupling + Cloudflare RealDomainPort replacing the Porkbun stub + domain.buy fail-closed hard-block; **micro-lane at I3/I4 merge — BLOCKS arming**; supersedes "in the GA-gates wave" option in the program entry). B2 → realSendPathLive corrected to `engineConfig && inboxKitConfig` + G3 guard extended (engine-armed/InboxKit-unbound must read pending, not active). 4 NB dispositions folded into the design (re-screen-on-brand-rewrite, stale-reserve reaper, review-queue match context, OFAC honesty statement). Design now cleared for build per program order. Also 07-23: Stripe live account re-verified charges_enabled TRUE + payouts TRUE + details_submitted TRUE (the flip precondition that was propagation-pending). ⚠️ watchdog cron re-arm DENIED twice by the auto-mode classifier (CronCreate blocked) — no cross-gap resume net this session; founder can allow the tool or re-arm from a session where it passes. ✅ evidence: G5 micro-lane 91f8227; GA wave 32d5b6b/8674da7, Worker a8a0b117 (L92)
- [x] 2026-07-27 [ASK] Paid-conversion blind spot: `GET /admin/ops/digest` with ADMIN_TOKEN answers "of 61 signups, how many paid" — main loop classifier-blocked from local secret probing; founder runs the one curl in `docs/research/post-ship-audit-2026-07-27.md` §(f) or shares where ADMIN_TOKEN lives. ✅ evidence: HANDOFF line 3 + Landmines (Keychain admin-token); digest read every watch tick
- [x] 2026-07-29 [ASK] **`registerDomains` rides EVERY setup call (documentation footgun, adversary round-2):** a setup call omitting it resets a prior opt-in to 0 (safe direction — 503, zero buys — but surprising). Every registrar-using setup call must carry `registerDomains:true` + full `registrant`. MCP tool description says required-when-true; consider adding the every-call semantics explicitly + dashboard/docs mention. ✅ evidence: apps/platform/src/mcp/tools.ts:74 ('OMITTING it leaves any consent you previously gave UNCHANGED'); continuity wave cf9b323
- [x] 2026-08-05 [IDEA] **Per-account system→agent message channel (founder 2026-08-05):** a lightweight inbound `messages` store per tenant that the customer’s agent reads via an MCP tool (and/or a field on `infrastructure_status`), letting our system + the operator drop STRUCTURED notices to the customer’s agent WITHOUT a full relay — activation/next-step instructions, retry-with-same-key prompts, the OAuth-mint-ready signal, incident notices. Plus an EMAIL opt-in mirror. Directly addresses the manual-handoff gaps the 08-05 incident exposed (today the founder must hand-relay "retry now" / "you can send now" to Mordy). Composes with the pipeline-adversary H-alert. **FOUNDER RULED 2026-08-05: FULL CHANNEL AUTHORIZED** (in-session AskUserQuestion) — Inc1 ratified retroactively (the prior build-before-sign-off discrepancy is resolved); fix Finding 1 → re-gate clean → deploy; then Increments 2-4 (operator route, list_messages/ack_message tools, email mirror) each adversary-gated. ✅ evidence: list_messages/ack_message live (28 MCP tools); Inc4 = ROADMAP ## Now L16
- [x] 2026-08-05 [ORDER] **Full boundary audit (founder 2026-08-05, "how do we completely get everything"):** the prior audit missed heavily because reviews ran against a sandbox built to always-succeed + inner-helper unit tests + docs claiming a nonexistent crash-sim. Method: (a) build failure-capable test doubles FIRST (the SANDBOX-REALISM lane — fakes that return errors/stall/race/already-owned) — prerequisite, nothing is visible without it; (b) enumerate EVERY external boundary (vendor mutation, cross-store write, time source, idempotency key, error path, multi-step saga) — 5 classes mapped so far (the 08-05 sweeps), confirm ALL; (c) gate each by EXECUTING the real entry point with faults injected, not code-reading; (d) reflection-based coverage guard per closed class. Founder gating the SCALE (dozens of agents) before a full multi-agent launch; runs incrementally inside the PROVISIONING FIX WAVE until then. **Progress (2026-08-05):** 2 of 7 mapped gaps closed — credstore PASS (`docs/adversarial/audit-credstore-2026-08-05.md`), dunning-sweep FAIL (`docs/adversarial/audit-dunning-2026-08-05.md`, P0 above). Boundary-map correction: `apps/engine` is NOT unswept (4 frozen reviews + a SIGKILL proof already exist for it); the registrar-arming NO-SHIP is STALE — a round-2 SHIP exists in the SAME doc (`docs/adversarial/registrar-arming-review-2026-07-28.md`, merged `c1e24a5`). 3 gaps remain, staged and ranked: (5) dashboard mutation idempotency, (6) aging OFAC + outbound-webhook lanes (cheap "did files change since review" diff-checks), (7) ops-sweep/watchtower alerting completeness. Gap (3) Stripe webhook signature-verify/replay is now CLOSED — audit FAIL 3-blocking (`docs/adversarial/audit-stripe-webhook-2026-08-06.md`, currently untracked in the `main` worktree) → all three fixed in `fix/stripe-webhook-p0` `9b432a8` → folded into wave-2 `f7ee786`, deployed `8187d36e`; 2 new non-blocking residuals ledgered (`readDeclineCode` dead-on-real-payloads; the Stripe audit's own item 7, a 413 content-length-omission bypass — see the `## Open` 2026-08-06 readDeclineCode entry). Keep the boundary-map's gap list as the fan-out plan. ✅ evidence: L60/L61 shipped 2026-08-09 (17e05d4 / 0f197c4d); gap 3 via 9b432a8 folded into f7ee786
- [x] 2026-08-06 [ORDER] **Alerting-completeness audit (boundary gaps 6+7) — FAIL, 3 BLOCKING, one root defect: the watchtower cannot report a failure of anything it stands on; fix lane `watchtower-hardening-2026-08-06` dispatched same session** (frozen `docs/adversarial/audit-alerting-completeness-2026-08-06.md`; 14 fault-injection assertions vs the real Worker+D1+DO harness, asserting actual ops emails, never code-reading). **B1** a SUSTAINED D1 outage is a total silent alerting blackout: `evaluateHealthChecks` hits unguarded D1 reads (`admin/watchtower.ts:184,191`) and `reconcileAlerts` reads `watchtower_state` (`:294`) BEFORE any send — while `RealOpsMailer` needs only the `send_email` binding, so the alert IS deliverable during the outage (ordering defect, executed: 10/10 legs fail, 0 emails; intermittent blips DO alert). **B2** no dead-man anywhere: `last_sweep_ts` is read only by the sweep itself; cron death silences every alert and silence ≡ health; the planned external prober (still-open founder click) wouldn't catch it — `/status` is a bare SELECT 1, green with a dead cron. **B3** a wedged tenant DO (real class — 3 repo precedents cited) silently drops that tenant from every check AND every sweep leg while `do_storage` reports HEALTHY (probe pings a RateLimiterDO canary, wrong DO class). **NB-1 ARMS WITH THE CUSTOMER:** `failure_signals` flaps on intermittent failure (executed: 24 emails/2 simulated hours, alternating UNHEALTHY/RECOVERED; the exact founder-reported 160-email cry-wolf class `sdn-alert.ts` was built to fix) — windowed/streak evaluation required BEFORE real sends arm. NB-2 cron-leg error counters (`errors`/`budgetExpiries`/`skippedForLegDeadline`) are console-logged and never read by anything. NB-3 digest `watchdogAlerts` (incl. `gaveUpWarmupCancels` — "money leaking" — and `pendingCredentialPushes`) computed every 5 min and thrown away; pull-only via /admin/ops/digest. NB-4 plain provisioning vendor-failure on a paying tenant alerts nobody (known-deferred F8; inventory only, self-healing via adopt-on-retry). Gap-6 diff-checks: OFAC lane CLEAN with a CAVEAT — `fad3a3e`'s brand-override on the sanctions screen carries one sentence of coverage inside a provisioning gate; no OFAC-lane review since 07-27 (focused re-review candidate). Outbound-webhook lane CLEAN (`7ce59dc` covered by the warm-lead review, SHIP). Observations (no verdict weight): `cred_push_aging` clear-path can wedge forever on a deleted mailbox row; `watchtower_state` grows unbounded, full-table SELECT every 5 min. **NOW SHIPPED + DEPLOYED 2026-08-09 (verified in code): B1, B2, B3, NB-1 fixed.** B1 — `evaluateHealthChecks`/`reconcileAlerts` now NEVER THROW on a D1 outage (`admin/watchtower.ts:82,321`, code-verified). B2 — dead-man built as a new `WatchtowerDO` Durable Object (self-scheduling alarm, `watchtower-do.ts:42,93,121,128`, code-verified) + migration tag v3 (`wrangler.toml:102-104`, code-verified) + a new `/status` field `sweepAgeSeconds` (`routes/status.ts:31-33`, code-verified; live-confirmed 200 `{"status":"ok","sweepAgeSeconds":215}` both hosts this pass, well under the 900s `SWEEP_STALE_MS` threshold, `admin/watchtower-grading.ts:62`). B3 wedged-DO visibility and NB-1 flap windowing per the merge commit `17e05d4` message (not independently code-read this pass beyond the commit's own description). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). **NB-2 (cron-leg error counters unread), NB-3 (digest thrown away), NB-4 (provisioning vendor-failure alerts nobody) — the ORIGINAL audit's own non-blocking findings — were NOT part of this wave and stay open, not yet tracked as their own bullets.** (Distinct from the wave-3 integration gate's OWN NB-2/NB-3/NB-5, which already have separate `## Open` bullets dated 2026-08-09.) Per-tenant DO wedge B3's new `watchtower_state` prefix (`tenant_do_wedged:<tenantId>`) also feeds the still-open unbounded-growth NB-2 bullet above. ✅ evidence: merged 17e05d4 / deployed 0f197c4d; watchtower-alerts.ts:168 CRON_LEGS_CHECK; engine/isolated-failure-alerts.ts (vendor-truth lane 2, a0fd314)
- [x] 2026-08-06 [ORDER] **readDeclineCode dead on real payloads (webhook-lane finding, out of its scope):** on a real unexpanded invoice.payment_failed, `charge`/`payment_intent` are bare id STRINGS so every decline-code branch returns null — now that F1 makes the handler run, every real decline grades TRANSIENT and the A5 permanent-decline fast path (lost_card/stolen_card/fraudulent) never fires; those customers grind the full 4-strike cycle instead of immediate suspend. Real fix = a second Stripe read (retrieve the PaymentIntent/charge) — design decision, queue with the next webhook increment. Also open: audit item 7 (413 cap reads declared content-length, bypassed by streamed body) — untouched, cheap. **BOTH FIXED + SHIPPED + DEPLOYED 2026-08-09 (verified in code):** readDeclineCode now does the second Stripe read — `resolveDeclineCode`/`getDeclineCode` retrieve the PaymentIntent for `last_payment_error.decline_code` (`billing.ts:335,750-751`, `stripe-client.ts:322-353`, code-verified) — the A5 permanent-decline fast path should now fire on real payloads. Item 7 fixed via `readTextBodyWithCap`, which caps ACTUAL bytes read (not declared Content-Length) and cancels the stream at the ceiling (`validate.ts:37-79`, code-verified, docstring cites this exact audit finding by name). Merged `main` `17e05d4`, deployed `0f197c4d`, combined gate r2 SHIP (`docs/adversarial/wave3-integration-gate-2026-08-09.md`). **Still open (brief-confirmed, not this wave's scope):** live-verify readDeclineCode against a REAL unexpanded `invoice.payment_failed` delivery (the shape is code-correct but unproven against a live Stripe payload) — same live-verify need as `## Open`'s webhook-inc2-residuals bullet item (b) above; the two are the same open question from two angles. ✅ evidence: billing.ts:335 resolveDeclineCode, validate.ts:37-79 readTextBodyWithCap; merged 17e05d4, deployed 0f197c4d
- [x] 2026-08-06 [ORDER] **BYO teardown — FOUNDER RULED: NEVER touch a customer-connected domain at teardown.** Teardown skips the vendor domain-release for connection_type='connected'/BYO domains — remove only OUR records/mailboxes; the customer disconnects their own domain themselves. Zero live risk today (no BYO customers); BUILD AFTER the wave2/platform-cred lane lands (same file: engine/lifecycle.ts). Founder digest naming InboxKit: ops-only surface, acceptable under the customer-surface rule — no action. ✅ evidence: apps/platform/src/engine/lifecycle.ts:424-452
- [x] 2026-08-09 [ORDER] **Wave-3 gate NB-2 — `watchtower_state` growth still unbounded post-rewrite:** no `DELETE FROM watchtower_state` anywhere in `src/` or `migrations/` (grep-verified); the rewrite widens it with a fifth per-entity prefix `tenant_do_wedged:<tenantId>` alongside `mailbox_provisioning:`/`mailbox_rebuy:`/`cred_push_aging:`/`send_starved:` — every sweep does two full-table reads (`readWatchtowerState`, `readReportedCheckNames`, `watchtower.ts:390,395`) every 5 min, rows never reclaimed. Needs a retention/pruning design. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`, lands on main with the wave-3 merge) ✅ evidence: apps/platform/src/admin/watchtower.ts:1589 CHECK_RETENTION_MS, :1626-1634 DELETE FROM watchtower_state; scheduled.ts:147 retireChecks leg
- [x] 2026-08-09 [ORDER] **Wave-3 gate NB-3 — B3 wedged-DO visibility covers a tenant DO that THROWS, not one that HANGS:** `scanTenants` awaits `opsSummary(sinceMs)` per tenant sequentially with no `AbortSignal`/timeout, so one hung-but-not-throwing DO stalls the watchtower leg and every leg after it in `scheduled.ts`, including the heartbeat leg (deliberately last) — the dead-man then fires, but with the WRONG stated cause ("The 5-minute Cron Trigger appears to have stopped"). Fix = per-tenant timeout/race in the sweep legs. (frozen `docs/adversarial/wave3-integration-gate-2026-08-09.md`) ✅ evidence: apps/platform/src/isolated-loop.ts:125 withItemBudget; admin/ops-sweep.ts:454; documented as shipped in L246
- [x] 2026-08-10 [ORDER] **Deadlock-class detector (P0 fix's gate finding #3, highest-value follow-up):** the P0 fix above closes the PURCHASED instance only — a domain that lands in the `'unknown'` connection-type branch still waits on propagation verdicts that, if it's actually purchased, never arrive: fail-safe (no spend) but permanent and invisible, the same six-day-silence shape that just cost Mordy. Gate-enumerated every existing ops check — `mailboxProvisioningCheckName`/`mailboxRebuyCheckName`/`credPushAgingCheckName`/`sendStarvedCheckName`/`tenantDoWedgedCheckName` (`admin/watchtower.ts:56-71`) — none covers a domain stuck at `dns_status='pending'`; `DOMAIN_DNS_PENDING` (`domain-dns.ts:194-205`) is `logAction` only, no `reportCheck`, no founder mail (code-verified: single call site). Send-starved can't substitute — it needs `dueNonDemoPendingSends>0` (`watchtower.ts:265`, code-verified), and a pre-first-mailbox tenant has none. Add a watchtower `reportCheck` for domains pending past a threshold. (frozen `docs/adversarial/purchased-dns-wait-gate-2026-08-10.md`) ✅ evidence: apps/platform/src/admin/watchtower.ts:138 domainDnsAgingCheckName + :464 reportCheck; fired live for theauthorpitchdesk.com (L32)
- [x] 2026-08-11 [ORDER/ACCEPTED-RISK] **Inc5 concurrent-twin phantom residual:** a CONCURRENT identical `(body,urgency)` call that dedups against the doomed admission row in the sub-ms window BEFORE `revokeAdmission` runs still receives the phantom `201`. Requires the TRIPLE conjunct — concurrency + partial D1 write-degradation + identical body/urgency (gate r3 `R3-C1/C2/C4` confirmed each axis alone is NOT reachable); self-heals (0 orphan rows post-burst, measured; a post-recovery retry files a real ticket). Founder-orchestrator RULED ship-acceptable 2026-08-11. ⚠️ **Gate r3 refinement to the ruling's own reasoning** (`docs/adversarial/msgchannel-inc5-gate-2026-08-11.md:635-647`, verified this pass): the "originating call throws so the agent gets a signal" mitigation is THIN — of 3 concurrent twins driven in the gate's reproduction, **1 threw and 2 returned phantom `201`s**, so success is the MAJORITY response, not the error. The real mitigator is the triple conjunct + transience, not the error signal. TRUE closure (if ever needed) = a two-phase confirmed flag (dedup ignores unconfirmed rows), NOT the reconcile sweep above. **Non-blocking, same gate (NEW-4):** a revoked call frees its own rate slot, so the 5/hr cap is not enforced while EVERY call is failing — 30 sequential calls under a sustained D1 write-outage produced 30 throws / 0 tickets / 0 emails / 0 log rows, but 2 D1 round-trips each, uncapped, against an already-degraded D1. Fails closed on every effect (no ticket, no email, no phantom) — mild retry-amplification against a degraded dependency, worth a line, not a blocker. ✅ evidence: entry's own ruling; gate docs/adversarial/msgchannel-inc5-gate-2026-08-11.md r3
- [x] 2026-08-11 [ORDER] **Wire-A operator message to Mordy — FOUNDER-AUTHORIZED 2026-08-11 ("send mordys agent the updated message"; lifts the founder-gate on the 2026-08-10 P0 entry's click item):** updated paste-ready curl delivered in-chat 2026-08-11 — body says the DNS-wait fix is live (2026-08-10), retry `setup_infrastructure` with the original idempotency key `apd-setup-a-2mbx` (adopt-in-place, no re-purchase, no double charge), and mentions the new `contact_operator` tool. Pending ONLY the token-bearing execution by Yaakov: `POST /admin/tenants/ten_91aab24a-43a8-45c1-bf43-af88ef633221/messages` with `Authorization: Bearer $ADMIN_TOKEN` (prod `ADMIN_TOKEN` owner-held, no local copy; Keychain probe classifier-blocked this session). Expected `201 {"emitted":true}`; his agent sees it on its next `infrastructure_status`/`list_messages` read. His agent self-retries against the fixed gate regardless — this is acceleration, not a dependency. ✅ evidence: HANDOFF line 3: Claude holds prod ADMIN_TOKEN in Keychain since 2026-08-14; operator notice SENT 2026-08-18 04:10Z (L20)
- [x] 2026-08-11 [ORDER] **Ledger correction, verified live this pass:** `vendor_spend_entries` (remote D1, `coldstart-platform-db`) holds exactly 3 rows, all `created_at=1785182620941` (`2026-07-27T20:03:40Z`), all `tenant_id=ten_91aab24a-43a8-45c1-bf43-af88ef633221`, `period_key='2026-07'`, `kind='domain'`, `est_cents=1500` — one `status='committed'` (`actual_cents=1500`) + two `status='released'` (`actual_cents=null`). A 2026-08-02 bookkeeping pass recorded this table as empty; that claim was wrong. Confirmed old activity (pre-dates the 2026-08-10 P0 incident window), benign, no action needed — noted for metric hygiene per the standing `tenants_index` filtering caution. ✅ evidence: entry text; live D1 read 2026-08-11
- [x] 2026-08-13 [ORDER-SUPERSEDED] **🔥 P0 (original entry, root-cause detail) — idempotent setup retry MINTS A NEW LOOKALIKE DOMAIN each pass (live, customer-reported via contact_operator's FIRST real use — ticket `sup_3ca260e4`, 2026-08-12 ~19:30Z):** Mordy's agent retried `apd-setup-a-2mbx` on the post-DNS-wait-fix build; instead of resuming `goauthorpitchdesk.com` it generated AND registered a fresh lookalike `theauthorpitchdesk.com` (workspace now 2 purchased domains, both `dns_propagation_status=pending`, mailboxes still 0) and STILL failed at the step-aware error `{"code":"vendor_error","step":"domain DNS setup","retryable":true}`. Two defects implied: (a) the idempotency key is NOT gating domain generation on the resume path — contradicts the 2026-08-05 incident-hotfix's execution-verified adopt-in-place (same key → zero new buys); (b) `retryable:true` + the wire-A `retry_setup` actionHint ("retry with the same idempotency key") would burn one domain per retry in an unattended poll loop — his agent spotted this and is HOLDING; do NOT send it a retry-go until fixed. Diagnosis dispatched 2026-08-13 (read-only, hard-builder). Reply to his agent requires the owner-held `ADMIN_TOKEN` (still absent from Keychain). The 2026-08-10 vendor-contract-capture item got its capture: both purchased domains show `last_nameserver_check=null` — the vendor's checker genuinely never runs pre-mailbox. **ROOT CAUSE CONFIRMED 2026-08-13 (full chain in the fix-lane's gate doc when it lands):** wave-3 commit `85f48af` changed the domain-intent key derivation (`${setupKey}#${ordinal}` → `tenant:${tenantId}#${ordinal}`, `provision-intents.ts:119-121`) with NO backfill/fallback — Mordy's committed intent sits under the OLD key `apd-setup-a-2mbx#0` (the 2026-08-05 gate's own transcript pins it), so resume never fires, and `findAdoptableDomain` excludes any domain that already has a live `domains` row — the paid domain is simultaneously too owned to adopt and not committed enough to resume. The DNS-step failure is STRUCTURAL, not the wrong-op class: in-call wait ~2.3s vs vendor registration ~32s — EVERY first buy ends in the customer-facing retryable error that drives the retry loop. The 08-12 mint is ONE-SHOT (a new committed intent at the new key persisted before the throw — next retry resumes, buys nothing; HIGH confidence, one unverifiable link: no read path to live `domain_intents`). Classes: **C1** durable-key derivation changed without migration (sweep all persisted derivations; `mailboxIntentKey` verified unchanged); **C2** recorded-but-unclaimed resource limbo (resume keys off intents; domains rows only EXCLUDE); **C3** in-call vendor-wait budget < vendor async latency (DESIGN ASK, founder); **C4** acceptance fixtures cannot express previous-build state (the 08-05 gate verified adopt against THIS-build state — never regressed, never as broad as believed; the 08-10 gate's "self-heals on first retry" arm claim is FALSE in prod — the backfill sits behind the resume branch the orphaned key blocks). Spend truth: $30.00 booked domains, 0 mailboxes. HOLD stands: (a) ordinals ≥1 unprotected if his call asks domains≥2 (his input unreadable from here); (b) goauthorpitchdesk.com stranded either way; (c) the decisive link is the unverifiable one. Two read-only asks for his agent (ride the ADMIN_TOKEN reply): `account()` → `recentActions`, and his call's `domains`/`inboxesEach` values. FIX ARC DISPATCHED 2026-08-13: dated one-shot legacy-key reconciliation + C1 systemic guard + stale `tenant-do.ts:682-686` H1 comment + C1 sweep + C4-class fixture; direction-1 (resume-from-resource redesign) and C3 ledgered as founder design asks, NOT folded in. ✅ evidence: C3 ship 6c5140b/Worker 52a65306 (L25); Mordy provisioned 4 mailboxes 2026-08-18 (HANDOFF item 7)
- [x] 2026-08-16 [ASK] **Blind agent-recommendation probe (founder-ordered) → coldrig surfaced in ZERO of 51 queries; both fresh models picked Salesforge's Forge stack on criteria that ARE coldrig's pitch** (one first-party MCP+CLI across the full lifecycle, llms.txt, programmatic deliverability measurement, transparent self-serve pricing). We lost on discoverability, not capability — and both agents named two universal market gaps (agentic signup/billing; deliverability REMEDIATION as an API) that coldrig substantially already has and nobody markets. Frozen synthesis: `docs/research/blind-agent-recommendation-probe-2026-08-16.md`. **CORRECTED DIAGNOSIS 2026-08-16 (founder challenge "didn't we do all this already" — he was right):** the ON-SITE layer is ALREADY BUILT and thorough (llms.txt 200, /for-agents, /agent-evaluation.md, guide-* pages for the exact probe queries, compare-vs-{salesforge,smartlead-instantly,agentmail,skyp,foxreach,maildoso,diy}) and technically clean (robots OK, no noindex, Googlebot/GPTBot/ClaudeBot all 200 — verified live). The failure is one level down: **coldrig.dev has ZERO pages in the search index** (branded query + domain-restricted search both return nothing — verified live 2026-08-16). Zero backlinks → no indexing → no query can return us. The remaining option space is entirely OFF-SITE: (1) index-triggering citations — public GitHub repo for the CLI/MCP client, MCP registries/directories (mcp.so/Smithery/PulseMCP/awesome-mcp-servers), directory submissions, Search Console/Bing index requests; (2) placement in the comparison guides agents fetch (salestools.club-class — dogfood our own platform for the outreach); (3) r/coldemail organic presence; (4) G2/Trustpilot trust floor; (5) moonshot: first vendor on Stripe agent-billing (the #1 universal gap both probes named). Founder ruling needed on lanes + the public-repo decision. ✅ evidence: L30 (repo verified PUBLIC); L260 orders 1-5 are the ruling on lanes; execution tracked at L30/L259/L260
- [x] 2026-08-17 [ORDER] **Program WIDENED (founder, in-session): "fix every issue in the last 20 chats and class sweep everything and make sure it fully operational for 100's of customers."** Three additions on top of the trains entry below: (a) **full backlog drain** — consolidate every outstanding issue/defect/deferral from the last ~20 sessions (ROADMAP Now+Open, HANDOFF landmines, adversarial-gate non-blocking notes, ledgered follow-ons) into one triaged inventory and FIX them, not just ledger them; (b) class sweeps on everything (reaffirms the trains entry); (c) **NEW scale-readiness dimension — fully operational at hundreds of customers**: audit + fix what breaks at 100-500 tenants (cron sweep fan-out, DO/D1 contention, InboxKit rate limits + slot-tier laddering, engine capacity, alert/digest volume, Stripe webhook throughput, support-digest size). Inventory + scale audit dispatched 2026-08-17 late; results route into new trains behind 1+2. **SCALE AUDIT DONE 2026-08-18 (frozen `docs/adversarial/scale-readiness-audit-2026-08-17.md`): SCALE-READY: NO — three independent ceilings in hit order: (1) ~21 live mailboxes platform-wide (`DEFAULT_SPEND_CEILING_CENTS=15000` is per-PLATFORM/month at 690¢/mbx, `engine/spend-ceiling.ts:43` — config knob, but a money-guard change = founder-visible); (2) ~100 mailboxes ≈ 20-33 paying customers — the shared single InboxKit workspace + `DEFAULT_INBOXKIT_PLAN_SLOTS=10` account-wide counter, vendor top tier ~100 slots ⇒ [gated:founder] VENDOR-ARCHITECTURE DECISION (multi-workspace / direct-reseller / build-own-infra — escalates the 2026-07-28 unit-economics [ASK]); (3) ~122 total tenants — cron sweep subrequest fan-out, MEASURED subrequests(N)=8.0N+29, and above the cap trailing legs silently vanish INCLUDING the dead-man heartbeat (S1). TRAIN 6 (scale wave, queued behind 1+2, overlaps trains 3/5 members to be reconciled at brief time): S1 fan-out restructure (batch/rotate legs, bound `listAllTenantIds`) · S3 InboxKit rate-limit compliance (10 req/min, read Retry-After — bites at even 2 concurrent checkouts) · S4 `cron_legs` skip-vs-error conflation (a dying leg produces NO new alert) · S5 watchtower_state immortal rows (re-emit + D1 write every tick forever, survive release; zero DELETE in src — PROVEN by execution) · S6 285s<300s invariant omits six unbounded legs (360-435s true at 500 tenants) · S7 engine store unbounded + sync compaction (500k sends = 1.13s frozen loop; 2M = 4.9s + 1.2GB heap) · S8 unpaginated cross-tenant operator reads (merges with the promoted admin-read-endpoint findings) · S9 per-signup full 17k-row SDN pull · S10 N² clearing loops · S11 serial mailbox polling ⇒ 35-min effective cadence at 500 tenants. Passes that held: leg isolation, D1 param chunking, no shared Stripe watermark contention, per-tenant read bounds. UNVERIFIABLE from here: real CF subrequest cap (needs `wrangler tail` at ~150 tenants), CPU ceiling, live vendor limits. NOTE: arming `PROVISIONING_RECONCILE_ENABLED` adds a 7th O(N) leg (S1 ceiling ~122→~110) — one more reason the arm-gate stays shut.** **FOUNDER RULINGS 2026-08-18 (in-session):** (1) spend-ceiling "why is that even there" answered — it is the deliberate pilot blast-radius bound (overestimate-biased, $150/mo ≈ 2× pilot spend, per its own design comment); its Train-6 remedy = scale it with paying-tenant count (money-guard change, founder-visible at deploy); (2) **S2 (InboxKit workspace ceiling): DEFERRED** until more than ~1 customer at scale; direction when it binds = upgrade the plan tier or create multiple workspaces — [gated:founder] tag stands but no action now; (3) **"let's fix everything" — GO on all remaining scale findings** (S1, S3-S11) = TRAIN 6 authorized; (4) operator message to Mordy's agent SENT 2026-08-18 ~04:10Z (201 emitted, `regarding sup_3ca260e4`, severity action_required: ordinal-0 complete, goauthorpitchdesk waits on its mailbox purchase to trigger vendor DNS config, one more retry same key is spend-safe, retry-reliability fix deploying within hours). ✅ evidence: HANDOFF item 1 'TRAINS PROGRAM BUILD PHASE COMPLETE 2026-08-20'; b260bd0/ce6218b; S2 deferral tracked at L229
- [x] 2026-08-17 [ORDER] **Product-hardening remediation program (founder: "class sweeps on everything, fix for future").** Evidence base: `docs/adversarial/agent-channel-product-audit-2026-08-17.md` (PRODUCT-READY:NO, F1-F11, commit `9d3ec7e`) + six class-sweep docs (`docs/adversarial/class-sweep-{cached-terminal,hol-blocking,claim-drift,signal-inversion,dedup-semantics,watch-completeness}-2026-08-17.md`, commit `81cc3e1`, ~75 IN members total across cached-terminal 14 / hol-blocking 9 / claim-drift ~50 sites / signal-inversion 13 / dedup-semantics 23 / watch-completeness 15, overlaps included) + **sweep-completeness pass: DONE, GAPS-FOUND** (`docs/adversarial/sweep-completeness-pass-2026-08-17.md`, commit `15e6e23` — 12 missed members, a parent class ruled, 2 premises refuted, 11 consistency rulings), folded into the routing below. Remediation structured as sequenced trains. **Parent-class ruling:** "outcome doesn't carry its own provenance" — a shared result vocabulary in `packages/shared` is now a TRAIN 1 deliverable AND the acceptance criterion across every train. **Sequencing ruling:** trains 1+2 collide in `engine/provisioning.ts` (`:640-690`, 4 defects landing as ONE edit) — TRAIN 1 holds that file and merges TRAIN 2's branch when done; `feat/channel-truth-2026-08-17` becomes the shared integration branch for 1+2, gated by ONE combined adversary gate (not two). **TRAIN 1 [✅ SHIPPED 2026-08-18]** channel-truth wave (branch `feat/channel-truth-2026-08-17`) — audit F1/F3/F10/F11 + cached-terminal members 1-3,5,10 (member-10 severity CORRECTED DOWN per the completeness pass — not unrecoverable, scope = default+CHECK only) + `reconcileAlerts` loop isolation + severity 'terminal' rung + recovery-basis typing + "operator has been notified" honesty + retry-message dedup downgrade + 2 UNCERTAIN settles + the shared result-vocabulary deliverable + owns the `provisioning.ts` merge from train 2. **TRAIN 2 [✅ SHIPPED 2026-08-18]** loop-isolation wave (branch `feat/loop-isolation-2026-08-17`) — HOL members 1-5,7-9 + `forEachIsolated` guard + coverage tripwire + U1 pepper / U3 boot-crash settles. **TRAINS 1+2 SHIPPED 2026-08-18:** merged `2cef30d` (4-round adversary gate SHIP, frozen `docs/adversarial/wave-1-2-integration-gate-2026-08-18.md`); deployed Worker `3c549e5e` live on both hosts, verified `/status` ok + 28 MCP tools + new tool descriptions on the wire; site deployed Pages `1ea77c9e`, live `openapi.yaml` byte-identical to committed (verified this pass); `origin/main` confirmed at `2cef30d` (0 ahead/behind). Battery: 186f/1766p/1skip + typecheck clean, all real (non-piped) exit codes; main-HEAD re-run exact match. Non-gating residuals carried forward, each routed: **R4-1** (the new chunk-atomicity comment overstates its guarantee — a caught mid-turn throw between chunks can land a short set as terminal-success; unreachable by input today, a read-back length check hardens it — TRAIN 4, alongside dedup-semantics) · **NB-R3-1** (a `request_idempotency` key reused after 30-day ageout reports success-shaped `{deduplicated:true}` having released nothing — clean fix is `Collapsed<T>`'s `deduplicated` flag finding its first real consumer — TRAIN 4) · **R2-residual** (`commitD1Alert`'s `pending`-transition throw loses `unhealthyObs`, accepted-risk, documented in-code at `watchtower-infra.ts:75-79` — no train, closed as accepted) · gate's highest-value follow-up: **`MAILBOX_RELEASE_FAILED` / `DOMAIN_ORDINAL_FAILED` / `MAILBOX_SLOT_FAILED` reach no watchtower check** (customer-visible activity rows only, no ops-side alert path — route to TRAIN 5, monitoring denominators). **TRAIN 3 [✅ SHIPPED 2026-08-20]** claim-surface truth sweep (~50 sites: webhook enum drift ×20 surfaces, jobId async fiction, metrics claim ×7, `remove_mailboxes` quote field, support-kb 12-tools, openapi missing `remove_mailboxes` path) + claim-guard test extension + **WIDENED (completeness pass):** dashboard blind spots — W-M5 (dashboard cannot render operator messages at all — the other half of why the founder's msgread reply sat unread) + C-M1/C-M2 (9th hand-mirrored DTO copy, `vendorHealth` discriminator omitted). **TRAIN 4 [✅ SHIPPED 2026-08-20]** dedup-semantics wave (reply Message-ID data-loss contract change, DSN dedup key, thread body-hash fallback, gradeStreak/debounce intermittent-blindness, tenant-messages re-stamp/pagination, `Collapsed<T>` disclosure type) + **WIDENED (completeness pass):** `gradeFailureSignals` sub-threshold blindness (arm C widened to "any grader whose no-signal state reads as healthy") + dead `clock_multiplier` config delete-or-apply (C-M3) + demo 29d/3d clock-jump note. **TRAIN 5 [queued]** monitoring denominators (platform `counts.total` guards + watch-spec leg assertions incl. InboxKit pagination) + **WIDENED (completeness pass):** W-M1 (`cron_legs` cannot count failed alert deliveries — `AlertOutcome.emailSent` exists but `counterOf` reads only 3 number names), W-M2 (SDN refresh failure invisible to roll-up), W-M3 (false coverage docblock), W-M4 (`sweepSignals` leg not in its own bag — the alerting leg cannot alert on its own death while dead-man stays green) + accidental-invariant pins (dunning key, closed-status digest, screening default). Each train: builder → fresh adversary gate (sweeps' failing-test sketches = closure gates) → fold → deploy. **Deferred-to-[IDEA]:** mixed-time-base class DISMISSED (premise refuted — the 1440x rate is never applied; two sweeps reasoned from an unimplemented mechanism); sub-threshold-persistent class no longer deferred — folded into TRAIN 4 above. **New [IDEA]:** dedicated accidental-invariants sweep post-program (class ruled REAL; 4 members meanwhile folded into trains 1/5). **TRAINS 3+4 SHIPPED 2026-08-20:** lanes `feat/claim-truth-2026-08-19` (train 3: `003ad4d`/`cac362a`/`ea1fdaa`) + `feat/dedup-semantics-2026-08-19` (train 4: `5d0e720`..`d1dabc8`), folded on `integrate/wave-a-2026-08-20` (`7f1b50c` integration + `8582c3d` fix round + `6e3021c` ordered correction), merged `2c07add`, dashboard bundle rebuild `69ec655`. Gate `docs/adversarial/wave-a-trains-3-4-gate-2026-08-20.md`: r1 **FAIL** — 1 BLOCKING (B1: `remove_mailboxes` reported `deduplicated: true` on a call that had just irreversibly released real mailboxes — the wave's own headline field lying on the money path) + 6 non-blocking → fix `8582c3d` → r2 **SHIP**, 0 blocking, 0 new (gate re-ran its own probes byte-identically). Battery green at merged HEAD (`merged-battery`, independently re-run): typecheck EXIT:0 (5 workspaces); platform 223 files/2156 passed/1 skipped; dashboard 31 files/165 tests; engine 17 files passed|2 skipped (19)/144 passed|4 skipped (148); CLI 12/12. Deployed 2026-08-20: Worker `a74ae0ab-14ae-42cb-8bff-31ef247c2831` both hosts (`/status` ok both); site Pages `8a8333f0` (manual, project `agent-cold-email`, branch `main`); live `coldrig.dev/openapi.yaml` byte-identical to committed (`/remove-mailboxes` path + `deduplicated` field on the wire); dashboard MessagesPage chunk live (HTTP 200). r2 gate's non-blocking follow-ups routed to fresh 2026-08-20 `## Open` entries below: the generic `deduplicated` stamp at `engine/idempotency.ts:126-127` for Wave B (N1, all three `Collapsed<T>` consumers); IN-7's 30-day epoch-reset residual (NB1 — reachable member survives, qualifies rather than closes the IN-7 class-closure claim); `syncMailboxQuantity`'s disposition CORRECT but its stated reason FALSE (N2 — it does not throw on failure, contra the builder's report); support@epiphanymade.com monitoring status [ASK]; AGENTS.md:85 wording vs `capacity_pending` [ASK]; `/checkout`'s missing openapi path [IDEA]; a `contact_operator` dedup-replay 201-vs-200 wire question [IDEA, UNVERIFIED this pass]; and confirmation that the Wave-B deferral set (IN-9/10/11/12/17 + U-2, gate finding 5) is ONE alert-state design increment, not six separate items. ✅ evidence: 2cef30d (T1+T2), 2c07add (T3+T4), b260bd0 (T5+T6); HANDOFF item 1
- [x] 2026-08-17 [ORDER] **Promoted from the backlog-inventory pass (`docs/research/full-backlog-inventory-2026-08-17.md` — 97 items, 71 OPEN; the only two genuine ledger fall-throughs found):** (a) THREE never-ledgered non-blocking findings from `docs/adversarial/admin-read-endpoints-gate-2026-08-17.md` on the freshly-shipped read endpoints: unbounded queries (13.8MB response at 50k rows), `?limit=` empty-string silently truncating to 1 row, SQL LIKE pattern-vs-prefix bug — route into TRAIN 3/5 or a standalone mini-lane after the 1+2 deploy; (b) **msgchannel Inc4 (email mirror)** — founder-authorized 2026-08-05 alongside Inc2/3/5 (all shipped), never got its own tracking bullet; restored here as a queued build lane. ✅ evidence: 5bafa6c (bound reads + prefix filter), 7241a57 (S8 bound checks); Inc4 = ROADMAP ## Now L16
- [x] 2026-08-18 [ORDER] **CUSTOMER-CONTINUITY program (founder: "change documentation as well as our own response system so 1. this never happens again 2. we keep making sure he and other customers can always be continuing along").** Two pillars, built as the NEXT wave after vendor-truth deploys (folds train 3's claim-surface work in): **(P1) SELF-GUIDING RESPONSES + DOCS** — every terminal API/MCP response carries its own next action: a done setup response states what remains OUT of scope and the exact call to provision it (today's gap: a domains:1 done said nothing about slot 1 — an operator message was required); tool descriptions teach slot semantics, persona-address determinism, key lifecycle, retry cadence, AND warmup/sendReady semantics (ticket sup_71e8b4d1 needed an operator to explain: vendor-pool warmup is feed-invisible by design, sendReady=calendar-flag-not-send-gate, ramp caps allow capped sending, per-mailbox not per-setup — all four belong in the tool docs) AND the registrar_unarmed wording (tickets sup_dce385a8/sup_9d2c9a3a 2026-08-18: customer's now-UNATTENDED agent omitted registerDomains:true, got "Domain registration is not yet enabled for this account" — indistinguishable from a platform fault; the customer's own feedback: say "registerDomains was not set on this request" so an agent self-corrects instead of filing a ticket — the A-class one seam over; ⚠️ ALSO a LIVE catch against the continuity design: its example nextSteps params OMIT registerDomains on the premise omission leaves consent unchanged — this incident proves omission trips registrar_unarmed on buy-bearing calls; the design gate must rule); failure paths already gained this in the vendor-truth wave (operator_pending + same-key wording) — P1 extends it to SUCCESS paths + docs. Acceptance: replay today's incident transcript — at every step the response/doc ALONE must tell the agent its next move. **(P2) STUCK-CUSTOMER DETECTION** — per-tenant watchtower check (customer_progress:<tenant>): unfinished infrastructure (committed intents unprovisioned / paid seats unused / stalled domain) + no agent activity past a bound (~24h) → operator digest line + alert; phase 2 [gated:founder separately]: automated professional nudge message to the tenant's agent stating the next step. Generalizes the manual Mordy watch into the product. Design → adversary gate on the DESIGN (P1 changes customer-facing contracts) → build → gate → deploy. **DESIGN DONE 2026-08-18 (`docs/research/customer-continuity-design-2026-08-18.md`, 563 lines: one deriveNextSteps primitive feeding responses+watchtower+nudges, dry-run-not-sentence anti-drift rule, convergence guard I3, 8 increments) — DESIGN GATE ROUND 1: SHIP-AFTER-FIXES, 14 blocking deltas + 9 non-blocking (frozen `docs/adversarial/customer-continuity-design-gate-2026-08-18.md`; core mechanism SURVIVED — one-derivation/three-consumers/derive-don't-store; headline B1: the flagship recommended call 503s on the live tenant via the registerDomains read-path trap; B4: activity stamp must key on authVia bearer/cookie or the dashboard's timer polling makes P2 unfireable; Q1's one-nudge ruling is a persistence requirement — episode identity via AlertState, dedup key must NOT include what the nudge writes; Q2 exact-fit needs INSERT-only inboxes_each rethought + exposes R6; Q3 blame-split = blame-in-check-name + channel field on AlertPolicy, mixed→email; L1 consent branch: register_domains=0 means the step is `available` never `owed` — consent is never auto-emitted; L2 registrar_unarmed two-leg message split folded into wave). DESIGN v2 in flight → gate round 2 on the delta.** **DESIGN COMPLETE 2026-08-18 (~23:35Z): 4 gate passes, 20 blocking closed (14+4+E1+X1), 16 non-blocking folded/deferred-with-reason, 4 founder rulings incorporated, ZERO open questions; core mechanism unchanged v1→final (every attack on it failed). Final doc 1752 lines, §7 binding with precedence §7.17→§7.18→§7.19; builders start at §7.19. THE TWO SILENT-FAILURE RULES for every build brief: (1) §7.19 wave-level clock clamp — every aged anchor read as MIN(anchorTs, realNow); clock-migration shifted exactly six columns and is one-shot-already-ran, so the clamp is the ONLY fix, permanently (omission = checks silently never fire for demo-era-upgraded tenants); (2) §7.17.2 — every suppression/skip/exclusion lives in deriveNextSteps; a consumer needing its own filter means the primitive is under-specified (omission = I15 passes its own tests while un-nudging every future stall). BUILD LAUNCHED: phase A (I1-I8 contract core, hard-builder) → phase B (I9-I15 observability+nudge, spec-builder) sequential in worktree continuity, branch feat/customer-continuity-2026-08-18 → battery → fresh build gate → deploy.** **⚠️ NEW 4th ARM-GATE BLOCKER for PROVISIONING_RECONCILE_ENABLED (gate R6, pre-existing+dark): the reconciler's skip test is a row COUNT while the provisioner derives per SLOT — an acknowledged remove_mailboxes hole is invisible to the count and the re-provision is a REAL PURCHASE; also R5: INSERT-only inboxes_each means a narrowed distribution can't update, and the reconciler would re-drive toward the stale wider count.** FOUNDER RULINGS on the design's 3 questions (in-session 2026-08-18): (Q1) nudge = in-product message ONLY, exactly ONE per stall episode, sent 1 day after stall onset — no cadence, no email; (Q2) BUILD per-domain distribution into setup_infrastructure (additive contract change, own design+gate inside this wave) so any seat shape is expressible — concentration stays the customer's choice; guidance recommends exact-fit via it; (Q3) customer_progress alert channel SPLIT BY BLAME — waitingOn:operator (our blocker) → EMAIL; customer-side inaction while paying → DIGEST-ONLY ("if they're paying, who cares").** **Fold in the vendor-truth gate's non-blocking batch (gate SHIP 2026-08-18, doc `docs/adversarial/vendor-truth-gate-2026-08-18.md`):** NB-1 grade the bare-{code,message} 404 by OP not body shape (live-false premise: a valid route + bogus id returns the same shape) · NB-2 tripwire (b) accepts a pure SELECT as a durable claim (CLAIM_MARKERS includes readMailboxIntent — mutation-proven miss) · NB-3 tripwire (a) scan root excludes 3 hand-built permanents in mailbox-acquisition/mailbox-provisioning · NB-5 provisioningFailureCount now measures orphaned-intents-past-grace, not provisioning failures (D-shape inside D's fix — rename or split the field) · byo-intake.test.ts is load-flaky (5s timeout under full-suite load) · vitest --reporter=basic can exit 1 having run ZERO tests — never accept a verdict without a Test Files/Tests summary line. **BUILD COMPLETE + SHIPPED TO PRODUCTION 2026-08-19** — design gate (4 passes) → build phases A+B → 5 build-gate rounds (r1 SHIP-AFTER-FIXES → r2 SHIP-AFTER-FIXES → r3 NO-SHIP → r4 SHIP-AFTER-FIXES → r5 SHIP) → merged `cf9b323` → post-gate cleanups `0cc9feb` → deployed Worker `319e15c4-1de7-4690-be8b-61a68082a226` + site Pages `40fe9f0c`; full evidence in the fresh `[x]` entry in `## Open` below, not restated here. New findings from the fix rounds routed to fresh 2026-08-19 `## Open` entries (fill-remedy re-buy, domain-liveness mismatch, burned-ordinal-no-reason, legacy-NULL-inboxes_each closure tie, N1-N3, `billClaimSentence` anchor bug, withheld-remedy persona-split, r2-gate leftovers, emit-coverage guard idea, post-deploy watch items). The PROVISIONING_RECONCILE_ENABLED arm-gate blocker (R5/R6, this same bullet, above) is UNCHANGED by this wave and remains shut. ✅ evidence: merged cf9b323 + cleanups 0cc9feb; Worker 319e15c4; HANDOFF 'Where we are' item 4
- [x] 2026-08-18 [ORDER] **Wave-1+2 gate round 2 (re-attack at `36bec2d`): B1/B2/B3 all verified CLOSED; N1 ruled BLOCKING and pulled IN-WAVE.** The gate measured the over-release residual as UNBOUNDED, not "up to count" (20 live / ask 3 / one stuck → 12 healthy mailboxes destroyed across 6 instructed retries, non-terminating — each ~4wk of warmup reputation, irreversible), reversing the earlier ship-with-disclosure ruling which was made against the wrong number. Fix = durable per-key remove-intent (absolute retry: re-drive exactly the recorded target set's unreleased members; gate verified the mechanism on the hard case) — BUILDING now as part of this wave, closes what was briefly ledgered here as a fast-follow. N2 (teardown_records DDL via addColumnIfMissing) ruled SOUND; N3 claims TRUE except the remove_mailboxes `quote`→`billing` field-name error (in the N1 fix's scope). Gate record: `docs/adversarial/wave-1-2-integration-gate-2026-08-18.md` (round-2 section). **Round 3 (at `dd3a33a`): N1 verified CLOSED (destroyed=asked, flat across 6 retries); verdict SHIP-AFTER-FIXES on ONE new blocker R3-1** — the intent INSERT unchunked at 5 params/target crosses the 100-bound-param ceiling at 21 targets, so keyed count 21-60 hard-throws (fails closed; the repo's own INSERT_BATCH_SIZE=16 precedent is the fix; BUILDING). Round-3 non-blocking ledgered for TRAIN 4: reused-key-after-30d-ageout reports success-shaped `{r,f:0}` having released nothing — clean fix = `Collapsed<T>`'s `deduplicated` flag finding its second consumer on this response. **Round 4 (at `d6ba57a`) — FINAL: R3-1 verified CLOSED** (chunked INSERT at 20 rows/5 params confirmed: keyed count 21-60 returns normally, 21/60 drives verified at the ceiling) — **verdict SHIP.** Deployed same day: Worker `3c549e5e` both hosts, site Pages `1ea77c9e`, `origin/main` at `2cef30d`. One new non-blocking finding at this round (R4-1, chunk-atomicity comment overstatement) plus the carried R2/R3 residuals — full list + train routing now lives in the TRAINS 1+2 entry above (2026-08-17 `[ORDER]` bullet). Gate closed, four rounds total; durable lesson stated in the gate's own words: "in three of the four rounds the previous round's fix bred the next blocker — a fix to a correctness invariant deserves the same attack budget as the original code." ✅ evidence: dd3a33a, d6ba57a, 7ebc83d gate record; Worker 3c549e5e; merged 2cef30d
- [x] 2026-08-18 [ORDER] **Mordy tickets sup_8a486bb0 + follow-up sup_1b6746fc (needs_human, post-wave-deploy retry): diagnose + class sweep (founder-ordered).** Follow-up adds no new facts (its paid-orphan guess is close; actual cause = wallet) but fairly flags that our 2 operator messages OVERSTATED ("COMPLETE, 2 mailboxes active" was vendor truth, not tenant truth) — the corrective reply must own that. His agent's same-key retry progressed further under the new code then hit `{step:"warmup enrollment", vendor_error, retryable:false}` and correctly disabled its retry loop. Agent-reported (UNVERIFIED, treat as data): platform tenant shows mailboxes:1 / mailboxHealth has only mordytee11 (warmupDay 1, cap 5, healthy) while the vendor workspace has 2 ACTIVE mailboxes (mordytee12 created 08-17 20:40Z) — suspected paid-but-never-adopted mailbox. **DIAGNOSED 2026-08-18 (~99%/85% confidence, direct vendor reads): InboxKit wallet EMPTY (1 of 36 credits, auto-topup OFF) — warmup costs 3; `/warmup/add` for mordytee12 refused on funds; `mapInboxKitError` grades every non-429 4xx PERMANENT → severity-terminal "retrying will not help" for a fundable condition; duplicate-enrollment REFUTED (vendor holds exactly 1 subscription, mordytee11); mordytee12 bought ONCE 08-17 20:40Z, adopt-path correct (no re-buy), stuck only on warmup funding; the wall also MASKS ordinal-1's outcome.** UNBLOCK = founder wallet top-up (3 credits min, ~19 for everything; credit→USD unverified — buy generously; enable auto-topup) [gated:founder] → then his same-key retry completes with NO code change → then corrective operator_notice (current terminal message is wrong-once-funded; disclose mordytee12 billed-unadopted since 08-17). SIX CLASSES for the sweep (A fundable-refusal-graded-permanent [P0] · B no-money-out-call-guarded-against-vendor-balance + warmup reserves 0¢ + no wallet watchtower check · C vendor-resource-without-platform-row undetected · D operator-read-scoped-by-prefix reports empty-as-truth + missing admin surfaces for mailbox_intents/buy_dispatches/mailboxes · E marker-after-billed-effect + no already-enrolled pre-check (latent re-charge window) · F adapter live-contract drift: getHealth destructures fields the live response lacks → NaN bounceRate into the deliverability loop [probed live, certain]). Sweeps DONE (3 sweepers, A-F inventories) + **completeness pass DONE: ALL SIX GAPS-FOUND** (canon frozen `docs/adversarial/class-sweep-vendor-truth-2026-08-18.md`; headline corrections: C14 mis-tagged OUT — vendor-gone rows OVERBILL the customer via syncMailboxQuantity billing on released_at IS NULL counts; `release()` NOT idempotent — retry-after-success throws permanent, released_at never written, row bills forever [manufactures the phantom rows]; E4 findAdoptableDomain buys on a FAILED pre-check [catch→null]; 401/402/403 graded permanent [one rotated vendor JWT = every tenant told "check your inputs" terminal]; guard-A-as-sketched would REOPEN the vendor-verdict class [scope to THROWING branches only — 3 sites return inconclusive deliberately]; wallet response is snake_case [camelCase reader under `body as T` = check healthy forever]; warmup-reserve>0 test PREMISE-WRONG [the $3 is inside COST_MAILBOX_CENTS — do not double-reserve]; severity rung = QUARTET not quintet [no DB CHECK]; no migrations needed). **SCOPE RULING (orchestrator, 2026-08-18): FIX WAVE = two lanes, one integration, one gate, deploy.** LANE 1 (hard-builder, adapter+engine+shared contract): shared grader scoped to throwing branches + operatorActionable on VendorError (funding + 401/402/403 + insufficient-balance-URL site) + honest emission (error-response third branch per CapacityPendingError template, setup_failed non-terminal for operator-actionable, severity 4th rung quartet incl. tools.ts+openapi) + E1 pre-check-then-act (warmupSubscriptionState promoted to MailboxPort, re-read not snapshot, inconclusive→retryable) + release() idempotency (absent-at-vendor on a recorded-owned row = released, write released_at; vendor-truth-conflation lessons apply) + E4 throw-on-failed-list + F3 zod seam on READ endpoints + getHealth honest (real live fields, no NaN, no fabricated default) + showMailboxCredentials operator-actionable + fictional fixtures deleted + clamp01 non-finite + rewrite the 2 defect-pinning tests + tripwires (grader coverage, billed-effect pre-check). LANE 2 (spec-builder, observability): vendor_wallet watchtower check (account-wide, snake-case-correct schema-parsed read, floor ~10, unhealthy when below-floor OR autoTopup-off-below-floor, + duplicate-warmup-subscription detection on the same call family) + C stage 1 intent-orphan checks (per-address names, rides opsSummary fan-out, grace bound) + D minimal (provisioning-state gains mailbox_intents+buy_dispatches + scope param default ALL + limit AND total together; D8 hardcoded provisioningFailureCount wired to the orphan check or deleted; D9 limit= empty bug; D7 unhealthyCount two-store disclosure) + REPLACE_DOMAIN operator alert on plain VendorError + ACTIVATION.md wallet-funding step. **DEFERRED to trains (ledgered, not dropped):** C stage 2 bidirectional vendor diff + vendor_spend_entries resource-identity schema (train 5/6) · D full tripwire + remaining admin reads (train 5) · B cached-balance pre-flight reserve phase (design follow-up) · E10/E12/F13 + /warmup/cancel-duplication + funds-refusal wire shape (vendor questions / live capture at next event) · F live-contract probe cadence (arming steps). C17 ruling: C's detection must NOT depend on the dark reconcile (ratified). **TRAIN 3 ADDITION (found 2026-08-18 while resolving the incident): the setup_infrastructure tool description documents NOTHING about slot semantics** — `domains: N` covers tenant ordinals 0..N-1 (a repeat domains:1 call is a no-op once slot 0 is satisfied; reaching slot 1 requires domains:2; persona must stay identical or new deterministic addresses get bought on satisfied slots). The customer's agent needed an operator message to learn this — the tool description must teach it. **WAVE ADDENDUM (founder 2026-08-18): operator surface renames readAt → ackedAt** (read_at's only writer is the tenant's explicit ackMessage; null ≠ unseen — this misled the operator all day during the incident; lane 2 owns the rename, lane 1 clarifies the MCP tool descriptions; tenant-facing field names untouched). — is that mailbox monitored/routed? Every other surface says `support@coldrig.dev` (claim-drift sweep UNCERTAIN). Founder confirm or we change `terms.html` in train 3. **FIX WAVE SHIPPED 2026-08-18:** merged `a0fd314` (both lanes + integration), deployed Worker `54b00702` (both hosts — confirmed as the latest version via `wrangler deployments list`, created 20:52:42Z) + site Pages `62115561` (Production, `main`, commit `a0fd314`, confirmed via `wrangler pages deployment list`); `/status` 200 both hosts this pass. **Gate doc RESTORED and verified this pass:** `docs/adversarial/vendor-truth-gate-2026-08-18.md` (re-emitted post-reap; original was untracked in the reaped lane worktree — authentic re-emission by the gate agent from its own context, committed `629a20e`, unpushed local-`main`-only as of this pass) reads **SHIP, 0 blocking, 6 non-blocking (NB-1..NB-6)**, battery `190 files/1831 tests passed/1 skipped, exit 0` on the clean re-run (one earlier run redded on an unrelated `byo-intake.test.ts` 5s timeout under load, isolation-confirmed non-regression) — matches this ledger's earlier "190f/1831p" citation exactly, and **bookkeeper-reconfirmed this pass by an independent full re-run**: `npm test` → 190 files/1831 tests passed/1 skipped exit 0 (platform) + CLI 12/12 passed exit 0; `npm run typecheck` → exit 0 across all 5 workspaces. Gate's own **LIVE-STATE CORRECTION is the authoritative incident timeline**: the pilot's retry settled `setup_infrastructure:apd-setup-a-2mbx` to `status:"done"` at **2026-08-18T18:22:37Z** (read live from `provisioning-state`), `mordytee12`'s warmup enrolled 18s later (`2026-08-18T18:22:55Z`), and wallet `credits_used` moved 35→38 (exactly one `/warmup/add`) — all UNDER THE OLD CODE, before this deploy. At gate-probe time (between 18:23Z and the 20:52Z deploy) `goauthorpitchdesk.com` still read `dnsStatus:"pending"`, 0 mailboxes, "still owed a fresh `setup_infrastructure` key" — the brief's further claim of a ~19:14Z setup-B buying 2 mailboxes there with DNS flipping READY is a LATER event this gate doc does not cover; **orchestrator-reported, not independently re-verified against live admin reads this pass** (Keychain-probe classifier-blocked). Warmup-semantics ticket `sup_71e8b4d1` answered; his side is now fully unattended agent-to-agent (ticket `sup_dce385a8` retracted by `sup_9d2c9a3a`, registrar_unarmed wording feedback — folded into the customer-continuity design above). Watch continues. ✅ evidence: merged a0fd314, Worker 54b00702, gate 629a20e SHIP 0-blocking; HANDOFF item 7
- [x] 2026-08-19 [ORDER] **(moved from `## Now` at the 2026-07-28 re-triage; originally dated 2026-07-28, content unchanged)** **2026-07-27/28 hardening + claims wave COMPLETE — five items shipped, zero active build lanes** (`git worktree list`: only this repo + the external `coldrig-design` worktree remain). (1) SDN weekend-false-alarm fix — 2-round adversary arc (NO-SHIP r1 → fix A → SHIP r2), merged `f583fe5`, deployed Worker `b672b403`; LIVE-VERIFIED via a real droplet re-push → HTTP 200 `{"reason":"unchanged"}`, `fetched_at` advanced, `failure_streak` 0 — weekend alerts dead, droplet-down backstop proven by a co-fire test; honesty-statement addendum appended `docs/research/ofac-v1-honesty-statement-2026-07-23.md`. (2) Pre-send intent log (closes the GA double-send / ACTIVATION Gate-2 residual (1)) — design SHIP-after-fixes → build SHIP 0-blocking → NB1 `sentOnWire` guard DELTA-SHIP; merged `608c80a` (`apps/engine/src/send-log.ts` WAL + `apps/engine/src/reconcile.ts` boot reconciliation); **engine-side, `[dark-unarmed]` until arming is verified in prod** — ⚠️ `ACTIVATION.md`'s Gate-2 residual (1) line still reads "uncommitted, pending review + adversary re-gate + merge" as of this pass — now STALE, the chain has since merged; not corrected here (out of this bookkeeping brief's file scope), flagging for the next doc-touch. Frozen verdicts `docs/adversarial/{pre-send-intent-log-design-review,intent-log-build-review}-2026-07-27.md`. (3) CLI stale-claims fix — retired waitlist/early-access line killed, claim-surface guard test added, `0.2.0`→`0.2.1` (verified: `packages/cli/package.json` reads 0.2.1); merged `7dd0182`/`5e09d46`. npm publish of 0.2.1 still PENDING founder OTP (click queue below). (4) Go-live UX — 3-round adversary arc (money-path selector-inert defect caught pre-deploy; billable-vs-count-all display≠charge class closed via a single-sourced `billableMailboxCount` helper; state-derived chips; double-checkout server guard 400-on-active; work-email→Email; stale "not active yet" banner killed; sandbox banner + checklist go-live step); merged `15a2e01`, deployed Worker `3a031af4` + site — live-verified; frozen `docs/adversarial/golive-ux-review-2026-07-27.md` + screenshots `pw-shots/golive-review-2026-07-27/`. (5) Claims-truth pass — price-math rubric ("The all-in math, not just the sticker price." — live heading `site/pricing.html:173`) on pricing/for-agents/llms.txt/README/compare; purchase-path + Stripe promo-code clarity (verified live on `site/for-agents.html`/`site/faq.html`); concierge swept to scoped truth across 40 surfaces incl. `plugin.json`/`llms-install`/`cli-README`; no temporal activation claims; provisioned-count billing line; 177-assertion guard; adversary SHIP-after-fixes applied; merged `ec0f972` (hand-resolved 8 conflicts toward 25-tools/0.2.1/scoped-truth); site deployed `e1bfe52c` (bookkeeper-reconfirmed: Pages deployment off `ec0f972`, `coldrig.dev` → 200) + IndexNow 200; frozen `docs/adversarial/claims-truth-pass-review-2026-07-27.md`. Bookkeeper-reconfirmed this pass: Worker `/status` → `{"status":"ok"}`, engine `/health` → `{"status":"ok"}`; main is 14 commits ahead of `origin/main` / 0 behind (push pending, not a divergence). ✅ evidence: f583fe5/b672b403, 608c80a, 7dd0182, 15a2e01/3a031af4, ec0f972/e1bfe52c
- [x] 2026-08-19 [ORDER] **(moved from `## Now` at the 2026-07-28 re-triage; originally dated 2026-07-28, content unchanged)** **🎉 MORDY PAID — first real customer (17:06Z).** tenant "Press Outreach" (`ten_91aab24a-43a8-45c1-bf43-af88ef633221`); Stripe sub `sub_1TyESe…` ACTIVE (platform×1 + mailbox×5), MORDYPILOT redeemed, invoice `in_1TyESe…` amount_paid **$39.60** (verified). ⚠️ `tenants_index.plan` still reads `demo` — that column is NEVER updated post-signup (by design); read the DO/Stripe for truth. Post-payment fixes shipped SAME session: Stripe success/cancel redirects were UNROUTED (404 JSON — what Mordy saw) → now 302 → /app/billing (Worker `860542ea`, commit `6c84648`); `api.coldrig.dev` branded API host added (Worker `f94139e1`, commit `fcb233e`; workers.dev kept). **CRITICAL PATH to his first send — (a) NOW CLOSED 2026-07-29, (b)-(d) PENDING his agent:** (a) `REGISTRAR_PROVIDER=inboxkit` + per-tenant `registerDomains` opt-in — SHIPPED LIVE + ARMED (full evidence chain drained to `archive/ROADMAP-done.md` 2026-07-29: merge `c1e24a5`, deploy `4a7bbf0d`, `REGISTRAR_PROVIDER` secret armed, post-arm live checks green); (b) his agent still needs to run `setup_infrastructure` **twice** — `{domains:1, inboxesEach:3}` then `{domains:1, inboxesEach:2}` = 2 domains × (3+2) mailboxes, all 5 paid seats, bill stays $39.60; (c) fleet-mint OAuth verify (his mailboxes are its FIRST live test) or the 10-min manual grant fallback; (d) first-live warmup-ramp watch. Wallet funded $36 (founder). `authorpitchdesk.com` domain-connect still walled (CF-registrar) but MOOT — pilot uses InboxKit-registered lookalikes, not his domain. Founder is next to send Mordy the go message (drafted in-chat) — nothing in (b)-(d) has happened yet as of this bookkeeping pass. **UPDATE 2026-08-06 (wave-2 close):** provisioning fix + auto-send + the real wall-clock migration are ALL LIVE (`main` `f7ee786`, Worker `8187d36e-056d-4d3b-a52f-3814dd4fcff9`, D1 migration 0016 confirmed applied remote — `wrangler d1 migrations list --remote` reads "No migrations to apply!"). His retry (same key `apd-setup-a-2mbx`) now adopts-in-place + provisions once the vendor's DNS/NS-checker flips ready (still pending as of the last poll — workspace 0 mailboxes, `goauthorpitchdesk.com` DNS-pending, vendor's own nameserver checker never run). Once provisioned, sending still needs the manual OAuth credential mint — no longer a silent gap: the `cred_push_aging` watchtower alert (`admin/watchtower.ts`) now surfaces it. ✅ evidence: HANDOFF item 7: 4 mailboxes across 2 domains provisioned 2026-08-18, ripening ~Sep 14-15; (c) tracked at L228
