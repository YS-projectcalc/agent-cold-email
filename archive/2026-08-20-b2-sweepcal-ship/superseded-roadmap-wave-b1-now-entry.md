# Superseded ROADMAP.md `## Now` entry (Wave B.1 ship summary)

Kept verbatim for provenance. Superseded 2026-08-20 by the Wave B.2 (alert-state build) + sweep-calibration fix `## Now` entry — Wave B.1's own facts are unchanged and remain live in `HANDOFF.md`/`MEMORY.md`.

---

- [ ] 2026-08-20 [ORDER] **WAVE B.1 (TRAINS 5+6 core: scale + monitoring) SHIPPED TO PRODUCTION 2026-08-20** (supersedes the Wave A `## Now` entry that was here — Wave A's own content is unchanged and preserved at `archive/2026-08-20-wave-b1-ship/superseded-roadmap-wave-a-now-entry.md`, still live in `HANDOFF.md`/`MEMORY.md`). Lanes `feat/scale-monitoring-2026-08-20` (`67f3535`/`83ad672`) + `feat/scale-modules-2026-08-20` (7 commits to `6434b4b`), folded on `integrate/wave-b1-2026-08-20` (+ S8 increment `7241a57`, fix rounds `a6a0b0b`/`de1619f`), merged `b260bd0` (current `main` HEAD = `origin/main`). Gate `docs/adversarial/wave-b1-scale-monitoring-gate-2026-08-20.md`: r1 FAIL (1 BLOCKING — B1, the `sdnRecovery` leg could spend the whole invocation budget ahead of the dead-man heartbeat and silently kill every trailing leg incl. the dead-man itself, exactly the failure class this wave exists to prevent; +9 non-blocking) → r2 checklist-SHIP (0 blocking, 4 NEW: a reaper same-class gap, a tautological budget guard, N7 unswept consumers incl. a money guard, an N6 boundary) → r3 FINAL SHIP (dual-oracle budget guard replacing the tautology, reaper bounded, `ABSENCE_MIN_AGE` raised to 45min with a pinned ordering invariant; 0 blocking, 1 non-blocking prose fix). Battery green at merged HEAD (independently re-run by `merged-battery`): typecheck 0; platform 233f/2268p/1skip; dashboard 31f/165p; engine 18f+2s(20)/153p+4s(157); cli 12/12; wrangler dry-run clean, zero binding delta. **Deployed 2026-08-20:** D1 migrations `0019_sweep_cursor` + `0020_sdn_entries_name_index` applied remote (confirmed via `wrangler d1 migrations list --remote`, clean, no 7403); Worker `133fc911-defd-4713-beee-1654ee53d7e2` both hosts (`/status` ok both, live-reconfirmed this bookkeeping pass); NO site deploy needed (gate-verified: no `site/`/`packages/`/`apps/dashboard`/`wrangler.toml` change in any of the 3 rounds, changed admin endpoints are operator-only, no published `openapi.yaml` path touched). Post-deploy live-reconfirmed this pass: `GET /admin/ops/checks?unhealthy=1` returns the new paged/denominated response shape (`count`/`total`/`truncated`/`expected`/`missing`/`sweepAgeSeconds`/`sweepStale`/`retentionMs`), current baseline **1 unhealthy** (`customer_progress_agent:ten_91aab24a…`, owed `message_action_required` step outstanding >142h — until his agent acks); `GET /admin/support/digest` `unaccounted:0`. **FOUNDER-VISIBLE money-guard change, DEPLOYED and LIVE:** default monthly spend ceiling moved **$150 → $180** at 1 paying tenant (`apps/platform/src/engine/spend-ceiling.ts` — `ceiling = PLATFORM_BASE_CENTS($60) + PER_PAYING_TENANT_CENTS($120) × PAYING_TENANT_COUNT`, `PAYING_TENANT_COUNT` unset defaults to 1; `SPEND_CEILING_CENTS` is an absolute override that FREEZES the formula; a raise is durable for the calendar month — manual lowering is a `wrangler d1 execute` UPDATE on `vendor_spend_ledger.ceiling_cents`, no admin route exists yet, form documented in the `readInForceCeilingCents` docstring). Founder informed in-session 2026-08-20; explicit ack still awaited (see `## Open`). **ALERT-STATE DESIGN FROZEN at v4** (`docs/research/alert-state-design-2026-08-20.md`, 831 lines, complete) after a 4-round design gate (`docs/adversarial/alert-state-design-gate-2026-08-20.md`, blocking count 6→2→1→0), consolidated into an 18-constraint build brief in the gate doc's final section (`## CONSOLIDATED BUILD BRIEF`). **`## Now` = WAVE B.2 (alert-state build against the frozen design), dispatching now** — msgchannel Inc4 (email mirror) still queued behind it. ⚠️ [RATIFY:founder] pending on the design's §9.13 alert-volume ceiling stated in inbox units (ratified baseline ~2/day today; ~30/day measured in a worst-case full per-entity storm; ~42/day theoretical incl. up-to-2/day `alert_budget_exceeded` notices, empirically ~1.1/day debounced) — this gates ARMING the increment, not building it.
