# ColdStart / coldrig — Handoff

Agent-operated cold-email platform. **LIVE:** site https://coldrig.dev · API+dashboard https://agent-cold-email-api.yaakovscher.workers.dev (`/app`) · npm `agent-cold-email@0.2.0` (0.2.1 built on disk, npm publish PENDING founder OTP) · MCP Registry 0.2.2 (25 tools) · repo https://github.com/YS-projectcalc/agent-cold-email · Code: `~/dev/coldstart/`

> **You are resuming coldrig with zero prior context. Re-orient from `## Resume` below, then VERIFY its preconditions still hold** (lane states, git SHAs, live URLs). If they hold and the step is non-destructive, proceed — don't ask open-endedly what to work on. If anything CHANGED, surface exactly what and ask. **STOP and confirm before any destructive/irreversible/founder-owned step** — EXCEPT where the 2026-07-22 autonomy grant (below) explicitly covers it.

## Where we are right now (2026-07-28, 07-27/28 hardening + claims wave COMPLETE — zero active build lanes)

**Standing authorization (founder, verbatim on the ledger):** *"Keep working autonomously. You have authorization to merge and push and deploy everything when done so that it's ready for all customers to go. make sure to update all info everywhere."* Plus arming authorization: arming steps execute autonomously (Keychain keys + SSH droplet + wrangler secrets); only browser-consent clicks return to the founder. Full program definition: `ROADMAP.md ## Open` → "AUTONOMOUS GO-LIVE PROGRAM AUTHORIZED" entry (2026-07-22). Confirmed this pass: `git worktree list` shows only this repo (`main`, at `ec0f972`) plus one external worktree, `/Users/yaakovscher/Documents/Codex/2026-07-14/i-m-building-out-coldrig-you/coldrig-design` on branch `coldrig-human-interface` — everything under `.claude/worktrees/` against this repo has been reaped. Main is **14 commits ahead of `origin/main`, 0 behind** — push is pending founder/main-loop cadence, not a divergence.

- **The program's original build phase (quantity billing / SDN relay / provisioning-core GA) is still complete** — see the entries below, unchanged since 07-24. **On top of that, a 2026-07-27→28 hardening + claims wave shipped five more items**, all bookkeeper-reconfirmed this pass; full evidence in `ROADMAP.md ## Now`'s 2026-07-28 entry: (1) the SDN weekend-false-alarm class fixed (merged `f583fe5`, deployed Worker `b672b403` — a byte-identical weekend re-push is now `ok:true, reason:"unchanged"`, HTTP 200, `fetched_at` advances so the direct-refresh cron doesn't also storm); (2) a durable pre-send intent log built in the engine (merged `608c80a`, `apps/engine/src/send-log.ts`) closing the GA double-send residual — **`[dark-unarmed]`, not yet armed in prod**; (3) a CLI stale-claims fix (waitlist line killed, `0.2.0`→`0.2.1`, merged `7dd0182`/`5e09d46`) — npm publish of 0.2.1 still pending founder OTP; (4) a 3-round go-live-UX adversary arc (money-path + billing-display defects closed) merged `15a2e01`, deployed Worker `3a031af4` + site; (5) a claims-truth pass (price-math rubric, purchase-path/promo-code clarity, concierge caveat scoped truthfully across ~40 surfaces) merged `ec0f972`, site deployed `e1bfe52c`. **Live prod right now: Worker `3a031af4` (bookkeeper-verified `/status` → `{"status":"ok"}` this pass), site Pages deployment `e1bfe52c` off `ec0f972` (bookkeeper-verified `coldrig.dev` → 200 this pass), engine `/health` → `{"status":"ok"}` via the Cloudflare Tunnel (armed 2026-07-27).**
- **Quantity billing SHIPPED LIVE (07-24, unchanged this pass).** Design arc closed (founder delegated the committed-vs-active meter call → landed on ACTIVE/PROVISIONED; a second adversary round caught a genuine double-billing bug in burn-replacement, fixed via a bill-invariance governing rule; FINAL SHIP `5b1edf7`). Build (10 commits) merged `4e53e2d`. **Battery #7 caught a real class**: 817/817 tests green but `tsc` failed — the plan-tier collapse left 2 stale `"launch"` literals in a test file that only typecheck caught, not vitest; fixed `313963e`. Deployed `7f197f7d` (migration 0015 confirmed applied remote). **Live-verified via a read-only Stripe Price lookup (no checkout session created):** `coldrig_platform_monthly_v1` = $49.00/mo product "Coldrig Platform", `coldrig_mailbox_monthly_v1` = $10.00/mo product "Coldrig Mailbox", both `livemode:true` — a 5-mailbox checkout totals exactly $99.00. Full evidence + the API-breaking-change note: `ROADMAP.md ## Open` "QUANTITY BILLING SHIPPED LIVE".
- **SDN screening runs against the real Treasury list, and the weekend-false-alarm class is now closed** (see item (1) above). Relay branch merged `3b29fc5`, deployed through `71bfa830` after fixing two live wire-shape bugs (Treasury HEAD/GET mismatch; a trailing 0x1A DOS-EOF byte). `sdn_entries` verified holding 19,241+ real rows historically. Full evidence: `ROADMAP.md ## Open` "SDN RELAY SAGA COMPLETE" + the false-alarm entry.
- **Repo chores + billing-claims truth sweep both merged and deployed (07-24), now further hardened by the 07-27/28 claims-truth pass above.** Chores (`5aa4154`): openapi pinned 3.0.3, BYO tool-count row added, aeo-panel tracks both brand names, dev-doc titles rebranded. Billing-claims sweep (`66928b9`, 11 files): every "billing live" claim on the site is accurate; site redeployed (`7c6c240d`), superseded by the later `e1bfe52c` deploy.
- **Ops facts, 2026-07-28 midday (D1-verified this pass):** 63 total tenants in `tenants_index`; tenant #63 "Northwind Robotics" (`ten_763c5eb7…`, `contact_email` in the `demo+<timestamp>@example.com` pattern) is a CLI demo run, **not a new human signup**. Mordy's tenant "Press Outreach" (`ten_91aab24a…`, `mydevpros@gmail.com`) is live in sandbox with `plan="demo"` — **unpaid**. Glama's own listing API still reports "~12 tools" after the founder's Build & Release click (unrelated to the confirmed-correct 25-tool server-card/site truth) — needs a manual description-field edit, now in the click queue. Overnight paid-tenant watch reported expired clean (not independently re-checked this pass).
- **Founder rulings (07-21/22, still governing; all on ledger):** OFAC-stubbed pilot YES (superseded — real screening is now live) · prewarm pilot buy WITHDRAWN · Mordy price 60% off · registrar = Cloudflare default · review-sites list-now/PH-defer + dogfood scope (delegated) · magic-link login + human signup + one-funnel "Free sign up" (shipped) · warm-lead Q1-Q6 ratified (built + deployed).

## In flight / next

- **In flight: NONE.** No agent build lane is currently running — both the original build phase and the 07-27/28 hardening + claims wave are complete. Do not start new autonomous build work without a fresh founder-scoped ask; the remaining items below are gated on the founder, on Mordy's onboarding, or on the InboxKit connect wall — not on further building.
- **Founder click queue, by leverage — DONE this wave:** Cloudflare Tunnel login (engine armed) → `ENGINE_BASE_URL` set → droplet firewall 8080 dropped → Bing Webmaster verify + Request-Indexing → GSC Request-Indexing → AlternativeTo account created → Glama "Build & Release" clicked (but the listing's own tool-count cache did NOT refresh — see Ops facts above — a manual description edit is now queued). **REMAINING:** InboxKit UI connect attempt ★ (2 min — see the CF-registrar wall below) → Glama manual description edit (drafted text delivered in-chat) → npm publish 0.2.1 (founder OTP) → send Mordy his ready-message → G2/Capterra listing packs → `listings@` routing rule → PulseMCP word → Stripe dashboard support_email → uptime-prober service name.
- **InboxKit CF-registrar connect wall (found 2026-07-27, research frozen 2026-07-28 — `docs/research/cf-registrar-domain-connect-2026-07-28.md`):** Cloudflare-registered domains can't change nameservers, so InboxKit's NS-delegation connect path is impossible for them, and the alternate `cloudflare-domains/connect` API path 400s `"domain is not allowed"` even on a bare pre-created zone — cause unresolved from public sources (account-scope-token and undocumented-feature-gate hypotheses survive; a sharpened support ticket is drafted in the research doc). This blocks BOTH `dmhadvisor.com` (our own probe domain) and Mordy's `authorpitchdesk.com` (RDAP-confirmed CF-registered). Industry check: Maildoso's precedent validates our lookalike-domain default and rules out subdomain-NS-delegation as a first-class path. Next step is the founder's 2-minute UI connect attempt (`app.inboxkit.com` → Domains → Connect Existing Domain → paste the CF token from Keychain `cf-dns-dmhadvisor`) — the verbose UI error usually states the real objection; if it also fails, send the drafted support ticket.
- **Mordy onboarding gates (unblocked by his signup, not by us):** tenant "Press Outreach" is live in sandbox, unpaid, plan=`demo` — ready-message to him still pending founder send. Domain connect on `authorpitchdesk.com` is blocked by the CF-registrar wall above. Engine leg is ARMED — the `ENGINE_TENANTS` allowlist is RETIRED, replaced by the self-serve activation predicate (`apps/platform/src/engine/activation.ts`), so what remains for him is the credential-push grant at provisioning time · `MORDYPILOT` (60% off, single-use) is live and ready.
- **Post-GA follow-ons, ledgered non-blocking:** baseline-reset recovery doc for a >10% legit SDN delisting · event-driven alert class sweep (per-tenant-action storms) · agency-bundle billing design (deferred out of this arc) · buyer-panel cycle 3 (after the founder's Glama re-scan) · optional `promoCode` checkout-tool input (build candidate from the purchase-path-clarity class, not yet built).
- **Watchdog:** the 3h session-only cron has not been confirmed re-armed since the 07-22 classifier denials — verify/re-arm from a session where `CronCreate` passes, or get the founder to allow the tool.
- **Open founder items (none block anything):** www.coldrig.dev custom-domain click (NXDOMAIN — and the Stripe business profile lists `https://www.coldrig.dev`!) · Cline test [gated:founder-hold] · Instant Start SKU price ratification (gated on credits→USD verify).

## Landmines / gotchas

- **Live money is now real (as of 2026-07-23), on TWO durable Prices now.** The Worker runs LIVE Stripe keys — NEVER complete a live checkout in testing; a completed session charges a real card. `MORDYPILOT` is live-mode, single-use, and consumable by anyone with the code. Checkout now creates a subscription against durable live Prices (`coldrig_platform_monthly_v1` $49, `coldrig_mailbox_monthly_v1` $10 × mailbox count) rather than the old inline `price_data` — verified this pass via a read-only Stripe Price lookup (no session created). Never print Keychain values; read via `security find-generic-password -a coldrig -s <service> -w`.
- **⚠️ BREAKING API CHANGE: `POST /checkout` now REQUIRES a `mailboxes` count (5-60) and has no `plan` tier field at all.** The old tiered-plan checkout shape is gone (`packages/shared/src/intents.ts`, `CheckoutInput`). `site/openapi.yaml` and the MCP tool descriptions are confirmed already updated to the new `{ mailboxes, interval }` shape — but any EXTERNAL cached example (a customer's agent that memorized the old request shape, a screenshot, a third-party integration doc) will now 400. If you see a checkout failure that looks like a stale-shape request, check for a `plan` field in the body before assuming a real bug.
- **InboxKit is ARMED and real provisioning is REACHABLE for paid+screened tenants.** The provisioning-core GA merge (`8674da7`) wired `inboxKitConfig` into the DO factory call — a tenant that goes paid + active + OFAC-screened-clear now triggers REAL InboxKit vendor calls, not a sandbox stub. Containment: $150 vendor-spend ceiling, slot cap, G5 domain hard-block, and — as of this bookkeeping pass — zero tenants have actually reached a real-send-activated state.
- **SDN screening runs against the REAL Treasury list — never manually swap SDN lists without the monotonicity/baseline context.** `sdn_entries` holds 19,241 real rows (verified this pass) loaded via the droplet-relay ingest (`3b29fc5` → `71bfa830`). The ingest has a content-hash check AND a monotonicity guard: a byte-identical re-push (e.g. the droplet's daily weekend relay when Treasury publishes nothing new) is accepted as `ok:true, reason:"unchanged"` (200, no swap of `active_version`/`content_hash`/`entry_count` — class fix 2026-07-27, was wrongly `reason:"stale"`/422/ok:false before, which conflated a benign no-new-publication with a refresh failure and grew the ops-alert streak every weekend). **Fix A (same day, adversary NO-SHIP on the first cut — `docs/adversarial/sdn-unchanged-fix-review-2026-07-27.md`):** an "unchanged" push ALSO advances `sdn_list_meta.fetched_at` to the push time — the actual weekend-storm cause is the Worker's OWN 5-min direct-refresh cron (`maybeRefreshSdnList`, `sdn-refresh.ts`) 525-ing every attempt once `fetched_at` is >24h stale; leaving `fetched_at` untouched (the first cut) left that loop firing all weekend and even produced false RECOVERED↔failing flapping. Advancing it makes the direct refresh see "fresh" and skip its own doomed fetch for 24h; the genuine-staleness backstop is preserved (relay stops → `fetched_at` goes stale → direct refresh resumes → 525 → alarm), proven co-fired in `apps/platform/test/ofac-sdn-storm-interaction.test.ts`. A suspicious single-step entry-count DROP of >10% from the active list is still rejected as `reason:"stale"`/422 — that guard's security semantics are unchanged. If you ever need to hand-load or replace the list, you MUST go through the same ingest path with the same guards — a raw D1 write that bypasses `swapInSdnList`/the monotonicity check could silently accept a corrupt or truncated list. A genuine large legitimate delisting (>10% drop) would ALSO get rejected by that guard — known, accepted, non-blocking (baseline-reset recovery doc unbuilt, see In-flight); don't "fix" it ad hoc without reading that context first.
- **Worktrees against this repo: ZERO active (confirmed via `git worktree list` this pass).** Every `.claude/worktrees/` lane from the build phase and the 07-27/28 hardening wave is reaped. The ONLY entry `git worktree list` shows besides the main `~/dev/coldstart` checkout is an external, unrelated worktree at `/Users/yaakovscher/Documents/Codex/2026-07-14/i-m-building-out-coldrig-you/coldrig-design` (branch `coldrig-human-interface`) — that one is not part of this program and is expected to keep showing up. If you see any OTHER worktree directory in a future session, treat it as new/unexpected, not a residual.
- **Pre-send intent log is BUILT but `[dark-unarmed]` — do not treat it as closing the double-send residual until arming is verified live.** Merged `608c80a` (`apps/engine/src/send-log.ts` fsync'd WAL + `apps/engine/src/reconcile.ts` boot reconciliation + `SendUnverifiedError` intent gate, entirely engine-side, zero platform edits). `ACTIVATION.md`'s Gate-2 residual (1) line is STALE as of this pass — it still reads "BUILT 2026-07-27 (uncommitted, pending review + adversary re-gate + merge)", which predates the merge; not corrected here (out of this bookkeeping pass's file scope — flag for the next doc-touch). The founder-accepted-for-pilot double-send acceptance still stands until arming is verified in prod.
- **Glama's own listing API is stale ("~12 tools") independent of the site/server-card, which is confirmed correct at 25.** The founder's Glama dashboard "Build & Release" click (2026-07-27) did not refresh Glama's cached tool count — this needs a manual description-field edit in the Glama dashboard (drafted text delivered in-chat), not a re-build. Don't mistake this for a regression in the tool-count sweep (`ROADMAP.md ## Open` "TOOL-COUNT 24→25 CLASS SWEEP") — that sweep is confirmed correct on our own surfaces; the gap is entirely on Glama's cache side.
- **Non-blocking residual carried from I3+I4 (07-23, still open):** a crash between the `released_at` UPDATE and the revoke call can leave a single mailbox's vendor token un-revoked (skipped on retry via the `released_at IS NULL` filter) — same class as the founder-accepted Gate-2 crash residual, narrower than the original never-revoked bug. Optional hardening: revoke before the UPDATE.
- **Simulate endpoint:** `GET /checkout/simulate` 404s whenever Stripe OR engine is armed (`isRealSpendArmed`) — expected, not a bug. Adding any vendor env binding without extending that guard trips `apps/platform/test/spend-armed-env-coverage.test.ts` RED by design.
- **promotion_codes API:** pin `Stripe-Version: 2024-06-20` or POST /v1/promotion_codes rejects `coupon` (see repo `MEMORY.md`).
- **Site deploys whatever is on disk** (`npx wrangler pages deploy site --project-name=agent-cold-email --branch=main`) — check `git status` first. Deploy-ordering law: Worker before site whenever counts/claims change.
- **Gov/vendor download URLs: pin the FINAL host, never a redirect front-end** — treasury.gov's redirect front-end previously rejected the Workers TLS/fetch handshake (HTTP 525) even though a normal client follows the redirect fine; the relay's droplet-side `curl` hit a related-but-distinct quirk (HEAD 200 vs GET 302) fixed with `curl -L`; the real feed also has a trailing 0x1A DOS-EOF byte, tail-tolerant-parsed. Same class risk for any future fetch against a gov/vendor download endpoint.
- **Split typecheck/vitest discipline caught a real class this pass:** a plan-tier collapse left 2 stale string literals that vitest's runtime never exercised but `tsc` caught — always run BOTH `npm test` and `npm run typecheck` before calling a battery clean; a green test suite alone is not sufficient evidence.
- UNVERIFIED left: the SDN-relay "recovery email delivered" claim (no inbox access to check it directly — the D1 alert state is fully consistent with it) and the droplet-side `/root/sdn-relay.env` file permissions (not re-checked over SSH; the live successful ingest is strong indirect evidence). Everything else across this program's final two passes was command/API-verified: `wrangler secret list`/`deployments list`, remote D1 (`sdn_list_meta`/`sdn_entries`/`sdn_alert_state`), live Stripe Price lookup by `lookup_key`, a fresh `npm test` (817/817) and `npm run typecheck` (0 errors) run, `redocly lint`, `git show --stat`/`git merge-base`/`git worktree list`. Prior history: `archive/2026-07-22-golive-program/prior-HANDOFF.md`.

## Key files

- `ROADMAP.md ## Now / ## Open` — single source of truth for every open item + the program entry. `archive/ROADMAP-done.md` — drained done items.
- `docs/adversarial/*-2026-07-2{1,2}.md` — frozen verdicts (committed as of `c8b9ee3`) · `docs/research/*-2026-07-2{1,2}.md` — frozen designs/research.
- `archive/2026-07-22-golive-program/` — prior-HANDOFF snapshot + promo-checkout E2E evidence (screenshots + browser script).
- `apps/platform/src/engine/activation.ts` (live activation gate) · `apps/platform/src/engine/billing.ts` (`isRealSpendArmed`) · `apps/platform/src/billing/stripe-client.ts` (durable-Price bootstrap via `ensureStripePrices`/`lookup_key` — NO LONGER inline `price_data`, that shape retired by the quantity-billing build).

## Resume — KIND B: program build-complete, process founder clicks + Mordy onboarding + the InboxKit wall

Verify preconditions: `git -C ~/dev/coldstart status -sb` → main at/past `ec0f972` (as of 2026-07-28, main is **14 commits ahead of `origin/main`, 0 behind** — push is pending founder/main-loop cadence, not a divergence to fix). Live Worker version as of this pass: `3a031af4-6810-4ff4-9b22-6226064390de`; site Pages deployment `e1bfe52c-8989-4c40-8287-5fc40bd9e67a` off the same commit `ec0f972`. `curl -s https://agent-cold-email-api.yaakovscher.workers.dev/status` → `{"status":"ok"}`; `curl -s https://engine.coldrig.dev/health` → `{"status":"ok"}`. `git worktree list` should show exactly TWO entries: the main `~/dev/coldstart` checkout and the unrelated external `coldrig-design` worktree (branch `coldrig-human-interface`) — zero `.claude/worktrees/` lanes against this repo. If you see any OTHER worktree, something new started after this handoff and supersedes the "build-complete" framing below; investigate before assuming it still holds. Confirm SDN is still live: `cd apps/platform && npx wrangler d1 execute coldstart-platform-db --remote --command "SELECT COUNT(*) FROM sdn_entries;"` should read ~19,000+ (never fewer by more than the monotonicity guard's 10% tolerance), and the weekend-false-alarm fix (merged `f583fe5`/deployed `b672b403`) should mean `sdn_alert_state.failure_streak` stays near 0 even after a benign weekend re-push. `npx wrangler secret list` shows STRIPE_SECRET_KEY/STRIPE_WEBHOOK_SECRET/INBOXKIT_API_KEY/INBOXKIT_WORKSPACE_ID/SDN_INGEST_TOKEN/ENGINE_BASE_URL/ENGINE_AUTH_SECRET present, GMAIL_OAUTH_GRANTS absent (never print values). If all of that holds: **there is no build lane to resume** — both the original build phase and the 07-27/28 hardening + claims wave are complete. Process the founder click queue, the InboxKit CF-registrar wall, and Mordy-onboarding gates in `ROADMAP.md ## Now` in leverage order; do not start new autonomous build work without a fresh founder-scoped ask (post-GA follow-ons listed in `## Now` are candidates for a NEW dispatch, not automatic continuations, and the pre-send intent log needs ARMING verified live, not more building). Founder-blocked items live in `ROADMAP.md ## Now`'s click queue — never work around the classifier on them. A lane that died at a usage limit: RESUME via SendMessage to its agent name — never re-dispatch its scope. Continue autonomously under the grant; stop only at browser-consent clicks or genuinely new founder-scope decisions.
